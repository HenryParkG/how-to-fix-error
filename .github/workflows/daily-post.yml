name: Daily Error Post

on:
  schedule:
    - cron: '0 0 * * *' # 매일 UTC 0시 (한국시간 오전 9시) 실행
  workflow_dispatch: # 수동 실행 버튼

jobs:
  generate-post:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 리포지토리 쓰기 권한

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-generativeai python-dotenv

      - name: Generate Post
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # topics.txt 파일 존재 및 내용 확인
          if [ ! -s topics.txt ]; then
            echo "::warning::No topics found in topics.txt"
            exit 0
          fi
          
          # 첫 번째 줄(주제) 읽기
          TOPIC=$(head -n 1 topics.txt)
          echo "Processing topic: $TOPIC"
          
          # 포스팅 생성 스크립트 실행
          python auto_poster.py "$TOPIC"
          
          # 성공 시 해당 주제 삭제 (임시 파일 사용)
          tail -n +2 topics.txt > topics.txt.tmp && mv topics.txt.tmp topics.txt
          
          # Git 사용자 설정
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # 변경사항 커밋 및 푸시
          git add data/
          git add topics.txt
          git commit -m "📝 Auto-post: $TOPIC" || echo "No changes to commit"
          git push
