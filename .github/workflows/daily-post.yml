name: Daily Error Post

on:
  schedule:
    - cron: '0 0 * * *' # 매일 UTC 0시 (한국시간 오전 9시) 실행
  workflow_dispatch: # 수동 실행 버튼

jobs:
  generate-post:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 리포지토리 쓰기 권한

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-generativeai python-dotenv

      - name: Generate Post (Autonomous Mode)
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: |
          # 인자 없이 실행하면 AI가 알아서 주제 선정
          python auto_poster.py
          
          # Git 사용자 설정
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # 변경사항 커밋 및 푸시
          git add data/
          git commit -m "🤖 Auto-brainstormed & Post: Autonomous Daily Update" || echo "No changes to commit"
          git push
