window.onPostDataLoaded({
    "title": "Go Runtime: Scheduler Starvation & Preemption Failure",
    "slug": "go-runtime-non-cooperative-preemption-failure-goroutine-scheduler-starvation",
    "language": "Go",
    "code": "Scheduler Starvation",
    "tags": [
        "Go",
        "Concurrency",
        "Performance",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Go's concurrency model, built around goroutines and the M:P:G scheduler, aims to provide efficient multiplexing of lightweight goroutines onto OS threads. Historically, the Go scheduler relied on <strong>cooperative preemption</strong>, where goroutines would yield control at specific points like function calls, channel operations, or blocking I/O. However, a tight CPU-bound loop that made no such calls could run indefinitely, starving other goroutines on the same OS thread.</p><p>To address this, Go 1.14 introduced <strong>non-cooperative (asynchronous) preemption</strong>. This mechanism uses OS signals (e.g., SIGURG) to periodically interrupt long-running goroutines, allowing the scheduler to gain control and switch contexts. While a significant improvement, extremely tight, CPU-bound loops that perform computations entirely within registers or stack frames, making no function calls, memory allocations, or explicit yields for an extended period, can still occasionally bypass even non-cooperative preemption's stack checks.</p><p>This leads to <strong>goroutine scheduler starvation</strong>: one goroutine monopolizes an OS thread (a P in the M:P:G model), preventing other runnable goroutines (including those handling network requests or background tasks) from executing. Symptoms include unresponsive HTTP endpoints, high latency for seemingly simple operations, and CPU profiles showing a single function consuming disproportionate CPU time on a single OS thread.</p>",
    "root_cause": "A goroutine executing a tight, CPU-bound loop or computation that performs no function calls, memory allocations, or other yield points for an extended duration (typically milliseconds to seconds), thereby preventing the Go scheduler from preempting it and scheduling other goroutines on the same OS thread.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\nfunc cpuIntensiveTask() {\n\t// This loop is designed to be extremely CPU-bound without explicit yields.\n\t// Even with non-cooperative preemption (Go 1.14+), if the loop is tight\n\t// enough (no function calls, no allocations), it can starve other goroutines.\n\tfmt.Println(\"Starting CPU intensive task...\")\n\tfor i := 0; i < 1_000_000_000_000; i++ {\n\t\t// Simulate a complex, non-yielding calculation\n\t\t_ = i * 2 * 3 / 4 // Pure CPU work, no function calls or memory interaction\n\t}\n\tfmt.Println(\"CPU task finished.\")\n}\n\nfunc handler(w http.ResponseWriter, r *http.Request) {\n\tfmt.Fprintf(w, \"Hello from handler! Current time: %s\\n\", time.Now().Format(time.RFC3339))\n}\n\nfunc main() {\n\tgo cpuIntensiveTask() // This goroutine will likely starve the HTTP handler\n\n\thttp.HandleFunc(\"/\", handler)\n\tfmt.Println(\"Server starting on :8080. Try to access / while CPU task is running.\")\n\t// The HTTP server might become unresponsive if cpuIntensiveTask starves the scheduler\n\thttp.ListenAndServe(\":8080\", nil)\n}\n```",
    "solution_desc": "<p>To prevent goroutine scheduler starvation, particularly from CPU-bound tasks, the goal is to ensure that goroutines periodically yield control back to the Go scheduler:</p><ol><li><b>Explicit Yielding (`runtime.Gosched()`)</b>: The most direct fix is to insert <code>runtime.Gosched()</code> periodically within tight loops. This explicitly tells the scheduler that the current goroutine is willing to yield its turn, allowing other runnable goroutines to execute.</li><li><b>Break Down Work</b>: Decompose large, CPU-intensive tasks into smaller, manageable chunks. This naturally introduces more opportunities for the scheduler to preempt.</li><li><b>Use `context.Context`</b>: For long-running operations, incorporate `context.Context` with a `select` statement that checks `ctx.Done()`. While primarily for cancellation, this also serves as a natural yield point.</li><li><b>Increase `GOMAXPROCS`</b>: Ensure `runtime.GOMAXPROCS` is set to the number of available CPU cores. While not a direct fix for starvation on a single P, it allows the Go runtime to utilize all physical cores, preventing other Ps from being idle if a single P is blocked.</li><li><b>Offload Extreme CPU Tasks</b>: For extremely demanding computational tasks that cannot be easily broken down or yielded, consider offloading them to external services, worker queues, or even separate processes/containers.</li></ol>",
    "good_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"runtime\" // Import the runtime package\n\t\"time\"\n)\n\nfunc cpuIntensiveTaskFixed() {\n\tfmt.Println(\"Starting CPU intensive task (fixed)...\")\n\tfor i := 0; i < 1_000_000_000_000; i++ {\n\t\t_ = i * 2 * 3 / 4 // Computation\n\n\t\t// Periodically yield control to the scheduler\n\t\t// The frequency depends on the desired responsiveness and task duration.\n\t\tif i % 100_000_000 == 0 { // Yield after a reasonable number of iterations\n\t\t\tfmt.Println(\"CPU task yielding...\")\n\t\t\truntime.Gosched() // Explicitly yield to the scheduler\n\t\t}\n\t}\n\tfmt.Println(\"CPU task finished.\")\n}\n\nfunc handlerFixed(w http.ResponseWriter, r *http.Request) {\n\tfmt.Fprintf(w, \"Hello from handler (fixed)! Current time: %s\\n\", time.Now().Format(time.RFC3339))\n}\n\nfunc main() {\n\t// It's good practice to set GOMAXPROCS to the number of available CPU cores\n\truntime.GOMAXPROCS(runtime.NumCPU())\n\n\tgo cpuIntensiveTaskFixed() // This goroutine will now yield periodically\n\n\thttp.HandleFunc(\"/\", handlerFixed)\n\tfmt.Println(\"Server starting on :8081 (fixed). Try to access / while CPU task is running.\")\n\thttp.ListenAndServe(\":8081\", nil)\n}\n```",
    "verification": "<p>Verify the fix by:</p><ol><li><b>Latency Measurement</b>: Observe the response times of other goroutines (e.g., HTTP handlers). They should remain consistently low and responsive, even when the CPU-intensive task is running in the background.</li><li><b>`pprof` CPU Profiling</b>: Use Go's built-in `pprof` tool (`go tool pprof http://localhost:6060/debug/pprof/profile?seconds=30`). The CPU profile should show a more balanced distribution of CPU time across various functions and goroutines, indicating that the scheduler is effectively switching contexts. The CPU-intensive loop should no longer appear to dominate the entire CPU time of a single OS thread.</li><li><b>Scheduler Trace (`GODEBUG=scheduler=1`)</b>: Run the Go program with `GODEBUG=scheduler=1` environment variable. This will output detailed scheduler events. Look for `p id: n: ...` lines indicating context switches and preemption events. You should observe these events occurring regularly, confirming that goroutines are not being unduly delayed or starved.</li></ol>",
    "date": "2026-09-11",
    "id": 1789092457,
    "type": "error"
});