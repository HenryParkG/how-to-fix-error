window.onPostDataLoaded({
    "title": "Go: Goroutine Leaks & Scheduler Starvation",
    "slug": "go-goroutine-leaks-scheduler-starvation",
    "language": "Go",
    "code": "Goroutine Leak",
    "tags": [
        "Concurrency",
        "Performance",
        "Go",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutines are lightweight threads managed by the Go runtime, enabling highly concurrent applications. However, improper management can lead to goroutine leaks, where goroutines are spawned but never complete, consuming memory and CPU resources indefinitely. This can degrade application performance, exhaust system resources, and eventually lead to service outages.</p><p>A common scenario for leaks involves goroutines waiting on unbuffered channels or select statements without a default case, where the sender or receiver never materializes. If these goroutines are part of a request-response cycle, each request could potentially leave a lingering goroutine, leading to a steady increase in resource usage over time.</p><p>Scheduler starvation, while related, is a distinct issue. It occurs when a goroutine (or a group of goroutines) holds a CPU-bound task for too long without yielding, preventing other goroutines from getting scheduled. While Go's preemptive scheduler mitigates this, long-running, non-yielding computation loops, especially combined with external Cgo calls, can still cause issues, leading to high latency for other tasks.</p>",
    "root_cause": "Unclosed channels or channels with no corresponding sender/receiver, causing goroutines to block indefinitely. Lack of context cancellation mechanisms or timeouts to gracefully shut down long-running tasks. Excessive CPU-bound work without natural yield points.",
    "bad_code": "package main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\nfunc handleRequest(w http.ResponseWriter, r *http.Request) {\n\t// This goroutine will leak if nobody ever reads from 'done'\n\t// or if the server crashes before it sends anything.\n\tdone := make(chan struct{})\n\tgo func() {\n\t\tselect {\n\t\tcase <-done:\n\t\t\tfmt.Println(\"Goroutine finished.\")\n\t\t// No timeout, no default case, will block forever\n\t\t}\n\t}()\n\n\t// Simulate some work\n\ttime.Sleep(50 * time.Millisecond)\n\tfmt.Fprintf(w, \"Request processed, goroutine launched.\")\n\t// We never send to 'done', so the goroutine leaks.\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/\", handleRequest)\n\tfmt.Println(\"Server listening on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}",
    "solution_desc": "To prevent goroutine leaks, always ensure that goroutines have a clear exit path. This often involves using `context.Context` with timeouts or cancellation signals, or ensuring that channels are properly closed or have receivers. For CPU-bound tasks, consider periodically yielding control or breaking down large computations. For scheduler starvation, identify long-running goroutines using profiling tools (`pprof`) and break them down, or add explicit yield points if necessary (though generally, Go's scheduler handles this well).",
    "good_code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\nfunc handleRequest(w http.ResponseWriter, r *http.Request) {\n\tctx, cancel := context.WithTimeout(r.Context(), 500*time.Millisecond)\n\tdefer cancel() // Ensure resources are released\n\n\tgo func(ctx context.Context) {\n\t\tselect {\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Println(\"Goroutine finished due to context cancellation or timeout.\")\n\t\tcase <-time.After(1 * time.Second): // Example: If task takes longer than expected\n\t\t\tfmt.Println(\"Goroutine finished after its own timeout.\")\n\t\t}\n\t}(ctx)\n\n\t// Simulate some work\n\ttime.Sleep(50 * time.Millisecond)\n\tfmt.Fprintf(w, \"Request processed, cancellable goroutine launched.\")\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/\", handleRequest)\n\tfmt.Println(\"Server listening on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}",
    "verification": "Monitor the number of active goroutines using `runtime.NumGoroutine()` or `pprof`. A steadily increasing number of goroutines without corresponding decrease indicates a leak. Use Go's `pprof` tools (`go tool pprof http://localhost:8080/debug/pprof/goroutine?debug=1`) to inspect stack traces of live goroutines. For scheduler starvation, observe CPU usage patterns, latency distributions, and scheduler trace output (GODEBUG=schedtrace=1000ms). Ensure that resource usage (memory, CPU) remains stable under sustained load.",
    "date": "2026-09-04",
    "id": 1788526942,
    "type": "error"
});