window.onPostDataLoaded({
    "title": "Go: Goroutine Leaks & Scheduler Starvation",
    "slug": "go-goroutine-leaks-scheduler-starvation",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Concurrency",
        "Performance",
        "Go",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks occur when goroutines are launched but never terminate, consuming system resources (memory, CPU) indefinitely. This often happens due to unhandled blocking operations, channels that are never closed or sent to, or simply forgotten goroutines that lack a clear exit path. As leaked goroutines accumulate, they contribute to increased memory usage and put pressure on the Go runtime's scheduler. While the Go scheduler is preemptive (since Go 1.14), a high number of runnable goroutines still means more context switching, which can lead to 'scheduler starvation' for critical tasks. This manifests as increased latency, reduced throughput, and sometimes, OOM (Out Of Memory) errors, even if individual goroutines are not doing much work.</p><p>The root cause is typically a failure to manage the lifecycle of concurrently executing tasks, particularly in scenarios involving unbounded goroutine creation or asynchronous operations without proper cancellation mechanisms.</p>",
    "root_cause": "Unbounded goroutine creation, lack of context propagation/cancellation, blocking operations without timeouts, or resource exhaustion causing goroutines to wait indefinitely for an event that never occurs.",
    "bad_code": "package main\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc processRequest(id int) {\n\tfmt.Printf(\"Processing request %d...\\n\", id)\n\t// Simulate a long-running, potentially blocking operation\n\t// This goroutine has no way to be told to stop or to time out itself.\n\ttime.Sleep(5 * time.Second) \n\tfmt.Printf(\"Finished request %d.\\n\", id)\n}\n\nfunc main() {\n\tfmt.Println(\"Starting server with leaky goroutines...\")\n\tfor i := 0; i < 1000; i++ {\n\t\t// Uncontrolled goroutine creation in a loop. No mechanism to wait or cancel.\n\t\tgo processRequest(i)\n\t}\n\tfmt.Println(\"Launched 1000 goroutines. Main exiting soon, but many might still run.\")\n\t// Main goroutine exits, but the others might continue to run indefinitely or leak.\n\ttime.Sleep(10 * time.Second) // Give some time for observations\n\tfmt.Println(\"Main goroutine exiting.\")\n}",
    "solution_desc": "The architectural solution involves treating goroutines as managed resources with defined lifecycles. Always use `context.Context` for cancellation and timeouts across goroutine boundaries. Implement a worker pool pattern to bound concurrency, preventing an excessive number of goroutines from running simultaneously. Ensure all blocking operations (e.g., I/O, channel reads) are either bounded by a timeout or monitored for completion using `select` statements with `context.Done()`. For operations requiring completion, `sync.WaitGroup` should be used meticulously, ensuring `Done()` is called for every `Add()`. Design goroutines to have clear exit conditions.",
    "good_code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc processRequestCtx(ctx context.Context, id int, wg *sync.WaitGroup) {\n\tdefer wg.Done()\n\tfmt.Printf(\"Processing request %d...\\n\", id)\n\tselect {\n\tcase <-time.After(3 * time.Second): // Simulate work with a timeout\n\t\tfmt.Printf(\"Finished request %d normally.\\n\", id)\n\tcase <-ctx.Done(): // Context cancellation signal received\n\t\tfmt.Printf(\"Request %d cancelled: %v\\n\", id, ctx.Err())\n\t\treturn // Graceful exit\n\t}\n}\n\nfunc main() {\n\tfmt.Println(\"Starting server with controlled goroutines...\")\n\tvar wg sync.WaitGroup\n\t// Create a context that will cancel all child goroutines after 5 seconds.\n\tctx, cancel := context.WithTimeout(context.Background(), 5*time.Second) \n\tdefer cancel() // Ensure cancellation function is called to release context resources\n\n\tconst numGoroutines = 10 // Limiting for demonstration; a real pool would manage this\n\n\tfor i := 0; i < numGoroutines; i++ {\n\t\twg.Add(1)\n\t\tgo processRequestCtx(ctx, i, &wg)\n\t}\n\n\t// Wait for all goroutines to finish or the context to cancel.\n\twg.Wait()\n\tfmt.Println(\"All controlled goroutines finished or cancelled. Main exiting.\")\n}",
    "verification": "Monitor the number of active goroutines using `runtime.NumGoroutine()` or via `go tool pprof` (http handler `/debug/pprof/goroutine?debug=1`). A healthy application should show a stable or predictable number of goroutines, not an ever-increasing count. Observe CPU and memory usage patterns for stability, especially under sustained load. Ensure critical tasks meet their latency targets without unexpected spikes. In a test environment, actively trigger cancellation and timeout scenarios to confirm goroutines exit gracefully.",
    "date": "2026-09-01",
    "id": 1788250084,
    "type": "error"
});