window.onPostDataLoaded({
    "title": "PostgreSQL: MultiXact ID Wraparound & Autovacuum Saturation",
    "slug": "postgresql-multixact-id-wraparound-autovacuum-saturation",
    "language": "SQL, PostgreSQL",
    "code": "MultiXactWraparound",
    "tags": [
        "SQL",
        "PostgreSQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a transaction ID (XID) system to manage concurrency and MVCC. For certain operations, like row-level locking (e.g., <code>SELECT FOR UPDATE</code>) or `READ COMMITTED` snapshot management, a different identifier called a MultiXact ID is used. This ID identifies a group of transaction IDs that are simultaneously locking a row or involved in a shared snapshot. Like XIDs, MultiXact IDs are 32-bit integers and are also subject to wraparound, meaning they will cycle back to zero after approximately 2 billion increments.</p><p>When the MultiXact ID approaches its wraparound limit (controlled by <code>autovacuum_multixact_freeze_max_age</code>, typically 400 million by default), PostgreSQL initiates an 'emergency autovacuum' to prevent potential data loss or corruption. This emergency process, critical for database integrity, overrides normal autovacuum parameters and attempts to freeze multi-transaction IDs aggressively. Because it's a high-priority operation, it can consume all available I/O and CPU resources, causing severe I/O saturation across the entire database instance. This leads to a dramatic slowdown or complete outage of regular database operations, as user queries and other maintenance tasks contend for critically scarce resources with the emergency vacuum processes.</p>",
    "root_cause": "Infrequent or ineffective vacuuming, a workload with high usage of row-level locks (e.g., many <code>SELECT FOR UPDATE</code> statements), long-running transactions preventing cleanup, or misconfigured autovacuum parameters (e.g., <code>autovacuum_multixact_freeze_max_age</code> set too high, or <code>autovacuum_vacuum_cost_delay</code> too large for the workload).",
    "bad_code": "```sql\n-- Workload/configuration leading to MultiXact ID Wraparound issues\n\n-- Example of a high-churn workload that uses row-level locking extensively\n-- If this runs frequently and on many rows without sufficient vacuuming:\nBEGIN;\n    SELECT * FROM my_table WHERE status = 'pending' FOR UPDATE;\n    -- Process rows, update, commit\nCOMMIT;\n\n-- Example of inadequate autovacuum configuration (often default values on busy systems)\n-- (These are typically found in postgresql.conf)\n-- autovacuum = on\n-- autovacuum_max_workers = 3\n-- autovacuum_vacuum_cost_delay = 10ms  -- Could be too high for high churn\n-- autovacuum_vacuum_cost_limit = 200\n-- autovacuum_freeze_max_age = 200000000 -- Primary XID freeze age, not MultiXact ID\n-- autovacuum_multixact_freeze_max_age = 400000000 -- Default, might be hit frequently\n-- autovacuum_vacuum_multixact_age_table_freeze = 400000000 -- Max age for table freeze\n\n-- Or, long-running transactions that prevent freezing\nBEGIN ISOLATION LEVEL SERIALIZABLE;\n    -- Long-running complex query or user interaction\n    SELECT expensive_aggregation FROM another_table;\n    PERFORM pg_sleep(300); -- 5-minute wait\nCOMMIT;\n```",
    "solution_desc": "Proactive monitoring of MultiXact ID age is crucial. Regularly check `pg_stat_database.datfrozenxid_multixact` and `pg_class.relminmxid` to identify tables approaching the threshold. Tune autovacuum parameters: specifically lower `autovacuum_multixact_freeze_max_age` to trigger proactive vacuums sooner (e.g., 200M or 100M). Adjust `autovacuum_vacuum_cost_delay` and `autovacuum_vacuum_cost_limit` to allow autovacuum to work more aggressively, but without completely starving foreground operations. Identify and optimize long-running transactions that prevent MultiXact IDs from being frozen. If in an emergency, consider temporarily increasing `autovacuum_max_workers` and reducing `autovacuum_vacuum_cost_delay` for specific tables to accelerate the emergency vacuum, then revert to safer settings.",
    "good_code": "```sql\n-- 1. Monitor MultiXact ID age for databases\nSELECT \n    datname,\n    datfrozenxid_multixact AS current_multixact_id_age,\n    (2^31 - 1) - datfrozenxid_multixact AS remaining_multixact_ids,\n    (setting::bigint) AS autovacuum_multixact_freeze_max_age,\n    CASE\n        WHEN (2^31 - 1) - datfrozenxid_multixact < (setting::bigint) THEN 'CRITICAL: Approaching wraparound!'\n        WHEN (2^31 - 1) - datfrozenxid_multixact < (setting::bigint * 1.5) THEN 'WARNING: Approaching threshold'\n        ELSE 'OK'\n    END AS status\nFROM pg_stat_database\nJOIN pg_settings ON pg_settings.name = 'autovacuum_multixact_freeze_max_age'\nWHERE datname = current_database();\n\n-- 2. Tune autovacuum settings (apply via ALTER SYSTEM or in postgresql.conf)\n-- These values are examples and should be adjusted based on workload and hardware\nALTER SYSTEM SET autovacuum_multixact_freeze_max_age = '200000000'; -- Trigger vacuum sooner\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '2ms';            -- Vacuum more aggressively\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = '1000';           -- Allow more I/O per cycle\n-- You might also consider increasing autovacuum_max_workers if your system has spare CPU/IO\n-- ALTER SYSTEM SET autovacuum_max_workers = '5';\n\n-- Reload config for changes to take effect\nSELECT pg_reload_conf();\n\n-- 3. To address specific tables approaching mxid wraparound immediately (emergency fix)\n-- Identify tables with high relminmxid\nSELECT relname, relminmxid FROM pg_class WHERE relkind = 'r' ORDER BY relminmxid DESC LIMIT 10;\n\n-- Force an aggressive vacuum on a specific table\nVACUUM (FREEZE, VERBOSE, ANALYZE) my_problem_table;\n```",
    "verification": "Monitor `pg_stat_database.datfrozenxid_multixact` and `pg_stat_activity` for running vacuum processes. Look for the MultiXact ID age to stabilize or decrease after implementing fixes. Use OS-level monitoring tools (e.g., `iostat`, `vmstat`, `top`) to check for I/O saturation and CPU usage. After tuning, ensure the database performance metrics (latency, throughput) return to normal levels without frequent spikes caused by emergency autovacuums. Regularly review the PostgreSQL logs for 'database is going to shut down' or 'MultiXact ID wrap around' warnings.",
    "date": "2026-09-06",
    "id": 1788697742,
    "type": "error"
});