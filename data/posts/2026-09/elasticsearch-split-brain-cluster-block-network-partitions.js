window.onPostDataLoaded({
    "title": "Elasticsearch: Split-Brain & Cluster Block Due to Partitions",
    "slug": "elasticsearch-split-brain-cluster-block-network-partitions",
    "language": "Elasticsearch",
    "code": "SplitBrain",
    "tags": [
        "Elasticsearch",
        "Infra",
        "Clustering",
        "Networking",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch clusters are designed for high availability and scalability, relying on a distributed architecture with multiple nodes. A crucial aspect of this architecture is the election of a master node, which is responsible for cluster-wide operations like creating/deleting indices, managing nodes, and allocating shards. When a network partition occurs, a group of nodes might lose connectivity to other nodes in the cluster. This can lead to two critical problems:</p><ul><li><b>Split-Brain:</b> If the `minimum_master_nodes` setting (now `cluster.initial_master_nodes` and `discovery.seed_hosts` for modern ES) is not correctly configured or strictly adhered to, a network partition can cause two or more segments of the cluster to independently elect their own master nodes. Each 'brain' believes it is the true master, leading to data inconsistencies (e.g., updates on one master not reflecting on another, conflicting metadata, data loss).</li><li><b>Cluster Block:</b> If the remaining nodes in a partition cannot form a quorum (i.e., they don't meet the `minimum_master_nodes` requirement), they will be unable to elect a master. Without a master, the cluster becomes blocked, unable to perform any write operations (and often read operations too), rendering it effectively unavailable. This state is often indicated by `CLUSTER_BLOCKED` status or nodes being in a `red` or `yellow` state.</li></ul>",
    "root_cause": "The primary root cause is network instability or misconfiguration, leading to isolated groups of nodes. Specific triggers include: 1) Incorrect `minimum_master_nodes` setting (e.g., setting it to 1 in a multi-node cluster, or not using the formula `(N/2) + 1` where N is the number of master-eligible nodes). 2) Improper `discovery.seed_hosts` or `cluster.initial_master_nodes` preventing nodes from finding each other correctly. 3) Transient network outages that segment the cluster. 4) Resource saturation (CPU, memory, disk I/O) making nodes unresponsive, causing them to be perceived as down by other nodes.",
    "bad_code": "```yaml\n# elasticsearch.yml snippet - Bad Configuration Example (ES 7+)\n\n# Assuming a 3-node cluster: node1, node2, node3\n\n# Issue 1: Not all master-eligible nodes are in seed hosts (node3 missing in node1/node2 config)\n# This can prevent forming a quorum if node1/node2 lose connection to node3 but not each other\n# Or if node1/node2 start up and node3 is down, they might elect master even if not majority\ndiscovery.seed_hosts: [\"node1:9300\", \"node2:9300\"]\n\n# Issue 2: initial_master_nodes not set on first startup for all master-eligible nodes\n# Or initial_master_nodes only contains one node, making it too easy for split brain\ncluster.initial_master_nodes: [\"node1\"]\n\n# For older ES (pre-7.x), minimum_master_nodes set to 1 in a 3-node cluster is problematic\n# discovery.zen.minimum_master_nodes: 1 # BAD for 3+ node clusters\n\n```",
    "solution_desc": "To prevent split-brain and cluster blocks: 1) **Configure Quorum Correctly:** For Elasticsearch 7.x+, ensure `cluster.initial_master_nodes` lists *all* master-eligible nodes on the *first* startup. Post-startup, `minimum_master_nodes` logic is handled internally. For older versions, set `discovery.zen.minimum_master_nodes` to `(N/2) + 1`, where N is the number of master-eligible nodes. 2) **Robust Network Infrastructure:** Ensure a stable, low-latency network between cluster nodes. Use dedicated network interfaces for inter-node communication if possible. 3) **Dedicated Master Nodes:** In larger clusters, designate specific master-eligible nodes (`node.master: true`, `node.data: false`, `node.ingest: false`) to improve stability and prevent resource contention with data operations. 4) **Accurate `discovery.seed_hosts`:** List all master-eligible nodes (or a subset sufficient to find others) in `discovery.seed_hosts` for all nodes. 5) **Monitor Cluster Health:** Actively monitor cluster health (`_cluster/health`), node logs, and network metrics to detect partitions or master election issues early. 6) **Avoid Over-provisioning:** Ensure nodes have sufficient resources (CPU, RAM, disk I/O) to prevent unresponsiveness that can mimic network issues.",
    "good_code": "```yaml\n# elasticsearch.yml snippet - Good Configuration Example (ES 7+)\n\n# Assuming a 3-node cluster (node1, node2, node3), all master-eligible\n\n# 1. List all master-eligible nodes for discovery\ndiscovery.seed_hosts: [\"node1:9300\", \"node2:9300\", \"node3:9300\"]\n\n# 2. On initial startup, list ALL master-eligible nodes\n# This tells the nodes which other nodes they should form a cluster with.\n# This setting is only used once; remove or comment out after the cluster forms.\ncluster.initial_master_nodes: [\"node1\", \"node2\", \"node3\"]\n\n# Recommended: Use dedicated master-eligible nodes in production for larger clusters\n# node.roles: [ master ] # on node1, node2, node3 if they are dedicated masters\n# node.roles: [ data, ingest ] # on other nodes if they are data/ingest only\n```",
    "verification": "1. **Check Cluster Health:** Use `GET _cluster/health` to confirm the cluster is `green` and all expected nodes are present. 2. **Monitor Logs:** Look for 'master elected' or 'master not discovered' messages in node logs. Frequent re-elections are a red flag. 3. **Simulate Partition (Carefully):** In a test environment, deliberately cut network connectivity between groups of nodes (e.g., using `iptables` or network virtualization) and observe cluster behavior. Ensure that only one master is elected in the largest partition, and smaller partitions correctly block. 4. **API Testing:** Perform write operations during simulated stress or node failures to ensure data consistency and availability. 5. **Quorum Enforcement:** Verify that if more than `(N/2)` master-eligible nodes are isolated, they form a functional cluster, and the remaining nodes block.",
    "date": "2026-09-04",
    "id": 1788487410,
    "type": "error"
});