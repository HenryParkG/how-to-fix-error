window.onPostDataLoaded({
    "title": "PostgreSQL: TXID Wraparound & Autovacuum Thrashing",
    "slug": "postgresql-txid-wraparound-autovacuum-thrashing",
    "language": "PostgreSQL",
    "code": "TXID Wraparound",
    "tags": [
        "Database",
        "Maintenance",
        "SQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL assigns a unique 32-bit transaction ID (XID) to every transaction. These XIDs are used for MVCC (Multi-Version Concurrency Control), allowing concurrent reads and writes without locking. The XID is finite and, after approximately 2 billion transactions, it will \"wraparound\" to 0. If this happens without older transaction IDs being properly frozen (marked as visible to all past transactions), PostgreSQL will interpret active transactions as being in the distant past, leading to data corruption and a forced shutdown to prevent data loss. This critical state is known as Transaction ID (XID) Wraparound.</p><p>To prevent XID wraparound, PostgreSQL employs an `autovacuum` daemon. `autovacuum` is responsible for cleaning up dead tuples (rows marked for deletion but not yet removed) and freezing old transaction IDs. When `autovacuum` struggles to keep up with transaction volume, or when specific tables frequently generate many dead tuples, it can enter a state of \"autovacuum thrashing.\" This means `autovacuum` runs too frequently, consuming significant I/O and CPU resources, yet still fails to catch up, leading to performance degradation and increasing the risk of XID wraparound.</p><p>Autovacuum thrashing often stems from poorly tuned autovacuum parameters, highly active tables with many updates/deletes, long-running transactions preventing cleanup, or tables with a high fillfactor, which requires more frequent vacuums.</p>",
    "root_cause": "High transaction rate without sufficient autovacuuming, long-running transactions preventing XID cleanup, large tables with frequent updates/delates, inefficient autovacuum configuration (`autovacuum_vacuum_scale_factor`, `autovacuum_analyze_scale_factor`, `autovacuum_vacuum_cost_delay`, `autovacuum_vacuum_cost_limit`). Data skew or tables with many dead tuples.",
    "bad_code": "-- This isn't \"bad code\", but it's a query that shows symptoms of neglect\n-- and potentially leads to the problem.\n-- High 'age' on a table indicates it's approaching TXID wraparound.\nSELECT\n    relname,\n    age(relfrozenxid) AS xid_age,\n    pg_size_pretty(pg_relation_size(oid)) AS table_size,\n    last_autovacuum,\n    last_autoanalyze\nFROM pg_class\nWHERE relkind = 'r'\nORDER BY xid_age DESC\nLIMIT 10;\n\n-- Long-running transactions blocking autovacuum\nSELECT pid, usename, client_addr, application_name, backend_start, state,\n           state_change, query_start, query\nFROM pg_stat_activity\nWHERE state <> 'idle'\n      AND xact_start IS NOT NULL\n      AND age(backend_xid) > 1000000 -- Example: looking for old transaction IDs\nORDER BY xact_start;",
    "solution_desc": "Prevent XID wraparound and autovacuum thrashing through proactive monitoring and intelligent configuration.\n1.  **Monitor XID Age**: Regularly check `age(relfrozenxid)` on all tables. Set alerts when it approaches `autovacuum_freeze_max_age` or `vacuum_freeze_table_age`.\n2.  **Tune Autovacuum**:\n    *   **`autovacuum_vacuum_cost_delay`**: Reduce it (e.g., to 1-10ms) on busy systems to allow autovacuum to run more aggressively.\n    *   **`autovacuum_vacuum_cost_limit`**: Increase if disk I/O allows, to complete vacuuming faster.\n    *   **`autovacuum_vacuum_scale_factor`**: Reduce for highly active tables (e.g., 0.05 instead of 0.2) to trigger vacuum sooner.\n    *   **`autovacuum_analyze_scale_factor`**: Similar to above for analysis.\n    *   Consider per-table autovacuum settings for specific problematic tables.\n3.  **Identify Long-Running Transactions**: Use `pg_stat_activity` to find and terminate transactions that prevent XID cleanup. Implement application-level safeguards against long-running transactions.\n4.  **Optimize Queries and Schema**: Reduce unnecessary updates/deletes. Lower `fillfactor` on frequently updated tables can reduce the need for vacuuming but increases storage size.\n5.  **Manual VACUUM FREEZE**: As a last resort, if XID age is critically high, perform `VACUUM FREEZE` on problematic tables during a maintenance window.",
    "good_code": "-- Example of global autovacuum tuning (restart required for some)\n-- In postgresql.conf:\n-- autovacuum = on\n-- autovacuum_max_workers = 3           # Default is 3, increase for busy systems\n-- autovacuum_naptime = 1min            # How often autovacuum checks for work\n-- autovacuum_vacuum_cost_delay = 10ms  # Reduce from default 20ms for faster processing\n-- autovacuum_vacuum_cost_limit = 10000 # Increase from default 200 for more aggressive work\n-- vacuum_freeze_table_age = 1500000000 # Default is 150M, consider reducing for safety\n\n-- Example of per-table autovacuum tuning\nALTER TABLE your_large_and_active_table SET (\n    autovacuum_vacuum_scale_factor = 0.05,\n    autovacuum_vacuum_cost_delay = 5ms\n);\n\n-- Monitoring script for XID age (can be run periodically)\nSELECT\n    c.relname,\n    c.relkind,\n    pg_class.relpages,\n    pg_class.reltuples,\n    pg_database.datfrozenxid,\n    pg_class.relfrozenxid,\n    age(pg_class.relfrozenxid) AS xid_age,\n    (SELECT setting FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') AS freeze_max_age,\n    CASE\n        WHEN age(pg_class.relfrozenxid) > (SELECT setting::numeric * 0.9 FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') THEN 'CRITICAL'\n        WHEN age(pg_class.relfrozenxid) > (SELECT setting::numeric * 0.7 FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') THEN 'WARNING'\n        ELSE 'OK'\n    END AS xid_status,\n    last_autovacuum,\n    last_autoanalyze\nFROM pg_class c\nJOIN pg_namespace n ON n.oid = c.relnamespace\nLEFT JOIN pg_stat_user_tables ON pg_stat_user_tables.relid = c.oid\nLEFT JOIN pg_database ON pg_database.datname = current_database()\nWHERE c.relkind IN ('r', 'm')\n  AND n.nspname NOT IN ('pg_catalog', 'information_schema')\nORDER BY xid_age DESC;\n\n-- Find bloat on tables\n-- (Requires pg_repack or similar for online vacuum full)\nSELECT * FROM pg_stat_user_tables ORDER BY n_dead_tup DESC LIMIT 10;",
    "verification": "Continuously monitor `age(relfrozenxid)` for all tables. Ensure no table consistently approaches critical XID age thresholds. Observe `autovacuum` activity in `pg_stat_activity` and logs (if `log_autovacuum_min_duration` is set). Check system metrics for I/O and CPU usage to ensure autovacuum isn't thrashing or causing bottlenecks. Monitor for `ERROR: database is shut down to prevent data loss` messages in logs, which indicates an actual wraparound. Regular health checks should include XID age monitoring.",
    "date": "2026-09-04",
    "id": 1788526944,
    "type": "error"
});