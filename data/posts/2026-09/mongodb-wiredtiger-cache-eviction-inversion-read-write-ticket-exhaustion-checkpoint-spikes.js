window.onPostDataLoaded({
    "title": "MongoDB WiredTiger: Cache & Ticket Exhaustion",
    "slug": "mongodb-wiredtiger-cache-eviction-inversion-read-write-ticket-exhaustion-checkpoint-spikes",
    "language": "Backend",
    "code": "WiredTigerPerf",
    "tags": [
        "MongoDB",
        "SQL",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses a sophisticated cache to manage data, balancing memory and disk I/O. However, under specific workload patterns, issues like 'cache eviction inversion' can occur. This happens when the cache's eviction algorithm, designed to remove least recently used (LRU) data, inadvertently evicts frequently accessed 'hot' data in favor of less frequently accessed 'cold' data. This is often triggered by large analytical scans or batch operations that touch vast amounts of data once, polluting the cache and pushing out the working set, leading to increased disk I/O for subsequent operations.</p><p>Concurrently, WiredTiger employs a ticket-based concurrency control system, limiting the number of in-flight read and write operations. When the available 'readTickets' or 'writeTickets' are exhausted, new operations must wait in a queue. This 'ticket exhaustion' directly translates to increased latency and reduced throughput for your application, as requests block until a ticket becomes available.</p><p>These two issues often combine with 'checkpoint spikes'. Checkpoints are periodic operations where MongoDB flushes all dirty data from the WiredTiger cache to disk to ensure data durability. If the cache is under extreme pressure (due to eviction inversion) and tickets are scarce (due to exhaustion), a checkpoint operation can become exceptionally long and resource-intensive, consuming many tickets and blocking other operations, leading to dramatic latency spikes across the entire database.</p>",
    "root_cause": "Imbalanced workload patterns causing cache eviction inversion, combined with insufficient read/write tickets for the current concurrency, which together lead to system-wide latency, reduced throughput, and amplified checkpoint costs.",
    "bad_code": "/* Example mongod.conf with default/unoptimized settings */\nsystemLog:\n  destination: file\n  path: /var/log/mongodb/mongod.log\n  logAppend: true\nstorage:\n  dbPath: /var/lib/mongo\n  engine: wiredTiger\n  wiredTiger:\n    engineConfig:\n      # cacheSizeGB is often left at default (50% of RAM up to 64GB)\n      # but might be insufficient for hot data or too large for mixed workloads.\n      cacheSizeGB: 0 # Default calculation\n    sessionConfig:\n      # Default tickets are 128 for reads and writes\n      readTickets: 128\n      writeTickets: 128\n\n/* Application code performing frequent, wide-range queries and updates */\ndb.collection.find({ timestamp: { $gte: ISODate(\"...\"), $lt: ISODate(\"...\") } }).sort({ _id: 1 })\ndb.collection.updateMany({ status: \"pending\" }, { $set: { status: \"processing\" } })\n",
    "solution_desc": "To address these issues, careful configuration and workload optimization are key. Ensure <code>wiredTiger.engineConfig.cacheSizeGB</code> is appropriately sized: large enough to hold your working set, but not so large that it competes excessively with the OS for memory. For read/write ticket exhaustion, adjust <code>wiredTiger.sessionConfig.readTickets</code> and <code>writeTickets</code> upwards, monitoring their utilization to find an optimal balance. Review and optimize query patterns to reduce full collection scans and ensure proper indexing. For analytical workloads that pollute the cache, consider using secondary replica sets or a separate analytical store. Regularly monitor <code>db.serverStatus()</code> metrics for WiredTiger cache behavior and concurrent transactions to identify bottlenecks. Ensure underlying storage (IOPS) can keep up with demand.",
    "good_code": "/* mongod.conf with optimized WiredTiger settings */\nsystemLog:\n  destination: file\n  path: /var/log/mongodb/mongod.log\n  logAppend: true\nstorage:\n  dbPath: /var/lib/mongo\n  engine: wiredTiger\n  wiredTiger:\n    engineConfig:\n      # Example: Dedicate 75% of 64GB RAM to WiredTiger cache\n      cacheSizeGB: 48\n      statisticsLogDelaySecs: 0\n    sessionConfig:\n      # Increase tickets for higher concurrency, based on workload profiling\n      readTickets: 512\n      writeTickets: 256\n    # Optional: adjust checkpoint frequency for specific workloads\n    # checkpoint:\n    #   waitForCheckpoint: true\n    #   checkpointIntervalSeconds: 60\n\n/* Application code with optimized queries and indexing */\n// Create appropriate index for efficient querying\ndb.collection.createIndex({ timestamp: 1 })\n\n// Use targeted queries with index\ndb.collection.find({ timestamp: { $gte: ISODate(\"...\"), $lt: ISODate(\"...\") } }).sort({ _id: 1 }).limit(100)\n",
    "verification": "Verify the fix by monitoring key MongoDB metrics via <code>db.serverStatus()</code>. Pay close attention to <code>wiredTiger.cache</code> statistics (e.g., 'bytes currently in the cache', 'pages read into cache', 'pages evicted from cache') to ensure cache hit ratios improve and eviction inversion is reduced. Observe <code>wiredTiger.concurrentTransactions</code> to see if 'available tickets' are consistently low or 'read/write tickets granted' are high, indicating that the increased ticket limits are being utilized effectively. Monitor system-level I/O operations and CPU usage. Crucially, track checkpoint times in MongoDB logs (look for 'storage.syncer.fsync.write_bytes_total' and 'storage.checkpoint.data_size') and overall application latency/throughput to confirm performance improvements.",
    "date": "2026-09-07",
    "id": 1788746213,
    "type": "error"
});