window.onPostDataLoaded({
    "title": "Istio: Envoy Policy Mismatches & mTLS Failures",
    "slug": "istio-envoy-policy-mismatches-mtls-failures",
    "language": "Istio, Envoy, Kubernetes",
    "code": "mTLS Failure",
    "tags": [
        "Istio",
        "Envoy",
        "Kubernetes",
        "mTLS",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Istio's control plane uses Envoy sidecars to inject powerful traffic management, security, and observability features into your Kubernetes applications. A core security feature is mutual TLS (mTLS), which encrypts and authenticates communication between services within the mesh. When mTLS handshake failures occur, it typically means Envoy proxies are unable to establish a secure, authenticated connection, leading to HTTP 503 errors (service unavailable) or connection resets.</p><p>These failures often stem from subtle mismatches in Istio's security policies: <code>PeerAuthentication</code>, <code>DestinationRule</code>, and <code>AuthorizationPolicy</code>. <code>PeerAuthentication</code> dictates whether a workload requires mTLS (<code>STRICT</code>) or tolerates both mTLS and plain text (<code>PERMISSIVE</code>). <code>DestinationRule</code> defines traffic policies for services, including the TLS mode (e.g., <code>ISTIO_MUTUAL</code> for mTLS). Lastly, <code>AuthorizationPolicy</code> defines access control rules based on identity (derived from mTLS certificates).</p><p>A common scenario is applying a <code>STRICT</code> <code>PeerAuthentication</code> policy to a namespace or service before all clients communicating with it are also configured for mTLS, or before the services themselves are ready to serve mTLS traffic. Another pitfall is a misconfigured <code>DestinationRule</code> for the client-side proxy, preventing it from initiating mTLS. These misconfigurations lead to the Envoy sidecar rejecting connections, as the required security posture isn't met.</p>",
    "root_cause": "The primary root cause for mTLS handshake failures in Istio is a mismatch between the expected mTLS mode enforced by a <code>PeerAuthentication</code> policy and the actual mTLS configuration or capability of the communicating services/clients, often exacerbated by incorrect <code>DestinationRule</code> settings for client-side Envoy proxies. Specifically: 1. A service requiring <code>STRICT</code> mTLS, but a client attempting plain text or improperly configured mTLS. 2. A <code>DestinationRule</code> missing <code>host</code> or <code>trafficPolicy.tls.mode: ISTIO_MUTUAL</code> for the target service. 3. Certificate rotation issues or misconfigured trust domains.",
    "bad_code": "```yaml\n# Scenario 1: PeerAuthentication too strict, too soon\napiVersion: security.istio.io/v1beta1\nkind: PeerAuthentication\nmetadata:\n  name: default-strict-mtls\n  namespace: default\nspec:\n  mtls:\n    mode: STRICT\n---\n# Scenario 2: Missing DestinationRule TLS configuration for client-side mTLS initiation\napiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: my-service\n  namespace: default\nspec:\n  host: my-service.default.svc.cluster.local\n  # trafficPolicy is missing or not configured for mTLS, \n  # causing client Envoy to not initiate mTLS even if server expects it.\n  # trafficPolicy:\n  #   tls:\n  #     mode: ISTIO_MUTUAL\n```",
    "solution_desc": "Systematically debug Istio mTLS issues by checking the security posture at each layer. Start by verifying <code>PeerAuthentication</code> policies: if you're experiencing failures, temporarily set it to <code>PERMISSIVE</code> for the problematic namespace/service to confirm mTLS is the culprit. Once confirmed, ensure all clients sending traffic to that service have an appropriate <code>DestinationRule</code> applied with <code>trafficPolicy.tls.mode: ISTIO_MUTUAL</code> for the target host. Use <code>istioctl analyze</code> to detect common misconfigurations. Inspect Envoy's runtime configuration using <code>istioctl proxy-config listeners <pod-name> -o json</code> and <code>istioctl proxy-config clusters <pod-name> -o json</code> to verify that the Envoy sidecars are configured to expect/initiate mTLS. Check <code>kubectl logs <pod-name> -c istio-proxy</code> for specific error messages during handshake.",
    "good_code": "```yaml\n# Correct PeerAuthentication (start with PERMISSIVE, then move to STRICT once stable)\napiVersion: security.istio.io/v1beta1\nkind: PeerAuthentication\nmetadata:\n  name: my-service-mtls\n  namespace: default\nspec:\n  selector:\n    matchLabels:\n      app: my-service\n  mtls:\n    mode: PERMISSIVE # Start with PERMISSIVE to allow smooth transition\n\n---\n# Correct DestinationRule for client-side mTLS initiation\napiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: my-service\n  namespace: default\nspec:\n  host: my-service.default.svc.cluster.local\n  trafficPolicy:\n    tls:\n      mode: ISTIO_MUTUAL # Instruct client Envoy to use mTLS\n```",
    "verification": "1. Use <code>istioctl proxy-config listeners <client-pod> -o json</code> and check for an <code>INBOUND</code> listener on the target port with <code>TLS_INSPECTION</code> and <code>MUTUAL_TLS</code> config. 2. Use <code>istioctl proxy-config clusters <client-pod> -o json</code> and verify the cluster for the target service has <code>transport_socket</code> configured for <code>TLS</code> with <code>ISTIO_MUTUAL</code>. 3. Make a request from a client to the service and check <code>kubectl logs <client-pod> -c istio-proxy</code> and <code>kubectl logs <server-pod> -c istio-proxy</code> for successful mTLS handshake messages. 4. Use <code>curl -v</code> from within a client pod to the service; successful mTLS often shows certificate exchange details. 5. Monitor Istio telemetry (e.g., Kiali) for traffic flow and security metrics, ensuring mTLS is reported as active and successful.",
    "date": "2026-09-19",
    "id": 1789784270,
    "type": "error"
});