window.onPostDataLoaded({
    "title": "PostgreSQL: Transaction ID Wraparound & Database Freezing",
    "slug": "postgresql-txid-wraparound-freezing",
    "language": "SQL, PostgreSQL",
    "code": "PG_TXID_WRAPAROUND",
    "tags": [
        "PostgreSQL",
        "SQL",
        "Database",
        "Data",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a Multi-Version Concurrency Control (MVCC) architecture, where each transaction is assigned a unique Transaction ID (XID). When a row is modified, a new version of the row is created, and the old version remains accessible to transactions that started before the modification. This allows for concurrent reads and writes without locking. However, XIDs are 32-bit integers, meaning they eventually 'wrap around' after approximately 2 billion transactions. To prevent data corruption where old transaction IDs appear to be in the future (causing data visibility issues), PostgreSQL employs a mechanism called 'freezing'.</p><p>The `VACUUM` process, particularly `autovacuum`, is responsible for marking old transaction IDs as 'frozen'. Frozen tuples are visible to all transactions, regardless of their XID, effectively making them immortal. If `autovacuum` falls behind and the database's `datfrozenxid` (the oldest transaction ID that is still 'unfrozen' across the entire database) approaches the wraparound threshold (2 billion transactions or `autovacuum_freeze_max_age`), PostgreSQL will enter a critical state. It will start issuing warnings, then escalate to read-only mode, and eventually halt new transactions entirely to prevent data loss. This 'database freezing' or 'transaction ID wraparound' scenario is a catastrophic event for any production system, rendering the database unusable until the issue is resolved.</p>",
    "root_cause": "The root cause of Transaction ID wraparound and subsequent database freezing is insufficient or ineffective `VACUUM` operations, specifically the `autovacuum` daemon failing to keep up with the rate of new transaction IDs. This can be triggered by several factors: 1. **High Write/Update Workloads:** Rapid creation, update, and deletion of rows generate many XIDs and require frequent vacuuming. 2. **Long-Running Transactions:** Transactions that stay open for extended periods prevent `VACUUM` from clearing old XIDs effectively, as they need to maintain visibility of old row versions. 3. **Misconfigured `autovacuum`:** Settings like `autovacuum_vacuum_scale_factor`, `autovacuum_vacuum_cost_delay`, or `autovacuum_freeze_max_age` being too conservative can cause `autovacuum` to be too slow or not trigger often enough. 4. **Table Bloat:** Extreme table bloat can make `VACUUM` operations very expensive and time-consuming, preventing them from completing within the required window. 5. **Indexes on Volatile Columns:** Frequent updates to indexed columns can also contribute to the problem.",
    "bad_code": "```sql\n-- This isn't 'bad code' in the traditional sense, but commands to diagnose a potential issue.\n-- If the 'age' values are consistently high and approaching 2 billion (or autovacuum_freeze_max_age),\n-- you are heading towards a wraparound crisis.\n\n-- Check the age of the oldest unfrozen transaction ID for each database:\nSELECT datname, age(datfrozenxid) FROM pg_database ORDER BY age DESC;\n\n-- Check the age for specific tables in the 'public' schema:\nSELECT c.relname, age(c.relfrozenxid) \nFROM pg_class c \nJOIN pg_namespace n ON c.relnamespace = n.oid \nWHERE n.nspname = 'public' AND c.relkind IN ('r', 'matview') -- Regular tables and materialized views\nORDER BY age(c.relfrozenxid) DESC;\n\n-- Identify long-running transactions that might be blocking VACUUM:\nSELECT pid, usename, client_addr, backend_start, query_start, state, query \nFROM pg_stat_activity \nWHERE state IN ('active', 'idle in transaction') AND xact_start IS NOT NULL \nORDER BY xact_start ASC;\n```",
    "solution_desc": "To prevent and mitigate PostgreSQL Transaction ID wraparound, proactive monitoring and `autovacuum` tuning are paramount. First, regularly monitor the `age(datfrozenxid)` for databases and `age(relfrozenxid)` for tables; set alerts when these values approach critical thresholds. Identify and resolve any long-running transactions that prevent `VACUUM` from completing. Tune `autovacuum` parameters globally or per-table to be more aggressive, especially on high-churn tables. Increase `autovacuum_vacuum_scale_factor` to trigger more often, decrease `autovacuum_vacuum_cost_delay` to make it faster, and potentially increase `autovacuum_max_workers`. In an emergency, or for specific tables, manually run `VACUUM FREEZE VERBOSE ANALYZE some_table;` to aggressively freeze tuples. For extreme cases, `VACUUM FULL` can reclaim space and reset the `relfrozenxid`, but it requires an exclusive lock and rewrites the entire table. Consider partitioning large, high-write tables to reduce the scope of `VACUUM` operations. Finally, ensure sufficient I/O resources for `autovacuum` to perform its duties efficiently.",
    "good_code": "```sql\n-- 1. Tune autovacuum settings (globally in postgresql.conf or per table):\n-- Make autovacuum more aggressive on high-write tables (e.g., 5% changes trigger vacuum)\nALTER TABLE my_high_churn_table SET (autovacuum_vacuum_scale_factor = 0.05);\n-- Reduce the delay between vacuum passes (less pause for I/O, more continuous vacuuming)\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = 10; -- Milliseconds (default is 20ms)\n-- Increase number of autovacuum workers if resources allow\nALTER SYSTEM SET autovacuum_max_workers = 5; -- (default is 3)\n\n-- Apply changes (requires reload for ALTER SYSTEM changes, or restart for postgresql.conf changes)\nSELECT pg_reload_conf();\n\n-- 2. Terminate long-running idle transactions (use with caution!):\n-- Identify PIDs of transactions older than, say, 1 hour, that are idle in transaction.\nSELECT pid, usename, application_name, query_start, xact_start, state, query\nFROM pg_stat_activity\nWHERE state = 'idle in transaction' AND xact_start < NOW() - INTERVAL '1 hour';\n-- To terminate a specific PID:\n-- SELECT pg_terminate_backend(12345); -- Replace 12345 with actual PID\n\n-- 3. Manually run VACUUM FREEZE on critical tables (emergency measure):\nVACUUM FREEZE VERBOSE ANALYZE my_critical_table;\n\n-- 4. To initiate a full database-wide autovacuum run (requires superuser):\n-- SELECT pg_wal_replay_resume(); -- If database was in recovery/frozen state\n-- VACUUM FREEZE; -- On specific tables/databases or the entire cluster if critical (be careful).\n-- Ensure no long-running transactions before attempting cluster-wide VACUUM FREEZE.\n```",
    "verification": "After implementing changes, verify by continually monitoring `age(datfrozenxid)` and `age(relfrozenxid)` values using the diagnostic queries. You should see these values stabilize and, ideally, decrease below critical thresholds. Regularly check `pg_stat_activity` for long-running transactions and ensure they are being addressed. Monitor PostgreSQL logs for `autovacuum` activity and any warnings related to transaction ID wraparound. Crucially, test the system under load to confirm that `autovacuum` can keep pace with transaction generation without impacting application performance. Set up alerts for high transaction ID ages in your monitoring system to ensure early detection of any regression.",
    "date": "2026-09-08",
    "id": 1788873188,
    "type": "error"
});