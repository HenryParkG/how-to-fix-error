window.onPostDataLoaded({
    "title": "WebGPU: Async Buffer Mapping Races & Device Loss",
    "slug": "webgpu-async-buffer-mapping-races-cascading-device-loss",
    "language": "WebGPU, TypeScript",
    "code": "WebGPU Device Loss",
    "tags": [
        "WebGPU",
        "Graphics",
        "TypeScript",
        "Frontend",
        "Error Fix"
    ],
    "analysis": "<p>WebGPU introduces a modern, low-level API for GPU programming on the web, prioritizing asynchronous operations to prevent blocking the main thread. A common pitfall arises with `GPUBuffer.mapAsync()`, which asynchronously requests a mapped range of a buffer. The promise returned by `mapAsync()` resolves when the buffer is ready for CPU access. However, if multiple `mapAsync()` calls are made concurrently on the same buffer, or if a buffer is unmapped or used by the GPU while an `mapAsync()` operation is pending or active on the CPU, a race condition can occur. Such races can lead to corrupted data, undefined behavior, or, more severely, a 'device loss'. Device loss in WebGPU means the GPU context becomes unavailable, rendering all subsequent WebGPU operations invalid and often unrecoverable without reloading the page. A single race condition can thus cascade into a complete application failure.</p>",
    "root_cause": "The specific technical reason for async buffer mapping races and cascading device loss failures is improper synchronization between CPU-side `mapAsync()`/`unmap()` operations and GPU-side buffer usage. This occurs when: 1) `mapAsync()` is called on a buffer that is already mapped or has a pending `mapAsync()` operation. 2) `unmap()` is called before all CPU read/write operations on the mapped buffer have completed, or while the GPU is still using a buffer that was previously mapped. 3) The mapped buffer is used in a GPU command queue submission before the `mapAsync()` promise resolves and the CPU writes are complete, or after `unmap()` has been called. The asynchronous nature makes these timing issues subtle and difficult to debug, often manifesting as non-deterministic failures.",
    "bad_code": "```typescript\n// Bad Code Example: Async Buffer Mapping Race\nasync function renderLoopBad(device: GPUDevice, buffer: GPUBuffer, data: Float32Array) {\n    // Simulate a high-frequency update scenario\n    setInterval(async () => {\n        // Race condition: Multiple mapAsync calls can overlap if previous ones haven't resolved\n        // or if unmap() is called prematurely.\n        const arrayBuffer = await buffer.mapAsync(GPUMapMode.WRITE, 0, data.byteLength);\n        const mappedData = new Float32Array(arrayBuffer);\n        mappedData.set(data.map(val => val + Math.random())); // Update data\n        buffer.unmap(); // Unmap immediately, potentially before GPU consumes it or while another map is pending\n\n        // Submit to queue (GPU might try to use buffer before unmap is truly complete or while it's still mapped)\n        const commandEncoder = device.createCommandEncoder();\n        // ... Use buffer in a render pass ...\n        device.queue.submit([commandEncoder.finish()]);\n    }, 16); // ~60 FPS update rate\n}\n\n// Potential issue: This might also cause a device loss if the queue is submitted\n// while the buffer is still mapped by the CPU or unmap is not fully processed.\n// The WebGPU specification states that a buffer must be unmapped to be used on the GPU.\n// Calling unmap() only dispatches the unmap operation, it's not immediately complete.\n// And mapAsync() itself on a currently mapped buffer will cause a device loss.\n```",
    "solution_desc": "To prevent async buffer mapping races and device loss, strict synchronization is required. Always `await` the `mapAsync()` promise before attempting to access the mapped buffer data. Do not call `mapAsync()` on a buffer that is already mapped or has an ongoing `mapAsync()` operation. Ensure that `unmap()` is only called after all CPU writes/reads are complete, and that the buffer is only used by the GPU (via command queue submission) after it has been fully unmapped. A robust pattern involves using a state flag to track buffer mapping status or employing double-buffering. For dynamic data updates, consider using `device.queue.writeBuffer()` as a more efficient and safer alternative for frequent small updates, or use two buffers (one for CPU write, one for GPU read) and swap them. Always handle `device.lost` events gracefully in your application.",
    "good_code": "```typescript\n// Good Code Example: Safe Async Buffer Mapping\nlet isBufferMapped = false; // State to prevent concurrent maps\n\nasync function renderLoopGood(device: GPUDevice, buffer: GPUBuffer, data: Float32Array) {\n    setInterval(async () => {\n        if (isBufferMapped) {\n            console.warn(\"Buffer already mapped or map operation pending. Skipping update.\");\n            return;\n        }\n\n        isBufferMapped = true;\n        try {\n            // Await mapAsync to ensure buffer is ready and only one operation occurs at a time\n            const arrayBuffer = await buffer.mapAsync(GPUMapMode.WRITE, 0, data.byteLength);\n            const mappedData = new Float32Array(arrayBuffer);\n            mappedData.set(data.map(val => val + Math.random())); // Update data\n        } catch (e) {\n            console.error(\"Error mapping buffer:\", e);\n            // Handle device loss or other errors\n            isBufferMapped = false;\n            return;\n        }\n\n        buffer.unmap(); // Safely unmap after all CPU writes\n        isBufferMapped = false;\n\n        // Ensure GPU command submission happens AFTER unmap completes for this frame.\n        // The most robust way is often to use device.queue.onSubmittedWorkDone() for critical timing,\n        // or simply ensure subsequent GPU usage follows the unmap call in the event loop.\n        // For many cases, simply awaiting mapAsync and then calling unmap suffices if the GPU submission\n        // is part of the next frame's loop iteration.\n\n        const commandEncoder = device.createCommandEncoder();\n        // ... Use buffer in a render pass ...\n        device.queue.submit([commandEncoder.finish()]);\n\n    }, 16);\n}\n\n// Alternatively, use writeBuffer for frequent small updates, which avoids mapping overhead and races.\nfunction renderLoopWriteBuffer(device: GPUDevice, buffer: GPUBuffer, data: Float32Array) {\n    setInterval(() => {\n        data = data.map(val => val + Math.random()); // Update data\n        device.queue.writeBuffer(buffer, 0, data);\n\n        const commandEncoder = device.createCommandEncoder();\n        // ... Use buffer in a render pass ...\n        device.queue.submit([commandEncoder.finish()]);\n    }, 16);\n}\n\n// Always handle device loss\nnavigator.gpu.requestAdapter().then(adapter => {\n    adapter?.requestDevice().then(device => {\n        device.onuncapturederror = (event) => {\n            console.error(\"WebGPU uncaptured error:\", event.error);\n        };\n        device.lost.then((info) => {\n            console.error(`WebGPU device was lost: ${info.message}`);\n            // Attempt to re-initialize or inform user.\n        });\n        // ... setup and call renderLoopGood or renderLoopWriteBuffer\n    });\n});\n```",
    "verification": "Verify the fix by running the WebGPU application for extended periods under stress (e.g., rapid data updates, multiple simultaneous operations). Monitor the browser's developer console for WebGPU-specific error messages, particularly those indicating device loss (`device.lost`). A healthy application should not report any device loss events. Additionally, visually inspect the rendering output for any data corruption or flickering, which could indicate a subtle race condition not severe enough to cause device loss. Tools like Spector.js (if WebGPU support is added) or browser-native GPU debuggers can provide deeper insights into buffer states and command queue submissions.",
    "date": "2026-09-13",
    "id": 1789285782,
    "type": "error"
});