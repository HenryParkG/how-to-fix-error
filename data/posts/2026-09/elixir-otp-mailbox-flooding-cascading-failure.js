window.onPostDataLoaded({
    "title": "Elixir OTP: Unbounded Mailbox Flooding & Cascading Failure",
    "slug": "elixir-otp-mailbox-flooding-cascading-failure",
    "language": "Elixir",
    "code": "Resource Exhaustion",
    "tags": [
        "Elixir",
        "OTP",
        "Concurrency",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>In Elixir, processes communicate by sending messages to each other's mailboxes. Each Elixir process (a lightweight Erlang VM process) has an unbounded message queue, or 'mailbox'. While this design is incredibly powerful for concurrency, it introduces a subtle but critical vulnerability: if a process sends messages to another process faster than the receiver can process them, or if the receiver process becomes unresponsive or crashes, its mailbox can grow indefinitely.</p><p>This 'unbounded mailbox flooding' leads to a dangerous cascade of resource exhaustion. As the mailbox accumulates messages, it consumes system memory. If the sender continues to send messages, or multiple senders target the same overwhelmed process, the recipient's memory usage can spiral out of control, eventually exhausting all available memory on the node. This triggers the Erlang VM's 'out of memory' killer, which will terminate the entire node, including all running applications. Since Elixir applications are typically structured using OTP supervision trees, the termination of a critical process due to mailbox flooding can lead to its supervisor attempting to restart it repeatedly (often without addressing the underlying problem), or worse, the entire supervision tree (and thus the application) crashing if the supervisor itself runs out of memory or cannot cope with the rapid restarts. This results in a complete application outage, making the system brittle under load or failure conditions.</p>",
    "root_cause": "Lack of backpressure or flow control mechanisms between communicating Elixir processes, allowing a sender to enqueue messages into a recipient's unbounded mailbox faster than the recipient can process them, leading to memory exhaustion and subsequent process/node termination.",
    "bad_code": "A common pattern where a producer process (`ProducerGenServer`) continuously sends messages to a consumer process (`ConsumerGenServer`) without any consideration for the consumer's processing capacity.\n\n```elixir\n# lib/my_app/producer_gen_server.ex\ndefmodule MyApp.ProducerGenServer do\n  use GenServer\n\n  def start_link(_opts) do\n    GenServer.start_link(__MODULE__, [], name: __MODULE__)\n  end\n\n  @impl true\n  def init([]) do\n    {:ok, %{}, {:continue, :send_messages}}\n  end\n\n  @impl true\n  def handle_continue(:send_messages, state) do\n    # This loop sends messages relentlessly without backpressure\n    Enum.each(1..1_000_000, fn i ->\n      GenServer.cast(MyApp.ConsumerGenServer, {:data, i})\n      # A real-world scenario might have a loop driven by external events\n      # or a timer, still potentially overwhelming the consumer.\n    end)\n    {:noreply, state}\n  end\nend\n\n# lib/my_app/consumer_gen_server.ex\ndefmodule MyApp.ConsumerGenServer do\n  use GenServer\n\n  def start_link(_opts) do\n    GenServer.start_link(__MODULE__, [], name: __MODULE__)\n  end\n\n  @impl true\n  def init([]) do\n    {:ok, %{}}\n  end\n\n  @impl true\n  def handle_cast({:data, i}, state) do\n    # Simulate slow processing or a potential crash\n    Process.sleep(100) # Each message takes 100ms to process\n    IO.puts \"Processed data: #{i}\"\n    {:noreply, state}\n  end\nend\n```\nIf `MyApp.ConsumerGenServer` is slower than `MyApp.ProducerGenServer`, its mailbox will quickly fill up, consuming all memory and crashing the VM.",
    "solution_desc": "To prevent unbounded mailbox flooding, implement backpressure mechanisms. This ensures that a sender only dispatches messages at a rate the receiver can handle. Strategies include:\n\n1.  **Bounded mailboxes:** While Erlang/Elixir mailboxes are unbounded by default, you can implement application-level bounds by having the receiver acknowledge processing or signal its capacity. If the sender doesn't receive an acknowledgment, it slows down or retries.\n2.  **`GenStage` or `Broadway`:** For data processing pipelines, these libraries are explicitly designed for flow control and backpressure, allowing producers, consumers, and consumers to adapt to varying processing speeds.\n3.  **Dynamic Supervision/Concurrency:** Use libraries like `poolboy` or `jobs` to limit the number of active consumer processes, distributing work and preventing a single bottleneck. Or, implement a 'demand-driven' system where consumers explicitly ask for work.\n4.  **Timeouts and Monitoring:** Implement timeouts for `GenServer.call` operations and monitor mailbox sizes (e.g., using `Process.info/2`) to detect potential flooding early.",
    "good_code": "Using `GenStage` is an excellent way to implement backpressure for data pipelines, preventing mailbox flooding by design.\n\n```elixir\n# lib/my_app/producer_stage.ex\ndefmodule MyApp.ProducerStage do\n  use GenStage\n\n  def start_link(_opts) do\n    GenStage.start_link(__MODULE__, :ok, name: __MODULE__)\n  end\n\n  @impl true\n  def init(:ok) do\n    {:producer, :no_state, dispatcher: GenStage.BroadcastDispatcher}\n  end\n\n  @impl true\n  def handle_demand(demand, state) when demand > 0 do\n    # The producer only generates events when there is demand from consumers.\n    # This is the core of backpressure.\n    events = Enum.map(1..demand, fn _ -> {:data, System.unique_integer([:positive])} end)\n    {:noreply, events, state}\n  end\nend\n\n# lib/my_app/consumer_stage.ex\ndefmodule MyApp.ConsumerStage do\n  use GenStage\n\n  def start_link(_opts) do\n    GenStage.start_link(__MODULE__, :ok, name: __MODULE__)\n  end\n\n  @impl true\n  def init(:ok) do\n    {:consumer, :no_state}\n  end\n\n  @impl true\n  def handle_events(events, _from, state) do\n    # Simulate slow processing\n    Enum.each(events, fn {:data, i} ->\n      Process.sleep(100) # Each event takes 100ms to process\n      IO.puts \"Processed data: #{i}\"\n    end)\n    {:noreply, [], state}\n  end\nend\n\n# In your application supervisor, link them:\n# children = [\n#   MyApp.ProducerStage,\n#   {MyApp.ConsumerStage, subscribe_to: [MyApp.ProducerStage]}\n# ]\n```\nWith `GenStage`, `MyApp.ProducerStage` will only generate `demand` number of events, preventing `MyApp.ConsumerStage`'s mailbox from flooding. If the consumer is slow, it will simply request less demand, and the producer will pause.",
    "date": "2026-09-16",
    "id": 1789525502,
    "type": "error"
});