window.onPostDataLoaded({
    "title": "RCU Deadlocks & Memory Reclamation Failures",
    "slug": "rcu-deadlocks-memory-reclamation-failures",
    "language": "C",
    "code": "RCU_DEADLOCK",
    "tags": [
        "Linux Kernel",
        "Concurrency",
        "RCU",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>RCU (Read-Copy-Update) is a synchronization mechanism designed for read-heavy workloads in the the Linux kernel, allowing readers to proceed without locks while writers modify data by copying and then replacing it. The 'update' part of RCU involves waiting for all pre-existing readers to complete their 'quiescent states' (periods when they are not accessing the protected data) before reclaiming the old memory. A grace period is the time interval during which all readers active before the update are guaranteed to have passed through a quiescent state.</p><p>Deadlocks and memory reclamation failures in RCU often arise when a CPU or thread gets stuck within an RCU read-side critical section or fails to enter a quiescent state for an extended period. This prevents the grace period from completing, leading to old data not being freed. If the writer then attempts to acquire a lock that a reader already holds within its RCU critical section, or if the reader itself is blocked waiting for a resource held by the writer, a deadlock can occur. Furthermore, an uncompleted grace period can cause memory exhaustion as old versions of data accumulate, leading to system instability or crashes.</p>",
    "root_cause": "A CPU failing to pass through a quiescent state (e.g., due to a non-preemptible loop, spinlock contention, or a CPU going offline unexpectedly within an RCU read-side critical section) indefinitely prolongs or prevents RCU grace period completion. This can be exacerbated by incorrect nesting of RCU critical sections with other locks, or by readers blocking within an RCU read-side section, preventing the grace period from concluding.",
    "bad_code": "```c\n// Hypothetical problematic kernel module code\nstruct my_data {\n    int value;\n    // ... other fields\n};\n\nstruct my_data *global_data __read_mostly; // RCU protected data\nDEFINE_SPINLOCK(my_data_lock); // Lock for writers\n\nvoid __init my_module_init(void) {\n    global_data = kzalloc(sizeof(*global_data), GFP_KERNEL);\n    global_data->value = 10;\n    rcu_assign_pointer(global_data, global_data);\n}\n\nvoid my_rcu_reader_func(void) {\n    struct my_data *p;\n    rcu_read_lock();\n    // Assume some operation here might acquire a spinlock\n    // which the writer also tries to acquire, or blocks indefinitely.\n    // For example, if a slow, non-preemptible operation occurs here\n    // or a lock is acquired that also needs to be held by the writer\n    // for a very long time. This is simplified and illustrative.\n    // The real problem is often subtle scheduling or lock inversion.\n    // Example of a problematic (conceptual) long-running op:\n    // while (some_condition) { /* busy loop without yielding */ }\n    p = rcu_dereference(global_data);\n    if (p) {\n        printk(KERN_INFO \"Value: %d\\n\", p->value);\n    }\n    rcu_read_unlock();\n}\n\nvoid my_rcu_writer_func(int new_value) {\n    struct my_data *old_p, *new_p;\n    new_p = kzalloc(sizeof(*new_p), GFP_KERNEL);\n    if (!new_p) return;\n    new_p->value = new_value;\n\n    spin_lock(&my_data_lock); // Protects pointer assignment and concurrent modifications\n    old_p = rcu_dereference_protected(global_data, lockdep_is_held(&my_data_lock));\n    rcu_assign_pointer(global_data, new_p);\n    spin_unlock(&my_data_lock);\n\n    // This blocks until all previous readers finish.\n    // If a reader in my_rcu_reader_func is stuck, this call will never return.\n    synchronize_rcu();\n    kfree(old_p); // old_p is never freed if synchronize_rcu() blocks\n}\n```",
    "solution_desc": "The solution involves ensuring that all RCU read-side critical sections are truly quiescent-state friendly. This means avoiding long-running, non-preemptible operations, blocking calls, or acquiring spinlocks that could lead to contention or deadlocks with the RCU grace period. For scenarios requiring blocking within an RCU read-side section, consider using `rcu_read_unlock_irqrestore()` and `rcu_read_lock_irqsave()` if IRQs are relevant, or carefully structuring the code to exit the RCU read-side section before blocking. Utilize `rcu_synchronize_light()` or `call_rcu()` for asynchronous reclamation in situations where the writer cannot afford to block indefinitely. Debugging tools like `RCU_LOCKDEP_ENABLE` and `CONFIG_PROVE_LOCKING` can help identify lock ordering issues. For debugging specific stuck grace periods, `debug_rcu_cpu_stall` can be useful.",
    "good_code": "```c\n// Hypothetical improved kernel module code\nstruct my_data {\n    int value;\n    struct rcu_head rcu_head; // Added for call_rcu\n    // ... other fields\n};\n\nstruct my_data *global_data __read_mostly;\nDEFINE_SPINLOCK(my_data_lock);\n\nvoid __init my_module_init(void) {\n    global_data = kzalloc(sizeof(*global_data), GFP_KERNEL);\n    global_data->value = 10;\n    rcu_assign_pointer(global_data, global_data);\n}\n\n// RCU callback structure for asynchronous memory reclamation\nvoid my_rcu_free_callback(struct rcu_head *rh) {\n    struct my_data *p = container_of(rh, struct my_data, rcu_head);\n    kfree(p);\n}\n\nvoid my_rcu_reader_func(void) {\n    struct my_data *p;\n    rcu_read_lock();\n    // Ensure no long-running or blocking operations within the RCU critical section.\n    // If a blocking operation is needed, exit the RCU critical section first.\n    p = rcu_dereference(global_data);\n    if (p) {\n        printk(KERN_INFO \"Value: %d\\n\", p->value);\n    }\n    rcu_read_unlock();\n    // If there was a blocking call:\n    // my_blocking_function(); // Call it after rcu_read_unlock()\n}\n\nvoid my_rcu_writer_func(int new_value) {\n    struct my_data *old_p, *new_p;\n    new_p = kzalloc(sizeof(*new_p), GFP_KERNEL);\n    if (!new_p) return;\n    new_p->value = new_value;\n\n    spin_lock(&my_data_lock);\n    old_p = rcu_dereference_protected(global_data, lockdep_is_held(&my_data_lock));\n    rcu_assign_pointer(global_data, new_p);\n    spin_unlock(&my_data_lock);\n\n    // Instead of blocking with synchronize_rcu(), use asynchronous reclamation.\n    // This won't block the writer. The old_p will be freed once a grace period completes.\n    if (old_p) {\n        call_rcu(&old_p->rcu_head, my_rcu_free_callback);\n    }\n}\n```",
    "verification": "Monitor `debug_rcu_cpu_stall` output in `dmesg` or `/sys/kernel/debug/rcu` for stall warnings. Use `ftrace` or `perf` to trace RCU events and identify long-running reader critical sections or CPUs failing to enter quiescent states. Analyze `/proc/meminfo` and specific kernel memory allocators (e.g., SLAB/SLUB) to detect increasing memory usage that might indicate reclamation failures. System stability and responsiveness should improve, and unexpected crashes related to RCU should cease.",
    "date": "2026-09-05",
    "id": 1788573871,
    "type": "error"
});