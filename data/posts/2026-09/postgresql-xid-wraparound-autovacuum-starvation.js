window.onPostDataLoaded({
    "title": "PostgreSQL: XID Wraparound & Aggressive Autovacuum Starvation",
    "slug": "postgresql-xid-wraparound-autovacuum-starvation",
    "language": "PostgreSQL",
    "code": "XID Wraparound",
    "tags": [
        "PostgreSQL",
        "SQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL relies on a 32-bit Transaction ID (XID) counter to manage concurrency and MVCC (Multi-Version Concurrency Control). Each new transaction increments this counter. To prevent the XID from 'wrapping around' (overflowing and restarting from zero, which would cause old, active transactions to appear as future transactions, leading to data corruption), PostgreSQL employs a background process called Autovacuum.</p><p>Autovacuum's critical role includes marking old transaction IDs as 'frozen' through a process called <code>VACUUM FREEZE</code>. This effectively pushes the 'oldest unfrozen XID' forward, ensuring the 2 billion transaction ID window (half of the 32-bit range) always encompasses all active transactions. When Autovacuum is starved or severely delayed, it cannot keep up with the transaction rate. The <code>datfrozenxid</code> for a database, or <code>relfrozenxid</code> for individual tables, falls behind. If the oldest unfrozen XID gets too close to the XID wraparound point (typically 2 billion transactions away from the current XID), PostgreSQL will issue warnings and eventually shut down to prevent data loss, requiring manual intervention to freeze transactions.</p><p>Aggressive autovacuum starvation occurs when system resources are insufficient, autovacuum is throttled too heavily (e.g., high <code>autovacuum_vacuum_cost_delay</code>), or there's an unusually high transaction volume, especially UPDATE/DELETE operations, preventing autovacuum from completing its freezing cycles in time.</p>",
    "root_cause": "Autovacuum process is unable to complete freezing transactions fast enough due to resource contention, aggressive throttling (e.g., high cost delay), or excessively high transaction write volume, causing the <code>datfrozenxid</code> or <code>relfrozenxid</code> to approach the 32-bit XID limit.",
    "bad_code": "```sql\n-- Example of configuration that might inadvertently starve autovacuum\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '100ms'; -- Default is 20ms, 100ms significantly slows down\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = 200;    -- Default is 200, but in high load, this is too low\n\n-- Or simply a heavily loaded system with many transactions,\n-- where autovacuum cannot keep up with its default settings.\n\n-- Checking current XID age (approaching max_freeze_age is dangerous)\nSELECT datname, age(datfrozenxid) AS xid_age FROM pg_database;\nSELECT relname, age(relfrozenxid) AS xid_age FROM pg_class WHERE relkind = 'r' ORDER BY xid_age DESC;\n```",
    "solution_desc": "To fix XID wraparound risk, ensure Autovacuum can perform its duties efficiently. This involves monitoring XID age, adjusting autovacuum parameters, and potentially increasing system resources. Prioritize tables with high <code>relfrozenxid</code> values. For immediate relief, a manual <code>VACUUM FREEZE</code> can be performed on critical tables or the entire database. Architecturally, consider workload partitioning to reduce transaction volume on single databases, or use connection pooling to manage transaction lifecycles more efficiently. Ensure sufficient I/O and CPU resources for both application transactions and background vacuuming.",
    "good_code": "```sql\n-- 1. Check current XID ages and identify problem areas:\nSELECT datname, age(datfrozenxid) AS xid_age FROM pg_database ORDER BY xid_age DESC;\nSELECT relname, age(relfrozenxid) AS xid_age, pg_size_pretty(pg_relation_size(oid)) AS rel_size\nFROM pg_class WHERE relkind = 'r' ORDER BY xid_age DESC;\n\n-- 2. Tune autovacuum settings (example, adjust based on system load and resources):\n-- Increase autovacuum workers if your system has many databases/tables\nALTER SYSTEM SET autovacuum_max_workers = 5; -- Default 3\n-- Decrease cost delay to make autovacuum more aggressive\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '10ms'; -- Default 20ms\n-- Increase cost limit to allow more work per cycle\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = 1000; -- Default 200\n-- Set a higher threshold for freeze age to trigger vacuum earlier (per-table or globally)\nALTER TABLE my_critical_table SET (autovacuum_freeze_max_age = 500000000); -- Default 200M\n-- Apply changes and reload PostgreSQL configuration (or restart)\nSELECT pg_reload_conf();\n\n-- 3. If critically close to wraparound, perform a manual VACUUM FREEZE (can be resource-intensive):\nVACUUM (FREEZE, VERBOSE, ANALYZE) my_critical_table;\n-- Or for the entire database (use with extreme caution in production, may block):\nVACUUM (FREEZE, VERBOSE, ANALYZE);\n```",
    "verification": "Continuously monitor the XID age using `SELECT datname, age(datfrozenxid) AS xid_age FROM pg_database;` and `pg_stat_all_tables.relfrozenxid`. Verify that `xid_age` values are consistently well below the `autovacuum_freeze_max_age` threshold (default 200 million). Check PostgreSQL logs for `autovacuum: xid ... freeze limit is ...` warnings. Observe system resource usage (I/O, CPU) during autovacuum cycles to ensure it has enough capacity to operate without causing application performance degradation.",
    "date": "2026-09-20",
    "id": 1789891548,
    "type": "error"
});