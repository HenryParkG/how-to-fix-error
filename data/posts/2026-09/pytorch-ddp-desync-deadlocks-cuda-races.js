window.onPostDataLoaded({
    "title": "PyTorch DDP Desync Deadlocks & CUDA Races",
    "slug": "pytorch-ddp-desync-deadlocks-cuda-races",
    "language": "Python, PyTorch",
    "code": "DDPDeadlock",
    "tags": [
        "PyTorch",
        "Distributed",
        "Deep Learning",
        "Python",
        "Error Fix"
    ],
    "analysis": "<p>PyTorch's DistributedDataParallel (DDP) is a powerful tool for scaling deep learning training across multiple GPUs or machines. However, it can be notoriously difficult to debug when deadlocks or performance issues arise. A common source of deadlocks in DDP setups is 'desynchronization' of collective operations. DDP relies on all participating processes (ranks) to execute the same sequence of collective communication primitives (e.g., all-reduce for gradients, broadcast for model synchronization) in a consistent order. If one rank deviates from this order, perhaps due to a conditional branch that is only taken on some ranks, it can lead to a deadlock where ranks wait indefinitely for an operation that will never arrive from another rank.</p><p>CUDA stream race conditions often exacerbate these DDP desync issues or cause silent data corruption/performance degradation. PyTorch uses CUDA streams to overlap computation and communication. While DDP typically manages stream synchronization for its internal collectives, custom asynchronous CUDA operations (e.g., `torch.cuda.current_stream().record_event()`, `stream.wait_event()`) performed by the user without proper `torch.distributed.barrier()` calls or explicit stream synchronization can lead to: (1) different ranks completing parts of their computation at different times, causing desynchronized DDP collectives; or (2) data being read/written to by different streams in an uncoordinated manner, leading to incorrect gradient computations or model updates. These issues are particularly insidious because they might not always deadlock, but manifest as training instability, convergence issues, or seemingly random crashes.</p>",
    "root_cause": "Mismatched collective operations across DDP ranks (e.g., an `all_reduce` called conditionally on only some ranks), or improper synchronization of custom asynchronous CUDA operations causing ranks to diverge in execution flow or access shared memory inconsistently.",
    "bad_code": "```python\n# bad_ddp_code.py\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\n\ndef setup(rank, world_size):\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\ndef cleanup():\n    dist.destroy_process_group()\n\nclass ToyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear = nn.Linear(10, 10)\n    def forward(self, x):\n        return self.linear(x)\n\ndef train(rank, world_size):\n    setup(rank, world_size)\n    torch.cuda.set_device(rank)\n\n    model = ToyModel().to(rank)\n    ddp_model = DDP(model, device_ids=[rank])\n    optimizer = optim.SGD(ddp_model.parameters(), lr=0.01)\n\n    for i in range(5):\n        inputs = torch.randn(20, 10).to(rank)\n        labels = torch.randn(20, 10).to(rank)\n\n        # *** POTENTIAL DESYNC ISSUE: Conditional execution of a collective\n        if rank == 0 and i == 2: # Only rank 0 executes this path\n            # This might hide a collective that only rank 0 expects.\n            # For example, if there was a dist.barrier() here, others would deadlock.\n            # Even without explicit collectives, this path divergence can cause issues.\n            _ = ddp_model.module.linear.weight.sum() # Just an example non-collective op\n\n        optimizer.zero_grad()\n        outputs = ddp_model(inputs)\n        loss = (outputs - labels).pow(2).sum()\n        loss.backward()\n\n        # *** POTENTIAL CUDA RACE / ASYNC ISSUE:\n        # If custom CUDA kernels or asynchronous ops were here for some ranks only,\n        # without proper stream synchronization or DDP interaction, it could cause desync.\n        # Example: if rank 0 calls a custom non-blocking kernel, while others wait.\n\n        optimizer.step()\n    \n    cleanup()\n\nif __name__ == '__main__':\n    world_size = 2\n    import multiprocessing\n    processes = []\n    for rank in range(world_size):\n        p = multiprocessing.Process(target=train, args=(rank, world_size))\n        p.start()\n        processes.append(p)\n    for p in processes:\n        p.join()\n\n```",
    "solution_desc": "To fix DDP desync deadlocks and CUDA race conditions, ensure all ranks execute the exact same PyTorch operations, especially distributed communication primitives, in the same order. Avoid conditional logic that might cause ranks to diverge. For custom asynchronous CUDA operations, always explicitly synchronize streams using `torch.cuda.current_stream().synchronize()` or `event.wait()` where necessary, or use `torch.distributed.barrier()` to ensure all ranks reach a specific point before proceeding. If a model has unused parameters (e.g., due to conditional forward passes), either ensure `find_unused_parameters=True` is set in the `DDP` constructor (which has a performance overhead) or make sure all parameters contribute to the loss on every rank. When moving data between CPU and GPU or across devices, ensure it's done consistently. For debugging, enabling `PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:X` can sometimes reveal CUDA memory issues. Using `torch.autograd.set_detect_anomaly(True)` can help pinpoint where NaNs or infinities originate, which sometimes appear due to race conditions. Finally, simplify the code to isolate the problem; remove non-essential parts of the training loop and custom operations until the issue disappears.",
    "good_code": "```python\n# good_ddp_code.py\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\n\ndef setup(rank, world_size):\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\ndef cleanup():\n    dist.destroy_process_group()\n\nclass ToyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear = nn.Linear(10, 10)\n    def forward(self, x):\n        return self.linear(x)\n\ndef train(rank, world_size):\n    setup(rank, world_size)\n    torch.cuda.set_device(rank)\n\n    model = ToyModel().to(rank)\n    # Set find_unused_parameters=True if your model has conditional paths where some params might not be used on all ranks\n    ddp_model = DDP(model, device_ids=[rank]) #, find_unused_parameters=True)\n    optimizer = optim.SGD(ddp_model.parameters(), lr=0.01)\n\n    for i in range(5):\n        inputs = torch.randn(20, 10).to(rank)\n        labels = torch.randn(20, 10).to(rank)\n\n        optimizer.zero_grad()\n        outputs = ddp_model(inputs)\n        loss = (outputs - labels).pow(2).sum()\n        loss.backward()\n\n        # All ranks execute the same sequence of operations.\n        # If custom async CUDA operations are involved, ensure explicit synchronization:\n        # custom_async_op(inputs) # Example non-blocking op\n        # torch.cuda.current_stream().synchronize() # Ensure it completes before DDP collectives\n        # If a collective is needed, ensure all ranks call it:\n        # if i == 2: dist.barrier() # Example of a conditional barrier, if needed, ensure ALL ranks call it.\n\n        optimizer.step()\n    \n    cleanup()\n\nif __name__ == '__main__':\n    world_size = 2\n    import multiprocessing\n    processes = []\n    for rank in range(world_size):\n        p = multiprocessing.Process(target=train, args=(rank, world_size))\n        p.start()\n        processes.append(p)\n    for p in processes:\n        p.join()\n\n```",
    "verification": "Verify the fix by running the DDP training script on multiple GPUs/processes. Look for successful completion without deadlocks. Monitor GPU utilization; a sudden drop to 0% often indicates a deadlock. If the issue was subtle (e.g., convergence problems), run extensive training to confirm stability and accuracy. Use `torch.distributed.barrier()` at various points in the training loop to isolate where desynchronization might be occurring. Compare logs from different ranks to ensure they are progressing through the training loop in lockstep. Tools like NVIDIA's `nvprof` or `Nsight Systems` can help visualize CUDA stream activity and identify potential race conditions or idle periods caused by waiting on unsynchronized operations.",
    "date": "2026-09-04",
    "id": 1788507935,
    "type": "error"
});