window.onPostDataLoaded({
    "title": "Redis: CoW Memory & BGSAVE Stalls (THP)",
    "slug": "redis-copy-on-write-memory-amplification-bg-save-stalls-transparent-huge-pages",
    "language": "Redis",
    "code": "Memory Amplification",
    "tags": [
        "Redis",
        "Linux",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Redis's `BGSAVE` command and AOF rewriting (`BGREWRITEAOF`) rely on the `fork()` system call to create a child process. This child process then performs the disk-write operation, allowing the parent Redis instance to continue serving requests. The `fork()` system call, on Linux, implements a Copy-on-Write (CoW) mechanism. This means that the child process initially shares all memory pages with the parent. Only when either the parent or the child writes to a shared page does a private copy of that specific page get made for the writing process.</p><p>This CoW behavior is generally efficient, but it becomes problematic when combined with Transparent Huge Pages (THP). THP is a Linux kernel feature that attempts to use larger memory pages (typically 2MB instead of 4KB) to improve TLB hit rates and overall memory access performance. While beneficial for some workloads, it has a detrimental effect on Redis's `BGSAVE` because of CoW. If Redis is running on a system with THP enabled and it allocates memory using huge pages, then during a `BGSAVE` operation, even a single byte write to a 2MB huge page by the parent Redis process will trigger the kernel to copy the <i>entire 2MB page</i> for that process. This is known as memory amplification.</p><p>The consequence of this memory amplification is significant: during `BGSAVE`, Redis's effective memory usage can temporarily double or even triple, leading to excessive page faults, increased I/O, and potentially swapping. If the system starts swapping, Redis's performance will degrade drastically, leading to high latency and request stalls, even though the primary instance is not performing the actual save operation. This can lead to the Redis instance becoming unresponsive for considerable periods, effectively stalling the application that relies on it.</p>",
    "root_cause": "The specific technical reason for failure is the interaction between Redis's `fork()`-based background saving mechanism (which relies on Copy-on-Write) and the Linux kernel's Transparent Huge Pages (THP) feature. When THP is enabled, the granularity of CoW becomes the huge page size (e.g., 2MB) instead of the standard page size (4KB). This means any write operation by the parent Redis process during `BGSAVE` (even a small update) forces a copy of the entire 2MB huge page, leading to significant and rapid memory amplification, increased page fault rate, potential swapping, and ultimately performance degradation and stalls.",
    "bad_code": "```bash\n# This is not 'bad code' in the traditional sense, but an environment/configuration state.\n# Redis server running on a Linux system where Transparent Huge Pages (THP) are enabled.\n# You can check THP status with:\ncat /sys/kernel/mm/transparent_hugepage/enabled\n# [always] madvise never  <- 'always' or 'madvise' means THP is enabled and problematic.\n\n# Or for older kernels/different distros:\ncat /sys/kernel/mm/redhat_transparent_hugepage/enabled\n\n# The problematic behavior occurs when Redis data changes during a BGSAVE/BGREWRITEAOF.\n# Example Redis configuration (redis.conf) with THP implicitly enabled due to OS config:\n# RDB files will be created periodically even with default settings, e.g.:\nsave 900 1\nsave 300 10\nsave 60 10000\n# Or AOF rewrite triggered:\nauto-aof-rewrite-percentage 100\nauto-aof-rewrite-min-size 64mb\n\n# During such operations, if the OS has THP enabled, memory amplification will occur.\n```",
    "solution_desc": "The most effective and widely recommended solution is to disable Transparent Huge Pages (THP) on the Linux system where Redis is running. This forces the kernel to use standard 4KB pages for Redis's memory allocations, making the Copy-on-Write mechanism much more efficient during `BGSAVE` and AOF rewrites. Each write will only copy a 4KB page, drastically reducing memory amplification.\n\nSteps to disable THP:\n<ol>\n  <li><b>Temporarily (until next reboot):</b> Run the following commands as root:\n    <pre><code>echo never > /sys/kernel/mm/transparent_hugepage/enabled\necho never > /sys/kernel/mm/transparent_hugepage/defrag</code></pre>\n  </li>\n  <li><b>Permanently (recommended):</b> Modify your system's boot configuration (e.g., GRUB) or create a systemd unit/init script to set THP to `never` on startup.\n    <ul>\n      <li><b>For systemd-based systems (e.g., CentOS 7+, Ubuntu 15+):</b> Create a service file, e.g., <code>/etc/systemd/system/disable-thp.service</code>, with content like:\n        <pre><code>[Unit]\nDescription=Disable Transparent Huge Pages (THP)\nAfter=sysinit.target local-fs.target\n\n[Service]\nType=oneshot\nExecStart=/bin/sh -c 'echo never > /sys/kernel/mm/transparent_hugepage/enabled && echo never > /sys/kernel/mm/transparent_hugepage/defrag'\n\n[Install]\nWantedBy=multi-user.target</code></pre>\n        Then enable and start it:\n        <pre><code>sudo systemctl daemon-reload\nsudo systemctl enable disable-thp.service\nsudo systemctl start disable-thp.service</code></pre>\n      </li>\n      <li><b>For older systems or `rc.local`:</b> Add the `echo never ...` lines to `/etc/rc.local` (ensure it's executable).</li>\n      <li><b>Via GRUB (less common for Redis but an option):</b> Add `transparent_hugepage=never` to your kernel boot parameters in `/etc/default/grub` and run `update-grub` (or `grub2-mkconfig`).</li>\n    </ul>\n  </li>\n</ol>\nAdditional mitigation strategies (less impactful than disabling THP but good practices):\n<ul>\n  <li>Reduce the frequency of `BGSAVE` operations if possible.</li>\n  <li>Ensure your system has ample RAM to handle temporary memory spikes during `BGSAVE` without resorting to swap.</li>\n  <li>Use AOF persistence mode primarily, as it's generally more robust against data loss and offers `fsync` control. However, AOF rewriting still uses `fork()` and is affected by THP.</li>\n  <li>Run Redis on a machine with enough free memory to prevent swap usage.</li>\n</ul>",
    "good_code": "```bash\n# Step 1: Check current THP status\ncat /sys/kernel/mm/transparent_hugepage/enabled\n# Expected output before fix: [always] madvise never (or similar showing it's active)\n\n# Step 2: Disable THP permanently (example for systemd-based Linux)\n# Create a systemd service file\nsudo tee /etc/systemd/system/disable-thp.service > /dev/null <<EOF\n[Unit]\nDescription=Disable Transparent Huge Pages (THP)\nAfter=sysinit.target local-fs.target\n\n[Service]\nType=oneshot\nExecStart=/bin/sh -c 'echo never > /sys/kernel/mm/transparent_hugepage/enabled && echo never > /sys/kernel/mm/transparent_hugepage/defrag'\n\n[Install]\nWantedBy=multi-user.target\nEOF\n\n# Reload systemd, enable and start the service\nsudo systemctl daemon-reload\nsudo systemctl enable disable-thp.service\nsudo systemctl start disable-thp.service\n\n# Step 3: Verify THP status after applying the fix\ncat /sys/kernel/mm/transparent_hugepage/enabled\n# Expected output after fix: always madvise [never]\n\n# After this, restart your Redis server to ensure it allocates memory with standard pages.\n# sudo systemctl restart redis-server\n```",
    "verification": "To verify that disabling THP has fixed the memory amplification and BGSAVE stalls, follow these steps:\n<ol>\n  <li><b>Check THP Status:</b> After applying the fix and rebooting (if necessary), confirm that `cat /sys/kernel/mm/transparent_hugepage/enabled` outputs `always madvise [never]`.</li>\n  <li><b>Monitor Memory Usage During BGSAVE:</b>\n    <ul>\n      <li>Use `INFO memory` in `redis-cli` before, during, and after a `BGSAVE`. Pay close attention to `used_memory_rss`.</li>\n      <li>Use system tools like `top`, `htop`, or `smem` (specifically `smem -H -P redis` to see actual page sizes) to monitor Redis's Resident Set Size (RSS) during a `BGSAVE` operation. You should observe a significantly smaller and more stable increase in memory usage compared to when THP was enabled.</li>\n      <li>Monitor `/proc/<redis-pid>/smaps` or `/proc/<redis-pid>/numa_maps` for the presence of 'AnonHugePages' before and after the fix. It should be zero or negligible after THP is disabled for Redis.</li>\n    </ul>\n  </li>\n  <li><b>Monitor `BGSAVE` Duration and Performance:</b>\n    <ul>\n      <li>Trigger `BGSAVE` manually and observe its duration (`LASTSAVE` command or Redis logs). It should be faster and more consistent.</li>\n      <li>Run a load test against your Redis instance during a `BGSAVE`. Measure client-side latency and throughput. You should see significantly less impact (or no impact) on Redis responsiveness during the background save operation.</li>\n      <li>Check system logs for any swap activity (e.g., `vmstat 1`, `dstat`). There should be no or minimal swap usage during `BGSAVE` if sufficient RAM is available.</li>\n    </ul>\n  </li>\n  <li><b>Repeat Load Tests:</b> Conduct comprehensive load testing under conditions that previously triggered the stalls. Confirm that Redis maintains its performance and doesn't experience significant latency spikes during its background persistence operations.</li>\n</ol>",
    "date": "2026-09-03",
    "id": 1788420956,
    "type": "error"
});