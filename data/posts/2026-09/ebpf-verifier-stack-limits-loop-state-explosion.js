window.onPostDataLoaded({
    "title": "eBPF Verifier: Stack Limits & Loop State Explosion",
    "slug": "ebpf-verifier-stack-limits-loop-state-explosion",
    "language": "C, eBPF",
    "code": "eBPF Verifier Error",
    "tags": [
        "eBPF",
        "Linux Kernel",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>The eBPF verifier is a critical component of the Linux kernel that ensures the safety and security of eBPF programs before they are loaded and executed. It statically analyzes the program's bytecode to guarantee it terminates, doesn't crash the kernel, and adheres to strict security policies. However, this rigorous analysis can become a bottleneck for complex programs, leading to verifier rejections. Two common culprits are exceeding the 512-byte stack limit and triggering 'bounded loop state explosion'.</p><p>The 512-byte stack limit is a hard constraint for all eBPF programs, designed to prevent excessive kernel stack usage and simplify verifier analysis. Programs that declare large local variables, use complex structs on the stack, or have deep function call chains (even though eBPF to eBPF calls are limited and carefully checked) can easily hit this limit. When the verifier detects stack usage exceeding this boundary, it rejects the program outright.</p><p>Bounded loop state explosion refers to the verifier's difficulty in proving program termination and safety within loops. The verifier tracks the state of registers and stack values at each instruction. For loops, it must ensure that the loop executes a finite number of times and that the program state doesn't diverge in an unbounded way. If a loop's bounds are not clear, or if the loop modifies many different memory locations or register values in each iteration, the number of possible states the verifier needs to explore can grow exponentially, leading to a 'state explosion'. Eventually, the verifier hits its internal complexity limits (e.g., maximum instructions to analyze or maximum states to track) and rejects the program, often with messages like 'loop detected, path too complex'.</p>",
    "root_cause": "The eBPF verifier rejects programs due to explicit 512-byte stack limit violations or the inability to statically prove bounded loop termination and safety within its complexity constraints, leading to 'state explosion' where too many register/stack states need to be tracked across loop iterations.",
    "bad_code": "```c\n// bad_ebpf_program.c\n#include <linux/bpf.h>\n#include <bpf/bpf_helpers.h>\n\nstruct large_data {\n    long data[60]; // ~480 bytes\n    char more_data[50]; // ~50 bytes\n    // Total > 512 bytes on stack\n};\n\nSEC(\"xdp\")\nint xdp_prog_func(struct xdp_md *ctx)\n{\n    struct large_data my_large_struct; // Stack usage issue\n    // Initialize or use my_large_struct\n\n    // Complex loop for verifier state explosion\n    unsigned int sum = 0;\n    for (int i = 0; i < 1000; i++) {\n        // Imagine a more complex scenario involving pointers\n        // and non-linear memory access, making verification hard.\n        // For simplicity, this loop itself might be flagged if too many states are tracked.\n        sum += i; \n    }\n    bpf_printk(\"Sum: %d\\n\", sum);\n    return XDP_PASS;\n}\nchar _license[] SEC(\"license\") = \"GPL\";\n```",
    "solution_desc": "To address the 512-byte stack limit, refactor your eBPF program to reduce local variable storage. Instead of large structs on the stack, consider using eBPF maps for persistent or larger data storage, passing pointers to map values, or breaking down complex logic into smaller, helper-callable functions (though function calls still consume stack space). For bounded loop state explosion, simplify loop logic. Ensure loop bounds are compile-time constants or derived from trusted input (e.g., packet lengths checked against known maximums). Avoid complex pointer arithmetic or non-linear memory accesses within loops that make it difficult for the verifier to track state. Sometimes, unrolling small loops or refactoring them into multiple, simpler loops can help the verifier. Using `bpf_loop()` helper (if available for your kernel version) for iterating over map elements can also be more verifier-friendly.",
    "good_code": "```c\n// good_ebpf_program.c\n#include <linux/bpf.h>\n#include <bpf/bpf_helpers.h>\n\n// Define a map for larger data if necessary, or pass pointers\n// For stack-limit, avoid large local structs\n\nSEC(\"xdp\")\nint xdp_prog_func(struct xdp_md *ctx)\n{\n    // No large struct on stack; if needed, use a map.\n    // Example: int* value = bpf_map_lookup_elem(&my_map, &key);\n\n    // Simplified and explicitly bounded loop\n    unsigned int sum = 0;\n    for (int i = 0; i < 10; i++) { // Smaller, constant bounds\n        sum += i;\n    }\n\n    // Or for more complex iteration, if applicable:\n    // bpf_loop(count, callback_fn, ctx, flags);\n\n    bpf_printk(\"Sum: %d\\n\", sum);\n    return XDP_PASS;\n}\nchar _license[] SEC(\"license\") = \"GPL\";\n```",
    "verification": "Compile your eBPF program using `clang` and `llvm` (e.g., `clang -target bpf -O2 -g -c bad_ebpf_program.c -o bad_ebpf_program.o`). Then attempt to load it into the kernel using `bpftool prog load` or through a user-space loader. If the verifier rejects the program, `dmesg` will typically show detailed error messages from the verifier, indicating the exact instruction or reason for rejection (e.g., 'stack depth exceeded', 'loop detected, path too complex', 'unbounded loop'). After applying fixes, the program should load successfully, and `dmesg` should show no verifier errors. Observe the program's intended behavior using `bpf_printk` or by verifying its effects on network traffic/system calls.",
    "date": "2026-09-17",
    "id": 1789653478,
    "type": "error"
});