window.onPostDataLoaded({
    "title": "Airflow: Scheduler Heartbeat Starvation & Zombie Task Cascades",
    "slug": "airflow-scheduler-heartbeat-starvation-zombie-tasks",
    "language": "Airflow",
    "code": "Scheduler Starvation",
    "tags": [
        "Airflow",
        "Python",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>The Apache Airflow scheduler is the brain of any Airflow deployment. It continuously monitors and triggers tasks, manages DAG runs, and ensures the system's overall health. A critical part of its operation is the heartbeat mechanism. The scheduler periodically sends a 'heartbeat' to the metadata database, signifying that it's alive and healthy. Workers also send heartbeats, reporting their status.</p><p>Scheduler heartbeat starvation occurs when the scheduler process becomes overloaded and cannot perform its routine checks, including sending heartbeats, within the configured interval. This can happen due to a myriad of reasons: a large number of DAGs to parse, complex DAGs with many tasks, slow queries against the metadata database, insufficient CPU/memory resources, or an overly aggressive parallelism setting for the scheduler.</p><p>When the scheduler misses its heartbeats, Airflow's internal monitoring mechanisms might deem it unhealthy or dead. A direct consequence is the generation of 'zombie tasks'. Zombie tasks are tasks that the scheduler believes should be running (or were running) but are no longer monitored or are in an inconsistent state, often because the scheduler that initiated them became unresponsive. These zombie tasks consume resources and block downstream dependencies, leading to cascading failures across DAGs and significantly degrading the reliability and performance of the Airflow environment.</p>",
    "root_cause": "The Airflow scheduler is overloaded, preventing it from sending heartbeats to the metadata database within the expected interval. Common contributing factors include excessive DAG parsing time, a high number of active DAG runs, slow metadata database performance, insufficient CPU/memory allocated to the scheduler, or an overly aggressive `scheduler_jobs_queue_size` or `max_active_tasks` setting.",
    "bad_code": "```ini\n# airflow.cfg snippet that might contribute to scheduler starvation\n\n[scheduler]\n# default is 5s, but if DB is slow or load is high, this might be too aggressive\njob_heartbeat_sec = 5\n\n# default is -1 (infinite), but if too many tasks are submitted at once, scheduler can choke\nscheduler_jobs_queue_size = -1\n\n# default is 16, but with hundreds/thousands of DAGs, this might be too low to keep up\ndag_dir_list_interval = 300\n\n# default is 0.5 (disabled), increasing this too much can cause scheduler to get stuck\n# in parsing if there are many complex DAGs\nparsing_processes = 2 # Enabling too many parsing processes without enough CPU/memory\n\n# Overly complex or numerous DAGs leading to long parsing times:\n# No direct 'bad code', but a conceptual representation:\n# for _ in range(1000):\n#    with DAG(f'my_complex_dag_{_}', ...) as dag:\n#        # ... hundreds of tasks ...\n```",
    "solution_desc": "Mitigating scheduler heartbeat starvation involves a multi-pronged approach: optimizing DAGs, tuning Airflow configuration, and ensuring adequate infrastructure resources. Reduce DAG parsing time by simplifying DAG files, moving complex logic out of the top level, and increasing `dag_dir_list_interval`. Optimize the metadata database with proper indexing and sufficient resources. Scale the scheduler horizontally (using Kubernetes or Celery setups for multiple schedulers) or vertically (more CPU/memory). Tune `scheduler_heartbeat_sec` to be more forgiving if network latency is an issue, but don't mask underlying performance problems. Regularly clean up old DAG runs and task instances to keep the metadata database lean. Monitor scheduler logs for warnings about missed heartbeats or slow operations.",
    "good_code": "```ini\n# airflow.cfg snippet for improved scheduler robustness\n\n[scheduler]\n# Increase heartbeat interval slightly if network/DB latency is a known issue,\n# but avoid making it too long as it delays problem detection.\njob_heartbeat_sec = 10\n\n# Limit the number of jobs the scheduler pushes to the queue to avoid overload.\n# Adjust based on your worker capacity and typical task duration.\nscheduler_jobs_queue_size = 500\n\n# Increase DAG directory list interval if you have many static DAGs and few changes,\n# reducing constant disk I/O and parsing overhead.\ndag_dir_list_interval = 600\n\n# Adjust parsing processes based on CPU cores. Avoid over-provisioning.\n# Ensure sufficient CPU is available for all processes (scheduler, workers, DB).\nparsing_processes = 1 # Keep low if DAGs are very complex and few in count, or scale out if many simple DAGs\n\n# Ensure your database is properly indexed and provisioned.\n\n# Python example for simplifying DAGs to reduce parsing load:\n# Instead of dynamic imports or complex calculations at the top level:\n# Before (bad, executed on every parse): my_data = load_large_config()\n# After (good, lazy loaded or moved to task): from my_module import load_large_config_lazy\n\n# A simple DAG for demonstration, ensuring minimal parsing overhead:\nfrom airflow.models.dag import DAG\nfrom airflow.operators.bash import BashOperator\nfrom datetime import datetime\n\nwith DAG(\n    dag_id='simple_fast_dag',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['optimized'],\n) as dag:\n    start_task = BashOperator(\n        task_id='start',\n        bash_command='echo \"Starting simple task\"',\n    )\n\n    end_task = BashOperator(\n        task_id='end',\n        bash_command='echo \"Finished simple task\"',\n    )\n\n    start_task >> end_task\n```",
    "verification": "Monitor scheduler logs for messages like 'Heartbeat missed' or 'Scheduler running slow'. Check the `task_instance` table in the metadata database for tasks stuck in 'running' state after their workers have died (potential zombies). Use `airflow dags list` to gauge DAG parsing time. Regularly check system metrics (CPU, memory, disk I/O) on the scheduler host. Use tools like `pg_stat_activity` (for PostgreSQL metadata DB) to identify slow queries. Ensure `airflow webserver` shows no unexpected zombie tasks and that DAGs are being scheduled promptly.",
    "date": "2026-09-20",
    "id": 1789891549,
    "type": "error"
});