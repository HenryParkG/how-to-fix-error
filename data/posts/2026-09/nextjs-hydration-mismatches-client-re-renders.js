window.onPostDataLoaded({
    "title": "Next.js: Hydration Mismatches & Client-Side Re-renders",
    "slug": "nextjs-hydration-mismatches-client-re-renders",
    "language": "Next.js",
    "code": "Hydration Mismatch",
    "tags": [
        "Next.js",
        "React",
        "Frontend",
        "TypeScript",
        "Error Fix"
    ],
    "analysis": "<p>Hydration is a core concept in Next.js (and other React server-side rendering frameworks) where the server renders the initial HTML, and then the client-side JavaScript 'attaches' event handlers and state to that pre-rendered HTML. A 'hydration mismatch' occurs when the server-rendered HTML structure or content differs from what the client-side React expects to render. This often manifests as a console warning: <code>Warning: Prop `className` did not match. Server: \"foo\" Client: \"bar\"</code> or <code>Warning: Did not expect server HTML to contain a &lt;div&gt; in &lt;span&gt;</code>. Beyond warnings, it can lead to unexpected UI behavior, re-renders, or even broken interactivity as React attempts to reconcile the discrepancies.</p><p>Common culprits include dynamic content (e.g., current date/time, user-specific data) rendered on the server without proper synchronization on the client, client-only components that render differently or are completely omitted on the server, or browser-specific APIs (like <code>window</code> or <code>localStorage</code>) being accessed during server-side rendering. Conditional rendering based on environment checks (<code>typeof window !== 'undefined'</code>) or user agent can also introduce mismatches if not handled carefully.</p>",
    "root_cause": "Discrepancy between server-rendered HTML and client-side React component output, often due to client-only logic executing on the server or dynamic data not being synchronized.",
    "bad_code": "import React, { useState, useEffect } from 'react';\n\nfunction DynamicContent() {\n  const [isClient, setIsClient] = useState(false);\n\n  useEffect(() => {\n    setIsClient(true);\n  }, []);\n\n  // This will cause a mismatch if 'new Date().toLocaleString()' is different\n  // on server vs client, or if client-only content is rendered.\n  return (\n    <div>\n      <p>Current time (server/client): {new Date().toLocaleString()}</p>\n      {isClient && <p>This content only renders on the client.</p>}\n    </div>\n  );\n}",
    "solution_desc": "<p>To resolve hydration mismatches, ensure that the content rendered by the server is identical to what the client expects on its initial render. For dynamic content like dates or random IDs, generate them on the client-side after hydration. For components that explicitly rely on browser APIs, render them only on the client or delay their rendering until after the component has mounted (i.e., when <code>useEffect</code> runs). Next.js provides mechanisms like <code>useEffect</code> for client-side execution, or you can use a custom hook to manage client-only rendering state. The key is to avoid rendering anything on the server that is inherently client-specific or prone to environmental variance.</p><p>For components that truly need to be client-only, consider dynamically importing them with <code>next/dynamic</code> and setting <code>ssr: false</code>. For data-dependent mismatches, ensure data fetching strategies (like <code>getServerSideProps</code> or <code>getStaticProps</code>) provide consistent data for the initial render, or manage client-side fetching in <code>useEffect</code> for highly dynamic data.</p>",
    "good_code": "import React, { useState, useEffect } from 'react';\nimport dynamic from 'next/dynamic';\n\n// Option 1: Render client-only content after mount\nfunction ClientOnlyTime() {\n  const [currentTime, setCurrentTime] = useState('');\n\n  useEffect(() => {\n    setCurrentTime(new Date().toLocaleString());\n  }, []);\n\n  return <p>Current time (client-rendered): {currentTime}</p>;\n}\n\n// Option 2: Dynamically import a component with SSR disabled\nconst ClientOnlyComponent = dynamic(\n  () => Promise.resolve(ClientOnlyTime), // Or import a separate component file\n  { ssr: false }\n);\n\nfunction DynamicContentFixed() {\n  return (\n    <div>\n      <p>Static content</p>\n      <ClientOnlyTime /> {/* Example of Option 1 */}\n      <ClientOnlyComponent /> {/* Example of Option 2 */}\n    </div>\n  );\n}",
    "verification": "After implementing the fix, inspect your browser's developer console for any hydration mismatch warnings. The warnings should be gone. Verify that the UI behaves as expected and interactive elements are correctly attached without unexpected re-renders or flickering. Test both initial page load and client-side navigation.",
    "date": "2026-09-03",
    "id": 1788440876,
    "type": "error"
});