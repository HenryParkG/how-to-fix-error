window.onPostDataLoaded({
    "title": "Go: Goroutine Leaks & Context Cancellation Fails",
    "slug": "go-goroutine-leaks-context-cancellation-failures",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Concurrency",
        "Context",
        "Go",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks are a common class of resource exhaustion bugs in Go applications. They occur when a goroutine is started but never properly terminated, continuing to consume memory and CPU resources indefinitely. This often happens in concurrent patterns where a producer goroutine sends data on a channel, but the consumer goroutine exits prematurely or stops reading, leaving the producer blocked indefinitely, or vice-versa. A critical aspect of preventing such leaks, especially in network services or long-running tasks, is the proper use and propagation of <code>context.Context</code>. When context cancellation is not respected, child goroutines can outlive their parent operations, leading to accumulation of zombie goroutines and eventual system instability or Out Of Memory (OOM) errors.</p><p>Specifically, cancellation propagation failures arise when a parent context is cancelled, but child goroutines or operations initiated with that context (or derived from it) do not check <code>ctx.Done()</code> or are not designed to respond to cancellation signals. This can lead to blocked network connections, stalled I/O operations, or goroutines holding onto resources even when the original request or process that spawned them has long since completed or timed out.</p>",
    "root_cause": "Goroutines not explicitly checking and responding to <code>context.Context.Done()</code> signals, leading to indefinite blocking on channel operations or long-running I/O/computations that aren't aware of parent context cancellation. This leaves child goroutines alive and consuming resources even after their logical parent operation has finished.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"time\"\n)\n\n// Bad example: goroutine doesn't respect context cancellation\nfunc longRunningTask(ctx context.Context, dataCh chan int) {\n\tdefer fmt.Println(\"longRunningTask exited\")\n\tfor i := 0; ; i++ {\n\t\tselect {\n\t\tcase dataCh <- i:\n\t\t\t// Sending data, but what if consumer stops reading?\n\t\t\t// This 'select' doesn't handle context cancellation on the send path effectively if the channel blocks\n\t\t\ttime.Sleep(100 * time.Millisecond)\n\t\t}\n\t}\n}\n\nfunc main() {\n\tctx, cancel := context.WithCancel(context.Background())\n\n\tdataCh := make(chan int)\n\tgo longRunningTask(ctx, dataCh)\n\n\t// Consumer reads for a short period then stops\n\tgo func() {\n\t\tdefer fmt.Println(\"Consumer exited\")\n\t\tfor i := 0; i < 5; i++ {\n\t\t\tselect {\n\t\t\tcase data := <-dataCh:\n\t\t\t\tfmt.Printf(\"Consumer received: %d\\n\", data)\n\t\t\tcase <-time.After(500 * time.Millisecond):\n\t\t\t\tfmt.Println(\"Consumer timed out waiting for data.\")\n\t\t\t\treturn // Consumer exits, but longRunningTask might still be blocked.\n\t\t\t}\n\t\t}\n\t}()\n\n\ttime.Sleep(2 * time.Second)\n\tcancel() // Cancel the context, but longRunningTask might not notice right away.\n\n\tfmt.Println(\"Main exiting, wait a bit for goroutines...\")\n\ttime.Sleep(1 * time.Second)\n}\n```",
    "solution_desc": "The architectural fix involves consistently propagating <code>context.Context</code> to all goroutines and functions that perform long-running operations or I/O, and crucially, ensuring these goroutines actively listen for and respond to the <code>ctx.Done()</code> channel. For producer-consumer patterns, both sides should include a <code>select</code> statement that checks <code>ctx.Done()</code> alongside their primary channel operations. When <code>ctx.Done()</code> closes, the goroutine should perform necessary cleanup and return. This ensures that when a parent context is cancelled (e.g., due to a client disconnecting, a request timing out, or an application shutdown), all descendant goroutines gracefully terminate, releasing their resources and preventing leaks. For I/O operations, use context-aware versions where available (e.g., <code>net/http</code> client methods with context) or implement cancellation mechanisms manually.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\n// Good example: goroutine respects context cancellation\nfunc longRunningTaskFixed(ctx context.Context, dataCh chan int) {\n\tdefer fmt.Println(\"longRunningTaskFixed exited\")\n\tfor i := 0; ; i++ {\n\t\tselect {\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Println(\"longRunningTaskFixed: Context cancelled, exiting.\")\n\t\t\treturn // Crucial: exit when context is cancelled\n\t\tcase dataCh <- i:\n\t\t\t// Sending data. If dataCh blocks and ctx.Done() is closed, we'll exit.\n\t\t\ttime.Sleep(100 * time.Millisecond)\n\t\t}\n\t}\n}\n\nfunc main() {\n\tfmt.Printf(\"Goroutines at start: %d\\n\", runtime.NumGoroutine())\n\tctx, cancel := context.WithCancel(context.Background())\n\n\tdataCh := make(chan int)\n\tgo longRunningTaskFixed(ctx, dataCh)\n\n\t// Consumer reads for a short period then stops\n\tgo func() {\n\t\tdefer fmt.Println(\"Consumer exited\")\n\t\tfor i := 0; i < 5; i++ {\n\t\t\tselect {\n\t\t\tcase data := <-dataCh:\n\t\t\t\tfmt.Printf(\"Consumer received: %d\\n\", data)\n\t\t\tcase <-time.After(500 * time.Millisecond):\n\t\t\t\tfmt.Println(\"Consumer timed out waiting for data.\")\n\t\t\t\tcancel() // IMPORTANT: Cancel parent context when consumer is done or timed out.\n\t\t\t\treturn\n\t\t\t}\n\t\t}\n\t\tcancel() // IMPORTANT: Cancel parent context when consumer successfully finishes.\n\t}()\n\n\ttime.Sleep(2 * time.Second)\n\t// If consumer didn't cancel, we'd cancel here as a fallback.\n\n\tfmt.Println(\"Main exiting, wait a bit for goroutines...\")\n\ttime.Sleep(1 * time.Second)\n\tfmt.Printf(\"Goroutines at end: %d\\n\", runtime.NumGoroutine())\n}\n```",
    "verification": "To verify the fix, monitor the number of active goroutines before and after the operation using <code>runtime.NumGoroutine()</code>. In the provided example, after the consumer goroutine exits and cancels the context, <code>longRunningTaskFixed</code> should print its exit message and terminate, leading to a stable or decreasing goroutine count. For more complex applications, use Go's pprof tool to analyze goroutine profiles (e.g., <code>go tool pprof http://localhost:6060/debug/pprof/goroutine</code>) and observe if the number of 'live' goroutines stabilizes or returns to expected levels after operations complete or are cancelled. Also, ensure that logs show goroutines gracefully terminating upon context cancellation, rather than running indefinitely.",
    "date": "2026-09-23",
    "id": 1790171982,
    "type": "error"
});