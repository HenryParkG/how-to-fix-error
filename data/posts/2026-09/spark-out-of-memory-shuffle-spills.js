window.onPostDataLoaded({
    "title": "Spark: Out-of-Memory Errors & Shuffle Spills",
    "slug": "spark-out-of-memory-shuffle-spills",
    "language": "Spark",
    "code": "OOM / Shuffle Spill",
    "tags": [
        "Big Data",
        "Performance",
        "Spark",
        "Java",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Spark's distributed processing model relies on partitioning data across executors. However, operations like `groupByKey`, `reduceByKey`, `join`, or `sort` often require shuffling data across the network to bring related data together. When the volume of data shuffled is large, or when the data skew is significant (i.e., a few partitions receive a disproportionately large amount of data), executors can experience Out-of-Memory (OOM) errors.</p><p>OOM errors occur when an executor tries to hold more data in memory than it has been allocated (controlled by `spark.executor.memory`). This can happen during map-side aggregation, shuffle write, or shuffle read stages. When an executor runs out of memory, it typically crashes, leading to task failures and potentially job failure or significant recomputation.</p><p>Shuffle spills are Spark's mechanism to cope with memory pressure during shuffle operations. When intermediate shuffle data exceeds the configured memory buffer, Spark will \"spill\" this data to disk. While spilling prevents OOM errors, excessive spilling can severely degrade performance because disk I/O is orders of magnitude slower than memory access. Frequent and large spills are often a symptom of insufficient memory allocation or inefficient data partitioning strategies.</p>",
    "root_cause": "Insufficient executor memory (`spark.executor.memory`), large partitions due to data skew (e.g., a few keys have many records), inefficient data structures, or complex UDFs consuming excessive memory. Wide transformations causing large data shuffling.",
    "bad_code": "from pyspark.sql import SparkSession\nfrom pyspark.sql.functions import count\n\nspark = SparkSession.builder.appName(\"OOM_Example\").getOrCreate()\n\n# Create a DataFrame with skewed data for demonstration\ndata = [(\"user_A\", 1), (\"user_A\", 2), (\"user_B\", 3)] * 1000000 + \\\n       [(\"user_C\", i) for i in range(1, 10000000)] # Highly skewed key 'user_C'\n\ndf = spark.createDataFrame(data, [\"user_id\", \"value\"])\n\n# This operation will trigger a shuffle and might lead to OOM\n# or significant spills if 'user_id' is very skewed.\nresult = df.groupBy(\"user_id\").agg(count(\"value\").alias(\"count\"))\n\n# Actions like show() or write() will materialize the result\nresult.show()\n\nspark.stop()",
    "solution_desc": "Address OOM errors and shuffle spills by optimizing memory allocation and data partitioning.\n1.  **Increase `spark.executor.memory`**: Allocate more memory to executors if available.\n2.  **Tune `spark.sql.shuffle.partitions`**: Reduce the number of partitions for aggregation/join if they are too small, or increase them to spread data out more, but be mindful of too many small tasks.\n3.  **Handle Data Skew**: Use salting techniques, broadcast joins for small tables, or repartition data strategically. Spark 3.x introduced Adaptive Query Execution (AQE) which can mitigate skew automatically.\n4.  **Optimize Data Structures**: Minimize the use of complex objects in RDDs/DataFrames; prefer primitive types.\n5.  **Predicate Pushdown**: Ensure filters are applied early to reduce data size before shuffles.\n6.  **`spark.memory.fraction` & `spark.memory.storageFraction`**: Adjust these for better memory management between execution and storage.\n7.  **`spark.reducer.maxSizeInFlight`**: Tune for shuffle read behavior.\n8.  **Use `mapPartitions` or `coalesce`**: For specific scenarios, fine-tune partitioning.\n9.  **Prefer `reduceByKey` over `groupByKey`**: `reduceByKey` performs map-side aggregation, reducing data shuffled.",
    "good_code": "from pyspark.sql import SparkSession\nfrom pyspark.sql.functions import count, col, sha2, concat_ws, lit, when, split, sum\nfrom pyspark.sql.types import StructType, StructField, StringType, IntegerType\n\nspark = SparkSession.builder \\\n    .appName(\"OOM_Solution_Example\") \\\n    .config(\"spark.executor.memory\", \"8g\") # Increase executor memory \\\n    .config(\"spark.sql.shuffle.partitions\", \"200\") # Adjust partitions \\\n    .config(\"spark.sql.adaptive.enabled\", \"true\") # Enable AQE for Spark 3.x+\n    .getOrCreate()\n\n# Create a DataFrame with skewed data\nschema = StructType([\n    StructField(\"user_id\", StringType(), True),\n    StructField(\"value\", IntegerType(), True)\n])\ndata = [(\"user_A\", 1), (\"user_A\", 2), (\"user_B\", 3)] * 1000000 + \\\n       [(\"user_C\", i) for i in range(1, 10000000)]\n\ndf = spark.createDataFrame(data, schema)\n\n# Example of salting for highly skewed keys if AQE is not sufficient\n# For 'user_C', add a random salt to spread it across partitions\nSALT_BUCKETS = 10 # Number of salt buckets\ndf_salted = df.withColumn(\"salted_user_id\",\n    when(col(\"user_id\") == lit(\"user_C\"), concat_ws(\"_\", col(\"user_id\"), (sha2(col(\"value\").cast(StringType()), 256) % SALT_BUCKETS).cast(StringType())))\n    .otherwise(col(\"user_id\"))\n)\n\n# Perform aggregation on the salted key, then unsalt and aggregate again if needed\nintermediate_result = df_salted.groupBy(\"salted_user_id\").agg(count(\"value\").alias(\"count\"))\n\n# If original user_id is needed, unsalt and aggregate\nfinal_result = intermediate_result.withColumn(\"user_id\",\n    split(col(\"salted_user_id\"), \"_\").getItem(0)\n).groupBy(\"user_id\").agg(sum(\"count\").alias(\"final_count\"))\n\nfinal_result.show()\n\nspark.stop()",
    "verification": "Monitor Spark UI during job execution, specifically the \"Stages\" tab. Look for \"Spill (Memory)\", \"Spill (Disk)\" metrics in task details. Excessive disk spills (e.g., orders of magnitude larger than memory spills) indicate memory pressure. Check executor logs for `java.lang.OutOfMemoryError` messages. Use Ganglia or other cluster monitoring tools to observe CPU, memory, and disk I/O on executor nodes. Analyze the DAG visualization for wide transformations and data shuffle sizes. Leverage Spark History Server to compare metrics across different configurations.",
    "date": "2026-09-04",
    "id": 1788526943,
    "type": "error"
});