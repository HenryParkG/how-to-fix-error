window.onPostDataLoaded({
    "title": "WebGPU Device Loss & Buffer Mapping Races",
    "slug": "webgpu-device-loss-buffer-mapping-races",
    "language": "JavaScript, WebGPU",
    "code": "WebGPUFailure",
    "tags": [
        "WebGPU",
        "Graphics",
        "JavaScript",
        "Frontend",
        "Error Fix"
    ],
    "analysis": "<p>WebGPU is a modern web graphics API designed for high performance and low-level control, akin to Vulkan, Metal, or DirectX 12. Unlike WebGL, WebGPU is inherently asynchronous, which provides efficiency but introduces new challenges, particularly around device loss and buffer mapping race conditions.</p><p>'Device loss' refers to scenarios where the underlying GPU or its driver becomes unavailable or crashes. This can happen due to external factors (e.g., driver update, system reset, TDR - Timeout Detection and Recovery on Windows) or due to incorrect or resource-intensive WebGPU usage (e.g., exceeding GPU memory limits). When a device is lost, all associated WebGPU objects become invalid, and any further operations on them will fail. Since WebGPU operations are asynchronous, a device loss event might occur at any time, even while commands are queued or buffers are being mapped, leading to unhandled errors or application crashes if not properly anticipated and handled.</p><p>'Buffer mapping race conditions' arise from the asynchronous nature of `GPUBuffer.mapAsync()`. This function returns a `Promise` that resolves when the buffer's data is ready for CPU access. A common mistake is to attempt to access or modify the buffer's contents immediately after calling `mapAsync()` without awaiting the promise, or to remap/unmap the buffer while a previous `mapAsync()` operation is still pending. Such race conditions can lead to reading stale or uninitialized data, writing to incorrect memory regions, or generating `GPUPipelineError`s because the GPU-side usage of the buffer conflicts with the CPU-side mapping state. These issues are difficult to debug as they might manifest intermittently depending on timing and system load.</p>",
    "root_cause": "Failure to monitor and respond to the `GPUDevice.lost` promise, leading to operations on an invalid device. Incorrect handling of `GPUBuffer.mapAsync()` and `unmap()` operations, such as attempting to access mapped data before the promise resolves or conflicting GPU usage with CPU mapping operations.",
    "bad_code": "```javascript\n// bad_webgpu_code.js\nasync function initBadWebGPU() {\n    const adapter = await navigator.gpu.requestAdapter();\n    const device = await adapter.requestDevice();\n\n    // *** DEVICE LOSS NOT HANDLED ***\n    // If device is lost here, subsequent operations will fail silently or crash.\n\n    const bufferSize = 1024;\n    const buffer = device.createBuffer({\n        size: bufferSize,\n        usage: GPUBufferUsage.MAP_WRITE | GPUBufferUsage.COPY_SRC\n    });\n\n    // *** BUFFER MAPPING RACE CONDITION ***\n    buffer.mapAsync(GPUMapMode.WRITE, 0, bufferSize).then(() => {\n        // This promise resolves asynchronously.\n        // If we try to use the buffer on the GPU here before unmapping,\n        // or if another map/unmap is called, a race can occur.\n        const arrayBuffer = buffer.getMappedRange();\n        const data = new Float32Array(arrayBuffer);\n        data.fill(Math.random());\n        // Missing device.unmap() here, or if we call it too early,\n        // or if GPU usage conflicts.\n        // buffer.unmap(); // Should be here\n    });\n\n    // Imagine other GPU operations on 'buffer' happening immediately after mapAsync\n    // without waiting for the promise to resolve and unmap.\n    // e.g., device.queue.copyBufferToBuffer(buffer, ...) - could conflict.\n}\ninitBadWebGPU();\n```",
    "solution_desc": "To address WebGPU device loss, always monitor the `GPUDevice.lost` promise. This promise resolves when the device is lost, providing a `GPUDeviceLostReason` and allowing the application to gracefully handle the situation, inform the user, or attempt to re-initialize WebGPU. For buffer mapping race conditions, strictly adhere to the asynchronous pattern of `mapAsync()`. Always `await` the `mapAsync()` promise before accessing `getMappedRange()`. After finishing CPU-side modifications, explicitly call `buffer.unmap()` before queueing any GPU commands that use that buffer. Crucially, a buffer cannot be used on the GPU while it is mapped to the CPU, and it cannot be mapped to the CPU multiple times concurrently without careful management. Use separate buffers for CPU-write/GPU-read and GPU-write/CPU-read scenarios if frequent, concurrent access is needed to simplify synchronization. Consider using `queue.writeBuffer` for small, frequent CPU-to-GPU transfers as it avoids mapping overheads and potential races.",
    "good_code": "```javascript\n// good_webgpu_code.js\nasync function initGoodWebGPU() {\n    const adapter = await navigator.gpu.requestAdapter();\n    const device = await adapter.requestDevice();\n\n    // *** CORRECT DEVICE LOSS HANDLING ***\n    device.lost.then((info) => {\n        console.error(`WebGPU device was lost: ${info.message} (${info.reason})`);\n        // Handle device loss gracefully:\n        // 1. Disable rendering, show error message to user.\n        // 2. Attempt to re-initialize WebGPU if appropriate (e.g., driver update).\n        // 3. Clean up resources.\n    });\n\n    const bufferSize = 1024;\n    const buffer = device.createBuffer({\n        size: bufferSize,\n        usage: GPUBufferUsage.MAP_WRITE | GPUBufferUsage.COPY_SRC | GPUBufferUsage.STORAGE\n    });\n\n    // *** CORRECT BUFFER MAPPING ***\n    async function updateBufferData() {\n        // Ensure previous map/unmap operations are complete if chaining.\n        await buffer.mapAsync(GPUMapMode.WRITE, 0, bufferSize);\n        const arrayBuffer = buffer.getMappedRange();\n        const data = new Float32Array(arrayBuffer);\n        data.fill(Math.random() * 100); // Modify data\n        buffer.unmap(); // Unmap immediately after modifications\n\n        // Now the buffer can be used safely on the GPU.\n        // Example: queue a command using the updated buffer\n        // const commandEncoder = device.createCommandEncoder();\n        // ... use buffer in a compute pass or render pass ...\n        // device.queue.submit([commandEncoder.finish()]);\n        console.log(\"Buffer data updated and unmapped.\");\n    }\n\n    await updateBufferData();\n    // Subsequent GPU operations on 'buffer' can now proceed safely.\n}\ninitGoodWebGPU();\n```",
    "verification": "To verify the fix for device loss, you can try to simulate a GPU crash (though this is system-dependent and often involves external tools or driver manipulation). In Chrome, you can sometimes trigger a device loss by navigating to `chrome://gpu`, then finding the 'GPU reset' button (if available for your configuration). Observe if the application catches the `device.lost` event and handles it as expected. For buffer mapping race conditions, implement stress tests involving rapid, successive calls to `mapAsync()` and `unmap()`, interspersing them with GPU commands that use the same buffer. Monitor for `GPUPipelineError`s, unexpected rendering artifacts, or incorrect data. Tools like the browser's developer console will log WebGPU validation errors, which are crucial for catching these types of issues.",
    "date": "2026-09-04",
    "id": 1788507936,
    "type": "error"
});