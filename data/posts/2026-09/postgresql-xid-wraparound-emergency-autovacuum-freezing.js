window.onPostDataLoaded({
    "title": "PostgreSQL XID Wraparound & Emergency Freezing",
    "slug": "postgresql-xid-wraparound-emergency-autovacuum-freezing",
    "language": "PostgreSQL, SQL",
    "code": "XIDWraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "Maintenance",
        "SQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses 32-bit Transaction IDs (XIDs) to manage transaction visibility and data integrity. This design means there are approximately 4 billion unique XIDs available. When the system uses up these XIDs, it 'wraps around' back to zero. To prevent data corruption, PostgreSQL requires that all transaction IDs appearing in any row remain 'younger' than the current transaction ID modulo 2^31. If older XIDs are not 'frozen' (i.e., marked as permanently completed) by the `VACUUM` process, particularly `autovacuum`, the database can enter a critical state known as XID wraparound. When the system detects that the current XID is approaching the wraparound limit (controlled by `autovacuum_freeze_max_age`), it triggers an 'emergency autovacuum freezing' operation.</p><p>During emergency autovacuum freezing, the database becomes highly resource-constrained, prioritizing `VACUUM FREEZE` operations on tables with old un-frozen transactions. This can lead to severe performance degradation, blocking new queries, and potentially rendering the database effectively unusable until the situation is resolved. In extreme cases, if the emergency freezing cannot catch up, PostgreSQL may even shut down to prevent data loss. This scenario highlights the critical importance of regular and efficient `VACUUM` operations.</p>",
    "root_cause": "The primary root cause is insufficient or misconfigured `autovacuum` settings, preventing the timely freezing of old transaction IDs. This can be exacerbated by extremely high transaction rates, very large tables with frequent updates/deletes, tables with high `fillfactor` (less free space for new tuples, requiring more vacuuming), and long-running transactions that prevent `VACUUM` from acquiring locks or cleaning up old tuple versions. Lack of monitoring for transaction ID age (`pg_database.datfrozenxid`) often allows the problem to escalate unnoticed until it reaches a critical state.",
    "bad_code": "```sql\n-- Simulate an issue by querying transaction ID status and autovacuum config\n-- Note: This code won't *cause* XID wraparound, but helps diagnose conditions.\n\n-- Check current XID and estimated remaining transactions\nSELECT \n    (2^31 - (SELECT txid_current())) AS remaining_xids_before_wraparound_warning,\n    (2^32 - (SELECT txid_current())) AS remaining_xids_before_wraparound_hard_limit;\n\n-- Check the age of the oldest unfrozen XID for each database\n-- A large 'age' means the database is approaching the wraparound limit.\nSELECT \n    datname,\n    age(datfrozenxid) AS xid_age,\n    datfrozenxid\nFROM pg_database\nORDER BY xid_age DESC;\n\n-- Check autovacuum settings\nSHOW autovacuum;\nSHOW autovacuum_freeze_max_age;\nSHOW autovacuum_vacuum_cost_delay;\n\n-- A common sign in logs:\n-- ERROR: database is not accepting commands to avoid wraparound data loss in database \"mydb\"\n-- HINT: Stop the postmaster and use a standalone backend to VACUUM that database.\n```",
    "solution_desc": "The key to preventing XID wraparound is ensuring `autovacuum` runs effectively. First, monitor `pg_database.datfrozenxid` and `txid_current()` regularly. Adjust `autovacuum_freeze_max_age` to a lower value (e.g., 200 million transactions) if your transaction rate is very high, prompting `autovacuum` to freeze more frequently. Review `autovacuum_vacuum_cost_delay` and `autovacuum_vacuum_cost_limit` to ensure `autovacuum` isn't too throttled to complete its work in time. For tables particularly prone to bloat or high churn, consider setting table-specific `autovacuum` parameters. Identify and terminate long-running transactions that might be holding back `VACUUM`. In an emergency, connect to the database in single-user mode (if regular connections are blocked) and manually run `VACUUM FREEZE` on the problematic tables or the entire database (e.g., `VACUUM FREEZE;` or `VACUUM FREEZE VERBOSE <table>;`). After resolution, thoroughly review and optimize your `autovacuum` configuration and monitoring strategy.",
    "good_code": "```sql\n-- 1. Adjust autovacuum settings (use ALTER SYSTEM for persistence)\n-- Consider lowering freeze max age for very busy systems\nALTER SYSTEM SET autovacuum_freeze_max_age = '200000000'; -- Default is 200M\n-- Consider reducing cost delay if autovacuum is too slow\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '10ms'; -- Default is 20ms\n-- Increase cost limit if autovacuum needs to do more work per cycle\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = '2000'; -- Default is 200\nSELECT pg_reload_conf(); -- Apply changes after ALTER SYSTEM\n\n-- 2. Manually run VACUUM FREEZE on specific problematic tables if needed\n-- Use VERBOSE to see progress\nVACUUM FREEZE VERBOSE my_large_table;\n\n-- 3. Identify long-running transactions (if they prevent vacuuming)\nSELECT \n    pid,\n    age(query_start, now()) AS duration,\n    state,\n    query\nFROM pg_stat_activity\nWHERE \n    state != 'idle' \n    AND query NOT ILIKE '%pg_stat_activity%'\nORDER BY duration DESC;\n\n-- 4. Set table-specific autovacuum parameters for critical tables\nALTER TABLE my_high_churn_table SET (autovacuum_vacuum_scale_factor = 0.05, autovacuum_freeze_table_age = 100000000);\n```",
    "verification": "After applying fixes, regularly monitor the `age(datfrozenxid)` for all databases using the query provided in the 'bad_code' section. Ensure that the `xid_age` remains well below `autovacuum_freeze_max_age`. Check `pg_stat_activity` for any long-running transactions that could impede `autovacuum`. Review PostgreSQL logs for messages indicating successful `autovacuum` runs and the absence of 'database is not accepting commands to avoid wraparound' errors. Over time, observe performance metrics to confirm that database operations are no longer being throttled by emergency freezing.",
    "date": "2026-09-23",
    "id": 1790130509,
    "type": "error"
});