window.onPostDataLoaded({
    "title": "WebGPU: Pipeline Stalls & Unsynchronized Buffer Race Hazards",
    "slug": "webgpu-pipeline-stalls-buffer-mapping-race-hazards",
    "language": "WebGPU",
    "code": "GPU Stalls / Race Hazards",
    "tags": [
        "WebGPU",
        "TypeScript",
        "Frontend",
        "Error Fix"
    ],
    "analysis": "<p>WebGPU is a powerful web standard that offers low-level access to GPU capabilities, enabling high-performance graphics and compute on the web. However, its asynchronous nature and explicit control over GPU resources can lead to common pitfalls: pipeline compilation stalls and unsynchronized buffer mapping race hazards.</p><p><b>Pipeline Compilation Stalls:</b> Creating render or compute pipelines with <code>device.createRenderPipeline()</code> or <code>device.createComputePipeline()</code> is a relatively expensive operation as it involves compiling shaders and configuring GPU state. If these operations are performed frequently, especially within the main render loop, they can introduce significant stalls. The browser's JavaScript thread might be blocked, or the GPU driver might be busy compiling, leading to dropped frames, stuttering animations, and a poor user experience. This is exacerbated on less powerful hardware or with complex shaders.</p><p><b>Unsynchronized Buffer Mapping Race Hazards:</b> WebGPU buffers can be mapped for CPU access using <code>buffer.mapAsync()</code>, allowing JavaScript to read from or write to GPU memory. This operation is asynchronous. The critical hazard arises when developers attempt to access the buffer's mapped range (via <code>getMappedRange()</code>) before the <code>mapAsync</code> Promise resolves, or after the buffer has been unmapped (<code>buffer.unmap()</code>). Additionally, mapping a buffer that is currently being used by the GPU (e.g., as part of an active render pass) will cause errors or unexpected behavior, as the CPU and GPU would be racing for access to the same memory region without proper synchronization.</p>",
    "root_cause": "1. **Pipeline Stalls:** Repeated, synchronous, or just-in-time creation of new GPU pipelines within a performance-critical loop (e.g., render loop), leading to blocking GPU driver calls or main thread stalls. 2. **Buffer Race Hazards:** Accessing mapped buffer memory without awaiting the `mapAsync` promise resolution, or attempting to map a buffer that is still bound and in use by the GPU, leading to undefined behavior or `GPUValidationError`.",
    "bad_code": "```typescript\n// ---------------- BAD CODE: Pipeline Compilation Stall ----------------\nfunction renderLoop() {\n    // DON'T DO THIS: Creating pipeline every frame or frequently\n    const pipeline = device.createRenderPipeline({\n        layout: 'auto',\n        vertex: { module: shaderModule, entryPoint: 'vs_main' },\n        fragment: { module: shaderModule, entryPoint: 'fs_main', targets: [{ format: presentationFormat }] },\n        primitive: { topology: 'triangle-list' },\n    });\n    // ... use pipeline ...\n    requestAnimationFrame(renderLoop);\n}\n\n// ---------------- BAD CODE: Unsynchronized Buffer Mapping ----------------\nasync function processBufferData(buffer: GPUBuffer, device: GPUDevice) {\n    // DON'T DO THIS: Accessing mapped range before mapAsync resolves or not unmapping\n    buffer.mapAsync(GPUMapMode.WRITE, 0, buffer.size);\n    // This will likely throw an error or contain stale data because mapAsync is not awaited.\n    const arrayBuffer = buffer.getMappedRange(); \n    new Float32Array(arrayBuffer).fill(0.5); // Data might not be written or readable correctly\n    buffer.unmap(); // Unmapping too early, before GPU has a chance to read/write, or before CPU is done\n\n    // DON'T DO THIS: Mapping a buffer while it's in use by an active command encoder\n    const encoder = device.createCommandEncoder();\n    encoder.setComputePipeline(computePipeline);\n    encoder.setBuffer(0, buffer);\n    device.queue.submit([encoder.finish()]);\n    // Attempting to map buffer immediately after submitting it to the queue (GPU still using it)\n    await buffer.mapAsync(GPUMapMode.READ);\n}\n```",
    "solution_desc": "For pipeline stalls, the solution is to pre-compile pipelines as early as possible (e.g., during application startup or resource loading) and reuse them. Maintain a cache of commonly used pipelines. For dynamic scenes requiring many pipelines, explore techniques like pipeline factories and asynchronous compilation where supported. For buffer mapping race hazards, strictly adhere to the asynchronous contract of <code>mapAsync</code>: always await its resolution before accessing <code>getMappedRange()</code>. Ensure that a buffer is neither currently mapped nor in active use by the GPU (i.e., submitted to the queue in a command encoder) when attempting to map it. Use fences or carefully sequence operations to ensure GPU completion before CPU readback.",
    "good_code": "```typescript\n// ---------------- GOOD CODE: Pre-compile and Reuse Pipelines ----------------\nlet cachedPipeline: GPURenderPipeline | null = null;\n\nasync function initializeGraphics(device: GPUDevice, presentationFormat: GPUTextureFormat, shaderModule: GPUShaderModule) {\n    // Pre-compile pipeline once during initialization\n    cachedPipeline = await device.createRenderPipelineAsync({\n        layout: 'auto',\n        vertex: { module: shaderModule, entryPoint: 'vs_main' },\n        fragment: { module: shaderModule, entryPoint: 'fs_main', targets: [{ format: presentationFormat }] },\n        primitive: { topology: 'triangle-list' },\n    });\n    console.log('Pipeline compiled and cached!');\n}\n\nfunction renderLoop(device: GPUDevice, context: GPUCanvasContext, textureView: GPUTextureView) {\n    if (!cachedPipeline) {\n        console.error('Pipeline not initialized!');\n        return;\n    }\n    const commandEncoder = device.createCommandEncoder();\n    const passEncoder = commandEncoder.beginRenderPass({\n        colorAttachments: [{ view: textureView, loadOp: 'clear', storeOp: 'store' }],\n    });\n    passEncoder.setPipeline(cachedPipeline); // Reuse the pre-compiled pipeline\n    // ... draw calls ...\n    passEncoder.end();\n    device.queue.submit([commandEncoder.finish()]);\n    requestAnimationFrame(() => renderLoop(device, context, textureView));\n}\n\n// ---------------- GOOD CODE: Synchronized Buffer Mapping ----------------\nasync function processBufferDataSynchronized(buffer: GPUBuffer, device: GPUDevice) {\n    // Wait for mapAsync to resolve before accessing range\n    await buffer.mapAsync(GPUMapMode.WRITE, 0, buffer.size);\n    const arrayBuffer = buffer.getMappedRange();\n    new Float32Array(arrayBuffer).fill(0.5);\n    buffer.unmap(); // Unmap after CPU operations are complete\n\n    // Example of reading data after GPU has finished using it:\n    // 1. Submit GPU commands that use the buffer\n    const encoder = device.createCommandEncoder();\n    // ... GPU commands using 'buffer' ...\n    device.queue.submit([encoder.finish()]);\n\n    // 2. Wait for GPU to finish work on the queue before attempting to read back\n    // In WebGPU, this is implicitly handled by mapAsync's queue protection\n    // when mapping a buffer that has been submitted to the queue. The driver\n    // will ensure GPU work completes before CPU access is granted.\n    await buffer.mapAsync(GPUMapMode.READ);\n    const readArrayBuffer = buffer.getMappedRange();\n    const data = new Float32Array(readArrayBuffer);\n    console.log('Read back data:', data);\n    buffer.unmap();\n}\n```",
    "verification": "Use browser developer tools (e.g., Chrome's Performance tab, Firefox's WebGPU Inspector) to profile frame rates and GPU activity. Look for spikes in 'GPU Compile' or 'Scripting' time during rendering. Check the console for `GPUValidationError` messages related to buffer usage. Ensure visual output is correct and free of artifacts, which can indicate corrupted data from race conditions. For buffer mapping, run tests that read/write data to buffers after GPU operations and verify data integrity.",
    "date": "2026-09-20",
    "id": 1789891550,
    "type": "error"
});