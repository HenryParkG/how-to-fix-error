window.onPostDataLoaded({
    "title": "PostgreSQL: Transaction ID Wraparound & Autovacuum",
    "slug": "postgresql-transaction-id-wraparound-autovacuum-misconfiguration",
    "language": "PostgreSQL",
    "code": "XIDWraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "SQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit transaction ID (XID) for managing MVCC (Multi-Version Concurrency Control). This XID is a monotonic counter assigned to every transaction. With 2<sup>32</sup> possible values, this number might seem huge, but under high transaction loads, it can eventually wrap around. If a transaction ID wraps around, older transactions would appear to be in the future, leading to data corruption and potential data loss. To prevent this, PostgreSQL marks old transactions as 'frozen' through the <code>VACUUM</code> process.</p><p>The <code>autovacuum</code> daemon is responsible for automatically running <code>VACUUM</code> and <code>ANALYZE</code> operations to keep the database healthy, including freezing old transaction IDs. However, if <code>autovacuum</code> is misconfigured (e.g., disabled, too infrequent, or its thresholds are too high) or if the transaction rate is exceptionally high, it might not be able to freeze XIDs fast enough. When the oldest unfrozen transaction ID approaches 2 billion, PostgreSQL enters an emergency 'anti-wraparound' mode, which can significantly degrade performance and eventually lead to the database shutting down to prevent data loss, requiring manual intervention to run a full <code>VACUUM FREEZE</code>.</p>",
    "root_cause": "The oldest transaction ID (<code>datfrozenxid</code>) in a database gets too old, approaching the 2-billion transaction threshold. This is primarily caused by insufficient <code>VACUUM</code> operations, often due to misconfigured <code>autovacuum</code> settings (e.g., high <code>autovacuum_vacuum_threshold</code>, large <code>autovacuum_vacuum_scale_factor</code>, or an excessively large <code>autovacuum_freeze_max_age</code>) or by extremely high transaction rates that outpace autovacuum's ability to process and freeze old transactions.",
    "bad_code": "Disabling or severely restricting autovacuum in <code>postgresql.conf</code>:\n<pre><code># postgresql.conf snippet\nautovacuum = off\n# OR\nautovacuum_vacuum_cost_delay = 1000 # very high delay, makes autovacuum slow\nautovacuum_vacuum_threshold = 5000000 # very high threshold, infrequent vacuums\nautovacuum_freeze_max_age = 2000000000 # dangerously close to wraparound limit\n</code></pre>\nThese settings would prevent autovacuum from running frequently enough to freeze old XIDs, making wraparound inevitable under load.",
    "solution_desc": "Properly tune <code>autovacuum</code> parameters in <code>postgresql.conf</code> to match your workload. Key parameters include <code>autovacuum_vacuum_scale_factor</code>, <code>autovacuum_vacuum_threshold</code>, and critically, <code>autovacuum_freeze_max_age</code>. Ensure `autovacuum` is enabled (`autovacuum = on`). Monitor the transaction ID age of your databases using `age(datfrozenxid)` from `pg_database`. Set alerts to warn when XID age approaches critical levels (e.g., 1.5 billion transactions). If a database is in danger, a manual `VACUUM FREEZE` on problematic tables or the entire database can temporarily alleviate the issue. Consider increasing maintenance_work_mem and effective_cache_size for autovacuum to run more efficiently.",
    "good_code": "Recommended <code>postgresql.conf</code> settings for autovacuum and XID management:\n<pre><code># postgresql.conf snippet\nautovacuum = on # Ensure autovacuum is enabled\nautovacuum_max_workers = 3 # Adjust based on CPU cores\nautovacuum_vacuum_cost_delay = 10ms # Lower for more aggressive vacuuming\nautovacuum_vacuum_scale_factor = 0.05 # Vacuum tables sooner (5% of tuples updated/deleted)\nautovacuum_freeze_max_age = 500000000 # Ensure freezing happens well before the limit\nmaintenance_work_mem = 256MB # Increase for better vacuum performance\n</code></pre>\nMonitor and adjust these values based on database activity and `pg_stat_activity` insights.",
    "verification": "Regularly monitor the XID age of all databases using the SQL query: `SELECT datname, age(datfrozenxid) FROM pg_database;`. The `age` should ideally remain well below `autovacuum_freeze_max_age`. Observe `pg_stat_activity` for active autovacuum processes and check `pg_stat_user_tables` for individual table vacuum statistics (e.g., `last_autovacuum`, `autovacuum_count`). Ensure autovacuum is running regularly and completing its tasks. If `age(datfrozenxid)` consistently grows or exceeds thresholds, further tuning or manual intervention might be required.",
    "date": "2026-09-18",
    "id": 1789697691,
    "type": "error"
});