window.onPostDataLoaded({
    "title": "Spark Shuffle Spill Disk Thrashing & OOM Cascades",
    "slug": "apache-spark-shuffle-spill-disk-thrashing-driver-heartbeat-starvation-oom-cascades",
    "language": "Java",
    "code": "SparkOOM",
    "tags": [
        "Java",
        "Spark",
        "Big Data",
        "Distributed Systems",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Apache Spark's shuffle operations (e.g., <code>groupByKey</code>, <code>join</code>, <code>sort</code>) are notorious for being resource-intensive. During a shuffle, data needs to be exchanged between different partitions, often across different executors. If the intermediate shuffle data for a task exceeds the memory available to the executor, Spark will 'spill' this data to local disk. While spilling is a necessary mechanism to prevent OOMs, excessive spilling leads to 'disk thrashing'.</p><p>Disk thrashing occurs when executors spend most of their time reading and writing to disk rather than performing actual computation, severely degrading performance. This high I/O wait and CPU utilization can cause executors to become unresponsive. An unresponsive executor might fail to send timely heartbeats to the Spark driver. The driver, perceiving a lack of activity, will then mark the executor as failed and re-schedule its tasks on other executors.</p><p>This re-scheduling can trigger an 'OOM cascade'. If the remaining executors are already struggling with memory or I/O pressure, receiving additional tasks from failed executors will push them over the edge, leading to their own Out-Of-Memory errors. This creates a vicious cycle where executors continuously fail, tasks are retried, and the cluster eventually collapses or the job fails, despite seemingly having enough total resources.</p>",
    "root_cause": "Insufficient executor memory for shuffle operations, leading to excessive disk spilling and thrashing. This causes executors to become unresponsive, miss driver heartbeats, and fail, initiating a cascade of task retries and OOMs on other executors.",
    "bad_code": "/* spark-submit command */\nspark-submit \\\n  --master yarn \\\n  --deploy-mode cluster \\\n  --executor-memory 4g \\\n  --num-executors 50 \\\n  --conf \"spark.executor.cores=2\" \\\n  --conf \"spark.sql.shuffle.partitions=200\" \\\n  your-spark-app.jar\n\n/* Inside Spark application (Scala/Python) */\nval largeDF = spark.read.parquet(\"large_dataset.parquet\")\nval shuffledDF = largeDF.groupByKey(row => row.getAs[String](\"high_cardinality_key\")).count()\nshuffledDF.write.parquet(\"output.parquet\")\n",
    "solution_desc": "The primary solution is to provide adequate memory to executors for shuffle operations. Increase <code>spark.executor.memory</code> significantly, considering the size of your shuffle data. Tune <code>spark.memory.fraction</code> and <code>spark.memory.storageFraction</code> (defaults are usually good, but adjustments might be needed for specific workloads). Optimize shuffle partition count via <code>spark.sql.shuffle.partitions</code>: too few can lead to large partitions and spills, too many can increase network overhead. Avoid data-skew if possible (e.g., by salting keys before a <code>join</code> or <code>groupByKey</code>). Use more memory-efficient transformations like <code>reduceByKey</code> instead of <code>groupByKey</code> when aggregation is needed. Monitor the Spark UI (Storage tab, Stages tab for shuffle read/write sizes, GC metrics) and executor logs for signs of disk I/O bottlenecks and OOMs.",
    "good_code": "/* spark-submit command with optimized settings */\nspark-submit \\\n  --master yarn \\\n  --deploy-mode cluster \\\n  --executor-memory 16g \\\n  --num-executors 50 \\\n  --conf \"spark.executor.cores=4\" \\\n  --conf \"spark.sql.shuffle.partitions=1000\" \\\n  --conf \"spark.driver.memory=8g\" \\\n  --conf \"spark.driver.maxResultSize=4g\" \\\n  --conf \"spark.yarn.executor.memoryOverhead=4096\" \\\n  your-spark-app.jar\n\n/* Inside Spark application (Scala/Python) - using reduceByKey */\nval largeDF = spark.read.parquet(\"large_dataset.parquet\")\nimport spark.implicits._\nval shuffledDF = largeDF.map(row => (row.getAs[String](\"high_cardinality_key\"), 1))\n                         .rdd.reduceByKey(_ + _)\n                         .toDF(\"key\", \"count\")\nshuffledDF.write.parquet(\"output.parquet\")\n",
    "verification": "Monitor the Spark UI during job execution. Specifically, check the 'Storage' tab for RDD/DataFrame persistence levels and whether data is spilling to disk. In the 'Stages' tab, examine 'Shuffle Read' and 'Shuffle Write' metrics for any stage; look for high 'Spill (Memory)' and 'Spill (Disk)' values. Check executor logs for 'OutOfMemoryError' or warnings about excessive GC. Observe the overall job completion time and resource utilization (CPU, memory, disk I/O) on your cluster. Optimized jobs should show significantly less disk I/O and fewer executor failures.",
    "date": "2026-09-07",
    "id": 1788746212,
    "type": "error"
});