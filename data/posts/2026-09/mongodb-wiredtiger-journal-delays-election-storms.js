window.onPostDataLoaded({
    "title": "WiredTiger Journal Delays & Election Storms",
    "slug": "mongodb-wiredtiger-journal-delays-election-storms",
    "language": "Go",
    "code": "MDB_ELECTION_STORM",
    "tags": [
        "MongoDB",
        "WiredTiger",
        "Replica Set",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses a write-ahead log (WAL), or journal, to ensure data durability and atomicity. Changes are first written to the journal before being applied to data files. Journal flush delays occur when the rate of incoming writes exceeds the storage subsystem's ability to sync journal entries to disk. This backlog can lead to `fdatasync()` calls blocking the write path, increasing write latency, and potentially causing the MongoDB primary to become unresponsive.</p><p>In a replica set, the primary node periodically sends \"heartbeats\" and \"oplog\" entries (a record of data modifications) to secondary nodes. If the primary experiences significant journal flush delays, its responsiveness can degrade, leading to delayed heartbeats or inability to send oplog entries in a timely manner. Secondary nodes, unable to receive timely updates or heartbeats from the primary, might deem the primary unreachable or unhealthy. This can trigger a replica set election, where secondaries attempt to elect a new primary. If the original primary recovers its responsiveness but still struggles with flushes, or if multiple secondaries independently detect issues, a cascade of elections, known as an \"election storm,\" can occur. This results in frequent primary changes, service disruptions, and data inconsistency windows, severely impacting application availability.</p>",
    "root_cause": "The fundamental root cause is an I/O bottleneck on the primary's storage subsystem, specifically for journal writes. This can be due to: insufficient IOPS from the underlying disk, high latency storage, misconfigured filesystem settings (e.g., ext4, XFS not optimized for MongoDB's I/O), excessive disk contention from other processes, or a working set exceeding available RAM, leading to constant paging. These journal flush delays then cause replica set communication timeouts, triggering election storms.",
    "bad_code": "```yaml\n# Problematic MongoDB configuration / application pattern (conceptual)\n\n# 1. MongoDB without dedicated, high-IOPS storage:\n#    e.g., Running on a VM with general-purpose cloud disk that has limited IOPS.\n#    This causes journal flush delays under heavy write load.\n#    Example: AWS gp2 EBS volumes with default burst limits.\n\n# 2. Default MongoDB journal commit interval (e.g., 100ms) combined with slow I/O:\n#    This means every 100ms the system *must* flush, and if it can't, it backs up.\n#    Monitor: db.serverStatus().wiredTiger.journal_flush_to_disk_ns\n\n# 3. Application performing very high-frequency, small, unbatched writes:\n#    Example (Go pseudo-code):\n#    for i := 0; i < 10000; i++ {\n#        collection.InsertOne(context.Background(), BSON{\"data\": \"value\"})\n#    }\n#    Each `InsertOne` requires a journal write. If not batched, it puts immense\n#    pressure on the journal and the storage system.\n\n# 4. Replica Set configuration with aggressively reduced election timeouts:\n#    While MongoDB's defaults are usually good, if `electionTimeoutMillis`\n#    is reduced without understanding the implications, minor primary\n#    stalls can quickly trigger elections.\n#    Monitor: rs.conf().settings.electionTimeoutMillis (default is 10 seconds)\n```",
    "solution_desc": "Addressing journal flush delays and election storms requires a multi-pronged approach focusing on I/O performance, MongoDB configuration, and application write patterns:\n1.  **Upgrade Storage**: Use SSDs with guaranteed high IOPS (e.g., AWS io1/io2 EBS, Azure Premium SSD, GCP Persistent Disk SSD) or local NVMe SSDs. Consider separating journal files onto a different physical disk.\n2.  **Filesystem Optimization**: Use XFS (recommended on Linux) mounted with `noatime`. Tune `vm.swappiness=1` and set Transparent Huge Pages (THP) to `never`.\n3.  **MongoDB Configuration**: Increase `storage.wiredTiger.engineConfig.journalCompressor` to `snappy` or disable it. Ensure `replication.replSetElectionTimeoutMillis` is at a reasonable value (default 10s). Ensure oplog size is sufficient.\n4.  **Application-level Optimizations**: **Batch writes** using `bulkWrite` operations to significantly reduce the number of journal flushes. Use appropriate write concerns, prioritizing `w:majority` with `j:true` for critical data. For extremely high write loads, implement **sharding**.\n5.  **Monitoring**: Use `mongostat`, `mongotop`, `db.serverStatus().wiredTiger.journal` (especially `flush_to_disk_ns`), `db.serverStatus().repl`, and OS-level tools like `iostat` to pinpoint bottlenecks.",
    "good_code": "```go\n// Improved application code: Batching writes to reduce journal flush pressure\npackage main\n\nimport (\n\t\"context\"\n\t\"log\"\n\t\"time\"\n\n\t\"go.mongodb.org/mongo-driver/bson\"\n\t\"go.mongodb.org/mongo-driver/mongo\"\n\t\"go.mongodb.org/mongo-driver/mongo/options\"\n)\n\nfunc main() {\n\tclient, err := mongo.Connect(context.TODO(), options.Client().ApplyURI(\"mongodb://localhost:27017\"))\n\tif err != nil {\n\t\tlog.Fatal(err)\n\t}\n\tdefer client.Disconnect(context.TODO())\n\n\tcollection := client.Database(\"mydatabase\").Collection(\"mycollection\")\n\n\tlog.Println(\"Starting batched inserts...\")\n\tconst batchSize = 1000\n\tvar documents []mongo.WriteModel\n\n\tfor i := 0; i < 100000; i++ {\n\t\tdocuments = append(documents, mongo.NewInsertOneModel(bson.M{\"value\": i, \"timestamp\": time.Now()}))\n\n\t\tif len(documents) >= batchSize {\n\t\t\t// Use ordered: false for better performance if write order doesn't strictly matter.\n\t\t\t// Set write concern w:majority, j:true for strong durability across the replica set.\n\t\t\tbulkOptions := options.BulkWrite().SetOrdered(false).SetWriteConcern(options.WriteConcern{W: \"majority\", Journal: options.Bool(true)})\n\t\t\t_, err := collection.BulkWrite(context.Background(), documents, bulkOptions)\n\t\t\tif err != nil {\n\t\t\t\tlog.Printf(\"Error during bulk write: %v\", err)\n\t\t\t}\n\t\t\tdocuments = []mongo.WriteModel{} // Reset batch\n\t\t}\n\t}\n\n\t// Flush any remaining documents\n\tif len(documents) > 0 {\n\t\tbulkOptions := options.BulkWrite().SetOrdered(false).SetWriteConcern(options.WriteConcern{W: \"majority\", Journal: options.Bool(true)})\n\t\t_, err := collection.BulkWrite(context.Background(), documents, bulkOptions)\n\t\tif err != nil {\n\t\t\tlog.Printf(\"Error during final bulk write: %v\", err)\n\t\t}\n\t}\n\tlog.Println(\"Finished batched inserts\")\n\n\t// Example of update operation, which can also be batched\n\t// var updateDocs []mongo.WriteModel\n\t// updateDocs = append(updateDocs, mongo.NewUpdateOneModel(\n\t//     bson.M{\"value\": 10},\n\t//     bson.M{\"$set\": bson.M{\"updatedAt\": time.Now()}},\n\t// ))\n\t// collection.BulkWrite(context.Background(), updateDocs)\n}\n```",
    "verification": "After implementing the fixes, rigorously monitor the primary's I/O metrics (IOPS, latency, disk queue depth) and MongoDB-specific journal metrics (`db.serverStatus().wiredTiger.journal`). Observe write latency `storage.wiredTiger.write_latency_histogram` and look for `journal_flush_to_disk_ns` to stabilize at low values. Crucially, monitor replica set status (`rs.status()`) and ensure that no unplanned elections occur. Check `electionCount` in `db.serverStatus().repl.electionMetrics` is minimal or zero over extended periods. Application-level metrics like write throughput and latency should show significant improvement and stability.",
    "date": "2026-09-05",
    "id": 1788573873,
    "type": "error"
});