window.onPostDataLoaded({
    "title": "PostgreSQL XID Wraparound Lockouts & Autovacuum Starvation",
    "slug": "postgresql-xid-wraparound-lockouts-autovacuum-starvation",
    "language": "PostgreSQL, SQL",
    "code": "XID Wraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit Transaction ID (XID) for MVCC (Multi-Version Concurrency Control). Every transaction is assigned a unique XID. While 32 bits offer 4 billion IDs, these IDs eventually 'wrap around' to zero. PostgreSQL handles this by marking old transactions as 'frozen' to ensure they appear committed to all future transactions, preventing data corruption. The `autovacuum` process is critical for performing this 'freezing' and reclaiming space. If autovacuum cannot keep up with transaction generation or is misconfigured, the oldest unfrozen XID (tracked by `datfrozenxid` per database) can approach the wraparound point (2 billion XIDs away from the current XID). When this critical threshold is breached, PostgreSQL enters an emergency state, effectively locking out new transactions and forcing aggressive vacuuming to prevent data loss. This can lead to severe service outages, as the database becomes practically read-only or completely unresponsive until the XID crisis is resolved.</p>",
    "root_cause": "The specific technical reason for failure is the `datfrozenxid` (the oldest unfrozen transaction ID in a database) nearing the 2 billion transaction threshold from the current transaction ID. This happens because the `autovacuum` daemon, responsible for freezing old tuples, either falls behind due to high transaction volume, inefficient vacuuming (e.g., too few workers, `autovacuum_vacuum_cost_delay` set too high), or specific tables rarely being updated/vacuumed. Insufficient `vacuum_freeze_min_age` or `autovacuum_freeze_max_age` settings can also exacerbate the issue by delaying freezing operations.",
    "bad_code": "# Example of a non-optimal postgresql.conf setting\n# that could contribute to autovacuum starvation:\nautovacuum = on\n# Delay autovacuum for 200ms after each page cleaned.\n# Too high, making autovacuum slow.\nautovacuum_vacuum_cost_delay = 200ms\n\n# Or, a high-volume application that frequently inserts/updates\n# but does not allow autovacuum to run effectively, e.g.,\n# frequent large writes followed by immediate read workload\n# SELECT pg_stat_activity WHERE backend_type = 'autovacuum';\n# This query might show autovacuum processes stuck or absent,\n# indicating starvation.",
    "solution_desc": "The primary solution is proactive monitoring and aggressive autovacuum tuning. Monitor `pg_database.datfrozenxid` and `pg_database.relfrozenxid` for tables. Ensure `autovacuum` is enabled and configured with sufficient resources (e.g., `autovacuum_max_workers`, `autovacuum_vacuum_cost_delay`, `autovacuum_vacuum_cost_limit`). For immediate relief in an emergency, force a manual `VACUUM FREEZE` on the affected database or tables. In severe cases, PostgreSQL might require a restart into single-user mode to run `VACUUM FREEZE` on system catalogs. Configure `vacuum_freeze_table_age` and `autovacuum_freeze_max_age` appropriately. Long-running transactions can also block autovacuum; identify and resolve them. Additionally, consider partitioning large tables to reduce vacuum scope.",
    "good_code": "# Optimized postgresql.conf settings for better autovacuum performance:\nautovacuum = on\n# Reduce delay for faster page cleaning by autovacuum\nautovacuum_vacuum_cost_delay = 10ms\n# Increase max workers to handle more concurrent vacuuming\nautovacuum_max_workers = 5\n# Adjust the age at which tuples are frozen (default is 200M, reduce for high load)\nautovacuum_freeze_max_age = 150000000\n\n# To check XID status:\nSELECT datname, age(datfrozenxid) AS xid_age FROM pg_database;\n\n# To manually freeze a specific table in an emergency:\nVACUUM FREEZE VERBOSE ANALYZE my_table;\n\n# To manually freeze the entire database (use with caution, can block others):\nVACUUM FREEZE VERBOSE ANALYZE;",
    "verification": "Monitor the `age(datfrozenxid)` value for all databases using `SELECT datname, age(datfrozenxid) FROM pg_database;`. This value should remain well below the `autovacuum_freeze_max_age` threshold (e.g., typically below 1.5 billion XIDs). Monitor `pg_stat_activity` for `autovacuum` processes to ensure they are actively running and not stuck. Check PostgreSQL logs for autovacuum-related messages and warnings about XID wraparound. After applying fixes, observe a stable `datfrozenxid` age and reduced occurrence of XID-related warnings.",
    "date": "2026-09-13",
    "id": 1789285780,
    "type": "error"
});