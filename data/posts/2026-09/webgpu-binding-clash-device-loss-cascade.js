window.onPostDataLoaded({
    "title": "WebGPU Pipeline Binding Clashes & Device Loss",
    "slug": "webgpu-binding-clash-device-loss-cascade",
    "language": "TypeScript",
    "code": "GPUPipelineError",
    "tags": [
        "WebGPU",
        "Graphics",
        "TypeScript",
        "Error Fix"
    ],
    "analysis": "<p>WebGPU strictly enforces pipeline layout and resource compatibility before submitting command buffers to the GPU queue. When shaders define bind group interfaces via <code>@group(n) @binding(m)</code>, the corresponding <code>GPUPipelineLayout</code> must strictly mirror the resource types, buffer binding visibility, and texture dimensions. When applications rely on implicit pipeline layouts generated via <code>layout: 'auto'</code> across multiple compute or render passes, subtle mismatches in binding visibility (e.g., storage buffer marked read-only in WGSL but write-enabled in layout) trigger WebGPU validation errors.</p><p>Because WebGPU operates asynchronously, unhandled validation errors propagate up to the context boundary. If an application attempts to execute commands on a pipeline built upon failed validation, or submits mismatched bind groups inside a render pass, the underlying browser graphics stack marks the logical <code>GPUDevice</code> as lost (invoking <code>device.lost</code> with reason <code>'destroyed'</code> or <code>'unknown'</code>). Once device loss occurs, all subsequent queue submissions, texture allocations, and pipeline creations fail instantly across the entire application.</p>",
    "root_cause": "Resource contract mismatches between WGSL shader decorators and explicit GPUBindGroupLayout descriptors, combined with failure to isolate validation errors via error scopes, triggering fatal GPUDevice loss cascades.",
    "bad_code": "// Buggy WebGPU pipeline creation without matching layouts and error isolation\nconst shaderModule = device.createShaderModule({\n  code: `\n    @group(0) @binding(0) var<storage, read_write> data: array<f32>;\n    @compute @workgroup_size(64)\n    fn main(@builtin(global_invocation_id) id: vec3<u32>) {\n      data[id.x] = data[id.x] * 2.0;\n    }\n  `\n});\n\n// Bad: Creating bind group with mismatched 'read-only-storage' type\nconst bindGroupLayout = device.createBindGroupLayout({\n  entries: [{\n    binding: 0,\n    visibility: GPUShaderStage.COMPUTE,\n    buffer: { type: 'read-only-storage' } // Conflict: Shader declares read_write!\n  }]\n});\n\nconst pipelineLayout = device.createPipelineLayout({ bindGroupLayouts: [bindGroupLayout] });\n// Fails validation asynchronously; unhandled, subsequent submit causes device loss\nconst pipeline = device.createComputePipeline({\n  layout: pipelineLayout,\n  compute: { module: shaderModule, entryPoint: 'main' }\n});",
    "solution_desc": "Define explicit, fully synchronized GPUBindGroupLayouts that match WGSL storage access declarations. Wrap pipeline creation within WebGPU error scopes (pushErrorScope/popErrorScope) to capture validation failures gracefully, and register a device.lost lifecycle handler to restore the context upon driver resets.",
    "good_code": "async function createSafeComputePipeline(device: GPUDevice): Promise<GPUComputePipeline> {\n  // Handle unexpected device loss\n  device.lost.then((info) => {\n    console.error(`GPUDevice lost: ${info.message} (Reason: ${info.reason})`);\n  });\n\n  const shaderCode = `\n    @group(0) @binding(0) var<storage, read_write> bufferData: array<f32>;\n    @compute @workgroup_size(64)\n    fn main(@builtin(global_invocation_id) id: vec3<u32>) {\n      bufferData[id.x] *= 2.0;\n    }\n  `;\n\n  const shaderModule = device.createShaderModule({ code: shaderCode });\n\n  const bindGroupLayout = device.createBindGroupLayout({\n    entries: [{\n      binding: 0,\n      visibility: GPUShaderStage.COMPUTE,\n      buffer: { type: 'storage' } // Exactly matches WGSL read_write\n    }]\n  });\n\n  const layout = device.createPipelineLayout({ bindGroupLayouts: [bindGroupLayout] });\n\n  // Capture validation errors without crashing GPUDevice\n  device.pushErrorScope('validation');\n  const pipeline = await device.createComputePipelineAsync({\n    layout,\n    compute: { module: shaderModule, entryPoint: 'main' }\n  });\n  const error = await device.popErrorScope();\n\n  if (error) {\n    throw new Error(`Pipeline compilation error: ${error.message}`);\n  }\n  return pipeline;\n}",
    "verification": "Check browser developer console for zero WebGPU validation errors, and verify that `device.popErrorScope()` returns `null` during initialization runs.",
    "date": "2026-09-25",
    "id": 1790345930,
    "type": "error"
});