window.onPostDataLoaded({
    "title": "PyTorch DDP: Multi-GPU Gradient AllReduce Deadlocks",
    "slug": "pytorch-ddp-multi-gpu-gradient-allreduce-deadlocks-cuda-stream-race-conditions",
    "language": "PyTorch",
    "code": "DDP Deadlock",
    "tags": [
        "Python",
        "PyTorch",
        "Multi-GPU",
        "Distributed Training",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Distributed Data Parallel (DDP) in PyTorch is a powerful tool for scaling deep learning training across multiple GPUs, potentially on multiple nodes. It works by having each process compute gradients on a subset of the data, then aggregating these gradients across all processes using an <code>AllReduce</code> operation. The <code>AllReduce</code> operation (typically implemented with NVIDIA NCCL) is a collective communication primitive that ensures all processes have the same, averaged gradients before the optimizer step.</p><p>Deadlocks in DDP often occur when the implicit synchronization points introduced by <code>AllReduce</code> are mismatched across different processes. If one process reaches an <code>AllReduce</code> call and blocks, waiting for other processes to also reach it, but another process is delayed (or never reaches it), a deadlock ensues. This typically manifests as the training hanging indefinitely without error messages, or with cryptic NCCL timeout errors.</p><p>Common culprits include:</p><ul><li><b>Uneven computation graphs:</b> If certain samples or control flow paths cause a different number of layers or operations to be executed on different GPUs, the gradient computation for some parameters might be skipped, or an <code>AllReduce</code> operation might be conditionally executed, leading to a mismatch.</li><li><b>Manual CUDA stream synchronization:</b> Explicit calls to <code>torch.cuda.synchronize()</code> or custom CUDA kernels that manage streams can interfere with DDP's internal stream management, leading to race conditions or unexpected blocking. DDP expects to manage its own streams for gradient communication.</li><li><b>Input data variance:</b> If data preprocessing or loading is non-deterministic and leads to varying batch sizes or graph structures across processes (e.g., due to different numbers of augmentations or padding), it can cause a divergence in the computational graph.</li><li><b>Unused parameters:</b> If a model has parameters that are not part of the current forward pass (and thus have no gradients), DDP by default attempts to <code>AllReduce</code> their gradients, which won't exist. This can cause a deadlock if only some processes skip certain parts of the model.</li></ul>",
    "root_cause": "The primary root cause is a desynchronization of the collective <code>AllReduce</code> operation across DDP processes. This desynchronization can be triggered by non-deterministic computational graphs (e.g., conditional layers, varying input shapes), explicit manual CUDA stream management interfering with DDP's asynchronous gradient aggregation, or the presence of model parameters that are not used in every forward pass across all ranks, leading to missed gradient contributions for <code>AllReduce</code>.",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\nimport os\n\ndef setup(rank, world_size):\n    os.environ['MASTER_ADDR'] = 'localhost'\n    os.environ['MASTER_PORT'] = '12355'\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\ndef cleanup():\n    dist.destroy_process_group()\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear1 = nn.Linear(10, 10)\n        self.linear2 = nn.Linear(10, 1)\n\n    def forward(self, x, skip_layer2=False):\n        x = self.linear1(x)\n        # BAD CODE: Conditional execution of a layer based on input data or rank\n        # This can cause a deadlock if 'skip_layer2' is true for some ranks \n        # but false for others, or if it's true for all ranks but DDP expects\n        # gradients for linear2 parameters from all ranks.\n        if not skip_layer2:\n            x = self.linear2(x)\n        return x\n\ndef train(rank, world_size):\n    setup(rank, world_size)\n    torch.cuda.set_device(rank)\n\n    model = SimpleModel().to(rank)\n    # If skip_layer2 is always True, linear2 params are unused.\n    # DDP by default expects gradients for ALL parameters.\n    ddp_model = DDP(model, device_ids=[rank]) \n\n    optimizer = optim.SGD(ddp_model.parameters(), lr=0.01)\n    loss_fn = nn.MSELoss()\n\n    for i in range(5):\n        optimizer.zero_grad()\n        inputs = torch.randn(20, 10).to(rank)\n\n        # Simulate a condition that might lead to unused parameters\n        # For example, if rank 0 has skip_layer2=True but rank 1 has skip_layer2=False\n        # This would cause a deadlock.\n        skip = (rank % 2 == 0) and (i % 2 == 0) # Conditional for demo\n        outputs = ddp_model(inputs, skip_layer2=skip)\n        \n        target = torch.randn(20, 1).to(rank)\n        if not skip: # Only compute loss if outputs were generated by linear2\n            loss = loss_fn(outputs, target)\n            loss.backward()\n\n        optimizer.step()\n        print(f\"Rank {rank}, Epoch {i}: Loss {loss.item() if not skip else 'N/A'}\")\n\n    cleanup()\n\nif __name__ == '__main__':\n    world_size = 2\n    # To run this, you'd typically use torch.multiprocessing.spawn\n    # Example: \n    # torch.multiprocessing.spawn(train, args=(world_size,), nprocs=world_size, join=True)\n    print(\"Running with potential deadlock scenario. Use `torch.multiprocessing.spawn` to test.\")\n```",
    "solution_desc": "To prevent DDP deadlocks, ensure all processes follow the exact same computational graph, especially regarding which parameters contribute to gradients. The most common fix for conditional graph execution or unused parameters is to initialize DDP with <code>find_unused_parameters=True</code>. This tells DDP to automatically identify and handle parameters that do not receive gradients, preventing it from waiting indefinitely for their <code>AllReduce</code>. Alternatively, ensure that all parameters are used in the forward pass across all ranks. Avoid manual CUDA stream synchronization unless absolutely necessary and with a deep understanding of DDP's internal workings. For variable input shapes, ensure padding or batching logic is consistent across ranks, or handle it carefully to avoid graph divergence.",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\nimport os\n\ndef setup(rank, world_size):\n    os.environ['MASTER_ADDR'] = 'localhost'\n    os.environ['MASTER_PORT'] = '12355'\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\ndef cleanup():\n    dist.destroy_process_group()\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear1 = nn.Linear(10, 10)\n        self.linear2 = nn.Linear(10, 1)\n\n    def forward(self, x, skip_layer2=False):\n        x = self.linear1(x)\n        # Conditional logic is still present, but DDP will be configured to handle it.\n        if not skip_layer2:\n            x = self.linear2(x)\n        else:\n            # Ensure the output shape is consistent even if a layer is skipped\n            # This might require careful design, e.g., using a no-op layer or padding.\n            # For simplicity, we'll just return x from linear1.\n            # If output shape must be (batch_size, 1), this needs adjustment.\n            pass \n        return x\n\ndef train(rank, world_size):\n    setup(rank, world_size)\n    torch.cuda.set_device(rank)\n\n    model = SimpleModel().to(rank)\n    # GOOD CODE: Set find_unused_parameters=True to prevent deadlocks\n    # when some parameters might not receive gradients on certain ranks/iterations.\n    ddp_model = DDP(model, device_ids=[rank], find_unused_parameters=True) \n\n    optimizer = optim.SGD(ddp_model.parameters(), lr=0.01)\n    loss_fn = nn.MSELoss()\n\n    for i in range(5):\n        optimizer.zero_grad()\n        inputs = torch.randn(20, 10).to(rank)\n\n        # Conditional skip as before, but DDP handles unused params now.\n        skip = (rank % 2 == 0) and (i % 2 == 0)\n        outputs = ddp_model(inputs, skip_layer2=skip)\n        \n        # Adjust target size if outputs shape varies due to `skip`\n        if skip:\n            # If linear2 is skipped, output from linear1 is (20,10), not (20,1)\n            # This highlights the need for consistent graph/output shape handling.\n            # For a proper fix, one might use a placeholder tensor or ensure layer ops produce same shape.\n            target = torch.randn(20, 10).to(rank)\n            loss = loss_fn(outputs, target)\n        else:\n            target = torch.randn(20, 1).to(rank)\n            loss = loss_fn(outputs, target)\n            \n        loss.backward()\n\n        optimizer.step()\n        print(f\"Rank {rank}, Epoch {i}: Loss {loss.item()}\")\n\n    cleanup()\n\nif __name__ == '__main__':\n    world_size = 2\n    # To run this, use `torch.multiprocessing.spawn`\n    torch.multiprocessing.spawn(train, args=(world_size,), nprocs=world_size, join=True)\n    print(\"Training completed without deadlock (with find_unused_parameters=True).\")\n```",
    "verification": "The fix can be verified by running the DDP training process across multiple GPUs/nodes. The training should complete successfully without hanging or reporting NCCL timeout errors. Monitor GPU utilization and network traffic to ensure that all processes are actively participating and communicating efficiently. Inspect the logs for any warnings from DDP regarding unused parameters, which, while not errors, can indicate areas for potential graph simplification or optimization if those parameters are truly never needed. Performance metrics, such as throughput (samples/second), should also be consistent across runs and close to expected values for distributed training.",
    "date": "2026-09-08",
    "id": 1788853190,
    "type": "error"
});