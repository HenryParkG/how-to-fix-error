window.onPostDataLoaded({
    "title": "Go: Goroutine Leaks & Context Cancellation",
    "slug": "go-goroutine-leakage-context-cancellation-deadlocks",
    "language": "Go",
    "code": "Goroutine Leak",
    "tags": [
        "Go",
        "Concurrency",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutines are Go's lightweight concurrency primitive, allowing developers to easily perform concurrent operations. However, improper management can lead to goroutine leaks: goroutines that are launched but never terminate, consuming memory and CPU resources indefinitely. A common pattern for managing goroutine lifecycles is using the <code>context</code> package, specifically <code>context.WithCancel</code>, to signal cancellation.</p><p>A goroutine leak typically occurs when a goroutine blocks indefinitely, waiting for an event that never happens (e.g., reading from a channel that's never written to, or writing to a channel that's never read from), or if it enters an infinite loop without an exit condition. While <code>context.Done()</code> provides a robust way to signal a goroutine to stop, misusing it can lead to new issues. For instance, a goroutine might deadlock if it blocks on a channel that will never receive data because the parent context has been cancelled, but the goroutine itself doesn't explicitly check <code>ctx.Done()</code> and exit.</p>",
    "root_cause": "The root cause of goroutine leaks often stems from inadequate cleanup logic, specifically: 1. A goroutine blocking on an unbuffered or full buffered channel without a mechanism to unblock or exit. 2. Failure to propagate context cancellation signals or not reacting to <code>ctx.Done()</code> within a goroutine that performs long-running or blocking operations. 3. Forgetting to call <code>cancel()</code> on a <code>context.WithCancel</code>, leading to the context (and its resources) never being released, potentially also holding goroutines open indirectly. Deadlocks can occur when a goroutine expects a channel operation to complete but the operation is implicitly blocked by a cancelled context, and the goroutine doesn't have an alternative path to exit via <code>ctx.Done()</code>.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc leakyWorker(ctx context.Context, data chan int) {\n\tgo func() {\n\t\tfor {\n\t\t\t// This goroutine will leak if 'data' channel is never written to\n\t\t\t// or if the main function exits without closing the channel\n\t\t\t// and without the goroutine checking ctx.Done()\n\t\t\tselect {\n\t\t\tcase val := <-data:\n\t\t\t\tfmt.Printf(\"Worker received: %d\\n\", val)\n\t\t\t// Missing ctx.Done() case, so it can't exit gracefully\n\t\t\t}\n\t\t}\n\t}()\n}\n\nfunc main() {\n\tctx, cancel := context.WithCancel(context.Background())\n\tdata := make(chan int) // Unbuffered channel\n\n\tleakyWorker(ctx, data)\n\tfmt.Printf(\"Goroutines before: %d\\n\", runtime.NumGoroutine())\n\n\t// Simulate some work, but the channel 'data' is never closed or written to.\n\t// The worker goroutine will remain blocked on `<-data` indefinitely.\n\t// And cancel() is called too late, or context propagation is incomplete.\n\ttime.Sleep(100 * time.Millisecond)\n\n\tcancel() // This will signal the context, but the goroutine isn't listening.\n\ttime.Sleep(100 * time.Millisecond)\n\tfmt.Printf(\"Goroutines after: %d (expected 1, but still 2 due to leak)\\n\", runtime.NumGoroutine())\n}\n```",
    "solution_desc": "To prevent goroutine leaks and manage context cancellation gracefully, always ensure that goroutines performing blocking operations or long-running tasks listen for cancellation signals from their context. Use a <code>select</code> statement with a <code>case <-ctx.Done(): return</code> clause to allow the goroutine to exit when the context is cancelled. Additionally, ensure that channels used for communication are properly closed by the sender, or that readers are prepared for the channel to be closed. When using <code>context.WithCancel</code>, always pair the context creation with a <code>defer cancel()</code> call to release resources associated with the context as soon as the function returns.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc wellBehavedWorker(ctx context.Context, data chan int) {\n\tgo func() {\n\t\tdefer fmt.Println(\"Worker goroutine exited\")\n\t\tfor {\n\t\t\tselect {\n\t\t\tcase val := <-data:\n\t\t\t\tfmt.Printf(\"Worker received: %d\\n\", val)\n\t\t\tcase <-ctx.Done(): // Listen for context cancellation\n\t\t\t\tfmt.Println(\"Worker received cancellation signal, exiting.\")\n\t\t\t\treturn\n\t\t\t}\n\t\t}\n\t}()\n}\n\nfunc main() {\n\tctx, cancel := context.WithCancel(context.Background())\n\tdefer cancel() // Ensure cancel is called to free context resources\n\n\tdata := make(chan int) \n\n\twellBehavedWorker(ctx, data)\n\tfmt.Printf(\"Goroutines before: %d\\n\", runtime.NumGoroutine())\n\n\t// Simulate some work, sending some data\n\tfor i := 0; i < 3; i++ {\n\t\tselect {\n\t\tcase data <- i:\n\t\t\ttime.Sleep(50 * time.Millisecond)\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Println(\"Main context cancelled, stopping data send.\")\n\t\t\tbreak\n\t\t}\n\t}\n\n\t// Now we decide to stop the worker\n\tcancel() \n\n\t// Give some time for the worker to receive the signal and exit\n\ttime.Sleep(200 * time.Millisecond)\n\tfmt.Printf(\"Goroutines after: %d (expected 1 for main goroutine only)\\n\", runtime.NumGoroutine())\n\tclose(data) // Always close channels when done sending to allow readers to exit\n}\n```",
    "verification": "The primary method to verify that goroutine leaks are resolved is by using Go's built-in <code>runtime.NumGoroutine()</code> to monitor the number of active goroutines. After an operation that spawns goroutines completes (and signals cancellation), the count should return to the baseline (usually 1 for the main goroutine). For more advanced debugging, use <code>go tool pprof</code> with <code>http://localhost:6060/debug/pprof/goroutine?debug=1</code> to get detailed stack traces of all live goroutines, identifying any that are unexpectedly still running. Stress test with high load and frequent cancellations to ensure robustness.",
    "date": "2026-09-19",
    "id": 1789784271,
    "type": "error"
});