window.onPostDataLoaded({
    "title": "Go Runtime: Preemption Delays & Netpoller Starvation",
    "slug": "go-runtime-preemption-netpoller-starvation-thread-exhaustion",
    "language": "Go",
    "code": "PreemptionDelay",
    "tags": [
        "Go",
        "Backend",
        "Performance",
        "Runtime",
        "Concurrency",
        "Error Fix"
    ],
    "analysis": "<p>Go's scheduler is a marvel, designed to efficiently multiplex many goroutines onto a smaller number of operating system threads (Ms). Its non-cooperative preemption mechanism, introduced in Go 1.14, aimed to prevent a single CPU-bound goroutine from hogging an OS thread indefinitely, ensuring fairness and responsiveness. It works by injecting preemption signals, typically at function call boundaries or loop back-edges, forcing the goroutine to yield.</p><p>However, delays can occur when a CPU-bound goroutine performs a long-running, non-interruptible computation without explicit yield points (like I/O or <code>runtime.Gosched()</code>) or frequent function calls. This can starve critical runtime components, particularly the Netpoller. The Netpoller is responsible for handling network I/O events, waking up goroutines waiting for network operations, and is crucial for the responsiveness of any network-facing Go application. If the Netpoller's goroutines can't get scheduled due to other goroutines monopolizing OS threads, network operations will block, leading to high latency and reduced throughput.</p><p>In extreme cases, if the scheduler repeatedly fails to find available OS threads (Ms) to run the Netpoller or other critical runtime work, it may attempt to spin up new OS threads. This can lead to thread (M) exhaustion, where the Go runtime creates more OS threads than necessary or efficient, increasing memory usage, context switching overhead, and ultimately degrading overall application performance and stability.</p>",
    "root_cause": "Long-running, CPU-bound Go routines executing without explicit yield points or sufficient function call density, causing scheduler delays that starve critical runtime components like the Netpoller and potentially lead to OS thread (M) exhaustion.",
    "bad_code": "package main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\nfunc cpuBoundTask() {\n\t// This loop runs for a long time without any I/O or function calls\n\t// that would trigger a preemption check.\n\tfor i := 0; i < 1e10; i++ {\n\t\t_ = i * i // Busy work\n\t}\n\tfmt.Println(\"CPU bound task finished\")\n}\n\nfunc main() {\n\tgo cpuBoundTask() // Launch a CPU-bound goroutine\n\n\thttp.HandleFunc(\"/hello\", func(w http.ResponseWriter, r *http.Request) {\n\t\tfmt.Fprintf(w, \"Hello, Go!\\n\")\n\t})\n\n\tfmt.Println(\"Server starting on :8080\")\n\t// This server will become unresponsive if cpuBoundTask starves the Netpoller\n\thttp.ListenAndServe(\":8080\", nil)\n}\n",
    "solution_desc": "To mitigate preemption delays and Netpoller starvation, ensure that CPU-bound goroutines periodically yield control or are broken into smaller, more manageable units. Introduce explicit yield points using <code>runtime.Gosched()</code>, especially in tight loops or long computations. For extremely intensive tasks, consider distributing the work across multiple goroutines using channels and worker pools, allowing the scheduler to interleave execution more effectively. Regularly profile your application using <code>pprof</code> to identify CPU hotspots and long-running goroutines. Monitor Go runtime metrics, such as scheduler latency and OS thread count, to detect early signs of contention. Adjust <code>GOMAXPROCS</code> if necessary, but generally, leaving it at its default (number of CPU cores) is optimal.",
    "good_code": "package main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc cpuBoundTaskWithYield() {\n\tfor i := 0; i < 1e10; i++ {\n\t\t_ = i * i // Busy work\n\t\t// Periodically yield control to allow other goroutines to run\n\t\tif i%1e6 == 0 {\n\t\t\truntime.Gosched()\n\t\t}\n\t}\n\tfmt.Println(\"CPU bound task finished\")\n}\n\nfunc main() {\n\tgo cpuBoundTaskWithYield() // Launch CPU-bound goroutine with yield points\n\n\thttp.HandleFunc(\"/hello\", func(w http.ResponseWriter, r *http.Request) {\n\t\tfmt.Fprintf(w, \"Hello, Go!\\n\")\n\t})\n\n\tfmt.Println(\"Server starting on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}\n",
    "verification": "Verify the fix by monitoring application throughput and latency under load. Use <code>pprof</code> to ensure that CPU-bound goroutines are no longer monopolizing CPU time for extended periods. Observe Go runtime metrics using tools like Prometheus/Grafana (if instrumented) for scheduler latency, goroutine count, and OS thread (M) count. A healthy application should show consistent network request handling times and a stable number of OS threads, indicating the scheduler is effectively managing goroutines without starvation.",
    "date": "2026-09-07",
    "id": 1788746211,
    "type": "error"
});