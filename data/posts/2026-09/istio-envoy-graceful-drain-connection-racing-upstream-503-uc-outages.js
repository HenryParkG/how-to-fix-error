window.onPostDataLoaded({
    "title": "Istio Envoy: Drain Racing & Upstream 503 UC Outages",
    "slug": "istio-envoy-graceful-drain-connection-racing-upstream-503-uc-outages",
    "language": "Kubernetes",
    "code": "Envoy503Outage",
    "tags": [
        "Istio",
        "Envoy",
        "Kubernetes",
        "Microservices",
        "Networking",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>When a Kubernetes pod running an application with an Istio sidecar (Envoy proxy) is terminated or scaled down, it initiates a graceful shutdown process. The goal is to allow existing connections to drain and new connections to be routed elsewhere. However, a common problem known as \"drain racing\" or \"graceful termination racing\" can occur, leading to intermittent 503 Upstream Connection (UC) errors.</p><p>This happens when there's a mismatch in the timing between Kubernetes' lifecycle events (preStop hooks, terminationGracePeriodSeconds, readiness/liveness probes), Envoy's draining logic, and the application's actual shutdown. Specifically, a pod might be marked as terminating by Kubernetes, and new requests should ideally be stopped. But if Envoy is still considered healthy by the load balancer (e.g., Istio's internal load balancing or an external one) for a brief period, or if existing connections are still being forwarded, new requests (or requests on existing long-lived connections) can be sent to a terminating Envoy sidecar. If the application behind that Envoy has already started shutting down, or if Envoy itself has entered a state where it cannot proxy, it will respond with a 503 UC error, indicating it failed to connect to its upstream (the application container).</p>",
    "root_cause": "The root cause lies in the delicate orchestration required during pod termination in a microservices environment:</p><ul><li><b>Probe Timing Mismatch:</b> Kubernetes readiness probes might become unhealthy too late, or the interval between readiness failure and the start of actual termination is too short, allowing traffic to still be directed to the pod.</li><li><b>Insufficient `terminationGracePeriodSeconds`:</b> The default Kubernetes `terminationGracePeriodSeconds` (30 seconds) is often insufficient for applications with long-lived connections or heavy traffic, especially for Envoy's graceful drain to complete.</li><li><b>Missing or Ineffective `preStop` Hook:</b> Without a `preStop` hook, Envoy might not be explicitly signaled to begin draining connections *before* Kubernetes starts sending `SIGTERM` to the application. This can lead to the application shutting down before Envoy has a chance to update its status or drain connections.</li><li><b>Application Ignoring `SIGTERM`:</b> If the application doesn't gracefully handle `SIGTERM` and shuts down abruptly, it can leave Envoy with no healthy upstream, even if Envoy itself is still active.</li><li><b>Envoy's `min_healthy_percentage` Setting:</b> In some cases, if the service entry's `min_healthy_percentage` is set too low or not configured, Istio's load balancer might still route traffic to a partially unhealthy service.</li></ul>",
    "bad_code": "A common Kubernetes deployment configuration that is prone to this issue due to missing explicit draining signals and potentially insufficient grace period:\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-service\nspec:\n  template:\n    spec:\n      containers:\n      - name: app\n        image: my-app:latest\n        ports:\n        - containerPort: 8080\n        readinessProbe:\n          httpGet:\n            path: /health\n            port: 8080\n          initialDelaySeconds: 5\n          periodSeconds: 5\n        # Missing preStop hook, and default terminationGracePeriodSeconds (30s) might be too short.\n        # This means SIGTERM hits the app and Envoy simultaneously, and Envoy might not drain properly.\n      terminationGracePeriodSeconds: 30 # Often too short for proper draining\n```",
    "solution_desc": "To mitigate and eliminate 503 UC outages during graceful shutdowns, a robust strategy involves coordinating Kubernetes, Envoy, and the application's lifecycle:</p><ul><li><b>Increase `terminationGracePeriodSeconds`:</b> Extend this value (e.g., 60-120 seconds or more) to provide ample time for Envoy to drain connections and the application to shut down.</li><li><b>Implement a `preStop` Hook for Envoy:</b> The most effective solution. Use a `preStop` hook to explicitly tell the Envoy sidecar to immediately enter drain mode by calling its `/healthcheck/fail` endpoint. This causes Envoy to fail its readiness checks, prompting Istio to remove it from the load balancing pool. A short delay (e.g., 5 seconds) before failing the health check allows time for Istio to propagate the change.</li><li><b>Ensure Application Graceful Shutdown:</b> Design your application to respond to `SIGTERM` by stopping accepting new requests, completing existing requests, and cleaning up resources within the allotted `terminationGracePeriodSeconds`.</li><li><b>Configure Envoy's `min_healthy_percentage`:</b> For critical services, ensure this is set to a sufficiently high value (e.g., 100%) so that no traffic is routed to instances during their draining phase.</li><li><b>Tune Readiness/Liveness Probes:</b> Adjust `periodSeconds`, `failureThreshold`, and `initialDelaySeconds` to ensure probes accurately reflect the application's state without being overly aggressive or too slow.</li></ul>",
    "good_code": "An improved Kubernetes deployment configuration, incorporating a `preStop` hook and an extended `terminationGracePeriodSeconds`:\n\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-service\nspec:\n  template:\n    spec:\n      containers:\n      - name: app\n        image: my-app:latest\n        ports:\n        - containerPort: 8080\n        readinessProbe:\n          httpGet:\n            path: /health\n            port: 8080\n          initialDelaySeconds: 5\n          periodSeconds: 5\n          failureThreshold: 3 # Allow a few failures before marking unhealthy\n        lifecycle:\n          preStop:\n            exec:\n              # Introduce a small sleep to allow time for K8s to update endpoints.\n              # Then, signal Envoy sidecar (port 15020 is common for Istio proxy) to fail health checks.\n              # '|| true' prevents the pod from getting stuck if wget fails for some reason.\n              command: [\"/bin/sh\", \"-c\", \"sleep 5 && wget -q -O- http://localhost:15020/healthcheck/fail || true\"]\n      terminationGracePeriodSeconds: 120 # Provide ample time for draining (adjust as needed)\n```\n\nAdditionally, ensure your application code handles `SIGTERM` gracefully:\n```python\nimport signal\nimport sys\nimport time\n\ndef graceful_shutdown(signum, frame):\n    print(f\"Received signal {signum}, starting graceful shutdown...\")\n    # Stop accepting new connections (e.g., close server socket)\n    # Allow existing requests to complete\n    time.sleep(30) # Simulate work completion\n    sys.exit(0)\n\nsignal.signal(signal.SIGTERM, graceful_shutdown)\n\n# Your main application logic\n# ...\n```",
    "verification": "During a rolling update or pod scale-down, closely monitor the Kubernetes events (`kubectl get events`), pod logs (`kubectl logs -f <pod> -c istio-proxy`), and application logs. Look for signs that the `preStop` hook executes successfully and that Envoy reports health check failures before the `SIGTERM` hits the application. Crucially, monitor your service's error rates (e.g., 5xx errors from Istio's metrics) during these lifecycle events. The goal is to see a smooth transition of traffic with no spikes in 503 UC errors. Performing soak tests with frequent deployments can help identify any remaining race conditions.",
    "date": "2026-09-10",
    "id": 1789046311,
    "type": "error"
});