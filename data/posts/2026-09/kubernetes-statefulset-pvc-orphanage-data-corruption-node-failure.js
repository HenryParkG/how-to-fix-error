window.onPostDataLoaded({
    "title": "Kubernetes: StatefulSet PVC Orphanage & Data Corruption on Node Failure",
    "slug": "kubernetes-statefulset-pvc-orphanage-data-corruption-node-failure",
    "language": "Kubernetes",
    "code": "PVC Orphan",
    "tags": [
        "Kubernetes",
        "StatefulSet",
        "PVC",
        "Data Integrity",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>StatefulSets in Kubernetes are designed to manage stateful applications, providing stable network identities and persistent storage. They leverage Persistent Volume Claims (PVCs) for dynamic provisioning of Persistent Volumes (PVs). A critical challenge arises during node failures or incorrect StatefulSet scaling/deletion: Persistent Volume Claims can become 'orphaned,' meaning the PVC still exists even after the StatefulSet pod that used it is gone, potentially leading to data accumulation or incorrect re-attachment. More severely, during a node failure, especially with storage solutions that don't enforce strict single-writer semantics, there's a risk of data corruption. If Kubernetes schedules a new pod for the StatefulSet on a healthy node while the old pod on the failed node still holds a lease or is in a 'terminating' state, both pods might attempt to write to the same PV, resulting in split-brain scenarios and data corruption. This is particularly prevalent with RWO (ReadWriteOnce) volumes in environments that lack proper fencing or STONITH (Shoot The Other Node In The Head) mechanisms.</p>",
    "root_cause": "The default `volumeReclaimPolicy: Retain` on many Persistent Volume types (especially for dynamically provisioned volumes) means PVCs and their underlying PVs are not automatically deleted when a StatefulSet is scaled down or deleted. This leads to orphaned storage. Concurrently, during node failure, the Kubelet on the failed node might not be able to signal the termination of the old pod holding the volume lease, while the Kube-scheduler rapidly re-schedules a replacement pod on a new node. If the storage driver doesn't actively fence the old node or dismount the volume, the new pod can attach to the volume, leading to simultaneous writes.",
    "bad_code": "apiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: my-stateful-app\nspec:\n  serviceName: \"my-service\"\n  replicas: 3\n  selector:\n    matchLabels: \n      app: my-stateful-app\n  template:\n    metadata:\n      labels:\n        app: my-stateful-app\n    spec:\n      containers:\n      - name: my-container\n        image: busybox\n        command: [\"/bin/sh\", \"-c\", \"while true; do echo $(date) >> /data/log.txt; sleep 5; done\"]\n        volumeMounts:\n        - name: data\n          mountPath: /data\n  volumeClaimTemplates:\n  - metadata:\n      name: data\n    spec:\n      accessModes: [ \"ReadWriteOnce\" ]\n      resources:\n        requests:\n          storage: 1Gi\n      # Default reclaim policy is 'Retain' for many dynamic provisioners\n      # This can lead to orphaned PVCs on StatefulSet deletion/scale down.\n      # If a node fails, and this volume is re-attached elsewhere without proper fencing,\n      # data corruption is possible.",
    "solution_desc": "To address PVC orphanage, explicitly set `volumeReclaimPolicy: Delete` on the underlying `StorageClass` or `PersistentVolume` if data persistence beyond the application's lifecycle is not required. For data corruption, robust storage solutions are key: use storage classes that support `ReadWriteOncePod` access mode (if your Kubernetes version and CSI driver support it) or implement explicit fencing mechanisms (e.g., using a CSI driver with `NodeUnpublishVolume` to forcibly detach volumes from failed nodes). Ensure your application is designed for crash recovery and can handle potential volume re-attachments gracefully. For critical stateful applications, consider operators that embed application-level fencing or data replication.",
    "good_code": "apiVersion: storage.k8s.io/v1\nkind: StorageClass\nmetadata:\n  name: fast-storage\nprovisioner: csi.my-storage-provider.com # Replace with your CSI provisioner\nvolumeBindingMode: Immediate\nreclaimPolicy: Delete # Ensure PVCs are deleted with their StatefulSet\nallowVolumeExpansion: true\n# If supported by your CSI driver and Kubernetes version (>=1.22):\n# accessModes: [ \"ReadWriteOncePod\" ] # Prefer this for strong single-writer semantics\n---\napiVersion: apps/v1\nkind: StatefulSet\nmetadata:\n  name: my-stateful-app\nspec:\n  serviceName: \"my-service\"\n  replicas: 3\n  selector:\n    matchLabels: \n      app: my-stateful-app\n  template:\n    metadata:\n      labels:\n        app: my-stateful-app\n    spec:\n      containers:\n      - name: my-container\n        image: busybox\n        command: [\"/bin/sh\", \"-c\", \"while true; do echo $(date) >> /data/log.txt; sleep 5; done\"]\n        volumeMounts:\n        - name: data\n          mountPath: /data\n  volumeClaimTemplates:\n  - metadata:\n      name: data\n    spec:\n      accessModes: [ \"ReadWriteOnce\" ] # Or ReadWriteOncePod if using the SC above\n      storageClassName: fast-storage # Reference the StorageClass with Delete reclaimPolicy\n      resources:\n        requests:\n          storage: 1Gi",
    "verification": "To verify the fix for PVC orphanage, scale down or delete the StatefulSet and observe if the associated PVCs are automatically deleted. For data corruption, simulate a node failure (e.g., by power cycling or stopping the Kubelet) with a StatefulSet running, and then observe the behavior of the application. Perform data integrity checks (e.g., checksums, application-level validation) on the re-attached volume. Test with various node failure scenarios and ensure the storage solution's fencing mechanisms (if any) are effective in preventing simultaneous writes from multiple nodes.",
    "date": "2026-09-15",
    "id": 1789439533,
    "type": "error"
});