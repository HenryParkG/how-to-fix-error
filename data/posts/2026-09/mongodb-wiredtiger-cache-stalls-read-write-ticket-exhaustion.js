window.onPostDataLoaded({
    "title": "MongoDB: WiredTiger Cache Stalls & Read/Write Ticket Exhaustion",
    "slug": "mongodb-wiredtiger-cache-stalls-read-write-ticket-exhaustion",
    "language": "MongoDB",
    "code": "CacheStalls",
    "tags": [
        "MongoDB",
        "Infra",
        "Database",
        "Performance",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine utilizes an in-memory cache to store both data and indexes. This cache is crucial for performance, reducing the need to read from slower disk. However, under high write load or specific access patterns, the WiredTiger cache can become a source of performance degradation, leading to 'stalls'. These stalls often manifest as increased latencies, reduced throughput, and sometimes even temporary unresponsiveness of the database.</p><p>The root of the problem lies in the interplay between cache eviction, dirty pages, and the internal concurrency control mechanisms (read/write tickets) of WiredTiger. When a significant portion of the cache is occupied by 'dirty' pages (pages modified in memory but not yet written to disk), WiredTiger's eviction threads work aggressively to write these pages out to free up cache space. If the disk I/O cannot keep up, these eviction threads consume an increasing number of I/O tickets. This can lead to 'read ticket exhaustion' or 'write ticket exhaustion', where new client operations (reads or writes) cannot acquire the necessary tickets to proceed, causing them to queue and effectively stall the database.</p>",
    "root_cause": "Insufficient WiredTiger cache size for the working set and write load, coupled with slow underlying storage I/O, causes an accumulation of dirty pages. This triggers aggressive cache eviction that consumes an excessive number of read/write tickets, starving client operations.",
    "bad_code": "<p>This issue is observed through monitoring, not direct 'bad code'. Symptoms include:</p><ul><li>High values for `wiredTiger.cache.tracked dirty bytes in the cache` and `wiredTiger.cache.pages currently in the cache`.</li><li>`wiredTiger.concurrentTransactions.write.out` or `wiredTiger.concurrentTransactions.read.out` approaching or hitting `0` for extended periods, indicating ticket exhaustion.</li><li>High `wiredTiger.cache.eviction walk target` values, indicating aggressive eviction attempts.</li><li>Increased disk I/O wait times and queue lengths (`iostat`, `vmstat`).</li><li>Elevated `fsync` lock waits in `db.serverStatus().opLatencies`.</li></ul><p><b>Example observation (conceptual):</b></p><pre><code>mongostat --port 27017\n...\nWT Cache: 16.0G/20.0G dirty: 14.0G, reads: 12345/s, writes: 6789/s\nWT Tickets: read: 1/128, write: 3/128\n...\n</code></pre><p>Here, a 20GB cache has 14GB dirty, and read/write tickets are critically low, indicating stalls.</p>",
    "solution_desc": "The primary solutions involve optimizing the WiredTiger cache configuration and improving disk I/O. Firstly, ensure the `storage.wiredTiger.engineConfig.cacheSizeGB` is adequately provisioned to hold the working set and accommodate peak write loads without constant aggressive eviction. A good starting point is often 50-75% of RAM, but it needs tuning. Secondly, upgrade or optimize the underlying storage to provide higher IOPS and lower latency (e.g., faster SSDs, NVMe). Reduce write amplification by optimizing schema design, indexing strategies, and batching writes where possible. Lastly, monitor and potentially adjust `storage.wiredTiger.engineConfig.maxActiveEvictionThreads` (though this is rarely needed for most setups, and defaults are usually good) and ensure `ulimit` settings are appropriate for file descriptors.",
    "good_code": "<p>Configuration changes in `mongod.conf`:</p><pre><code># /etc/mongod.conf\nstorage:\n  wiredTiger:\n    engineConfig:\n      cacheSizeGB: 32 # Increase cache size (e.g., from 20GB to 32GB)\n      # maxActiveEvictionThreads: 2 # Only adjust if absolutely necessary and after careful testing\n    # Note: Default for maxActiveEvictionThreads is 1. If eviction is consistently lagging, increasing it *might* help,\n    # but often points to deeper I/O or cache sizing issues. Rarely recommended.\n</code></pre><p>Consider schema and query optimization for reducing writes:</p><ul><li>Use <a href=\"https://www.mongodb.com/docs/manual/core/capped-collections/\">Capped Collections</a> for log-like data where old entries are automatically purged.</li><li>Leverage <a href=\"https://www.mongodb.com/docs/manual/core/document-validation/\">Document Validation</a> to enforce schema and prevent malformed writes that could increase document size.</li><li>Batch updates/inserts using bulk operations to reduce I/O overhead.</li></ul>",
    "verification": "After implementing changes, continuously monitor MongoDB's `db.serverStatus()` metrics, especially those under `wiredTiger.cache` and `wiredTiger.concurrentTransactions`. Look for:<ul><li>`wiredTiger.cache.tracked dirty bytes in the cache` remaining within a reasonable range (e.g., less than 25% of total cache size during normal operation).</li><li>`wiredTiger.concurrentTransactions.read.available` and `wiredTiger.concurrentTransactions.write.available` consistently staying high (e.g., > 100).</li><li>`wiredTiger.cache.eviction walk target` stabilizing at lower values.</li><li>Overall reduction in query latency and increased throughput (e.g., observed via `mongostat` and client-side metrics).</li><li>System-level I/O metrics (`iostat -x 1`, `atop`) showing healthier disk utilization and reduced wait times.</li></ul>",
    "date": "2026-09-20",
    "id": 1789910530,
    "type": "error"
});