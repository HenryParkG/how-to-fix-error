window.onPostDataLoaded({
    "title": "MySQL: InnoDB Deadlocks & Gap Locks",
    "slug": "mysql-innodb-deadlocks-gap-locks",
    "language": "MySQL",
    "code": "InnoDB Deadlock",
    "tags": [
        "SQL",
        "Infra",
        "Database",
        "Concurrency",
        "Error Fix"
    ],
    "analysis": "<p>InnoDB deadlocks occur when two or more transactions form a circular dependency for locks on resources, preventing any of them from proceeding. MySQL's InnoDB storage engine automatically detects these deadlocks and rolls back one of the transactions (the \"victim\") to break the cycle, returning an error like <code>Deadlock found when trying to get lock; try restarting transaction</code>. While typical deadlocks involve row-level locks, a more subtle and often misunderstood cause are \"gap locks\" and \"next-key locks\".</p><p>Gap locks are a feature of InnoDB's repeatable read isolation level. They are used to lock ranges between index records, or before the first record, or after the last record, to prevent other transactions from inserting new records into these \"gaps\". This ensures that a range scan or unique constraint check within a transaction will yield the same results if repeated. A next-key lock is a combination of a record lock on the index record itself and a gap lock on the gap before it. Deadlocks involving gap locks typically happen when multiple transactions concurrently try to insert, update, or delete records in overlapping gaps or ranges, or when performing range-based operations that inadvertently conflict.</p>",
    "root_cause": "Concurrent transactions attempting to acquire conflicting locks on data records or, more critically, on gaps between records. This often arises from:\n<ol>\n    <li>Transactions performing range-based operations (e.g., `SELECT ... WHERE id > X FOR UPDATE`, `INSERT ... ON DUPLICATE KEY UPDATE`) that implicitly acquire gap or next-key locks.</li>\n    <li>Implicit locks during index scans for unique constraint checks, even on `INSERT` statements.</li>\n    <li>Inconsistent order of lock acquisition across transactions, especially when using `REPEATABLE READ` isolation level which aggressively uses gap locks to prevent phantom reads.</li>\n</ol>",
    "bad_code": "-- Assume a table `products` with `id` (PK), `name`, `price`.\n-- `id` is an auto-increment primary key.\nCREATE TABLE products (\n    id INT AUTO_INCREMENT PRIMARY KEY,\n    name VARCHAR(100) UNIQUE,\n    price DECIMAL(10, 2)\n);\nINSERT INTO products (name, price) VALUES ('Apple', 1.00), ('Banana', 0.50), ('Cherry', 2.00);\n\n-- Scenario leading to a gap lock deadlock:\n\n-- Transaction 1 Session\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ;\nSTART TRANSACTION;\n-- T1 implicitly locks the gap after 'Cherry' (ID 3) to ensure consistency.\n-- This SELECT MAX(id) FOR UPDATE acquires an exclusive lock on the max record\n-- and importantly, a gap lock covering the range *after* the max record.\nSELECT MAX(id) FROM products FOR UPDATE;\n\n-- Transaction 2 Session (run concurrently, immediately after T1's SELECT MAX(id))\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ;\nSTART TRANSACTION;\n-- T2 attempts to insert a record, which would also attempt to acquire a gap lock\n-- in the same range (the gap after ID 3).\n-- T2 will now block, waiting for T1's gap lock.\nINSERT INTO products (name, price) VALUES ('Date', 3.00);\n\n-- Back to Transaction 1 Session\n-- T1 now attempts an insert in the same gap.\n-- This INSERT also requires a gap lock on the range. Since T2 is waiting for T1's\n-- gap lock and T1 is now waiting for T2's implicit gap lock (or the lock T2 is holding\n-- by being blocked on T1's), a deadlock occurs.\nINSERT INTO products (name, price) VALUES ('Elderberry', 4.00);\n-- T1 will now block, creating a circular wait condition. MySQL detects and rolls back one.",
    "solution_desc": "Mitigating gap lock deadlocks involves reducing the scope and duration of locks, or ensuring a consistent lock acquisition order.\n<ol>\n    <li><b>Lower Isolation Level</b>: If `REPEATABLE READ` is not strictly necessary, consider using `READ COMMITTED`. This reduces the use of gap locks significantly.</li>\n    <li><b>Shorten Transactions</b>: Keep transactions as short as possible to minimize the time locks are held.</li>\n    <li><b>Consistent Lock Order</b>: Ensure all transactions access and lock records (or ranges) in a consistent, predetermined order. For example, always process `id` in ascending order for `SELECT ... FOR UPDATE` clauses.</li>\n    <li><b>Batch Operations</b>: Combine multiple `INSERT` or `UPDATE` statements into a single transaction where logical, rather than individual transactions.</li>\n    <li><b>Use `LOCK IN SHARE MODE` / `FOR UPDATE` Judiciously</b>: Be explicit about lock types and use them only when necessary. If only a read is needed without modifying the data, avoid `FOR UPDATE`.</li>\n    <li><b>Atomic `INSERT ... ON DUPLICATE KEY UPDATE`</b>: This single statement is often more atomic and less prone to gap lock deadlocks than separate `SELECT FOR UPDATE` and `INSERT` statements, as InnoDB optimizes its internal locking.</li>\n    <li><b>Retry Logic</b>: Implement robust retry mechanisms in the application layer for transactions that encounter deadlocks.</li>\n</ol>",
    "good_code": "-- Solution strategy 1: Lower Isolation Level (if feasible for application)\n-- Transaction 1 Session\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;\nSTART TRANSACTION;\nINSERT INTO products (name, price) VALUES ('Date', 3.00);\nCOMMIT;\n\n-- Transaction 2 Session (run concurrently)\nSET TRANSACTION ISOLATION LEVEL READ COMMITTED;\nSTART TRANSACTION;\nINSERT INTO products (name, price) VALUES ('Elderberry', 4.00);\nCOMMIT;\n-- In READ COMMITTED, gap locks are generally not acquired for simple inserts.\n-- Next-key locks are used for foreign key checks and duplicate key checks.\n-- This greatly reduces the chance of gap lock deadlocks on simple inserts.\n\n-- Solution strategy 2: Using atomic operations to reduce contention\n-- Instead of separate SELECT FOR UPDATE and INSERT, use a single, atomic statement.\n-- Transaction 1 Session\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ; -- Or READ COMMITTED\nSTART TRANSACTION;\nINSERT INTO products (name, price) VALUES ('Grape', 5.00)\nON DUPLICATE KEY UPDATE name=VALUES(name), price=VALUES(price);\nCOMMIT;\n\n-- Transaction 2 Session (run concurrently)\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ; -- Or READ COMMITTED\nSTART TRANSACTION;\nINSERT INTO products (name, price) VALUES ('Fig', 6.00)\nON DUPLICATE KEY UPDATE name=VALUES(name), price=VALUES(price);\nCOMMIT;\n-- The `ON DUPLICATE KEY UPDATE` clause allows InnoDB to handle the unique key check\n-- and insert/update atomically within a single operation, reducing the window\n-- for deadlock scenarios compared to multi-step logic.",
    "verification": "Enable InnoDB status monitoring using `SHOW ENGINE INNODB STATUS;` or query `information_schema.INNODB_TRX` and `information_schema.INNODB_LOCKS` tables. Look for the \"LATEST DETECTED DEADLOCK\" section in the status output. This provides a detailed graph of the transactions involved and the locks they were holding/waiting for, including `LOCK_GAP` or `LOCK_ORDINARY` (record lock + gap lock). After implementing fixes, run concurrent load tests to simulate the deadlock scenario and verify that deadlocks are eliminated or significantly reduced. Application logs should show fewer `Deadlock found` errors, and system monitoring should indicate improved database stability under contention.",
    "date": "2026-09-21",
    "id": 1789957520,
    "type": "error"
});