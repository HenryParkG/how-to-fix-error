window.onPostDataLoaded({
    "title": "Spark: Shuffle Spills & Executor OOM Due to Data Skew",
    "slug": "spark-shuffle-spills-executor-oom-data-skew",
    "language": "Spark",
    "code": "OOM/Shuffle Spills",
    "tags": [
        "Spark",
        "Big Data",
        "Performance",
        "Java",
        "Error Fix"
    ],
    "analysis": "<p>Apache Spark is an industry-leading distributed processing engine, leveraging parallel execution across a cluster. A key component of many Spark jobs is the 'shuffle' operation, which involves redistributing data across executors to group it by a common key (e.g., for `groupByKey`, `reduceByKey`, `join` operations). During a shuffle, data from different partitions on various executors is sent over the network to new executors, where it's typically sorted and aggregated.</p><p>Data skew occurs when a small number of keys have a significantly larger volume of associated data compared to other keys. When this skewed data enters a shuffle stage, the tasks responsible for processing these large keys become bottlenecks. An executor tasked with a highly skewed partition might receive a disproportionately large amount of data. This can overwhelm its memory, leading to 'shuffle spills' where the executor has to write intermediate shuffle data to local disk. If the data for a single task is too large to fit even on disk or exceeds the executor's configured memory limits, it results in an 'Executor Out-Of-Memory' (OOM) error, causing the job to fail. These issues degrade performance, increase job latency, and often lead to unstable Spark clusters.</p>",
    "root_cause": "Uneven distribution of data keys, typically during a shuffle operation (`groupByKey`, `reduceByKey`, `join`, `repartition`). A few tasks receive an extremely large amount of data associated with highly frequent keys, overwhelming the assigned executor's memory and disk resources, leading to spills and eventual OOM errors.",
    "bad_code": "```python\nfrom pyspark.sql import SparkSession\n\nspark = SparkSession.builder \\\n    .appName(\"SkewedSparkJob\") \\\n    .config(\"spark.executor.memory\", \"2g\") \\\n    .config(\"spark.sql.shuffle.partitions\", \"200\") \\\n    .getOrCreate()\n\n# Create a DataFrame with highly skewed data\n# Key 'A' appears 1,000,000 times, other keys appear only once\ndata = [(\"A\", i) for i in range(1_000_000)] + [(str(i), i) for i in range(1_000_000, 1_000_000 + 100)]\nskewed_df = spark.createDataFrame(data, [\"key\", \"value\"])\n\n# This operation (groupBy) will cause significant data skew and potential OOM\n# as all 1,000,000 rows for key 'A' will likely go to a single partition/executor\nresult_df = skewed_df.groupBy(\"key\").count()\n\n# Trigger action to execute the job\nprint(\"Starting skewed aggregation...\")\nresult_df.collect()\nprint(\"Job completed (or failed due to OOM/spills).\")\n\nspark.stop()\n```",
    "solution_desc": "Mitigating data skew requires specific strategies to evenly distribute the workload during shuffle operations. Key approaches include:\n1.  **Salting**: For highly skewed keys, add a random suffix (salt) to the key, performing a preliminary aggregation/join on the salted key, then removing the salt and performing a final aggregation/join. This spreads the skewed key's data across multiple partitions.\n2.  **Broadcast Join**: If one of the DataFrames in a join is small enough to fit in executor memory, broadcast it to all executors. This avoids shuffling the larger DataFrame entirely.\n3.  **Adaptive Query Execution (AQE)**: Spark 3.0+ offers AQE, which can dynamically detect and handle skewed joins at runtime by splitting skewed partitions into smaller sub-partitions and joining them independently.\n4.  **Skewed Join Hints**: For Spark 3.0+, you can explicitly hint which keys are skewed in your SQL or DataFrame API, allowing Spark's optimizer to apply specific skew handling techniques.\n5.  **Increase Shuffle Partitions**: While not directly solving skew, increasing `spark.sql.shuffle.partitions` can distribute non-skewed data more broadly and might alleviate mild skew. However, it can also lead to more smaller files and overhead.\n6.  **Filter/Separate Skewed Keys**: If a few keys are known to be highly skewed and their processing is different, they can be filtered out, processed separately with more resources, and then unioned back with the main dataset.\n7.  **Optimize Data Types and Serialization**: Smaller data sizes reduce memory footprint, indirectly helping with OOM. Consider more efficient serialization formats like Parquet/ORC and avoid storing large objects in memory.",
    "good_code": "```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, concat_ws, lit, rand, substring\n\nspark = SparkSession.builder \\\n    .appName(\"FixSkewedSparkJob\") \\\n    .config(\"spark.executor.memory\", \"4g\") \\\n    .config(\"spark.sql.shuffle.partitions\", \"500\") \\\n    .getOrCreate()\n\n# Create a DataFrame with highly skewed data (same as bad_code for comparison)\ndata = [(\"A\", i) for i in range(1_000_000)] + [(str(i), i) for i in range(1_000_000, 1_000_000 + 100)]\nskewed_df = spark.createDataFrame(data, [\"key\", \"value\"])\n\n# Solution: Salting the skewed key 'A'\nnum_salt_buckets = 200 # Number of buckets to spread skewed key 'A' across\n\nsalted_df = skewed_df.withColumn(\n    \"salted_key\",\n    # Add a random suffix for key 'A', keep others as is\n    when(col(\"key\") == lit(\"A\"), concat_ws(\"_\", col(\"key\"), (rand() * num_salt_buckets).cast(\"int\")))\n    .otherwise(col(\"key\"))\n)\n\n# First aggregation on salted key (distributes 'A' across 200 partitions)\npartial_result_df = salted_df.groupBy(\"salted_key\").count()\n\n# Unsalt the key and perform final aggregation\nfinal_result_df = partial_result_df.withColumn(\n    \"original_key\",\n    substring(col(\"salted_key\"), 1, length(col(\"salted_key\")) - (when(col(\"salted_key\").contains(\"_\"), instr(col(\"salted_key\"), \"_\") - 1).otherwise(length(col(\"salted_key\")))))\n)\n\n# For simplicity, if we just want count, we don't need a second groupBy on original_key if the count is already done\n# If we needed to aggregate values (e.g., sum, collect_list), a second groupBy on original_key would be needed.\n# For 'count', the partial counts are already correct.\nfinal_counts = partial_result_df.groupBy(substring(col(\"salted_key\"), 1, when(col(\"salted_key\").contains(\"_\"), instr(col(\"salted_key\"), \"_\") - 1).otherwise(length(col(\"salted_key\")))).alias(\"key\")).sum(\"count\")\n\nprint(\"Starting salted aggregation...\")\nfinal_counts.collect()\nprint(\"Job completed successfully with salting.\")\n\nspark.stop()\n```",
    "verification": "After implementing skew mitigation strategies, verify the fix by monitoring the Spark UI. Look at the 'Stages' tab for the problematic shuffle stage. Observe:\n1.  **Task Durations**: Ensure task durations are more uniform across the stage. Highly skewed tasks typically have significantly longer runtimes.\n2.  **Shuffle Write Bytes/Records**: Check the 'Shuffle Write' metrics. Reduced shuffle write bytes to disk (less spilling) indicates improved memory utilization.\n3.  **Executor Disk Usage**: Monitor executor disk usage; it should be lower and more stable.\n4.  **Executor Logs**: Confirm that no OOM errors appear in the executor logs.\n5.  **Job Completion**: The job should now complete successfully without failures caused by resource exhaustion. For Spark 3.x with AQE, you can also check the 'SQL' tab in Spark UI to see if AQE detected and handled skew joins dynamically.",
    "date": "2026-09-02",
    "id": 1788314683,
    "type": "error"
});