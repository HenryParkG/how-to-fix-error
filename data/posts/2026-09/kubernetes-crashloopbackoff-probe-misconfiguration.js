window.onPostDataLoaded({
    "title": "Kubernetes: CrashLoopBackOff & Probe Misconfigurations",
    "slug": "kubernetes-crashloopbackoff-probe-misconfiguration",
    "language": "Kubernetes",
    "code": "CrashLoopBackOff",
    "tags": [
        "Kubernetes",
        "Probes",
        "YAML",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p><code>CrashLoopBackOff</code> is one of the most common and frustrating statuses encountered in Kubernetes. It signifies that a container within a Pod is repeatedly starting, crashing, and restarting after a back-off delay. While the underlying cause for the crash can vary widely (application bugs, resource limits, misconfigured volumes), a frequent culprit, especially for seemingly healthy applications, is misconfigured liveness and readiness probes.</p><p>Liveness probes determine if an application is still running and healthy. If a liveness probe fails, Kubernetes restarts the container. Readiness probes determine if an application is ready to serve traffic. If a readiness probe fails, Kubernetes removes the Pod from service load balancers. Misconfigurations can range from incorrect port numbers or paths, overly aggressive thresholds that trigger restarts for transient issues, to probes expecting an HTTP 200 OK from an endpoint that doesn't exist or isn't yet exposed. For example, a liveness probe firing too quickly before an application has fully initialized can lead to a continuous `CrashLoopBackOff` as Kubernetes repeatedly restarts the container before it's ready.</p>",
    "root_cause": "Container repeatedly crashes due to liveness probe failure, application start-up failure, or resource constraint violations. Misconfigured liveness probes are a common cause, where the probe fails even if the application might eventually recover, leading to unnecessary restarts.",
    "bad_code": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-webapp-bad\nspec:\n  selector:\n    matchLabels:\n      app: my-webapp\n  template:\n    metadata:\n      labels:\n        app: my-webapp\n    spec:\n      containers:\n      - name: webapp\n        image: my-company/my-webapp:1.0.0\n        ports:\n        - containerPort: 8080\n        livenessProbe:\n          httpGet:\n            path: /healthz-nonexistent\n            port: 8080\n          initialDelaySeconds: 5\n          periodSeconds: 3\n          failureThreshold: 1 # Too aggressive, restarts on first failure\n        readinessProbe:\n          httpGet:\n            path: /readyz\n            port: 8080\n          initialDelaySeconds: 10\n          periodSeconds: 5\n          failureThreshold: 3\n```",
    "solution_desc": "Carefully design and configure liveness and readiness probes. Liveness probes should check for critical application health (e.g., JVM deadlocks, database connection loss) and be configured with `initialDelaySeconds` and `failureThreshold` that are generous enough to allow the application to start and handle transient issues. Readiness probes should confirm the application is fully functional and ready to serve requests. Implement dedicated, lightweight health check endpoints within your application that return appropriate HTTP status codes. Use different endpoints for liveness and readiness if their definitions of 'health' differ. Regularly review probe logs and metrics to identify trends. Consider using a `startupProbe` for applications with long initialization times to prevent liveness probes from prematurely restarting the container.",
    "good_code": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-webapp-good\nspec:\n  selector:\n    matchLabels:\n      app: my-webapp\n  template:\n    metadata:\n      labels:\n        app: my-webapp\n    spec:\n      containers:\n      - name: webapp\n        image: my-company/my-webapp:1.0.0\n        ports:\n        - containerPort: 8080\n        # Startup probe gives the application ample time to initialize without failing liveness\n        startupProbe:\n          httpGet:\n            path: /startup\n            port: 8080\n          initialDelaySeconds: 5\n          periodSeconds: 5\n          failureThreshold: 12 # Allows up to 60 seconds (5s * 12) for startup\n        livenessProbe:\n          httpGet:\n            path: /healthz # A dedicated endpoint for critical health checks\n            port: 8080\n          initialDelaySeconds: 30 # Only start checking after app is likely up\n          periodSeconds: 10\n          failureThreshold: 3 # Allows for some transient issues before restart\n        readinessProbe:\n          httpGet:\n            path: /readyz # Endpoint checks if application is ready to serve traffic\n            port: 8080\n          initialDelaySeconds: 15 # Shorter delay than liveness, as readiness is for traffic routing\n          periodSeconds: 5\n          failureThreshold: 3\n```",
    "verification": "Monitor the Pod status using `kubectl get pods`. If it stabilizes to `Running` and `Ready`, the fix is likely effective. Use `kubectl describe pod <pod-name>` to inspect the 'Events' section for probe failures and restarts. Check application logs with `kubectl logs <pod-name>` to ensure the application itself is starting correctly and its health endpoints are responding as expected. Continuously observe for any `CrashLoopBackOff` or excessive restarts, adjusting probe parameters incrementally until stability is achieved.",
    "date": "2026-09-20",
    "id": 1789871177,
    "type": "error"
});