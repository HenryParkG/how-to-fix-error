window.onPostDataLoaded({
    "title": "Elasticsearch: Shard Rebalancing & Split-Brain",
    "slug": "elasticsearch-shard-rebalancing-split-brain",
    "language": "Elasticsearch",
    "code": "Cluster Instability",
    "tags": [
        "Elasticsearch",
        "Distributed System",
        "Clustering",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch is a distributed system that relies on a cluster of nodes to store and search data. Data is divided into 'shards', which are distributed across nodes. When nodes join or leave the cluster, Elasticsearch automatically initiates 'shard rebalancing' to distribute shards evenly and maintain data redundancy (replica shards). While essential for resilience and scalability, aggressive or continuous rebalancing can lead to a 'rebalancing storm' if nodes frequently become unstable, disconnect, or join/leave the cluster in quick succession. This storm consumes significant I/O, CPU, and network resources, degrading cluster performance and potentially causing data inconsistencies or even data loss.</p><p>A related and more severe issue is the 'split-brain' scenario. This occurs in a distributed system when a network partition causes a cluster to split into two or more independent sub-clusters, each believing it is the authoritative cluster master. In Elasticsearch, if multiple nodes are elected as master in different parts of a partitioned network, they might independently accept writes and diverge, leading to data inconsistencies and corruption once the network partition heals. This compromises data integrity and the reliability of the entire cluster.</p>",
    "root_cause": "<ul><li>**Shard Rebalancing Storms:** Frequent node failures or restarts, unstable network connections causing nodes to temporarily disconnect, inadequate hardware resources (CPU, RAM, disk I/O) unable to handle the rebalancing load, or misconfigured cluster settings (e.g., too many shards, small shard sizes).</li><li>**Split-Brain Scenarios:** Network partitions (network instability, firewall issues) preventing communication between nodes, and incorrect configuration of `discovery.zen.minimum_master_nodes`. If `minimum_master_nodes` is set too low (e.g., to 1), multiple nodes can independently elect themselves as master when isolated, leading to multiple 'brains' in the cluster.</li></ul>",
    "bad_code": "```yaml\n# elasticsearch.yml (example of a problematic configuration)\n\n# This setting is NOT recommended for production clusters with multiple nodes.\n# Setting minimum_master_nodes to 1 or omitting it (defaults to 1 in older versions)\n# is a common cause of split-brain in multi-node clusters.\n# discovery.zen.minimum_master_nodes: 1\n\n# In newer versions (7.x+), these are removed and replaced by 'cluster.initial_master_nodes'\n# which is only used for initial cluster bootstrap and not for ongoing quorum checks.\n# The default quorum is dynamically calculated in recent versions (7.x+)\n# but explicit configuration is still key for robustness.\n\n# Overly aggressive shard allocation settings (optional, can contribute to storms)\n# cluster.routing.allocation.cluster_concurrent_rebalance: 10 # Too many concurrent rebalances\n# cluster.routing.allocation.node_concurrent_incoming_recoveries: 10\n# cluster.routing.allocation.node_concurrent_outgoing_recoveries: 10\n\n# No dedicated master nodes, leading to master election overhead on data nodes\n# node.roles: [ master, data, ingest ]\n```",
    "solution_desc": "To prevent shard rebalancing storms and split-brain scenarios, adhere to best practices for Elasticsearch cluster configuration and network stability. <br/> **For Split-Brain:** The most crucial setting is `cluster.initial_master_nodes` (for bootstrapping 7.x+) and ensuring a proper quorum configuration for ongoing master election. For clusters with more than 1 node, `discovery.zen.minimum_master_nodes` (for 6.x and below) should be set to `(N/2) + 1` where N is the total number of dedicated master-eligible nodes. For 7.x+, `cluster.initial_master_nodes` lists master-eligible nodes to form the initial cluster. Elasticsearch 7.x+ handles quorum dynamically but relies on correct `discovery.seed_hosts` and stable network. <br/> **For Rebalancing Storms:** Use dedicated master-eligible nodes to isolate master election from data storage and querying. Ensure network stability between nodes. Optimize shard allocation settings (`cluster.routing.allocation.*`) to control the speed and concurrency of rebalancing. Monitor cluster health and resource utilization to identify and address bottlenecks. Implement node lifecycle management (e.g., graceful node shutdown) to avoid sudden node removals that trigger full rebalances.",
    "good_code": "```yaml\n# elasticsearch.yml (example of a robust configuration)\n\n# --- Common settings for all nodes ---\ncluster.name: my-production-cluster\nnetwork.host: 0.0.0.0 # Or specific IP if available\nhttp.port: 9200\ntransport.port: 9300\n\n# --- Master-eligible node configuration (e.g., node01, node02, node03) ---\n# These nodes should be stable, have good network connectivity, and sufficient resources.\n# At least 3 dedicated master-eligible nodes are recommended for high availability.\n# node.name: es-master-01\n# node.roles: [ master ]\n\n# For Elasticsearch 7.x and later, this is used for initial cluster bootstrapping.\n# List all master-eligible node names or their network.host:transport.port\n# ONLY SET ONCE when bootstrapping the cluster, remove for subsequent restarts\n# cluster.initial_master_nodes: [\"es-master-01\", \"es-master-02\", \"es-master-03\"]\n\n# In older versions (Elasticsearch 6.x and below), this was crucial for split-brain prevention.\n# (Total master-eligible nodes / 2) + 1. For 3 master-eligible nodes, it's (3/2)+1 = 2.\n# discovery.zen.minimum_master_nodes: 2 \n\n# List of seed hosts for discovery, ensuring all nodes know about master-eligible nodes.\n# Use their hostnames or IPs. Ensure these are consistent and reachable.\ndiscovery.seed_hosts: [\"es-master-01\", \"es-master-02\", \"es-master-03\"]\n\n# --- Data node configuration (e.g., node04, node05) ---\n# node.name: es-data-01\n# node.roles: [ data, ingest ] # Can also be ingest, ml, etc.\n# A data node should NOT be master-eligible for best practice in larger clusters.\n# node.roles: [ data ]\n\n# Shard allocation settings to prevent rebalancing storms\n# Adjust these based on your cluster's I/O capacity and network bandwidth.\n# Default values are often conservative. Increase gradually if needed.\ncluster.routing.allocation.cluster_concurrent_rebalance: 2 # Default is 2. Reduce if needed.\ncluster.routing.allocation.node_concurrent_incoming_recoveries: 2 # Default is 2. Reduce if needed.\ncluster.routing.allocation.node_concurrent_outgoing_recoveries: 2 # Default is 2. Reduce if needed.\ncluster.routing.allocation.enable: all # Ensures allocation is enabled\n\n# Disable allocation for specific indices if needed during maintenance\n# index.blocks.read_only_allow_delete: true\n```",
    "verification": "To verify the fix, perform a series of controlled tests: <br/> 1. **Network Partition Simulation:** Temporarily block network traffic between groups of nodes (e.g., using `iptables`) to simulate a split-brain condition. Observe cluster logs (`_cat/master` and `_cat/nodes`) to ensure only one master is elected and that other nodes correctly recognize the master or become `non_master`. The cluster should gracefully degrade or recover without forming multiple active masters. <br/> 2. **Node Failure/Restart Simulation:** Gracefully shut down and restart data nodes, and then master-eligible nodes one by one. Monitor `_cat/health`, `_cat/recovery`, and `_cat/allocation` APIs to observe shard rebalancing. Ensure that rebalancing completes in a timely manner without causing excessive load or triggering cascading failures. The cluster status should return to green. <br/> 3. **Load Testing:** Subject the cluster to peak write and read loads while simulating node failures. Monitor CPU, memory, disk I/O, and network usage across all nodes using tools like Prometheus/Grafana or Elasticsearch's built-in monitoring (Stack Monitoring) to ensure the cluster remains stable and performs within acceptable latency bounds during and after rebalancing events.",
    "date": "2026-09-23",
    "id": 1790150885,
    "type": "error"
});