window.onPostDataLoaded({
    "title": "Linux CFS Throttling: Micro-Burst Tail Latency",
    "slug": "linux-cfs-throttling-micro-burst-tail-latency-cpu-starvation",
    "language": "Linux Kernel",
    "code": "CPU Starvation",
    "tags": [
        "Linux",
        "Containers",
        "Kubernetes",
        "Docker",
        "Error Fix"
    ],
    "analysis": "<p>The Linux Completely Fair Scheduler (CFS) provides CPU bandwidth control through cgroups, using parameters like <code>cpu.cfs_period_us</code> and <code>cpu.cfs_quota_us</code>. These settings define how much CPU time a cgroup (and thus a container) can consume within a specified period. For example, a <code>quota</code> of 50,000 \u00b5s (50ms) within a <code>period</code> of 100,000 \u00b5s (100ms) means the container can use 50% of a CPU core every 100ms.</p><p>While this mechanism effectively limits average CPU consumption, it introduces a significant challenge for latency-sensitive applications: micro-burst tail latency and CPU starvation. The quota is not distributed evenly across the period; it's a budget that can be consumed at the beginning of the period. If a container has a short, intense burst of CPU activity (e.g., handling a sudden spike in requests, garbage collection, or a complex database query), it can quickly deplete its entire allocated quota within a few milliseconds, even if the average CPU usage for the entire period remains low. Once the quota is exhausted, the container's processes are throttled (put to sleep) until the next `cfs_period_us` begins, regardless of how much CPU is available on the host. This sudden throttling causes arbitrary delays and massive increases in tail latencies (p99, p99.9), creating an unpredictable and poor user experience, even when the system appears to have ample idle CPU capacity.</p>",
    "root_cause": "The CFS bandwidth control mechanism, by design, allows for burst consumption of CPU quota within a period. Intense, short-lived CPU demands (micro-bursts) can rapidly exhaust the entire `cfs_quota_us`, leading to immediate and severe throttling until the start of the next `cfs_period_us`. This 'budget exhaustion' behavior introduces artificial latency, as processes are forcibly idled even if the physical CPU is underutilized on average.",
    "bad_code": "```yaml\n# Kubernetes Pod/Container spec with restrictive CPU limits\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-latency-sensitive-app\nspec:\n  containers:\n  - name: app-container\n    image: my-image:latest\n    resources:\n      limits:\n        cpu: \"500m\" # Corresponds to cpu.cfs_quota_us=50000, cpu.cfs_period_us=100000\n      requests:\n        cpu: \"200m\"\n```\n<p>Or a Docker run command:</p>\n```bash\ndocker run --cpu-quota=50000 --cpu-period=100000 my-image\n```\n<p>A <code>cpu-limit</code> of <code>500m</code> (50% of a core) might seem reasonable, but can cause severe throttling if the application experiences micro-bursts that exceed this budget rapidly.</p>",
    "solution_desc": "Mitigating CFS bandwidth throttling's impact on tail latency involves rethinking how CPU limits are applied and how applications handle burstiness:\n<ol>\n    <li><b>Increase CPU Quota and Period:</b> The most direct fix is to increase <code>cpu.cfs_quota_us</code> (or Kubernetes <code>cpu.limits</code>). A common strategy is to set <code>quota</code> equal to <code>period</code> (e.g., 100,000 \u00b5s/100,000 \u00b5s for 1 full core) for latency-critical services, or even remove CPU limits entirely if over-provisioning isn't a concern. If a fractional CPU is still needed, increase both proportionally (e.g., 200,000 \u00b5s quota, 400,000 \u00b5s period for 0.5 CPU) to allow larger bursts.</li>\n    <li><b>Observe and Right-Size:</b> Monitor <code>/sys/fs/cgroup/cpu/cpu.stat</code> (for cgroup v1) or <code>/sys/fs/cgroup/cpu.stat</code> (for cgroup v2) for `nr_throttled` and `throttled_time` metrics. High values indicate active throttling. Use this data to determine optimal CPU limits, prioritizing performance over strict resource isolation if tail latency is critical.</li>\n    <li><b>Application-Level Throttling/Resilience:</b> Design applications to be less bursty, or more resilient to intermittent CPU unavailability. This could involve internal queues, rate limiting, or distributing bursty workloads across more instances.</li>\n    <li><b>CPU Pinning (<code>cpuset</code>):</b> For extreme cases where predictable latency is paramount and shared CPU resources are problematic, `cpuset` (e.g., Kubernetes `Guaranteed` QoS class combined with `cpuManager` policy `static`) can assign dedicated CPU cores to containers, completely bypassing CFS bandwidth control for those pinned cores. This provides strict isolation but can lead to lower overall host utilization.</li>\n</ol>\n<p>The core idea is to provide enough CPU budget within each period to absorb expected bursts without triggering throttling, or to remove the throttling mechanism where appropriate.</p>",
    "good_code": "```yaml\n# Kubernetes Pod/Container spec with increased CPU limits (or removed)\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-latency-sensitive-app-fixed\nspec:\n  containers:\n  - name: app-container\n    image: my-image:latest\n    resources:\n      limits:\n        # Option 1: Increase quota to allow larger bursts\n        cpu: \"2000m\" # Allows up to 2 full cores, less likely to hit throttling with bursts\n        # Option 2: Remove CPU limits if host resources allow (often preferred for critical apps)\n        # cpu: \"4\" # Or higher, effectively removing the quota if it matches allocatable\n      requests:\n        cpu: \"500m\" # Requests should still be set for scheduling\n```\n<p>Or a Docker run command:</p>\n```bash\n# Option 1: Increase quota, e.g., for 2 full cores over 100ms period\ndocker run --cpu-quota=200000 --cpu-period=100000 my-image\n# Option 2: Effectively remove quota if host has many cores (e.g., 8 cores)\n# This is not strictly removing but setting it very high.\ndocker run --cpu-quota=800000 --cpu-period=100000 my-image\n```\n<p>For Kubernetes, typically you would set <code>limits.cpu</code> higher, or even omit it if a shared CPU pool is acceptable and cluster capacity management is handled differently.</p>",
    "verification": "After applying the fix (increasing CPU limits or removing them), monitor your application's tail latency metrics (p99, p99.9, p99.99) for a significant reduction and increased stability. Crucially, observe the cgroup CPU throttling statistics: check `nr_throttled` and `throttled_time` from `cpu.stat` (or `cpu.pressure` in cgroup v2) within the container or host. These metrics should show a drastic decrease, ideally approaching zero, indicating that the container is no longer being artificially throttled by CFS. Tools like `cadvisor`, Prometheus with `node_exporter`, or Kubernetes metrics APIs can provide easy access to these statistics.",
    "date": "2026-09-14",
    "id": 1789352792,
    "type": "error"
});