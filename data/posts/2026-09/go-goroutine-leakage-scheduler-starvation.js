window.onPostDataLoaded({
    "title": "Go: Goroutine Leaks & Scheduler Starvation",
    "slug": "go-goroutine-leakage-scheduler-starvation",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Concurrency",
        "Performance",
        "Go",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leakage is a common but insidious problem in Go applications where goroutines are started but never properly terminated. Each goroutine consumes memory (stack space, typically 2KB initially but can grow), and if thousands or millions of them accumulate over time, they can exhaust available memory, leading to OutOfMemory (OOM) errors. Beyond memory, an excessive number of goroutines can lead to a phenomenon known as 'scheduler starvation'.</p><p>Go's scheduler (M:N, mapping M goroutines to N OS threads) is highly efficient, but it has limits. When there are too many runnable goroutines, the scheduler spends more time context switching and managing these goroutines, leading to increased CPU usage for scheduling overhead and reduced throughput for actual application logic. Furthermore, if many goroutines are blocked indefinitely (e.g., waiting on a channel that will never receive or send), they still occupy scheduler resources to some extent, waiting for an event that never comes. This can starve other legitimate goroutines of CPU time, making the application unresponsive and slow, even if CPU utilization isn't at 100% for productive work.</p><p>Common causes include: unhandled blocking channel operations (sending to a channel with no receiver, receiving from a channel with no sender), launching goroutines without a mechanism to signal their termination, or long-running operations without timeouts.</p>",
    "root_cause": "Unbounded goroutine creation or indefinite blocking operations without proper termination mechanisms (e.g., context cancellation), leading to resource exhaustion and excessive scheduler overhead.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc leakyWorker(dataChan <-chan int) {\n\tgo func() {\n\t\tfor data := range dataChan {\n\t\t\t// Simulate processing work\n\t\t\tfmt.Printf(\"Processing: %d\\n\", data)\n\t\t\ttime.Sleep(100 * time.Millisecond)\n\t\t}\n\t\tfmt.Println(\"Worker exiting (this might never happen if channel isn't closed).\")\n\t}()\n}\n\nfunc main() {\n\tdataCh := make(chan int)\n\n\t// Launch a worker that expects data from dataCh\n\tleakyWorker(dataCh)\n\n\t// Send some data\n\tfor i := 0; i < 5; i++ {\n\t\tdataCh <- i\n\t}\n\n\t// The main function never closes dataCh, nor signals the worker to stop.\n\t// The goroutine launched by leakyWorker will block indefinitely on 'range dataChan'.\n\t// It will never exit, leading to a goroutine leak.\n\tfmt.Println(\"Main function finished sending data. Goroutine will leak.\")\n\ttime.Sleep(5 * time.Second) // Keep main alive to observe the leak (e.g., via profiler)\n\t// If more leakyWorker calls are made, more goroutines will accumulate.\n}\n```",
    "solution_desc": "The most robust solution to goroutine leakage is to use `context.Context` for cancellation and explicit signaling. Every long-running goroutine, especially those interacting with channels or performing I/O, should accept a `context.Context` and listen for its `Done()` signal. When the context is cancelled, the goroutine should gracefully exit, releasing its resources. For channel operations, use a `select` statement to listen for both data and the context's `Done()` signal. Ensure that channels are closed when no more data will be sent, or design patterns that prevent indefinite blocking (e.g., buffered channels with appropriate capacity, or fan-out/fan-in patterns that manage channel lifetimes).",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc resilientWorker(ctx context.Context, dataChan <-chan int) {\n\tgo func() {\n\t\tdefer fmt.Println(\"Resilient worker goroutine exited.\")\n\t\tfor {\n\t\t\tselect {\n\t\t\tcase <-ctx.Done():\n\t\t\t\t// Context was cancelled, time to exit\n\t\t\t\treturn\n\t\t\tcase data, ok := <-dataChan:\n\t\t\t\tif !ok {\n\t\t\t\t\t// Channel was closed, no more data\n\t\t\t\t\treturn\n\t\t\t\t}\n\t\t\t\t// Simulate processing work\n\t\t\t\tfmt.Printf(\"Processing: %d\\n\", data)\n\t\t\t\ttime.Sleep(100 * time.Millisecond)\n\t\t\t}\n\t\t}\n\t}()\n}\n\nfunc main() {\n\tctx, cancel := context.WithCancel(context.Background())\n\tdataCh := make(chan int)\n\n\t// Launch a worker that respects context cancellation\n\tresilientWorker(ctx, dataCh)\n\n\t// Send some data\n\tfor i := 0; i < 5; i++ {\n\t\tdataCh <- i\n\t}\n\n\t// After some time or when work is done, cancel the context\n\tfmt.Println(\"Main function finished sending data. Signaling worker to stop...\")\n\tclose(dataCh) // Close channel to signal no more data (optional, but good practice)\n\tcancel()      // Cancel the context to terminate the goroutine\n\n\ttime.Sleep(1 * time.Second) // Give some time for the goroutine to clean up and exit\n\tfmt.Println(\"Main function finished.\")\n}\n```",
    "verification": "<p>To verify the fix, monitor the number of active goroutines using `runtime.NumGoroutine()` before and after the goroutines are expected to terminate. Tools like `go tool pprof` can be used to generate CPU and memory profiles, allowing you to inspect active goroutines and their stacks. A declining goroutine count (or one that stabilizes at a reasonable baseline after operations complete) indicates successful termination. Additionally, observe the application's memory footprint; it should not continuously grow when idle or after specific operations complete. Check logs for confirmation messages (like \"Resilient worker goroutine exited.\"). For scheduler starvation, observe CPU utilization and latency metrics; a healthy system should show CPU utilization proportional to actual work and stable response times.</p>",
    "date": "2026-09-09",
    "id": 1788919935,
    "type": "error"
});