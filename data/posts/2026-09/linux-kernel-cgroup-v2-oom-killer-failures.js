window.onPostDataLoaded({
    "title": "cgroup v2 Resource Isolation Failures & OOM Killer",
    "slug": "linux-kernel-cgroup-v2-oom-killer-failures",
    "language": "Linux Kernel",
    "code": "OOM Killer",
    "tags": [
        "Linux Kernel",
        "cgroup",
        "Resource Management",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Linux cgroup v2 provides a hierarchical and unified mechanism for managing system resources across different processes and containers. While powerful, misconfigurations or a lack of understanding of its nuances, particularly concerning memory management, can lead to unexpected Out-Of-Memory (OOM) killer invocations. This is especially prevalent in containerized environments (e.g., Docker, Kubernetes) where containers are given strict memory limits. The OOM killer intervenes when the system's total available memory (including swap) is exhausted, or when a cgroup exceeds its defined memory limits, leading to process termination to free up resources. Failures often stem from misinterpreting `memory.high` vs `memory.max`, accounting discrepancies, or the aggressive nature of the OOM killer when not properly configured for specific workloads.</p><p>Understanding how the kernel accounts for memory (e.g., page cache, kernel memory) and how different cgroup parameters interact is crucial. A common scenario involves a container configured with a `memory.max` that is too low for its peak workload, or without a `memory.high` limit, causing performance degradation before an OOM kill. Furthermore, shared memory and specific kernel overheads might not be fully controllable within a cgroup, leading to a container hitting OOM while the host still appears to have available memory.</p>",
    "root_cause": "Misconfiguration of cgroup v2 memory limits (e.g., `memory.max`, `memory.high`) leading to premature OOM killer invocation, or discrepancies in kernel memory accounting causing resource exhaustion within a cgroup before system-wide memory is critically low. Lack of proper `memory.swap.max` settings can also exacerbate the issue by allowing excessive swapping before OOM.",
    "bad_code": "```bash\ndocker run -d --name my-app --memory=512m my-image\n```\n<p>This command sets `memory.max` to 512MB. If the application has bursty memory usage or a large page cache requirement, it might hit OOM prematurely without adequate warning from `memory.high` or allowing for controlled memory pressure. Without explicit swap limits, the container might also excessively swap, leading to performance degradation before being killed.</p>",
    "solution_desc": "Architecturally, the solution involves a multi-pronged approach: 1) Precisely define `memory.max` based on peak application requirements, including shared memory and page cache. 2) Implement `memory.high` to create a soft limit, allowing the kernel to proactively reclaim memory or throttle processes within the cgroup before the hard `memory.max` limit is reached. This provides a grace period and prevents sudden OOM kills. 3) Configure `memory.swap.max` to prevent excessive swapping which can lead to performance degradation. 4) Utilize `memory.oom.group` to ensure that if OOM occurs, only processes within that specific cgroup are targeted, rather than potentially critical host processes. 5) Monitor cgroup `memory.events` and `dmesg` to understand memory pressure and OOM signals.",
    "good_code": "```bash\ndocker run -d --name my-app \\\n  --memory=1g \\\n  --memory-swap=2g \\\n  --oom-score-adj=500 \\\n  --ulimit memlock=-1:-1 \\\n  --cap-add SYS_RESOURCE \\\n  my-image\n```\n<p>This example sets `memory.max` to 1GB and allows 1GB of swap (`memory-swap` combines `memory.max` and `memory.swap.max`). The `--oom-score-adj` influences the OOM killer's preference. For containers requiring strict memory handling, it's often beneficial to run with `memory.high` as well, which is harder to set directly with Docker CLI but possible via Kubernetes or systemd cgroup configurations:</p>\n\n<p>Equivalent `systemd` unit settings:</p>\n```ini\n[Service]\nMemoryMax=1G\nMemorySwapMax=1G\nMemoryHigh=800M\nOOMScoreAdjust=500\n```\n<p>This configuration uses `MemoryHigh` to signal memory pressure earlier, allowing the system to react more gracefully.</p>",
    "verification": "<p>Verify the fix by:</p><ul><li>**Monitoring `dmesg` output:** Check for OOM killer messages specifically for your application.</li><li>**Observing cgroup `memory.events`:** Look for `low`, `high`, `oom`, and `oom_kill` events (`cat /sys/fs/cgroup/memory/my-cgroup/memory.events`). Ideally, you'll see `high` events indicating proactive memory reclamation, but not `oom_kill`.</li><li>**Application Stability under load:** Stress-test the application with simulated peak loads and observe its memory usage and stability.</li><li>**Resource Monitoring:** Use tools like `cadvisor`, `node_exporter`, or `kubectl top` (in Kubernetes) to track container memory usage against its limits.</li><li>**Eliminating unexpected terminations:** Confirm that the application no longer experiences sudden, unexplained process terminations due to OOM.</li></ul>",
    "date": "2026-09-07",
    "id": 1788791966,
    "type": "error"
});