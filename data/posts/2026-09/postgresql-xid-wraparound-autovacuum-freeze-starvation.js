window.onPostDataLoaded({
    "title": "PostgreSQL XID Wraparound & Autovacuum Freeze Starvation",
    "slug": "postgresql-xid-wraparound-autovacuum-freeze-starvation",
    "language": "PostgreSQL",
    "code": "XIDWraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "Infra",
        "SQL",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit Transaction ID (XID) for every transaction. This XID increments with each transaction and is used internally for MVCC (Multi-Version Concurrency Control) visibility checks. Crucially, older rows in tables must be 'frozen' (marked with a special XID value) to prevent them from becoming invisible due to XID wraparound. If the current XID wraps around to a value smaller than an existing row's XID, that row would appear to be in the future and thus invisible.</p><p>The XID counter is finite; after approximately 2 billion transactions, it will 'wrap around'. To prevent data loss or database shutdown, PostgreSQL tracks a `datfrozenxid` for each database, representing the oldest transaction ID that is still visible. If the current transaction ID approaches `datfrozenxid` too closely (specifically, `current_xid - datfrozenxid` exceeds `autovacuum_freeze_max_age`), the database will enter an emergency shutdown. The `autovacuum` daemon is responsible for regularly scanning tables, marking old rows as frozen, and advancing `datfrozenxid` to prevent this catastrophic event. When autovacuum fails to keep up, often due to being starved of resources or blocked, the database faces an imminent wraparound shutdown.</p>",
    "root_cause": "The primary root cause is the `autovacuum` daemon failing to process and 'freeze' old transaction IDs on tables in a timely manner. This can happen due to several factors: <ul><li><b>Autovacuum is disabled or misconfigured:</b> Global or table-level settings preventing it from running.</li><li><b>Long-running transactions:</b> These can hold `datfrozenxid` preventing autovacuum from marking rows as frozen, as it cannot vacuum rows visible to active transactions.</li><li><b>Insufficient resources:</b> Autovacuum workers may not have enough CPU, I/O, or memory to keep up with the transaction rate, especially on large or very busy tables.</li><li><b>Aggressive `autovacuum_vacuum_cost_delay`:</b> A high delay value can slow down autovacuum significantly.</li><li><b>Table bloat:</b> Extremely bloated tables require more effort for autovacuum to process.</li></ul>",
    "bad_code": "No specific 'bad code' snippet in the application directly causes this; it's an operational/configuration issue. However, one might check `autovacuum` settings:<pre><code class=\"language-sql\">-- Check global autovacuum settings\nSHOW autovacuum;\nSHOW autovacuum_freeze_max_age;\nSHOW autovacuum_max_workers;\nSHOW autovacuum_vacuum_cost_delay;\n\n-- Check table-specific settings (if overridden)\nSELECT relname, reloptions FROM pg_class WHERE relname = 'your_table';</code></pre>And the current state of frozen XIDs:<pre><code class=\"language-sql\">-- Check age of the oldest unfrozen XID for each database\nSELECT datname, age(datfrozenxid) AS xid_age_from_frozen, current_setting('autovacuum_freeze_max_age')::int AS freeze_max_age\nFROM pg_database\nORDER BY xid_age_from_frozen DESC;\n\n-- Check oldest XID for specific tables\nSELECT relname, age(relfrozenxid) FROM pg_class WHERE relkind = 'r' ORDER BY age(relfrozenxid) DESC;</code></pre>",
    "solution_desc": "Addressing XID wraparound and autovacuum starvation requires a multi-pronged approach: <ul><li><b>Enable & Configure Autovacuum:</b> Ensure `autovacuum` is enabled (`autovacuum = on`) and its parameters are tuned for your workload. Increase `autovacuum_max_workers` if needed, and consider decreasing `autovacuum_vacuum_cost_delay` for more aggressive vacuuming on busy systems.</li><li><b>Identify & Kill Long Transactions:</b> Regularly monitor `pg_stat_activity` to find long-running transactions that might be blocking autovacuum and terminate them if safe to do so.</li><li><b>Manual Vacuum & Freeze:</b> In critical situations, manually run `VACUUM FREEZE` on tables with high XID ages. This is an intensive operation and should be done carefully. For specific tables, `VACUUM FREEZE VERBOSE your_table;` provides progress.</li><li><b>Index Maintenance:</b> Bloated indexes can slow down vacuuming. Rebuilding indexes (e.g., `REINDEX TABLE your_table;`) can help.</li><li><b>Table Partitioning:</b> For very large tables, partitioning can break them into smaller, more manageable chunks for autovacuum.</li><li><b>Monitor:</b> Implement robust monitoring for XID age, autovacuum activity, and transaction duration to catch issues early.</li></ul>",
    "good_code": "<pre><code class=\"language-sql\">-- Tune autovacuum parameters (example, adjust based on workload)\nALTER SYSTEM SET autovacuum_max_workers = '5';\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '10ms';\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = '1000';\nSELECT pg_reload_conf();\n\n-- Manually vacuum and freeze a critical table (use with caution, can be resource-intensive)\nVACUUM (FREEZE, VERBOSE, ANALYZE) your_critical_table;\n\n-- Identify long-running transactions (potential blockers)\nSELECT pid, usename, client_addr, backend_start, state, query_start, query\nFROM pg_stat_activity\nWHERE state != 'idle'\nAND query NOT LIKE 'autovacuum:%'\nAND query_start &lt; now() - interval '5 minutes'\nORDER BY query_start;\n\n-- To cancel a blocking transaction (replace &lt;pid&gt; with actual PID)\n-- SELECT pg_cancel_backend(&lt;pid&gt;);</code></pre>",
    "verification": "After applying fixes, monitor the XID age of your databases and tables using the provided SQL queries (e.g., `SELECT datname, age(datfrozenxid) FROM pg_database;`). The `xid_age_from_frozen` values should start decreasing and remain well below `autovacuum_freeze_max_age`. Check PostgreSQL logs for `autovacuum` activity to ensure it's running regularly and processing tables without errors or significant delays. Observe `pg_stat_user_tables` to see `last_autovacuum` timestamps and `relfrozenxid` values for individual tables.",
    "date": "2026-09-15",
    "id": 1789460179,
    "type": "error"
});