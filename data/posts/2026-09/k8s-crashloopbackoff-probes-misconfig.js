window.onPostDataLoaded({
    "title": "K8s CrashLoopBackOff: Liveness/Readiness Probe Misconfig",
    "slug": "k8s-crashloopbackoff-probes-misconfig",
    "language": "Kubernetes, YAML",
    "code": "CrashLoopBackOff",
    "tags": [
        "Kubernetes",
        "Containerization",
        "DevOps",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>The `CrashLoopBackOff` status in Kubernetes indicates that a pod's container has repeatedly started, crashed, and restarted. This loop often happens due to application errors within the container, but a very common, yet often overlooked, cause is the misconfiguration of Kubernetes Liveness and Readiness probes. These probes are critical for Kubernetes to manage the lifecycle and availability of your applications.</p><p>A **Liveness Probe** determines if a container is running and healthy. If it fails, Kubernetes kills the container and restarts it. A misconfigured Liveness Probe might check for a condition the application isn't ready for yet, or it might be too aggressive (e.g., short `initialDelaySeconds`, low `timeoutSeconds`), leading to the container being prematurely killed and restarted in an endless cycle, resulting in `CrashLoopBackOff`.</p><p>A **Readiness Probe** determines if a container is ready to serve traffic. If it fails, Kubernetes removes the pod's IP address from the service's endpoints, preventing traffic from being routed to it. While a failing Readiness Probe doesn't cause `CrashLoopBackOff` directly (it results in a `NotReady` state), if combined with a problematic Liveness Probe or underlying application issue, it can complicate debugging. The core problem is that if the Liveness Probe keeps failing, the container never gets a chance to become ready, hence `CrashLoopBackOff` persists.</p>",
    "root_cause": "The specific technical reason for `CrashLoopBackOff` due to probe misconfiguration is typically an overly aggressive Liveness Probe. This often involves: 1) `initialDelaySeconds` being too short for the application to fully initialize. 2) `timeoutSeconds` being too low, causing the probe to fail if the application is temporarily slow. 3) The probe endpoint checking an aspect of the application that isn't stable early in its lifecycle or is prone to transient failures. 4) The application itself crashing immediately after startup, which is then exacerbated by the Liveness Probe continually restarting it.",
    "bad_code": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-web-app\nspec:\n  replicas: 1\n  selector:\n    matchLabels:\n      app: my-web-app\n  template:\n    metadata:\n      labels:\n        app: my-web-app\n    spec:\n      containers:\n      - name: web-container\n        image: my-private-repo/my-web-app:1.0.0\n        ports:\n        - containerPort: 8080\n        livenessProbe:\n          httpGet:\n            path: /healthz\n            port: 8080\n          initialDelaySeconds: 5  # BAD: Too short, app might not be ready yet\n          periodSeconds: 3      # BAD: Too frequent, triggers restarts quickly\n          timeoutSeconds: 1     # BAD: Too low, might fail on transient network lag\n          failureThreshold: 1   # BAD: Fails immediately on first check\n        readinessProbe:\n          httpGet:\n            path: /ready\n            port: 8080\n          initialDelaySeconds: 2 # Could be too short for slow apps\n          periodSeconds: 5\n          timeoutSeconds: 2\n          failureThreshold: 3\n```",
    "solution_desc": "To fix `CrashLoopBackOff` due to probe misconfiguration, adjust the probe parameters to be more forgiving during application startup and robust during runtime. Increase `initialDelaySeconds` for both Liveness and Readiness probes to allow the application sufficient time to fully initialize. Set `timeoutSeconds` to a reasonable value (e.g., 5-10 seconds) to account for transient delays. Increase `periodSeconds` to reduce the frequency of checks, giving the application more time to recover between checks. Crucially, set `failureThreshold` higher (e.g., 3-5) for Liveness probes, so that a container isn't killed after just one failed check. Ensure the probe endpoints (`/healthz`, `/ready`) are distinct and correctly reflect the application's state: `/healthz` for core process health (quick and reliable), `/ready` for external dependency checks and readiness to serve traffic (may take longer).",
    "good_code": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-web-app\nspec:\n  replicas: 1\n  selector:\n    matchLabels:\n      app: my-web-app\n  template:\n    metadata:\n      labels:\n        app: my-web-app\n    spec:\n      containers:\n      - name: web-container\n        image: my-private-repo/my-web-app:1.0.0\n        ports:\n        - containerPort: 8080\n        livenessProbe:\n          httpGet:\n            path: /healthz\n            port: 8080\n          initialDelaySeconds: 30 # GOOD: Increased, allow app to warm up\n          periodSeconds: 10       # GOOD: Less frequent checks\n          timeoutSeconds: 5       # GOOD: More time for response\n          failureThreshold: 3     # GOOD: Tolerates transient failures\n        readinessProbe:\n          httpGet:\n            path: /ready\n            port: 8080\n          initialDelaySeconds: 15 # GOOD: Allows for dependency initialization\n          periodSeconds: 5\n          timeoutSeconds: 5\n          failureThreshold: 5\n```",
    "verification": "<p>After applying the fixed YAML, use `kubectl get pods` to observe the pod's status. It should transition from `ContainerCreating` to `Running` and then `Ready` without entering `CrashLoopBackOff`. Use `kubectl describe pod <pod-name>` to check the events log for probe-related messages. Finally, `kubectl logs <pod-name>` can provide application-specific insights into startup. If the application itself is crashing, the logs will be key to understanding the underlying issue beyond just probe misconfiguration.</p>",
    "date": "2026-09-02",
    "id": 1788336175,
    "type": "error"
});