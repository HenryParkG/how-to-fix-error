window.onPostDataLoaded({
    "title": "C++20 Coroutines: Awaitable Lifetime & Memory Leaks",
    "slug": "cpp20-coroutines-lifetime-memory-leaks",
    "language": "C++",
    "code": "CoroutineLeak",
    "tags": [
        "C++",
        "Concurrency",
        "Memory Management",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>C++20 Coroutines offer a powerful way to write asynchronous code, but they introduce new challenges in lifetime management. A common pitfall is encountered when custom awaitables or the coroutine frame itself allocates resources that are not properly deallocated. This typically manifests during the suspend/resume cycle. If a coroutine is suspended (e.g., by <code>co_await</code> returning <code>std::suspend_always</code>) and then subsequently destroyed without being resumed, any resources held by its promise_type or the awaitable object itself might leak. The issue becomes complex because the coroutine's lifetime is decoupled from its caller, and its destruction path can vary significantly based on whether it completed, threw an exception, or was simply abandoned.</p><p>Specifically, if a custom awaitable allocates dynamic memory in its constructor or <code>await_suspend</code> method, and that memory is not explicitly freed in <code>await_resume</code> or the promise_type's destructor (or by RAII mechanisms), memory leaks will occur. Furthermore, if the promise_type itself allocates resources, its destructor must be carefully implemented to clean up these resources, considering all possible exit paths of the coroutine.</p>",
    "root_cause": "Mismanagement of heap-allocated memory associated with custom awaitable objects or the coroutine frame's <code>promise_type</code>, particularly when a suspended coroutine is destroyed without a proper resumption path or explicit resource cleanup.",
    "bad_code": "```cpp\n#include <coroutine>\n#include <iostream>\n\nstruct MyAwaitable {\n    int* data_ = nullptr;\n\n    bool await_ready() const { return false; }\n    void await_suspend(std::coroutine_handle<>) {\n        data_ = new int(42); // Allocation without clear deallocation path\n        std::cout << \"Awaitable suspended, data allocated.\\n\";\n    }\n    int await_resume() {\n        std::cout << \"Awaitable resumed, data is: \" << *data_ << \"\\n\";\n        // 'data_' might not be freed here if coroutine never resumes\n        return *data_;\n    }\n    \n    // Missing destructor to free data_ if awaitable is destroyed before resume\n    // ~MyAwaitable() { delete data_; }\n};\n\nstruct MyPromise {\n    int value_ = 0;\n    struct FinalSuspend {\n        bool await_ready() const noexcept { return true; }\n        void await_suspend(std::coroutine_handle<>) const noexcept { }\n        void await_resume() const noexcept { }\n    };\n    \n    MyAwaitable initial_suspend() { return {}; }\n    FinalSuspend final_suspend() noexcept { return {}; }\n    void unhandled_exception() { std::terminate(); }\n    std::coroutine_handle<MyPromise> get_return_object() {\n        return std::coroutine_handle<MyPromise>::from_promise(*this);\n    }\n    void return_value(int val) { value_ = val; }\n};\n\nstruct MyTask {\n    using promise_type = MyPromise;\n    std::coroutine_handle<MyPromise> handle_;\n\n    MyTask(std::coroutine_handle<MyPromise> h) : handle_(h) {}\n    ~MyTask() { if (handle_) handle_.destroy(); }\n};\n\nMyTask buggy_coroutine() {\n    int val = co_await MyAwaitable{}; // Allocate data here\n    co_return val; \n}\n\nint main() {\n    std::cout << \"Starting buggy coroutine example.\\n\";\n    MyTask task = buggy_coroutine(); // Coroutine created and suspended\n    // No resume, task destructor called when main exits, data_ in MyAwaitable leaks\n    std::cout << \"Buggy coroutine task created. Exiting.\\n\";\n    return 0;\n}\n```",
    "solution_desc": "To fix lifetime management and prevent memory leaks, adopt RAII (Resource Acquisition Is Initialization) principles. Ensure that any dynamic allocations within custom awaitables or the <code>promise_type</code> have corresponding deallocations in their destructors. For awaitables, if resources are allocated in <code>await_suspend</code>, consider a mechanism to free them in <code>await_resume</code> or within the awaitable's destructor if the coroutine is never resumed. Use smart pointers like <code>std::unique_ptr</code> or <code>std::shared_ptr</code> for managing dynamically allocated objects to automate memory cleanup. The <code>promise_type</code>'s destructor is crucial for releasing any resources held by the coroutine frame itself. Additionally, ensure all possible coroutine exit paths (completion, exception, destruction) correctly trigger cleanup.",
    "good_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <memory> // For std::unique_ptr\n\nstruct MyAwaitable {\n    std::unique_ptr<int> data_; // Use unique_ptr for automatic memory management\n\n    bool await_ready() const noexcept { return false; }\n    void await_suspend(std::coroutine_handle<>) {\n        data_ = std::make_unique<int>(42); // Allocation via smart pointer\n        std::cout << \"Awaitable suspended, data allocated.\\n\";\n    }\n    int await_resume() {\n        std::cout << \"Awaitable resumed, data is: \" << *data_ << \"\\n\";\n        return *data_;\n    }\n    // Destructor is implicitly handled by unique_ptr\n};\n\nstruct MyPromise {\n    int value_ = 0;\n    // Example: If promise_type needs its own resources\n    std::unique_ptr<char[]> buffer_ = std::make_unique<char[]>(100);\n\n    struct FinalSuspend {\n        bool await_ready() const noexcept { return true; }\n        void await_suspend(std::coroutine_handle<>) const noexcept { }\n        void await_resume() const noexcept { }\n    };\n    \n    MyAwaitable initial_suspend() { return {}; }\n    FinalSuspend final_suspend() noexcept { return {}; }\n    void unhandled_exception() { std::terminate(); }\n    std::coroutine_handle<MyPromise> get_return_object() {\n        return std::coroutine_handle<MyPromise>::from_promise(*this);\n    }\n    void return_value(int val) { value_ = val; }\n\n    // Destructor to ensure cleanup of promise_type specific resources\n    ~MyPromise() { \n        std::cout << \"MyPromise destructor called. Buffer freed.\\n\";\n        // unique_ptr handles buffer_ cleanup automatically\n    }\n};\n\nstruct MyTask {\n    using promise_type = MyPromise;\n    std::coroutine_handle<MyPromise> handle_;\n\n    MyTask(std::coroutine_handle<MyPromise> h) : handle_(h) {}\n    ~MyTask() { \n        if (handle_) { \n            std::cout << \"MyTask destructor called, destroying coroutine handle.\\n\";\n            handle_.destroy(); \n        }\n    }\n};\n\nMyTask correct_coroutine() {\n    int val = co_await MyAwaitable{}; // Data handled by unique_ptr\n    co_return val; \n}\n\nint main() {\n    std::cout << \"Starting correct coroutine example.\\n\";\n    MyTask task = correct_coroutine(); // Coroutine created and suspended\n    std::cout << \"Correct coroutine task created. Exiting.\\n\";\n    // Task destructor called, then handle_ is destroyed, then MyPromise destructor\n    // Then MyAwaitable's unique_ptr is destructed, freeing its memory\n    return 0;\n}\n```",
    "verification": "To verify the fix, use memory leak detection tools such as Valgrind (on Linux) or AddressSanitizer (ASan) during testing. Compile the code with ASan enabled (e.g., <code>g++ -fsanitize=address -fno-omit-frame-pointer -std=c++20 -o my_program my_program.cpp</code>) and run it. The absence of memory leak reports indicates a successful fix. Additionally, add print statements to destructors of custom awaitables and promise_type to visually confirm their invocation upon coroutine destruction or completion. Unit tests should cover scenarios where coroutines are never resumed, ensuring proper cleanup in such cases.",
    "date": "2026-09-14",
    "id": 1789399969,
    "type": "error"
});