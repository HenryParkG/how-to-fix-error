window.onPostDataLoaded({
    "title": "Elasticsearch: Shard Hotspots & Cluster Imbalance",
    "slug": "elasticsearch-shard-hotspots-cluster-imbalance",
    "language": "Elasticsearch",
    "code": "Cluster Imbalance",
    "tags": [
        "Elasticsearch",
        "Distributed Systems",
        "Performance",
        "Kubernetes",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch is a distributed, RESTful search and analytics engine that scales horizontally by distributing data across multiple nodes in a cluster. Data is partitioned into 'shards,' with each index composed of one or more primary shards and their optional replicas. These shards are distributed among the data nodes in the cluster. While this architecture provides excellent scalability and fault tolerance, it can lead to 'shard hotspots' and cluster imbalance if not managed carefully.</p><p>A shard hotspot occurs when a disproportionate amount of read or write traffic is directed towards a small subset of shards. This often happens in time-series data (e.g., logs, metrics) where all new incoming data is written to the latest active index, making its shards 'hot' for writes. Similarly, a highly queried index or specific document routing keys can create read hotspots. The consequence is that the nodes hosting these hot shards become overloaded with CPU, memory, and I/O operations, while other nodes remain underutilized. This imbalance leads to performance degradation, increased latency, slower query times, and potential cluster instability or node failures, ultimately impacting the reliability and responsiveness of your Elasticsearch deployment.</p>",
    "root_cause": "1.  **Index Hotspots**: Concentrated write traffic to newly created, often time-based, indices (e.g., all current day's logs going to a single index's shards).\n2.  **Document Hotspots**: Uneven distribution of documents within an index, often due to poor routing keys causing specific shards to receive an overwhelming amount of traffic from a few active tenants or IDs.\n3.  **Inefficient Shard Sizing**: Too many small shards lead to high overhead, while a few very large shards can make rebalancing difficult and lead to longer recovery times. Improper shard count for nodes.\n4.  **Imbalanced Primary/Replica Distribution**: An uneven distribution of primary and replica shards across nodes can lead to some nodes handling more write operations or serving more reads.\n5.  **Node Type and Resource Imbalance**: Heterogeneous nodes (e.g., different hardware specs) not properly accounted for in shard allocation, or mixing node roles (data, master) without sufficient dedicated resources.",
    "bad_code": "```json\n# Scenario: Daily index creation without rollover or ILM\n# All new writes for the current day will go to this single index.\nPUT /logs-2023.10.27\n{\n  \"settings\": {\n    \"number_of_shards\": 5,      \n    \"number_of_replicas\": 1,    \n    \"index.refresh_interval\": \"1s\" \n  },\n  \"mappings\": {\n    \"properties\": {\n      \"@timestamp\": { \"type\": \"date\" },\n      \"message\": { \"type\": \"text\" },\n      \"user_id\": { \"type\": \"keyword\" } \n    }\n  }\n}\n\n# All new log entries for 2023.10.27 will be indexed into these 5 primary shards.\n# If there's high write volume, these 5 shards and their replicas will become hotspots.\n# A specific 'user_id' might also create a hotspot if used for routing and is very active.\n```",
    "solution_desc": "Addressing shard hotspots and cluster imbalance requires a multi-pronged strategy focused on better data distribution and resource management:\n1.  **Index Lifecycle Management (ILM)**: Implement ILM policies to automate index rollovers based on size, age, or document count. This creates new indices regularly, spreading write traffic across multiple 'hot' indices rather than concentrating it on one.\n2.  **Optimal Shard Sizing**: Aim for shard sizes between 10GB and 50GB (or up to 100GB for hot-tier nodes) for optimal performance. Reindex or shrink large shards, and avoid having too many small shards.\n3.  **Custom Routing**: Use custom routing (`_routing` field) carefully. While it can co-locate related documents on a single shard, if the routing key is highly active, it can create a hotspot on that specific shard. Consider tenant-specific indices for extremely large or active tenants.\n4.  **Data Tiers**: Implement a hot-warm-cold architecture. Hot data (recent, frequently accessed, high write) resides on fast, powerful nodes. Warm data moves to nodes with more capacity/cheaper storage. Cold data is archived or moved to very cheap, slow storage.\n5.  **Shard Allocation Awareness**: Configure allocation awareness (e.g., `cluster.routing.allocation.awareness.attributes`) to ensure shards are distributed across different physical racks, availability zones, or even Kubernetes nodes.\n6.  **Monitoring and Alerting**: Continuously monitor node-level metrics (CPU, memory, disk I/O, search/index rates, JVM heap) and shard distribution using tools like Kibana, Cerebro, or Elastic Cloud's monitoring. Set up alerts for high resource utilization on specific nodes/shards.\n7.  **Even Replica Distribution**: Ensure primary and replica shards are evenly distributed across nodes to provide redundancy and balance read load. Elasticsearch's default allocation usually handles this, but manual intervention might be needed in specific scenarios.\n8.  **Force Merge**: For read-heavy, immutable indices, `_forcemerge` (especially to 1 segment) can reduce resource consumption by optimizing the underlying Lucene segments.",
    "good_code": "```json\n# Step 1: Define an Index Lifecycle Management (ILM) policy\nPUT /_ilm/policy/my_logs_rollover_policy\n{\n  \"policy\": {\n    \"phases\": {\n      \"hot\": {\n        \"actions\": {\n          \"rollover\": { \n            \"max_age\": \"1d\",             \n            \"max_docs\": 50000000,        \n            \"max_size\": \"30gb\"          \n          },                             \n          \"set_priority\": { \"priority\": 100 }\n        }\n      },\n      \"warm\": {\n        \"min_age\": \"7d\",                 \n        \"actions\": {\n          \"forcemerge\": { \"max_num_segments\": 1 }, \n          \"shrink\": { \"number_of_shards\": 1 },     \n          \"set_priority\": { \"priority\": 50 }\n        }\n      },\n      \"cold\": {\n        \"min_age\": \"30d\",                \n        \"actions\": {\n          \"set_priority\": { \"priority\": 0 }\n        }\n      },\n      \"delete\": {\n        \"min_age\": \"90d\",                \n        \"actions\": { \"delete\": {} }\n      }\n    }\n  }\n}\n\n# Step 2: Create an index template to apply the ILM policy to new indices\nPUT /_index_template/logs_template\n{\n  \"index_patterns\": [\"logs-*\"],\n  \"data_stream\": {},\n  \"template\": {\n    \"settings\": {\n      \"number_of_shards\": 3,             \n      \"number_of_replicas\": 1,           \n      \"index.lifecycle.name\": \"my_logs_rollover_policy\", \n      \"index.lifecycle.rollover_alias\": \"logs-write\" \n    },\n    \"mappings\": {\n      \"properties\": {\n        \"@timestamp\": { \"type\": \"date\", \"format\": \"yyyy-MM-dd'T'HH:mm:ss.SSSZ\" },\n        \"message\": { \"type\": \"text\" }\n      }\n    }\n  }\n}\n\n# Step 3: Create an initial data stream to start the process\n# All new documents will be written to the current 'write' index managed by this data stream.\nPUT /_data_stream/logs-write\n\n# Now, all writes to 'logs-write' alias will be directed to the latest index in the data stream.\n# When rollover conditions are met, a new index will be automatically created.\n```",
    "verification": "To verify the fix, continuously monitor your Elasticsearch cluster using Kibana's Stack Monitoring, or other tools like Cerebro or Elastic Cloud UI. Specifically, look for:\n1.  **Node Resource Utilization**: Check CPU, memory, JVM heap, and disk I/O metrics across all data nodes. Confirm that these resources are now evenly distributed and no single node is consistently overloaded.\n2.  **Shard Distribution**: Visualize the distribution of shards (primary and replica) across your data nodes. Ensure an even spread. Elastic's built-in rebalancer should distribute them, but confirm the visual representation.\n3.  **ILM Policy Execution**: Verify that ILM policies are correctly rolling over indices when conditions (max age, size, docs) are met. Check the `_ilm/explain` API for indices under ILM.\n4.  **Indexing and Search Performance**: Monitor indexing rates and search latencies. They should be stable and within acceptable limits, without spikes indicating bottlenecks.\n5.  **No More OOM/Disk Errors**: Confirm that no more out-of-memory errors or excessive disk usage alerts are triggered in your node logs or monitoring system.",
    "date": "2026-09-02",
    "id": 1788314684,
    "type": "error"
});