window.onPostDataLoaded({
    "title": "Rust: Stacked Borrows & Unsafe Pin Aliasing Hazards",
    "slug": "rust-stacked-borrows-unsafe-pin-aliasing",
    "language": "Rust",
    "code": "Stacked Borrows",
    "tags": [
        "Memory Safety",
        "Concurrency",
        "Rust",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Rust's <code>Pin</code> type is fundamental for creating self-referential structs and guaranteeing future-compatibility for asynchronous operations, especially with <code>async</code>/<code>await</code>. It achieves this by ensuring that data within a <code>Pin<&mut T></code> or <code>Pin<Box<T>></code> will not be moved in memory. However, <code>Pin</code> interacts critically with Rust's aliasing rules, specifically the \"Stacked Borrows\" model used by Miri (Rust's UB checker).</p><p>A Stacked Borrows violation occurs when an <code>unsafe</code> block creates an invalid alias, particularly when projecting fields out of a <code>Pin<&mut T></code> or <code>Pin<Box<T>></code>. While <code>Pin</code> itself guarantees immobility, directly obtaining a <code>&mut</code> reference to a pinned field (e.g., using <code>unsafe fn field_mut(self: Pin<&mut Self>) -> Pin<&mut FieldType></code>) requires careful consideration. If not done correctly, this can lead to two mutable references (or a mutable and shared reference) to the same memory location, violating Stacked Borrows and causing Undefined Behavior (UB), even if the Rust compiler accepts it.</p><p>The core problem lies in projecting out a <code>&mut</code> reference from a <code>Pin</code> without ensuring that the projected reference also maintains the immovability guarantee, or that no other mutable references to the same underlying memory exist. This is especially tricky when the projection isn't trivial or when the projected field itself needs to be <code>Pin</code>ned.</p>",
    "root_cause": "Attempting to obtain a mutable reference (`&mut T`) to a field within a `Pin<&mut S>` without using `Pin::get_unchecked_mut` or a `Pin`-aware projection method, or by creating a `&mut T` reference that overlaps with an existing `Pin<&mut T>` reference (even to a sub-part) without correctly invalidating the original `Pin`'s unique access token within the Stacked Borrows model. This leads to invalid mutable aliasing.",
    "bad_code": "```rust\nuse std::pin::Pin;\nuse std::future::Future;\n\nstruct MyFuture {\n    state: i32,\n    // Imagine some data here that needs to be self-referential or pinned\n    // For simplicity, we'll just demonstrate the projection issue.\n}\n\nimpl MyFuture {\n    // INCORRECT: This projection breaks Pin's contract and Stacked Borrows\n    // by returning a &mut i32 without proper Pin handling.\n    unsafe fn get_state_mut_bad(self: Pin<&mut Self>) -> &mut i32 {\n        &mut self.get_unchecked_mut().state\n    }\n}\n\nasync fn do_something_async() {\n    let mut my_fut = MyFuture { state: 0 };\n    let mut pinned_fut = Box::pin(my_fut);\n\n    // This operation is problematic with get_state_mut_bad\n    let state_ref = unsafe { pinned_fut.as_mut().get_state_mut_bad() };\n    *state_ref = 42;\n\n    // The original `pinned_fut` still exists and is considered active\n    // within the Stacked Borrows model, even though `state_ref` mutably\n    // aliases part of it. This is UB.\n    println!(\"State after unsafe modification: {}\", pinned_fut.state);\n}\n\n// fn main() {\n//     // For demonstration, not typically run directly like this\n//     // with async functions.\n//     futures::executor::block_on(do_something_async());\n// }\n```",
    "solution_desc": "The fundamental fix is to adhere strictly to `Pin`'s guarantees and avoid creating mutable aliases. When projecting fields from a `Pin<&mut T>`, the projected reference should also be `Pin<&mut FieldType>` to maintain the immovability contract. This is typically done using `Pin::map_unchecked_mut`. If you genuinely need a `&mut FieldType`, it implies the field is not self-referential or immovability isn't critical *for that specific field*. In such cases, you must ensure that the `Pin` contract is not violated and that no other `Pin` or `&mut` references to the same memory region exist for the duration of the new `&mut` reference.\nFor fields that *must* remain pinned, `map_unchecked_mut` is the correct approach. For fields that don't need pinning, a mutable reference can be obtained *only after* dropping the `Pin` context, e.g., by consuming `Pin<Box<T>>` into `Box<T>` using `Pin::into_inner_unchecked()` if the data is no longer needed in a pinned state.\nAlternatively, design your structs such that fields that need mutable access via non-pinned references are separated from those that require pinning, or use an `UnsafeCell` if you need interior mutability within a pinned context, but this introduces its own set of `unsafe` invariants.",
    "good_code": "```rust\nuse std::pin::Pin;\nuse std::future::Future;\n\nstruct MyFuture {\n    state: i32,\n    // More complex pinned data might be here, e.g., self-referential\n}\n\nimpl MyFuture {\n    // CORRECT: Projects a Pin<&mut i32> to the state field.\n    // This maintains the Pin contract for the projected field.\n    unsafe fn get_state_pin_mut(self: Pin<&mut Self>) -> Pin<&mut i32> {\n        self.map_unchecked_mut(|s| &mut s.state)\n    }\n}\n\nasync fn do_something_async_fixed() {\n    let my_fut = MyFuture { state: 0 };\n    let mut pinned_fut = Box::pin(my_fut);\n\n    // 1. Using the correct Pin projection:\n    let mut state_pin_ref = unsafe { pinned_fut.as_mut().get_state_pin_mut() };\n    // To modify, we need to dereference the Pin.\n    // This is safe because `Pin::get_unchecked_mut` on `state_pin_ref`\n    // does not violate any existing active Pin references from `pinned_fut`\n    // because it's a derived Pin, not a raw &mut that breaks the stack.\n    // It's conceptually 'moving' the Pin token down the stack.\n    unsafe { *state_pin_ref.as_mut().get_unchecked_mut() = 42; }\n\n    println!(\"State after safe Pin projection modification: {}\", pinned_fut.state);\n\n    // 2. If you absolutely need a non-pinned &mut reference to the whole struct\n    //    and are done with its pinned state (e.g., after the future completes):\n    //    This assumes `MyFuture` is no longer pinned or needs to be pinned.\n    let mut unpinned_fut = Pin::into_inner(pinned_fut); // Consumes the Pin<Box<T>>\n    unpinned_fut.state = 100; // Safe direct mutation\n    println!(\"State after unpinning and direct modification: {}\", unpinned_fut.state);\n}\n\n// fn main() {\n//     futures::executor::block_on(do_something_async_fixed());\n// }\n```",
    "verification": "Thoroughly test `unsafe` Rust code with Miri, the Rust-specific interpreter for checking undefined behavior. Running tests with `cargo miri test` will detect Stacked Borrows violations and other forms of UB. Additionally, extensive unit and integration tests should be written to cover all code paths involving `Pin` and `unsafe` operations. Carefully review the Rust Nomicon's `Pin` chapter and the `std::pin` module documentation to understand the invariants.",
    "date": "2026-09-25",
    "id": 1790303678,
    "type": "error"
});