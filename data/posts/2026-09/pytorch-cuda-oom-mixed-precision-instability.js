window.onPostDataLoaded({
    "title": "PyTorch: CUDA OOM & Mixed Precision Instability",
    "slug": "pytorch-cuda-oom-mixed-precision-instability",
    "language": "PyTorch, CUDA",
    "code": "CUDA OOM",
    "tags": [
        "PyTorch",
        "Deep Learning",
        "CUDA",
        "Python",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>CUDA Out-Of-Memory (OOM) errors are a common headache in deep learning, particularly when working with large models, high-resolution inputs, or ambitious batch sizes. They indicate that your GPU's VRAM is insufficient to store all model parameters, activations, and gradients required for a given forward and backward pass. While simply reducing batch size or model complexity is an option, it often comes with performance or accuracy trade-offs.</p><p>Mixed precision training addresses this by utilizing lower-precision floating-point formats, typically <code>float16</code> (half-precision), for most computations, while keeping a few critical parts (like batch normalization weights or final output layers) in <code>float32</code>. This significantly reduces memory footprint and can accelerate training on GPUs with Tensor Cores. However, naive mixed precision can introduce numerical instability: gradients, especially small ones, can underflow to zero when represented in <code>float16</code>, leading to NaN (Not-a-Number) losses and training divergence. Conversely, large gradients can overflow to infinity.</p>",
    "root_cause": "Insufficient GPU memory for the current model, batch size, and input data combination, leading to CUDA OOM. When using mixed precision, the primary root cause of instability is the limited dynamic range of <code>float16</code>, causing gradient underflow (clipping to zero) or overflow (clipping to infinity) during backpropagation.",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.layer1 = nn.Linear(1024, 2048)\n        self.relu = nn.ReLU()\n        self.layer2 = nn.Linear(2048, 1024)\n\n    def forward(self, x):\n        return self.layer2(self.relu(self.layer1(x)))\n\nmodel = SimpleModel().cuda()\noptimizer = optim.Adam(model.parameters())\ncriterion = nn.MSELoss()\n\n# Simulating large inputs, prone to OOM or instability without proper handling\nfor epoch in range(5):\n    # Imagine a large batch size here, e.g., batch_size = 512, input_dim = 1024\n    inputs = torch.randn(512, 1024).cuda() \n    targets = torch.randn(512, 1024).cuda()\n\n    optimizer.zero_grad()\n    outputs = model(inputs)\n    loss = criterion(outputs, targets)\n    loss.backward()\n    optimizer.step()\n\n    print(f\"Epoch {epoch+1}, Loss: {loss.item():.4f}\")\n```",
    "solution_desc": "To fix CUDA OOM and achieve stable mixed precision training, PyTorch's Automatic Mixed Precision (AMP) with <code>torch.cuda.amp.GradScaler</code> is the recommended approach. AMP automatically casts operations to the appropriate precision (<code>float16</code> or <code>float32</code>) based on a heuristic, saving memory and speeding up computation. <code>GradScaler</code> is crucial for stability: it scales up the loss before the backward pass, preventing small gradients from underflowing to zero. After gradients are computed in <code>float16</code>, they are unscaled back to their original magnitude before the optimizer step, which typically operates in <code>float32</code>. This dynamic scaling ensures numerical stability without manual intervention.",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nfrom torch.cuda.amp import autocast, GradScaler\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.layer1 = nn.Linear(1024, 2048)\n        self.relu = nn.ReLU()\n        self.layer2 = nn.Linear(2048, 1024)\n\n    def forward(self, x):\n        return self.layer2(self.relu(self.layer1(x)))\n\nmodel = SimpleModel().cuda()\noptimizer = optim.Adam(model.parameters())\ncriterion = nn.MSELoss()\nscaler = GradScaler() # Initialize GradScaler for mixed precision\n\n# Simulating large inputs\nfor epoch in range(5):\n    inputs = torch.randn(512, 1024).cuda() \n    targets = torch.randn(512, 1024).cuda()\n\n    optimizer.zero_grad()\n    with autocast(): # Context manager for automatic mixed precision\n        outputs = model(inputs)\n        loss = criterion(outputs, targets)\n\n    scaler.scale(loss).backward() # Scale loss before backward()\n    scaler.step(optimizer)        # Unscale gradients and call optimizer.step()\n    scaler.update()               # Update the scale factor for the next iteration\n\n    print(f\"Epoch {epoch+1}, Loss: {loss.item():.4f}\")\n```",
    "verification": "To verify the fix: 1. Monitor GPU memory usage (e.g., with <code>nvidia-smi</code>) to confirm reduced VRAM consumption compared to full <code>float32</code> training. 2. Observe the training loss curve. It should be stable and converge without sudden spikes to NaN or infinity, indicating that numerical stability issues are resolved. 3. Ensure that the training speed has improved, especially on GPUs with Tensor Cores, as mixed precision often offers significant throughput gains.",
    "date": "2026-09-19",
    "id": 1789784269,
    "type": "error"
});