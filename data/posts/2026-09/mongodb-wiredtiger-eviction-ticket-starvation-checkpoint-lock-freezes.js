window.onPostDataLoaded({
    "title": "MongoDB WiredTiger: Eviction Ticket Starvation",
    "slug": "mongodb-wiredtiger-eviction-ticket-starvation-checkpoint-lock-freezes",
    "language": "Infra",
    "code": "WiredTiger Lock",
    "tags": [
        "MongoDB",
        "NoSQL",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses a sophisticated page cache and an eviction mechanism to manage memory. It operates on an optimistic concurrency control model (MVCC), providing document-level locking. A critical component of its memory management is the concept of <strong>eviction tickets</strong>, which control the number of concurrent eviction operations.</p><p>Under sustained high write workloads, especially with random writes or large documents, the WiredTiger cache can become saturated with dirty (modified) pages faster than the eviction process can flush them to disk and free up space. This backlog causes a scarcity of eviction tickets. When other operations, such as background checkpoints, attempts to read pages into cache, or even user queries, require eviction tickets or available cache pages, they can experience <strong>eviction ticket starvation</strong>.</p><p>This starvation can lead to significant performance degradation. Most notably, <strong>checkpoint operations</strong>, which require a consistent view of the data and hold a global write lock (checkpoint lock) for a brief period, can become prolonged or even temporarily freeze. This results in high latency spikes for client applications, reduced throughput, and in severe cases, temporary unavailability of the database instance as it struggles to complete critical maintenance tasks.</p>",
    "root_cause": "Sustained high write workload, often with a working set larger than the allocated WiredTiger cache or inefficient document sizes/schema design, causing the eviction mechanism to fall behind in freeing up cache pages. This backlog, in turn, starves other critical operations (like checkpoints) of necessary resources (eviction tickets or available memory pages).",
    "bad_code": "```yaml\n# mongod.conf snippet demonstrating potential problematic configuration\nstorage:\n  wiredTiger:\n    engineConfig:\n      cacheSizeGB: 10 # This might be insufficient for the application's working set\n      # default eviction settings, which might not be aggressive enough for high write loads\n\n# Application-side: Frequent, unbatched single document writes\n# Example in Node.js\n// for (let i = 0; i < 10000; i++) {\n//     await collection.insertOne({ data: generateLargeDoc(), timestamp: new Date() });\n// }\n```",
    "solution_desc": "<p>Resolving eviction ticket starvation and checkpoint freezes primarily involves optimizing WiredTiger's cache usage and managing write pressure:</p><ol><li><b>Increase WiredTiger Cache Size</b>: The most straightforward solution, if server RAM allows. The cache should be large enough to hold the database's active working set to minimize disk I/O. MongoDB generally recommends allocating 50-75% of physical RAM to WiredTiger.</li><li><b>Optimize Write Workload</b>: <br>a. <b>Batch Writes</b>: Group multiple write operations into a single <code>insertMany</code>, <code>updateMany</code>, or <code>bulkWrite</code> call to reduce overhead.<br>b. <b>Efficient Schema Design</b>: Avoid excessively large documents or frequently updated embedded arrays that cause document growth and high write amplification.<br>c. <b>Smaller Updates</b>: Use field-specific <code>$set</code>, <code>$inc</code> operations instead of replacing entire documents.</li><li><b>Adjust Eviction Configuration</b>: Advanced tuning of <code>wiredTiger.engineConfig.eviction_dirty_target_percent</code> and <code>eviction_trigger_target_percent</code> can make eviction more aggressive or allow more dirty pages to remain in cache, but this requires careful monitoring and understanding.</li><li><b>Hardware Upgrade</b>: Faster storage (NVMe SSDs) can significantly improve the speed at which dirty pages are flushed, and more RAM directly supports a larger cache.</li><li><b>Sharding</b>: Distribute the write load horizontally across multiple <code>mongod</code> instances in a sharded cluster to reduce pressure on any single instance.</li></ol>",
    "good_code": "```yaml\n# mongod.conf snippet demonstrating improved configuration\nstorage:\n  wiredTiger:\n    engineConfig:\n      # Increase cache size to accommodate the working set (e.g., 50-75% of RAM)\n      cacheSizeGB: 30 # Example: Increased from 10GB\n      # Consider adjusting eviction aggressiveness if default is not sufficient after cache increase\n      # eviction_dirty_target_percent: 40 # Lower this to evict dirty pages sooner\n      # eviction_trigger_target_percent: 75 # Higher to start eviction only when cache is fuller\n\n# Application-side: Use bulk writes\n# Example in Node.js\nasync function performBulkInsert(collection, documents) {\n    if (documents.length > 0) {\n        // Use insertMany for efficiency, reducing network round trips and server load\n        const result = await collection.insertMany(documents, { ordered: false });\n        console.log(`${result.insertedCount} documents inserted.`);\n    }\n}\n\n// Usage example:\n// const docsToInsert = [];\n// for (let i = 0; i < 10000; i++) {\n//     docsToInsert.push({ data: generateDoc(), timestamp: new Date() });\n//     if (docsToInsert.length === 1000) { // Batch in groups of 1000\n//         await performBulkInsert(collection, docsToInsert);\n//         docsToInsert.length = 0; // Clear the array\n//     }\n// }\n// await performBulkInsert(collection, docsToInsert); // Insert any remaining docs\n```",
    "verification": "<p>To verify the effectiveness of the fix, use <code>db.serverStatus()</code> to monitor key WiredTiger metrics:</p><ol><li><b><code>db.serverStatus().wiredTiger.cache.eviction_tickets_available</code></b>: This value should consistently remain above zero and not frequently drop to low single digits or zero.</li><li><b><code>db.serverStatus().wiredTiger.checkpoint.last_finished_checkpoint_duration_seconds</code></b>: The duration of checkpoints should be stable and low. Spikes indicate issues.</li><li><b><code>db.serverStatus().metrics.operation.write.latency</code></b>: Monitor write operation latencies; these should stabilize and show fewer severe spikes.</li><li><b>WiredTiger cache statistics</b>: Observe <code>pages_read_into_cache</code> versus <code>pages_evicted_from_cache</code>. A healthy system balances these operations.</li><li><b>System-level monitoring</b>: Track disk I/O (especially write IOPS and throughput), CPU utilization, and RAM usage. Ensure they are within healthy operational ranges and not consistently saturated, which would indicate underlying resource bottlenecks.</li></ol>",
    "date": "2026-09-11",
    "id": 1789092456,
    "type": "error"
});