window.onPostDataLoaded({
    "title": "ClickHouse MergeTree: Mutation Amplification & Lockups",
    "slug": "clickhouse-mergetree-mutation-amplification-part-lockups",
    "language": "SQL",
    "code": "CDC Bottleneck",
    "tags": [
        "ClickHouse",
        "Database",
        "Real-time",
        "CDC",
        "SQL",
        "Error Fix"
    ],
    "analysis": "<p>ClickHouse's MergeTree family of table engines is optimized for high-throughput analytical workloads, primarily by appending new data in immutable parts and merging them in the background. However, when faced with a high volume of real-time Change Data Capture (CDC) events that involve frequent <code>ALTER TABLE ... UPDATE/DELETE</code> mutations on existing rows, the MergeTree engine can encounter significant performance bottlenecks, manifested as \"mutation amplification\" and \"part lockups.\"</p><p>Mutations in MergeTree are not in-place updates or deletes. Instead, they are asynchronous operations that work by rewriting entire data parts that contain the affected rows. For example, if a single row update occurs in a part, ClickHouse will re-write that entire part, marking the old part for deletion. In a high-volume CDC scenario where thousands or millions of updates/deletes are flowing into ClickHouse per second, this mechanism leads to a vicious cycle: each mutation triggers a part rewrite, which is an I/O and CPU intensive operation. If mutations occur frequently across many parts, the background merge process becomes overwhelmed. It spends all its resources rewriting parts for mutations, neglecting regular background merges of smaller parts into larger ones. This leads to an explosion in the number of small data parts, increased query latency due to more files to scan, and critical performance degradation, including potential deadlocks or lockups on parts as they become targets for multiple concurrent mutations or merges.</p>",
    "root_cause": "ClickHouse MergeTree's mutation mechanism relies on rewriting entire data parts. High-frequency, real-time CDC updates/deletes on existing rows trigger excessive part rewrites, leading to an overwhelming I/O and CPU load on the background merge scheduler. This \"mutation amplification\" prevents regular part merges, causing an accumulation of small parts, increased resource contention, and eventual part lockups or deadlocks within the merge coordinator.",
    "bad_code": "```sql\n-- Assume a table `events_log` with primary key (event_id, timestamp)\nCREATE TABLE events_log (\n    event_id UUID,\n    timestamp DateTime,\n    status String,\n    value Float64\n) ENGINE = MergeTree()\nPRIMARY KEY (event_id, timestamp)\nORDER BY (event_id, timestamp);\n\n-- Ingesting CDC updates frequently, e.g., once per second for many rows\n-- This pattern is detrimental for high update rates.\nALTER TABLE events_log UPDATE status = 'processed', value = 123.45 WHERE event_id = '...' AND timestamp = '...';\n\n-- Or frequent DELETEs\nALTER TABLE events_log DELETE WHERE timestamp < now() - INTERVAL 1 DAY;\n\n-- Direct, unbatched ingestion of individual CDC updates\n-- This is conceptually bad as it leads to high mutation rates\n-- INSERT INTO events_log ... followed by frequent ALTER TABLE UPDATE\n```\n<p>The problem isn't necessarily a specific SQL syntax but the pattern of applying frequent, unbatched <code>ALTER TABLE ... UPDATE/DELETE</code> operations, which trigger the costly part rewriting mechanism.</p>",
    "solution_desc": "Addressing mutation amplification and part lockups in ClickHouse under high CDC ingestion requires a shift in ingestion strategy and schema design:\n<ol>\n    <li><b>Batch CDC Operations:</b> Instead of applying individual `ALTER TABLE ... UPDATE/DELETE` for each CDC event, batch them into larger transactions or apply them less frequently. This reduces the number of mutation commands, allowing ClickHouse's merge processes to catch up.</li>\n    <li><b>Leverage `ReplacingMergeTree` or `VersionedCollapsingMergeTree`:</b> For upsert-like CDC streams, these engines are designed to handle new versions of rows more efficiently. New rows with the same primary key (and a higher `version` column for `ReplacingMergeTree` or an `sign` column for `VersionedCollapsingMergeTree`) will naturally replace older versions during background merges, eliminating the need for explicit `ALTER TABLE UPDATE` mutations.</li>\n    <li><b>Batch `INSERT` with `FINAL` or Materialized Views:</b> If `ReplacingMergeTree` is used, data can be inserted in batches. Queries can use `SELECT ... FINAL` to get the latest state (at the cost of query performance), or a Materialized View can continuously aggregate the latest state efficiently.</li>\n    <li><b>Optimize Data Organization:</b> Ensure data is well-ordered by the primary key to minimize the number of parts affected by a mutation.</li>\n    <li><b>Hardware Scaling and Tuning:</b> Provide ample I/O bandwidth (NVMe SSDs) and CPU resources. Tune ClickHouse merge-related settings, such as `max_concurrent_queries_for_user` (for mutations), `background_pool_size`, and `max_parts_to_merge_at_once`, though these are usually reactive measures rather than fundamental solutions.</li>\n    <li><b>Lambda Architecture / Batch Layer:</b> For extremely high-volume, real-time updates, consider a lambda architecture where raw CDC data goes into a fast, append-only store (e.g., Kafka) and a separate batch layer processes and aggregates updates into ClickHouse less frequently.</li>\n</ol>\n<p>The primary goal is to minimize the number of explicit <code>ALTER TABLE ... UPDATE/DELETE</code> operations on large parts and instead rely on ClickHouse's natural merge processes to handle row versioning.</p>",
    "good_code": "```sql\n-- Using ReplacingMergeTree for efficient upserts\nCREATE TABLE events_log_replacing (\n    event_id UUID,\n    timestamp DateTime,\n    status String,\n    value Float64,\n    _version UInt64 -- Add a version column\n) ENGINE = ReplacingMergeTree(_version)\nPRIMARY KEY (event_id)\nORDER BY (event_id, timestamp);\n\n-- Ingest CDC updates as new inserts with an incremented version number\n-- This implicitly handles updates during merges without explicit ALTER TABLE UPDATE.\nINSERT INTO events_log_replacing VALUES\n('a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11', '2023-01-01 10:00:00', 'pending', 10.0, 1),\n('a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a12', '2023-01-01 10:05:00', 'pending', 20.0, 1);\n\n-- To update an existing row (event_id 'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11'):\n-- Insert a new row with the same primary key and a higher version\nINSERT INTO events_log_replacing VALUES\n('a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11', '2023-01-01 10:10:00', 'processed', 15.0, 2); -- Note version 2\n\n-- To query the latest state (implicitly handled by merges, or explicitly with FINAL):\nSELECT * FROM events_log_replacing FINAL WHERE event_id = 'a0eebc99-9c0b-4ef8-bb6d-6bb9bd380a11';\n\n-- Batched deletion can still be done with ALTER TABLE DELETE, but less frequently.\n-- For logical deletes, you can add a 'is_deleted' flag and rely on ReplacingMergeTree\n-- to only keep the latest version (where is_deleted=1 if deleted).\n```",
    "verification": "To verify the fix, monitor ClickHouse's `system.merges` and `system.mutations` tables. You should observe a significant reduction in the number of active mutations (`is_done=0` for mutations) and a smoother, more consistent rate of background merges. System resource usage (CPU, I/O) should stabilize and reduce during periods of heavy ingestion. More importantly, observe end-to-end query latencies for your applications, especially tail latencies (p99, p99.9), which should improve and become more predictable. Ensure the number of data parts in `system.parts` does not continuously grow out of control, indicating that background merges are working effectively.",
    "date": "2026-09-14",
    "id": 1789352793,
    "type": "error"
});