window.onPostDataLoaded({
    "title": "Elasticsearch: Fielddata Circuit Breaker & Lucene Merge Starvation",
    "slug": "elasticsearch-fielddata-circuit-breaker-lucene-merge-starvation",
    "language": "Elasticsearch",
    "code": "ResourceExhaustion",
    "tags": [
        "Elasticsearch",
        "Lucene",
        "Java",
        "Infra",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch, built on Lucene, relies heavily on efficient data structures for search and analytics. Two common performance bottlenecks, often intertwined, are the `fielddata` circuit breaker tripping and Lucene merge thread starvation. Both ultimately stem from improper field mapping and high data cardinality.</p><p><b>Fielddata Circuit Breaker:</b> When performing aggregations, sorting, or scripting on `text` fields, Elasticsearch needs to load the entire field's values into JVM heap memory. This in-memory data structure is called `fielddata`. Unlike `keyword` fields which use `doc_values` (an on-disk, column-oriented structure), `text` fields are optimized for full-text search and tokenization. Loading `fielddata` for high-cardinality `text` fields can consume a significant portion of the JVM heap. The `fielddata` circuit breaker is a safeguard that prevents OutOfMemoryError (OOM) by estimating the memory required for a query and rejecting it if it exceeds a configured threshold (default 60% of JVM heap). Frequent trips lead to query failures and instability.</p><p><b>Lucene Merge Thread Starvation:</b> High ingest rates, especially for high-cardinality fields, lead to a large number of small Lucene segments being written to disk. Elasticsearch (via Lucene) continuously merges these small segments into larger ones in the background to improve query performance and reduce resource usage. This merging is performed by dedicated merge threads. If the rate of new segment creation outpaces the merge threads' ability to consolidate them, you can experience a buildup of small segments. This leads to increased I/O, higher CPU usage (for merge operations), and can starve merge threads, causing indexing to slow down or even stall. The problem is exacerbated by `text` fields with many unique terms, as each term adds to the segment complexity.</p><p>These issues are often linked: incorrect field mapping (e.g., using `text` for aggregations where `keyword` is appropriate) not only triggers the `fielddata` circuit breaker but also contributes to the overhead of creating and merging segments.</p>",
    "root_cause": "Using `text` fields with `fielddata=true` for aggregations or sorting on high-cardinality data, leading to excessive JVM heap usage and circuit breaker trips. Concurrently, high ingest rates and a large number of unique terms in fields (especially `text` fields) generate many small Lucene segments, overwhelming merge threads and causing I/O bottlenecks.",
    "bad_code": "```json\n# 1. Index mapping (problematic):\nPUT my_log_index\n{\n  \"mappings\": {\n    \"properties\": {\n      \"user_agent\": { \n        \"type\": \"text\",\n        \"fielddata\": true // Explicitly enabling fielddata on a high-cardinality text field is dangerous\n      },\n      \"client_ip\": {\n        \"type\": \"text\" // Default text field, no doc_values for aggregations\n      },\n      \"timestamp\": {\n        \"type\": \"date\"\n      }\n    }\n  }\n}\n\n# 2. Example Query (triggering circuit breaker):\nGET my_log_index/_search\n{\n  \"size\": 0,\n  \"aggs\": {\n    \"top_user_agents\": {\n      \"terms\": {\n        \"field\": \"user_agent\", // Aggregating on the fielddata=true text field\n        \"size\": 100\n      }\n    },\n    \"unique_ips\": {\n      \"cardinality\": {\n        \"field\": \"client_ip\" // Cardinality aggregation on a text field without doc_values\n      }\n    }\n  }\n}\n\n# 3. High ingest rate with many unique 'user_agent' or 'client_ip' values\n# (Implicit: many unique documents being indexed quickly leads to many small segments)\nPOST my_log_index/_doc\n{\n  \"user_agent\": \"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/123.0.0.0 Safari/537.36\",\n  \"client_ip\": \"192.168.1.1\",\n  \"timestamp\": \"2023-10-27T10:00:00Z\"\n}\n```",
    "solution_desc": "<p>The core solution revolves around proper field mapping and resource management:</p><ul><li><b>Fielddata Circuit Breaker:</b><br/><ul><li><b>Use `keyword` for aggregations/sorting:</b> For fields you intend to aggregate, sort, or use in `term` queries, always use the `keyword` field type. `keyword` fields use `doc_values` (on-disk, column-oriented structure), which are efficient and do not consume JVM heap for aggregations.</li><li><b>Multi-fields:</b> If you need both full-text search capabilities and aggregations on the same data, use <a href=\"https://www.elastic.co/guide/en/elasticsearch/reference/current/multi-fields.html\">multi-fields</a>. Index the data as a `text` field for search and as a `keyword` sub-field for aggregations.</li><li><b>Disable `fielddata`:</b> If `fielddata=true` was accidentally enabled for a `text` field, change the mapping and reindex. For new indices, avoid enabling `fielddata` on `text` fields unless absolutely necessary and you understand the memory implications.</li></ul></li><li><b>Lucene Merge Thread Starvation:</b><br/><ul><li><b>Optimize Field Mappings:</b> Ensure fields are mapped correctly. Unnecessary `text` fields or `keyword` fields with excessively large `ignore_above` values can contribute to segment bloat.</li><li><b>Reduce Ingest Rate (if possible):</b> If the problem is persistent, evaluate if the current ingest rate is sustainable for your cluster's hardware.</li><li><b>Increase I/O Capacity:</b> Merge operations are I/O intensive. Ensure your disks are fast (SSDs are highly recommended, NVMe even better) and have sufficient IOPS.</li><li><b>JVM Heap and Thread Pool Tuning:</b> While less common to explicitly tune merge threads, ensuring the JVM has enough heap and that the `merge` thread pool is not starved (check `thread_pool.merge.queue_size`) can help. By default, Elasticsearch manages this well, but I/O bottlenecks or CPU contention can implicitly starve these threads.</li><li><b>Shard Sizing:</b> Ensure your shards are of an appropriate size (e.g., 20-50GB for most use cases) to minimize the number of segments per shard. Too many small shards can worsen merge overhead.</li></ul></li></ul>",
    "good_code": "```json\n# 1. Improved Index mapping using multi-fields and appropriate types:\nPUT my_log_index_good\n{\n  \"mappings\": {\n    \"properties\": {\n      \"user_agent\": { \n        \"type\": \"text\",\n        \"fields\": { // Define a multi-field\n          \"keyword\": {\n            \"type\": \"keyword\",\n            \"ignore_above\": 256 // Optional: Truncate long keyword values to save memory/disk\n          }\n        }\n      },\n      \"client_ip\": {\n        \"type\": \"keyword\" // Use keyword for exact matching, aggregations, sorting on IP addresses\n      },\n      \"timestamp\": {\n        \"type\": \"date\"\n      }\n    }\n  }\n}\n\n# 2. Example Query (now efficient and safe):\nGET my_log_index_good/_search\n{\n  \"size\": 0,\n  \"aggs\": {\n    \"top_user_agents\": {\n      \"terms\": {\n        \"field\": \"user_agent.keyword\", // Aggregate on the keyword sub-field\n        \"size\": 100\n      }\n    },\n    \"unique_ips\": {\n      \"cardinality\": {\n        \"field\": \"client_ip\" // Cardinality aggregation on the keyword field (uses doc_values)\n      }\n    }\n  }\n}\n\n# 3. Ingesting data is now more efficient for aggregations\nPOST my_log_index_good/_doc\n{\n  \"user_agent\": \"Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/123.0.0.0 Safari/537.36\",\n  \"client_ip\": \"192.168.1.1\",\n  \"timestamp\": \"2023-10-27T10:00:00Z\"\n}\n```",
    "verification": "<p>Verification involves monitoring Elasticsearch cluster health and specific metrics:</p><ul><li><b>Circuit Breaker Metrics:</b> Use `GET _nodes/stats/jvm,breaker` and observe the `fielddata` circuit breaker. You should see `limit_size_in_bytes`, `estimated_size_in_bytes`, and crucially, `tripped` count. After the fix, `tripped` should be zero or significantly reduced.</li><li><b>Thread Pool Metrics:</b> Use `GET _cat/thread_pool?v` or `GET _nodes/stats/thread_pool` and focus on the `merge` thread pool. Monitor `active`, `queue`, and `rejected` counts. `queue` should be consistently low, and `rejected` should be zero. High `active` threads are normal during merges, but a continually growing `queue` indicates starvation.</li><li><b>Segment Count:</b> Use `GET _cat/segments?v` or `GET _nodes/stats/indices/segments` to monitor the total number of segments per shard. A high and constantly increasing number of segments, especially for smaller segments, indicates merge issues.</li><li><b>I/O Usage:</b> Monitor disk I/O (IOPS, throughput) on your Elasticsearch data nodes. High I/O without corresponding high query load can indicate intense merge activity.</li><li><b>Heap Usage:</b> Monitor JVM heap usage (e.g., via `_cat/nodes?h=heap.percent`). After fixing `fielddata` issues, heap usage for querying should be more stable and lower.</li></ul>",
    "date": "2026-09-17",
    "id": 1789633442,
    "type": "error"
});