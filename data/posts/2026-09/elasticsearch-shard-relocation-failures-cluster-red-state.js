window.onPostDataLoaded({
    "title": "Elasticsearch: Shard Relocation Failures & Cluster Red State",
    "slug": "elasticsearch-shard-relocation-failures-cluster-red-state",
    "language": "Elasticsearch",
    "code": "CLUSTER_RED_STATE",
    "tags": [
        "Elasticsearch",
        "Cluster",
        "Monitoring",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>An Elasticsearch cluster reaching a 'red' health state is a critical issue, indicating that one or more primary shards (and thus the data they contain) are unavailable. This often stems from persistent shard relocation failures. When Elasticsearch attempts to rebalance shards, recover from a node failure, or add new nodes, it performs shard relocations. These operations move data across the cluster to ensure high availability and even distribution. However, if a relocation fails repeatedly or for a sustained period, the cluster can become unstable and eventually transition to a red state.</p><p>Common causes for shard relocation failures include insufficient disk space on target nodes, network connectivity issues between nodes, misconfigured resource limits (e.g., low heap memory), data corruption, or version incompatibilities when nodes are upgraded unevenly. A single unassigned primary shard is enough to turn an entire index red. If all primary shards of an index are unassigned, the entire cluster will go red. This leads to data loss (or at least unavailability) for affected indices and severely impacts cluster operations.</p>",
    "root_cause": "Persistent unassigned primary shards due to underlying infrastructure issues (e.g., disk full, network partition, node failures, severe resource constraints like heap memory pressure) preventing successful shard relocation or recovery.",
    "bad_code": "```bash\n# Example of checking a cluster in RED state\ncurl -X GET \"localhost:9200/_cluster/health?pretty\"\n\n# Expected (bad) output might include:\n# {\n#   \"cluster_name\" : \"my-es-cluster\",\n#   \"status\" : \"red\",\n#   \"timed_out\" : false,\n#   \"number_of_nodes\" : 3,\n#   \"number_of_data_nodes\" : 3,\n#   \"active_primary_shards\" : 10,\n#   \"active_shards\" : 20,\n#   \"relocating_shards\" : 0,\n#   \"initializing_shards\" : 0,\n#   \"unassigned_shards\" : 5, <--- Indication of issues\n#   \"delayed_unassigned_shards\" : 0,\n#   \"number_of_pending_tasks\" : 0,\n#   \"number_of_in_flight_fetch\" : 0,\n#   \"task_max_waiting_in_queue_millis\" : 0,\n#   \"active_shards_percent_as_number\" : 80.0\n# }\n\n# To see specific unassigned shards\ncurl -X GET \"localhost:9200/_cat/shards?v&h=index,shard,prirep,state,node,unassigned.reason\"\n```",
    "solution_desc": "To resolve a red cluster state and prevent shard relocation failures, a methodical troubleshooting approach is required. First, identify the exact unassigned shards using `_cat/shards` and `_cluster/allocation/explain`. Address the root cause: free up disk space on affected nodes, resolve network issues, restart unstable nodes, or increase JVM heap size. For persistent allocation issues, temporarily adjust cluster allocation settings (e.g., `cluster.routing.allocation.enable` or `cluster.routing.allocation.same_shard.host`). If data corruption is suspected, consider restoring from a snapshot for affected indices. For minor, non-critical data, deleting the red index might be a last resort. Always ensure nodes in a cluster are running compatible Elasticsearch versions, especially during rolling upgrades. Implement proactive monitoring for disk usage, network latency, and heap memory to catch issues before they escalate.",
    "good_code": "```bash\n# Example of checking a cluster in GREEN state\ncurl -X GET \"localhost:9200/_cluster/health?pretty\"\n\n# Expected (good) output:\n# {\n#   \"cluster_name\" : \"my-es-cluster\",\n#   \"status\" : \"green\",\n#   \"timed_out\" : false,\n#   \"number_of_nodes\" : 3,\n#   \"number_of_data_nodes\" : 3,\n#   \"active_primary_shards\" : 10,\n#   \"active_shards\" : 20,\n#   \"relocating_shards\" : 0,\n#   \"initializing_shards\" : 0,\n#   \"unassigned_shards\" : 0, <--- All shards assigned\n#   \"delayed_unassigned_shards\" : 0,\n#   \"number_of_pending_tasks\" : 0,\n#   \"number_of_in_flight_fetch\" : 0,\n#   \"task_max_waiting_in_queue_millis\" : 0,\n#   \"active_shards_percent_as_number\" : 100.0\n# }\n\n# Example: Unblock disk watermarks if they are causing issues (use with caution)\n# curl -X PUT \"localhost:9200/_cluster/settings\" -H 'Content-Type: application/json' -d'\n# {\n#   \"persistent\": {\n#     \"cluster.routing.allocation.disk.threshold_enabled\": false\n#   }\n# }'\n\n# Example: Forcing a replica allocation (use after root cause fixed)\n# curl -X POST \"localhost:9200/_cluster/reroute?pretty\" -H 'Content-Type: application/json' -d'\n# {\n#   \"commands\": [\n#     {\n#       \"allocate_replica\": {\n#         \"index\": \"my-index\",\n#         \"shard\": 0,\n#         \"node\": \"target-node-name\"\n#       }\n#     }\n#   ]\n# }'\n```",
    "verification": "The primary verification is to query the cluster health using `GET /_cluster/health?pretty`. The cluster status should return 'green'. Additionally, use `GET /_cat/shards?v` to confirm that all shards are in an `STARTED` state and there are no `UNASSIGNED` shards. Check `GET /_cluster/allocation/explain?pretty` for any remaining unassigned shards; if the output is empty, it means all shards are assigned. Continuously monitor the cluster's health and resource usage to ensure stability and prevent recurrence.",
    "date": "2026-09-20",
    "id": 1789871178,
    "type": "error"
});