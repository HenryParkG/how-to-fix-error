window.onPostDataLoaded({
    "title": "Elasticsearch: Shard Rebalancing Storms & Split-Brain",
    "slug": "elasticsearch-shard-storms-split-brain",
    "language": "Elasticsearch",
    "code": "ClusterInstability",
    "tags": [
        "Elasticsearch",
        "Distributed Systems",
        "High Availability",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch, as a distributed system, relies on a robust cluster state mechanism to ensure data consistency and availability. A 'split-brain' scenario occurs when a network partition separates a cluster into two or more segments, each believing itself to be a fully functional, independent cluster with its own master node. This leads to conflicting cluster states, where different segments accept writes, perform shard reallocations, and update metadata independently. When the network partition heals, merging these conflicting states is impossible without data loss or corruption, leading to data inconsistency and severe operational headaches.</p><p>Shard rebalancing storms can exacerbate this. In a split-brain, each isolated segment might detect 'missing' nodes (those in other segments) and initiate aggressive shard rebalancing to compensate. This not only consumes significant network and I/O resources but also creates multiple, conflicting copies of shards across the different 'brains'. When nodes eventually rejoin, the cluster has to resolve these conflicts, often leading to data loss or requiring manual intervention to restore a consistent state, further impacting availability and performance.</p>",
    "root_cause": "Network partition causing the Elasticsearch cluster to divide into multiple segments, each electing its own master node due to insufficient quorum settings. This results in conflicting cluster states and chaotic shard rebalancing across isolated segments.",
    "bad_code": "```json\n# elasticsearch.yml (Bad Configuration: Missing or insufficient quorum settings)\n# For Elasticsearch 7.x and later, 'cluster.initial_master_nodes' is critical.\n# For Elasticsearch 6.x and earlier, 'discovery.zen.minimum_master_nodes' is critical.\n\n# Example of default or insufficient settings that can lead to split-brain:\n# cluster.name: my-es-cluster\n# node.name: node-1\n# network.host: 0.0.0.0\n\n# Without proper 'cluster.initial_master_nodes' (for 7.x+) or\n# 'discovery.zen.minimum_master_nodes' (for 6.x-),\n# a network partition can easily lead to multiple masters.\n\n# In a 3-node cluster, if only 2 nodes are listed, and 1 node is partitioned,\n# the remaining 2 might form a quorum, but if 2 nodes are partitioned away\n# from 1, and the network allows, both segments could elect masters.\n# The absence of a strict quorum allows multiple groups of master-eligible nodes\n# to form independent clusters.\n```",
    "solution_desc": "The fundamental solution is to properly configure master node quorum. For Elasticsearch 7.x and later, this involves setting <code>cluster.initial_master_nodes</code> with a list of master-eligible node names during initial cluster bootstrap. Post-bootstrap, the cluster automatically manages voting configurations. For older versions (6.x and earlier), set <code>discovery.zen.minimum_master_nodes</code> to <code>(N/2) + 1</code>, where N is the total number of master-eligible nodes. This ensures that a majority of master-eligible nodes must be reachable to elect a master, preventing split-brain. Additionally, ensure network stability and low latency between master-eligible nodes, and consider dedicating specific nodes as master-only for better stability in larger clusters.",
    "good_code": "```json\n# elasticsearch.yml (Good Configuration: Quorum settings for a 3-node cluster)\n\n# For Elasticsearch 7.x and later:\n# On ALL master-eligible nodes (node-1, node-2, node-3):\ncluster.name: my-es-cluster\nnode.name: node-1 # or node-2, node-3 depending on the node\nnode.roles: [master, data] # or [master] for dedicated masters\nnetwork.host: 0.0.0.0\n\n# IMPORTANT: cluster.initial_master_nodes must be set on all master-eligible nodes\n# when bootstrapping a new cluster for the first time.\n# It specifies the names of all master-eligible nodes in the cluster that should be considered\n# for the initial master election. For a 3-node cluster with nodes 'node-1', 'node-2', 'node-3':\ncluster.initial_master_nodes: [\"node-1\", \"node-2\", \"node-3\"]\n\n# discovery.seed_hosts should list IP addresses or hostnames of master-eligible nodes\n# to help nodes discover each other.\ndiscovery.seed_hosts: [\"node-1-ip\", \"node-2-ip\", \"node-3-ip\"]\n\n# For Elasticsearch 6.x and earlier (legacy configuration):\n# On ALL master-eligible nodes:\n# discovery.zen.minimum_master_nodes: 2 # (3 master-eligible nodes / 2) + 1 = 1.5 + 1 = 2.5 -> 2\n# This ensures at least 2 master-eligible nodes must be visible to elect a master.\n```",
    "verification": "To verify the fix, perform controlled network partition tests. Use network tools (e.g., <code>iptables</code>, cloud security groups) to isolate master-eligible nodes from each other. Observe Elasticsearch cluster logs and health status (e.g., using <code>_cluster/health?pretty</code> and <code>_cat/nodes?v</code>). A correctly configured cluster should either elect a single master in the largest partition (if it meets the quorum) or become unavailable if no partition can meet the quorum, rather than forming multiple independent clusters. Look for logs indicating 'no master elected' or 'waiting for a quorum' instead of multiple nodes claiming to be master.",
    "date": "2026-09-14",
    "id": 1789399971,
    "type": "error"
});