window.onPostDataLoaded({
    "title": "Spark: Skewed Partitions & OOM from Shuffle Spills",
    "slug": "spark-skewed-data-partitions-shuffle-spills-oom",
    "language": "Spark (Scala/Python/Java)",
    "code": "SparkOOM",
    "tags": [
        "Big Data",
        "Performance",
        "Spark",
        "Java",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Data skew is a prevalent issue in distributed computing frameworks like Apache Spark. It occurs when a small subset of keys (or partitions) contains a disproportionately large amount of data compared to others. In Spark, operations such as `join`, `groupByKey`, `reduceByKey`, or `distinct` involve shuffling data across the network and writing it to disk. During a shuffle, data with the same key is moved to the same partition, and therefore to the same executor.</p><p>When data is skewed, one or a few executors end up receiving and processing a massive amount of data for certain keys. This imbalance leads to several problems:</p><ol><li><b>Performance Bottleneck:</b> The tasks assigned to these 'hot' partitions take much longer to complete, bottlenecking the entire job as other tasks wait for them.</li><li><b>Shuffle Spills:</b> The executor processing the skewed partition might not have enough memory to hold all the intermediate shuffle data. Spark will then 'spill' this data to local disk. While spilling is a normal optimization, excessive spilling severely degrades performance due as it converts memory operations into slower disk I/O.</li><li><b>Out-Of-Memory (OOM) Errors:</b> In extreme cases, a single skewed partition can be so large that it exceeds the executor's memory *even after* spilling to disk, or it might exhaust available disk space, leading to an OutOfMemory error. This causes the executor to crash, potentially failing the entire Spark job.</li></ol>",
    "root_cause": "Highly uneven distribution of data across shuffle keys, leading to specific partitions becoming excessively large, overwhelming executor memory and causing large shuffle spills, eventually exhausting memory or disk resources.",
    "bad_code": "```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import lit\n\nspark = SparkSession.builder.appName(\"SkewedJoinExample\").getOrCreate()\n\n# Create two DataFrames with skewed data\n# df1 has many rows for key 'A'\ndata1 = [(\"A\", 1), (\"B\", 2), (\"C\", 3)] + [(\"A\", i) for i in range(1000000)]\ndf1 = spark.createDataFrame(data1, [\"key\", \"value1\"])\n\n# df2 has some rows, also including 'A'\ndata2 = [(\"A\", 10), (\"D\", 4)] + [(\"A\", i + 100) for i in range(10000)]\ndf2 = spark.createDataFrame(data2, [\"key\", \"value2\"])\n\n# Perform a join operation on the skewed key\n# This join will likely cause OOM or very slow performance for the partition handling key 'A'\nresult = df1.join(df2, on=\"key\", how=\"inner\")\n\nprint(\"Executing skewed join...\")\nresult.count() # Trigger action\n\nspark.stop()\n```",
    "solution_desc": "Addressing data skew involves strategies to redistribute the data more evenly. Key solutions include:</p><ol><li><b>Salting:</b> For skewed join keys, add a random prefix/suffix to the skewed key in one DataFrame and duplicate rows in the other DataFrame with all possible prefixes/suffixes. This effectively breaks down a single hot key into multiple smaller, more manageable keys.</li><li><b>Broadcast Join:</b> If one of the tables in a join is small enough to fit into memory (typically <1GB), use a broadcast join. Spark will send the smaller table to all executor nodes, avoiding a shuffle for that table entirely.</li><li><b>Adaptive Query Execution (AQE):</b> Spark 3.0+ offers AQE, which can dynamically handle data skew during runtime by splitting skewed partitions. Enable `spark.sql.adaptive.enabled=true` and `spark.sql.adaptive.skewedJoin.enabled=true`.</li><li><b>Filter Skewed Keys:</b> If the skewed keys are known and can be processed separately (e.g., in a smaller, specialized job), filter them out, process them, and then union the results.</li><li><b>Custom Partitioning:</b> For `groupBy` or `reduce` operations, consider a custom partitioner if standard hashing doesn't distribute data well.</li></ol>",
    "good_code": "```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import lit, concat, expr, rand, col\n\nspark = SparkSession.builder \\\n    .appName(\"SkewedJoinFixExample\") \\\n    .config(\"spark.sql.adaptive.enabled\", \"true\") \\\n    .config(\"spark.sql.adaptive.skewedJoin.enabled\", \"true\") \\\n    .config(\"spark.sql.adaptive.coalescePartitions.enabled\", \"true\") \\\n    .getOrCreate()\n\n# Create two DataFrames with skewed data (same as bad_code)\ndata1 = [(\"A\", 1), (\"B\", 2), (\"C\", 3)] + [(\"A\", i) for i in range(1000000)]\ndf1 = spark.createDataFrame(data1, [\"key\", \"value1\"])\n\ndata2 = [(\"A\", 10), (\"D\", 4)] + [(\"A\", i + 100) for i in range(10000)]\ndf2 = spark.createDataFrame(data2, [\"key\", \"value2\"])\n\n# --- Solution: Salting for highly skewed keys (manual approach if AQE is not enough) ---\n# Assuming 'A' is the highly skewed key\nsalt_factor = 10 # Number of sub-keys to create for the skewed key\n\n# 1. Salt the smaller DataFrame (df2) by replicating rows for skewed keys\nskewed_keys_df2 = df2.filter(col(\"key\") == \"A\")\nnon_skewed_keys_df2 = df2.filter(col(\"key\") != \"A\")\n\nsalted_skewed_df2 = skewed_keys_df2.withColumn(\"salt\", expr(\"explode(sequence(0, {}))\".format(salt_factor - 1)))\nsalted_skewed_df2 = salted_skewed_df2.withColumn(\"salted_key\", concat(col(\"key\"), lit(\"_\"), col(\"salt\")))\n\n# Re-combine non-skewed keys (no salt) and salted skewed keys for df2\ndf2_salted = non_skewed_keys_df2.withColumn(\"salted_key\", col(\"key\")) \\\n                 .unionByName(salted_skewed_df2.select(\"key\", \"value2\", \"salted_key\"))\n\n# 2. Salt the larger DataFrame (df1) by adding a random salt to skewed keys\ndf1_salted = df1.withColumn(\"salted_key\", \n                             expr(\"CASE WHEN key = 'A' THEN concat(key, '_', floor(rand() * {})) ELSE key END\".format(salt_factor)))\n\n# Perform the join on the 'salted_key'\n# Note: If AQE is enabled, it often handles skew joins automatically without manual salting.\n# The manual salting is for cases where AQE might not be sufficient or is not available.\nresult_salted = df1_salted.join(df2_salted, on=\"salted_key\", how=\"inner\").drop(\"salted_key\")\n\nprint(\"Executing salted join (or AQE optimized join if enabled and effective)...\")\nresult_salted.count() # Trigger action\n\nspark.stop()\n```",
    "verification": "<p>Verify the fix by monitoring the Spark UI during job execution. Navigate to the 'Stages' tab and inspect the task durations. With proper skew handling, task durations within a stage should be relatively uniform, without a few tasks taking significantly longer (the 'stragglers'). Check the 'Summary Metrics' for each task to ensure 'Shuffle Read Size / Records' and 'Shuffle Write Size / Records' are distributed evenly. Also, observe executor memory usage and garbage collection statistics; a successful fix should show stable memory profiles without OOM errors, and fewer and shorter garbage collection pauses. Ultimately, the job completion time should significantly decrease, indicating improved performance.</p>",
    "date": "2026-09-09",
    "id": 1788919936,
    "type": "error"
});