window.onPostDataLoaded({
    "title": "Elasticsearch Split-Brain Scenarios",
    "slug": "elasticsearch-split-brain-data-inconsistency-cluster-reconfiguration",
    "language": "Elasticsearch",
    "code": "SplitBrain",
    "tags": [
        "Elasticsearch",
        "Infra",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch operates as a distributed system, relying on a master node to manage cluster state, index metadata, and shard allocation. In a healthy cluster, a single master node is elected by a consensus mechanism. A \"split-brain\" scenario occurs when, due to a network partition, a cluster fragments into two or more independent sub-clusters, each believing it contains the legitimate master node. This typically happens when communication between a subset of master-eligible nodes is disrupted.</p><p>When split-brain occurs, each \"brain\" (partition) can operate independently, accepting write requests and making changes to its perceived cluster state. This leads to data inconsistency where indices and documents might exist in one partition but not another, or different versions of the same document appear across partitions. Upon network recovery, Elasticsearch faces an impossible choice of reconciling these divergent states, often resulting in data loss, corrupt indices, or an unstable cluster that struggles to form a coherent view.</p>",
    "root_cause": "<p>The fundamental root cause of Elasticsearch split-brain is insufficient quorum enforcement during master election when a network partition occurs. Specifically:</p><ol><li><strong>Network Partitioning:</strong> Physical or logical network issues isolate a subset of nodes from the rest of the cluster.</li><li><strong>Insufficient Quorum Settings:</strong> Prior to Elasticsearch 7.0, the `discovery.zen.minimum_master_nodes` setting was critical. If this value was set too low (e.g., 1), a minority partition could elect its own master. In Elasticsearch 7.0+, this setting was replaced by a more robust voting configuration (<code>cluster.initial_master_nodes</code> and automatic voting configuration via `cluster.election.vote_quorum`). If not configured correctly or if the cluster is allowed to operate with fewer than the required voting nodes, a split-brain can still occur.</li><li><strong>Delayed Node Communication:</strong> While less common for full split-brain, temporary network delays can cause nodes to briefly believe the master is down, triggering unnecessary elections and potential inconsistencies.</li></ol>",
    "bad_code": "```yaml\n# elasticsearch.yml (Pre-Elasticsearch 7.0 - INSECURE)\n# This configuration is highly susceptible to split-brain if you have more than 2 master-eligible nodes\n# For a 3-node master-eligible cluster, this means any single node can become master.\ndiscovery.zen.minimum_master_nodes: 1\n\n# elasticsearch.yml (Elasticsearch 7.0+ - Default or incorrect config)\n# If using default, it might be fine, but if explicitly set incorrectly, problem persists.\n# The default behavior in 7.0+ with auto-quorum usually prevents this,\n# but manually overriding `cluster.initial_master_nodes` incorrectly or\n# allowing too many master-eligible nodes to be isolated can still cause issues.\n# e.g., if initial_master_nodes only lists one node and others are added later without correct voting setup.\ncluster.name: my-es-cluster\nnode.name: node-1\nnode.master: true\nnode.data: true\n# If the voting configuration is later corrupted or mismanaged, split-brain can happen.\n```",
    "solution_desc": "<p>Preventing split-brain in Elasticsearch primarily involves enforcing a strict quorum during master elections, ensuring that a majority of master-eligible nodes must agree on the master. For different Elasticsearch versions, the approach varies:</p><ol><li><strong>Elasticsearch 6.x and Earlier: `discovery.zen.minimum_master_nodes`</strong><p>Set `discovery.zen.minimum_master_nodes` to `(N/2) + 1`, where `N` is the total number of master-eligible nodes. For example, in a 3-master-eligible node cluster, this should be `(3/2) + 1 = 2`. This guarantees that in any network partition, only the partition containing a majority of master-eligible nodes can elect a master.</p></li><li><strong>Elasticsearch 7.x and Later: Voting Configuration</strong><p>Elasticsearch 7.x replaced `minimum_master_nodes` with a more robust voting configuration. The primary mechanism is during initial cluster bootstrap using `cluster.initial_master_nodes`. After bootstrap, the cluster dynamically maintains a voting configuration. It's crucial to:</p><ul><li>List all initial master-eligible nodes in `cluster.initial_master_nodes` during bootstrap.</li><li>Ensure master-eligible nodes are correctly configured and have stable network connectivity.</li><li>Avoid manually tinkering with `cluster.election.vote_quorum` unless absolutely necessary and understood, as the default behavior is generally safer.</li></ul></li><li><strong>Network Reliability:</strong> While software configuration is key, robust and redundant networking infrastructure is also critical to minimize the chances of partitions occurring in the first place.</li><li><strong>Node Roles:</strong> Separate master, data, and ingest nodes. Dedicated master nodes are less burdened and more reliable for cluster state management.</li></ol><p>If a split-brain does occur, manual intervention is typically required to recover. This often involves isolating one partition, force-restarting nodes in the incorrect partition, and ensuring they rejoin the correct master.</p>",
    "good_code": "```yaml\n# elasticsearch.yml (For Elasticsearch 6.x)\n# Example for a 3-node cluster with master-eligible nodes: node-1, node-2, node-3\ncluster.name: my-es-cluster\nnode.name: node-1 # or node-2, node-3\nnode.master: true\nnode.data: true\n# The crucial setting for preventing split-brain\ndiscovery.zen.minimum_master_nodes: 2 # (3 master-eligible nodes / 2) + 1 = 2\n\n# elasticsearch.yml (For Elasticsearch 7.x+)\n# Example for a 3-node cluster with master-eligible nodes: node-a, node-b, node-c\n# This configuration is for the bootstrap phase on *each* master-eligible node.\ncluster.name: my-es-cluster\nnode.name: node-a # or node-b, node-c\nnode.master: true\nnode.data: true\n# This setting is only read on initial startup for a new cluster.\n# List all master-eligible node names or their network addresses.\ncluster.initial_master_nodes: [\"node-a\", \"node-b\", \"node-c\"]\n\n# After initial bootstrap, the voting configuration is managed automatically.\n# No manual `minimum_master_nodes` equivalent needed for stability.\n```",
    "verification": "<p>Verifying the split-brain protection involves simulating network partitions and observing the cluster's behavior:</p><ol><li><strong>Network Partition Simulation:</strong> Use tools like `iptables` or cloud security groups to temporarily block communication between master-eligible nodes, dividing your cluster into two partitions (e.g., node A from nodes B and C).</li><li><strong>Master Node Check:</strong> After partitioning, query `_cluster/health` or `_cat/master` on nodes in each partition. Only the partition containing the majority of master-eligible nodes (e.g., nodes B and C) should successfully elect a master and maintain a green cluster state. The minority partition (e.g., node A) should ideally remain in a yellow or red state, or fail to elect a master, indicating it cannot form a quorum.</li><li><strong>Write Operations:</strong> Attempt to perform write operations (indexing documents) in both partitions. Verify that only the master-holding partition accepts writes. Writes to the minority partition should fail or eventually be discarded when network connectivity is restored and nodes rejoin the correct cluster.</li><li><strong>Log Analysis:</strong> Monitor Elasticsearch logs for messages indicating master election failures, nodes leaving/joining the cluster, and `cluster.initial_master_nodes` or `minimum_master_nodes` related warnings/errors.</li><li><strong>Network Recovery:</strong> Restore network connectivity between partitions and ensure the cluster converges back to a single, healthy state without data loss or inconsistencies.</li></ol>",
    "date": "2026-09-11",
    "id": 1789132499,
    "type": "error"
});