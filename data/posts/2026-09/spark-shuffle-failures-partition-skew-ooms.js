window.onPostDataLoaded({
    "title": "Spark: Shuffle Fetch Failed & Partition Skew OOMs",
    "slug": "spark-shuffle-failures-partition-skew-ooms",
    "language": "Apache Spark",
    "code": "Shuffle Fetch Failed",
    "tags": [
        "Apache Spark",
        "Big Data",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Apache Spark's shuffle mechanism is a critical but resource-intensive operation where data is repartitioned and exchanged across the cluster. It typically occurs during wide transformations like <code>groupByKey</code>, <code>reduceByKey</code>, <code>join</code>, or <code>repartition</code>. When data is unevenly distributed across partitions, a phenomenon known as <b>partition skew</b> arises. This means a small number of partitions become disproportionately large, carrying a massive amount of data compared to others.</p><p>Partition skew can lead to several severe problems, primarily: </p><ol><li><b>Out-Of-Memory (OOM) Errors:</b> An executor assigned to process an extremely large, skewed partition may attempt to load all its data into memory, quickly exhausting its heap space and leading to OOM errors. When an executor fails due to OOM, any tasks running on it are lost, and its shuffle data stored on disk becomes inaccessible.</li><li><b>Shuffle Fetch Failed Exceptions:</b> When an executor processing a subsequent stage tries to fetch shuffle data from a previous stage's executor that has crashed (due to OOM or other issues), it will fail with a <code>Shuffle Fetch Failed</code> or <code>FetchFailedException</code>. This happens because the required shuffle blocks are no longer available on the dead executor or were never fully written.</li><li><b>Long-running Tasks & Stage Failures:</b> Even if OOM doesn't occur, processing a highly skewed partition can take an exceedingly long time, leading to task timeouts. A few straggler tasks can hold up an entire stage, eventually causing the stage or job to fail due to timeouts.</li><li><b>Resource Underutilization:</b> While one or a few executors are overloaded with skewed data, many other executors might finish their work quickly and remain idle, leading to inefficient cluster resource utilization.</li></ol>",
    "root_cause": "Data skew, typically introduced during wide transformations like `groupByKey`, `join`, or `repartition`, where a few keys have a significantly higher number of associated records than others. This causes certain partitions to become excessively large, overwhelming the memory capacity of the executor processing them, leading to OOMs and subsequent `Shuffle Fetch Failed` exceptions when other executors attempt to retrieve data from the failed node.",
    "bad_code": "```python\n# Example of highly skewed data\ndata = [\n    (\"popular_key\", 1),\n    (\"popular_key\", 2),\n    # ... hundreds of thousands of entries for \"popular_key\"\n    (\"popular_key\", 3),\n    (\"key_b\", 4),\n    (\"key_c\", 5)\n]\n\n# RDD example with skewed groupByKey\nrdd = sc.parallelize(data)\nskewed_rdd = rdd.groupByKey() # This will create a very large partition for \"popular_key\"\n\n# DataFrame example with skewed join\ndf1 = spark.createDataFrame([\n    (\"popular_key\", \"value1\"), \n    # ... many entries for popular_key\n    (\"popular_key\", \"valueX\"),\n    (\"key_b\", \"value2\")\n], [\"id\", \"data1\"])\n\ndf2 = spark.createDataFrame([\n    (\"popular_key\", \"info1\"), \n    (\"key_c\", \"info2\")\n], [\"id\", \"data2\"])\n\nskewed_join_df = df1.join(df2, \"id\") # If \"popular_key\" is heavily duplicated in df1\n```",
    "solution_desc": "<p>Addressing partition skew and its associated OOMs and shuffle failures requires strategies to redistribute the skewed data more evenly:</p><ol><li><b>Salting the Skewed Key:</b> For `join` or `groupByKey` operations, you can introduce a 'salt' to the skewed key. This involves appending a random number (e.g., 0 to N) to the skewed key, effectively splitting it into N sub-keys. After the transformation, you can remove the salt. This is especially effective if one key is highly dominant.</li><li><b>Broadcast Joins (for small DataFrames):</b> If one of the DataFrames in a join operation is small enough to fit into executor memory, use a broadcast join. This avoids shuffling the smaller DataFrame entirely and sends it to all executors.</li><li><b>Adaptive Query Execution (AQE):</b> Enable AQE in Spark 3.0+. AQE can automatically handle skewed joins by dynamically re-optimizing query plans during runtime, including skew join optimization and coalescing shuffle partitions.</li><li><b>Increase Executor Memory:</b> As a temporary measure or if the skew is mild, increasing `spark.executor.memory` can provide more headroom. However, this doesn't fix the underlying skew and might just delay the OOM.</li><li><b>`reduceByKey` instead of `groupByKey`:</b> If your goal is aggregation, `reduceByKey` performs a local pre-aggregation combiner step before the shuffle, significantly reducing the amount of data transferred and processed compared to `groupByKey`.</li><li><b>Custom Partitioner:</b> For RDDs, you can define a custom partitioner to control how keys are mapped to partitions, distributing skewed keys more evenly.</li><li><b>Filter Skewed Data:</b> If skewed keys are rare and not critical for the analysis, consider filtering them out or processing them separately.</li></ol>",
    "good_code": "```python\n# Example 1: Salting a skewed key for RDD groupByKey\nfrom random import randint\nNUM_SALT_BUCKETS = 100 # Choose based on expected skew and parallelism\n\ndef salt_key(key_value):\n    key, value = key_value\n    if key == \"popular_key\": # Identify the skewed key\n        return (f\"{key}_{randint(0, NUM_SALT_BUCKETS-1)}\", value)\n    return key_value\n\ndef unsalt_key(key_value):\n    key, value = key_value\n    if key.startswith(\"popular_key_\"):\n        return (\"popular_key\", value)\n    return key_value\n\nsalted_rdd = rdd.map(salt_key) # Map to add salt\nskewed_rdd_fixed = salted_rdd.groupByKey().map(lambda x: (unsalt_key(x)[0], list(x[1])))\n\n# Example 2: Salting a skewed key for DataFrame join (using Spark SQL functions)\nfrom pyspark.sql.functions import concat, lit, rand, expr\n\ndf1_salted = df1.withColumn(\"salted_id\", \n                            expr(\"CASE WHEN id = 'popular_key' THEN concat(id, '_', floor(rand() * 100)) ELSE id END\"))\n\ndf2_salted = df2.withColumn(\"salted_id\", \n                            expr(\"CASE WHEN id = 'popular_key' THEN concat(id, '_', floor(rand() * 100)) ELSE id END\"))\n\n# Perform a salted join\nsalted_join_df = df1_salted.join(df2_salted, \"salted_id\")\n\n# Re-join with original df2 to restore original 'id' and filter salted_id to original\n# Or, drop salted_id and aggregate if necessary based on your logic.\n# This example is simplified for illustration. Actual desalt might be more complex.\n\n# Example 3: Enabling Adaptive Query Execution (Spark 3.x+)\nspark.conf.set(\"spark.sql.adaptive.enabled\", \"true\")\nspark.conf.set(\"spark.sql.adaptive.skewJoin.enabled\", \"true\")\n```",
    "verification": "<p>Verifying the fix involves monitoring the Spark UI and executor logs:</p><ol><li><b>Spark UI Stages Tab:</b> Observe the duration and progress of tasks within the skewed stage. Look for a more even distribution of task durations and less variance. The 'Event Timeline' can show fewer executor failures.</li><li><b>Spark UI Tasks Tab:</b> Check the 'Input Size' and 'Shuffle Write Size' columns for individual tasks. Ideally, these should be more uniform across tasks in a given stage, indicating reduced partition skew.</li><li><b>Executor Logs:</b> Confirm the absence of OOM errors (<code>java.lang.OutOfMemoryError</code>) in the executor logs.</li><li><b>Shuffle Fetch Metrics:</b> In the Spark UI, inspect the 'Shuffle Read' metrics for the subsequent stages to ensure fewer 'Failed Fetches'.</li><li><b>Data Distribution Analysis:</b> Before and after applying a fix, you can programmatically inspect data distribution using commands like `df.groupBy(\"key\").count().orderBy(desc(\"count\"))` to confirm that the number of records per key/partition is more balanced.</li></ol>",
    "date": "2026-09-19",
    "id": 1789803880,
    "type": "error"
});