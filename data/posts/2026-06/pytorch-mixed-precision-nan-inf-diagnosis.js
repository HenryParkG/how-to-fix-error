window.onPostDataLoaded({
    "title": "PyTorch Mixed-Precision Training Instability: Diagnosing NaN/Inf",
    "slug": "pytorch-mixed-precision-nan-inf-diagnosis",
    "language": "Python",
    "code": "MixedPrecisionNaN",
    "tags": [
        "PyTorch",
        "AI/ML",
        "Mixed Precision",
        "NaN/Inf",
        "Python",
        "Error Fix"
    ],
    "analysis": "<p>Mixed-precision training in PyTorch involves using `float16` (half-precision) for certain operations and `float32` (single-precision) for others, primarily to speed up computation and reduce memory usage on hardware that accelerates `float16` (like NVIDIA Tensor Cores). While beneficial, `float16` has a significantly smaller dynamic range compared to `float32`. This limited range makes models susceptible to numerical instabilities, manifesting as Not-a-Number (NaN) or Infinity (Inf) values propagating through the network, leading to training divergence or complete failure.</p><p>Common scenarios for NaN/Inf propagation include: 1) **Underflow to zero**: Gradients, especially those with small magnitudes, can underflow to zero when represented in `float16`. If these zero gradients are then used in divisions or inverse operations, it can quickly lead to NaNs. 2) **Overflow to Inf**: Large intermediate activations or weight updates, when converted to `float16`, can exceed its maximum representable value, resulting in `Inf`. Subsequent operations involving `Inf` can quickly spread it throughout the network. 3) **Activation Functions**: Some activation functions (e.g., `sigmoid`, `tanh`) can produce very small or very large outputs, pushing values to the edges of `float16`'s range. 4) **Loss Functions**: Loss calculations that involve logarithms or divisions can be particularly sensitive to `float16` precision, especially when inputs are near zero.</p><p>Diagnosing these issues requires careful monitoring of weights, activations, and gradients at different layers. The key is to identify *where* the NaN/Inf first appears and trace back the operations that led to it.</p>",
    "root_cause": "Numerical instability in `float16` operations due to its limited dynamic range, leading to gradient underflow (small gradients become zero, causing NaNs in subsequent divisions) or overflow (large activations/gradients become Inf, propagating through the network). This is often exacerbated by model architecture, aggressive learning rates, or specific activation/loss functions.",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\n\n# A simple model that might be prone to instability\nclass MyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear1 = nn.Linear(10, 1000)\n        self.relu = nn.ReLU()\n        self.linear2 = nn.Linear(1000, 10)\n\n    def forward(self, x):\n        x = self.linear1(x)\n        x = self.relu(x)\n        x = self.linear2(x)\n        return x\n\nmodel = MyModel().cuda()\noptimizer = optim.SGD(model.parameters(), lr=0.01)\nloss_fn = nn.MSELoss()\n\n# Simulating mixed precision without a scaler - prone to issues\nfor epoch in range(5):\n    inputs = torch.randn(16, 10).cuda()\n    targets = torch.randn(16, 10).cuda()\n\n    optimizer.zero_grad()\n\n    # Manual type casting, often leading to underflow/overflow\n    with torch.cuda.amp.autocast(enabled=False): # Intentionally disabling autocast for 'bad_code'\n        outputs = model(inputs.to(torch.float16)).to(torch.float32) # Inefficient/incorrect way\n\n    loss = loss_fn(outputs, targets)\n    loss.backward()\n    optimizer.step()\n\n    if torch.isnan(loss) or torch.isinf(loss):\n        print(f\"Epoch {epoch}: Detected NaN/Inf in loss!\")\n        # Often requires manual inspection of model parameters/gradients\n        for name, param in model.named_parameters():\n            if param.grad is not None and (torch.isnan(param.grad).any() or torch.isinf(param.grad).any()):\n                print(f\"  NaN/Inf in gradient of {name}\")\n        break\n    print(f\"Epoch {epoch}: Loss = {loss.item():.4f}\")\n\n```",
    "solution_desc": "The primary solution for mixed-precision instability in PyTorch is using `torch.cuda.amp.GradScaler`. `GradScaler` scales loss values *before* calling `backward()` to prevent gradients from underflowing to zero in `float16`. After gradients are computed, it unscales them before the optimizer step. It also handles skips for gradient updates if NaNs/Infs are detected in gradients after unscaling. Additionally, gradient clipping can help prevent large gradients from causing overflows. Careful initialization of weights, choosing appropriate learning rates, and sometimes avoiding `float16` for certain sensitive layers (e.g., softmax, normalization layers) can also improve stability. Monitoring loss, activations, and gradient norms (`torch.norm(param.grad)`) is crucial for early detection.",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nfrom torch.cuda.amp import autocast, GradScaler\n\n# A simple model that might be prone to instability\nclass MyModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear1 = nn.Linear(10, 1000)\n        self.relu = nn.ReLU()\n        self.linear2 = nn.Linear(1000, 10)\n\n    def forward(self, x):\n        x = self.linear1(x)\n        x = self.relu(x)\n        x = self.linear2(x)\n        return x\n\nmodel = MyModel().cuda()\noptimizer = optim.SGD(model.parameters(), lr=0.01)\nloss_fn = nn.MSELoss()\n\n# Initialize GradScaler\nscaler = GradScaler()\n\nfor epoch in range(5):\n    inputs = torch.randn(16, 10).cuda()\n    targets = torch.randn(16, 10).cuda()\n\n    optimizer.zero_grad()\n\n    # Use autocast for mixed precision, and GradScaler for gradient scaling\n    with autocast():\n        outputs = model(inputs)\n        loss = loss_fn(outputs, targets)\n\n    scaler.scale(loss).backward() # Scale loss before backward\n    \n    # Optional: Gradient clipping can be applied here before scaler.step()\n    # scaler.unscale_(optimizer) # Must unscale before clipping\n    # torch.nn.utils.clip_grad_norm_(model.parameters(), max_norm=1.0)\n\n    scaler.step(optimizer) # Unscale gradients and then call optimizer.step()\n    scaler.update() # Update the scaler for the next iteration\n\n    if torch.isnan(loss) or torch.isinf(loss):\n        print(f\"Epoch {epoch}: Detected NaN/Inf in loss after fix!\")\n        break\n    print(f\"Epoch {epoch}: Loss = {loss.item():.4f}\")\n\n```",
    "verification": "Monitor the training loss over multiple epochs; it should converge smoothly without spikes or abrupt transitions to NaN/Inf. Inspect the `GradScaler`'s internal state (e.g., `scaler._get_scale_update_ratio()`) to ensure it's adjusting the scale effectively. Periodically check `torch.norm(param.grad)` for all model parameters to verify that gradient magnitudes remain within reasonable bounds and do not contain NaNs/Infs. If issues persist, try gradually reducing the learning rate, using a different optimizer (e.g., AdamW which is less sensitive), or temporarily disabling mixed precision for specific problematic layers.",
    "date": "2026-06-26",
    "id": 1782456306,
    "type": "error"
});