window.onPostDataLoaded({
    "title": "Debugging C++20 Coroutine Lifetime & Stack Unwinding",
    "slug": "cpp20-coroutine-lifetime-stack-unwinding-debug",
    "language": "C++",
    "code": "Coroutine Lifetime",
    "tags": [
        "C++",
        "Concurrency",
        "Debugging",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>C++20 Coroutines introduce a powerful new paradigm for asynchronous programming, allowing developers to write asynchronous code that looks and flows like synchronous code. However, their 'stackless' nature and custom memory management (via the promise_type's get_return_object, initial_suspend, and final_suspend methods) introduce complex lifetime challenges that differ significantly from traditional stack-based functions. A coroutine's frame is allocated on the heap (or via custom allocators) and its lifetime is managed by its associated <code>std::coroutine_handle</code>. Issues arise when the handle is destroyed prematurely, the state it points to becomes invalid, or if resources captured by the coroutine outlive the coroutine's execution but are implicitly released by a containing scope.</p><p>Debugging these issues is particularly tricky because standard stack unwinding mechanisms don't directly apply. A coroutine's execution can suspend and resume across different call stacks and threads, making traditional stack traces less informative. Common problems include use-after-free errors, dangling coroutine handles, and data races if shared state is not properly synchronized across suspension points.</p>",
    "root_cause": "Mismanagement of the <code>std::coroutine_handle</code> lifetime, capturing variables by reference from scopes that terminate before the coroutine completes, or improper synchronization of shared state accessed by potentially concurrent coroutine continuations. The coroutine frame is destroyed while still reachable or actively processing, leading to undefined behavior.",
    "bad_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <string>\n#include <vector>\n\nstruct MyTask {\n    struct promise_type {\n        std::string value;\n        MyTask get_return_object() { return {}; }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_never final_suspend() noexcept { return {}; }\n        void unhandled_exception() {}\n        void return_value(std::string v) { value = std::move(v); }\n    };\n};\n\nMyTask buggy_coroutine(const std::string& input) {\n    // DANGER: 'input' is a const reference. If the caller's 'input' goes out of scope\n    // before this coroutine finishes, 'input' becomes a dangling reference.\n    // In this simplified example, it might not crash immediately, but imagine a co_await\n    // point here, and the caller's 'input' gets destroyed while suspended.\n    co_return input + \"_processed\"; \n}\n\nint main() {\n    std::string temp_scope_data = \"initial_data\";\n    // In a real async scenario, this might complete much later, after temp_scope_data is gone.\n    MyTask task = buggy_coroutine(temp_scope_data); \n    // For this simple example, task runs synchronously and might not show immediate crash.\n    // But if 'buggy_coroutine' had co_await, the issue would be more apparent.\n    std::cout << \"Task completed.\" << std::endl; // The return value is in task.promise().value\n    return 0;\n}\n```",
    "solution_desc": "To fix coroutine lifetime issues, ensure that all resources and data captured by a coroutine outlive its entire execution, including any suspension points. This often involves capturing by value using `std::move` for temporary objects, or using `std::shared_ptr` for dynamically allocated objects that need shared ownership. For complex state, consider a dedicated coroutine state object managed by `std::shared_ptr`. Always ensure `std::coroutine_handle` instances are properly managed and destroyed only when the coroutine is truly finished and no longer needed. Use tools like AddressSanitizer (ASan) or Valgrind during development, as they can often detect use-after-free issues even with the complex memory patterns of coroutines. Implement detailed logging within the coroutine's `promise_type` to track its state changes.",
    "good_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <string>\n#include <vector>\n#include <memory>\n\nstruct MyTask {\n    struct promise_type {\n        std::string value;\n        MyTask get_return_object() { return {}; }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_never final_suspend() noexcept { return {}; }\n        void unhandled_exception() { std::terminate(); }\n        void return_value(std::string v) { value = std::move(v); }\n    };\n};\n\nMyTask safe_coroutine(std::string input_data) {\n    // SAFE: 'input_data' is captured by value. It is now owned by the coroutine frame\n    // and will exist for the entire lifetime of the coroutine's execution.\n    std::string result = input_data + \"_processed_safely\";\n    co_return result;\n}\n\n// Alternative: using shared_ptr for shared state that might outlive caller\nMyTask shared_state_coroutine(std::shared_ptr<std::string> shared_data) {\n    if (shared_data) {\n        *shared_data += \"_modified_by_coroutine\";\n    }\n    co_return \"Shared data processed\";\n}\n\nint main() {\n    // Example 1: Capturing by value\n    std::string temp_data = \"initial_data\";\n    MyTask task1 = safe_coroutine(std::move(temp_data)); // 'temp_data' is moved, coroutine owns it\n    std::cout << \"Task 1 result: \" << task1.promise().value << std::endl;\n\n    // Example 2: Using shared_ptr\n    auto shared_str = std::make_shared<std::string>(\"global_resource\");\n    MyTask task2 = shared_state_coroutine(shared_str);\n    std::cout << \"Task 2 result: \" << task2.promise().value << std::endl;\n    std::cout << \"Shared string after coroutine: \" << *shared_str << std::endl;\n\n    return 0;\n}\n```",
    "verification": "Compile with AddressSanitizer (ASan) if available (e.g., `g++ -fsanitize=address ...`). Run your application under Valgrind to detect memory leaks and use-after-free errors. Implement comprehensive logging within your `promise_type`'s constructor, destructor, `initial_suspend`, `final_suspend`, and return/exception handling methods to track coroutine frame creation, suspension, resumption, and destruction. Utilize debugger features to inspect coroutine handles and the memory they point to, paying close attention to object lifetimes around `co_await` and `co_return` points.",
    "date": "2026-06-18",
    "id": 1781785092,
    "type": "error"
});