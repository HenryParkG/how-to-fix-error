window.onPostDataLoaded({
    "title": "Fixing WebGPU Buffer Mapping Race Conditions",
    "slug": "webgpu-buffer-mapping-race-conditions",
    "language": "TypeScript",
    "code": "OperationError (Buffer Mapped State)",
    "tags": [
        "WebGPU",
        "TypeScript",
        "React",
        "Error Fix"
    ],
    "analysis": "<p>When developing graphics or heavy GPGPU compute pipelines using WebGPU, a common bug occurs during host-device synchronization. Programmers frequently hit WebGPU validation errors stating that a buffer cannot be mapped because its state is not unmapped, or that the buffer is currently in use by the GPU. This occurs because the GPU execution timeline operates asynchronously from the CPU main thread javascript runtime.</p><p>Attempting to call <code>GPUBuffer.mapAsync()</code> immediately after submitting a command buffer to the device queue, without coordinating the execution states, results in a race condition. The CPU tries to map the buffer memory space before the WebGPU engine has finished executing the pipeline commands that read from or write to that buffer. This invalidates the CPU-side read operation.</p>",
    "root_cause": "The application issues a `GPUBuffer.mapAsync` request on a storage buffer while the GPU queue is still executing commands that write to it. This violates WebGPU's strict buffer state machine, which forbids mapping buffers currently locked by active queue operations.",
    "bad_code": "// Bad Practice: Initiating buffer mapping immediately after submission \n// without waiting for the GPU timeline to complete execution.\nasync function runComputePipeline(device: GPUDevice, pipeline: GPUComputePipeline, bindGroup: GPUBindGroup, resultBuffer: GPUBuffer) {\n    const commandEncoder = device.createCommandEncoder();\n    const passEncoder = commandEncoder.beginComputePass();\n    passEncoder.setPipeline(pipeline);\n    passEncoder.setBindGroup(0, bindGroup);\n    passEncoder.dispatchWorkgroups(64);\n    passEncoder.end();\n\n    device.queue.submit([commandEncoder.finish()]);\n\n    // Race condition: Buffer mapping requested while GPU is still running the compute pass!\n    const mapPromise = resultBuffer.mapAsync(GPUMapMode.READ);\n    await mapPromise;\n    const arrayBuffer = resultBuffer.getMappedRange();\n    console.log(new Float32Array(arrayBuffer));\n    resultBuffer.unmap();\n}",
    "solution_desc": "To fix WebGPU buffer mapping race conditions, synchronize CPU and GPU workloads using staging buffers and queue execution promises. First, copy the computed output to a staging buffer designated for readback (`GPUBufferUsage.COPY_DST | GPUBufferUsage.MAP_READ`). Next, use `device.queue.onSubmittedWorkDone()` to wait for GPU execution to complete. Finally, map the staging buffer, read the data, and call `unmap()` before submitting the next frame.",
    "good_code": "// Good Practice: Synchronizing host/device timelines using a staging buffer and waiting on work completion.\nasync function runComputePipelineSynchronized(\n    device: GPUDevice, \n    pipeline: GPUComputePipeline, \n    bindGroup: GPUBindGroup, \n    gpuResultBuffer: GPUBuffer, // Buffer used strictly inside GPU pipeline\n    stagingBuffer: GPUBuffer   // Buffer created with GPUBufferUsage.MAP_READ | GPUBufferUsage.COPY_DST\n) {\n    const commandEncoder = device.createCommandEncoder();\n    const passEncoder = commandEncoder.beginComputePass();\n    passEncoder.setPipeline(pipeline);\n    passEncoder.setBindGroup(0, bindGroup);\n    passEncoder.dispatchWorkgroups(64);\n    passEncoder.end();\n\n    // Safely copy compute results from GPU-private memory to CPU-mappable staging buffer\n    commandEncoder.copyBufferToBuffer(gpuResultBuffer, 0, stagingBuffer, 0, stagingBuffer.size);\n    device.queue.submit([commandEncoder.finish()]);\n\n    // Properly synchronize: Wait until GPU finishes all queue actions\n    await device.queue.onSubmittedWorkDone();\n\n    // Safely map staging buffer now that the GPU has released ownership\n    await stagingBuffer.mapAsync(GPUMapMode.READ);\n    \n    const arrayBuffer = stagingBuffer.getMappedRange();\n    const results = new Float32Array(arrayBuffer.slice(0)); // Clone array to retain data safely\n    \n    stagingBuffer.unmap(); // Immediately unmap to return ownership back to GPU for future frames\n    \n    console.log(\"Synchronized compute outputs:\", results);\n    return results;\n}",
    "verification": "Run the application inside a browser like Chrome with WebGPU validation layers enabled. Verify that the console is free of any `Validation Error: Buffer is not in an unmapped state` errors, and confirm that mapping execution is structurally deferred until after queue submission.",
    "date": "2026-06-26",
    "id": 1782474041,
    "type": "error"
});