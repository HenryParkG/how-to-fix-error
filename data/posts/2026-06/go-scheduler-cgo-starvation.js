window.onPostDataLoaded({
    "title": "Fixing Go Scheduler Starvation from Blocking CGO Calls",
    "slug": "go-scheduler-cgo-starvation",
    "language": "Go",
    "code": "SchedulerStarvation",
    "tags": [
        "Go",
        "CGO",
        "Concurrency",
        "Performance",
        "Go",
        "Error Fix"
    ],
    "analysis": "<p>Go's concurrency model relies on a highly efficient scheduler that maps lightweight goroutines to a smaller number of operating system (OS) threads. This 'M:N' scheduling (M goroutines to N OS threads) is usually transparent and performs exceptionally well. However, when a Go program makes a CGO call to C code, the Go runtime must bind the executing goroutine to an OS thread. If that CGO call then performs a long-running, blocking operation (e.g., synchronous I/O, a CPU-intensive computation without yielding, or waiting on a C lock), it effectively blocks the underlying OS thread.</p><p>If a significant number of goroutines simultaneously make such blocking CGO calls, and the number of available OS threads (which defaults to <code>GOMAXPROCS</code>) is small, the Go scheduler can become starved of threads. It has runnable goroutines but no free OS threads to execute them on. This leads to severe latency spikes, reduced throughput, and even deadlocks as the entire Go application slows down, waiting for the blocking CGO calls to complete and release their OS threads. This problem is particularly insidious because it might not show high CPU usage by Go itself, but rather by the C library or I/O waits.</p>",
    "root_cause": "The specific technical reason for failure is that blocking CGO calls hold onto the underlying OS threads (Ms) without releasing them back to the Go scheduler. If the number of such blocked CGO calls exceeds the available `GOMAXPROCS` threads, the Go runtime runs out of OS threads to schedule other, non-CGO goroutines, leading to scheduler starvation and latency spikes across the entire Go application. This often happens when C libraries perform synchronous file I/O, network I/O, or heavy computation without yielding control.",
    "bad_code": "```go\npackage main\n\n/*\n#include <unistd.h>\nvoid blocking_c_func() {\n    // Simulate a long-running blocking C call, e.g., complex computation or I/O.\n    // In a real scenario, this might be a library call to zlib, libpq, etc.\n    sleep(5); // Blocks the calling OS thread for 5 seconds\n}\n*/\nimport \"C\"\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc main() {\n\t// Set GOMAXPROCS to a low value to make starvation more evident\n\t// In production, GOMAXPROCS defaults to the number of CPU cores.\n\truntime.GOMAXPROCS(1)\n\n\tfmt.Println(\"GOMAXPROCS set to:\", runtime.GOMAXPROCS(0))\n\tfmt.Println(\"Starting Go program with problematic CGO calls...\")\n\n\tvar wg sync.WaitGroup\n\t// Launch multiple goroutines that will all call the blocking C function\n\t// With GOMAXPROCS=1, only one OS thread is available, leading to starvation.\n\tfor i := 0; i < 5; i++ {\n\t\twg.Add(1)\n\t\tgo func(id int) {\n\t\t\tdefer wg.Done()\n\t\t\tfmt.Printf(\"Goroutine %d: Initiating blocking CGO call...\\n\", id)\n\t\t\tstart := time.Now()\n\t\t\tC.blocking_c_func() // This blocks the OS thread and thus the Go scheduler\n\t\t\tfmt.Printf(\"Goroutine %d: CGO call finished after %v\\n\", id, time.Since(start))\n\t\t}(i)\n\t}\n\n\t// Meanwhile, other Go routines try to run but might be starved\n\tgo func() {\n\t\tfor i := 0; i < 10; i++ {\n\t\t\ttime.Sleep(500 * time.Millisecond)\n\t\t\tfmt.Println(\"Background Go routine: Doing some non-CGO work.\")\n\t\t}\n\t}()\n\n\twg.Wait()\n\tfmt.Println(\"All CGO calls finished. Main program exiting.\")\n}\n```\n<p>In this example, setting <code>GOMAXPROCS(1)</code> drastically limits available OS threads. Multiple goroutines simultaneously calling <code>C.blocking_c_func()</code> will quickly exhaust the single OS thread, preventing the background goroutine from running on time and causing severe delays and scheduler starvation.</p>",
    "solution_desc": "<p>To prevent Go scheduler starvation from blocking CGO calls, the primary goal is to minimize the time an OS thread is blocked by C code or to ensure that blocking C calls do not prevent the Go scheduler from making progress. One robust solution is to move blocking CGO calls to a dedicated worker pool where each worker goroutine explicitly calls <code>runtime.LockOSThread()</code>. This guarantees that a specific OS thread is allocated for that goroutine (and its CGO calls), preventing it from being returned to the scheduler's pool and thus isolating the blocking behavior. After the CGO call, <code>runtime.UnlockOSThread()</code> releases the OS thread.</p><p>Alternatively, if the C library supports it, use non-blocking C functions or asynchronous C APIs. For CPU-bound C computations, consider breaking them down or using parallelism within C, ensuring the C code yields periodically. For C-based I/O, prefer rewriting the I/O operations directly in Go if feasible, or ensure the C library is optimized for non-blocking I/O (e.g., <code>epoll</code>, <code>kqueue</code>) that can be integrated with Go's network poller. Lastly, ensure <code>GOMAXPROCS</code> is set appropriately (usually to the number of CPU cores) to provide enough OS threads, though this alone won't solve fundamental blocking issues.</p>",
    "good_code": "```go\npackage main\n\n/*\n#include <unistd.h>\nvoid blocking_c_func() {\n    sleep(5); // Still a blocking C call\n}\n*/\nimport \"C\"\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\n// callBlockingCGOInDedicatedThread runs the CGO call on an OS thread\n// locked to this specific goroutine, isolating its blocking behavior.\nfunc callBlockingCGOInDedicatedThread(id int) {\n\truntime.LockOSThread() // Lock the current goroutine to an OS thread\n\tdefer runtime.UnlockOSThread()\n\n\tfmt.Printf(\"Goroutine %d (locked thread): Initiating blocking CGO call...\\n\", id)\n\tstart := time.Now()\n\tC.blocking_c_func() // This C call now blocks only this dedicated OS thread\n\tfmt.Printf(\"Goroutine %d (locked thread): CGO call finished after %v\\n\", id, time.Since(start))\n}\n\nfunc main() {\n\t// GOMAXPROCS is usually left at default (number of cores) for optimal Go scheduling.\n\t// For demonstration, let's reset to 4 to show it's not simply about having enough threads.\n\truntime.GOMAXPROCS(4)\n\n\tfmt.Println(\"GOMAXPROCS set to:\", runtime.GOMAXPROCS(0))\n\tfmt.Println(\"Starting Go program with CGO calls handled by dedicated threads...\")\n\n\tvar wg sync.WaitGroup\n\n\t// Launch multiple goroutines, each using a dedicated OS thread for the CGO call\n\tfor i := 0; i < 5; i++ {\n\t\twg.Add(1)\n\t\tgo func(id int) {\n\t\t\tdefer wg.Done()\n\t\t\tcallBlockingCGOInDedicatedThread(id)\n\t\t}(i)\n\t}\n\n\t// This background goroutine will now run without starvation because\n\t// the blocking CGO calls are isolated to their own OS threads.\n\tgo func() {\n\t\tfor i := 0; i < 10; i++ {\n\t\t\ttime.Sleep(500 * time.Millisecond)\n\t\t\tfmt.Println(\"Background Go routine: Doing some non-CGO work, no starvation!\")\n\t\t}\n\t}()\n\n\twg.Wait()\n\tfmt.Println(\"All CGO calls finished. Main program exiting.\")\n}\n```\n<p>By using <code>runtime.LockOSThread()</code>, each goroutine executing <code>callBlockingCGOInDedicatedThread</code> gets its own dedicated OS thread for the duration of the CGO call. This isolates the blocking behavior, allowing the Go scheduler to continue using other OS threads (up to <code>GOMAXPROCS</code>) to execute non-CGO goroutines without interference or starvation.</p>",
    "verification": "<p>To verify that CGO-induced scheduler starvation has been fixed:</p><ol><li><b>Go pprof:</b> Use <code>go tool pprof -web http://localhost:PORT/debug/pprof/block</code> to analyze blocking profiles. You should see fewer or no long-running blocking calls related to CGO, and the overall time spent blocked should decrease significantly. Also, check <code>/debug/pprof/goroutine?debug=1</code> to observe goroutine states; fewer goroutines should be in a 'syscall' state for extended periods if `runtime.LockOSThread` is used correctly.</li><li><b>GODEBUG Output:</b> Run your application with <code>GODEBUG=scheduler=1</code>. Analyze the scheduler trace. You should observe healthy scheduling behavior with goroutines frequently switching and no long pauses indicating a lack of available Ms.</li><li><b>Latency Metrics:</b> Monitor application-level latency for all RPCs and internal operations. Spikes caused by scheduler starvation should be eliminated or drastically reduced. Throughput should also improve significantly.</li><li><b>CPU Utilization:</b> While starvation might not always manifest as high CPU, consistent CPU utilization (appropriate for the workload) without periods of underutilization or unexplained lulls in Go processing can indicate a healthier scheduler.</li><li><b>Thread Count:</b> Observe the number of OS threads created by the Go process. With <code>runtime.LockOSThread()</code>, you might see a temporary increase in OS threads corresponding to the number of concurrent CGO calls, which is expected as each gets its own thread.</li></ol>",
    "date": "2026-06-02",
    "id": 1780368222,
    "type": "error"
});