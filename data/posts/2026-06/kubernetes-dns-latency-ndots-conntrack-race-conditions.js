window.onPostDataLoaded({
    "title": "Kubernetes DNS Latency: ndots & Conntrack Race",
    "slug": "kubernetes-dns-latency-ndots-conntrack-race-conditions",
    "language": "Kubernetes, Networking",
    "code": "DNS Latency",
    "tags": [
        "Kubernetes",
        "DNS",
        "Networking",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Applications running in Kubernetes clusters often experience mysterious DNS resolution delays, manifesting as slow service discovery, increased connection timeouts, and general application slowness. This issue frequently stems from a combination of the default <code>ndots:5</code> setting in container <code>resolv.conf</code> files and kernel <code>conntrack</code> table limitations, particularly for UDP-based DNS queries.</p><p>When a container attempts to resolve a hostname (e.g., <code>my-service</code>), the <code>ndots:5</code> option instructs the resolver to append up to five search domains (e.g., <code>my-service.default.svc.cluster.local</code>, <code>my-service.svc.cluster.local</code>, etc.) before attempting an absolute lookup. For each appended domain that fails, a separate DNS query is made. This process creates a burst of UDP DNS queries.</p><p>The Linux kernel's <code>conntrack</code> module tracks network connections. For UDP, a connection entry is created and typically times out after a short period (e.g., 30 seconds by default). High volumes of short-lived UDP DNS queries, especially with repeated lookups due to <code>ndots:5</code>, can quickly fill the <code>conntrack</code> table. When the table is full, new connections (including DNS queries) are dropped, leading to retransmissions, increased latency, and eventually timeouts. This becomes a race condition where legitimate DNS requests might get dropped while <code>conntrack</code> table entries are being flushed, further exacerbating the problem.</p>",
    "root_cause": "The primary root cause is the combination of the default <code>ndots:5</code> setting in Kubernetes pods (leading to multiple DNS lookups for unqualified hostnames) and the Linux kernel's <code>conntrack</code> table reaching its limits under high load from these frequent, short-lived UDP DNS queries. This results in dropped DNS packets and subsequent retransmissions, causing latency spikes.",
    "bad_code": "```bash\n# Example /etc/resolv.conf inside a typical Kubernetes pod without explicit dnsConfig\nsearch default.svc.cluster.local svc.cluster.local cluster.local us-east-1.compute.internal\nnameserver 10.96.0.10\noptions ndots:5 # This default option triggers multiple lookups\n```",
    "solution_desc": "The most effective solution involves configuring Kubernetes pods to use <code>ndots:1</code>. This minimizes the number of DNS queries for service discovery within the cluster by first attempting to resolve hostnames directly, rather than exhaustively searching all configured domains. This reduces the load on the DNS server and the <code>conntrack</code> table. Additionally, consider deploying NodeLocal DNSCache to further reduce the load on the cluster DNS and improve resolution latency by caching responses directly on the node. Regularly monitoring and potentially increasing the size of the <code>conntrack</code> table (<code>net.netfilter.nf_conntrack_max</code>) and reducing its timeout (<code>net.netfilter.nf_conntrack_udp_timeout_stream</code>) on worker nodes can also alleviate pressure, though addressing <code>ndots</code> is usually the first and most impactful step.",
    "good_code": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-application-pod\nspec:\n  containers:\n  - name: my-app\n    image: my-image:latest\n    command: [\"sh\", \"-c\", \"echo 'Application starting...' && sleep 3600\"]\n  dnsConfig:\n    options:\n      - name: ndots\n        value: \"1\"\n```\n<p>For deployments, apply this to the <code>podTemplate</code>:</p>\n```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-application-deployment\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: my-app\n  template:\n    metadata:\n      labels:\n        app: my-app\n    spec:\n      containers:\n      - name: my-app\n        image: my-image:latest\n        command: [\"sh\", \"-c\", \"echo 'Application starting...' && sleep 3600\"]\n      dnsConfig:\n        options:\n          - name: ndots\n            value: \"1\"\n```",
    "verification": "After applying the <code>dnsConfig</code> change, monitor application logs for fewer DNS-related timeout errors. Use network diagnostic tools like <code>tcpdump</code> or <code>tshark</code> within a pod to observe DNS query patterns; you should see fewer repeated lookups for unqualified hostnames. Track DNS resolution latency using Prometheus/Grafana metrics from CoreDNS or application-level metrics. Observe <code>conntrack</code> table usage on worker nodes using <code>conntrack -L | wc -l</code> to ensure it no longer reaches saturation during peak load.",
    "date": "2026-06-09",
    "id": 1781006080,
    "type": "error"
});