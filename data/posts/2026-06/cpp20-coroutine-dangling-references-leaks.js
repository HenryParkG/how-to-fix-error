window.onPostDataLoaded({
    "title": "Fixing C++20 Coroutine Dangling References & Leaks",
    "slug": "cpp20-coroutine-dangling-references-leaks",
    "language": "C++",
    "code": "Lifetime Leak",
    "tags": [
        "Rust",
        "C++",
        "Memory Management",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>C++20 coroutines introduce powerful asynchronous primitives but transfer lifetime ownership responsibilities entirely to the developer. A common point of failure is passing parameters by reference (e.g., <code>const T&amp;</code>) or forwarding reference (<code>T&amp;&amp;</code>) to a coroutine that suspends. Unlike normal functions, when a coroutine suspends via <code>co_await</code> or <code>co_yield</code>, its stack frame is preserved on the heap, but the caller's stack frame continues executing. If the arguments passed by reference were temporaries or local stack variables of the caller, those objects are destroyed while the coroutine is suspended. When the coroutine resumes, accessing those references results in undefined behavior (dangling reference).</p><p>Additionally, memory leaks occur when the <code>std::coroutine_handle</code> is not properly destroyed. If the return object of the coroutine (typically a custom <code>Task&lt;T&gt;</code>) does not manage the lifetime of the handle via RAII, or if the promise type specifies <code>std::suspend_always</code> for <code>final_suspend</code> but never calls <code>.destroy()</code> manually on the handle, the allocated coroutine frame will leak permanently.</p>",
    "root_cause": "Coroutine parameters passed by reference are stored as references within the dynamically allocated coroutine frame. If the underlying reference object goes out of scope during a suspension point, the coroutine frame holds a dangling pointer. Furthermore, heap-allocated coroutine frames are not automatically cleaned up if the RAII wrapper fails to call handle.destroy() when final_suspend returns std::suspend_always.",
    "bad_code": "#include <iostream>\n#include <coroutine>\n#include <string>\n#include <future>\n\nstruct BadTask {\n    struct promise_type {\n        BadTask get_return_object() { return BadTask{std::coroutine_handle<promise_type>::from_promise(*this)}; }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void unhandled_exception() { std::terminate(); }\n        void return_void() {}\n    };\n    std::coroutine_handle<promise_type> handle;\n};\n\n// BUG: Argument passed by reference. When suspended, the temporary string dies.\nBadTask print_delayed(const std::string& message) {\n    co_await std::suspend_always{}; // Suspends execution\n    std::cout << message << std::endl; // UNDEFINED BEHAVIOR: message is dangling!\n}\n\nint main() {\n    auto task = print_delayed(std::string(\"Temp String\"));\n    // Temp String is destroyed here, but task is resumed next:\n    task.handle.resume(); \n    // Memory leak: task.handle.destroy() is never called, and final_suspend is suspend_always.\n    return 0;\n}",
    "solution_desc": "To fix dangling reference issues, always pass parameters by value to coroutines that can suspend, forcing the compiler to copy or move the arguments directly into the coroutine frame where their lifetime matches the frame's lifetime. To fix memory leaks, implement strict RAII in the return object wrapper: store the std::coroutine_handle, prevent copying (only allow move), and call .destroy() in the destructor if the handle is valid.",
    "good_code": "#include <iostream>\n#include <coroutine>\n#include <string>\n#include <utility>\n\nstruct SafeTask {\n    struct promise_type {\n        SafeTask get_return_object() { \n            return SafeTask{std::coroutine_handle<promise_type>::from_promise(*this)}; \n        }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void unhandled_exception() { std::terminate(); }\n        void return_void() {}\n    };\n\n    std::coroutine_handle<promise_type> handle = nullptr;\n\n    explicit SafeTask(std::coroutine_handle<promise_type> h) : handle(h) {}\n    \n    // Implement RAII to prevent leaks\n    ~SafeTask() {\n        if (handle) {\n            handle.destroy();\n        }\n    }\n\n    // Prevent copying to avoid double destruction\n    SafeTask(const SafeTask&) = delete;\n    SafeTask& operator=(const SafeTask&) = delete;\n\n    // Support moving safely\n    SafeTask(SafeTask&& other) noexcept : handle(other.handle) {\n        other.handle = nullptr;\n    }\n    SafeTask& operator=(SafeTask&& other) noexcept {\n        if (this != &other) {\n            if (handle) handle.destroy();\n            handle = other.handle;\n            other.handle = nullptr;\n        }\n        return *this;\n    }\n};\n\n// FIXED: Parameter passed by value. Copied/Moved securely into coroutine frame.\nSafeTask print_delayed_safe(std::string message) {\n    co_await std::suspend_always{}; \n    std::cout << message << std::endl; // Safe: message owned by coroutine frame\n}\n\nint main() {\n    {\n        auto task = print_delayed_safe(std::string(\"Safe String\"));\n        task.handle.resume();\n    } // SafeTask Destructor automatically calls handle.destroy(), preventing leaks.\n    return 0;\n}",
    "verification": "Compile with AddressSanitizer (ASan) and UndefinedBehaviorSanitizer (UBSan) flags: `g++ -std=c++20 -fsanitize=address,undefined main.cpp -o main`. Run the executable; ensure there are zero allocation leaks detected upon termination and no memory segmentation/use-after-free faults during execution.",
    "date": "2026-06-05",
    "id": 1780626784,
    "type": "error"
});