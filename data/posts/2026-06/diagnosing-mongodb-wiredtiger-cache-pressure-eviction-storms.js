window.onPostDataLoaded({
    "title": "Diagnosing MongoDB WiredTiger Cache Pressure & Eviction Storms",
    "slug": "diagnosing-mongodb-wiredtiger-cache-pressure-eviction-storms",
    "language": "MongoDB",
    "code": "WiredTigerCache",
    "tags": [
        "MongoDB",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses an internal cache to hold frequently accessed data, enabling high-performance read and write operations by minimizing disk I/O. When the working set (the data actively accessed by the application) exceeds the allocated WiredTiger cache size, MongoDB experiences 'cache pressure.' This means the engine is forced to frequently evict older, less recently used data from the cache to make room for new data, even if that 'older' data is still quite relevant.</p><p>Persistent cache pressure can escalate into 'eviction storms,' where the database spends a disproportionate amount of time and CPU cycles on evicting pages and fetching data from disk. Symptoms include significantly increased disk I/O, high CPU utilization (especially system CPU for I/O waits or WiredTiger's internal eviction threads), degraded query performance, increased latency for all operations, and potentially server unresponsiveness. It's a critical bottleneck that directly impacts the user experience and overall system throughput.</p>",
    "root_cause": "The specific technical reason for WiredTiger cache pressure and eviction storms is often a mismatch between the application's working set size and the available memory allocated to the WiredTiger cache. This can stem from: <ul><li><b>Insufficient RAM:</b> The server simply doesn't have enough physical memory to accommodate the active data.</li><li><b>Under-configured WiredTiger cache:</b> The default cache size (typically 50% of RAM minus 1GB) might be too small for specific workloads, especially on systems with very large amounts of RAM where MongoDB needs more than 50%.</li><li><b>Inefficient queries & indexing:</b> Queries that scan large collections without proper indexes force MongoDB to load extensive amounts of data into memory, bloating the cache with unnecessary pages.</li><li><b>Large documents or inefficient schema:</b> Storing excessively large documents or a schema that frequently updates large parts of documents can increase memory churn.</li><li><b>High write volume:</b> Continuous writes generate new pages that need to be cached and eventually flushed, increasing pressure.</li><li><b>Misconfigured memory limits:</b> Other processes consuming a significant portion of system memory, leaving less for MongoDB.</li></ul>",
    "bad_code": "```javascript\n// Example of a potentially problematic query/schema that exacerbates cache pressure:\n\n// Imagine a collection 'logs' with billions of documents, no index on 'timestamp' or 'level'\ndb.logs.find({ \"timestamp\": { $gte: ISODate(\"2023-01-01T00:00:00Z\") }, \"level\": \"ERROR\" }).sort({ \"timestamp\": -1 }).limit(100)\n// This query performs a full collection scan (COLLSCAN) across a huge dataset, \n// pulling massive amounts of data into the WiredTiger cache unnecessarily.\n\n// Another example: Frequent updates to large embedded arrays without projection.\ndb.users.updateMany(\n    { \"status\": \"active\" },\n    { $push: { \"activityLog\": { \"action\": \"login\", \"time\": new Date() } } }\n)\n// If 'activityLog' is a large array and many 'active' users are updated,\n// this can cause many large documents to be loaded, modified, and re-cached.\n```",
    "solution_desc": "Architecturally, addressing WiredTiger cache pressure requires a multi-faceted approach. Prioritize ensuring the working set fits within the WiredTiger cache.<ul><li><b>Tune WiredTiger Cache Size:</b> Adjust `storage.wiredTiger.engineConfig.cacheSizeGB` in `mongod.conf`. For dedicated database servers, you can often allocate up to 70-80% of system RAM to WiredTiger, especially if the working set is large.</li><li><b>Optimize Queries and Indexes:</b> Analyze slow queries using `db.currentOp()` or profiler output. Create appropriate indexes to support queries, minimizing collection scans. Use covering indexes when possible to avoid fetching full documents.</li><li><b>Schema Design:</b> Store smaller, frequently accessed data separately from larger, less critical data. Avoid overly large documents if portions are frequently updated. Consider using `capped collections` for high-volume log data where older data can be automatically discarded.</li><li><b>Sharding:</b> Distribute your data across multiple servers if the working set is too large for a single instance, allowing each shard to manage a smaller portion of the overall data.</li><li><b>Hardware Upgrade:</b> Add more RAM if tuning and optimization prove insufficient. Faster storage (SSDs/NVMe) can mitigate the impact of evictions but doesn't solve the root cause.</li><li><b>Monitor Read/Write Concerns:</b> While not a direct fix, understand that stronger read/write concerns (`majority`) can sometimes increase I/O.</li></ul>",
    "good_code": "```javascript\n// 1. Optimize WiredTiger Cache Size in mongod.conf:\n// (Example for a server with 64GB RAM, dedicating 40GB to WiredTiger)\n// /etc/mongod.conf\n// storage:\n//   wiredTiger:\n//     engineConfig:\n//       cacheSizeGB: 40\n//       journalDirectory: /var/lib/mongodb/journal\n//       directoryForIndexes: false # Keep indexes in same directory as data\n\n// 2. Add an index to support the problematic query:\ndb.logs.createIndex({ \"timestamp\": -1, \"level\": 1 })\n// This index allows the query to use an IXSCAN, reading only relevant data.\n\n// Optimized query (remains the same, but now uses the index):\ndb.logs.find({ \"timestamp\": { $gte: ISODate(\"2023-01-01T00:00:00Z\") }, \"level\": \"ERROR\" }).sort({ \"timestamp\": -1 }).limit(100)\n\n// 3. For updates to large arrays, consider using $elemMatch and positional operators\n// or if possible, denormalize or use separate collections for highly dynamic large arrays.\n// Example using $elemMatch to update a specific element in an array without loading the whole array:\ndb.users.updateOne(\n    { \"_id\": ObjectId(\"...\"), \"activityLog.action\": \"login_failed\" },\n    { $set: { \"activityLog.$.status\": \"reviewed\" } }\n)\n// This only applies if you're updating an existing element. For $push, it's harder to avoid loading the document.\n// If the `activityLog` grows excessively large, consider storing it in a separate collection, linked by `user_id`.\n```",
    "verification": "Verify the fix by continually monitoring MongoDB's performance metrics. Key indicators from `db.serverStatus().wiredTiger.cache` include: <ul><li>`tracked dirty bytes in the cache`: Should be low relative to total cache size.</li><li>`bytes currently in the cache`: Should be stable and not constantly at maximum, indicating enough room.</li><li>`pages evicted from the cache (Delta)`: Should decrease significantly, indicating fewer eviction storms.</li><li>`pages read into cache`: Should ideally be lower than before, or stable if the workload is consistent.</li><li>`cache hit ratio`: Aim for 95%+ for reads.</li></ul>Also, observe overall server metrics: reduce disk I/O, lower system CPU, stable memory utilization, and improved query latency. Tools like MongoDB Atlas monitoring, `mongostat`, `mongotop`, and `db.currentOp()` are invaluable for real-time and historical analysis. The `ftdc` (Full Time Diagnostic Data Capture) data can also be invaluable for post-mortem analysis.",
    "date": "2026-06-29",
    "id": 1782700613,
    "type": "error"
});