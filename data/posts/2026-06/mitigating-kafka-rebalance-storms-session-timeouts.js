window.onPostDataLoaded({
    "title": "Mitigating Kafka Rebalance Storms & Session Timeouts",
    "slug": "mitigating-kafka-rebalance-storms-session-timeouts",
    "language": "Kafka",
    "code": "KafkaRebalance",
    "tags": [
        "Kafka",
        "Distributed Systems",
        "Message Queue",
        "Java",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Kafka consumer group rebalances are a fundamental mechanism for distributing partitions among consumers and reacting to changes in the group (e.g., new consumers joining, existing consumers leaving, or consumer failures). While essential, frequent or prolonged rebalances, often termed 'rebalance storms', can severely impact application performance and data processing stability. These storms lead to service interruptions, increased latency, message processing stalls, and even duplicate processing as consumers repeatedly stop and restart processing partitions.</p><p>A common symptom is consumers logging 'Group coordinator dead' or 'Revoking partitions' repeatedly, followed by 'Assigning partitions'. This churn indicates that the consumer group is struggling to stabilize. Session timeouts are closely related; if a consumer fails to send heartbeats to the group coordinator within a configured duration, it's considered dead, triggering a rebalance. Similarly, if a consumer takes too long to process messages within a poll interval, it might miss heartbeats, leading to the same outcome.</p>",
    "root_cause": "Frequent Kafka rebalance storms and session timeouts typically stem from a combination of misconfigured consumer properties and inefficient message processing logic. Key root causes include: (1) Aggressive `session.timeout.ms` or `heartbeat.interval.ms` leading to premature consumer eviction. (2) High `max.poll.interval.ms` values that are too short for the actual message processing time, causing the consumer to be deemed inactive. (3) Slow or blocking message processing within the `poll()` loop, preventing timely heartbeats. (4) Large batch sizes causing `poll()` to take longer. (5) Frequent consumer application crashes or restarts. (6) Network issues disrupting communication between consumers and brokers.",
    "bad_code": "```java\nProperties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"group.id\", \"my-consumer-group\");\n// Aggressive or default timeouts\nprops.put(\"session.timeout.ms\", \"10000\"); // 10 seconds\nprops.put(\"heartbeat.interval.ms\", \"3000\"); // 3 seconds\nprops.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes (default, but can be too short for slow processing)\nprops.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"auto.offset.reset\", \"earliest\");\n\ntry (KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props)) {\n    consumer.subscribe(Collections.singletonList(\"my-topic\"));\n    while (true) {\n        ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n        for (ConsumerRecord<String, String> record : records) {\n            // SIMULATE SLOW PROCESSING OR EXTERNAL CALLS\n            Thread.sleep(5000); // Imagine a database write or network call per record\n            System.out.printf(\"Consumed record offset = %d, key = %s, value = %s%n\",\n                              record.offset(), record.key(), record.value());\n        }\n        // Manual commit is often missed or happens too late\n        // consumer.commitSync(); \n    }\n}\ncatch (Exception e) {\n    e.printStackTrace();\n}\n```",
    "solution_desc": "To mitigate Kafka rebalance storms and session timeouts, a multi-pronged approach is required. First, re-evaluate and adjust Kafka consumer configuration properties: increase `session.timeout.ms` to allow more buffer for consumer responsiveness (e.g., 30-45 seconds), ensure `heartbeat.interval.ms` is significantly smaller than `session.timeout.ms` (e.g., 1/3rd). Crucially, set `max.poll.interval.ms` to an appropriate value that accommodates the maximum expected time for processing a batch of messages. If message processing is inherently slow, consider offloading processing outside the `poll()` loop into a separate thread pool. This allows the consumer thread to quickly return from `poll()` and send heartbeats, even if actual message processing takes longer. Ensure efficient processing within the consumer, optimize application code, and implement robust error handling. For very large message batches, consider reducing `max.poll.records`. Leverage incremental cooperative rebalancing (Kafka 2.4+) if possible for less disruptive rebalances. Finally, monitor consumer lag and rebalance events to identify issues proactively.",
    "good_code": "```java\nProperties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"group.id\", \"my-consumer-group\");\n// Optimized timeout settings\nprops.put(\"session.timeout.ms\", \"45000\"); // 45 seconds\nprops.put(\"heartbeat.interval.ms\", \"15000\"); // 15 seconds (1/3 of session timeout)\n// Ensure max.poll.interval.ms is long enough for processing or use separate threads\nprops.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes, adjust based on max processing time\nprops.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"auto.offset.reset\", \"earliest\");\nprops.put(\"enable.auto.commit\", \"false\"); // Disable auto-commit for explicit control\n\n// Using a separate thread pool for processing to avoid blocking the main consumer thread\nExecutorService executor = Executors.newFixedThreadPool(Runtime.getRuntime().availableProcessors());\n\ntry (KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props)) {\n    consumer.subscribe(Collections.singletonList(\"my-topic\"));\n    while (true) {\n        ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n        if (!records.isEmpty()) {\n            // Submit processing tasks to a separate thread pool\n            for (ConsumerRecord<String, String> record : records) {\n                executor.submit(() -> {\n                    try {\n                        // Actual processing logic, can be slow\n                        Thread.sleep(5000); \n                        System.out.printf(\"Processed record offset = %d, key = %s, value = %s%n\",\n                                          record.offset(), record.key(), record.value());\n                        // Handle acknowledgements if necessary, or commit after processing a batch\n                    } catch (InterruptedException e) {\n                        Thread.currentThread().interrupt();\n                        System.err.println(\"Processing interrupted for record: \" + record.offset());\n                    } catch (Exception e) {\n                        System.err.println(\"Error processing record: \" + record.offset() + \": \" + e.getMessage());\n                    }\n                });\n            }\n            // Commit offsets synchronously only after all records in the batch are processed or queued\n            consumer.commitSync(); \n        }\n    }\n}\ncatch (Exception e) {\n    e.printStackTrace();\n}\nfinally {\n    executor.shutdown();\n    try { \n        if (!executor.awaitTermination(60, TimeUnit.SECONDS)) {\n            executor.shutdownNow();\n        }\n    } catch (InterruptedException ex) {\n        executor.shutdownNow();\n        Thread.currentThread().interrupt();\n    }\n}\n```",
    "verification": "To verify the fix, monitor key Kafka metrics and application logs. Look for a significant reduction in 'Revoking partitions' and 'Assigning partitions' messages in consumer logs, indicating fewer rebalances. Observe consumer lag across all partitions to ensure it remains stable and doesn't grow uncontrollably. Use Kafka's built-in monitoring tools (e.g., JMX metrics for `kafka.consumer`) or external monitoring platforms to track `rebalance-total`, `rebalance-latency-avg`, and `commit-latency-avg`. Ensure that `poll` calls are returning promptly and heartbeats are sent regularly. Finally, track end-to-end latency and throughput of your message processing pipeline to confirm overall stability and performance improvements.",
    "date": "2026-06-19",
    "id": 1781872912,
    "type": "error"
});