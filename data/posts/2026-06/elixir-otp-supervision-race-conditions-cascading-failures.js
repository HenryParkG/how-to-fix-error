window.onPostDataLoaded({
    "title": "Elixir OTP Supervision: Race Conditions & Cascading Failures",
    "slug": "elixir-otp-supervision-race-conditions-cascading-failures",
    "language": "Elixir",
    "code": "Race Condition",
    "tags": [
        "Elixir",
        "OTP",
        "Concurrency",
        "Reliability",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Elixir's OTP (Open Telecom Platform) is renowned for building fault-tolerant, concurrent systems through its supervision trees. However, misconfigured or poorly designed supervision strategies can inadvertently introduce subtle race conditions or exacerbate failures, leading to cascading outages rather than preventing them. A common pitfall occurs when child processes within a supervision tree have implicit dependencies on each other's state or startup order without proper synchronization, or when their restart strategies are too aggressive or too lenient.</p><p>For instance, if two GenServers communicate, and one relies on the other being fully initialized before it can safely start, a race condition can occur if the supervisor starts them in parallel without explicit ordering or readiness checks. Similarly, a <code>one_for_all</code> restart strategy might tear down an entire subtree unnecessarily when only a single, isolated child process fails, causing a widespread service interruption that could have been contained with a <code>one_for_one</code> or <code>rest_for_one</code> strategy.</p>",
    "root_cause": "Incorrect supervision strategy (e.g., `one_for_all` for independent processes), implicit state dependencies between supervised processes, lack of explicit startup ordering or readiness handshakes, or aggressive restart settings.",
    "bad_code": "```elixir\n# lib/my_app/application.ex (example of problematic supervision)\ndef start(_type, _args) do\n  children = [\n    MyApp.DependentWorker, # Assumes MyApp.StateManager is ready\n    MyApp.StateManager\n  ]\n\n  opts = [strategy: :one_for_all, name: MyApp.Supervisor]\n  Supervisor.start_link(children, opts)\nend\n\n# lib/my_app/dependent_worker.ex\ndefmodule MyApp.DependentWorker do\n  use GenServer\n\n  def start_link(_opts) do\n    GenServer.start_link(__MODULE__, :ok, name: __MODULE__)\n  end\n\n  @impl true\n  def init(:ok) do\n    # This might fail if MyApp.StateManager isn't fully initialized yet\n    state_manager_pid = Process.whereis(MyApp.StateManager)\n    if is_nil(state_manager_pid) do\n      raise \"StateManager not found!\"\n    end\n    # ... perform setup using StateManager ...\n    {:ok, %{}}\n  end\nend\n```",
    "solution_desc": "The solution involves carefully designing the supervision hierarchy, explicitly managing dependencies, and selecting appropriate restart strategies. For dependent processes, supervisors can be nested to enforce startup order, or explicit \"wait-for-ready\" mechanisms can be implemented using `GenServer.call` during initialization. Employ `one_for_one` for independent processes, `rest_for_one` for processes with clear sequential dependencies where one failure impacts subsequent ones in the group, and `one_for_all` only when the failure of any child truly invalidates the entire group's state. Using `init/2` in GenServers for startup logic that can handle errors and potentially retry or report issues is key.",
    "good_code": "```elixir\n# lib/my_app/application.ex (improved supervision with ordered children and selective strategy)\ndef start(_type, _args) do\n  children = [\n    # Start StateManager first, then DependentWorker\n    MyApp.StateManager,\n    MyApp.DependentWorker\n  ]\n\n  # Use :one_for_one for independent processes, or :rest_for_one if DependentWorker\n  # truly depends on StateManager and both should restart together on StateManager's failure.\n  # Here, :one_for_one is chosen assuming DependentWorker can potentially re-initialize\n  # its link to StateManager if StateManager restarts independently.\n  opts = [strategy: :one_for_one, name: MyApp.Supervisor]\n  Supervisor.start_link(children, opts)\nend\n\n# lib/my_app/dependent_worker.ex (using GenServer.call for readiness check)\ndefmodule MyApp.DependentWorker do\n  use GenServer\n\n  def start_link(_opts) do\n    GenServer.start_link(__MODULE__, :ok, name: __MODULE__)\n  end\n\n  @impl true\n  def init(:ok) do\n    # During initialization, explicitly wait for StateManager to be ready\n    # This will block until StateManager replies or times out\n    case GenServer.call(MyApp.StateManager, :get_initial_state, :infinity) do\n      {:ok, initial_state} ->\n        {:ok, %{state: initial_state}}\n      {:error, reason} ->\n        # Log error and potentially exit, letting supervisor restart\n        Logger.error(\"Failed to get initial state from StateManager: #{inspect reason}\")\n        {:stop, :state_manager_unavailable}\n    end\n  end\n\n  # Add a handle_call in StateManager to respond to :get_initial_state\n  # Or, ensure StateManager is started, and handle cases where it might not be ready within DependentWorker.\n  # A better pattern might be DependentWorker registering with StateManager on startup.\nend\n\n# lib/my_app/state_manager.ex (example with a simple readiness check)\ndefmodule MyApp.StateManager do\n  use GenServer\n\n  def start_link(_opts) do\n    GenServer.start_link(__MODULE__, :ok, name: __MODULE__)\n  end\n\n  @impl true\n  def init(:ok) do\n    {:ok, %{initial_data: \"ready\"}}\n  end\n\n  @impl true\n  def handle_call(:get_initial_state, _from, state) do\n    {:reply, {:ok, state.initial_data}, state}\n  end\nend\n```",
    "verification": "Thoroughly test the system under various failure conditions. Simulate crashes of individual child processes (e.g., `Process.exit(pid, :kill)`). Observe the supervisor's restart behavior using `Logger` output and `Observer.start()`. Ensure that failures are contained according to the chosen strategy and that dependent processes either restart correctly or gracefully handle the unavailability of their dependencies. Introduce deliberate delays or resource contention during startup to expose potential race conditions, verifying that explicit readiness checks or sequential startup orders prevent issues.",
    "date": "2026-06-25",
    "id": 1782354515,
    "type": "error"
});