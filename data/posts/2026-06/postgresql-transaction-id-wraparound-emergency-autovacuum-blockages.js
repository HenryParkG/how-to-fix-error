window.onPostDataLoaded({
    "title": "PostgreSQL Transaction ID Wraparound: Autovacuum",
    "slug": "postgresql-transaction-id-wraparound-emergency-autovacuum-blockages",
    "language": "PostgreSQL, SQL",
    "code": "Wraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "Maintenance",
        "SQL",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit transaction ID (XID) system for its Multi-Version Concurrency Control (MVCC) architecture. Each transaction is assigned a unique XID. This 32-bit integer, however, has a finite limit (approximately 2 billion transactions). If the database generates new transaction IDs and eventually wraps around to reuse old IDs without properly 'freezing' older transaction IDs, it can lead to a critical issue known as transaction ID wraparound.</p><p>The problem arises because PostgreSQL determines visibility of data rows based on comparing current XIDs with the XIDs stored on the rows. If the counter wraps around, the database could mistakenly perceive future transactions as past, leading to data corruption and potential data loss. To prevent this, PostgreSQL marks older transactions as 'frozen,' making them appear as if they occurred in the distant past. This freezing process is primarily handled by the <code>autovacuum</code> daemon.</p><p>When the XID counter approaches its limit (typically 2 billion transactions from the oldest unfrozen XID), PostgreSQL enters an emergency shutdown mode to prevent data corruption. This 'emergency autovacuum' will relentlessly vacuum all tables to freeze old transactions, often bringing the database to a crawl or making it unresponsive until the crisis is averted. This is a critical production incident that needs immediate attention.</p>",
    "root_cause": "The specific technical reason for failure is the inability of <code>autovacuum</code> to keep pace with transaction generation and freeze old XIDs before the database's global transaction ID counter approaches its 32-bit limit. This can be caused by misconfigured <code>autovacuum</code> settings, tables with `autovacuum` disabled, or, most commonly, long-running transactions (e.g., open transactions from applications, replication slots, or logical decoding) that hold back the XID horizon, preventing older tuples from being vacuumed and frozen across critical tables.",
    "bad_code": "```sql\n-- Example of a long-running transaction blocking autovacuum\n-- This could be from an application, a misconfigured reporting tool, etc.\nBEGIN;\n\n-- Perform some query (or just hold the transaction open indefinitely)\nSELECT COUNT(*) FROM very_large_table;\n\n-- ... (long delay, potentially hours or days)\n\n-- This transaction holds back the 'oldest XID' visible to autovacuum.\n-- Autovacuum cannot freeze tuples older than this transaction's start XID.\n-- If not committed/rolled back, it will eventually cause a wraparound crisis.\n-- DO NOT RUN IN PRODUCTION WITHOUT UNDERSTANDING THE CONSEQUENCES.\n-- SELECT pg_sleep(3600); -- Simulates holding a transaction open for an hour\n\n-- END; -- Must be committed or rolled back to release the XID\n\n-- Potentially problematic autovacuum configuration (not recommended for production)\nALTER TABLE your_critical_table SET (autovacuum_enabled = off);\n-- Or globally:\n-- ALTER SYSTEM SET autovacuum = off;\n```",
    "solution_desc": "Proactive monitoring of XID age is paramount. Identify and resolve long-running transactions promptly using <code>pg_stat_activity</code>. Ensure <code>autovacuum</code> is enabled globally and for all critical tables, and its settings (<code>autovacuum_max_workers</code>, <code>autovacuum_vacuum_cost_delay</code>, <code>autovacuum_freeze_max_age</code>) are tuned for your workload. In an emergency, increase <code>autovacuum</code> aggressiveness (lower cost delay, more workers) or manually run <code>VACUUM FREEZE VERBOSE</code> on the tables with the highest XID age. Identify and eliminate any `ROLLBACK TO SAVEPOINT` usage which can cause XID generation even for rolled back subtransactions. If using replication, ensure replica lag does not prevent `autovacuum` from advancing on the primary due to `hot_standby_feedback` or `wal_sender` processes holding open old XIDs.",
    "good_code": "```sql\n-- 1. Monitor XID age for all tables (the age() function returns the number of transactions since relfrozenxid)\nSELECT\n    relname AS table_name,\n    age(relfrozenxid) AS xid_age_from_relfrozenxid,\n    pg_size_pretty(pg_relation_size(oid)) AS table_size,\n    'WARNING' AS status\nFROM pg_class\nWHERE relkind = 'r' AND age(relfrozenxid) > 1000000000 -- Adjust threshold as needed, often (autovacuum_freeze_max_age / 2)\nORDER BY xid_age_from_relfrozenxid DESC;\n\n-- 2. Identify long-running transactions that might be blocking autovacuum\nSELECT pid, usename, client_addr, application_name, backend_start, xact_start, state, query\nFROM pg_stat_activity\nWHERE state IN ('active', 'idle in transaction')\nORDER BY xact_start ASC;\n\n-- 3. Emergency manual vacuum freeze on a critical table (if necessary, after identifying problematic tables)\n-- This is an aggressive operation and can be resource intensive.\nVACUUM (FREEZE, VERBOSE, ANALYZE) your_table_name;\n\n-- 4. Check global autovacuum settings\nSHOW autovacuum;\nSHOW autovacuum_max_workers;\nSHOW autovacuum_vacuum_cost_delay;\nSHOW autovacuum_freeze_max_age;\n```",
    "verification": "Continuously monitor the `xid_age` of tables (from `pg_class` and `pg_database`) using the queries provided. Set up alerts for `xid_age` exceeding a safe threshold (e.g., 50% of `autovacuum_freeze_max_age`). Verify in PostgreSQL logs that `autovacuum` is running regularly and completing its tasks without errors. Ensure that no long-running transactions are consistently holding back the oldest XID. Post-fix, the `xid_age` values for all tables should stabilize at healthy levels, and emergency autovacuum warnings should disappear from the logs.",
    "date": "2026-06-09",
    "id": 1781006082,
    "type": "error"
});