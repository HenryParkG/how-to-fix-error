window.onPostDataLoaded({
    "title": "PostgreSQL Transaction ID Wraparound Prevention & Recovery",
    "slug": "postgresql-transaction-id-wraparound-prevention-recovery",
    "language": "PostgreSQL",
    "code": "Transaction ID Wraparound",
    "tags": [
        "SQL",
        "Infra",
        "Database",
        "AWS",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL's Multi-Version Concurrency Control (MVCC) relies on Transaction IDs (XIDs) to manage data visibility and ensure transactional integrity. Each transaction receives a unique 32-bit XID. Due to this finite nature, the XIDs will eventually 'wrap around' back to 0. To prevent data corruption, PostgreSQL uses a 'freezing' mechanism where old, committed tuples are marked as visible to all future transactions, effectively removing their dependency on XIDs. The <code>autovacuum</code> daemon plays a critical role in performing this freezing and general maintenance (cleaning dead tuples).</p><p>If <code>autovacuum</code> is unable to keep up with the transaction rate, or if there are long-running transactions preventing vacuum from freezing old XIDs, the database can approach the XID wraparound limit (approximately 2 billion transactions). Once this limit is critically close, PostgreSQL will shut down to prevent data loss. Recovery from such a shutdown can be a complex and time-consuming process, requiring special single-user mode operations and potentially significant downtime.</p>",
    "root_cause": "<ul><li><b>Insufficient Autovacuum:</b> The <code>autovacuum</code> daemon is not running frequently enough or is misconfigured, preventing it from processing old transactions and freezing tuples.</li><li><b>Long-Running Transactions:</b> Active transactions (e.g., from an application or a long-running query) hold open XID horizons, preventing <code>autovacuum</code> from freezing tuples that are older than the oldest active transaction.</li><li><b>High Transaction Rate:</b> An extremely high transaction rate, combined with inadequate <code>autovacuum</code> settings, can outpace the freezing process.</li><li><b>Incorrect <code>vacuum_freeze_min_age</code>:</b> While less common, an incorrectly large <code>vacuum_freeze_min_age</code> could delay freezing.</li></ul>",
    "bad_code": "No specific 'bad code' snippet in the traditional sense, as this is a configuration/operational issue rather than application code. The 'bad configuration' often manifests as:</p><pre><code># In postgresql.conf\nautovacuum = off             # DANGER: Disabling autovacuum is a common mistake\nautovacuum_vacuum_cost_delay = 100ms # Default might be too high for busy systems\nautovacuum_vacuum_scale_factor = 0.2 # Default might be too low, leading to infrequent vacuums\n</code></pre><p>Or application code that frequently creates long-running transactions without committing them in a timely manner, for example, an ETL process that opens a transaction and keeps it open for hours while processing large datasets.</p>",
    "solution_desc": "Proactive prevention is key. Regularly monitor the age of your database's oldest unfrozen transaction using the <code>age()</code> function on <code>pg_database.datfrozenxid</code> and <code>pg_class.relfrozenxid</code>. Configure <code>autovacuum</code> parameters (`autovacuum_vacuum_cost_delay`, `autovacuum_vacuum_scale_factor`, `autovacuum_freeze_max_age`) to match your workload. Identify and terminate long-running transactions promptly. If approaching the wraparound limit, perform manual `VACUUM FREEZE VERBOSE` on critical tables. In a recovery scenario (after database shutdown due to wraparound), the primary method involves restarting the database in single-user mode and manually running `VACUUM` on all affected tables. This is often followed by a full `REINDEX` and potentially a `pg_upgrade` if the issue was related to an older PostgreSQL version's default settings.",
    "good_code": "```sql\n-- 1. Monitor database-wide XID age:\nSELECT datname, age(datfrozenxid) AS xid_age, datfrozenxid FROM pg_database ORDER BY xid_age DESC;\n\n-- Recommended thresholds for xid_age:\n--   ~500 million: Warning, investigate autovacuum issues.\n--   ~1 billion: Critical, immediate action needed.\n--   ~2 billion: Database will shutdown.\n\n-- 2. Monitor table-level XID age (for tables nearing wraparound):\nSELECT relname, relkind, age(relfrozenxid) AS xid_age\nFROM pg_class\nWHERE relkind IN ('r', 'matview')\nORDER BY xid_age DESC\nLIMIT 10;\n\n-- 3. Identify long-running transactions that might be blocking autovacuum:\nSELECT pid, usename, client_addr, backend_start, state, query_start, query\nFROM pg_stat_activity\nWHERE state IN ('active', 'idle in transaction')\nORDER BY query_start ASC;\n\n-- 4. Manual Vacuum Freeze (emergency measure or for specific problematic tables):\n-- Replace 'your_table_name' with the actual table name.\nVACUUM FREEZE VERBOSE ANALYZE your_table_name;\n\n-- 5. Recommended postgresql.conf settings (adjust based on workload):\n-- autovacuum = on                   # Ensure autovacuum is enabled\n-- autovacuum_max_workers = 3        # Number of autovacuum processes\n-- autovacuum_naptime = 1min         # How often autovacuum checks\n-- autovacuum_vacuum_cost_delay = 10ms # Lower this for faster vacuuming on busy systems\n-- autovacuum_vacuum_cost_limit = -1 # No limit (or tune for your system)\n-- autovacuum_freeze_max_age = 200000000 # Default is usually good, but can be adjusted slightly\n```",
    "verification": "After implementing changes, continuously monitor the XID age of your databases and tables using the provided SQL queries. Ensure that the `xid_age` values are not steadily increasing towards the critical limits. Observe `pg_stat_activity` for any persistent long-running transactions that could block `autovacuum`. Check PostgreSQL logs for `autovacuum` activity and any warnings about XID age. Over time, the XID age should stabilize or decrease, indicating that `autovacuum` is effectively managing the transaction ID space.",
    "date": "2026-06-18",
    "id": 1781785094,
    "type": "error"
});