window.onPostDataLoaded({
    "title": "Spark Netty OOMs and Off-Heap Leaks in Large Shuffles",
    "slug": "spark-netty-ooms-off-heap-memory-leaks-shuffles",
    "language": "Apache Spark",
    "code": "OOM / Off-Heap Leak",
    "tags": [
        "Apache Spark",
        "JVM",
        "Java",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Apache Spark relies heavily on its network layer for shuffling data between executors, broadcasting variables, and fetching intermediate shuffle outputs. Netty, a high-performance asynchronous event-driven network application framework, powers this communication. While Netty is efficient, large-scale Spark shuffles or misconfigurations can lead to 'Off-Heap Memory' OutOfMemory (OOM) errors, especially when dealing with a high volume of data or many concurrent tasks. These OOMs occur not within the JVM heap but in direct memory buffers managed by Netty, which are allocated outside the garbage-collected heap.</p><p>The issue often manifests as executors crashing with errors like 'java.lang.OutOfMemoryError: Direct buffer memory' or 'Failed to allocate X bytes of direct memory'. This typically indicates that Netty has exhausted the available off-heap memory, preventing further network operations.</p>",
    "root_cause": "Netty uses direct memory (off-heap) for its network buffers to avoid JVM garbage collection overhead and enable zero-copy operations. The primary causes of off-heap OOMs include:\n1.  **Insufficient `spark.memory.offHeap.size`**: The allocated off-heap memory pool for Spark (if enabled) is too small.\n2.  **Excessive Concurrent Shuffles**: Too many tasks concurrently reading or writing shuffle data, overwhelming Netty's buffer pool.\n3.  **Slow Consumption**: Data is being received faster than it can be processed by the Spark application logic, leading to network buffers backing up.\n4.  **Large Shuffle Blocks**: Individual shuffle blocks are too large, requiring more direct memory than available for buffering.\n5.  **Default `spark.network.netty.receive.bufferSize`**: The default buffer size may be too small or too large for specific workloads, leading to inefficient memory usage or rapid exhaustion.",
    "bad_code": "```java\n// While not 'bad code' in the traditional sense, this represents a Spark\n// job that performs a large shuffle with default or insufficient configurations.\n// The following configuration implicitly relies on default settings for off-heap\n// memory, which might be inadequate for large data volumes or high concurrency.\n\nimport org.apache.spark.sql.SparkSession;\nimport org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\n\npublic class LargeShuffleJob {\n    public static void main(String[] args) {\n        SparkSession spark = SparkSession.builder()\n                .appName(\"LargeShuffleJob\")\n                .master(\"yarn\") // Or \"kubernetes\" or \"local[*]\"; often problematic on distributed clusters\n                // Missing explicit off-heap memory configurations like:\n                // .config(\"spark.memory.offHeap.enabled\", \"true\")\n                // .config(\"spark.memory.offHeap.size\", \"10g\")\n                // .config(\"spark.network.netty.receive.bufferSize\", \"1m\")\n                .getOrCreate();\n\n        int numRecords = 100_000_000;\n        int numPartitions = 1000;\n\n        Dataset<Row> df = spark.range(numRecords)\n                .repartition(numPartitions) // This triggers a large shuffle\n                .withColumnRenamed(\"id\", \"key\");\n\n        // Simulate a wide transformation that requires shuffling\n        df.groupBy(\"key\").count().show();\n\n        spark.stop();\n    }\n}\n```",
    "solution_desc": "Mitigating Netty OOMs involves a combination of Spark configuration tuning and understanding your workload:\n1.  **Enable and Configure Off-Heap Memory**: Explicitly enable off-heap memory for Spark and allocate sufficient size using `spark.memory.offHeap.enabled=true` and `spark.memory.offHeap.size=<size>` (e.g., `10g`). This should be balanced with container memory limits.\n2.  **Tune Netty Buffer Size**: Adjust `spark.network.netty.receive.bufferSize` (e.g., `1m` or `2m`). Larger buffers can reduce context switching but consume more memory per connection. Smaller buffers can fragment memory more.\n3.  **Optimize Shuffle Partitions**: Set `spark.sql.shuffle.partitions` appropriately. Too many small partitions increase metadata overhead; too few large partitions can lead to huge shuffle blocks and memory pressure.\n4.  **Increase Executor Memory**: Ensure executors have enough total memory (heap + off-heap) configured via `spark.executor.memory`.\n5.  **Reduce Shuffle Data**: Optimize your Spark jobs to minimize the data shuffled (e.g., filter early, use broadcast joins, `coalesce` before expensive shuffles).\n6.  **JVM Direct Memory Limits**: If `spark.memory.offHeap.enabled` is false, Netty still uses direct memory limited by `-XX:MaxDirectMemorySize`. Ensure this JVM option, if set explicitly, is sufficient. Spark will try to set it automatically when off-heap is enabled.\n7.  **Resource Limits**: Ensure your container orchestration platform (YARN, Kubernetes) allocates enough memory to executors, considering both heap and off-heap usage.",
    "good_code": "```java\n// Recommended Spark configurations in spark-defaults.conf or SparkSession.builder()\n// These settings should be tailored to your cluster resources and workload.\n\nimport org.apache.spark.sql.SparkSession;\nimport org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\n\npublic class LargeShuffleJobFixed {\n    public static void main(String[] args) {\n        SparkSession spark = SparkSession.builder()\n                .appName(\"LargeShuffleJobFixed\")\n                .master(\"yarn\")\n                .config(\"spark.memory.offHeap.enabled\", \"true\")       // Enable off-heap memory\n                .config(\"spark.memory.offHeap.size\", \"10g\")           // Allocate 10GB for off-heap\n                .config(\"spark.executor.memory\", \"20g\")               // Total executor memory (e.g., 10GB for heap + 10GB for off-heap)\n                .config(\"spark.network.netty.receive.bufferSize\", \"2m\") // Increase Netty receive buffer\n                .config(\"spark.shuffle.service.enabled\", \"true\")    // Use external shuffle service for stability\n                .config(\"spark.shuffle.file.buffer\", \"1m\")           // Buffer for shuffle writes\n                .config(\"spark.sql.shuffle.partitions\", \"2000\")       // Adjust shuffle partitions for better distribution\n                .getOrCreate();\n\n        int numRecords = 100_000_000;\n\n        Dataset<Row> df = spark.range(numRecords)\n                .repartition(spark.conf().get(\"spark.sql.shuffle.partitions\", \"200\")) // Use configured partitions\n                .withColumnRenamed(\"id\", \"key\");\n\n        df.groupBy(\"key\").count().show();\n\n        spark.stop();\n    }\n}\n```",
    "verification": "To verify the fix:\n1.  **Monitor Spark UI**: Observe the 'Executors' tab for memory usage (storage, task, and off-heap) and check for successful task completion without OOMs.\n2.  **Cluster Manager Logs**: Check YARN, Mesos, or Kubernetes logs for executor stdout/stderr, specifically looking for `OutOfMemoryError: Direct buffer memory` messages.\n3.  **JVM Monitoring**: Use tools like `jcmd <pid> VM.native_memory` on an executor process to inspect direct memory usage in real-time. This can reveal if direct buffers are accumulating.\n4.  **Prometheus/Grafana Metrics**: If integrated, monitor JVM direct memory usage and network I/O metrics for your Spark executors. Look for stable memory usage patterns during large shuffles.\n5.  **Reproduce and Scale**: Run your problematic job with the new configurations and gradually scale up data volume or concurrency to ensure the fix holds under stress.",
    "date": "2026-06-16",
    "id": 1781616974,
    "type": "error"
});