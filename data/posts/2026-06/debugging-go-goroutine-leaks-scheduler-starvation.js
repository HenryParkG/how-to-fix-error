window.onPostDataLoaded({
    "title": "Debugging Go Goroutine Leaks & Scheduler Starvation",
    "slug": "debugging-go-goroutine-leaks-scheduler-starvation",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Go",
        "Concurrency",
        "Debugging",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks occur when goroutines are started but never terminate, holding onto memory and resources indefinitely. While goroutines are lightweight, an accumulation of leaked goroutines can lead to significant memory consumption, eventually resulting in out-of-memory (OOM) errors or excessive garbage collection cycles that degrade application performance. Leaks often manifest subtly, as the application might continue to function, but with a steadily increasing memory footprint.</p><p>Scheduler starvation, often a consequence or a related symptom, happens when certain goroutines consume disproportionate CPU time or become stuck in tight loops or blocked operations without yielding. This prevents the Go scheduler from allocating CPU time to other ready goroutines, leading to application unresponsiveness, increased latency, and a perceived 'hang' even if the system has available CPU capacity. Identifying the specific goroutine causing starvation can be challenging, as it might involve analyzing runtime behavior rather than just memory profiles.</p>",
    "root_cause": "Common causes include forgetting to close channels, sending to a channel that no goroutine is reading from, receiving from a channel that no goroutine is sending to, or initiating long-running I/O operations without proper timeouts or cancellation mechanisms. External resources like database connections or network sockets not being properly released can also contribute, blocking goroutines indefinitely.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"time\"\n)\n\n// simulateWork simulates a task that might get stuck\nfunc simulateWork(taskID int, data chan int) {\n\tselect {\n\tcase d := <-data:\n\t\tfmt.Printf(\"Goroutine %d processed data %d\\n\", taskID, d)\n\t\t// Imagine some long-running work here\n\tcase <-time.After(10 * time.Minute):\n\t\t// This timeout is too long, effectively a leak if data is never sent\n\t\tfmt.Printf(\"Goroutine %d timed out (bad)\\n\", taskID)\n\t}\n}\n\nfunc main() {\n\tdata := make(chan int)\n\tfor i := 0; i < 5; i++ {\n\t\tgo simulateWork(i, data)\n\t}\n\n\t// Only one piece of data is sent, but 5 goroutines are waiting\n\t// The other 4 will leak until a very long timeout or program exit.\n\tdata <- 100\n\n\t// To observe the leak, we need to keep main alive\n\tfmt.Println(\"Main goroutine sleeping. Observe memory/goroutines with pprof.\")\n\ttime.Sleep(20 * time.Second)\n\t// The 'data' channel is never closed, nor are all goroutines explicitly cancelled.\n}\n```",
    "solution_desc": "To prevent goroutine leaks and scheduler starvation, prioritize robust concurrency patterns. Use `context.Context` with deadlines or cancellation signals to propagate timeouts and cancel operations across goroutine boundaries. Always ensure that channels have a corresponding sender and receiver, or use `select` statements with `default` cases or timeouts to prevent blocking indefinitely. When dealing with external resources, use `defer` to ensure timely release and `sync.WaitGroup` to track and wait for goroutine completion. Implement proper error handling and resource cleanup within goroutines.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"time\"\n)\n\n// simulateWorkFixed simulates a task that respects context cancellation\nfunc simulateWorkFixed(ctx context.Context, taskID int, data chan int, wg *sync.WaitGroup) {\n\tdefer wg.Done()\n\tselect {\n\tcase d := <-data:\n\t\tfmt.Printf(\"Goroutine %d processed data %d\\n\", taskID, d)\n\t\t// Simulate some work, ensure it checks context if long-running\n\t\tselect {\n\t\tcase <-time.After(50 * time.Millisecond):\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Printf(\"Goroutine %d cancelled during work.\\n\", taskID)\n\t\t\treturn\n\t\t}\n\tcase <-ctx.Done():\n\t\tfmt.Printf(\"Goroutine %d cancelled before processing.\\n\", taskID)\n\t\treturn\n\t}\n\tfmt.Printf(\"Goroutine %d finished.\\n\", taskID)\n}\n\nfunc main() {\n\tvar wg sync.WaitGroup\n\tdata := make(chan int)\n\n\t// Create a context that will cancel all goroutines after 5 seconds\n\tctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)\n\tdefer cancel()\n\n\tfor i := 0; i < 5; i++ {\n\t\twg.Add(1)\n\t\tgo simulateWorkFixed(ctx, i, data, &wg)\n\t}\n\n\t// Send one piece of data\n\tdata <- 100\n\n\t// Give some time for work and cancellation\n\t// In a real app, you'd close the channel after all sends are done\n\t// and then wait for goroutines.\n\tclose(data) // Signal that no more data will be sent\n\n\twg.Wait() // Wait for all goroutines to finish (or be cancelled)\n\n\tfmt.Println(\"Main goroutine finished. All goroutines should have exited.\")\n\n\t// Short sleep to allow cancellation to propagate fully if needed\n\ttime.Sleep(1 * time.Second)\n}\n```",
    "verification": "Use Go's built-in `pprof` tool. Start your application with `import _ \"net/http/pprof\"` and listen on a port. Access `http://localhost:6060/debug/pprof/goroutine?debug=1` to view the stack traces of all live goroutines. Look for goroutines that are blocked indefinitely (e.g., `chan receive`, `select` without a `default` or `context` case) or those that have not terminated as expected. Monitor the number of goroutines over time; a continuously increasing count often indicates a leak. Memory profiles (`/debug/pprof/heap`) can also reveal memory not being released, indirectly pointing to leaked goroutines holding references.",
    "date": "2026-06-29",
    "id": 1782700611,
    "type": "error"
});