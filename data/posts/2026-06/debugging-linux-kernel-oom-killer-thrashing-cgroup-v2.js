window.onPostDataLoaded({
    "title": "Debugging Linux Kernel OOM Killer Thrashing",
    "slug": "debugging-linux-kernel-oom-killer-thrashing-cgroup-v2",
    "language": "Linux Kernel, cgroup v2",
    "code": "OOM Thrashing",
    "tags": [
        "Linux",
        "cgroup",
        "OOM Killer",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>The Linux Out-Of-Memory (OOM) killer is a vital mechanism to prevent system instability when memory resources are exhausted. However, when an application consistently exceeds its allocated memory limits within a cgroup v2 hierarchy, it can lead to 'OOM killer thrashing'. This scenario involves the OOM killer repeatedly terminating processes as soon as they restart, creating a cycle of crashes, restarts, and re-terminations, severely impacting service availability.</p><p>cgroup v2, with its unified hierarchy and improved memory accounting, provides granular control over resource allocation. When <code>memory.max</code> (the hard memory limit) or <code>memory.high</code> (a soft limit that triggers throttling before <code>memory.max</code> is hit) are exceeded, the kernel invokes the OOM killer. The thrashing occurs when the application's true memory footprint or peak usage consistently surpasses these limits, and the application's startup or operational phase quickly breaches the threshold again after a restart.</p><p>Understanding Pressure Stall Information (PSI) is crucial here. PSI reports how much time processes spend waiting for CPU, memory, or I/O resources, providing a system-wide or cgroup-specific view of resource contention. High memory pressure PSI values within a cgroup often precede OOM events and indicate that processes are frequently waiting for memory, contributing to slow performance before the eventual OOM kill.</p>",
    "root_cause": "The primary root cause is an application's actual memory consumption consistently exceeding the <code>memory.max</code> limit set for its cgroup v2, often due to memory leaks, inefficient data structures, or under-provisioned memory limits. This can be exacerbated by: 1) Lack of swap space or disabled swap within the cgroup, preventing the kernel from offloading less-used memory pages. 2) Incorrect memory accounting by the application or monitoring tools that don't capture the full kernel-level memory footprint (e.g., shared memory, page cache). 3) A sudden surge in workload that pushes memory usage beyond the static limit. The OOM killer acts as a symptom, not the cause, of this fundamental resource contention.",
    "bad_code": "Example of a cgroup v2 configuration that might lead to thrashing if the application's baseline memory is higher than 2GB, or if there are periodic spikes.\n\n```bash\n# Assume 'my_app' is a new cgroup in the memory controller\nmkdir /sys/fs/cgroup/my_app\n\n# Set a hard memory limit of 2GB\necho 2G > /sys/fs/cgroup/my_app/memory.max\n\n# Move application PID into the cgroup\necho <APP_PID> > /sys/fs/cgroup/my_app/cgroup.procs\n```\n\nOr in a Kubernetes pod definition where memory limits are too restrictive:\n\n```yaml\n# kubernetes-pod-oom.yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-memory-hog\nspec:\n  containers:\n  - name: my-app\n    image: my-app-image:latest\n    resources:\n      limits:\n        memory: \"2Gi\" # Too low for the application's actual needs\n      requests:\n        memory: \"1Gi\"\n```",
    "solution_desc": "To resolve OOM killer thrashing, a multi-pronged approach is required. First, accurately profile the application's memory usage under typical and peak loads using tools like <code>valgrind</code>, <code>jemalloc</code>, <code>perf mem</code>, or application-specific metrics. Adjust the cgroup v2 <code>memory.max</code> limit to accommodate the actual peak memory footprint with a reasonable buffer. Consider using <code>memory.high</code> to throttle processes before the hard limit, giving the kernel time to reclaim memory or swap out pages gracefully. If applicable, enable and configure <code>memory.swap.max</code> to allow some memory to be swapped to disk, preventing immediate OOM kills. Implement proactive monitoring of PSI (Pressure Stall Information) at both the system and cgroup level to detect memory pressure before it escalates to an OOM event. Finally, review application code for potential memory leaks or inefficient memory patterns.",
    "good_code": "Adjusted cgroup v2 configuration to provide more realistic memory limits and utilize swap:\n\n```bash\n# Assume 'my_app' cgroup exists\nmkdir /sys/fs/cgroup/my_app\n\n# Set a more generous hard memory limit, e.g., 4GB based on profiling\necho 4G > /sys/fs/cgroup/my_app/memory.max\n\n# Set a high threshold for throttling at 3.5GB to apply pressure earlier\necho 3.5G > /sys/fs/cgroup/my_app/memory.high\n\n# Allow 1GB of swap for the cgroup (if system swap is enabled)\necho 1G > /sys/fs/cgroup/my_app/memory.swap.max\n\n# Move application PID into the cgroup\necho <APP_PID> > /sys/fs/cgroup/my_app/cgroup.procs\n```\n\nFor Kubernetes, adjust resource limits:\n\n```yaml\n# kubernetes-pod-fixed.yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-memory-hog\nspec:\n  containers:\n  - name: my-app\n    image: my-app-image:latest\n    resources:\n      limits:\n        memory: \"4Gi\" # Adjusted based on profiling\n      requests:\n        memory: \"2Gi\"\n```",
    "verification": "After implementing the fix, monitor the system and application behavior carefully. Check <code>dmesg | grep -i oom</code> for any new OOM killer entries. Observe the <code>memory.current</code>, <code>memory.peak</code>, and PSI metrics (<code>/sys/fs/cgroup/my_app/memory.stat</code> and <code>/sys/fs/cgroup/my_app/memory.pressure</code>) to ensure memory usage is stable and within limits. Application logs should show uninterrupted execution, and system-level memory pressure should decrease. Tools like <code>atop</code>, <code>htop</code>, or Prometheus/Grafana dashboards can provide real-time visibility into cgroup memory consumption and OOM events.",
    "date": "2026-06-11",
    "id": 1781181739,
    "type": "error"
});