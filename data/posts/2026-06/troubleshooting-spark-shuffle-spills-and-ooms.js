window.onPostDataLoaded({
    "title": "Troubleshooting Spark Shuffle Spills & OOMs",
    "slug": "troubleshooting-spark-shuffle-spills-and-ooms",
    "language": "Spark",
    "code": "SparkOOM",
    "tags": [
        "Java",
        "Big Data",
        "Apache Spark",
        "Infra",
        "Performance",
        "Error Fix"
    ],
    "analysis": "<p>Spark shuffle is the mechanism for redistributing data across partitions, typically required by 'wide transformations' such as <code>groupByKey</code>, <code>reduceByKey</code>, <code>join</code>, or <code>repartition</code>. During a shuffle, data from different partitions on different executors needs to be exchanged, which involves serialization, network transfer, and deserialization. When the amount of data to be shuffled exceeds the allocated memory buffers on an executor, Spark starts spilling this data to local disk. Excessive shuffling and spilling can drastically degrade performance due to slow disk I/O.</p><p>Shuffle spills are often a precursor to Out-Of-Memory (OOM) errors. When an executor runs out of memory, it can be for several reasons related to shuffle: the incoming shuffle data for a task is too large to fit in memory, the results of a transformation (e.g., a huge hash map for <code>groupByKey</code>) exceed memory limits, or memory pressure from other operations (e.g., broadcast variables, caching) leaves insufficient room for shuffle operations. OOMs lead to task failures, retries, and potentially job failures, costing significant compute resources and time.</p>",
    "root_cause": "The specific technical reason for shuffle spills and OOMs typically stems from data skew (uneven data distribution across partitions), excessively large records/objects, insufficient executor memory configuration, inefficient data serialization, or using wide transformations on very large datasets without proper optimization. Data skew is particularly problematic as a few 'hot' partitions can overwhelm single executors, leading to massive spills and OOMs, even if the overall dataset size seems manageable.",
    "bad_code": "```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col\n\nspark = SparkSession.builder \\\n    .appName(\"SparkShuffleOOMDemo\") \\\n    .config(\"spark.executor.memory\", \"2g\") \\\n    .config(\"spark.executor.cores\", \"1\") \\\n    .getOrCreate()\n\n# Simulate a large, skewed dataset\n# In a real scenario, this would come from a data source\ndata = []\nfor i in range(1000000): # 1 million records\n    if i % 100 == 0: # 1% of data goes to a 'hot' key\n        data.append((i % 10, 'hot_key_value_' + str(i)))\n    else:\n        data.append((i % 99, 'cold_key_value_' + str(i)))\n\n# Create a DataFrame with a column that will cause skew\ndf = spark.createDataFrame(data, ['key', 'value'])\n\n# Bad practice: Using groupByKey directly on RDD, or very wide join on skewed key\n# For DataFrames, a window function or a join on a skewed key could also cause issues.\n# This groupBy operation will likely cause spills and OOMs due to the 'hot_key_value'\n# concentrating a large amount of data on a single partition.\nprint(\"Starting a wide transformation (groupBy) on potentially skewed data...\")\n# For DataFrame, this would be `df.groupBy(\"key\").agg(...)`\n# For illustration with RDD to emphasize the raw data movement:\nresult_rdd = df.rdd.groupBy(lambda row: row.key).mapValues(list) # Aggregates all values into a list\n\n# Trigger action to compute and potentially fail\nresult_rdd.count()\n\nprint(\"Job completed (or failed with OOM/spills). Check Spark UI for metrics.\")\n\nspark.stop()\n```",
    "solution_desc": "<p>Addressing Spark shuffle spills and OOMs requires a multi-faceted approach focused on data, configuration, and code optimization. Architecturally, the first step is to identify and mitigate data skew. Techniques include salting (adding a random prefix/suffix to skewed keys before shuffle and then stripping it), broadcasting small tables in joins, or using an `optimistic` skewed join strategy (Spark 3.0+). Configuration-wise, increasing executor memory (<code>spark.executor.memory</code>) and tuning shuffle-specific parameters (<code>spark.shuffle.memoryFraction</code>, <code>spark.memory.offHeap.size</code> for off-heap memory) can provide more breathing room. Also, increasing the number of shuffle partitions (<code>spark.sql.shuffle.partitions</code>) helps distribute data more widely, reducing the load on individual tasks.</p><p>From a code perspective, prefer `reduceByKey` over `groupByKey` when possible, as `reduceByKey` performs partial aggregation on map-side, significantly reducing data shuffled. Use `DataFrame` APIs (e.g., `agg`, `join`) which often have better internal optimizations than raw RDD operations. Consider `repartition` or `coalesce` strategically to control the number of partitions before wide transformations. Choose efficient serialization formats (e.g., Kryo, Parquet) over Java serialization. Lastly, ensure that intermediate data is not accidentally materialized or cached unnecessarily.</p>",
    "good_code": "```python\nfrom pyspark.sql import SparkSession\nfrom pyspark.sql.functions import col, rand, lit, count, expr\n\nspark = SparkSession.builder \\\n    .appName(\"SparkShuffleOOMFix\") \\\n    .config(\"spark.executor.memory\", \"4g\") \\\n    .config(\"spark.executor.cores\", \"2\") \\\n    .config(\"spark.sql.shuffle.partitions\", \"500\") \\\n    .config(\"spark.default.parallelism\", \"500\") \\\n    .getOrCreate()\n\n# Simulate a large, skewed dataset\ndata = []\nfor i in range(1000000): # 1 million records\n    if i % 100 == 0:\n        data.append((i % 10, 'hot_key_value_' + str(i)))\n    else:\n        data.append((i % 99, 'cold_key_value_' + str(i)))\n\ndf = spark.createDataFrame(data, ['key', 'value'])\n\n# Step 1: Detect skew (e.g., via sampling or analyzing value counts)\n# This is a conceptual step; in practice, you might run a preliminary job or query.\n# df.groupBy(\"key\").agg(count(\"value\").alias(\"count\")).orderBy(col(\"count\").desc()).show()\n\n# Step 2: Implement Salting for skewed keys\n# Assume '10' is identified as a hot key\nnum_salt_buckets = 200 # Number of salt prefixes to distribute the hot key\n\n# Add salt to the hot key\nsalted_df = df.withColumn(\n    \"salted_key\",\n    expr(f\"CASE WHEN key = 10 THEN concat(key, '_', floor(rand() * {num_salt_buckets})) ELSE cast(key as string) END\")\n)\n\n# Repartition by the salted key to distribute hot keys across more partitions\nsalted_df = salted_df.repartitionByRange(\"salted_key\") # Or just repartition if range is not suitable\n\nprint(\"Starting a wide transformation (groupBy) on salted data...\")\n# Perform the aggregation on the salted key\n# After aggregation, if needed, you can remove the salt suffix.\nresult_df = salted_df.groupBy(\"salted_key\").agg(count(\"value\").alias(\"count\"))\n\n# If you need the original key back, you'd need another step to unsalt.\n# For example, by splitting the salted_key and casting back.\n\nresult_df.count() # Trigger action\n\nprint(\"Job completed with salting applied. Check Spark UI for reduced spills and balanced tasks.\")\n\nspark.stop()\n```",
    "verification": "<p>Verify the fix by monitoring the Spark UI during job execution. Look for key metrics such as:<ul><li><b>Executors tab:</b> Check for balanced task distribution across executors. Skew will show some executors running significantly more tasks or having much longer task durations.</li><li><b>Stages tab:</b> For shuffle stages, examine the 'Shuffle Write' and 'Shuffle Read' metrics. High 'Disk Bytes Spilled' indicates a problem. Ideally, this should be zero or very low. The 'Scheduler Delay' and 'Task Deserialization Time' should also decrease.</li><li><b>Event Timeline:</b> Observe task durations; they should be more consistent across tasks within a stage.</li><li><b>Memory Usage:</b> Check the 'Storage' and 'Task Memory' usage graphs for individual executors to ensure they are not consistently high or hitting limits.</li></ul>If OOMs were occurring, their absence is the primary indicator of success. Reduced spill counts and more evenly distributed task loads confirm that the shuffle process is more efficient.</p>",
    "date": "2026-06-19",
    "id": 1781838021,
    "type": "error"
});