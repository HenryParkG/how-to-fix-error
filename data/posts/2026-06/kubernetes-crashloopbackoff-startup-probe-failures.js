window.onPostDataLoaded({
    "title": "Kubernetes CrashLoopBackOff: Hidden Startup Probe Failures",
    "slug": "kubernetes-crashloopbackoff-startup-probe-failures",
    "language": "Kubernetes",
    "code": "CrashLoopBackOff",
    "tags": [
        "Kubernetes",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p><code>CrashLoopBackOff</code> is a common Kubernetes pod state indicating that a container repeatedly crashes after starting. While often attributed to application errors (e.g., unhandled exceptions, incorrect configuration files), a less obvious but frequent culprit can be misconfigured or overly aggressive startup, liveness, or readiness probes. Specifically, a healthy application might fail to initialize within the strict timelines imposed by these probes, leading Kubernetes to kill and restart the container before it ever gets a chance to become ready.</p><p>This cycle of crashing and restarting consumes cluster resources, causes service disruptions, and can mask the true underlying issue. Understanding how probes interact with application startup logic is crucial for effective debugging. A pod in <code>CrashLoopBackOff</code> usually means its primary container is exiting with a non-zero status code, but the *reason* for that exit can be external to the application code itself, driven by the orchestration layer's health checks.</p>",
    "root_cause": "The application fails to complete its initial startup phase (e.g., database connection, module loading) within the configured timeout or failure threshold of a Kubernetes startup or liveness probe, causing Kubernetes to terminate and restart the container.",
    "bad_code": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-app-pod\nspec:\n  containers:\n  - name: my-app\n    image: my-app:1.0.0\n    ports:\n    - containerPort: 8080\n    livenessProbe:\n      httpGet:\n        path: /healthz\n        port: 8080\n      initialDelaySeconds: 5\n      periodSeconds: 3\n      failureThreshold: 1\n      timeoutSeconds: 1\n```",
    "solution_desc": "The primary solution involves correctly configuring Kubernetes probes to align with the application's actual startup behavior. For applications with non-trivial startup times, the dedicated <code>startupProbe</code> should be leveraged. This probe delays the activation of <code>livenessProbe</code> and <code>readinessProbe</code> until the application signals it has started. Adjusting <code>initialDelaySeconds</code>, <code>periodSeconds</code>, <code>failureThreshold</code>, and <code>timeoutSeconds</code> parameters allows the application sufficient time to initialize. For simpler cases, extending the <code>initialDelaySeconds</code> of the <code>livenessProbe</code> might suffice. Additionally, ensuring the application has sufficient resources (CPU, Memory) to start quickly can mitigate probe failures.",
    "good_code": "```yaml\napiVersion: v1\nkind: Pod\nmetadata:\n  name: my-app-pod\nspec:\n  containers:\n  - name: my-app\n    image: my-app:1.0.0\n    ports:\n    - containerPort: 8080\n    startupProbe:\n      httpGet:\n        path: /startup\n        port: 8080\n      initialDelaySeconds: 0\n      periodSeconds: 5\n      failureThreshold: 12 # Allow up to 60 seconds (12 * 5s) for startup\n      timeoutSeconds: 2\n    livenessProbe:\n      httpGet:\n        path: /healthz\n        port: 8080\n      periodSeconds: 10\n      failureThreshold: 3\n      timeoutSeconds: 5\n      # initialDelaySeconds is implicitly handled by startupProbe\n    readinessProbe:\n      httpGet:\n        path: /ready\n        port: 8080\n      periodSeconds: 5\n      failureThreshold: 1\n      timeoutSeconds: 3\n```",
    "verification": "After applying the fix, monitor the pod status using <code>kubectl get pods</code>. It should transition from <code>ContainerCreating</code> to <code>Running</code> without entering <code>CrashLoopBackOff</code>. Inspect pod logs with <code>kubectl logs <pod-name></code> to confirm the application starts successfully. Use <code>kubectl describe pod <pod-name></code> to check probe status and event logs for any related errors, ensuring the probes are now succeeding.",
    "date": "2026-06-25",
    "id": 1782387829,
    "type": "error"
});