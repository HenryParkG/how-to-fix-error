window.onPostDataLoaded({
    "title": "MongoDB WiredTiger Ticket Starvation & Cache Stalls",
    "slug": "fixing-mongodb-wiredtiger-ticket-starvation-cache-stalls",
    "language": "MongoDB, WiredTiger",
    "code": "TicketStarvation",
    "tags": [
        "MongoDB",
        "WiredTiger",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses a sophisticated concurrency control mechanism based on 'tickets' to manage concurrent read and write operations. Each database operation requires a read or write ticket to proceed, limiting the number of concurrent operations to prevent resource exhaustion and ensure data consistency. WiredTiger also manages its own cache (separate from OS file system cache) for data and indexes.</p><p>Ticket starvation occurs when the demand for read or write tickets significantly exceeds their available supply. This often manifests as operations waiting indefinitely for a ticket, leading to high latency and reduced throughput. Cache eviction stalls are related: when the WiredTiger cache is under memory pressure, it initiates eviction processes to free up space. These eviction processes themselves require resources and tickets. If there's a heavy workload simultaneously competing for tickets and memory, the eviction process can become slow or even block, exacerbating ticket starvation and causing overall database stalls. This vicious cycle is particularly common under sustained high write loads, complex aggregations, or large index builds in environments with insufficient RAM or poorly tuned WiredTiger configurations.</p>",
    "root_cause": "An imbalance between the number of available WiredTiger read/write tickets and the concurrent workload demand, often compounded by aggressive or slow cache eviction processes under memory pressure. This leads to operations waiting for tickets or I/O, causing high latency and system stalls.",
    "bad_code": "```javascript\n// This is not 'bad code' in the traditional sense, but represents a scenario\n// or misconfiguration leading to WiredTiger ticket starvation. \n// The default settings, combined with a high-concurrency, write-heavy workload,\n// can trigger this issue. No specific code snippet exists for this, rather\n// it's a configuration/workload problem.\n\n// Example of a default or insufficient configuration that could exacerbate issues:\n// (Typically found in /etc/mongod.conf or similar)\n// storage:\n//   dbPath: /var/lib/mongodb\n//   wiredTiger:\n//     engineConfig:\n//       cacheSizeGB: 10 # Potentially too small for the working set\n//       # Default values for read/write tickets might be 128/128 or 256/256.\n//       # In high-concurrency scenarios, these can be insufficient.\n//       # read_tickets: 128 \n//       # write_tickets: 128 \n\n// High-concurrency application code can then trigger the starvation:\n// const operations = [];\n// for (let i = 0; i < 1000; i++) {\n//   operations.push(db.collection('mydata').insertOne({ value: Math.random() }));\n// }\n// Promise.all(operations).then(() => console.log('All inserts attempted'));\n// If many such concurrent operations run, they'll contend for a limited number of tickets.\n```",
    "solution_desc": "<p>Addressing WiredTiger ticket starvation and cache eviction stalls requires a multi-faceted approach focusing on resource allocation, configuration tuning, and workload optimization:</p><ol><li><b>Increase WiredTiger Cache Size:</b> Ensure the <code>storage.wiredTiger.engineConfig.cacheSizeGB</code> is large enough to hold your working set (data and indexes frequently accessed). A larger cache reduces the frequency of eviction and disk I/O.</li><li><b>Tune WiredTiger Tickets:</b> Adjust the number of concurrent read and write tickets via <code>storage.wiredTiger.engineConfig.configString</code>. For example, add <code>'read_maximum=512,write_maximum=512'</code> to increase the defaults. Monitor <code>wiredTiger.concurrentTransactions</code> to identify the optimal values.</li><li><b>Optimize Queries and Indexes:</b> Inefficient queries or missing indexes can dramatically increase the I/O and CPU cost per operation, consuming tickets longer. Use <code>explain()</code> to identify slow queries and create appropriate indexes.</li><li><b>Sharding and Replica Set Scaling:</b> Distribute your workload across multiple mongod instances using sharding for horizontal scaling. For read-heavy workloads, adding more replica set members can offload reads.</li><li><b>Monitor Eviction and Checkpoints:</b> Keep an eye on <code>wiredTiger.cache.eviction*</code> metrics and <code>wiredTiger.log.logDataHash*</code> for checkpointing. Long checkpointing times can also contribute to stalls. Ensure sufficient I/O bandwidth for disk operations.</li><li><b>OS Configuration:</b> Use an appropriate I/O scheduler (e.g., `noop` or `deadline` for SSDs) and ensure `vm.swappiness` is set to 1 (or 0 if truly no swapping is desired) to prevent MongoDB data from being swapped out by the OS.</li></ol>",
    "good_code": "```yaml\n# /etc/mongod.conf - Example of an optimized WiredTiger configuration\nstorage:\n  dbPath: /var/lib/mongodb\n  journal:\n    enabled: true\n  wiredTiger:\n    engineConfig:\n      cacheSizeGB: 20 # Example: Increased cache size for a larger working set\n      # Increase read and write tickets to allow more concurrency\n      # These values should be tuned based on monitoring and workload characteristics\n      configString: 'read_maximum=512,write_maximum=512,eviction_dirty_trigger=75,eviction_trigger=90'\n      # eviction_dirty_trigger: Percentage of cache occupied by dirty pages at which eviction starts.\n      # eviction_trigger: Percentage of cache occupied at which eviction starts.\n    collectionConfig:\n      blockCompressor: zstd # Or snappy/zlib - choose based on compression ratio vs CPU tradeoff\n    indexConfig:\n      prefixCompression: true # Can reduce index size and memory usage\n\nreplication:\n  replSetName: rs0\n# ... other configurations like network, security, etc.\n```",
    "verification": "<p>Verifying the fix involves comprehensive monitoring of MongoDB's performance metrics and application-level latency:</p><ol><li><b><code>db.serverStatus()</code> Metrics:</b> Pay close attention to <code>wiredTiger.concurrentTransactions.readTickets.available</code> and <code>writeTickets.available</code>. If these consistently hover near 0, operations are still waiting. Post-fix, you should see higher available tickets. Also, monitor <code>wiredTiger.cache.trackedBytesInCache</code> and <code>wiredTiger.cache.bytesCurrentlyBeingEvicted</code>.</li><li><b><code>mongostat</code> and <code>mongotop</code>:</b> Use these utilities to get a real-time view of database operations, I/O, and CPU usage. Look for consistent operation rates and reduced `locked` time.</li><li><b>Application Monitoring:</b> Observe end-to-end application latency. A successful fix should result in lower and more consistent response times.</li><li><b>System-level Monitoring:</b> Monitor CPU, RAM, and I/O usage on the MongoDB server. Ensure memory utilization is within expected bounds and that I/O wait times are low. Check `dmesg` for any OOM (Out Of Memory) killer events.</li><li><b>MongoDB Atlas / Cloud Monitoring:</b> If using a managed service, leverage their advanced monitoring dashboards to track these metrics over time and identify improvements.</li></ol>",
    "date": "2026-06-16",
    "id": 1781578296,
    "type": "error"
});