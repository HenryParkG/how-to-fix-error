window.onPostDataLoaded({
    "title": "Next.js Hydration Mismatches & Client-Side Errors",
    "slug": "nextjs-hydration-mismatches-client-side-rendering-errors",
    "language": "Next.js",
    "code": "HydrationMismatch",
    "tags": [
        "Next.js",
        "React",
        "Frontend",
        "Error Fix"
    ],
    "analysis": "<p>Next.js applications leverage server-side rendering (SSR) or static site generation (SSG) to deliver HTML to the browser quickly, improving initial page load and SEO. This pre-rendered HTML is then \"hydrated\" on the client-side by React, attaching event listeners and making the application interactive. A \"hydration mismatch\" occurs when the React component tree rendered on the server differs from the component tree rendered on the client. This discrepancy can lead to warnings in the console (e.g., \"Warning: Prop `className` did not match...\"), broken interactivity, or even unexpected layout shifts and client-side rendering errors.</p><p>Common causes for hydration mismatches include: using browser-specific APIs (like `window` or `localStorage`) during server rendering, conditionally rendering components based on client-only state, incorrect handling of `Date` objects, or third-party libraries that manipulate the DOM outside of React's control. These issues often stem from a lack of strict separation between server-side and client-side execution contexts, where developers inadvertently rely on browser globals that are undefined during Node.js rendering, or vice-versa.</p>",
    "root_cause": "The core reason for hydration mismatches is the divergence between the HTML generated by React on the server and the HTML generated by React on the client. Specific causes include: 1. **Browser-specific APIs on the Server**: Accessing `window`, `document`, `localStorage` during SSR. 2. **Time-dependent Content**: Rendering dates/times that vary by server/client locale/timezone. 3. **Random IDs/Client-only State**: Generating random IDs or conditionally rendering based on state that only exists or changes on the client. 4. **Third-Party Libraries**: Libraries directly manipulating the DOM. 5. **Incorrect `dangerouslySetInnerHTML`**: Producing different HTML server-side vs. client-side. 6. **`useEffect` Misuse**: Relying on `useEffect` to produce the final UI without an initial server-friendly state.",
    "bad_code": "```jsx\n// components/BadComponent.jsx\nimport React, { useState, useEffect } from 'react';\n\nfunction BadComponent() {\n  const [isClient, setIsClient] = useState(false); // Client-only state\n\n  useEffect(() => {\n    setIsClient(true);\n  }, []);\n\n  // Directly using window on server will cause error/mismatch\n  const userAgent = typeof window !== 'undefined' ? window.navigator.userAgent : 'Server';\n\n  return (\n    <div>\n      <h1>Welcome!</h1>\n      {/* This paragraph might not render on the server, causing a mismatch */}\n      {isClient && <p>You are viewing this on the client!</p>}\n      <p>User Agent: {userAgent}</p>\n      <p>\n        Current Time (Server/Client dependent):{' '}\n        {/* new Date() can be inconsistent between server/client */}\n        {new Date().toLocaleTimeString()}\n      </p>\n    </div>\n  );\n}\n\nexport default BadComponent;\n```",
    "solution_desc": "To prevent Next.js hydration mismatches: 1. **Conditional Rendering for Client-only Content**: Use `useEffect` to set state after mount for client-specific UI. Initialize `useState` with server-friendly defaults. 2. **`typeof window` Checks**: Guard browser-specific API calls with `typeof window !== 'undefined'` or, preferably, move them into `useEffect`. 3. **Consistent Data**: Ensure data (especially dates or generated IDs) is consistent between server and client. Pass ISO strings for dates and format on the client. 4. **Dynamic Imports for Client-only Components**: Use `next/dynamic` with `ssr: false` for components heavily relying on browser APIs. 5. **Standardize Third-Party Libraries**: Ensure compatibility with SSR or dynamically import them with `ssr: false`.",
    "good_code": "```jsx\n// components/GoodComponent.jsx\nimport React, { useState, useEffect } from 'react';\nimport dynamic from 'next/dynamic';\n\n// Dynamically import a client-only component\nconst ClientOnlyParagraph = dynamic(() => Promise.resolve(() => (\n  <p>This paragraph is only rendered on the client!</p>\n)), { ssr: false });\n\nfunction GoodComponent({ initialTimestamp }) { \n  const [currentTime, setCurrentTime] = useState('');\n  const [userAgent, setUserAgent] = useState('Server'); // Default for server\n\n  useEffect(() => {\n    // This effect runs only on the client\n    setUserAgent(window.navigator.userAgent);\n    const date = new Date(initialTimestamp); \n    setCurrentTime(date.toLocaleTimeString()); // Format on client\n  }, [initialTimestamp]);\n\n  return (\n    <div>\n      <h1>Welcome!</h1>\n      <ClientOnlyParagraph /> {/* This component will only render on the client */}\n      <p>User Agent: {userAgent}</p>\n      <p>Current Time: {currentTime || new Date(initialTimestamp).toLocaleTimeString()}</p>\n    </div>\n  );\n}\n\nexport default GoodComponent;\n\n// In a Next.js page (e.g., pages/index.js)\nexport async function getServerSideProps() {\n  const initialTimestamp = Date.now(); // Data consistent for server and client\n  return {\n    props: {\n      initialTimestamp,\n    },\n  };\n}\n```",
    "verification": "1. **Browser Console**: Check for React hydration warnings (e.g., \"Warning: Prop `x` did not match...\"). A clean console indicates success. 2. **Inspect Element & View Page Source**: Compare the HTML from \"View Page Source\" (server-rendered) with the \"Inspect Element\" HTML (client-rendered). They should be identical where intended. 3. **Disable JavaScript**: Temporarily disable JavaScript in the browser. The initial page content and layout should still be correct. 4. **Cross-Browser Testing**: Verify consistency across different browsers. 5. **Component Interaction**: Confirm all interactive elements function correctly after page load, validating successful hydration.",
    "date": "2026-06-18",
    "id": 1781750726,
    "type": "error"
});