window.onPostDataLoaded({
    "title": "Fixing Elasticsearch Shard Relocation Storms & Imbalance",
    "slug": "elasticsearch-shard-relocation-storms-cluster-imbalance",
    "language": "Elasticsearch",
    "code": "Shard Relocation Storm",
    "tags": [
        "Elasticsearch",
        "Distributed System",
        "Cluster Management",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch shard relocation storms occur when a large number of shards simultaneously begin moving across nodes within a cluster. This phenomenon typically arises after significant changes, such as node failures, planned cluster upgrades, scaling operations (adding/removing nodes), or network partitions. While shard relocation is a fundamental mechanism for maintaining high availability and even distribution, an uncontrolled storm can severely degrade cluster performance due to increased network I/O, disk activity, and CPU usage on both source and target nodes, leading to slow queries, indexing failures, and even further node instability. It can also exacerbate cluster imbalance rather than fix it, especially if the underlying cause of the imbalance isn't addressed.</p><p>Cluster imbalance, where shards are not evenly distributed by count or disk usage across data nodes, often precedes or is a consequence of relocation storms. It can be caused by nodes having different capacities, uneven data ingestion patterns, or incorrect allocation settings that prevent Elasticsearch from achieving an optimal distribution.</p>",
    "root_cause": "The specific technical reason for shard relocation storms and cluster imbalance often stems from: 1) Rapid, unplanned node lifecycle events (e.g., multiple nodes failing simultaneously or being restarted without proper draining). 2) Aggressive or default shard allocation settings (e.g., `cluster.routing.allocation.cluster_concurrent_rebalance`) allowing too many concurrent relocations. 3) Insufficient disk space on nodes, triggering disk-based allocation rules that frantically move shards. 4) Network instability causing nodes to frequently join/leave the cluster. 5) Uncontrolled rebalancing after a large data ingestion or deletion event. 6) Incorrect `cluster.routing.allocation.disk.watermark` settings.",
    "bad_code": "```json\n# No direct 'bad code' snippet as this is an operational/configuration issue.\n# However, a cluster state indicative of a problem might involve:\n# 1. Default or aggressive shard allocation settings combined with node failures.\n# 2. Nodes reaching disk watermarks without proper preventative measures.\n#\n# Example of problematic settings (defaults or too high):\n# \"cluster.routing.allocation.cluster_concurrent_rebalance\": \"2\" (Too aggressive for large clusters)\n# \"cluster.routing.allocation.node_initial_primaries_recoveries\": \"4\" (Too many concurrent recoveries)\n# \"cluster.routing.allocation.node_concurrent_recoveries\": \"2\" (Too many concurrent recoveries)\n# \"cluster.routing.allocation.disk.threshold_enabled\": \"true\"\n# \"cluster.routing.allocation.disk.watermark.low\": \"85%\"\n# \"cluster.routing.allocation.disk.watermark.high\": \"90%\"\n# (These watermarks might be too tight for heavy write workloads)\n```",
    "solution_desc": "To fix shard relocation storms and cluster imbalance, a multi-faceted approach is required. First, temporarily disable shard allocation (`cluster.routing.allocation.enable: none`) during maintenance or recovery periods to prevent uncontrolled movements. Then, systematically bring nodes back online or address the root cause of imbalance. Adjust shard allocation settings to be more conservative: reduce `cluster.routing.allocation.cluster_concurrent_rebalance`, `node_initial_primaries_recoveries`, and `node_concurrent_recoveries` to manage the rate of relocation. Ensure sufficient disk space and properly configure disk watermarks to prevent nodes from prematurely ejecting shards. For planned node removals, use the `cluster.routing.allocation.exclude._name` or `_ip` settings to gracefully drain shards. Utilize the `_cluster/reroute` API for fine-grained manual control over shard movements if needed. Optimize index lifecycle management to prevent indices from growing unbounded and causing storage issues.",
    "good_code": "```json\n# Temporarily disable all shard allocation during critical operations (e.g., node restart)\nPUT _cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.enable\": \"none\"\n  }\n}\n\n# Re-enable allocation when stable, e.g., after a controlled restart\n# PUT _cluster/settings\n# {\n#   \"persistent\": {\n#     \"cluster.routing.allocation.enable\": \"all\"\n#   }\n# }\n\n# Configure more conservative shard relocation settings to prevent storms\nPUT _cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.cluster_concurrent_rebalance\": \"1\",    # Allow only 1 concurrent rebalance per cluster\n    \"cluster.routing.allocation.node_initial_primaries_recoveries\": \"2\", # Limit concurrent primary recoveries\n    \"cluster.routing.allocation.node_concurrent_recoveries\": \"1\",     # Limit concurrent replica recoveries\n    \"cluster.routing.allocation.disk.watermark.low\": \"90%\",          # Adjust low watermark (e.g., 90% full)\n    \"cluster.routing.allocation.disk.watermark.high\": \"95%\",         # Adjust high watermark (e.g., 95% full)\n    \"cluster.routing.allocation.disk.watermark.flood_stage\": \"98%\",  # Critical flood stage\n    \"cluster.routing.allocation.disk.threshold_enabled\": \"true\"\n  }\n}\n\n# Gracefully exclude a node for maintenance, allowing shards to relocate off it\nPUT _cluster/settings\n{\n  \"transient\": {\n    \"cluster.routing.allocation.exclude._name\": \"node_to_remove\"\n  }\n}\n```",
    "verification": "After applying fixes, verify the cluster's health and shard distribution. Use the `_cluster/health` API to check the cluster status (should return `green`). Use the `_cat/shards` and `_cat/allocation` APIs to monitor shard movements and ensure they are relocating at a controlled rate, if enabled. The `_cluster/allocation/explain` API is invaluable for understanding why a specific shard is or isn't relocating. Regularly check `_cat/nodes?v` to observe disk usage, shard counts, and heap usage across all data nodes. Over time, confirm that shards are evenly distributed and that no nodes are consistently hitting disk watermarks or experiencing high I/O due to excessive relocation.",
    "date": "2026-06-28",
    "id": 1782644379,
    "type": "error"
});