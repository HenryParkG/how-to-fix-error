window.onPostDataLoaded({
    "title": "Elixir GenStage Deadlocks & Mailbox Overflows Debugging",
    "slug": "elixir-genstage-deadlock-mailbox-overflow",
    "language": "Elixir",
    "code": "GenStageDeadlock",
    "tags": [
        "Elixir",
        "Concurrency",
        "Flow Control",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Elixir's GenStage is a powerful abstraction for building pipelines with demand-driven flow control, enabling robust backpressure mechanisms. However, misconfigurations or unexpected runtime conditions can lead to subtle and hard-to-debug issues like deadlocks and mailbox overflows. A deadlock typically occurs when a consumer stops demanding, or a producer is blocked waiting for an acknowledgement that never arrives, often due to a saturated consumer or a circular dependency in demand. Mailbox overflows, conversely, happen when a producer rapidly emits events that a slower consumer cannot process, causing messages to accumulate in the consumer's process mailbox until system limits are hit, potentially leading to 'no_such_process' errors if the consumer crashes or system-wide memory issues.</p><p>Understanding GenStage's core principle of demand is crucial: producers only send events when consumers explicitly request them. This pull-based mechanism is designed to prevent congestion. Problems arise when this demand signal is interrupted, mismanaged, or when processing takes too long, effectively halting the flow or backing up messages upstream or downstream.</p>",
    "root_cause": "Mismanaged demand signals (e.g., consumer stops demanding, producer not respecting demand), slow consumer processing leading to queue buildup, circular demand dependencies, or a producer rapidly generating events without adequate backpressure, causing mailbox overflow in an intermediate stage or consumer.",
    "bad_code": "```elixir\ndefmodule BadProducer do\n  use GenStage\n\n  def start_link(initial_state) do\n    GenStage.start_link(__MODULE__, initial_state)\n  end\n\n  def init(state) do\n    {:producer, state}\n  end\n\n  def handle_demand(demand, state) do\n    # Producer generates events regardless of actual demand remaining\n    events = Enum.map(1..demand, fn _ -> :rand.uniform() end)\n    {:noreply, events, state}\n  end\nend\n\ndefmodule BadConsumer do\n  use GenStage\n\n  def start_link() do\n    GenStage.start_link(__MODULE__, nil)\n  end\n\n  def init(state) do\n    {:consumer, state, subscribe_to: [BadProducer]}\n  end\n\n  def handle_events(events, from, state) do\n    # Artificially slow processing, not demanding more if backlog exists\n    Process.sleep(100 * length(events)) # Simulates heavy work\n    # Consumer does not demand more if processing is slow, leading to potential starvation or backlog\n    {:noreply, [], state}\n  end\nend\n```",
    "solution_desc": "The key to resolving GenStage deadlocks and mailbox overflows lies in robust demand management and proper backpressure application. Ensure consumers always demand new events after processing a batch, even if it's a small fixed number. Producers must strictly adhere to the demand received, only emitting events up to the requested amount. For slow consumers, consider implementing internal buffers and processing events asynchronously (e.g., in a separate task or another GenStage stage) to unblock the main GenStage `handle_events` callback and allow it to issue new demand promptly. Use `min_demand` and `max_demand` options to tune how much demand is issued. Monitoring GenStage event queues and mailbox sizes with tools like `:sys.get_state/1` or Observer can provide early warnings.",
    "good_code": "```elixir\ndefmodule GoodProducer do\n  use GenStage\n\n  def start_link(initial_state) do\n    GenStage.start_link(__MODULE__, initial_state)\n  end\n\n  def init(state) do\n    {:producer, state}\n  end\n\n  def handle_demand(demand, state) do\n    # Producer respects the demand strictly\n    events_to_emit = Enum.take(state.pending_events, demand)\n    remaining_events = Enum.drop(state.pending_events, demand)\n    {:noreply, events_to_emit, %{state | pending_events: remaining_events}}\n  end\nend\n\ndefmodule GoodConsumer do\n  use GenStage\n\n  def start_link() do\n    GenStage.start_link(__MODULE__, nil)\n  end\n\n  def init(state) do\n    # Specify min_demand and max_demand for stable flow\n    {:consumer, state, subscribe_to: [{GoodProducer, min_demand: 10, max_demand: 20}]}\n  end\n\n  def handle_events(events, _from, state) do\n    # Process events in a non-blocking manner or manage an internal queue\n    Task.start(fn ->\n      Enum.each(events, fn event ->\n        Process.sleep(50) # Simulate work\n        IO.inspect({:processed, event})\n      end)\n    end)\n    # Always return demand, even if processing is ongoing, to keep the pipeline flowing\n    {:noreply, [], state}\n  end\nend\n```",
    "verification": "To verify the fix, monitor the consumer's mailbox size using `:sys.get_status(pid) |> elem(4) |> length()` in a busy system. It should remain within reasonable bounds and not grow indefinitely. Observe the demand flow using `Logger.debug` in `handle_demand` and `handle_events` to ensure demand is consistently issued and respected. For deadlocks, ensure the system processes events under sustained load without halting or crashing. Load test the pipeline with varying event rates and consumer processing times, ensuring the system naturally applies backpressure without failure.",
    "date": "2026-06-19",
    "id": 1781858707,
    "type": "error"
});