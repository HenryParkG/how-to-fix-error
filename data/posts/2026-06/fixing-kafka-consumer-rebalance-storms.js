window.onPostDataLoaded({
    "title": "Fixing Kafka Consumer Group Rebalance Storms and Heartbeats",
    "slug": "fixing-kafka-consumer-rebalance-storms",
    "language": "Java",
    "code": "Kafka-Rebalance-Timeout",
    "tags": [
        "Java",
        "Backend",
        "Kafka",
        "Error Fix"
    ],
    "analysis": "<p>In distributed Kafka message brokers, a consumer rebalance storm occurs when one or more consumer nodes fail to report status within expected system windows. When this happens, the coordinator partition assumes the target consumer node has died, evicts it from the group, and redistributes its partitions to surviving nodes.</p><p>As these surviving nodes now have to process their own tasks plus the newly assigned load, their processing latency spikes. Consequently, they also fail their next heartbeat/poll boundary, triggering another rebalance. This forms an infinite loop of rebalancing storms where no real data gets processed, and CPU utilization on the Kafka cluster spikes to 100%.</p>",
    "root_cause": "The consumer group's processing loop performs heavy, synchronous calculations, synchronous database transactions, or external API calls inside the record delivery thread. This causes the block of records fetched in a single poll to take longer to process than the configured `max.poll.interval.ms`, tricking the broker coordinator into believing the consumer is dead, even though the heartbeat thread is still running.",
    "bad_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\n\npublic class DangerousConsumer {\n    public static void runConsumer() {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"payment-processor\");\n        props.put(\"max.poll.records\", \"1000\"); // Too high for synchronous external API operations\n        props.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes\n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"transactions\"));\n\n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            for (ConsumerRecord<String, String> record : records) {\n                // BUG: Heavy synchronous process (e.g., 500ms API call per record)\n                // 1000 records * 500ms = 500 seconds, which vastly exceeds max.poll.interval.ms!\n                processPaymentWithExternalGateways(record.value());\n            }\n            consumer.commitSync();\n        }\n    }\n\n    private static void processPaymentWithExternalGateways(String value) {\n        try { Thread.sleep(500); } catch (InterruptedException e) {} \n    }\n}",
    "solution_desc": "To cure rebalance storms, implement a two-pronged solution: 1) Scale back `max.poll.records` to a drastically smaller batch size and increase `max.poll.interval.ms` safely to provide ample overhead. 2) Decouple the ingestion and processing phases by moving the processing tasks onto a dedicated worker pool (using a bounded thread-safe buffer or an ExecutorService), allowing the main consumer thread to call `poll()` uninterrupted.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\nimport java.util.concurrent.*;\n\npublic class ResilientConsumer {\n    private final KafkaConsumer<String, String> consumer;\n    private final ThreadPoolExecutor executor;\n    private final Map<TopicPartition, OffsetAndMetadata> offsetsToCommit = new ConcurrentHashMap<>();\n\n    public ResilientConsumer() {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"payment-processor-resilient\");\n        props.put(\"max.poll.records\", \"50\"); // Reduced batch size\n        props.put(\"max.poll.interval.ms\", \"600000\"); // Raised timeout buffer to 10 minutes\n        props.put(\"enable.auto.commit\", \"false\");\n\n        this.consumer = new KafkaConsumer<>(props);\n        // Bounded queue prevents OutOfMemory errors\n        this.executor = new ThreadPoolExecutor(4, 4, 0L, TimeUnit.MILLISECONDS,\n                new LinkedBlockingQueue<>(200), new ThreadPoolExecutor.CallerRunsPolicy());\n    }\n\n    public void run() {\n        this.consumer.subscribe(Collections.singletonList(\"transactions\"));\n        try {\n            while (!Thread.currentThread().isInterrupted()) {\n                ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n                if (!records.isEmpty()) {\n                    for (ConsumerRecord<String, String> record : records) {\n                        executor.submit(() -> {\n                            processTransaction(record);\n                            offsetsToCommit.put(\n                                new TopicPartition(record.topic(), record.partition()),\n                                new OffsetAndMetadata(record.offset() + 1)\n                            );\n                        });\n                    }\n                    // Safe asynchronous offset tracking updates\n                    if (!offsetsToCommit.isEmpty()) {\n                        consumer.commitAsync(offsetsToCommit, null);\n                        offsetsToCommit.clear();\n                    }\n                }\n            }\n        } finally {\n            executor.shutdown();\n            consumer.close();\n        }\n    }\n\n    private void processTransaction(ConsumerRecord<String, String> record) {\n        // Heavy processing isolated here safe from consumer heartbeats\n        try { Thread.sleep(500); } catch (InterruptedException e) { Thread.currentThread().interrupt(); }\n    }\n}",
    "verification": "Deploy the updated application and monitor Kafka's native coordinator metrics. Use JMX tools to inspect `kafka.consumer:type=consumer-coordinator-metrics,client-id=*` and track the `join-rate-metric` and `rebalance-latency-avg`. Under heavy artificial processing load, verify that the rebalance counts drop to zero and that logs are free of `CommitFailedException` occurrences.",
    "date": "2026-06-21",
    "id": 1782042012,
    "type": "error"
});