window.onPostDataLoaded({
    "title": "Kafka Rebalancing Storms: Diagnosing Thrashing",
    "slug": "kafka-rebalancing-storms-diagnosing-thrashing",
    "language": "Kafka",
    "code": "Rebalancing Storm",
    "tags": [
        "Kafka",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Kafka consumer groups distribute topic partitions among their members, allowing for parallel message processing. This distribution is managed through a process called rebalancing. A 'rebalancing storm' occurs when consumer groups frequently rebalance, causing partitions to be reassigned repeatedly. This thrashing leads to several critical issues: increased message processing latency, duplicate message processing (as consumers lose their assigned partitions and new ones take over from potentially uncommitted offsets), and potential data loss if messages are committed incorrectly or not at all before rebalance. The cluster's overall health and throughput can degrade significantly during such events.</p><p>Frequent rebalances often indicate underlying problems with consumer health, configuration, or network stability, and diagnosing the root cause requires careful inspection of consumer behavior and Kafka broker logs.</p>",
    "root_cause": "Frequent Kafka consumer group rebalances are typically triggered by: 1. `session.timeout.ms` being too short, causing consumers to be prematurely considered dead. 2. `max.poll.interval.ms` being too short, leading to consumer 'heartbeat' failures due to long message processing. 3. Long GC pauses in consumer applications. 4. Ungraceful consumer shutdowns or frequent crashes. 5. Network instability between consumers and brokers. 6. Adding/removing consumers too frequently.",
    "bad_code": "```java\n// Example Kafka consumer configuration (Java)\nProperties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"group.id\", \"my-consumer-group\");\nprops.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"session.timeout.ms\", \"10000\"); // Too short for potentially slow network/processing\nprops.put(\"max.poll.interval.ms\", \"300000\"); // Default, might be too short if processing takes long\n\n// Inside consumer loop, with slow processing:\nwhile (true) {\n    ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n    for (ConsumerRecord<String, String> record : records) {\n        // Simulate heavy processing that exceeds max.poll.interval.ms\n        Thread.sleep(60000); \n        System.out.printf(\"offset = %d, key = %s, value = %s\\n\", record.offset(), record.key(), record.value());\n    }\n    consumer.commitSync();\n}\n```",
    "solution_desc": "To mitigate rebalancing storms, adjust consumer configurations to match application behavior and network conditions. Increase `session.timeout.ms` to give consumers more time to respond. Extend `max.poll.interval.ms` if message processing can take a long time, but pair it with smaller `heartbeat.interval.ms` (must be less than `session.timeout.ms`) to ensure timely heartbeats. Optimize message processing to reduce latency, perhaps by offloading heavy tasks to a separate thread pool or by committing offsets more frequently within a long processing batch. Implement graceful shutdowns for consumers, allowing them to leave the group cleanly. For critical scenarios, consider Kafka's static membership feature to make consumer identity sticky across restarts, reducing rebalances.",
    "good_code": "```java\n// Example Kafka consumer configuration (Java) with fixes\nProperties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"group.id\", \"my-consumer-group\");\nprops.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"session.timeout.ms\", \"45000\"); // Increased to 45 seconds\nprops.put(\"heartbeat.interval.ms\", \"15000\"); // Must be less than session.timeout.ms\nprops.put(\"max.poll.interval.ms\", \"600000\"); // Increased to 10 minutes\n\n// Inside consumer loop, with optimized processing:\nwhile (running) { // 'running' flag for graceful shutdown\n    try {\n        ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n        if (!records.isEmpty()) {\n            for (ConsumerRecord<String, String> record : records) {\n                // Process record quickly or offload to a thread pool\n                processRecord(record); \n            }\n            consumer.commitSync(); // Commit offsets after processing a batch\n        }\n    } catch (WakeupException e) {\n        // Handle graceful shutdown\n    } catch (Exception e) {\n        // Log error and potentially restart consumer or seek to last committed offset\n    }\n}\nconsumer.close(); // Ensure consumer closes cleanly\n\n// Example of how to add graceful shutdown:\nRuntime.getRuntime().addShutdownHook(new Thread(() -> {\n    running = false;\n    consumer.wakeup();\n}));\n\n// Or if processing is inherently slow and cannot be optimized, consider a more robust setup\n// where each record is processed and committed individually, or use a separate thread pool\n// for processing while the main consumer thread keeps polling and sending heartbeats.\n```",
    "verification": "Monitor the consumer group's state using Kafka's `kafka-consumer-groups.sh --bootstrap-server <broker> --describe --group <group-id>` command. Look for stable partition assignments and fewer `REBALANCING` states. Check Kafka broker logs for `[GroupCoordinator]` messages indicating rebalances, and application logs for `GroupRebalance` events or errors related to heartbeats. A successful fix will show stable consumer group membership and consistent message processing throughput.",
    "date": "2026-06-25",
    "id": 1782387831,
    "type": "error"
});