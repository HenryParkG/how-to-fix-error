window.onPostDataLoaded({
    "title": "Fixing PyTorch CUDA Fragmentation & IPC Leaks",
    "slug": "pytorch-cuda-fragmentation-ipc-leaks",
    "language": "Python / PyTorch",
    "code": "CUDA OOM / IPC Handle Leak",
    "tags": [
        "Python",
        "PyTorch",
        "Docker",
        "Error Fix"
    ],
    "analysis": "<p>In multi-GPU Distributed Data Parallel (DDP) systems, training pipelines often scale memory allocation across multiple processes. When sharing tensors between training processes or handling high-throughput DataLoader workers, PyTorch relies on CUDA Inter-Process Communication (IPC) handles. If these processes crash or terminate abruptly, these shared memory allocations persist on the GPU, leading to immediate out-of-memory (OOM) errors on consecutive training runs.</p><p>Furthermore, training jobs that use dynamic sequence lengths cause the PyTorch Caching Allocator to fragment GPU memory. The allocator retains large blocks of memory in its cache, leaving little space for small allocations, even when the overall reported VRAM usage appears low.</p>",
    "root_cause": "CUDA IPC memory handles (tensors shared via `.share_memory_()`) bypass Python's typical garbage collection lifecycle when child processes crash, leaving physical GPU memory locked in the driver context. Concurrently, dynamic shape allocation requests split large continuous memory blocks into sub-blocks that cannot be coalesced, causing severe allocator fragmentation.",
    "bad_code": "import torch\nimport torch.multiprocessing as mp\n\ndef worker(shared_tensor):\n    # Simulate dynamic data processing which triggers fragmentation\n    x = torch.randn(1000, 1000, device=\"cuda:0\")\n    # Dynamic batch resizing\n    dynamic_tensor = torch.randn(x.size(0) * 2, 1000, device=\"cuda:0\")\n    print(\"Worker operating on shared tensor...\")\n\nif __name__ == \"__main__\":\n    # Bad: Sharing tensor memory without proper IPC cleanup hooks or context safety\n    shared_tensor = torch.zeros(5000, 5000).cuda(0)\n    shared_tensor.share_memory_() \n    \n    p = mp.Process(target=worker, args=(shared_tensor,))\n    p.start()\n    p.join()\n    # If process crashed, IPC handles to shared_tensor remain leaked in CUDA VRAM",
    "solution_desc": "First, configure the PyTorch Caching Allocator using the environment variable `PYTORCH_CUDA_ALLOC_CONF` to set a pool segmentation split threshold, forcing the recycling of fragmented blocks. Second, wrap the inter-process communication pipeline in a robust manager that aggressively purges stale shared memory tensors, forces garbage collection, and clears CUDA IPC references when processes exit.",
    "good_code": "import os\nimport gc\nimport torch\nimport torch.multiprocessing as mp\n\n# Configure PyTorch Allocator to proactively combat memory fragmentation\nos.environ[\"PYTORCH_CUDA_ALLOC_CONF\"] = \"max_split_size_mb:128,garbage_collection_threshold:0.8\"\n\ndef safe_worker(shared_tensor_raw_desc, shape, dtype):\n    # Reconstruct CUDA tensor inside a clean try/finally context\n    # Rely on explicit garbage collection to prevent resource leaks\n    try:\n        device = torch.device(\"cuda:0\")\n        # Process dynamics with fragmented allocation prevention\n        torch.cuda.empty_cache()\n        \n        # Simulated processing\n        temp_data = torch.randn(2000, 2000, device=device)\n        del temp_data\n    except Exception as e:\n        print(f\"Worker encountered error: {e}\")\n    finally:\n        # Proactively flush allocations and run Python GC\n        gc.collect()\n        torch.cuda.empty_cache()\n\nif __name__ == \"__main__\":\n    mp.set_start_method('spawn', force=True)\n    \n    # Instead of raw sharing, pass structural descriptions or use scoped allocations\n    tensor_shape = (5000, 5000)\n    tensor_dtype = torch.float32\n    \n    try:\n        # Ensure isolated context execution\n        p = mp.Process(target=safe_worker, args=(None, tensor_shape, tensor_dtype))\n        p.start()\n        p.join()\n    finally:\n        # Post-execution CUDA cleanup\n        gc.collect()\n        torch.cuda.ipc_collect()\n        torch.cuda.empty_cache()\n        print(\"CUDA Memory clean up completed successfully.\")",
    "verification": "Verify by running your training loop and checking memory profiles using `print(torch.cuda.memory_summary(device=None, abbreviated=False))`. Verify the removal of stale IPC handles on Linux systems by examining file descriptors linked to GPU driver nodes with: `lsof +D /dev/nvidia*` and inspecting the memory footprint of active child processes.",
    "date": "2026-06-16",
    "id": 1781599213,
    "type": "error"
});