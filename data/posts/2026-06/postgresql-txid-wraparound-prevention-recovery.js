window.onPostDataLoaded({
    "title": "PostgreSQL Transaction ID Wraparound Prevention & Recovery",
    "slug": "postgresql-txid-wraparound-prevention-recovery",
    "language": "SQL",
    "code": "TxIDWraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "Transaction ID",
        "Wraparound",
        "SQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL's Multi-Version Concurrency Control (MVCC) relies heavily on Transaction IDs (XIDs) to manage data visibility. Each transaction is assigned a unique XID, a 32-bit unsigned integer. This means there's a theoretical limit of approximately 4 billion transactions. When a transaction ID 'wraps around' (i.e., rolls over from the maximum value back to zero), older transactions might appear to be in the future, leading to data corruption or making data invisible. To prevent this, PostgreSQL uses a mechanism called 'freezing'.</p><p>The `VACUUM` process, particularly `autovacuum`, is critical. It marks old transaction IDs as 'frozen', effectively making them permanently visible to all future transactions and resetting their visibility age. If `VACUUM` (or `autovacuum`) fails to run frequently enough, or if there are long-running transactions preventing `VACUUM` from completing its work on older rows, the database's `datfrozenxid` (the oldest unfrozen transaction ID) can advance too slowly. If `txid_current()` approaches 2 billion transactions older than `datfrozenxid`, PostgreSQL will enter an emergency 'read-only' mode to prevent data loss. If it progresses beyond 2 billion, data corruption is imminent and the database will shut down, requiring manual intervention for recovery.</p><p>Understanding and proactively monitoring the transaction ID age is vital for any production PostgreSQL database.</p>",
    "root_cause": "Infrequent or ineffective `VACUUM` operations (especially `autovacuum`) leading to transaction IDs not being marked as old ('frozen'). Long-running transactions (e.g., open transactions, prepared transactions, replication slots with long horizons) can block `autovacuum` from freezing old XIDs, causing the `datfrozenxid` to fall behind the current transaction ID generation.",
    "bad_code": "```sql\n-- Simulating a long-running transaction blocking autovacuum\nBEGIN; -- Start a transaction and keep it open for a very long time\nSELECT pg_sleep(3600); -- Sleep for an hour or more, preventing XID cleanup\n-- ... do some work that generates many new transactions in other sessions ...\nROLLBACK; -- Eventually commit/rollback the blocking transaction\n\n-- Or, misconfigured autovacuum:\n-- In postgresql.conf, setting autovacuum = off (NEVER do this in production)\n-- Or, setting aggressive autovacuum thresholds that prevent it from running:\n-- autovacuum_vacuum_scale_factor = 0.5 (too high for busy tables)\n-- autovacuum_freeze_max_age = 1000000000 (too high, allows XIDs to get too old)\n\n-- Checking current XID age status (if it's getting too high):\nSELECT datname, age(datfrozenxid) AS xid_age_in_transactions, \n       'warning' AS status\nFROM pg_database WHERE age(datfrozenxid) > 1000000000; -- Nearing the 2 billion limit\n```",
    "solution_desc": "Proactive monitoring and correct `autovacuum` configuration are key. Ensure `autovacuum` is enabled (`autovacuum = on`). Tune `autovacuum` parameters for your workload, specifically `autovacuum_freeze_max_age` (default 200M, can be lowered for busy databases), `autovacuum_vacuum_scale_factor`, and `autovacuum_vacuum_cost_delay`. Identify and resolve long-running transactions (using `pg_stat_activity`) or prepared transactions (`pg_prepared_xacts`) that might be blocking `VACUUM`. Monitor replication slots if using logical replication, as inactive slots can also block XID cleanup. In an emergency (when `age(datfrozenxid)` approaches 2 billion), perform a manual `VACUUM FREEZE` on problematic tables or the entire database if possible, and then reboot if the database is in read-only mode.",
    "good_code": "```sql\n-- 1. Monitor XID age for all databases (run frequently)\nSELECT datname, \n       age(datfrozenxid) AS xid_age,\n       (SELECT setting FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') AS freeze_max_age,\n       CASE\n           WHEN age(datfrozenxid) > (SELECT setting::int * 0.75 FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') THEN 'CRITICAL'\n           WHEN age(datfrozenxid) > (SELECT setting::int * 0.50 FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') THEN 'WARNING'\n           ELSE 'OK'\n       END AS status\nFROM pg_database\nORDER BY xid_age DESC;\n\n-- 2. Identify long-running transactions (run frequently)\nSELECT pid, usename, client_addr, application_name, backend_start,\n       xact_start, state, query_start, query, wait_event_type, wait_event\nFROM pg_stat_activity\nWHERE state != 'idle' AND xact_start IS NOT NULL\nORDER BY xact_start;\n\n-- 3. To force a VACUUM FREEZE on a problematic table:\nVACUUM FREEZE VERBOSE ANALYZE my_table;\n\n-- 4. To force a VACUUM FREEZE on the entire database (requires downtime in severe cases):\n-- First, connect to another database (e.g., 'postgres') as you cannot VACUUM FREEZE\n-- the currently connected database if it's in a critical state.\n-- Then:\n-- psql -d postgres -c \"VACUUM FREEZE VERBOSE ANALYZE database_name;\"\n-- Or, if in emergency shutdown:\n-- pg_ctl -D /path/to/data_dir start -o \"-P\"\n-- Then connect and VACUUM FREEZE, then restart normally.\n```",
    "verification": "Regularly monitor the `age(datfrozenxid)` for all databases using the provided SQL query. Ensure that the 'status' never reaches 'CRITICAL' or even 'WARNING' for extended periods. Check `autovacuum` logs (if logging is enabled) to confirm that freezing is occurring. Verify that there are no persistently long-running transactions. After implementing fixes or recovery, observe `xid_age` metrics to confirm that `autovacuum` is effectively freezing old transaction IDs and that the age is decreasing or staying well below the threshold.",
    "date": "2026-06-26",
    "id": 1782456307,
    "type": "error"
});