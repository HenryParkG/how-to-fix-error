window.onPostDataLoaded({
    "title": "Resolving Spark Driver OOMs and Partition Skew",
    "slug": "resolving-spark-driver-ooms-partition-skew",
    "language": "Java",
    "code": "OutOfMemoryError",
    "tags": [
        "Java",
        "SQL",
        "AWS",
        "Error Fix"
    ],
    "analysis": "<p>In Apache Spark, driver OutOfMemory (OOM) errors and executor partition skew are common side effects of uneven data distribution and incorrect execution patterns during massive shuffle operations. Partition skew occurs when specific keys hold a disproportionately large volume of data compared to other keys, routing the majority of the dataset to a single executor task during a join or aggregation.</p><p>This causes the bottlenecked task to take significantly longer or fail with an OOM. Similarly, if drivers execute a collect operation on large datasets instead of reducing data volumes cluster-side, the aggregated results exceed the driver's JVM heap boundary, resulting in a system crash.</p>",
    "root_cause": "An un-salted join key with a high percentage of nulls/defaults concentrated processing on a single executor partition, combined with an unaggregated `.collect()` action pulling massive partitions directly into the Spark Driver's memory.",
    "bad_code": "import org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\nimport org.apache.spark.sql.SparkSession;\n\npublic class SparkOomJob {\n    public void execute(SparkSession spark) {\n        Dataset<Row> df1 = spark.read().parquet(\"s3a://bucket/huge-transactions\");\n        Dataset<Row> df2 = spark.read().parquet(\"s3a://bucket/users\");\n\n        // BUG 1: Joining on heavily skewed field 'null' or default values\n        // without salting, causing catastrophic partition skew during shuffle.\n        Dataset<Row> joined = df1.join(df2, df1.col(\"userId\").equalTo(df2.col(\"id\")));\n\n        // BUG 2: Collecting raw un-aggregated dataset directly into the driver\n        // causing driver OutOfMemoryError.\n        Row[] allRecords = (Row[]) joined.collect();\n    }\n}",
    "solution_desc": "Mitigate partition skew by salting the join keys to distribute identical values across multiple partitions, and then joining using salted versions of the keys. Resolve Driver OOM errors by avoiding direct `.collect()` calls, utilizing distributed aggregations, writing directly to storage, or using `.take()` or `.limit()` when sampling data.",
    "good_code": "import org.apache.spark.sql.Dataset;\nimport org.apache.spark.sql.Row;\nimport org.apache.spark.sql.SparkSession;\nimport static org.apache.spark.sql.functions.*;\n\npublic class SparkFixedJob {\n    public void execute(SparkSession spark) {\n        // Configure dynamic partition pruning & adaptive query execution (AQE)\n        spark.conf().set(\"spark.sql.adaptive.enabled\", \"true\");\n        spark.conf().set(\"spark.sql.adaptive.skewJoin.enabled\", \"true\");\n\n        Dataset<Row> df1 = spark.read().parquet(\"s3a://bucket/huge-transactions\");\n        Dataset<Row> df2 = spark.read().parquet(\"s3a://bucket/users\");\n\n        // Mitigate skew: Salt the key on df1 and replicate df2 across those salt values\n        int saltFactor = 10;\n        Dataset<Row> saltedDf1 = df1.withColumn(\"salt\", floor(rand().multiply(saltFactor)))\n                .withColumn(\"saltedUserId\", concat(col(\"userId\"), lit(\"_\"), col(\"salt\")));\n\n        // Explode dimension table (df2) with salt factors to complete the join relation safely\n        Dataset<Row> saltedDf2 = df2.withColumn(\"salt_arr\", array(sequence(lit(0), lit(saltFactor - 1))))\n                .withColumn(\"salt\", explode(col(\"salt_arr\")))\n                .withColumn(\"saltedId\", concat(col(\"id\"), lit(\"_\"), col(\"salt\")));\n\n        Dataset<Row> joined = saltedDf1.join(saltedDf2, saltedDf1.col(\"saltedUserId\").equalTo(saltedDf2.col(\"saltedId\")));\n\n        // Aggregate on executors, reducing payload size significantly before outputting\n        Dataset<Row> aggregated = joined.groupBy(\"id\").count();\n\n        // Write to storage instead of pulling massive data into the driver memory\n        aggregated.write().mode(\"overwrite\").parquet(\"s3a://bucket/output-counts\");\n    }\n}",
    "verification": "Check the Spark Web UI under the 'Stages' tab. Verify that the task execution time histogram is uniform (min, median, and max times are close) and that Spark's AQE successfully detected and handled the skewed partition join.",
    "date": "2026-06-21",
    "id": 1782010068,
    "type": "error"
});