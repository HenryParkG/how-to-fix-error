window.onPostDataLoaded({
    "title": "CoreDNS NDOTS Latency & Packet Drops in K8s",
    "slug": "coredns-ndots-latency-packet-drops-kubernetes",
    "language": "Kubernetes",
    "code": "DNS Latency",
    "tags": [
        "Kubernetes",
        "DNS",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>In high-scale Kubernetes clusters, CoreDNS, the default DNS server, plays a critical role. However, poorly configured DNS resolution, particularly the `ndots` option, can lead to significant latency spikes and even packet drops, impacting application performance and reliability. The `ndots` setting in `/etc/resolv.conf` dictates how many dots must appear in a name before it is considered an absolute domain name. For names with fewer dots, the system will append search domains (e.g., `namespace.svc.cluster.local`) and try to resolve them sequentially before trying the name as absolute.</p><p>When applications frequently make unqualified DNS queries (e.g., `my-service` instead of `my-service.my-namespace.svc.cluster.local`), the `ndots` value being high (default `5` in K8s) causes multiple unsuccessful lookups against search domains before the correct, fully qualified name is resolved. This generates a surge of unnecessary DNS queries on CoreDNS, leading to increased load, higher latency, and potential UDP packet drops under heavy traffic.</p>",
    "root_cause": "The default `ndots:5` setting in a Pod's `/etc/resolv.conf` combined with short (unqualified) service names. When a Pod tries to resolve `my-service`, the DNS resolver will try `my-service.namespace.svc.cluster.local`, then `my-service.svc.cluster.local`, then `my-service.cluster.local`, then `my-service.global-domain.com` (if configured), and finally `my-service` as an absolute name. Each failed attempt adds latency and load to CoreDNS. In high-QPS scenarios, this excessive querying can overwhelm CoreDNS, leading to increased latency, timeouts, and UDP packet drops at the network interface or within CoreDNS's buffer.",
    "bad_code": "```yaml\n# A typical Pod definition that will suffer from ndots latency issues\n# if it frequently resolves unqualified service names.\n# The default K8s resolv.conf for this pod will have:\n# search default.svc.cluster.local svc.cluster.local cluster.local\n# options ndots:5\n\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app-deployment\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: my-app\n  template:\n    metadata:\n      labels:\n        app: my-app\n    spec:\n      containers:\n      - name: my-app-container\n        image: my-app-image:latest\n        ports:\n        - containerPort: 8080\n        env:\n        - name: TARGET_SERVICE_HOST\n          value: \"backend-service\" # This short name will trigger ndots issues\n        # No specific dnsConfig provided, relying on default.\n```\n<p>Application code inside `my-app-container` making frequent DNS lookups for `backend-service` will experience elevated latency due to the multiple search domain attempts.</p>",
    "solution_desc": "To mitigate CoreDNS `ndots` latency and packet drops:\n1.  **Set `ndots:1` for specific Pods**: For Pods that frequently resolve short service names, explicitly set `dnsConfig.options.ndots: 1`. This tells the resolver to treat names with at least one dot as absolute, and for names with zero dots, to directly try them against search domains without unnecessary steps.\n2.  **Use FQDNs**: Encourage applications to use Fully Qualified Domain Names (FQDNs) like `backend-service.my-namespace.svc.cluster.local` whenever possible. This bypasses the search domain logic entirely.\n3.  **Deploy Node-Local DNS Cache**: Implement `node-local-dns` (e.g., DaemonSet `k8s.gcr.io/dns/k8s-dns-node-cache`). This places a caching DNS agent on each node, intercepting DNS requests locally, reducing traffic to CoreDNS, and providing faster responses for cached entries.\n4.  **Tune CoreDNS `Corefile`**: Optimize CoreDNS configuration. Consider adding the `cache` plugin, tuning `forward` plugin parameters (e.g., `health_check`, `expire`).\n5.  **Scale CoreDNS**: Increase CoreDNS replicas and ensure they have adequate CPU/memory resources. Use `cluster-proportional-autoscaler` for dynamic scaling.\n6.  **Increase UDP Buffer Sizes**: At the OS level on nodes, tune `net.core.rmem_max` and `net.core.wmem_max` to allow larger UDP receive/send buffers, reducing packet drops under load.",
    "good_code": "```yaml\n# Pod definition with dnsConfig to mitigate ndots issues\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app-deployment-fixed\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: my-app-fixed\n  template:\n    metadata:\n      labels:\n        app: my-app-fixed\n    spec:\n      containers:\n      - name: my-app-container\n        image: my-app-image:latest\n        ports:\n        - containerPort: 8080\n        env:\n        - name: TARGET_SERVICE_HOST\n          value: \"backend-service\" # Still using short name, but ndots:1 helps\n      dnsConfig:\n        options:\n        - name: ndots\n          value: \"1\" # Set ndots to 1 to reduce search domain lookups\n```\n<p>For `node-local-dns`, you would deploy the `kube-dns-autoscaler` and `node-local-dns` DaemonSet as per Kubernetes documentation, which is a separate manifest.</p>",
    "verification": "To verify the fix:\n1.  **Inside a Pod**: Run `cat /etc/resolv.conf` to confirm `options ndots:1` is present.\n2.  **DNS Latency Testing**: Use `dig` or `nslookup` inside a problematic Pod to compare resolution times for short vs. FQDNs before and after the fix. Tools like `k6` or `dnsperf` can simulate high-QPS scenarios.\n    ```bash\n    # Inside the pod with ndots:1\n    time dig backend-service\n    time dig backend-service.my-namespace.svc.cluster.local\n    ```\n3.  **CoreDNS Metrics**: Monitor CoreDNS Prometheus metrics (if configured) for queries per second, cache hit ratio, request duration, and error rates. Look for a reduction in total query volume and average latency after the fix.\n4.  **Application Logs**: Check application logs for DNS lookup errors or timeouts, which should decrease.\n5.  **Network Packet Capture**: Use `tcpdump` on a node to observe DNS traffic to CoreDNS. Look for a reduction in redundant queries for unqualified names.",
    "date": "2026-06-16",
    "id": 1781616975,
    "type": "error"
});