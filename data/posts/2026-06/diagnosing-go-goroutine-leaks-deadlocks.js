window.onPostDataLoaded({
    "title": "Diagnosing Go Goroutine Leaks & Deadlocks",
    "slug": "diagnosing-go-goroutine-leaks-deadlocks",
    "language": "Go",
    "code": "Goroutine Leak, Deadlock",
    "tags": [
        "Go",
        "Backend",
        "Concurrency",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks occur when goroutines are started but never complete or exit, leading to unreleased resources and memory exhaustion. Deadlocks happen when two or more goroutines are blocked indefinitely, each waiting for the other to release a resource. Both are critical issues in high-concurrency Go services, degrading performance and potentially crashing the application.</p>",
    "root_cause": "Goroutine leaks often stem from unbuffered channels where a sender is blocked but no receiver is present, or vice-versa, without proper timeout mechanisms or cancellation contexts. Common deadlocks involve incorrect mutex locking order, recursive locking, or channel operations where senders and receivers are out of sync without a path to unblock.",
    "bad_code": "package main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"time\"\n)\n\nfunc leakedWorker() {\n\tch := make(chan struct{})\n\t<-ch // Goroutine blocks forever, never exits\n\tfmt.Println(\"This line is never reached\")\n}\n\nfunc main() {\n\tfmt.Println(\"Goroutines before leak:\", runtime.NumGoroutine())\n\tgo leakedWorker() // Starts a goroutine that will leak\n\tfmt.Println(\"Goroutines after leak attempt:\", runtime.NumGoroutine())\n\n\t// Simulate a deadlock scenario (simplified):\n\t// This demonstrates a potential for a deadlock where two channels wait for each other.\n\tch1 := make(chan struct{})\n\tch2 := make(chan struct{})\n\n\tgo func() {\n\t\t<-ch1 // Goroutine 1 waits for ch1\n\t\tch2 <- struct{}{} // Goroutine 1 tries to send to ch2\n\t}()\n\n\tgo func() {\n\t\t<-ch2 // Goroutine 2 waits for ch2\n\t\tch1 <- struct{}{} // Goroutine 2 tries to send to ch1\n\t}()\n\n\ttime.Sleep(time.Second) // Give goroutines time to block\n\tfmt.Println(\"Main function exiting, goroutines might be deadlocked/leaked.\")\n}",
    "solution_desc": "Employ `context.Context` for cancellation and timeouts in long-running operations or network calls, ensuring goroutines can gracefully exit. Use buffered channels appropriately to prevent blocking, or ensure all senders/receivers have a clear path to complete. For deadlocks, establish a strict locking order for mutexes, use `select` with `default` clauses or timeouts on channels to prevent indefinite blocking, and consider `sync.WaitGroup` for proper goroutine synchronization and termination. Regularly profile with `pprof` to identify active goroutines and their stacks.",
    "good_code": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc reliableWorker(ctx context.Context, id int, wg *sync.WaitGroup) {\n\tdefer wg.Done()\n\tfor {\n\t\tselect {\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Printf(\"Worker %d: Context cancelled, exiting.\\n\", id)\n\t\t\treturn\n\t\tcase <-time.After(time.Millisecond * 100):\n\t\t\t// Simulate work\n\t\t\tfmt.Printf(\"Worker %d: Doing work...\\n\", id)\n\t\t}\n\t}\n}\n\nfunc main() {\n\tfmt.Println(\"Goroutines before workers:\", runtime.NumGoroutine())\n\n\tvar wg sync.WaitGroup\n\tctx, cancel := context.WithCancel(context.Background())\n\t\n\twg.Add(2)\n\tgo reliableWorker(ctx, 1, &wg)\n\tgo reliableWorker(ctx, 2, &wg)\n\n\ttime.Sleep(time.Second) // Let workers run for a bit\n\tcancel()                // Signal workers to stop\n\n\twg.Wait() // Wait for workers to finish\n\tfmt.Println(\"Goroutines after workers stopped and waited:\", runtime.NumGoroutine())\n\n\t// To prevent deadlocks in channel scenarios, ensure proper send/receive logic or timeouts\n\tdone := make(chan struct{})\n\tdata := make(chan int, 3) // Buffered channel to prevent sender blocking instantly\n\n\tgo func() {\n\t\tfor i := 0; i < 5; i++ {\n\t\t\tselect {\n\t\t\tcase data <- i:\n\t\t\t\tfmt.Println(\"Sent:\", i)\n\t\t\tcase <-time.After(time.Millisecond * 500): // Timeout for sending\n\t\t\t\tfmt.Println(\"Sender timed out trying to send\", i)\n\t\t\t\tbreak\n\t\t\t}\n\t\t}\n\t\tclose(data) // Close the channel when done sending\n\t}()\n\n\tgo func() {\n\t\tfor val := range data {\n\t\t\tfmt.Println(\"Received:\", val)\n\t\t}\n\t\tdone <- struct{}{} // Signal completion\n\t}()\n\n\t<-done // Wait for the receiver to finish\n\tfmt.Println(\"Main finished, all goroutines cleaned up.\")\n}",
    "verification": "1. <b>Goroutine Leaks</b>: Use `runtime.NumGoroutine()` before and after the operation to check for a steady state. More robustly, use Go's built-in `pprof` by exposing `/debug/pprof/goroutine?debug=1` endpoint. Analyze the goroutine profiles to identify goroutines that are blocked indefinitely or are unexpectedly long-lived.<br/>2. <b>Deadlocks</b>: `pprof` can also help here by showing blocked goroutines. For explicit deadlocks, Go's runtime will detect them in certain scenarios and print a stack trace to stderr, exiting the program. For more subtle application-level deadlocks, integrate a custom deadlock detector (e.g., using `sync.Mutex` and `runtime.Stack`) or thoroughly review code for locking order and channel synchronization issues. Utilize `go tool trace` for detailed concurrency visualization.",
    "date": "2026-06-17",
    "id": 1781700451,
    "type": "error"
});