window.onPostDataLoaded({
    "title": "MongoDB WiredTiger Cache Stalls & Write Ticket Exhaustion",
    "slug": "troubleshooting-mongodb-wiredtiger-cache-eviction-stalls-write-ticket-exhaustion",
    "language": "MongoDB",
    "code": "WiredTigerStalls",
    "tags": [
        "MongoDB",
        "Database",
        "NoSQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses an internal cache to hold active data and indexes, significantly improving read and write performance by minimizing disk I/O. However, under heavy load or misconfiguration, this cache can become a source of critical performance bottlenecks. 'WiredTiger cache eviction stalls' occur when the storage engine is unable to evict dirty (modified) pages from the cache quickly enough to make space for new incoming data or queries. This often happens when the cache is undersized or when the disk I/O cannot keep up with the rate of changes that need to be flushed.</p><p>'Write ticket exhaustion' is another common issue, where `mongod` limits concurrent write operations using an internal ticketing system. Each write operation consumes a ticket, and if all tickets are in use, subsequent write requests must wait, leading to increased write latency and application timeouts. Both issues are often intertwined and point to an I/O bottleneck, either due to insufficient memory, slow storage, or inefficient database operations.</p>",
    "root_cause": "The root causes for WiredTiger cache eviction stalls and write ticket exhaustion are often: (1) **Insufficient RAM:** The WiredTiger cache, by default, uses 50% of available RAM (minus 1GB). If this is too small for the active working set (indexes and frequently accessed data), constant cache eviction pressure and disk reads/writes will occur. (2) **High Write-Intensive Workloads:** Sustained high write operations, especially on documents requiring moves (e.g., adding large fields, increasing array sizes), lead to write amplification and rapid dirty page generation. (3) **Slow Storage I/O:** Underperforming disk subsystems (HDDs instead of SSDs, slow network storage) cannot flush dirty data or retrieve needed pages fast enough, causing a backlog and stalls. (4) **Inefficient Indexing:** Missing or poorly chosen indexes can lead to collection scans, increasing both read I/O and, for updates, potentially write I/O. (5) **High Concurrency:** Too many concurrent write operations exceeding the default 128 write tickets (or configured limit) will cause new writes to queue. (6) **Large Batches/Transactions:** Very large transactions or bulk writes can consume tickets for extended periods, blocking other writes.",
    "bad_code": "```yaml\n# mongod.conf snippet demonstrating potential problematic configuration\n# Often, problems arise from default settings on under-resourced hardware\nstorage:\n  dbPath: /var/lib/mongodb\n  journal:\n    enabled: true\n  wiredTiger:\n    engineConfig:\n      # Explicitly setting cacheSizeGB can be problematic if too small\n      # Or relying on defaults on a server with too little RAM\n      cacheSizeGB: 2 # If server has 8GB RAM, this is too small for active data\n      # If disk is slow, this won't help\n      journalCompressor: snappy\n      directoryForIndexes: false\n# default 128 write tickets - can be insufficient for very high concurrency\n# no explicit settings here for globalLock, further indicating a reliance on defaults\n```\n\n```javascript\n// Application code demonstrating a high-volume, potentially inefficient write pattern\n// Assume 'collection' is a MongoDB collection instance\n\nasync function simulateHighWrites(numOperations) {\n  for (let i = 0; i < numOperations; i++) {\n    // This kind of update can cause document moves if 'data' grows large\n    // leading to write amplification and cache pressure.\n    // Lack of proper indexing on 'userId' can also contribute to performance issues.\n    await collection.updateOne(\n      { userId: i % 100 }, // Assume 100 users, high contention\n      { $push: { data: { value: Math.random(), timestamp: new Date() } } },\n      { upsert: true }\n    );\n    if (i % 1000 === 0) {\n      console.log(`Completed ${i} writes.`);\n    }\n  }\n}\n\nsimulateHighWrites(100000); // Trigger a large number of potentially inefficient writes\n```",
    "solution_desc": "Solving WiredTiger cache stalls and write ticket exhaustion requires optimizing both MongoDB configuration and application-level operations. (1) **Increase RAM:** The most direct solution is to ensure the server has enough RAM so that WiredTiger's cache can hold the entire working set. Avoid explicitly setting `cacheSizeGB` unless you have a deep understanding of your workload; let WiredTiger manage it (which defaults to 50% of RAM minus 1GB, or 256MB on very small systems). (2) **Optimize Indexes:** Create covering indexes for read queries and ensure write operations efficiently use indexes. Analyze queries (`db.collection.explain()`) and create partial indexes or compound indexes to reduce I/O. (3) **Upgrade Storage:** Use high-performance SSDs (NVMe if possible) to reduce disk I/O latency. Ensure sufficient IOPS and throughput for your workload. (4) **Schema Design:** Optimize your schema to reduce document moves (e.g., using bucketing for time-series data, or pre-allocating array sizes if possible). (5) **Batch Writes:** For high-volume writes, use `db.collection.insertMany()` or `bulkWrite()` operations to reduce network overhead and ticket consumption. (6) **Sharding:** For extremely high-throughput or large datasets, implement sharding to distribute the load across multiple `mongod` instances. (7) **Monitor & Adjust:** Continuously monitor `wiredTiger.cache` metrics (`tracked bytes dirty`, `eviction calls`) and `globalLock.currentQueue` for write tickets. Consider increasing `wiredTiger.engineConfig.maxWriteTickets` (though this only helps if the bottleneck isn't disk I/O).",
    "good_code": "```yaml\n# mongod.conf snippet with optimized settings and assumptions of sufficient RAM/storage\nstorage:\n  dbPath: /var/lib/mongodb\n  journal:\n    enabled: true\n  wiredTiger:\n    engineConfig:\n      # Let WiredTiger manage cache size based on total RAM (default: 50% of RAM - 1GB)\n      # Only override if you have very specific requirements, e.g., for embedded systems.\n      # cacheSizeGB: <calculated-optimal-value> # ONLY if default is truly suboptimal\n      journalCompressor: snappy # Good default for performance/space\n      directoryForIndexes: false\n    # Consider increasing write tickets if I/O is NOT the bottleneck and CPU is underutilized\n    # This is a last resort and should be done with caution after profiling.\n    # maxWriteTickets: 256 # Default is 128\n```\n\n```javascript\n// Application code demonstrating efficient write patterns\n// Assume 'collection' is a MongoDB collection instance\n\nasync function performEfficientWrites(numOperations) {\n  const bulkOperations = [];\n  for (let i = 0; i < numOperations; i++) {\n    // Use bulk operations for efficiency. 'updateOne' is good for individual, but many are better in bulk.\n    // Using $set for specific field updates, reducing chances of document moves unless necessary.\n    bulkOperations.push({\n      updateOne: {\n        filter: { userId: i % 100 },\n        update: { $set: { lastActivity: new Date() }, $inc: { count: 1 } },\n        upsert: true,\n      },\n    });\n\n    if (bulkOperations.length >= 1000) { // Batch 1000 operations\n      await collection.bulkWrite(bulkOperations, { ordered: false });\n      console.log(`Completed ${i + 1} writes via bulkWrite.`);\n      bulkOperations.length = 0; // Clear the array\n    }\n  }\n\n  if (bulkOperations.length > 0) {\n    await collection.bulkWrite(bulkOperations, { ordered: false });\n    console.log(`Completed final writes via bulkWrite.`);\n  }\n}\n\n// Ensure appropriate indexes are created for efficient lookups/updates:\n// db.collection.createIndex({ userId: 1 });\n// Consider a partial index if 'data' is only relevant for a subset of documents:\n// db.collection.createIndex({ 'data.timestamp': -1 }, { partialFilterExpression: { hasData: true }});\n\nperformEfficientWrites(100000);\n```",
    "verification": "To verify the fixes, closely monitor MongoDB's diagnostic metrics and system resources. Key metrics to watch include: (1) `db.serverStatus().wiredTiger.cache`: Observe `bytes currently in the cache`, `max bytes configured`, `tracked bytes dirty`, `eviction calls` (should decrease), and `pages requested from the cache` vs. `pages read into cache`. (2) `db.serverStatus().globalLock`: Check `currentQueue.writers` and `totalTime` for write locks. Ideally, `writers` should be low or zero, and `totalTime` should not be excessively high. (3) `db.serverStatus().transactions`: Monitor `writeConflictExceptions` which indicate issues with concurrent writes. (4) System-level metrics: CPU utilization, RAM usage, and crucially, disk I/O (IOPS, latency, throughput). Look for reduced disk queue lengths and improved I/O wait times. A successful fix will show stable or reduced cache eviction activity, lower write queue times, and consistent, improved application throughput with lower latency for write operations.",
    "date": "2026-06-19",
    "id": 1781872914,
    "type": "error"
});