window.onPostDataLoaded({
    "title": "Debugging MongoDB WiredTiger Cache Eviction Stalls",
    "slug": "mongodb-wiredtiger-cache-eviction-stalls",
    "language": "MongoDB",
    "code": "CacheEvictionStalls",
    "tags": [
        "MongoDB",
        "WiredTiger",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses a sophisticated cache to hold data and index pages in RAM. This cache is crucial for performance, as it minimizes disk I/O. Under heavy write-heavy workloads, especially when the working set (the data and indexes frequently accessed) exceeds available RAM or when write rates are extremely high, WiredTiger's eviction mechanism kicks in. The evictor's job is to free up space in the cache by writing 'dirty' (modified) pages to disk and removing 'clean' (unmodified) pages.</p><p>When the eviction process struggles to keep up\u2014due to slow disk I/O, an undersized cache, or excessive dirty pages\u2014it can lead to 'eviction stalls.' These stalls occur when MongoDB threads (e.g., those handling client write operations) are forced to pause and wait for the evictor to free up cache pages before they can proceed. This manifests as sudden and significant latency spikes for write operations, reduced throughput, and a general degradation of database performance, even if CPU utilization appears low. It's a critical bottleneck that directly impacts the responsiveness of applications relying on MongoDB.</p>",
    "root_cause": "The specific technical reason for failure is the inability of the WiredTiger cache evictor to free up cache pages fast enough to satisfy incoming write requests. This is typically caused by a combination of: <ul><li><b>Insufficient RAM:</b> The WiredTiger cache is undersized relative to the active working set, forcing constant eviction.</li><li><b>Slow Disk I/O:</b> The underlying storage system cannot write dirty pages to disk quickly enough, backlogging the evictor.</li><li><b>High Write Rate:</b> A sustained, high volume of writes generates too many dirty pages for the evictor and I/O subsystem to handle.</li><li><b>Inefficient Indexing/Queries:</b> Poorly designed indexes or queries that scan large datasets unnecessarily pull more data into cache, increasing eviction pressure.</li><li><b>Large Document Sizes:</b> Writing or updating large documents requires more cache space and generates more dirty pages.</li></ul>",
    "bad_code": "```bash\n# Scenario: Running MongoDB on a VM with limited RAM (e.g., 4GB total, 2.5GB for WiredTiger cache)\n# and slow network-attached storage (e.g., standard HDD or low-IOPS SSD).\n# A client application then performs a sustained, high-volume write operation:\n\n# Assuming a collection 'largeDocs' with no indexes other than _id\ndb.largeDocs.createIndex({ field: 1 }, { background: true }); // Even index creation can exacerbate\n\n# Client application code (e.g., Node.js driver)\nconst { MongoClient } = require('mongodb');\nconst uri = \"mongodb://localhost:27017\";\nconst client = new MongoClient(uri);\n\nasync function insertHeavyData() {\n    await client.connect();\n    const database = client.db(\"testdb\");\n    const collection = database.collection(\"largeDocs\");\n\n    console.log(\"Starting heavy write workload...\");\n    const start = Date.now();\n    for (let i = 0; i < 1000000; i++) {\n        const doc = { \n            _id: i, \n            data: 'a'.repeat(5000), // 5KB document, generating lots of dirty pages\n            timestamp: new Date()\n        };\n        try {\n            await collection.insertOne(doc, { writeConcern: { w: 1 } });\n            if (i % 10000 === 0) {\n                console.log(`Inserted ${i} documents. Time: ${(Date.now() - start) / 1000}s`);\n            }\n        } catch (error) {\n            console.error(\"Write error:\", error);\n            break;\n        }\n    }\n    const end = Date.now();\n    console.log(`Finished heavy write workload. Total time: ${(end - start) / 1000}s`);\n    await client.close();\n}\n\ninsertHeavyData().catch(console.dir);\n```\n<p>This 'bad code' describes a scenario rather than a specific buggy code snippet. The combination of limited resources (RAM, slow disk) and a high-volume write workload with moderately large documents creates a perfect storm for WiredTiger cache eviction stalls. The <code>insertOne</code> operations will eventually block as the evictor struggles to free up space.</p>",
    "solution_desc": "<p>Addressing WiredTiger cache eviction stalls primarily involves optimizing resource allocation and write patterns. The most impactful solution is to ensure MongoDB has sufficient RAM, allowing the WiredTiger cache to comfortably hold the active working set. The default WiredTiger cache size is 50% of (RAM - 1GB) or 256MB, whichever is greater; this often needs manual adjustment. Upgrading to faster storage (NVMe SSDs) is critical, as it directly impacts how quickly dirty pages can be flushed to disk. For cloud deployments, provision higher IOPS storage.</p><p>Analyze write patterns: can writes be batched (e.g., <code>insertMany</code>) to reduce overhead? Can document sizes be reduced? Are indexes optimal, or are inefficient queries pulling too much data into memory? Consider sharding for extremely high write workloads to distribute data and I/O across multiple nodes. Monitoring WiredTiger cache metrics (pages read/written, bytes in/out, eviction rate, dirty pages) is key to understanding pressure points.</p><p>For a temporary relief or specific use cases, you might configure WiredTiger's internal parameters, like increasing the number of evictor threads or adjusting eviction thresholds, but this should be done cautiously and after thorough testing, as it can sometimes mask deeper issues.</p>",
    "good_code": "```yaml\n# /etc/mongod.conf (or equivalent configuration file)\n\nsystemLog:\n  destination: file\n  path: /var/log/mongodb/mongod.log\n  logAppend: true\n\nstorage:\n  dbPath: /var/lib/mongodb\n  journal:\n    enabled: true\n  engine: wiredTiger\n  wiredTiger:\n    engineConfig:\n      # Adjust cache size based on total system RAM and expected working set\n      # Rule of thumb: typically 50-70% of total RAM, leaving space for OS/other processes\n      # For 32GB RAM, 20GB might be a good starting point.\n      cacheSizeGB: 20\n      # Optional: Increase evictor threads if I/O is very fast but eviction lags\n      # Default is 1, increasing it can help only if the bottleneck is not disk I/O.\n      # wiredTigerConcurrentWriteTransactions: 256 (default, can be tuned cautiously)\n      # wiredTigerConcurrentReadTransactions: 256 (default, can be tuned cautiously)\n    # wiredTigerGlobalLimits:\n    #   # Advanced tuning, use with extreme caution\n    #   eviction_dirty_trigger: 5% # Default is 5%\n    #   eviction_dirty_target: 2%  # Default is 2%\n    #   eviction_trigger: 95%      # Default is 95%\n    #   eviction_target: 80%       # Default is 80%\n\nprocessManagement:\n  fork: true\n  pidFilePath: /var/run/mongodb/mongod.pid\n\nnet:\n  port: 27017\n  bindIp: 127.0.0.1\n\nreplication:\n  replSetName: rs0 # Example for replica sets\n```\n<p>The primary fix involves configuring the WiredTiger cache size. By setting <code>cacheSizeGB</code> appropriately (e.g., 20GB for a server with 32GB of RAM), you provide more working room for the database, reducing the frequency and intensity of evictions. This value should be carefully determined based on the system's total RAM, the size of your working set, and other processes running on the server. Other critical improvements would involve upgrading the underlying storage to high-performance SSDs (e.g., NVMe) and optimizing application-level write operations to be more efficient.</p>",
    "verification": "<p>Verifying the fix for WiredTiger cache eviction stalls involves continuous monitoring and analysis of MongoDB's internal metrics:</p><ol><li><b><code>db.serverStatus()</code> Output:</b> Focus on the <code>wiredTiger.cache</code> section. Look for reduced values in:<ul><li><code>pages queued for eviction</code></li><li><code>pages currently being written from cache</code></li><li><code>pages evicting btree pages</code></li><li><code>eviction sweeps and passes</code></li></ul><p>Conversely, look for increased <code>pages read into cache</code> and <code>pages written from cache</code> indicating efficient I/O, and ensure <code>bytes currently in cache</code> is stable and within expectations.</p></li><li><b><code>mongostat</code> and <code>mongotop</code>:</b> Use these tools to observe overall I/O patterns (<code>qr/qw</code> for queue reads/writes) and identify any collections or indexes under abnormal write pressure.</li><li><b>Operating System Metrics:</b> Monitor disk I/O (<code>iostat</code>, cloud provider metrics for IOPS/throughput), CPU utilization, and RAM usage. A successful fix should show lower disk I/O wait times and more consistent CPU usage.</li><li><b>Application Latency:</b> The most direct verification is a significant reduction in application-level write latency and improved overall database query performance, indicating that client requests are no longer stalling.</li><li><b>Load Testing:</b> Re-run the problematic write-heavy workloads and observe if the system maintains stable performance over extended periods without the latency spikes seen previously.</li></ol>",
    "date": "2026-06-02",
    "id": 1780368221,
    "type": "error"
});