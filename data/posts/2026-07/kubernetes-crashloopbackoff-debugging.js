window.onPostDataLoaded({
    "title": "Fixing Kubernetes CrashLoopBackOff: Deep Dive & Debugging",
    "slug": "kubernetes-crashloopbackoff-debugging",
    "language": "Kubernetes",
    "code": "CrashLoopBackOff",
    "tags": [
        "Kubernetes",
        "Docker",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p><code>CrashLoopBackOff</code> is one of the most common and frustrating statuses encountered by developers and operators in Kubernetes. It signifies that a pod is repeatedly starting, crashing, and then being restarted by the kubelet, only to crash again. Kubernetes implements an exponential back-off delay strategy to prevent overwhelming the node with constant restarts, which is why you see the 'BackOff' part. This status indicates a fundamental problem with the containerized application itself or its immediate environment within the pod.</p><p>Understanding the root cause is critical, as it can range from simple configuration errors to complex application bugs. The key is to systematically investigate the pod's lifecycle, its logs, events, and configuration against the expected behavior.</p>",
    "root_cause": "The container application inside the pod is terminating unexpectedly. Common reasons include: application bugs (unhandled exceptions, segmentation faults), incorrect command or arguments in the container entrypoint, missing environment variables or configuration files (ConfigMaps/Secrets not mounted), incorrect image tags, insufficient resource limits (OOMKilled), failed health checks (liveness probes), or issues with underlying storage/network dependencies.",
    "bad_code": "apiVersion: v1\nkind: Pod\nmetadata:\n  name: crashing-app\nspec:\n  containers:\n  - name: my-app\n    image: busybox:1.28 # Image might not have required tools or could be an old/non-existent tag\n    command: [\"sh\", \"-c\", \"echo 'Starting app'; sleep 5; exit 1\"] # Application intentionally exits with error after 5s\n    resources:\n      limits:\n        memory: \"10Mi\" # Potentially too low for some apps, though busybox is small\n        cpu: \"10m\"\n    # Missing liveness probe, or misconfigured one, allows crashes to loop without restart policy intervention\n",
    "solution_desc": "<p>Debugging a <code>CrashLoopBackOff</code> requires a systematic approach:</p><ol><li><b>Check Pod Status & Events:</b> Use <code>kubectl get pods</code> to see the status, then <code>kubectl describe pod &lt;pod-name&gt;</code> to get detailed events and status. Look for 'Last State' of the container. Events often reveal issues like <code>OOMKilled</code>, failed readiness/liveness probes, or image pull errors.</li><li><b>Examine Logs:</b> Use <code>kubectl logs &lt;pod-name&gt;</code> (and <code>kubectl logs -p &lt;pod-name&gt;</code> for previous container logs) to find application-level errors, stack traces, or configuration failures.</li><li><b>Verify Configuration:</b> Check the Pod's YAML manifest for correctness. Ensure image tags are valid, environment variables are correctly passed, and ConfigMaps/Secrets are properly mounted and accessible.</li><li><b>Check Commands and Arguments:</b> Ensure the <code>command</code> and <code>args</code> fields in the container definition are correct for your application's entrypoint.</li><li><b>Inspect Liveness/Readiness Probes:</b> Misconfigured probes (e.g., pointing to a wrong port, too aggressive timeouts) can cause healthy applications to be killed. Disable them temporarily for debugging if suspected.</li><li><b>Resource Constraints:</b> If <code>OOMKilled</code> is present in events, increase memory limits (<code>resources.limits.memory</code>).</li><li><b>Dependency Issues:</b> Ensure external services, databases, or network resources are reachable and correctly configured.</li></ol>",
    "good_code": "apiVersion: v1\nkind: Pod\nmetadata:\n  name: stable-app\nspec:\n  containers:\n  - name: my-app\n    image: busybox:latest # Ensure correct and existing image tag\n    command: [\"sh\", \"-c\", \"while true; do echo 'Application is running...'; sleep 5; done\"] # Application runs indefinitely\n    resources:\n      limits:\n        memory: \"64Mi\" # Sufficient memory limits\n        cpu: \"100m\"\n    livenessProbe: # Correctly configured liveness probe\n      exec:\n        command:\n        - sh\n        - -c\n        - echo 'Application process is still running' # Example check, could be a more robust health check script\n      initialDelaySeconds: 5\n      periodSeconds: 5\n      timeoutSeconds: 1\n      failureThreshold: 3\n",
    "verification": "<p>After applying a fix, monitor the pod's status using <code>kubectl get pods -w</code> to watch for the transition from <code>CrashLoopBackOff</code> to <code>Running</code>. Check <code>kubectl describe pod &lt;pod-name&gt;</code> to ensure no new errors appear in the events, specifically looking for `Container state: Running` and no restart count increments. Crucially, examine the logs with <code>kubectl logs &lt;pod-name&gt;</code> to confirm the application starts correctly and performs its expected functions without crashing. If the application exposes an endpoint, test its functionality. For critical applications, integrate automated health checks and monitoring tools to detect early signs of instability.</p>",
    "date": "2026-07-17",
    "id": 1784253001,
    "type": "error"
});