window.onPostDataLoaded({
    "title": "Fixing Rust Async Mutex Deadlocks Across Await Points",
    "slug": "rust-async-mutex-deadlocks-await-points",
    "language": "Rust",
    "code": "Deadlock / Send Sync Violation",
    "tags": [
        "Rust",
        "Backend",
        "Concurrency",
        "Error Fix"
    ],
    "analysis": "<p>In multi-threaded asynchronous Rust runtimes (like Tokio's multi-thread scheduler), holding a synchronous lock guard (e.g., <code>std::sync::MutexGuard</code>) across an <code>.await</code> point is a recipe for silent deadlocks and compilation failures. Because async tasks yield execution to the executor at <code>.await</code> points, holding a OS-level synchronous lock during thread suspension blocks other tasks on the same thread from acquiring the lock. Worse, if the task is rescheduled on a different OS thread after yielding, the compiler will emit a <code>Send</code> trait violation error because standard synchronous lock guards are not thread-safe to move across threads.</p>",
    "root_cause": "The standard library's `std::sync::MutexGuard` does not implement `Send` because it relies on OS-thread-specific unlocking mechanisms. When an `.await` point is encountered, the compiler generates a state machine representing the async task. If a non-`Send` type like `std::sync::MutexGuard` is held across that `.await` point, the entire `Future` ceases to be `Send`. Consequently, it cannot be spawned onto Tokio's multi-threaded work-stealing executor. Alternatively, if forced via single-threaded executors or unsafe paths, it blocks the executor thread completely when suspended, preventing the very task responsible for releasing the resource from executing.",
    "bad_code": "use std::sync::Mutex;\nuse std::sync::Arc;\n\nstruct SharedState {\n    counter: Mutex<u32>,\n}\n\nasync fn process_and_increment(state: Arc<SharedState>) {\n    // Acquiring std::sync::Mutex inside an async function\n    let mut lock = state.counter.lock().unwrap();\n    *lock += 1;\n    \n    // CRITICAL BUG: .await point while holding std::sync::MutexGuard\n    // This will cause a compilation error if spawned on a multi-threaded runtime\n    // because the MutexGuard is not Send.\n    some_async_io_call().await;\n    \n    println!(\"Current count: {}\", *lock);\n} // Lock dropped here",
    "solution_desc": "To fix this, you have two primary architectural options: 1) Minimize the lock's scope using a code block or explicit drop so the synchronous guard is dropped before the `.await` point, or 2) Migrate to Tokio's asynchronous mutex (`tokio::sync::Mutex`). The async mutex does not block the underlying OS thread when a task yields, and its guard implements `Send`, making it safe to carry across `.await` boundaries. Use synchronous mutexes inside block scopes for CPU-bound modifications, and reserve async mutexes for protecting resources across slow I/O operations.",
    "good_code": "use std::sync::Mutex;\nuse std::sync::Arc;\n// Or use tokio::sync::Mutex if the lock must be held across the await\n\nstruct SharedState {\n    counter: Mutex<u32>,\n}\n\nasync fn process_and_increment_fixed(state: Arc<SharedState>) {\n    // Option A: Limit scope so the lock is dropped BEFORE the await point\n    {\n        let mut lock = state.counter.lock().unwrap();\n        *lock += 1;\n    } // Lock is explicitly dropped here\n\n    // Safe to yield here; no lock is held\n    some_async_io_call().await;\n}\n\n// Option B: Using Tokio's Async Mutex when lock must persist across .await\nuse tokio::sync::Mutex as AsyncMutex;\n\nstruct AsyncSharedState {\n    counter: AsyncMutex<u32>,\n}\n\nasync fn process_with_async_lock(state: Arc<AsyncSharedState>) {\n    // Tokio's lock is async and its guard is Send\n    let mut lock = state.counter.lock().await;\n    *lock += 1;\n    \n    some_async_io_call().await; // Safe! No thread blocking or compiler errors.\n    \n    println!(\"Current count: {}\", *lock);\n}",
    "verification": "Compile the code using `cargo build`. If using Tokio's async lock or scoped synchronous locks, the compiler will successfully verify the `Send` bounds of the spawned task. Additionally, run your test suite with the environment variable `RUSTFLAGS=\"-Zsanitizer=thread\"` or use `tokio-console` to track task wait-times, ensuring no task remains suspended indefinitely while holding a lock.",
    "date": "2026-07-18",
    "id": 1784369396,
    "type": "error"
});