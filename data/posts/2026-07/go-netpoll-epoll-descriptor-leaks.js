window.onPostDataLoaded({
    "title": "Resolving Go netpoll Epoll Descriptor Leaks",
    "slug": "go-netpoll-epoll-descriptor-leaks",
    "language": "Go",
    "code": "Epoll Descriptor Leak",
    "tags": [
        "Go",
        "Networking",
        "Epoll",
        "File Descriptors",
        "Backend",
        "Concurrency",
        "Error Fix"
    ],
    "analysis": "<p>Go's <code>net</code> package heavily relies on the <code>netpoll</code> mechanism, which uses OS-level I/O multiplexing like <code>epoll</code> on Linux, <code>kqueue</code> on macOS/BSD, or <code>IOCP</code> on Windows. A \"descriptor leak\" refers to a situation where file descriptors (FDs), specifically epoll descriptors, are opened but not properly closed, leading to a gradual exhaustion of the per-process or system-wide FD limit. Under heavy connection churning (frequent opening and closing of many network connections), if the underlying <code>netpoll</code> mechanism or its interaction with Go's runtime isn't meticulously handled, FDs can accumulate. This often manifests when custom network programming, CGO, or low-level socket manipulations interfere with Go's default <code>netpoll</code> lifecycle management. While Go's runtime is generally robust, specific scenarios like premature socket closures, improper <code>syscall</code> usage, or interactions with libraries that manage their own FDs can bypass Go's cleanup, leaving epoll handles open. This silently degrades system stability, eventually causing \"too many open files\" errors (<code>EMFILE</code>) for new connections.</p>",
    "root_cause": "Incorrect resource management, typically involving direct `syscall` interaction with epoll or similar primitives, where `epoll_create` or `socket` calls are made without corresponding `close` calls, especially in error paths or rapid connection lifecycles. Alternatively, subtle bugs in Go's `net` package (less common now, but possible in older versions or specific OS interactions) could lead to this.",
    "bad_code": "```go\n// This is a simplified, illustrative example of how a leak *could* theoretically happen\n// if Go's net package wasn't handling closures correctly or if custom CGO/syscall\n// code was poorly integrated. In modern Go, this specific pattern is handled\n// by the runtime, but serves to illustrate the concept.\n\npackage main\n\nimport (\n\t\"fmt\"\n\t\"net\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc main() {\n\t// Simulate a client that frequently opens and closes connections without proper cleanup\n\t// (Note: Go's net.DialContext is usually safe, this is for illustration)\n\tvar wg sync.WaitGroup\n\tnumConnections := 10000 // High number to stress FD limits\n\n\tfor i := 0; i < numConnections; i++ {\n\t\twg.Add(1)\n\t\tgo func(id int) {\n\t\t\tdefer wg.Done()\n\t\t\tconn, err := net.Dial(\"tcp\", \"localhost:8080\") // Assuming a server is running\n\t\t\tif err != nil {\n\t\t\t\t// In a real scenario, this error path might omit a close on an internally created FD\n\t\t\t\tfmt.Printf(\"Client %d: Error dialing: %v\\n\", id, err)\n\t\t\t\treturn\n\t\t\t}\n\t\t\t// Simulate some work\n\t\t\ttime.Sleep(10 * time.Millisecond)\n\t\t\t// In this *bad_code* scenario, assume conn.Close() isn't always called\n\t\t\t// or an internal epoll descriptor used by net.Dial is leaked *before* conn is returned\n\t\t\t// For example, if a custom network library wraps `socket` and `epoll_create`\n\t\t\t// but fails to close them on certain error paths.\n\t\t\t// conn.Close() // This line *should* be called, but imagine it's missed or an internal FD is leaked.\n\t\t}(i)\n\t}\n\n\twg.Wait()\n\tfmt.Println(\"Simulated connection churning completed.\")\n\t// Monitor FDs here - `lsof -p <PID>`\n\ttime.Sleep(10 * time.Second) // Keep process alive for inspection\n}\n```",
    "solution_desc": "The primary solution is rigorous resource management. For Go applications:\n1.  **Always `defer conn.Close()`:** Ensure that every network connection (`net.Conn`, `net.Listener`, `os.File`, etc.) is properly closed, typically using `defer conn.Close()` immediately after it's opened. Go's runtime is designed to handle the underlying epoll descriptor cleanup when `net.Conn.Close()` is called.\n2.  **Review CGO/`syscall` Usage:** If your application uses CGO or direct `syscall`s for networking, meticulously audit the code for all `socket`, `epoll_create`, `open`, etc., calls and ensure there are corresponding `close` calls in all possible execution paths, especially error paths. Leverage `runtime.SetFinalizer` or similar patterns for C-allocated resources if direct `close` calls are hard to guarantee.\n3.  **Update Go Version:** Ensure you are running a recent stable version of Go. Older versions might have had subtle bugs in `netpoll` cleanup, which have likely been patched.\n4.  **Increase OS FD Limit (Temporary/Mitigation):** While not a fix, increasing the `ulimit -n` for the process or `/proc/sys/fs/file-max` system-wide can temporarily mask the problem and buy time for a proper fix. This doesn't solve the leak but postpones the crash.\n5.  **Use `Context` for Network Operations:** When dealing with `net.Dialer` or other network primitives that accept `Context`, use it to set timeouts and cancellation signals. This helps in gracefully closing connections that get stuck or are no longer needed, preventing potential leaks in edge cases.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"net\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc main() {\n\tvar wg sync.WaitGroup\n\tnumConnections := 10000 // High number for stress testing\n\n\tfor i := 0; i < numConnections; i++ {\n\t\twg.Add(1)\n\t\tgo func(id int) {\n\t\t\tdefer wg.Done()\n\n\t\t\t// Use context with timeout for robust connection handling\n\t\t\tctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)\n\t\t\tdefer cancel()\n\n\t\t\tconn, err := (&net.Dialer{}).DialContext(ctx, \"tcp\", \"localhost:8080\")\n\t\t\tif err != nil {\n\t\t\t\tfmt.Printf(\"Client %d: Error dialing: %v\\n\", id, err)\n\t\t\t\treturn\n\t\t\t}\n\t\t\t// Crucial: Ensure connection is closed\n\t\t\tdefer conn.Close()\n\n\t\t\t// Simulate some work\n\t\t\t_, err = conn.Write([]byte(fmt.Sprintf(\"Hello from client %d!\", id)))\n\t\t\tif err != nil {\n\t\t\t\tfmt.Printf(\"Client %d: Error writing: %v\\n\", id, err)\n\t\t\t\treturn\n\t\t\t}\n\n\t\t\tbuf := make([]byte, 1024)\n\t\t\t_, err = conn.Read(buf) // Simulate reading response\n\t\t\tif err != nil {\n\t\t\t\treturn\n\t\t\t}\n\n\t\t}(i)\n\t}\n\n\twg.Wait()\n\tfmt.Println(\"Simulated connection churning completed. All connections properly closed.\")\n\t// Allow time for OS to fully release FDs if not immediate.\n\ttime.Sleep(5 * time.Second)\n\tfmt.Println(\"Process exiting.\")\n}\n```",
    "verification": "1.  **Monitor File Descriptors:** Use `lsof -p <PID>` or `ls /proc/<PID>/fd | wc -l` (on Linux) to monitor the number of open file descriptors for the Go process. Run the application under heavy connection churning.\n2.  **Baseline & Stress Test:** Establish a baseline FD count under idle conditions. Then, run the application with the `bad_code` (if a controlled leak can be reproduced, e.g., by omitting `conn.Close()` on some paths in a custom network stack) and observe a continuous increase in FDs even after connections are supposedly closed.\n3.  **Apply Fix & Re-test:** Implement the `good_code` (ensuring `defer conn.Close()` and proper context handling). Rerun the stress test. The FD count should stabilize at a reasonable number proportional to concurrently open connections, eventually returning close to the baseline after the connection churning subsides.\n4.  **Error Log Check:** Ensure no \"too many open files\" (`EMFILE`) errors appear in the application logs or `dmesg`.",
    "date": "2026-07-06",
    "id": 1783323320,
    "type": "error"
});