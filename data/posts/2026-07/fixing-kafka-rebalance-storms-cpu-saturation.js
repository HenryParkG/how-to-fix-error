window.onPostDataLoaded({
    "title": "Fixing Kafka Rebalance Storms under CPU Saturation",
    "slug": "fixing-kafka-rebalance-storms-cpu-saturation",
    "language": "Java",
    "code": "CommitFailedException",
    "tags": [
        "Kafka",
        "Java",
        "Kubernetes",
        "Error Fix"
    ],
    "analysis": "<p>Under heavy CPU saturation, JVM garbage collection pauses or thread scheduling delays prevent the Kafka Consumer's heartbeat thread from executing within the configured <code>heartbeat.interval.ms</code>. Alternatively, the main processing thread may take longer than <code>max.poll.interval.ms</code> to process a single batch of records. When this happens, the Kafka Group Coordinator marks the consumer as dead, evicts it from the consumer group, and triggers a rebalance.</p><p>As other consumers pause to re-join the group, the overall consumption throughput drops to zero. Meanwhile, the unassigned partitions are reassigned to other saturated consumers, which immediately fail for the same reason. This creates a destructive cascading feedback loop known as a <strong>rebalance storm</strong>, bringing your real-time processing pipeline to a halt.</p>",
    "root_cause": "The consumer processing loop blocks the heartbeat thread or exceeds the max poll interval under CPU resource constraints, causing the broker's group coordinator to initiate recursive group evictions and partition reassignments.",
    "bad_code": "import org.apache.kafka.clients.consumer.ConsumerConfig;\nimport org.apache.kafka.clients.consumer.KafkaConsumer;\nimport java.util.Properties;\n\npublic class SaturatedConsumer {\n    public static Properties getProperties() {\n        Properties props = new Properties();\n        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, \"localhost:9092\");\n        props.put(ConsumerConfig.GROUP_ID_CONFIG, \"heavy-ingestion-group\");\n        // BAD: Processing heavy workloads with a high batch size and low poll timeout\n        // Under CPU spikes, processing 1000 records takes longer than 30 seconds\n        props.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG, \"1000\");\n        props.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG, \"30000\");\n        props.put(ConsumerConfig.SESSION_TIMEOUT_MS_CONFIG, \"10000\");\n        props.put(ConsumerConfig.HEARTBEAT_INTERVAL_MS_CONFIG, \"3000\");\n        return props;\n    }\n}",
    "solution_desc": "To fix this, decouple record ingestion from record processing using an internal thread pool (ExecutorService). This allows the Kafka consumer polling loop to remain active and uninterrupted, ensuring heartbeats are consistently sent back to the coordinator even if the processing pipeline is bottlenecked or saturated. Additionally, tune max.poll.records down and increase max.poll.interval.ms to give the worker threads sufficient headroom.",
    "good_code": "import org.apache.kafka.clients.consumer.ConsumerConfig;\nimport org.apache.kafka.clients.consumer.KafkaConsumer;\nimport java.util.Properties;\nimport java.util.concurrent.ArrayBlockingQueue;\nimport java.util.concurrent.ThreadPoolExecutor;\nimport java.util.concurrent.TimeUnit;\n\npublic class ResilientConsumer {\n    public static Properties getProperties() {\n        Properties props = new Properties();\n        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, \"localhost:9092\");\n        props.put(ConsumerConfig.GROUP_ID_CONFIG, \"heavy-ingestion-group\");\n        \n        // GOOD: Process smaller batches and allow up to 5 minutes before group eviction\n        props.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG, \"100\");\n        props.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG, \"300000\"); // 5 minutes\n        props.put(ConsumerConfig.SESSION_TIMEOUT_MS_CONFIG, \"45000\");     // Heartbeat window\n        props.put(ConsumerConfig.HEARTBEAT_INTERVAL_MS_CONFIG, \"15000\");\n        return props;\n    }\n\n    // Decoupled processing pool helper\n    public static ThreadPoolExecutor createExecutor() {\n        return new ThreadPoolExecutor(\n            4, 8, 60L, TimeUnit.SECONDS,\n            new ArrayBlockingQueue<>(500),\n            new ThreadPoolExecutor.CallerRunsPolicy() // Throttle poll loop if pool is full\n        );\n    }\n}",
    "verification": "Deploy the updated consumer code. Run a synthetic CPU loader (e.g., stress-ng --cpu 4) inside your Kubernetes container. Monitor Kafka JMX metrics: 'join-rate' should remain at 0, 'heartbeat-rate' must remain stable, and check the consumer logs to verify that no 'CommitFailedException' or unexpected rebalance events occur during the CPU saturation period.",
    "date": "2026-07-19",
    "id": 1784447989,
    "type": "error"
});