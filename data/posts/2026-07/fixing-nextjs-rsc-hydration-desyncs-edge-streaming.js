window.onPostDataLoaded({
    "title": "Fixing Next.js RSC Hydration Desyncs in Edge Streaming",
    "slug": "fixing-nextjs-rsc-hydration-desyncs-edge-streaming",
    "language": "TypeScript / Next.js",
    "code": "Hydration Mismatch / Content Desync",
    "tags": [
        "Next.js",
        "React",
        "TypeScript",
        "Error Fix"
    ],
    "analysis": "<p>When rendering React Server Components (RSC) streamed from Edge runtimes, hydration desync errors occur if the HTML generated by the Edge server differs from the initial DOM tree constructed during client-side hydration. Non-deterministic data like timestamps, locale settings, random tokens, or browser-only APIs (`window`, `localStorage`) evaluated directly inside Server Components produce dynamic values that fail React's reconciliation pass on the client.</p>",
    "root_cause": "Rendering non-deterministic or dynamic runtime values inside streamed Server Components without deferring client-specific state initialization until after component mounting.",
    "bad_code": "// app/components/DynamicHeader.tsx\nexport default function DynamicHeader() {\n  // Bug: Date.now() executes at server render time, causing hydration mismatch on client\n  const timestamp = new Date().toISOString();\n  return <header>Rendered at: {timestamp}</header>;\n}",
    "solution_desc": "Isolate dynamic client-only state into Client Components (`'use client'`). Initialize dynamic state within a `useEffect` hook or handle initial mounting state flags to ensure server HTML matches initial client hydration HTML before dynamic rendering occurs.",
    "good_code": "'use client';\n\nimport { useState, useEffect } from 'react';\n\nexport default function DynamicHeader() {\n  const [timestamp, setTimestamp] = useState<string | null>(null);\n\n  useEffect(() => {\n    setTimestamp(new Date().toISOString());\n  }, []);\n\n  return (\n    <header>\n      Rendered at: {timestamp ?? 'Loading...'}\n    </header>\n  );\n}",
    "verification": "Run the application in development mode (`next dev`) and inspect browser console logs during edge dynamic streaming to confirm the absence of React Hydration Mismatch warning warnings (`Hydration failed because the initial UI does not match...`).",
    "date": "2026-07-26",
    "id": 1785053345,
    "type": "error"
});