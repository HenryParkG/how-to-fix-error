window.onPostDataLoaded({
    "title": "Mitigating Kubernetes Pod Eviction Storms from Node Pressure",
    "slug": "mitigating-kubernetes-pod-eviction-storms-node-pressure",
    "language": "Kubernetes",
    "code": "PodEvictionStorm",
    "tags": [
        "Kubernetes",
        "Infra",
        "Containerization",
        "Error Fix"
    ],
    "analysis": "<p>Kubernetes nodes can experience pressure due to excessive resource consumption, primarily memory or disk space. When a node reaches certain thresholds (defined by the kubelet's eviction manager), it initiates pod evictions to reclaim resources and maintain node stability. While individual evictions are a normal part of Kubernetes' self-healing, 'eviction storms' occur when multiple pods are evicted across several nodes, often in a cascading fashion. This leads to service instability, unavailability, and applications being constantly rescheduled. These storms signify systemic issues in resource management, application behavior, or cluster configuration that must be addressed to ensure a robust and reliable Kubernetes environment.</p>",
    "root_cause": "Pod eviction storms are fundamentally caused by nodes running out of critical resources, triggering the kubelet's eviction manager. The primary root causes include: <ul><li><b>Inadequate Resource Requests/Limits:</b> Pods are scheduled without sufficient resource requests, leading to overcommitment, or they burst beyond their limits, consuming too much node memory/disk.</li><li><b>Memory Leaks:</b> Applications within pods have memory leaks, gradually consuming more memory until the node is pressured.</li><li><b>High Disk Usage:</b> Pods generate excessive logs, temporary files, or cache data, filling the node's disk. This can also include issues with container runtime disk usage or filesystem inodes.</li><li><b>Misconfigured Kubelet Eviction Thresholds:</b> Default eviction thresholds might be too aggressive or too lenient, leading to either premature evictions or node instability before evictions occur.</li><li><b>Noisy Neighbors:</b> One or a few resource-hungry applications consume disproportionate amounts of resources, starving others.</li><li><b>Lack of Monitoring and Alerting:</b> Insufficient visibility into node and pod resource usage prevents proactive intervention.</li></ul>",
    "bad_code": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: problematic-app\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: problematic-app\n  template:\n    metadata:\n      labels:\n        app: problematic-app\n    spec:\n      containers:\n      - name: web-server\n        image: nginx:latest\n        # Problem: No resource requests or limits defined\n        # This allows the pod to consume as much memory/CPU as available,\n        # potentially starving other pods or pressuring the node.\n        # If this container starts consuming a lot of memory/disk (e.g., serving huge files),\n        # it can easily lead to node pressure and eviction.\n        ports:\n        - containerPort: 80\n      # Also consider if initContainers or sidecars could be resource hungry\n```",
    "solution_desc": "Mitigate eviction storms through a multi-pronged approach focusing on resource management, application hygiene, and cluster configuration. First, meticulously define <code>resources.requests</code> and <code>resources.limits</code> for all containers. Requests should reflect the minimum required, and limits should cap maximum consumption, preventing individual pods from monopolizing resources. Use tools for rightsizing resources (e.g., Kube-Advisor, VPA). Implement robust monitoring of node metrics (memory, disk, inode usage) and pod resource consumption. Identify and fix memory leaks in applications. Implement log rotation and manage persistent storage effectively to prevent disk filling. Adjust kubelet eviction thresholds (<code>kubeReserved</code>, <code>systemReserved</code>, <code>eviction-hard</code>/<code>soft</code>) carefully to balance node stability and workload availability. Utilize Horizontal Pod Autoscaling (HPA) to scale out based on resource usage and PodDisruptionBudgets (PDBs) to ensure minimum replica counts during voluntary disruptions.",
    "good_code": "```yaml\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: well-behaved-app\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: well-behaved-app\n  template:\n    metadata:\n      labels:\n        app: well-behaved-app\n    spec:\n      containers:\n      - name: web-server\n        image: nginx:latest\n        resources:\n          requests:\n            memory: \"128Mi\" # Request a minimum amount of memory\n            cpu: \"100m\"    # Request a minimum amount of CPU\n          limits:\n            memory: \"256Mi\" # Cap memory usage to prevent node pressure\n            cpu: \"200m\"    # Cap CPU usage to prevent resource hogging\n        ports:\n        - containerPort: 80\n        volumeMounts:\n        - name: nginx-cache\n          mountPath: /var/cache/nginx\n      volumes:\n      - name: nginx-cache\n        emptyDir: {}\n      # Optional: Add nodeSelector, tolerations, or affinity for better scheduling\n      # Optional: Implement an efficient log rotation strategy at application level \n      # or via a sidecar/CSI driver for logs generated to mounted volumes.\n      # (Example: emptyDir for cache data, but consider PV for logs if persistent)\n\n# Example of Kubelet config snippet (applied via Kubeadm or cloud provider specific configs)\n# Ensure enough reserved memory/disk for system processes and kubelet itself\n# kubeReserved: { cpu: 200m, memory: 512Mi }\n# systemReserved: { cpu: 100m, memory: 256Mi }\n# evictionHard:\n#   memory.available: \"100Mi\"\n#   nodefs.available: \"10%\"\n#   nodefs.inodesFree: \"5%\"\n```",
    "verification": "Continuously monitor node health and resource utilization using Kubernetes dashboards (e.g., Grafana, Prometheus). Observe <code>kubectl describe node &lt;node-name&gt;</code> for `Allocatable` and `Capacity` resources, and check events for `Evicted` status. Analyze Kubelet logs (<code>journalctl -u kubelet</code>) for eviction manager decisions. Implement alerts for high memory/disk usage on nodes and pods. After applying resource limits and addressing application issues, verify that no new eviction events occur, pods remain stable, and services maintain their availability and performance SLAs.",
    "date": "2026-07-06",
    "id": 1783343101,
    "type": "error"
});