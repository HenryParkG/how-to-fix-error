window.onPostDataLoaded({
    "title": "Fixing Elixir GenServer Mailbox Bloat & Memory Exhaustion",
    "slug": "elixir-genserver-mailbox-bloat-memory-exhaustion",
    "language": "Elixir",
    "code": "GenServer Mailbox Bloat",
    "tags": [
        "Rust",
        "Backend",
        "Elixir",
        "Erlang",
        "Error Fix"
    ],
    "analysis": "<p>In Elixir and the Erlang VM (BEAM), processes communicate via asynchronous message passing. Each process has an unbounded, sequential FIFO mailbox. When a process receives a message, it is stored in its mailbox until the process processes it using <code>receive</code> (or through the <code>handle_cast/2</code> and <code>handle_call/3</code> callbacks in a GenServer).</p><p>If a GenServer receives messages at a rate significantly higher than its processing execution time, the mailbox accumulates messages. This phenomenon, known as Mailbox Bloat, causes linear memory consumption. Because the BEAM garbage collector must scan the mailbox during collections, a bloated mailbox leads to massive CPU overhead, latency degradation, and eventually triggers the Linux OOM killer or BEAM crash dumps due to memory exhaustion.</p>",
    "root_cause": "The root cause of mailbox bloat is the asynchronous, unbounded nature of Elixir's default message-passing model combined with synchronous/slow blocking operations inside a single GenServer's message loop (e.g., waiting on external HTTP requests, heavy database operations, or intensive CPU computations) without backpressure controls or worker pooling.",
    "bad_code": "defmodule HighThroughputIngestor do\n  use GenServer\n\n  def start_link(opts) do\n    GenServer.start_link(__MODULE__, :ok, opts)\n  end\n\n  def init(:ok) do\n    {:ok, %{}}\n  end\n\n  # Asynchronous cast allows caller to continue immediately,\n  # building an unbounded queue in the GenServer mailbox\n  def handle_cast({:process_payload, payload}, state) do\n    # Simulate slow, blocking database write or third-party API call\n    Process.sleep(50)\n    IO.puts(\"Processed payload #{inspect(payload)}\")\n    {:noreply, state}\n  end\nend",
    "solution_desc": "To fix GenServer mailbox bloat, decoupling consumption from processing is required. Implement worker pooling (via `NimblePool` or `Poolboy`) or dynamic task supervision (`Task.Supervisor`) to process payloads concurrently outside the GenServer's main thread loop. Additionally, replace unbounded `cast` calls with bounded backpressure mechanisms or rate-limiting patterns such as `GenStage` or `Broadway` to throttle incoming traffic when the consumer is overloaded.",
    "good_code": "defmodule ConcurrencyIngestor do\n  use GenServer\n  require Logger\n\n  # We use a Dynamic Supervisor or Task Supervisor to execute parallel work\n  @task_supervisor ConcurrencyIngestor.TaskSupervisor\n\n  def start_link(opts) do\n    GenServer.start_link(__MODULE__, :ok, opts)\n  end\n\n  def init(:ok) do\n    {:ok, %{}}\n  end\n\n  # Client API: Expose casting, but use backpressure check before queuing\n  def process_payload(pid, payload) do\n    # Check message queue length of the target process to prevent overload\n    {:message_queue_len, len} = Process.info(pid, :message_queue_len)\n    \n    if len > 5000 do\n      Logger.warning(\"Backpressure triggered! Drop payload or return 429\")\n      {:error, :overloaded}\n    else\n      GenServer.cast(pid, {:process_payload, payload})\n    end\n  end\n\n  # Handle cast asynchronously by spawning unlinked tasks\n  def handle_cast({:process_payload, payload}, state) do\n    # Offload the blocking task to a Task.Supervisor, freeing the GenServer mailbox immediately\n    Task.Supervisor.start_child(@task_supervisor, fn ->\n      perform_blocking_io(payload)\n    end)\n\n    {:noreply, state}\n  end\n\n  defp perform_blocking_io(payload) do\n    # Execute intensive operation outside of GenServer\n    Process.sleep(50) \n    Logger.info(\"Successfully processed payload: #{inspect(payload)}\")\n  end\nend\n\n# In your application supervisor startup:\n# children = [\n#   {Task.Supervisor, name: ConcurrencyIngestor.TaskSupervisor},\n#   {ConcurrencyIngestor, name: ConcurrencyIngestor}\n# ]",
    "verification": "Verify the fix by running load tests using a tool like k6. Monitor the Erlang process mailbox queue size using `:erlang.process_info(pid, :message_queue_len)` or Observer. Confirm that the mailbox length remains flat, processing tasks scale horizontally across CPU cores, and BEAM VM memory footprint stays stable.",
    "date": "2026-07-17",
    "id": 1784274775,
    "type": "error"
});