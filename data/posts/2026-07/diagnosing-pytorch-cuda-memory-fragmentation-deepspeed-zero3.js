window.onPostDataLoaded({
    "title": "Diagnosing PyTorch CUDA Mem Fragmentation with ZeRO-3",
    "slug": "diagnosing-pytorch-cuda-memory-fragmentation-deepspeed-zero3",
    "language": "Python, PyTorch, DeepSpeed",
    "code": "CUDAMemoryError, Fragmentation",
    "tags": [
        "PyTorch",
        "DeepSpeed",
        "CUDA",
        "GPU",
        "Distributed Training",
        "Python",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Training large language models (LLMs) often necessitates distributed training frameworks like DeepSpeed, particularly with its ZeRO (Zero Redundancy Optimizer) stages. ZeRO-3, in particular, shards all model states (parameters, gradients, optimizer states) across GPUs, dramatically reducing per-GPU memory footprint. While highly effective at enabling larger models, ZeRO-3's dynamic nature\u2014where model parameters are moved to/from host memory or between GPUs on-demand\u2014can exacerbate CUDA memory fragmentation issues.</p><p>PyTorch's CUDA memory allocator uses a caching mechanism to reuse allocated memory blocks, which generally improves performance. However, when varied-sized tensors are frequently allocated and deallocated (common in dynamic workloads, attention mechanisms, or when ZeRO-3 shuffles model states), the allocator can struggle to find contiguous blocks large enough for new requests, even if the total free memory is ample. This leads to fragmentation, manifesting as 'out of memory' errors (<code>CUDA out of memory. Tried to allocate X GiB</code>) despite <code>nvidia-smi</code> showing significant free VRAM. The problem is not lack of total memory, but lack of *contiguous* memory.</p><p>Diagnosing this requires more than just observing total memory, necessitating introspection into the allocator's internal state and identifying problematic allocation patterns.</p>",
    "root_cause": "The core root cause is the interaction between PyTorch's caching CUDA memory allocator and the dynamic memory access patterns introduced by DeepSpeed ZeRO-3. ZeRO-3 frequently offloads and reloads model parameters, gradients, and optimizer states, which often come in varying sizes. This leads to a 'swiss cheese' effect in GPU memory: many small, non-contiguous free blocks intermingled with allocated blocks. When a large contiguous block is requested (e.g., for a new intermediate activation, or a large unsharded parameter), the allocator cannot find it, even if the sum of all free blocks is sufficient. This is compounded by inconsistent tensor shapes across mini-batches or stages of a model.",
    "bad_code": "```python\nimport torch\nimport deepspeed\n\n# Standard training loop without fragmentation awareness\nclass MyModel(torch.nn.Module):\n    # ... model definition ...\n\nmodel = MyModel().cuda()\noptimizer = torch.optim.Adam(model.parameters())\n\nconfig_params = {\n    \"train_batch_size\": 8,\n    \"fp16\": {\"enabled\": True},\n    \"zero_optimization\": {\n        \"stage\": 3,\n        \"offload_optimizer\": {\"device\": \"cpu\", \"pin_memory\": True},\n        \"offload_param\": {\"device\": \"cpu\", \"pin_memory\": True}\n    }\n}\n\nmodel_engine, optimizer, _, _ = deepspeed.initialize(\n    model=model, optimizer=optimizer, config_params=config_params\n)\n\nfor epoch in range(num_epochs):\n    for batch in dataloader:\n        inputs, labels = batch\n        inputs = inputs.cuda() # May vary in shape/size\n        labels = labels.cuda()\n\n        outputs = model_engine(inputs)\n        loss = criterion(outputs, labels)\n\n        model_engine.backward(loss)\n        model_engine.step()\n        # Potentially long-running operations or dynamic tensor ops here\n        # without explicit memory management\n\n# This pattern, especially with dynamic inputs, can lead to fragmentation over time.\n```",
    "solution_desc": "Mitigating CUDA memory fragmentation under ZeRO-3 requires a multi-pronged approach:\n1.  **Reduce Dynamic Allocations:** Try to standardize tensor shapes as much as possible, or pad inputs to a common maximum length. Minimize operations that frequently allocate and deallocate large, varied-sized tensors within the forward/backward pass.\n2.  **Strategic `torch.cuda.empty_cache()`:** While generally discouraged for performance, in specific scenarios (e.g., between epochs if batch sizes significantly vary, or after a particularly memory-intensive phase), calling `torch.cuda.empty_cache()` can coalesce fragmented memory. Use sparingly.\n3.  **DeepSpeed Configuration Tuning:** Experiment with `gradient_accumulation_steps` to reduce the number of micro-batches and potentially smooth out memory patterns. Consider `zero_optimization.offload_optimizer.pin_memory` and `offload_param.pin_memory` for faster host-to-device transfers, which might indirectly reduce fragmentation pressure. Adjust `zero_optimization.overlap_comm` settings.\n4.  **Memory Profiling:** Use PyTorch's built-in `torch.cuda.memory_summary()` with `device=None` to get a detailed breakdown of memory usage, including fragmentation metrics. NVIDIA's `nvprof` or `nsight systems` can provide deeper insights into CUDA kernel launches and memory events.\n5.  **Re-ordering Operations:** Sometimes, re-ordering operations to group similar-sized allocations together can help the allocator.\n6.  **Increase Micro-Batch Size:** If possible, increase the `train_micro_batch_size_per_gpu` in DeepSpeed config. Larger, fewer allocations are often better than many small ones.",
    "good_code": "```python\nimport torch\nimport deepspeed\n\n# ... (Model and optimizer definition as above) ...\n\nconfig_params = {\n    \"train_batch_size\": 8,\n    \"gradient_accumulation_steps\": 4, # Example: increased accumulation\n    \"fp16\": {\"enabled\": True},\n    \"zero_optimization\": {\n        \"stage\": 3,\n        \"offload_optimizer\": {\"device\": \"cpu\", \"pin_memory\": True},\n        \"offload_param\": {\"device\": \"cpu\", \"pin_memory\": True}\n    },\n    # Other relevant DeepSpeed config options\n}\n\nmodel_engine, optimizer, _, _ = deepspeed.initialize(\n    model=model, optimizer=optimizer, config_params=config_params\n)\n\n# Helper to print memory summary\ndef print_gpu_memory_summary(stage):\n    print(f\"\\n--- GPU Memory Summary ({stage}) ---\")\n    print(torch.cuda.memory_summary(device=None, abbreviated=False))\n    print(\"-------------------------------------\\n\")\n\nfor epoch in range(num_epochs):\n    # Optional: Clear cache at epoch start if memory usage varies wildly per epoch\n    # print(f\"Clearing CUDA cache at epoch {epoch} start...\")\n    # torch.cuda.empty_cache()\n    # print_gpu_memory_summary(f\"Epoch {epoch} Start after cache clear\")\n\n    for batch_idx, batch in enumerate(dataloader):\n        inputs, labels = batch\n        # Ensure inputs are of consistent shape if possible, or pad them\n        inputs = inputs.cuda() # Example: inputs.pad_to_max_length().cuda()\n        labels = labels.cuda()\n\n        outputs = model_engine(inputs)\n        loss = criterion(outputs, labels)\n\n        model_engine.backward(loss)\n        # Step only after accumulation\n        if (batch_idx + 1) % config_params[\"gradient_accumulation_steps\"] == 0:\n            model_engine.step()\n            model_engine.zero_grad()\n            # Optional: Call empty_cache after step if large temporary buffers were used\n            # torch.cuda.empty_cache()\n\n    # Optional: More aggressive cache clearing at epoch end\n    # print(f\"Clearing CUDA cache at epoch {epoch} end...\")\n    # torch.cuda.empty_cache()\n    # print_gpu_memory_summary(f\"Epoch {epoch} End after cache clear\")\n\n# Final memory summary\nprint_gpu_memory_summary(\"End of Training\")\n```",
    "verification": "The primary verification method is to use PyTorch's `torch.cuda.memory_summary(device=None, abbreviated=False)`. Pay close attention to the `allocated_bytes.fragmentation_ratio` and `reserved_bytes.fragmentation_ratio` metrics. A high fragmentation ratio (e.g., above 20-30%) even with significant free memory indicates fragmentation. Monitor memory usage with `nvidia-smi -l 1` during training to observe spikes and overall trends. For deeper analysis, use `nvprof --profile-from-start off --print-gpu-trace python your_script.py` or `nsight systems` to visualize CUDA kernel launches and memory events, which can pinpoint specific operations causing fragmentation. Experiment with different DeepSpeed configurations and observe the `memory_summary` output for improvements.",
    "date": "2026-07-10",
    "id": 1783665347,
    "type": "error"
});