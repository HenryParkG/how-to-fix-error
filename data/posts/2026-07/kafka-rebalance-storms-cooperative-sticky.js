window.onPostDataLoaded({
    "title": "Fixing Kafka Rebalance Storms in Cooperative Sticky Assignor",
    "slug": "kafka-rebalance-storms-cooperative-sticky",
    "language": "Java",
    "code": "RebalanceStorm",
    "tags": [
        "Kafka",
        "Java",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Kafka's <code>CooperativeStickyAssignor</code> is designed to prevent full-stop partition rebalances by incrementally reassigning only the partitions that must move. However, under high resource contention, garbage collection pauses, or heavy-weight message batch processing, consumers can fail to complete their polling loop within the designated <code>max.poll.interval.ms</code>. This triggers a catastrophic failure sequence: the group coordinator presumes the consumer dead, initiates a rebalance, revokes its partitions, and reassigns them. Because cooperative sticky rebalances occur in multiple incremental steps, a slow consumer continuously falling out of sync causes a cascading rebalance loop (rebalance storm), halting real-time consumption.</p>",
    "root_cause": "The consumer group crashes into a rebalance storm when message processing times exceed the threshold specified by `max.poll.interval.ms`. Because the CooperativeStickyAssignor requires active participation in multiple rounds of joins and syncs, a blocked event loop fails to send heartbeat signals and join requests in a timely manner, forcing continuous renegotiations.",
    "bad_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\n\npublic class DangerousKafkaConsumer {\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"payment-processing-group\");\n        props.put(\"partition.assignment.strategy\", \"org.apache.kafka.clients.consumer.CooperativeStickyAssignor\");\n        // Unsafely low processing timeout\n        props.put(\"max.poll.interval.ms\", \"30000\");\n        props.put(\"max.poll.records\", \"1000\");\n        props.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\n        props.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\n\n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"transactions\"));\n\n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            for (ConsumerRecord<String, String> record : records) {\n                // DANGER: Blocking heavy I/O network call inside consumer poll thread\n                executeHeavyDatabaseQuery(record.value());\n            }\n        }\n    }\n    private static void executeHeavyDatabaseQuery(String val) { try { Thread.sleep(500); } catch (Exception e){} }\n}",
    "solution_desc": "Architectural solution: Decouple the polling thread from the execution thread pool to ensure heartbeats are sent reliably. Tune configuration parameters by increasing `max.poll.interval.ms` to comfortably cover worst-case execution times, reducing `max.poll.records` to process smaller batches, and adjusting consumer processing to run asynchronously on separate worker threads.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\nimport java.util.concurrent.*;\n\npublic class ResilientKafkaConsumer {\n    private static final ExecutorService workerPool = Executors.newFixedThreadPool(8);\n\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"payment-processing-group\");\n        props.put(\"partition.assignment.strategy\", \"org.apache.kafka.clients.consumer.CooperativeStickyAssignor\");\n        \n        // Robust config: high poll interval, smaller batch payload\n        props.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes\n        props.put(\"max.poll.records\", \"50\");\n        props.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\n        props.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\n\n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"transactions\"));\n\n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            if (!records.isEmpty()) {\n                // Dispatch to worker pool to keep the main consumer thread available for heartbeat & coordination\n                workerPool.submit(() -> {\n                    for (ConsumerRecord<String, String> record : records) {\n                        executeHeavyDatabaseQuery(record.value());\n                    }\n                    consumer.commitAsync();\n                });\n            }\n        }\n    }\n    private static void executeHeavyDatabaseQuery(String val) { try { Thread.sleep(100); } catch (Exception e){} }\n}",
    "verification": "Deploy the consumer and simulate process lag. Use `kafka-consumer-groups.sh --bootstrap-server localhost:9092 --describe --group payment-processing-group` to monitor stability. Verify that the group state stays in `Stable` and that partition revocations drop to zero during heavy computation cycles.",
    "date": "2026-07-02",
    "id": 1782991614,
    "type": "error"
});