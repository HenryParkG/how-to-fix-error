window.onPostDataLoaded({
    "title": "Fixing Airflow DAG Idempotency & Data Skew on Retries",
    "slug": "airflow-dag-idempotency-data-skew",
    "language": "Python",
    "code": "Idempotency Failure",
    "tags": [
        "Python",
        "Airflow",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Idempotency is a fundamental concept in distributed systems, especially in data pipelines like those orchestrated by Apache Airflow. An idempotent operation is one that can be applied multiple times without changing the result beyond the initial application. In Airflow, where tasks can fail and be retried, non-idempotent operations are a common source of data quality issues, leading to duplicate records, incorrect aggregates, or data skew. When a task retries, if it's not idempotent, it might re-process data that was already processed, or insert records again, causing inconsistencies.</p><p>Data skew can arise when retries cause certain partitions or data segments to be processed multiple times, while others are processed only once. This is particularly problematic in scenarios involving data transformations or aggregations where the order or number of operations matters. Understanding the state of your data and the side effects of your tasks is crucial for designing robust, idempotent Airflow DAGs.</p>",
    "root_cause": "Tasks performing operations with side effects (e.g., INSERT, UPDATE) without checking for the existence of the data or using a unique identifier for record updates. Lack of transactional boundaries for multi-step operations or appending data to files/tables without handling previous partial writes during retries.",
    "bad_code": "from airflow import DAG\nfrom airflow.operators.python import PythonOperator\nfrom datetime import datetime\nimport pandas as pd\nimport os\n\ndef process_data_non_idempotent():\n    # Simulate fetching data\n    new_data = pd.DataFrame({'id': [1, 2], 'value': ['A', 'B']})\n\n    # Non-idempotent operation: simply append to a CSV file\n    # If this task retries, data will be duplicated\n    output_file = '/tmp/output_data.csv'\n    try:\n        if os.path.exists(output_file):\n            existing_data = pd.read_csv(output_file)\n            combined_data = pd.concat([existing_data, new_data], ignore_index=True)\n        else:\n            combined_data = new_data\n    except Exception as e:\n        print(f\"Error reading existing data: {e}. Starting fresh.\")\n        combined_data = new_data\n    \n    combined_data.to_csv(output_file, index=False, mode='a', header=not os.path.exists(output_file))\n    print(\"Data processed and appended.\")\n\nwith DAG(\n    dag_id='non_idempotent_data_pipeline',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['non-idempotent'],\n) as dag:\n    process_task = PythonOperator(\n        task_id='process_data',\n        python_callable=process_data_non_idempotent,\n        retries=1, # Retries will cause duplicates in /tmp/output_data.csv\n    )",
    "solution_desc": "<p>To achieve idempotency in Airflow DAGs, tasks should be designed to produce the same outcome regardless of how many times they are executed. This can be achieved through several strategies:</p><ol><li><b>UPSERT Logic:</b> For database operations, use <code>UPSERT</code> (UPDATE or INSERT) statements that check for the existence of a record based on a unique key before inserting.</li><li><b>Atomic Operations:</b> Ensure that data writes are atomic. For files, write to a temporary location and then atomically move/rename the file. For databases, leverage transactions.</li><li><b>De-duplication:</b> Implement explicit de-duplication steps as part of the pipeline, either immediately after ingestion or before final storage.</li><li><b>Versioned Data:</b> Store data with versions or timestamps and only process the latest version.</li><li><b>Stateless Tasks:</b> Design tasks to be as stateless as possible, operating on immutable inputs and producing immutable outputs.</li><li><b>Transactional Boundaries:</b> For complex multi-step operations, ensure they are wrapped in database transactions that can be rolled back on failure.</li></ol>",
    "good_code": "from airflow import DAG\nfrom airflow.operators.python import PythonOperator\nfrom datetime import datetime\nimport pandas as pd\nimport os\n\ndef process_data_idempotent(execution_date_str):\n    # Simulate fetching data based on execution_date_str\n    # In a real scenario, `new_data` would be fetched for the specific `execution_date_str`\n    new_data = pd.DataFrame({'id': [1, 2], 'value': [f'A_{execution_date_str}', f'B_{execution_date_str}']})\n\n    # Strategy: Write to a date-partitioned file or use UPSERT for databases\n    # For file-based output, writing to a unique path per execution ensures idempotency.\n    output_dir = '/tmp/idempotent_output'\n    os.makedirs(output_dir, exist_ok=True)\n    output_file = os.path.join(output_dir, f'data_{execution_date_str}.csv')\n    temp_file = output_file + '.tmp'\n    \n    # Write to a temporary file, then atomically replace the final file\n    new_data.to_csv(temp_file, index=False)\n    os.replace(temp_file, output_file) # Atomic replacement\n    \n    print(f\"Data processed idempotently for {execution_date_str} and saved to {output_file}.\")\n\nwith DAG(\n    dag_id='idempotent_data_pipeline',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['idempotent'],\n) as dag:\n    process_task = PythonOperator(\n        task_id='process_data_idempotent',\n        python_callable=process_data_idempotent,\n        op_kwargs={'execution_date_str': '{{ ds }}'}, # Pass execution date string\n        retries=1,\n    )",
    "verification": "<p>Verification of idempotency involves rigorous testing. Unit tests should cover task logic with mocked external systems, simulating retries and partial failures. Integration tests should run the DAG end-to-end multiple times, intentionally triggering retries (e.g., by making tasks fail initially and then succeed). After multiple runs, inspect the final state of the data in your destination systems (databases, data lakes) to ensure there are no duplicates, inconsistencies, or data skew. Data quality checks, reconciliation scripts, and monitoring for abnormal data growth or deviations are essential ongoing verification steps. For file-based outputs, ensure that retries either overwrite the same logical partition or result in the same final dataset.</p>",
    "date": "2026-07-17",
    "id": 1784253000,
    "type": "error"
});