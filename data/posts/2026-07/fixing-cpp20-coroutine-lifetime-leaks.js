window.onPostDataLoaded({
    "title": "Fixing C++20 Coroutine Lifetime Leaks",
    "slug": "fixing-cpp20-coroutine-lifetime-leaks",
    "language": "C++",
    "code": "LifetimeViolation",
    "tags": [
        "C++",
        "Rust",
        "Networking",
        "Error Fix"
    ],
    "analysis": "<p>C++20 coroutines are stackless, meaning they yield execution back to the caller without preserving a physical stack frame. Instead, local variables and parameters are stored in a dynamically allocated heap structure called the <i>coroutine frame</i>. A catastrophic error pattern in high-performance networking engines emerges when coroutine parameters are passed by reference. Because C++ copies parameters into the coroutine frame <i>as references</i> rather than cloning the underlying values, any reference pointing to an object on the caller's transient stack will dangle the instant the coroutine encounters its first suspension point (such as waiting on an asynchronous socket read). This leads to silent memory corruption, undefined behavior, or segmentation faults.</p>",
    "root_cause": "When a C++20 coroutine takes arguments by reference (e.g., const T&), the compiler allocates space in the coroutine frame for a pointer/reference to the original object. If the calling scope destroys the argument before the coroutine resumes and completes, the coroutine reads deallocated stack or heap memory. Furthermore, failing to manage the lifetime of the std::coroutine_handle via RAII results in leaked coroutine frames.",
    "bad_code": "#include <iostream>\n#include <coroutine>\n#include <future>\n\nstruct Task {\n    struct promise_type {\n        Task get_return_object() { return Task{std::coroutine_handle<promise_type>::from_promise(*this)}; }\n        std::suspend_always initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void return_void() {}\n        void unhandled_exception() { std::terminate(); }\n    };\n    std::coroutine_handle<promise_type> handle;\n};\n\n// BUG: Passing data by reference to a coroutine that suspends.\n// The string 'data' is allocated on the caller's stack and will be destroyed\n// before the coroutine completes asynchronous execution.\nTask async_log_payload(const std::string& data) {\n    co_await std::suspend_always{}; // Suspends; control returns to caller\n    std::cout << \"Payload: \" << data << std::endl; // UNDEFINED BEHAVIOR: data is dangling!\n}",
    "solution_desc": "To fix lifetime violations, always pass arguments to asynchronous coroutines by value. The compiler will then move or copy the object directly into the coroutine frame, binding its lifetime to the coroutine itself. Additionally, wrap the std::coroutine_handle inside a robust RAII class that explicitly calls .destroy() in its destructor to guarantee that the coroutine frame is deallocated when the task goes out of scope.",
    "good_code": "#include <iostream>\n#include <coroutine>\n#include <utility>\n\nstruct Task {\n    struct promise_type {\n        Task get_return_object() { return Task{std::coroutine_handle<promise_type>::from_promise(*this)}; }\n        std::suspend_always initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void return_void() {}\n        void unhandled_exception() { std::terminate(); }\n    };\n    \n    std::coroutine_handle<promise_type> handle;\n    \n    // RAII destructor to prevent memory leaks of the coroutine frame\n    ~Task() {\n        if (handle) {\n            handle.destroy();\n        }\n    }\n    Task(Task&& other) noexcept : handle(std::exchange(other.handle, nullptr)) {}\n    Task& operator=(Task&& other) noexcept {\n        if (this != &other) {\n            if (handle) handle.destroy();\n            handle = std::exchange(other.handle, nullptr);\n        }\n        return *this;\n    }\n    explicit Task(std::coroutine_handle<promise_type> h) : handle(h) {}\n};\n\n// FIXED: Argument passed by value is copied into the coroutine frame\nTask async_log_payload(std::string data) {\n    co_await std::suspend_always{};\n    std::cout << \"Payload: \" << data << std::endl; // Safe and isolated\n}",
    "verification": "Compile the code using Clang or GCC with AddressSanitizer enabled (`-fsanitize=address,undefined`). Execute the networking routine under mock load and ensure that no 'use-after-poison' or 'heap-use-after-free' errors are reported in the runtime logs.",
    "date": "2026-07-11",
    "id": 1783764583,
    "type": "error"
});