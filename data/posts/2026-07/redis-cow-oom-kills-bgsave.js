window.onPostDataLoaded({
    "title": "Redis COW OOM Kills: Mitigating BGSAVE Issues",
    "slug": "redis-cow-oom-kills-bgsave",
    "language": "Redis",
    "code": "OOM_KILL",
    "tags": [
        "Redis",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Redis, known for its high performance and in-memory data storage, relies on a 'Copy-on-Write' (COW) mechanism for persistence operations like `BGSAVE` (background save) and `BGREWRITEAOF` (background AOF rewrite). When a `BGSAVE` command is issued, Redis forks a child process to write the RDB (Redis Database) file to disk. The child process initially shares all memory pages with the parent process.</p><p>The 'Copy-on-Write' behavior dictates that if the parent Redis process modifies a memory page that is shared with the child, a private copy of that page is made for the parent process before the modification. The child process continues to hold a reference to the original, unmodified page. This ensures the snapshot taken by the child is consistent, reflecting the database state at the moment of the fork.</p><p>Under high-write workloads, a significant number of memory pages can be modified by the parent process during a `BGSAVE` operation. Each modified page triggers a COW event, allocating additional memory for the parent's private copy. If the system does not have enough free RAM to accommodate these new copies, the Linux kernel's Out-Of-Memory (OOM) killer might be invoked. Since the Redis parent process typically holds the largest memory footprint, it often becomes the victim, leading to an unexpected shutdown and service disruption.</p>",
    "root_cause": "Insufficient free system memory (RAM) to handle the 'Copy-on-Write' overhead during `BGSAVE` or `BGREWRITEAOF` operations under high-write workloads. This is exacerbated by `vm.overcommit_memory=0` (default) where the kernel is more conservative, or a very high write rate causing many pages to be COWed, rapidly increasing RSS usage.",
    "bad_code": "```ini\n# redis.conf\n# Default settings for persistence, which can lead to OOM under high write load\nsave 900 1\nsave 300 10\nsave 60 10000\n\n# Or, a very large dataset with limited RAM\n# and high write activity during BGSAVE interval\nmaxmemory 10gb # If total system RAM is only 12GB, this is risky\nmaxmemory-policy noeviction\n\n# system setting (not Redis config, but contributes to issue)\n# cat /proc/sys/vm/overcommit_memory -> 0 (heuristic overcommit)\n```",
    "solution_desc": "The primary solution is to ensure adequate free system memory. A general rule of thumb is to have at least 1.5x to 2x the `used_memory_rss` reported by Redis's `INFO` command available as free RAM on the system during peak write operations. Other mitigation strategies include: 1. **Schedule BGSAVE during low-write periods:** Use `CRON` jobs to trigger `BGSAVE` at off-peak times instead of relying solely on automatic savepoints. 2. **Increase RAM:** The most straightforward solution. 3. **Tune `maxmemory` and `maxmemory-policy`:** Use `maxmemory` to prevent Redis from growing indefinitely and `volatile-lru` or `allkeys-lru` policies to evict old keys before OOM. 4. **Use replicas for persistence:** Configure one or more Redis replicas and use them to perform `BGSAVE` operations. This offloads the COW overhead from the primary instance. 5. **Enable `no-appendfsync-on-rewrite` for AOF:** If using AOF, this can reduce disk I/O contention during AOF rewrites. 6. **Adjust `vm.overcommit_memory`:** Setting `vm.overcommit_memory=1` makes the kernel always succeed memory allocations, potentially delaying OOM but still leading to OOM-kill if physical memory is exhausted. This should be used with caution and only if understood well. It's generally safer to provide enough RAM.",
    "good_code": "```ini\n# redis.conf\n# Disable automatic savepoints if using manual BGSAVE or replica persistence\n# save \"\"\n\n# Configure maxmemory and a suitable eviction policy\nmaxmemory 8gb # Adjust based on actual data size and RAM\nmaxmemory-policy allkeys-lru\n\n# Example of manual BGSAVE via script (e.g., cron job)\n# /usr/bin/redis-cli BGSAVE\n\n# System settings (adjust cautiously)\n# To prevent heuristic overcommit issues, ensure enough physical RAM first.\n# echo 1 > /proc/sys/vm/overcommit_memory # Use with extreme caution, prefer more RAM\n\n# Ensure enough swap space is configured, though swap hit during BGSAVE means performance degradation\n# but might prevent OOM kill. However, Redis on swap is very slow.\n```",
    "verification": "Monitor `used_memory_rss` and `used_memory_peak` metrics via `redis-cli INFO memory` before, during, and after a `BGSAVE` operation. Observe system-wide memory usage (`free -h`, `top`, `htop`, `vmstat`) to check for spikes in RSS (Resident Set Size) and available memory. Check `dmesg` or `/var/log/kern.log` for OOM killer messages (`Out of memory: Kill process...`). If using a replica for persistence, ensure the replica's `BGSAVE` completes successfully without impacting the primary.",
    "date": "2026-07-09",
    "id": 1783562828,
    "type": "error"
});