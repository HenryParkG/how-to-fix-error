window.onPostDataLoaded({
    "title": "Spark Executor JVM OOMs: Data Skew & Kryo Overheads",
    "slug": "spark-executor-jvm-ooms-data-skew-kryo-overheads",
    "language": "Java",
    "code": "OOM",
    "tags": [
        "Spark",
        "Big Data",
        "JVM",
        "Data Skew",
        "Kryo",
        "Java",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Spark applications frequently encounter Out-Of-Memory (OOM) errors in executor JVMs, especially when processing large datasets. Two primary culprits are often data skew and inefficient serialization, particularly with Kryo. Data skew occurs when a small subset of keys in a shuffled dataset receives a disproportionately large amount of data, causing a single task (and thus a single executor core) to attempt to process too much data, exceeding its allocated memory.</p><p>Kryo serialization, while generally more performant than Java serialization, can still contribute to OOMs through high memory overheads. This happens when object graphs are deeply nested, contain many small objects, or when custom serializers are not optimally configured. Each object serialized by Kryo consumes memory, and if a task accumulates a vast number of these objects in memory before writing them to disk or sending them over the network, it can quickly exhaust the executor's heap.</p><p>Diagnosing these issues requires careful monitoring of Spark UI for task durations, input/output sizes, shuffle spill metrics, and GC activity. Excessive shuffle spill (to disk) or long garbage collection pauses often indicate impending OOMs.</p>",
    "root_cause": "Data skew causing single tasks to process excessive data volumes, leading to heap exhaustion. Inefficient Kryo serialization configurations or complex object structures generating high memory overhead per object, cumulatively consuming too much executor heap.",
    "bad_code": "```scala\n// Example of code susceptible to data skew\nval df = spark.read.parquet(\"large_dataset.parquet\")\n\n// Assume 'join_key' has highly skewed values\nval joinedDf = df.join(anotherDf, \"join_key\")\n                 .groupBy(\"join_key\")\n                 .agg(count(\"*\"))\n\n// Example of default Kryo usage that might be inefficient for complex types\n// This is implicitly used, not explicitly written as 'bad_code' but represents\n// the default behavior without custom optimization.\n// spark.conf.set(\"spark.serializer\", \"org.apache.spark.serializer.KryoSerializer\")\n// spark.conf.set(\"spark.kryoserializer.buffer.max\", \"256m\") // Default might be too small\n\ncase class ComplexObject(id: String, values: Map[String, List[InnerObject]])\ncase class InnerObject(name: String, score: Double)\n\n// If ComplexObject instances are numerous and large, default Kryo might struggle\nval rdd = spark.sparkContext.parallelize(Seq(\n    ComplexObject(\"A\", Map(\"x\" -> List(InnerObject(\"i1\", 1.0), InnerObject(\"i2\", 2.0))))\n))\n// Operations on rdd or DataFrame with UDTs containing ComplexObject can lead to issues\n```",
    "solution_desc": "Mitigating Spark executor OOMs due to data skew involves re-distributing skewed data evenly across partitions. Techniques include 'salting' (appending a random prefix/suffix to skewed keys), broadcasting small tables, and manually repartitioning with a custom partitioner. For Kryo serialization, register all custom classes explicitly, especially for complex or nested types, to avoid reflection overhead. Tune `spark.kryoserializer.buffer.max` and `spark.kryoserializer.buffer.mb` to accommodate large objects. Consider implementing custom `KryoRegistrator` and even custom `Serializer` implementations for highly complex or frequently used types to optimize memory footprint and serialization/deserialization speed. Ensure garbage collection tuning (`spark.executor.extraJavaOptions` for G1GC parameters) is appropriate for the workload.",
    "good_code": "```scala\nimport org.apache.spark.sql.functions._\nimport org.apache.spark.SparkConf\nimport com.esotericsoftware.kryo.Kryo\nimport org.apache.spark.serializer.KryoRegistrator\n\n// Custom KryoRegistrator\nclass MyKryoRegistrator extends KryoRegistrator {\n  override def registerClasses(kryo: Kryo): Unit = {\n    kryo.register(classOf[ComplexObject])\n    kryo.register(classOf[InnerObject])\n    // Register other frequently used classes (e.g., scala collections)\n    kryo.register(classOf[scala.collection.immutable.Map[_, _]])\n    kryo.register(classOf[scala.collection.immutable.List[_]])\n  }\n}\n\n// Spark session with tuned Kryo and skew handling configurations\nval conf = new SparkConf()\n  .set(\"spark.serializer\", \"org.apache.spark.serializer.KryoSerializer\")\n  .set(\"spark.kryo.registrator\", \"MyKryoRegistrator\")\n  .set(\"spark.kryoserializer.buffer.max\", \"512m\") // Increase buffer size for large objects\n  .set(\"spark.shuffle.skewedPartitionFactor\", \"5\") // Factor to identify skewed partitions\n  .set(\"spark.shuffle.skewedPartitionThresholdInBytes\", \"256m\") // Threshold for skewed partitions\n  .set(\"spark.sql.adaptive.enabled\", \"true\") // Enable Adaptive Query Execution for automatic skew handling (Spark 3+)\n  .set(\"spark.sql.adaptive.skewJoin.enabled\", \"true\")\n  .set(\"spark.sql.adaptive.advisoryPartitionSizeInBytes\", \"128m\")\n\nval spark = SparkSession.builder()\n  .appName(\"SparkSkewKryoFix\")\n  .config(conf)\n  .getOrCreate()\n\n// Example of data skew mitigation using salting for joins (manual approach if AQE is not enough/available)\nval df = spark.read.parquet(\"large_dataset.parquet\")\nval anotherDf = spark.read.parquet(\"small_lookup_dataset.parquet\") // or another large one\n\n// 1. Identify skewed keys (e.g., through sampling or analysis)\n// 2. Salt the skewed keys in the larger DataFrame\nval numSaltBuckets = 200 // Number of salt buckets\nval saltedDf = df.withColumn(\"salted_join_key\", \n  when(col(\"join_key\") === \"highly_skewed_value\", concat(col(\"join_key\"), lit(\"_\"), floor(rand() * numSaltBuckets)))\n  .otherwise(col(\"join_key\"))\n)\n\nval saltedAnotherDf = anotherDf.withColumn(\"salted_join_key\", \n  when(col(\"join_key\") === \"highly_skewed_value\", concat(col(\"join_key\"), lit(\"_\"), lit(0)))\n  .otherwise(col(\"join_key\"))\n)\n\n// Perform a repartition after the join to balance the data if necessary\nval joinedDf = saltedDf.join(saltedAnotherDf, Seq(\"salted_join_key\"), \"inner\")\n                       .drop(\"salted_join_key\") // Drop the temporary key\n                       .repartition(2000, col(\"original_join_key\")) // Repartition on original key after handling skew\n                       .groupBy(\"original_join_key\")\n                       .agg(count(\"*\"))\n\n// Ensure classes are defined in a scope accessible by Kryo (e.g., top-level or in objects)\ncase class ComplexObject(id: String, values: Map[String, List[InnerObject]])\ncase class InnerObject(name: String, score: Double)\n\n// Operations using ComplexObject will now benefit from registered Kryo serializers\n```",
    "verification": "Monitor Spark UI: Look for balanced task durations, reduced shuffle read/write sizes, minimal shuffle spill, and healthy GC times (low pause times, high throughput). Profile executor JVMs using tools like `jstat` or Spark's built-in metrics to confirm lower memory usage and fewer/shorter Full GC cycles. Run the application with increased data volume to ensure scalability and observe if OOMs are still triggered. Specifically check `Stages` tab for any tasks with significantly higher input/output records or runtime, which could indicate residual skew. Inspect the `Environment` tab to confirm Kryo configurations are applied.",
    "date": "2026-07-10",
    "id": 1783650386,
    "type": "error"
});