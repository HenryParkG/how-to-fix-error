window.onPostDataLoaded({
    "title": "Solving Kafka Consumer Rebalance Storms",
    "slug": "kafka-consumer-rebalance-storms",
    "language": "Java",
    "code": "RebalanceStorm",
    "tags": [
        "Kafka",
        "Java",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>A Kafka Consumer Group Rebalance Storm occurs when consumers are repeatedly kicked out of the group, forcing continuous partitions reassignment. This issue manifests under heavy loads or long-running database transactions. When a consumer receives a batch of messages via <code>poll()</code>, it must process them and invoke the next <code>poll()</code> within the timeout window defined by <code>max.poll.interval.ms</code>.</p><p>If processing takes longer than this limit, the consumer coordinator assumes the consumer is dead, marks it as failed, and triggers a rebalance. This shifts the unprocessed load to remaining group members. Because these surviving consumers are now overloaded with additional partitions, they also exceed their timeouts, triggering a cascading failure loop that halts message consumption across the entire cluster.</p>",
    "root_cause": "The synchronous message processing block inside the main consumer thread exceeds the default max.poll.interval.ms threshold, causing the broker to trigger group rebalances.",
    "bad_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.Collections;\n\npublic class SlowConsumer {\n    public void runConsumer() {\n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(configs);\n        consumer.subscribe(Collections.singletonList(\"heavy-jobs\"));\n        \n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            for (ConsumerRecord<String, String> record : records) {\n                // BUG: Blocking synchronous call. If database latency spikes, \n                // this processing takes longer than max.poll.interval.ms (default 5 mins).\n                processDatabaseTransaction(record.value());\n            }\n            consumer.commitSync();\n        }\n    }\n    private void processDatabaseTransaction(String data) { /* Slow External write */ }\n}",
    "solution_desc": "Decouple the Kafka ingestion loop from the processing layer. Run a dedicated single thread for polling partitions and maintaining heartbeats, while offloading message processing to an asynchronous worker thread pool. Additionally, reduce the maximum number of records returned per poll using `max.poll.records` and tune `max.poll.interval.ms` upward.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\nimport java.util.concurrent.*;\n\npublic class ResilientConsumer {\n    private final ExecutorService workerPool = Executors.newFixedThreadPool(10);\n    \n    public void runConsumer(Properties configs) {\n        // Configure robust polling limits\n        configs.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG, \"50\"); \n        configs.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG, \"600000\"); // 10 minutes\n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(configs);\n        consumer.subscribe(Collections.singletonList(\"heavy-jobs\"));\n        \n        try {\n            while (true) {\n                ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n                if (!records.isEmpty()) {\n                    List<Future<?>> futures = new ArrayList<>();\n                    for (ConsumerRecord<String, String> record : records) {\n                        futures.add(workerPool.submit(() -> processDatabaseTransaction(record.value())));\n                    }\n                    // Wait for the current batch to finish before next poll to avoid out-of-order commits\n                    for (Future<?> future : futures) future.get();\n                    consumer.commitSync();\n                }\n            }\n        } catch (Exception e) {\n            // Handle exceptions and graceful shutdown\n        } finally {\n            consumer.close();\n        }\n    }\n    private void processDatabaseTransaction(String data) { /* Async Database write */ }\n}",
    "verification": "Monitor JMX consumer metrics: `join-rate-metric` and `rebalance-latency-avg`. If a rebalance occurs, verify through the cluster manager logs that consumer group state stabilizes in less than 1 cycle and stays at `Stable` under peak traffic spikes.",
    "date": "2026-07-09",
    "id": 1783597685,
    "type": "error"
});