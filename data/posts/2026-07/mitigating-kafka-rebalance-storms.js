window.onPostDataLoaded({
    "title": "Mitigating Kafka Rebalance Storms",
    "slug": "mitigating-kafka-rebalance-storms",
    "language": "Java",
    "code": "CommitFailedException",
    "tags": [
        "Kafka",
        "Java",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>In high-throughput event streams, a Kafka consumer group 'rebalance storm' occurs when a cascade of consumers fail to process their assigned partitions within the configured thresholds. In typical configurations, a consumer polls for a batch of messages, processes them synchronously, and then polls again. If processing a batch exceeds <code>max.poll.interval.ms</code>, the consumer coordinator assumes the consumer thread has hung or died. The coordinator then evicts the consumer and reassigns its partitions to other active nodes. This increases the load on the remaining nodes, causing them to also timeout, creating a devastating spiral of continuous rebalancing where zero progress is made.</p>",
    "root_cause": "The processing loop of the consumer takes longer than the 'max.poll.interval.ms' limit. This is typically driven by fetching too many records per poll (high 'max.poll.records') combined with slow, synchronous downstream operations such as external database writes or third-party API calls.",
    "bad_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\n\npublic class SlowConsumer {\n    public void start() {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"payment-group\");\n        props.put(\"max.poll.records\", \"500\"); // Too high for synchronous external API writes\n        props.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes\n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"payments\"));\n        \n        try {\n            while (true) {\n                ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n                for (ConsumerRecord<String, String> record : records) {\n                    // BUG: Synchronous slow operation (e.g., 1s per record)\n                    // 500 records * 1s = 500s (8.3 mins), which exceeds max.poll.interval.ms (5 mins)!\n                    processPayment(record.value());\n                }\n                consumer.commitSync();\n            }\n        } finally {\n            consumer.close();\n        }\n    }\n    \n    private void processPayment(String payload) throws Exception {\n        Thread.sleep(1000); // Mock database write or billing API call\n    }\n}",
    "solution_desc": "Architecturally decouple message ingestion from processing. Reduce `max.poll.records` to a smaller window, increase `max.poll.interval.ms` to allow a wider safety margin, and use an asynchronous worker pool. To prevent data loss or out-of-order execution, explicitly pause the consumer partitions during heavy processing, or commit offsets sequentially on a dedicated thread once processing successfully terminates.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport org.apache.kafka.common.TopicPartition;\nimport java.time.Duration;\nimport java.util.*;\nimport java.util.concurrent.*;\n\npublic class ResilientConsumer {\n    private final ExecutorService executor = Executors.newFixedThreadPool(8);\n    \n    public void start() {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"payment-group\");\n        props.put(\"max.poll.records\", \"50\"); // Lower batch limit\n        props.put(\"max.poll.interval.ms\", \"600000\"); // Raised limit to 10 minutes\n        props.put(\"enable.auto.commit\", \"false\");\n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"payments\"));\n        \n        try {\n            while (true) {\n                ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n                if (!records.isEmpty()) {\n                    // Submit to executor and block until the batch is complete\n                    // to maintain offset ordering safely within the poll threshold\n                    List<Future<?>> futures = new ArrayList<>();\n                    for (ConsumerRecord<String, String> record : records) {\n                        futures.add(executor.submit(() -> processPayment(record.value())));\n                    }\n                    for (Future<?> future : futures) {\n                        future.get(); // Blocks up to the safe timeout margin\n                    }\n                    consumer.commitSync();\n                }\n            }\n        } catch (Exception e) {\n            e.printStackTrace();\n        } finally {\n            consumer.close();\n            executor.shutdown();\n        }\n    }\n    \n    private void processPayment(String payload) {\n        try {\n            Thread.sleep(1000);\n        } catch (InterruptedException e) {\n            Thread.currentThread().interrupt();\n        }\n    }\n}",
    "verification": "Monitor JMX metrics `kafka.consumer:type=consumer-coordinator-metrics,client-id=[client-id]` for the metric `join-rate` and `rebalance-latency-avg`. Under synthetic slow processing tests, ensure that no `CommitFailedException` is raised and the consumer group remains stable.",
    "date": "2026-07-11",
    "id": 1783764584,
    "type": "error"
});