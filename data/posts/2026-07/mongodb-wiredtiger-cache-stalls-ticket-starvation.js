window.onPostDataLoaded({
    "title": "Mitigating MongoDB WiredTiger Cache Eviction Stalls & Ticket Starvation",
    "slug": "mongodb-wiredtiger-cache-stalls-ticket-starvation",
    "language": "MongoDB, Database",
    "code": "MongoDBPerf",
    "tags": [
        "MongoDB",
        "Database",
        "NoSQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's default storage engine, WiredTiger, is a sophisticated B-tree based engine designed for high-performance and concurrency. A critical component of WiredTiger is its internal cache, which stores active data and index pages in RAM to minimize disk I/O. While highly efficient, this cache can become a source of performance stalls under specific conditions, primarily <strong>cache eviction stalls</strong> and <strong>ticket starvation</strong>.</p><p><strong>Cache Eviction Stalls</strong> occur when the WiredTiger cache is under memory pressure, meaning there isn't enough RAM to hold the working set (frequently accessed data and indexes). When the cache fills up, WiredTiger must evict 'dirty' pages (pages modified in memory but not yet written to disk) to free up space for new data. This eviction process involves writing these dirty pages to disk, which can be a blocking operation. If the rate of incoming writes or reads exceeds the rate at which dirty pages can be evicted and written to disk, threads will stall, waiting for cache space to become available. This manifests as high I/O wait times and increased latency.</p><p><strong>Ticket Starvation</strong> relates to WiredTiger's internal concurrency control. It uses a system of 'tickets' to limit the number of active reads and writes. By default, there are 128 read tickets and 128 write tickets. When an operation needs to access the storage engine, it must acquire a ticket. If all write tickets are in use (e.g., during high write throughput, especially involving large documents or complex updates), subsequent write operations will be queued, waiting for a ticket to become free. This leads to increased write latency and can contribute to cache pressure if these waiting writes are also dirtying pages. While read tickets can also be starved, write ticket starvation is more common in scenarios leading to cache eviction issues because writes are what cause dirty pages to accumulate.</p>",
    "root_cause": "The primary root cause for both issues is often an undersized WiredTiger cache relative to the application's working set size and write throughput. This leads to aggressive dirty page eviction, which becomes an I/O bottleneck. For ticket starvation, the concurrent write load exceeds the default `wiredTigerConcurrentWriteTransactions` limit, or the write operations themselves are slow due to disk I/O or other system constraints, causing tickets to be held longer than necessary.",
    "bad_code": "```yaml\n# Example MongoDB configuration or workload patterns contributing to issues\n\n# Scenario 1: Default configuration on an under-provisioned server\n# (No explicit cacheSizeGB defined, allowing WiredTiger to use 50% of RAM minus 1GB)\n# If total RAM is too low for the working set, this will cause issues.\n# mongodb.conf\nstorage:\n  dbPath: /var/lib/mongodb\n  journal:\n    enabled: true\n# ... rest of the default config\n\n# Scenario 2: Heavy, unindexed write workload causing frequent disk writes and cache churn\n// Imagine a client application performing many document insertions or updates like this:\nfor (let i = 0; i < 100000; i++) {\n    db.myCollection.insertOne({\n        field1: 'value' + i,\n        field2: 'another value',\n        timestamp: new Date()\n    }, { writeConcern: { w: 1 } }); // Default write concern, but high volume is problematic\n}\n\n// Scenario 3: Large documents and updates leading to significant memory churn\ndb.largeDocs.updateOne(\n    { _id: 'someId' },\n    { $set: { largeArrayField: Array(10000).fill('some_data_string') } } // Causes large page rewrites\n);\n```",
    "solution_desc": "The primary solution involves correctly sizing the WiredTiger cache and optimizing disk I/O. First, ensure sufficient RAM for your MongoDB instance, ideally at least 2-3x the size of your working set if possible, to allow the cache to comfortably hold data and indexes. Configure the `storage.wiredTiger.engineConfig.cacheSizeGB` explicitly in `mongod.conf`. A good starting point is often 50% of total RAM, leaving space for the operating system and other processes. However, carefully monitor your `resident` memory and WiredTiger `bytes currently in cache` to fine-tune this value.\n\nTo address cache eviction stalls, ensure your disk subsystem can handle the write throughput. Use SSDs, ideally NVMe, and consider RAID configurations (e.g., RAID 10) for both performance and redundancy. Monitor disk I/O metrics (IOPS, latency, queue depth) closely. For ticket starvation, consider increasing the `wiredTigerConcurrentWriteTransactions` parameter in `mongod.conf`, but do so cautiously. While it can alleviate ticket pressure, increasing it too much without sufficient I/O capacity will simply shift the bottleneck to disk I/O and can worsen cache eviction. Before increasing, focus on optimizing individual write operations: ensure proper indexing to reduce document scanning, batch small writes using `insertMany` or `bulkWrite`, and avoid excessively large documents or updates that cause large page rewrites. Consider scaling horizontally with replica sets or sharding to distribute the write load.",
    "good_code": "```yaml\n# Optimized MongoDB configuration for better WiredTiger performance\n# (mongod.conf)\n\nstorage:\n  dbPath: /var/lib/mongodb\n  journal:\n    enabled: true\n  wiredTiger:\n    engineConfig:\n      # Explicitly set cache size based on monitoring working set and available RAM.\n      # Example: 16GB total RAM, working set ~6GB, allocate 8GB to WiredTiger.\n      cacheSizeGB: 8\n      # Consider increasing concurrent write tickets cautiously after I/O optimization.\n      # Default is 128, try 256 or 512, but monitor impact.\n      wiredTigerConcurrentWriteTransactions: 256\n    collectionConfig:\n      # Use snappy or zstd for compression; none for max performance if disk space is not an issue\n      blockCompressor: zstd\n\nsystemLog:\n  destination: file\n  path: /var/log/mongodb/mongod.log\n  logAppend: true\n  # Enable slow query logging to identify inefficient operations\n  component:\n    query: { verbosity: 1 }\n\nnet:\n  port: 27017\n  bindIp: 0.0.0.0\n\n# Example of optimized application write pattern\n// Use bulk inserts for many documents\nconst documents = [];\nfor (let i = 0; i < 100000; i++) {\n    documents.push({\n        field1: 'value' + i,\n        field2: 'another value',\n        timestamp: new Date()\n    });\n}\ndb.myCollection.insertMany(documents, { ordered: false }); // Use ordered: false for better performance if order doesn't matter\n\n// Ensure updates use indexes where applicable\ndb.myCollection.updateOne(\n    { _id: 'someId', 'nested.array.field': 'value' }, // Ensure 'nested.array.field' is indexed if queried often\n    { $set: { 'nested.array.$.data': 'new_data' } }\n);\n```",
    "verification": "Regularly monitor MongoDB's internal metrics via `db.serverStatus()`. Key metrics to watch include:\n<ul><li>`wiredTiger.cache.bytes currently in cache`: Should ideally be below `cacheSizeGB`. If it's constantly near the limit, it indicates memory pressure.</li><li>`wiredTiger.cache.tracked dirty bytes in the cache`: High values indicate many pending writes to disk.</li><li>`wiredTiger.cache.pages evicted_walked`: High numbers indicate aggressive eviction.</li><li>`wiredTiger.concurrentTransactions.write.out`: If consistently high (near `wiredTigerConcurrentWriteTransactions` value), it suggests ticket starvation. The `available` count should ideally not be 0 frequently.</li><li>`wiredTiger.transactions.transaction checkpoint time`: Long checkpoint times can indicate I/O bottlenecks.</li></ul>Also, monitor system-level metrics like CPU utilization (especially I/O wait), RAM usage, disk IOPS, and latency using tools like `iostat`, `vmstat`, and cloud provider monitoring dashboards. Look for correlations between high values in these metrics and increased application latency. Slow query logs (`systemLog.component.query.verbosity`) can help identify inefficient operations contributing to disk I/O.",
    "date": "2026-07-10",
    "id": 1783685812,
    "type": "error"
});