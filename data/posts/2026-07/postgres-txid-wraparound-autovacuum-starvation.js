window.onPostDataLoaded({
    "title": "Fixing Postgres TXID Wraparound & Autovacuum Starvation",
    "slug": "postgres-txid-wraparound-autovacuum-starvation",
    "language": "SQL",
    "code": "TXID Wraparound / Autovacuum Starvation",
    "tags": [
        "SQL",
        "Docker",
        "Infra",
        "PostgreSQL",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL relies on Multi-Version Concurrency Control (MVCC) to manage concurrent transactions. Each transaction is assigned a 32-bit Transaction ID (TXID). Because a 32-bit integer limits the active transaction space to roughly 4 billion transactions, PostgreSQL uses modulo-2^32 arithmetic to wrap transaction IDs around, considering half of the TXID space (2 billion) in the past and half in the future.</p><p>To prevent active TXIDs from colliding with old data, PostgreSQL must periodically run a VACUUM process to freeze ancient transaction IDs (converting them to a special <code>FrozenTransactionId</code>). However, under high write throughput combined with long-running transactions, open replication slots, or uncommitted prepared transactions, the autovacuum process gets starved or blocked. When the age of the oldest unfrozen transaction ID (<code>age(relfrozenxid)</code>) reaches 200 million (by default), PostgreSQL triggers an aggressive autovacuum. If it reaches 2.1 billion, PostgreSQL shuts down and goes into read-only recovery mode to prevent data corruption.</p>",
    "root_cause": "The root cause of TXID starvation is two-fold: First, default autovacuum configurations are heavily rate-limited (throttled by vacuum cost limits) to minimize I/O impact, preventing vacuum tasks from keeping up with intensive write operations. Second, long-running active sessions, abandoned replication slots, or orphan prepared transactions block the global minimum transaction ID (xmin), preventing VACUUM from cleaning or freezing tuples older than that point, resulting in unstoppable TXID age growth.",
    "bad_code": "-- Default or suboptimal PostgreSQL configuration promoting starvation under heavy write load\n-- file: postgresql.conf\n\nautovacuum_max_workers = 3\nautovacuum_vacuum_cost_delay = 20ms\nautovacuum_vacuum_cost_limit = 200\nautovacuum_vacuum_scale_factor = 0.2\nautovacuum_freeze_max_age = 200000000\n\n-- Example of a blocking scenario: Long-running transaction holding back xmin\nBEGIN TRANSACTION;\nSELECT * FROM heavy_ledger_table WHERE status = 'PENDING' FOR UPDATE;\n-- Transaction left open indefinitely due to lack of application-side timeouts\n-- This prevents autovacuum from freezing tuples across the entire database.",
    "solution_desc": "To resolve and prevent autovacuum starvation and TXID wraparound, optimize the PostgreSQL configuration to increase autovacuum throughput. Increase the resource limits by lowering cost delay and elevating cost limit. Next, implement strict timeouts for client connections to prevent idle-in-transaction states from pinning the transaction horizon. Finally, identify and remove stale replication slots or orphan two-phase transactions that inhibit table freezing.",
    "good_code": "-- Optimized PostgreSQL configuration to accelerate vacuum operations\n-- file: postgresql.conf\n\n# Maximize autovacuum worker throughput\nautovacuum_max_workers = 8\nautovacuum_vacuum_cost_delay = 2ms\nautovacuum_vacuum_cost_limit = 2000\n\n# Trigger autovacuum much earlier to avoid emergency spikes\nautovacuum_vacuum_scale_factor = 0.05\nautovacuum_vacuum_threshold = 100\nautovacuum_freeze_max_age = 200000000\n\n# Prevent runaway client transactions from blocking MVCC cleanup\nidle_in_transaction_session_timeout = 60000 # Terminate idle transactions after 60s\nstatement_timeout = 30000                   # Terminate any query taking over 30s\n\n-- Maintenance queries to identify and resolve blockers\n\n-- 1. Identify stale replication slots blocking vacuum\nSELECT slot_name, active, xmin, catalog_xmin \nFROM pg_replication_slots \nWHERE xmin IS NOT NULL OR catalog_xmin IS NOT NULL;\n\n-- Drop a stale slot if necessary\n-- SELECT pg_drop_replication_slot('stale_slot_name');\n\n-- 2. Identify and terminate long-running or idle-in-transaction sessions\nSELECT pid, age(backend_xmin), query, state\nFROM pg_stat_activity\nWHERE state != 'idle'\n  AND age(backend_xmin) > 1000000\nORDER BY age(backend_xmin) DESC;\n\n-- Forcefully terminate a blocking backend process\n-- SELECT pg_terminate_backend(blocking_pid);",
    "verification": "Verify the fix by querying pg_stat_user_tables to monitor the age of the oldest unfrozen transaction: 'SELECT relname, age(relfrozenxid) FROM pg_class WHERE relkind = 'r' ORDER BY age(relfrozenxid) DESC LIMIT 10;'. Monitor the reduction in TXID age post-optimization and trace active autovacuum worker activity using pg_stat_progress_vacuum.",
    "date": "2026-07-17",
    "id": 1784274774,
    "type": "error"
});