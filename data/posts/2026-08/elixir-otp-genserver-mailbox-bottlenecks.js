window.onPostDataLoaded({
    "title": "Elixir OTP: GenServer Mailbox Bottlenecks & Starvation",
    "slug": "elixir-otp-genserver-mailbox-bottlenecks",
    "language": "Elixir",
    "code": "Bottleneck",
    "tags": [
        "Elixir",
        "OTP",
        "Concurrency",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>In Elixir, GenServers are the cornerstone of concurrent, fault-tolerant applications. They provide a standardized interface for processes to handle messages sequentially, maintaining internal state. While this sequential processing model simplifies state management and race condition avoidance, it can become a significant bottleneck if not managed carefully. A GenServer processes messages from its mailbox one by one. If a `handle_call` or `handle_cast` function executes a long-running or blocking operation, subsequent messages will accumulate in the mailbox, leading to increased latency and potential mailbox overflow.</p><p>Process starvation occurs when a GenServer's mailbox fills up, or when its processing loop is continuously occupied by slow operations, preventing it from responding to or processing other critical messages in a timely manner. This can lead to timeouts for calling processes, cascading failures, and a degraded user experience. Common scenarios include synchronous database queries, external API calls, complex computations, or large data processing within a GenServer's handler function.</p>",
    "root_cause": "Long-running or blocking operations executed synchronously within a GenServer's `handle_call` or `handle_cast` functions, leading to message accumulation in the mailbox and delaying subsequent message processing.",
    "bad_code": "```elixir\ndef handle_call(:do_heavy_work, _from, state) do\n  # This simulates a long-running, blocking operation\n  Process.sleep(5000) # Simulating 5 seconds of work\n  {:reply, :done, state}\nend\n\ndef handle_cast(:log_something, state) do\n  # This message will be delayed if 'do_heavy_work' is running\n  Logger.info(\"Logging something important\")\n  {:noreply, state}\nend\n```",
    "solution_desc": "To alleviate GenServer mailbox bottlenecks and prevent process starvation, the core principle is to offload long-running operations from the GenServer's main event loop. Instead of performing blocking work directly in `handle_call` or `handle_cast`, delegate it to a separate, temporary process. `Task.start_child/2` or `spawn_link/3` are excellent choices for this, allowing the GenServer to immediately return `:noreply` and continue processing other messages. The new process can then perform the heavy work and, once complete, send a message back to the original GenServer (using `send/2`) to update its state or notify callers. Additionally, favor `GenServer.cast/2` over `GenServer.call/3` when a response is not immediately required, as `cast` is asynchronous and non-blocking for the caller. If a synchronous response is absolutely necessary after an asynchronous operation, the GenServer can store the caller's `from` tuple and reply to it later using `GenServer.reply/2`.",
    "good_code": "```elixir\ndef handle_call(:do_heavy_work, from, state) do\n  # Offload the heavy work to a Task\n  Task.start_child(fn ->\n    Process.sleep(5000) # Simulating 5 seconds of work\n    # Send result back to the GenServer\n    GenServer.cast(__MODULE__, {:heavy_work_completed, from, :done})\n  end)\n  {:noreply, state} # Immediately return, allowing other messages to be processed\nend\n\ndef handle_cast({:heavy_work_completed, from, result}, state) do\n  # When the work is done, reply to the original caller\n  GenServer.reply(from, result)\n  {:noreply, state}\nend\n\ndef handle_cast(:log_something, state) do\n  Logger.info(\"Logging something important\")\n  {:noreply, state}\nend\n```",
    "verification": "Verify the fix by using the `observer` tool (from `:observer.start()`) to inspect the GenServer's mailbox size and process reduction count. A healthy GenServer will show a consistently small mailbox, even under load, and its reduction count will reflect efficient message processing. Implement custom telemetry or logging to measure the latency of messages sent to the GenServer, ensuring critical operations are no longer experiencing significant delays. Stress test the application with concurrent requests targeting the previously problematic GenServer to confirm that response times are consistent and mailbox growth is contained, and that `handle_cast` messages are processed promptly.",
    "date": "2026-08-25",
    "id": 1787629275,
    "type": "error"
});