window.onPostDataLoaded({
    "title": "PostgreSQL: XID Wraparound & Autovacuum Starvation",
    "slug": "postgresql-xid-wraparound-autovacuum-starvation",
    "language": "PostgreSQL",
    "code": "XIDWraparound",
    "tags": [
        "PostgreSQL",
        "Database",
        "Autovacuum",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit transaction ID (XID) for every database transaction. While 32 bits seems like a lot, with billions of transactions, it will eventually 'wrap around' to zero. If the database reaches a point where the oldest unfrozen transaction ID is too close to the wraparound point, PostgreSQL will trigger an emergency shutdown to prevent data corruption. This critical state is known as XID Wraparound Failure.</p><p>The primary mechanism PostgreSQL employs to prevent XID wraparound is Autovacuum. Autovacuum processes are responsible for marking old transaction IDs as 'frozen,' effectively allowing the XID counter to continue its cycle without danger. When Autovacuum becomes starved \u2013 either due to misconfiguration, overwhelming workload, long-running transactions blocking its progress, or insufficient resources \u2013 it fails to freeze old XIDs fast enough. This leads to an accumulation of unfrozen XIDs, pushing the database closer to the wraparound limit and eventually leading to a hard shutdown.</p><p>Understanding the interplay between XID, Autovacuum, and transaction visibility is crucial. Each row version in PostgreSQL has `xmin` and `xmax` XIDs, indicating its creation and deletion transaction IDs. Autovacuum needs to process old rows to update these XIDs (or remove dead tuples) and ensure the `relfrozenxid` for each table and `datfrozenxid` for the database are advancing. If `autovacuum_freeze_max_age` is reached for a table, aggressive autovacuuming (anti-wraparound vacuum) is triggered, potentially causing performance degradation, which is a sign the system is already under stress.</p>",
    "root_cause": "Autovacuum processes are not running frequently enough, are misconfigured, are blocked by long-running transactions, or lack sufficient resources (e.g., `autovacuum_max_workers`), preventing them from freezing old transaction IDs and advancing `relfrozenxid` and `datfrozenxid` fast enough to stay ahead of the XID wraparound limit.",
    "bad_code": "The issue is typically a system configuration or workload problem, rather than a specific code snippet. However, a common contributor to autovacuum starvation is a long-running transaction (e.g., an uncommitted `BEGIN;` block or a poorly optimized query), which can hold back `oldest_xid` and block autovacuum from processing tables. \n\n```sql\n-- Example of a long-running transaction that could block autovacuum\nBEGIN;\n-- Perform some complex, time-consuming operations without committing\nSELECT pg_sleep(3600); -- Simulate a very long operation (1 hour)\n-- If this transaction is not committed or rolled back, it holds the xid_age\n-- and prevents autovacuum from freezing XIDs older than its own.\n```\n\nMisconfigurations in `postgresql.conf` that make autovacuum too passive:\n\n```\n# Ineffective autovacuum settings\nautovacuum = off              # Critical failure point: Autovacuum MUST be on\nautovacuum_max_workers = 1    # Too few workers for busy systems\nautovacuum_vacuum_cost_delay = 100ms # Too high, makes autovacuum slow\nautovacuum_freeze_max_age = 2000000000 # Too high, delays anti-wraparound vacuums\n```",
    "solution_desc": "The solution involves a multi-pronged approach: Proactive monitoring, aggressive and intelligent autovacuum configuration, and identifying/resolving blocking issues. \n\n1.  **Monitor XID Age:** Regularly check `SELECT datname, age(datfrozenxid) FROM pg_database;` to ensure `age` is well below `autovacuum_freeze_max_age` (default 200M, hard limit 2B). Also monitor `SELECT relname, age(relfrozenxid) FROM pg_class WHERE relkind = 'r' ORDER BY age(relfrozenxid) DESC;`.\n2.  **Optimize Autovacuum Configuration:** Adjust `postgresql.conf` parameters to be more aggressive for busy databases. Increase `autovacuum_max_workers`, decrease `autovacuum_vacuum_cost_delay`, and ensure `autovacuum_vacuum_scale_factor` and `autovacuum_analyze_scale_factor` are appropriate. Consider table-specific autovacuum settings if needed.\n3.  **Identify and Kill Blocking Queries:** Use `pg_stat_activity` to find long-running transactions or queries blocking autovacuum. Look for `state = 'active'` and `wait_event_type = 'Lock'` or `backend_type = 'autovacuum worker'` being blocked. Terminate problematic sessions if necessary (`pg_terminate_backend(pid)`).\n4.  **Regular Maintenance Vacuum:** In extreme cases or as a preventative measure, manually run `VACUUM FREEZE` on critical tables or `VACUUM FREEZE full_database;` (carefully, as it locks tables) to aggressively freeze XIDs.\n5.  **Address Transaction Id exhaustion warnings:** If you see warnings like \"database 'X' must be vacuumed within Y transactions\", act immediately.",
    "good_code": "```ini\n# In postgresql.conf, adjust these settings for a busy database\nautovacuum = on                   # Ensure autovacuum is always on\nautovacuum_max_workers = 3        # Increase workers (adjust based on CPU/IO)\nautovacuum_vacuum_cost_delay = 10ms # Decrease delay for faster vacuuming\nautovacuum_analyze_cost_delay = 10ms # Decrease analyze delay\nautovacuum_vacuum_cost_limit = -1 # Use autovacuum_vacuum_cost_limit from postgresql.conf (default -1 means no limit, use `vacuum_cost_limit`)\nautovacuum_freeze_max_age = 100000000 # Consider lowering to trigger anti-wraparound earlier\nlog_autovacuum_min_duration = 500ms # Log autovacuum runs taking longer than 500ms for monitoring\n```\n\n```sql\n-- Monitoring XID age\nSELECT datname, age(datfrozenxid) AS xid_age_database_level\nFROM pg_database;\n\nSELECT relname, age(relfrozenxid) AS xid_age_table_level\nFROM pg_class WHERE relkind = 'r' ORDER BY age(relfrozenxid) DESC LIMIT 10;\n\n-- Identifying blocking queries\nSELECT \n    pid,\n    usename,\n    application_name,\n    client_addr,\n    backend_start,\n    xact_start,\n    state,\n    wait_event_type,\n    wait_event,\n    query_start,\n    query\nFROM pg_stat_activity\nWHERE \n    (backend_type = 'client backend' OR backend_type = 'autovacuum launcher') AND \n    state != 'idle' \nORDER BY xact_start ASC;\n\n-- To terminate a problematic session (replace PID with actual process ID)\n-- SELECT pg_terminate_backend(PID);\n```",
    "verification": "After implementing changes, continuously monitor the `xid_age` of your databases and tables using the provided SQL queries. Check PostgreSQL logs for `xid wraparound` warnings or `autovacuum` related messages (especially if `log_autovacuum_min_duration` is set). Ensure that `autovacuum worker` processes are visible in `pg_stat_activity` and are actively processing tables. Verify that system performance remains stable or improves without hitting emergency autovacuum limits. The ultimate verification is the absence of XID wraparound-induced shutdowns.",
    "date": "2026-08-26",
    "id": 1787705038,
    "type": "error"
});