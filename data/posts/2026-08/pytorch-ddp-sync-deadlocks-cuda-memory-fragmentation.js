window.onPostDataLoaded({
    "title": "PyTorch DDP Sync Deadlocks & CUDA Memory Fragmentation",
    "slug": "pytorch-ddp-sync-deadlocks-cuda-memory-fragmentation",
    "language": "PyTorch, Python",
    "code": "DDP Sync Deadlocks",
    "tags": [
        "PyTorch",
        "Distributed Training",
        "CUDA",
        "Python",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PyTorch's DistributedDataParallel (DDP) is a powerful tool for scaling deep learning training across multiple GPUs and nodes. It works by replicating the model on each process/rank and then using collective communication (typically NCCL for GPUs) to synchronize gradients via an 'all-reduce' operation after each backward pass. DDP groups gradients into buckets and performs `all_reduce` on these buckets asynchronously to overlap communication with computation.</p><p>However, this asynchronous nature can lead to subtle issues. Synchronization deadlocks occur when different ranks encounter all-reduce operations in a non-deterministic order or one rank significantly lags, causing others to wait indefinitely. This is often exacerbated by dynamic computation graphs, varying tensor sizes across ranks, or imbalanced data loading. Meanwhile, CUDA memory fragmentation arises from the frequent allocation and deallocation of tensors of varying sizes during training, especially with dynamic batching or models that change shape, leading to wasted memory space and potential Out-Of-Memory (OOM) errors even when total free memory is sufficient.</p>",
    "root_cause": "<ul><li><b>DDP Deadlock:</b> Inconsistent computational graph across ranks, leading to different `all_reduce` call orders. One rank processing data or computation significantly slower than others, causing a bottleneck. Use of `find_unused_parameters=False` (default) with parts of the model not being trained/receiving gradients.</li><li><b>CUDA Memory Fragmentation:</b> PyTorch's default caching allocator, while efficient, can still lead to fragmentation over long training runs due to numerous allocations and deallocations of non-uniform tensor sizes, especially on GPUs with limited memory.</li></ul>",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\n\ndef setup(rank, world_size):\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear = nn.Linear(10, 10)\n\n    def forward(self, x, condition=False):\n        x = self.linear(x)\n        if condition and dist.get_rank() == 0: # Only rank 0 executes this branch\n            x = x * 2 # Creates different graph for rank 0\n        return x\n\ndef train(rank, world_size):\n    setup(rank, world_size)\n    model = SimpleModel().to(rank)\n    ddp_model = DDP(model, device_ids=[rank])\n    optimizer = torch.optim.SGD(ddp_model.parameters(), lr=0.01)\n\n    for i in range(5):\n        # Simulate different conditions across ranks causing graph divergence\n        input_data = torch.randn(2, 10).to(rank)\n        output = ddp_model(input_data, condition=(i % 2 == 0))\n        loss = output.mean()\n        loss.backward()\n        optimizer.step()\n        optimizer.zero_grad()\n\n    dist.destroy_process_group()\n\n# This simplified example demonstrates a potential for graph divergence leading to deadlock.\n# Actual fragmentation issues are harder to show in minimal code.\n```",
    "solution_desc": "<ul><li><b>Mitigating Deadlocks:</b> Ensure all ranks execute identical computation graphs. If dynamic graphs are necessary, set `find_unused_parameters=True` in DDP to allow unused parameters to be skipped in gradient synchronization. Manually ensure gradient synchronization for any parameters not part of the current graph. Use gradient accumulation to reduce the frequency of `all_reduce` calls. Disable Autograd anomaly detection (`torch.autograd.set_detect_anomaly(False)`) in production for performance, but enable it for debugging. Monitor data loading bottlenecks.</li><li><b>Addressing Fragmentation:</b> Periodically call `torch.cuda.empty_cache()` (use judiciously, as it can clear the cache and potentially slow down subsequent allocations). Consider setting `PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:X` environment variable to limit the maximum size of memory blocks that PyTorch's allocator can split, which can help reduce fragmentation for very large allocations, or `max_split_size_mb` can also be used to prevent very small allocations from consuming large contiguous blocks. Ensure tensor sizes are as uniform as possible. Profile memory usage with `torch.cuda.memory_summary()`.</li></ul>",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\n\ndef setup(rank, world_size):\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear = nn.Linear(10, 10)\n        # Add a dummy parameter that is always used to ensure consistent graph\n        self.dummy_param = nn.Parameter(torch.zeros(1))\n\n    def forward(self, x):\n        x = self.linear(x)\n        x = x + self.dummy_param # Always ensure all params are part of the graph\n        return x\n\ndef train_fixed(rank, world_size):\n    setup(rank, world_size)\n    model = SimpleModel().to(rank)\n    # Set find_unused_parameters=True if parts of graph are truly conditional\n    ddp_model = DDP(model, device_ids=[rank], find_unused_parameters=True)\n    optimizer = torch.optim.SGD(ddp_model.parameters(), lr=0.01)\n\n    for i in range(5):\n        input_data = torch.randn(2, 10).to(rank)\n        output = ddp_model(input_data)\n        loss = output.mean()\n        loss.backward()\n        optimizer.step()\n        optimizer.zero_grad()\n\n    # For memory fragmentation, consider:\n    if i % 100 == 0: # Periodically clear cache if memory is tight\n        torch.cuda.empty_cache()\n\n    dist.destroy_process_group()\n\n# Environment variable to mitigate fragmentation (set before runtime):\n# os.environ[\"PYTORCH_CUDA_ALLOC_CONF\"] = \"max_split_size_mb:128\"\n```",
    "verification": "<p>To verify the fix for deadlocks, monitor DDP training logs for stability across all ranks. Ensure no processes hang indefinitely. You can also temporarily enable `torch.autograd.set_detect_anomaly(True)` to catch inconsistencies in the backward pass graph, though this incurs a performance overhead. For fragmentation, regularly check GPU memory usage via `nvidia-smi` or `torch.cuda.memory_summary()` during training. Look for more consistent memory graphs without stair-step increases or unexpected OOMs. The total allocated memory should be closer to the actual tensors used, with less discrepancy between allocated and reserved memory.</p>",
    "date": "2026-08-17",
    "id": 1786938381,
    "type": "error"
});