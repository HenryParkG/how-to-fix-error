window.onPostDataLoaded({
    "title": "Elasticsearch Shard Relocation Storms & Disk I/O Saturation",
    "slug": "elasticsearch-shard-relocation-storms-disk-io-saturation",
    "language": "Elasticsearch",
    "code": "Disk Saturation",
    "tags": [
        "Elasticsearch",
        "Indexing",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch shard relocation storms occur when numerous shards simultaneously attempt to move between nodes. This phenomenon is often triggered by cluster changes such as a node failing, a new node joining, or significant reconfigurations of shard allocation settings. Each relocation involves copying the entire shard data (which can range from megabytes to gigabytes) across the network and then writing it to disk on the target node. If many relocations happen concurrently, the combined network and disk I/O operations can quickly saturate the capacities of the involved nodes.</p><p>This saturation has severe consequences: indexing rates plummet, search queries time out, the cluster becomes unresponsive, and nodes can even crash due to resource exhaustion. The problem is exacerbated by default settings that might allow too many concurrent relocations or by an underlying storage system that cannot handle the burst of I/O. Furthermore, during a storm, the cluster state can become unstable, leading to further cascading issues.</p>",
    "root_cause": "Elasticsearch shard relocation storms are caused by many shards simultaneously attempting to move between nodes, typically triggered by cluster topology changes (node failure/addition) or allocation setting modifications. This process saturates network bandwidth and, crucially, disk I/O on source, target, and other nodes, leading to performance degradation and instability.",
    "bad_code": "# Bad Elasticsearch Configuration / Scenario Leading to Storms\n\n# Scenario 1: Aggressive allocation settings for a large cluster\n# This might seem good for availability but can cause large-scale relocations.\ncluster.routing.allocation.cluster_concurrent_rebalance: 10 # Too many concurrent rebalances\ncluster.routing.allocation.node_concurrent_incoming_recoveries: 10 # Allow many incoming recoveries\ncluster.routing.allocation.node_concurrent_outgoing_recoveries: 10 # Allow many outgoing recoveries\n\n# Scenario 2: Node failure in a large cluster with default settings\n# When a node fails, all its shards (primaries and replicas) need to be\n# re-allocated and rebuilt across the remaining nodes, causing a surge.\n\n# Scenario 3: Adding a new node to a cluster with many small indices\n# Elasticsearch will try to balance shards, leading to many small relocations\n# which can still sum up to high I/O if the number is vast.\n\n# Scenario 4: Performing rolling restarts or upgrades without proper planning\n# Each node going offline/online can trigger waves of relocations.\n\n# No specific \"bad code\" snippet for a cluster config that causes this,\n# as it's more about operational patterns and defaults being overwhelmed.\n# The 'bad' is often implicit in operating a large, default-configured cluster\n# without controlling shard movement.",
    "solution_desc": "Mitigating shard relocation storms involves controlling the rate and concurrency of shard movements and ensuring underlying infrastructure can handle the load. These strategies provide a balance between cluster recovery speed and operational stability:<ol><li><b>Throttle Relocations</b>: Limit concurrent shard movements using cluster settings (<code>cluster.routing.allocation.cluster_concurrent_rebalance</code>, <code>node_concurrent_incoming_recoveries</code>, <code>node_concurrent_outgoing_recoveries</code>). Start with low values and incrementally increase as needed. Also, throttle recovery speed using <code>indices.recovery.max_bytes_per_sec</code>.</li><li><b>Dedicated Recovery Networks</b>: If possible in your cloud or on-premise setup, separate network traffic for shard recovery from regular search/indexing traffic to avoid contention.</li><li><b>Optimize Disk I/O</b>: Use fast storage (SSDs/NVMe) with sufficient IOPS. Ensure file systems are correctly configured (e.g., XFS with appropriate mount options).</li><li><b>Careful Node Management</b>: For planned maintenance (e.g., rolling restarts, node decommissioning), use \"graceful decommissioning\" by temporarily disabling shard allocation (<code>cluster.routing.allocation.enable</code>) and excluding the node (<code>cluster.routing.allocation.exclude._name</code>) to drain shards before shutting down.</li><li><b>Index Lifecycle Management (ILM)</b>: Manage shard sizes and number to prevent an explosion of tiny shards or extremely large ones. Smaller, more numerous shards can exacerbate relocation storms due to overhead per shard.</li><li><b>Monitoring</b>: Proactive monitoring of disk I/O, network traffic, CPU, and shard relocation status is crucial to detect and react to impending storms.</li></ol>",
    "good_code": "# Good Elasticsearch Configuration for controlling shard relocations\n\n# 1. Throttle concurrent rebalances/recoveries\n#   Lower values prevent I/O saturation but increase recovery time.\n#   Tune based on cluster size, disk speed, and network bandwidth.\ncluster.routing.allocation.cluster_concurrent_rebalance: 2 # Default is 2. Keep low for stability.\ncluster.routing.allocation.node_concurrent_incoming_recoveries: 2 # Default is 2. Limit incoming to a node.\ncluster.routing.allocation.node_concurrent_outgoing_recoveries: 2 # Default is 2. Limit outgoing from a node.\n\n# 2. Limit the speed of shard recovery (disk throttling)\n#   This limits the aggregate throughput of all shard recoveries on a node.\n#   Units: B, KB, MB, GB. Adjust based on your disk's actual I/O capacity.\nindices.recovery.max_bytes_per_sec: 50mb # Example: 50MB/s (default is 40MB/s)\n\n# 3. Use allocation awareness to prevent shards from being on same racks/zones\n#   Helps with availability, but ensure enough nodes are available in each zone.\n# cluster.routing.allocation.awareness.attributes: rack_id # Example attribute\n\n# 4. Proactive node decommissioning (API example)\n# When removing a node:\n# First, disable shard allocation entirely or just for primaries to prevent new allocations.\nPUT /_cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.enable\": \"primaries\" # Allow new primaries, but no replicas to be allocated\n  }\n}\n# Then, exclude the node by its name or IP to gracefully move all its shards off.\nPUT /_cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.exclude._name\": \"node_to_remove\"\n  }\n}\n# Wait until all shards have been moved off before shutting down the node.\n# Then, re-enable full allocation:\nPUT /_cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.enable\": \"all\"\n  }\n}\n\n# 5. Monitor disk utilization per node\n# GET _cat/allocation?v\n# GET _cat/shards?v\n# GET _nodes/stats?pretty",
    "verification": "<ul><li><b>Monitor Cluster Health</b>: Regularly check <code>GET _cluster/health</code> to ensure the cluster remains in <code>green</code> status.</li><li><b>Pending Tasks & Relocations</b>: Use <code>GET _cluster/pending_tasks</code> to see if the cluster is overloaded with management tasks. Use <code>GET _cat/recovery?v</code> or <code>GET _cat/shards?v</code> to observe ongoing shard recoveries/relocations, paying attention to their state and remaining bytes.</li><li><b>System Metrics</b>: Monitor host-level metrics like disk I/O (reads/writes per second, latency), network I/O, CPU utilization, and memory on all Elasticsearch nodes. Look for spikes correlating with cluster changes or shard movements.</li><li><b>Elasticsearch Metrics</b>: Use Node Stats API (<code>GET _nodes/stats</code>) to check segment merge statistics and <code>indices.recovery.current_throttle_bytes_per_sec</code> to confirm the throttling is active.</li><li><b>Alerting</b>: Set up alerts for disk I/O saturation, high CPU usage, yellow/red cluster status, or an unusually high number of concurrent shard recoveries.</li></ul>",
    "date": "2026-08-20",
    "id": 1787207468,
    "type": "error"
});