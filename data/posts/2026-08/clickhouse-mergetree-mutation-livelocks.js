window.onPostDataLoaded({
    "title": "Resolving ClickHouse MergeTree Mutation Livelocks",
    "slug": "clickhouse-mergetree-mutation-livelocks",
    "language": "ClickHouse",
    "code": "Mutation Livelock",
    "tags": [
        "ClickHouse",
        "SQL",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>ClickHouse's `MergeTree` family of tables is optimized for high-throughput data ingestion and analytical queries. Unlike traditional databases, `UPDATE` and `DELETE` operations (known as mutations) are not immediate atomic transactions. Instead, they are asynchronous background processes that apply changes by rewriting data parts. When a mutation is executed, ClickHouse marks the affected parts for mutation and applies the changes during subsequent merges or by creating new mutated parts.</p><p>A 'mutation livelock' can occur under high-frequency ingestion when new data parts are being constantly created and merged. The mutation process struggles to complete because the set of data parts it needs to modify is continually changing or being merged away before the mutation can fully apply. Each time a mutation attempts to process a part, new inserts might create more parts, or existing parts might merge, forcing the mutation to re-evaluate its target parts or restart its work. This can lead to mutations appearing to stall indefinitely, consuming resources without making progress, and creating a backlog of unapplied changes.</p>",
    "root_cause": "Asynchronous mutations continuously conflict with ongoing high-frequency data ingestion and background merge operations. The mutation process cannot find a stable set of data parts to apply its changes to, as new parts are constantly appearing or being merged, causing the mutation to perpetually restart or delay its progress.",
    "bad_code": "```sql\n-- Assuming a ClickHouse MergeTree table `my_table` with high-frequency INSERTs\nCREATE TABLE my_table\n(\n    `id` UInt64,\n    `value` String,\n    `timestamp` DateTime\n)\nENGINE = MergeTree\nORDER BY (id, timestamp);\n\n-- Continuous high-frequency INSERTs (simulated by frequent execution):\nINSERT INTO my_table VALUES (1, 'initial', now());\nINSERT INTO my_table VALUES (2, 'initial', now());\n-- ... thousands of inserts per second\n\n-- A mutation attempting to clean up old data or correct values\nALTER TABLE my_table DELETE WHERE timestamp < now() - INTERVAL 1 DAY;\n-- This DELETE mutation might livelock if INSERTs are too frequent\n```",
    "solution_desc": "<p>Resolving ClickHouse mutation livelocks under high-frequency ingestion requires strategies to reduce contention and optimize mutation execution:</p><ol><li><b>Batch Mutations:</b> Instead of frequent, small mutations, schedule mutations to run less often but on larger batches of data. This gives the mutation engine a better chance to complete its work between ingestion spikes.</li><li><b>Reduce Ingestion Spikes:</b> If possible, reduce or smooth out the ingestion rate during periods when mutations are critical. This might involve temporarily pausing non-essential inserts or buffering data externally.</li><li><b>Optimize MergeTree Settings:</b> Tune `MergeTree` parameters like `min_rows_for_wide_part`, `min_bytes_for_wide_part`, `min_rows_for_rollup_part`, etc., to control the size and frequency of data part merges. Larger parts can reduce the number of parts mutations need to consider.</li><li><b>Use `ReplicatedReplacingMergeTree` or `CollapsingMergeTree` for Updates/Deletes:</b> For use cases that involve updating or deleting records based on a primary key or version, consider using table engines specifically designed for this. `ReplacingMergeTree` can deduplicate data during merges, effectively acting as an update-on-insert. `CollapsingMergeTree` can collapse rows based on a `sign` column. These engines handle updates and deletions during merge time, reducing the need for explicit `ALTER TABLE ... UPDATE/DELETE` mutations.</li><li><b>Partitioning:</b> Ensure effective partitioning. If mutations are always on old data, partitioning by date/time can limit the scope of mutations to specific partitions, reducing the number of active parts they need to consider. Use `ALTER TABLE ... UPDATE/DELETE IN PARTITION` to target specific partitions.</li><li><b>Monitor Mutation Progress:</b> Regularly check the `system.mutations` table to track the status and progress of mutations.</li></ol>",
    "good_code": "```sql\n-- Use ReplacingMergeTree for deduplication/updates on insert\nCREATE TABLE my_table_replacing\n(\n    `id` UInt64,\n    `value` String,\n    `timestamp` DateTime\n)\nENGINE = ReplacingMergeTree(timestamp) -- Deduplicates based on id, keeping latest timestamp\nORDER BY id;\n\n-- Example of inserting new data, which might implicitly update old records with same ID\nINSERT INTO my_table_replacing VALUES (1, 'new_value', now());\nINSERT INTO my_table_replacing VALUES (2, 'another_new_value', now());\n\n-- To delete old data, batching and targeting partitions if possible:\n-- First, identify partitions to mutate (e.g., older than 1 day)\n-- SELECT DISTINCT partition FROM system.parts WHERE table = 'my_table' AND min_date < now() - INTERVAL 1 DAY;\n\n-- Then, execute a targeted, less frequent mutation\n-- (Assuming a partition column `dt` exists and is part of the partition key)\nALTER TABLE my_table DELETE WHERE dt < toDate(now() - INTERVAL 1 DAY) AND timestamp < now() - INTERVAL 1 DAY;\n\n-- To further reduce conflicts, adjust merge settings (example):\nALTER TABLE my_table MODIFY SETTING \n  merge_tree_min_rows_for_wide_part = 1000000, \n  merge_tree_min_bytes_for_wide_part = 1073741824; -- 1 GB\n```",
    "verification": "<p>Verify the fix by:</p><ul><li><b>Checking `system.mutations`:</b> After implementing the solutions, monitor the `system.mutations` table. Observe that mutations are progressing (`is_done = 1` eventually) and not stuck in a `STARTING` or perpetually incomplete state. The `num_parts_mutated_with_merge` and `num_parts_mutated_without_merge` should increment.</li><li><b>Monitoring System Performance:</b> Ensure that overall ClickHouse performance (query latency, ingestion rates) remains stable and doesn't degrade. Check for increased disk I/O or CPU usage related to merges and mutations.</li><li><b>Data Consistency:</b> Confirm that the mutations are correctly applied and the data reflects the intended changes.</li><li><b>Resource Usage:</b> Monitor CPU, memory, and disk usage for the ClickHouse server. The goal is to stabilize these resources, avoiding spikes due to continuous mutation attempts.</li></ul>",
    "date": "2026-08-14",
    "id": 1786701336,
    "type": "error"
});