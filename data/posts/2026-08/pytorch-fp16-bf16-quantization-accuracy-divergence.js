window.onPostDataLoaded({
    "title": "PyTorch Mixed Precision: Accuracy & Divergence",
    "slug": "pytorch-fp16-bf16-quantization-accuracy-divergence",
    "language": "PyTorch",
    "code": "Quantization Error",
    "tags": [
        "Deep Learning",
        "Python",
        "GPU",
        "Error Fix"
    ],
    "analysis": "<p>Mixed-precision training, using <code>fp16</code> (half-precision) or <code>bf16</code> (bfloat16) alongside <code>fp32</code> (single-precision), has become standard practice in Deep Learning for its significant benefits: reduced memory footprint, faster computations on compatible hardware (Tensor Cores), and improved power efficiency. However, these reduced precision formats come with limitations. <code>fp16</code> offers a wide dynamic range but limited precision, while <code>bf16</code> has a similar dynamic range to <code>fp32</code> but also limited precision. The accuracy degradation and model divergence problems typically stem from these precision constraints.</p><p>Specifically, issues arise when small values (e.g., gradients) underflow to zero, leading to vanishing gradients, or when large values (e.g., intermediate activations or weights during large updates) overflow to <code>Inf</code> or <code>NaN</code>. Catastrophic cancellation, where subtracting two nearly equal large numbers results in significant error, is also more common in lower precision. These numerical instabilities can cause training to stall, produce <code>NaN</code> losses, or result in a model that fails to converge or performs significantly worse than its <code>fp32</code> counterpart.</p>",
    "root_cause": "<p>The primary root cause is the limited numerical representation capabilities of <code>fp16</code> and <code>bf16</code> compared to <code>fp32</code>. <code>fp16</code> has only 10 bits for the mantissa, making it prone to precision loss and underflow for very small numbers. <code>bf16</code> (7 bits mantissa) mitigates the dynamic range issue (similar exponent range to <code>fp32</code>) but still has reduced precision, making it vulnerable to issues when computations require high precision (e.g., sums over many small values, or certain gradient calculations). Without careful management, operations like gradient accumulation, normalization layers, and activation functions can produce values that fall outside the representable range or lose critical precision, disrupting the learning process and causing training to diverge.</p>",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\n\nclass SimpleNet(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.fc = nn.Linear(10, 1)\n    def forward(self, x):\n        return self.fc(x)\n\nmodel = SimpleNet().cuda()\noptimizer = optim.SGD(model.parameters(), lr=0.01)\nloss_fn = nn.MSELoss()\n\n# Problematic: No gradient scaling for fp16, leading to potential underflow\n# This is a simplified example; real divergence often happens with complex models\n# and specific operations without proper AMP setup.\nfor epoch in range(5):\n    inputs = torch.randn(16, 10).cuda()\n    targets = torch.randn(16, 1).cuda()\n\n    optimizer.zero_grad()\n    with torch.cuda.amp.autocast(enabled=True, dtype=torch.float16): # Using fp16 directly\n        outputs = model(inputs)\n        loss = loss_fn(outputs, targets)\n\n    loss.backward() # Gradients might be too small for fp16, underflow to 0\n    optimizer.step()\n    print(f\"Epoch {epoch}, Loss: {loss.item()}\")\n    if torch.isnan(loss):\n        print(\"Model diverged!\")\n        break\n```",
    "solution_desc": "<p>The solution involves leveraging PyTorch's Automatic Mixed Precision (AMP) API with <code>GradScaler</code>. <code>GradScaler</code> dynamically scales the loss value before backpropagation, pushing gradients into a representable range for <code>fp16</code>. After gradients are computed, they are unscaled before the optimizer step. This prevents gradients from underflowing to zero. For <code>bf16</code>, which has a better dynamic range, gradient scaling is often not strictly necessary but can still be beneficial in extreme cases.</p><p>Additional strategies include:</p><ul><li><b>Monitoring:</b> Continuously monitor loss, gradients (norms), and activation distributions for <code>NaN</code>s or <code>Inf</code>s.</li><li><b>Layer Specificity:</b> Certain operations (e.g., softmax, sum reductions) might benefit from being kept in <code>fp32</code> even within an <code>autocast</code> block. PyTorch AMP typically handles this automatically, but custom <code>autocast</code> contexts can be created.</li><li><b>Initialization:</b> Robust weight initialization schemes are crucial to avoid initial instabilities.</li><li><b>Hyperparameters:</b> Small adjustments to learning rates or optimizer settings might be necessary.</li><li><b>Check for <code>NaN</code>s:</b> Implement checks for <code>NaN</code>s in losses or gradients during training and handle them (e.g., skip <code>optimizer.step()</code> for that batch).</li></ul>",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nfrom torch.cuda.amp import GradScaler, autocast\n\nclass SimpleNet(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.fc = nn.Linear(10, 1)\n    def forward(self, x):\n        return self.fc(x)\n\nmodel = SimpleNet().cuda()\noptimizer = optim.SGD(model.parameters(), lr=0.01)\nloss_fn = nn.MSELoss()\nscaler = GradScaler() # Initialize GradScaler for fp16 training\n\nfor epoch in range(5):\n    inputs = torch.randn(16, 10).cuda()\n    targets = torch.randn(16, 1).cuda()\n\n    optimizer.zero_grad()\n    # Use autocast with fp16\n    with autocast(enabled=True, dtype=torch.float16):\n        outputs = model(inputs)\n        loss = loss_fn(outputs, targets)\n\n    # Scale the loss and call backward()\n    scaler.scale(loss).backward()\n    # Unscale gradients and call optimizer.step()\n    scaler.step(optimizer)\n    # Update the scale for next iteration\n    scaler.update()\n\n    print(f\"Epoch {epoch}, Loss: {loss.item()}\")\n    if torch.isnan(loss):\n        print(\"Model diverged! NaN detected after scaler.step().\")\n        break\n```",
    "verification": "<p>Verification involves comparing the training stability and final model accuracy of the mixed-precision model against its <code>fp32</code> counterpart. During training, closely monitor the loss curves \u2013 they should be smooth and show clear convergence without erratic jumps or plateaus. Check for the presence of <code>NaN</code>s in the loss or model parameters; <code>NaN</code>s are a strong indicator of numerical instability. After training, evaluate the model on validation and test sets. The final accuracy of the mixed-precision model should be comparable to the <code>fp32</code> model, ideally within 0.1-0.5% margin. Tools like TensorBoard can be used to visualize loss, gradient norms, and activation histograms over time, which can highlight potential numerical issues.</p>",
    "date": "2026-08-28",
    "id": 1787904065,
    "type": "error"
});