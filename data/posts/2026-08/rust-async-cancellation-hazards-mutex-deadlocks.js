window.onPostDataLoaded({
    "title": "Rust Async: Cancellation Hazards & Mutex Deadlocks",
    "slug": "rust-async-cancellation-hazards-mutex-deadlocks",
    "language": "Rust",
    "code": "Async Cancellation",
    "tags": [
        "Rust",
        "Backend",
        "Concurrency",
        "Error Fix"
    ],
    "analysis": "<p>Rust's `async/await` ecosystem, particularly with runtimes like Tokio, provides powerful concurrency but introduces unique challenges, especially around task cancellation. Unlike synchronous code, an `async` task can be dropped (cancelled) at any `.await` point. This cancellation is not always atomic; if a task is cancelled mid-operation, it might leave shared resources in an inconsistent state or fail to release locks, leading to broken invariants or deadlocks.</p><p>A common pitfall involves `tokio::sync::Mutex`. If a task acquires a `tokio::sync::MutexGuard` and then `await`s an operation that is cancelled (e.g., due to a timeout or `tokio::select!` racing with a shutdown signal), the mutex guard might be dropped prematurely. If the guard's `Drop` implementation is responsible for releasing the lock, but the cancellation happens *before* the critical section completes or other invariants are restored, subsequent attempts to acquire the mutex could deadlock if the lock is never truly released or the protected data is left in an invalid state. This is distinct from `std::sync::Mutex` poison, as `tokio::sync::Mutex` doesn't poison.</p>",
    "root_cause": "Non-atomic async task cancellation. Specifically, holding a `tokio::sync::MutexGuard` across an `await` point in a cancellable task without ensuring proper resource cleanup or invariant restoration upon cancellation.",
    "bad_code": "```rust\nuse tokio::sync::Mutex;\nuse std::sync::Arc;\nuse tokio::time::{sleep, Duration};\n\nasync fn do_something_risky(data: Arc<Mutex<i32>>) {\n    println!(\"Task: Acquiring lock...\");\n    let mut guard = data.lock().await; // Acquire mutex\n    println!(\"Task: Lock acquired. Doing work...\");\n    \n    *guard += 1; // Critical section starts\n\n    // Simulate a long operation that could be cancelled\n    // If cancelled here, 'guard' is dropped, releasing the lock,\n    // but 'guard' might have left an invariant broken or not completed state.\n    // Or, more dangerously, if cancellation happened before 'guard' was dropped\n    // at an await point, and 'guard' was not properly setup to release.\n    // (In tokio::sync::Mutex, guard drop releases the lock. The issue is if\n    // *other* invariants are broken that *should* have been restored later.)\n    sleep(Duration::from_secs(5)).await; // Await point where cancellation can occur\n\n    // In a more complex scenario, imagine more operations here that restore invariants\n    // or write to other resources. If cancelled before these complete, inconsistencies.\n    *guard += 1; // More work, assuming this state is valid\n\n    println!(\"Task: Work done. Releasing lock.\");\n} // 'guard' is dropped here, releasing the mutex\n\n#[tokio::main]\nasync fn main() {\n    let shared_data = Arc::new(Mutex::new(0));\n    let data_clone = shared_data.clone();\n\n    let handle = tokio::spawn(async move {\n        do_something_risky(data_clone).await;\n    });\n\n    // Simulate a scenario where the task is cancelled after a short period\n    sleep(Duration::from_secs(1)).await;\n    println!(\"Main: Aborting task early.\");\n    handle.abort(); // Cancellation point\n\n    // Wait for the task to finish (or be aborted)\n    let _ = handle.await;\n\n    // Attempt to acquire the lock again after potential cancellation\n    println!(\"Main: Attempting to acquire lock again.\");\n    let final_guard = shared_data.lock().await; // This *could* deadlock if invariant broken.\n                                                // For tokio::sync::Mutex, the lock itself is released.\n                                                // The actual hazard is broken *data invariants*.\n                                                // For simplicity, this example illustrates a hazard.\n    println!(\"Main: Final data: {}\", *final_guard);\n}\n```",
    "solution_desc": "<p>To mitigate async cancellation hazards and prevent broken invariants/deadlocks, follow these principles:</p><ul><li><b>Keep critical sections short:</b> Minimize the amount of work done while holding a mutex. Release locks before any `.await` points where cancellation is possible.</li><li><b>Use cancellation-safe primitives:</b> `tokio::sync::Mutex` guards *do* release the lock when dropped, so the lock itself isn't poisoned. The danger lies in the *data* protected by the mutex being in an inconsistent state if a task is cancelled mid-update.</li><li><b>Isolate cancellable logic:</b> Structure your code so that tasks that can be cancelled (e.g., those passed to `tokio::select!`, or with explicit timeouts) perform only idempotent operations or operations that are safe to abort.</li><li><b>Leverage `tokio::select!` carefully:</b> When using `select!`, ensure all branches are cancellation-safe or handle cancellation explicitly. For example, if one branch completes and cancels another, ensure the cancelled branch cleans up properly.</li><li><b>Use `tokio::task::AbortHandle` for graceful shutdowns:</b> Instead of immediate `abort()`, consider sending a shutdown signal to a task and letting it complete its current work or perform a controlled exit.</li><li><b>Employ resource guards for consistency:</b> If an operation spans multiple await points and needs to maintain an invariant, ensure that resource guards (similar to how `MutexGuard` works) perform necessary cleanup in their `Drop` implementations, even if cancelled. Or, refactor to avoid holding the guard across `.await`.</li></ul>",
    "good_code": "```rust\nuse tokio::sync::Mutex;\nuse std::sync::Arc;\nuse tokio::time::{sleep, Duration, timeout};\nuse anyhow::Result;\n\nasync fn do_something_safe(data: Arc<Mutex<i32>>) -> Result<()> {\n    println!(\"Task: Acquiring lock...\");\n    let mut guard = data.lock().await; // Acquire mutex\n    println!(\"Task: Lock acquired. Doing work...\");\n    \n    // Perform all critical updates *without* an await point\n    *guard += 1; // First part of update\n    \n    // If other operations are needed, consider breaking them into smaller, atomic steps\n    // or explicitly handling potential inconsistencies if they span await points.\n    \n    println!(\"Task: Critical section complete. Releasing lock.\");\n    drop(guard); // Explicitly drop the guard before any potential await points\n\n    // Now, perform long-running or cancellable operations *outside* the mutex lock\n    println!(\"Task: Performing non-critical, cancellable work...\");\n    sleep(Duration::from_secs(5)).await;\n\n    println!(\"Task: Non-critical work done.\");\n    Ok(())\n}\n\n#[tokio::main]\nasync fn main() {\n    let shared_data = Arc::new(Mutex::new(0));\n    let data_clone = shared_data.clone();\n\n    println!(\"Main: Spawning task with a timeout.\");\n    let result = timeout(Duration::from_secs(1), do_something_safe(data_clone)).await;\n\n    match result {\n        Ok(Ok(_)) => println!(\"Main: Task completed successfully.\"),\n        Ok(Err(e)) => println!(\"Main: Task failed with error: {}\", e),\n        Err(_) => println!(\"Main: Task timed out and was cancelled.\"),\n    }\n\n    // Attempt to acquire the lock again after potential cancellation\n    println!(\"Main: Attempting to acquire lock again.\");\n    let final_guard = shared_data.lock().await;\n    println!(\"Main: Final data: {}\", *final_guard);\n    // The lock is never held across the cancellation point, ensuring consistency of shared_data.\n}\n```",
    "verification": "<p>Verify the fix by running unit and integration tests that simulate cancellation scenarios (e.g., using `tokio::time::timeout` or `tokio::select!` with an abort signal). Observe that shared resources remain in a consistent state and no deadlocks occur. Use tools like `tokio-console` for observing task lifecycles and potential resource leaks. Ensure that invariants related to shared state are explicitly checked before and after critical operations, especially when those operations involve potential await points.</p>",
    "date": "2026-08-31",
    "id": 1788143082,
    "type": "error"
});