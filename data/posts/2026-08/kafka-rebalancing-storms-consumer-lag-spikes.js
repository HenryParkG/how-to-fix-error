window.onPostDataLoaded({
    "title": "Kafka: Rebalancing Storms & Consumer Lag Spikes",
    "slug": "kafka-rebalancing-storms-consumer-lag-spikes",
    "language": "Kafka",
    "code": "RebalanceStorm",
    "tags": [
        "Messaging",
        "Distributed Systems",
        "Performance",
        "Kafka",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Kafka consumer group rebalancing is the mechanism by which partitions are distributed among the active consumers in a group. While essential for fault tolerance and scalability, frequent or poorly handled rebalances can lead to 'rebalancing storms' and significant 'consumer lag spikes'. During a rebalance, consumers stop processing messages, revoke their assigned partitions, and then reacquire new assignments. This pause in processing causes accumulated messages (lag), and when processing resumes, consumers may rush to catch up, leading to high CPU/memory usage and increased end-to-end latency. A rebalancing storm is a chain reaction of rebalances, where one rebalance triggers another, severely impacting the stability and performance of the consumer group.</p>",
    "root_cause": "The root causes for rebalancing storms and lag spikes are primarily related to misconfigurations or instability in consumer clients or the Kafka cluster itself:<ul><li><b>Misconfigured `session.timeout.ms` and `heartbeat.interval.ms`</b>: If `session.timeout.ms` (the maximum time a consumer can go without sending a heartbeat before being considered dead) is too short, or `heartbeat.interval.ms` (how often a consumer sends heartbeats) is too long, consumers may frequently be timed out, triggering rebalances.</li><li><b>Slow message processing</b>: If a consumer takes too long to process a batch of messages, exceeding `max.poll.interval.ms` (the maximum delay between invocations of `poll()` before the consumer leaves the group), it can be evicted, causing a rebalance.</li><li><b>Frequent consumer joins/leaves</b>: Unstable consumer applications (crashing, restarting, redeploying) constantly joining or leaving the group will trigger rebalances.</li><li><b>Large `max.poll.records` with slow processing</b>: If a consumer pulls a very large batch but processes it slowly, it's more likely to hit `max.poll.interval.ms`.</li><li><b>Broker instability</b>: Frequent broker restarts or network issues can cause consumers to lose connection and rebalance.</li><li><b>Client-side GC pauses</b>: Long garbage collection pauses in JVM-based Kafka clients can prevent heartbeats from being sent, leading to timeouts.</li></ul>",
    "bad_code": "```java\n// In a Kafka Consumer configuration (e.g., Spring Kafka, raw Java client)\nProperties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"group.id\", \"my-consumer-group\");\nprops.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\n\n// Potentially problematic defaults or short timeouts\nprops.put(\"session.timeout.ms\", \"10000\");        // 10 seconds, might be too short\nprops.put(\"heartbeat.interval.ms\", \"3000\");      // 3 seconds\nprops.put(\"max.poll.interval.ms\", \"300000\");     // 5 minutes, if processing takes longer, it will rebalance\nprops.put(\"max.poll.records\", \"500\");           // Pulls 500 records per poll\n\nKafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(Collections.singletonList(\"my-topic\"));\n\nwhile (true) {\n    ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n    for (ConsumerRecord<String, String> record : records) {\n        // Simulate slow processing for each record\n        try { Thread.sleep(50); } catch (InterruptedException e) {} // 50ms per record\n        // With max.poll.records = 500, 500 * 50ms = 25 seconds of processing.\n        // If there's overhead, this could exceed max.poll.interval.ms (300 seconds) easily\n        // leading to rebalance if overall processing time for a batch is high.\n    }\n    consumer.commitSync();\n}\n```",
    "solution_desc": "To mitigate rebalancing storms and lag spikes, a multi-pronged approach is needed:<ul><li><b>Tune Consumer Configuration</b>: Increase `session.timeout.ms` (e.g., 30-45 seconds) and ensure `heartbeat.interval.ms` is roughly 1/3 of `session.timeout.ms`. Crucially, adjust `max.poll.interval.ms` to be significantly longer than your *maximum expected batch processing time*.</li><li><b>Optimize Message Processing</b>: Ensure message processing is efficient. If processing is inherently slow, consider offloading it to a separate thread pool or a dedicated processing service after consuming, only committing offsets once processing is complete.</li><li><b>Implement Static Membership (Kafka 2.3+)</b>: For stateful applications, use `group.instance.id` to enable static membership. This allows a consumer instance to restart without triggering a full rebalance, significantly reducing rebalance frequency.</li><li><b>Monitor Consumer Health</b>: Track consumer lag (using tools like Kafka Lag Exporter), rebalance events, and consumer group state. Alerts should be set up for high lag or frequent rebalances.</li><li><b>Graceful Shutdowns</b>: Ensure consumer applications shut down gracefully, committing offsets and leaving the group cleanly.</li><li><b>Horizontal Scaling</b>: Ensure enough consumers are available to handle the topic's partitions without overwhelming individual instances.</li></ul>",
    "good_code": "```java\n// In a Kafka Consumer configuration\nProperties props = new Properties();\nprops.put(\"bootstrap.servers\", \"localhost:9092\");\nprops.put(\"group.id\", \"my-consumer-group\");\nprops.put(\"key.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\nprops.put(\"value.deserializer\", \"org.apache.kafka.common.serialization.StringDeserializer\");\n\n// Optimized configuration for stability\nprops.put(\"session.timeout.ms\", \"45000\");        // Increased to 45 seconds\nprops.put(\"heartbeat.interval.ms\", \"15000\");     // 15 seconds (1/3 of session.timeout.ms)\n// Crucial: Set max.poll.interval.ms based on your actual processing time.\n// If processing 500 records @ 50ms/record takes ~25s, allow ample buffer (e.g., 5 minutes or more)\nprops.put(\"max.poll.interval.ms\", \"600000\");     // 10 minutes, providing ample time for batch processing\nprops.put(\"max.poll.records\", \"500\");           // Keep this reasonable based on memory and processing capacity\n\n// For static membership (Kafka 2.3+): assign a unique instance ID\n// props.put(\"group.instance.id\", \"my-unique-consumer-instance-1\");\n\nKafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(Collections.singletonList(\"my-topic\"));\n\n// Graceful shutdown hook\nRuntime.getRuntime().addShutdownHook(new Thread(() -> {\n    System.out.println(\"Closing consumer...\");\n    consumer.close();\n}));\n\nwhile (true) {\n    ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n    if (!records.isEmpty()) {\n        // Process records efficiently (e.g., in a thread pool for parallel processing)\n        for (ConsumerRecord<String, String> record : records) {\n            // Actual processing logic here. Ensure it's optimized.\n            // Example: offload to another thread for non-blocking processing\n            // new Thread(() -> processRecord(record)).start();\n            System.out.printf(\"offset = %d, key = %s, value = %s\\n\", record.offset(), record.key(), record.value());\n        }\n        consumer.commitSync(); // Commit offsets after successful processing\n    }\n}\n```",
    "verification": "To verify the fix:<ul><li><b>Monitor Consumer Lag</b>: Use Kafka Lag Exporter or similar tools to track consumer group lag. Look for a stable, low lag, without frequent or drastic spikes.</li><li><b>Observe Rebalance Events</b>: Monitor Kafka broker logs for `GroupCoordinator` messages indicating rebalances. Your monitoring solution should also expose rebalance metrics. The goal is to see fewer rebalances, ideally only upon intentional deployment or scaling.</li><li><b>Application Metrics</b>: Check your application's throughput, latency, and resource (CPU, memory) usage. These should remain stable under load, without the erratic behavior associated with rebalance storms.</li><li><b>Kafka Tools</b>: Use `kafka-consumer-groups.sh --bootstrap-server <broker> --describe --group <group-id>` to inspect the state of your consumer group and ensure stable partition assignments.</li></ul>",
    "date": "2026-08-25",
    "id": 1787639442,
    "type": "error"
});