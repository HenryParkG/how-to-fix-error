window.onPostDataLoaded({
    "title": "Airflow Sensor Deadlocks: Avoiding DAG Cycles & Starvation",
    "slug": "airflow-sensor-deadlocks-dag-cycle-detection",
    "language": "Python",
    "code": "SensorDeadlock",
    "tags": [
        "Python",
        "Infra",
        "Airflow",
        "DAGs",
        "Error Fix"
    ],
    "analysis": "<p>Airflow sensors are powerful tools for orchestrating workflows by waiting for external conditions or the completion of other tasks/DAGs. However, their misuse can lead to insidious problems like deadlocks or resource starvation, severely impacting DAG execution. A common scenario for deadlocks arises when sensors form a circular dependency, either directly or indirectly. For instance, Task A waits for Task B, and Task B waits for Task A. While Airflow's DAG parser has some basic cycle detection for direct task dependencies within a single DAG, more complex, cross-DAG cycles involving sensors are harder to detect automatically.</p><p>Sensor deadlocks also occur when a sensor continuously consumes worker slots or database connections while waiting for an an event that will never happen due to a dependency loop, or because the condition it's waiting for is itself blocked. This can deplete available resources, preventing other tasks, including those that would resolve the sensor's condition, from running. Long-running poke-mode sensors are particularly prone to this if their intervals are too short or if they run indefinitely for conditions that are never met.</p>",
    "root_cause": "Circular dependencies involving sensors, either within a single DAG or, more commonly, across multiple DAGs, create a state where no task can proceed because each is waiting for a predecessor that is itself waiting. Additionally, excessive resource consumption by misconfigured sensors can lead to starvation, preventing the resolution of the sensor's waiting condition.",
    "bad_code": "from airflow import DAG\nfrom airflow.sensors.external_task import ExternalTaskSensor\nfrom airflow.operators.dummy import DummyOperator\nfrom datetime import datetime, timedelta\n\n# --- DAG A ---\nwith DAG(\n    dag_id='dag_a',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=timedelta(days=1),\n    catchup=False,\n    tags=['example']\n) as dag_a:\n    start_a = DummyOperator(task_id='start_a')\n    \n    # Sensor A waits for Task B in DAG B\n    wait_for_task_b_in_dag_b = ExternalTaskSensor(\n        task_id='wait_for_task_b_in_dag_b',\n        external_dag_id='dag_b',\n        external_task_id='task_b',\n        allowed_states=['success'],\n        failed_states=['failed', 'skipped'],\n        poke_interval=5,\n        timeout=600,\n        mode='poke' # Can consume worker slots\n    )\n\n    task_a = DummyOperator(task_id='task_a')\n\n    start_a >> wait_for_task_b_in_dag_b >> task_a\n\n# --- DAG B ---\nwith DAG(\n    dag_id='dag_b',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=timedelta(days=1),\n    catchup=False,\n    tags=['example']\n) as dag_b:\n    start_b = DummyOperator(task_id='start_b')\n    \n    # Sensor B waits for Task A in DAG A\n    wait_for_task_a_in_dag_a = ExternalTaskSensor(\n        task_id='wait_for_task_a_in_dag_a',\n        external_dag_id='dag_a',\n        external_task_id='task_a',\n        allowed_states=['success'],\n        failed_states=['failed', 'skipped'],\n        poke_interval=5,\n        timeout=600,\n        mode='poke' # Can consume worker slots\n    )\n\n    task_b = DummyOperator(task_id='task_b')\n\n    start_b >> wait_for_task_a_in_dag_a >> task_b\n\n# This creates a deadlock:\n# dag_a.wait_for_task_b_in_dag_b waits for dag_b.task_b\n# dag_b.wait_for_task_a_in_dag_a waits for dag_a.task_a\n# If task_a depends on wait_for_task_b_in_dag_b, and task_b depends on wait_for_task_a_in_dag_a,\n# neither can ever complete if they are also part of the chain they are waiting for.\n# More simply, if the entire DAG A must complete for task_a, and DAG B for task_b.\n# The example above is a simplified direct circular dependency.",
    "solution_desc": "Preventing sensor deadlocks and starvation requires careful DAG design and sensor configuration:\n1.  **Acyclic Dependencies**: Rigorously enforce acyclic dependencies across all DAGs. Cross-DAG dependencies should ideally flow in one direction (e.g., downstream DAGs wait for upstream DAGs, but never the reverse). Use an architectural diagram to visualize dependencies between DAGs to detect potential cycles.\n2.  **External Triggering**: Instead of sensors waiting for the *completion* of another DAG in a circular fashion, consider using `ExternalDagRunSensor` or external triggers (e.g., message queues, S3 events) to initiate downstream DAGs upon upstream completion, breaking direct Airflow-level waits.\n3.  **Smart Sensors/Deferrable Operators**: For resource-intensive waiting, switch from `poke` mode to `reschedule` mode for sensors where available. Even better, leverage Airflow 2.2+'s <a href=\"https://airflow.apache.org/docs/apache-airflow/stable/authoring-and-scheduling/deferrable-operators.html\">deferrable operators</a> which allow sensors to release worker slots while waiting, moving their logic to a dedicated \"triggerer\" component. This prevents worker starvation.\n4.  **Timeouts and Retries**: Always set reasonable `timeout` values for sensors to prevent them from waiting indefinitely. Configure `retries` and `retry_delay` appropriately.\n5.  **Monitoring and Alerts**: Implement monitoring for long-running or stuck sensors, and set up alerts to proactively identify potential deadlocks or non-progressing DAGs.\n6.  **Dependency Visualization Tools**: Use external tools or custom scripts to visualize inter-DAG dependencies, especially for larger Airflow deployments, to proactively identify potential circular logic that the Airflow UI might not fully expose.",
    "good_code": "from airflow import DAG\nfrom airflow.sensors.external_task import ExternalTaskSensor\nfrom airflow.operators.dummy import DummyOperator\nfrom datetime import datetime, timedelta\n\n# --- Upstream DAG (Dag A) ---\nwith DAG(\n    dag_id='upstream_dag_a',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=timedelta(days=1),\n    catchup=False,\n    tags=['example', 'fixed']\n) as upstream_dag_a:\n    start_a = DummyOperator(task_id='start_a')\n    task_a_processor = DummyOperator(task_id='task_a_processor')\n    end_a = DummyOperator(task_id='end_a') # This task signifies completion of A\n\n    start_a >> task_a_processor >> end_a\n\n# --- Downstream DAG (Dag B) ---\nwith DAG(\n    dag_id='downstream_dag_b',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=timedelta(days=1),\n    catchup=False,\n    tags=['example', 'fixed']\n) as downstream_dag_b:\n    start_b = DummyOperator(task_id='start_b')\n    \n    # Sensor B waits for the completion of 'end_a' in 'upstream_dag_a'\n    # This creates a one-way dependency. Dag B waits for Dag A, but Dag A does not wait for Dag B.\n    wait_for_upstream_dag_a = ExternalTaskSensor(\n        task_id='wait_for_upstream_dag_a_completion',\n        external_dag_id='upstream_dag_a',\n        external_task_id='end_a', # Wait for a specific task that signals completion\n        allowed_states=['success'],\n        failed_states=['failed', 'skipped'],\n        poke_interval=30, # Increased poke interval to reduce resource consumption\n        timeout=3600, # Longer timeout\n        mode='reschedule', # Use reschedule mode to free up worker slots\n        # For Airflow 2.2+, if `use_task_execution_date` is True, it waits for the exact run.\n        # If False, it waits for the *most recent* successful run before the current DAG run's execution_date.\n        deferrable=True # If using triggerer, this is ideal.\n    )\n\n    task_b_processor = DummyOperator(task_id='task_b_processor')\n    end_b = DummyOperator(task_id='end_b')\n\n    start_b >> wait_for_upstream_dag_a >> task_b_processor >> end_b\n\n# This setup ensures a clear, one-way dependency from downstream_dag_b to upstream_dag_a,\n# preventing circular waits. The use of 'reschedule' mode or 'deferrable'\n# reduces resource contention.",
    "verification": "Verify the fix by deploying the corrected DAGs to an Airflow environment.\n1.  **No Circular Dependencies**: Visually inspect the DAGs in the Airflow UI, or use Airflow's metadata database to query task dependencies, confirming that no sensor forms a backward link that would complete a cycle.\n2.  **Resource Utilization**: Monitor worker slot usage and Airflow metadata database connections during peak loads. Sensors configured in `reschedule` mode or `deferrable` mode should release worker slots while waiting, leading to lower concurrent worker usage compared to `poke` mode.\n3.  **Successful Execution**: Observe the successful and timely completion of DAG runs, particularly those involving cross-DAG dependencies. If a condition is not met, the sensor should time out gracefully instead of waiting indefinitely.\n4.  **Logs**: Check task logs for sensors; they should indicate rescheduling rather than continuous polling if `mode='reschedule'` is used.",
    "date": "2026-08-17",
    "id": 1786948554,
    "type": "error"
});