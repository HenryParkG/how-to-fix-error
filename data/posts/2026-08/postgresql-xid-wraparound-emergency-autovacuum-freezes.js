window.onPostDataLoaded({
    "title": "PostgreSQL XID Wraparound & Emergency Freeze",
    "slug": "postgresql-xid-wraparound-emergency-autovacuum-freezes",
    "language": "PostgreSQL",
    "code": "PSTG_XID_WRAP",
    "tags": [
        "PostgreSQL",
        "Database",
        "SQL",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit transaction ID (XID) counter to uniquely identify database transactions. This counter increments with every new transaction. While 32 bits provide a vast range of IDs, they are finite. At approximately 2 billion transactions, the XID counter wraps around to zero. If old transaction IDs are not marked as 'frozen' (meaning they are no longer relevant to snapshot visibility rules), new transactions could potentially use XIDs that are older than existing, unfrozen transaction IDs, leading to severe data corruption or unexpected behavior where old data appears new.</p><p>To prevent this, PostgreSQL's autovacuum process plays a crucial role. It scans tables and 'freezes' old transaction IDs, effectively marking them as permanently visible to all future transactions. If autovacuum cannot keep up with the transaction rate, or if it's disabled/misconfigured, the XID counter can get dangerously close to wraparound. When this critical threshold is reached (typically 2 billion transactions remaining until wraparound), PostgreSQL enters an 'emergency autovacuum freeze' mode, where it aggressively vacuums and freezes table rows to avoid catastrophic failure. If it still fails, the database will shut down to prevent data loss, signaling a fatal error.</p>",
    "root_cause": "The 32-bit transaction ID (XID) counter nearing its maximum value, coupled with insufficient or delayed autovacuum activity. Autovacuum is responsible for 'freezing' old transaction IDs, making them invisible to future wraparound conditions. If it fails to do so for a significant number of transactions, the database is at risk of XID wraparound, which can lead to data corruption or forced shutdown.",
    "bad_code": "The issue is typically operational/configuration, not directly 'bad code'. However, an application that generates an extremely high volume of short-lived transactions without adequate autovacuum tuning can accelerate the problem. Monitoring XID age is key:\n\n<pre><code>SELECT max(age(datfrozenxid)) FROM pg_database;\nSELECT c.relname, age(c.relfrozenxid) AS xid_age_rows FROM pg_class c JOIN pg_namespace n ON (c.relnamespace = n.oid) WHERE c.relkind IN ('r', 'm') AND n.nspname NOT IN ('pg_catalog', 'information_schema') ORDER BY xid_age_rows DESC;</code></pre>\n\nA high `max(age(datfrozenxid))` (e.g., > 1.8 billion) indicates a looming wraparound crisis.",
    "solution_desc": "The primary solution involves ensuring that autovacuum is running effectively and aggressively enough to keep XID age below critical thresholds. This may require tuning autovacuum parameters globally and at the table level. In an emergency, manually forcing a `VACUUM FREEZE` on problematic tables or the entire database is necessary. For long-term prevention, monitor `transaction_id_wraparound_age` and related metrics, adjust `autovacuum_freeze_max_age`, and consider increasing `autovacuum_vacuum_cost_limit` or `autovacuum_max_workers`. Identify and address any tables with exceptionally large `relfrozenxid` ages. If a database is already in emergency freeze mode, it's often best to let autovacuum complete its aggressive run, or manually intervene with `VACUUM FREEZE VERBOSE` on the most critical tables, typically starting with those reported by `pg_class` as having the oldest XID age.",
    "good_code": "To force an emergency freeze and monitor:\n\n<pre><code>-- Manually force vacuum freeze on a problematic table (replace 'your_table')\nVACUUM FREEZE VERBOSE your_table;\n\n-- Or for the entire database (use with extreme caution, can block)\nVACUUM FREEZE VERBOSE;\n\n-- Tune autovacuum settings (in postgresql.conf or for specific tables)\nALTER SYSTEM SET autovacuum_freeze_max_age = 150000000; -- Lower than default 200M\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = 2000; -- Increase cost limit\nALTER TABLE your_large_table SET (autovacuum_freeze_max_age = 100000000);\n\n-- Reload config to apply changes (for ALTER SYSTEM)\nSELECT pg_reload_conf();\n\n-- Monitor autovacuum activity\nSELECT datname, autovacuum_count, last_autovacuum FROM pg_stat_database;\nSELECT relname, last_autovacuum, last_user_vacuum FROM pg_stat_user_tables WHERE relname='your_table';</code></pre>",
    "verification": "After applying fixes, regularly monitor `max(age(datfrozenxid))` from `pg_database` and individual table `relfrozenxid` ages to ensure they are decreasing and remain well below the 2 billion transaction threshold (a safe margin is usually < 1.5 billion). Observe `pg_stat_activity` for active autovacuum processes, ensuring they are not being blocked or taking excessively long. Check PostgreSQL logs for warnings or errors related to XID wraparound or autovacuum failures. The goal is to see stable or decreasing XID age across the database.",
    "date": "2026-08-21",
    "id": 1787283798,
    "type": "error"
});