window.onPostDataLoaded({
    "title": "Mitigating Kafka Rebalance Storms & Cascades",
    "slug": "kafka-consumer-rebalance-storms-revocation-cascades",
    "language": "Java",
    "code": "CommitFailedException",
    "tags": [
        "Kafka",
        "Java",
        "Distributed Systems",
        "Error Fix"
    ],
    "analysis": "<p>In Apache Kafka consumer clusters processing heterogeneous or heavy workloads, consumer group instability often manifests as severe rebalance cascades. When a consumer node encounters a burst of complex records, record processing time exceeds <code>max.poll.interval.ms</code>, causing the consumer coordinator to consider the node dead and evict it from the consumer group.</p><p>Under legacy eager rebalance protocols (e.g., <code>RangeAssignor</code> or <code>RoundRobinAssignor</code>), an eviction forces <em>every</em> consumer in the group to revoke all assigned partitions, drop localized cache state, and pause message consumption. Reconnecting consumers trigger iterative join-group phases, creating a cascading storm where timeouts compound, offsets fail to commit (<code>CommitFailedException</code>), and message latency spikes uncontrollably.</p>",
    "root_cause": "Synchronous, long-running record processing inside the main consumer poll thread breaches `max.poll.interval.ms`, combined with the use of eager partition assignors that execute full revocation across all partitions during membership changes.",
    "bad_code": "Properties props = new Properties();\nprops.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, \"kafka:9092\");\nprops.put(ConsumerConfig.GROUP_ID_CONFIG, \"analytics-pipeline\");\nprops.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);\nprops.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class);\n// Buggy: Defaults to RangeAssignor (Eager) and small max.poll.interval.ms\nprops.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG, 10000);\n\nKafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(Collections.singletonList(\"events\"));\n\nwhile (true) {\n    ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n    for (ConsumerRecord<String, String> record : records) {\n        // Synchronous heavy compute blocks the poll loop, triggering consumer eviction\n        processExpensiveBusinessLogic(record.value()); \n    }\n    consumer.commitSync(); // Throws CommitFailedException after timeout\n}",
    "solution_desc": "Upgrade the partition assignment strategy to `CooperativeStickyAssignor`, enabling incremental cooperative rebalances where unaffected partitions continue processing uninterrupted. Offload heavyweight execution to a bounded worker thread pool, maintaining strict heartbeats and periodic lightweight `poll()` invocations.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport org.apache.kafka.common.serialization.StringDeserializer;\nimport java.time.Duration;\nimport java.util.*;\nimport java.util.concurrent.*;\n\npublic class ResilientKafkaConsumer {\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(ConsumerConfig.BOOTSTRAP_SERVERS_CONFIG, \"kafka:9092\");\n        props.put(ConsumerConfig.GROUP_ID_CONFIG, \"analytics-pipeline\");\n        props.put(ConsumerConfig.KEY_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class.getName());\n        props.put(ConsumerConfig.VALUE_DESERIALIZER_CLASS_CONFIG, StringDeserializer.class.getName());\n        \n        // 1. Cooperative rebalance avoids full partition revocation\n        props.put(ConsumerConfig.PARTITION_ASSIGNMENT_STRATEGY_CONFIG,\n                Collections.singletonList(CooperativeStickyAssignor.class.getName()));\n        props.put(ConsumerConfig.MAX_POLL_INTERVAL_MS_CONFIG, 300000); // 5 min headroom\n        props.put(ConsumerConfig.MAX_POLL_RECORDS_CONFIG, 500);\n        props.put(ConsumerConfig.ENABLE_AUTO_COMMIT_CONFIG, \"false\");\n\n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(List.of(\"events\"));\n\n        ExecutorService workers = new ThreadPoolExecutor(8, 8, 0L, TimeUnit.MILLISECONDS,\n                new ArrayBlockingQueue<>(1000), new ThreadPoolExecutor.CallerRunsPolicy());\n\n        try {\n            while (!Thread.currentThread().isInterrupted()) {\n                ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(250));\n                if (!records.isEmpty()) {\n                    List<Future<?>> tasks = new ArrayList<>();\n                    for (ConsumerRecord<String, String> record : records) {\n                        tasks.add(workers.submit(() -> processPayload(record.value())));\n                    }\n                    for (Future<?> task : tasks) {\n                        task.get(); // Wait for current batch with bounded thread execution\n                    }\n                    consumer.commitSync();\n                }\n            }\n        } catch (Exception e) {\n            Thread.currentThread().interrupt();\n        } finally {\n            consumer.close();\n            workers.shutdown();\n        }\n    }\n    private static void processPayload(String data) { /* Business logic */ }\n}",
    "verification": "Monitor JMX metrics `kafka.consumer:type=consumer-coordinator-metrics,client-id=*,name=rebalance-rate-per-hour` and `last-rebalance-seconds-ago`. Trigger node restarts and verify partitions on healthy nodes continue consuming without interruption.",
    "date": "2026-08-19",
    "id": 1787110714,
    "type": "error"
});