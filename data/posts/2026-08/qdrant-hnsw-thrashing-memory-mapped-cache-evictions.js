window.onPostDataLoaded({
    "title": "Qdrant HNSW Graph Thrashing & Memory-Mapped Evictions",
    "slug": "qdrant-hnsw-thrashing-memory-mapped-cache-evictions",
    "language": "Qdrant",
    "code": "CacheThrashing",
    "tags": [
        "Qdrant",
        "Vector Database",
        "Performance",
        "Backend",
        "Rust",
        "Error Fix"
    ],
    "analysis": "<p>Qdrant, a vector similarity search engine, relies heavily on Hierarchical Navigable Small World (HNSW) graphs for efficient nearest neighbor search. HNSW constructs a multi-layered graph where lower layers contain more connections (for fine-grained search) and higher layers fewer (for long-range traversal). Qdrant stores its vector data and HNSW index structures using memory-mapped files (mmap). This allows the operating system to manage what parts of the index and vectors reside in RAM (via the OS page cache) and what remains on disk.</p><p>The problem of HNSW graph thrashing and memory-mapped cache evictions arises when the total size of the vector database (including vectors and index) significantly exceeds the available physical RAM. In such scenarios, the OS page cache constantly loads parts of the data from disk into RAM, only to evict other parts to make space for new requests. This continuous cycle of loading and evicting data is known as 'cache thrashing'. It leads to extremely high disk I/O, increased query latency, and reduced throughput because Qdrant's operations (especially search and indexing) become bottlenecked by slow disk access rather than fast in-memory computations.</p><p>This issue is exacerbated by suboptimal HNSW parameters (`m`, `ef_construct`, `full_scan_threshold`) which control the index's memory footprint and search efficiency. An overly dense graph or a very large search parameter can require more data to be loaded per query, intensifying the thrashing when memory is constrained. Similarly, during batch indexing or updates, if large portions of the index need modification, they too will be pulled into memory, competing with search operations for cache space.</p>",
    "root_cause": "The primary root cause is the vector database size (vectors + HNSW index) significantly exceeding the physical RAM available to the Qdrant instance. This forces the operating system to constantly swap data between disk and the OS page cache (cache thrashing), leading to high disk I/O and degraded performance. Secondary causes include suboptimal HNSW graph parameters (`m`, `ef_construct`) that increase memory usage, and lack of horizontal scaling for large datasets.",
    "bad_code": "JSON (Qdrant Collection Config - illustrating potential issues)\n\n// A collection configuration that might lead to thrashing if data size >> RAM\n{\n  \"vectors\": {\n    \"size\": 1536,\n    \"distance\": \"Cosine\"\n  },\n  \"hnsw_config\": {\n    \"m\": 16,             // High `m` increases index memory usage\n    \"ef_construct\": 256, // High `ef_construct` increases index memory usage\n    \"full_scan_threshold\": 1000000 // If too large, forces HNSW on small segments even if full scan is faster\n  },\n  \"optimizers_config\": {\n    \"max_indexing_threads\": 0, // 0 = all cores. Can be too many for memory-bound ops\n    \"mem_map\": {\n      \"flush_interval_sec\": 5 // Default, but indicates reliance on OS cache\n    }\n  },\n  \"wal_config\": {\n    \"wal_capacity_mb\": 1000 // Not directly related, but part of overall memory footprint\n  }\n}",
    "solution_desc": "The most direct solution is to increase the physical RAM available to the Qdrant instance, ensuring it can comfortably hold the active working set of your vectors and HNSW index. If vertical scaling isn't feasible, consider optimizing the HNSW parameters: reduce `m` (number of connections per node) and `ef_construct` (size of the dynamic list for constructing the graph) if acceptable for query accuracy. Lowering these reduces memory footprint at the potential cost of recall. Evaluate the `full_scan_threshold` to ensure Qdrant isn't using HNSW for very small segments where a full scan would be faster. For very large datasets, horizontal scaling with distributed Qdrant is the ultimate solution, splitting the index across multiple nodes. Additionally, ensure the underlying storage is fast (NVMe SSDs). Monitor OS page cache behavior and disk I/O patterns to confirm the fix.",
    "good_code": "JSON (Qdrant Collection Config - optimized for reduced memory footprint)\n\n// Optimized collection config for memory-constrained environments\n{\n  \"vectors\": {\n    \"size\": 1536,\n    \"distance\": \"Cosine\"\n  },\n  \"hnsw_config\": {\n    \"m\": 8,             // Reduced `m` to decrease index size, common for good balance\n    \"ef_construct\": 100, // Reduced `ef_construct` to decrease indexing memory, adjust for recall\n    \"full_scan_threshold\": 20000 // More aggressively use full scan for small segments\n  },\n  \"optimizers_config\": {\n    \"max_indexing_threads\": 4, // Limit indexing threads to prevent excessive memory spikes\n    \"mem_map\": {\n      \"flush_interval_sec\": 5\n    },\n    \"max_segment_number\": 10 // Consider limiting segment merges for less memory churn\n  },\n  \"quantization_config\": {\n    \"scalar\": {          // Enable scalar quantization to reduce vector memory usage\n      \"type\": \"int8\",\n      \"quantile\": 0.75\n    }\n  }\n}",
    "verification": "To verify the fix, monitor key performance indicators: query latency, throughput (QPS), and disk I/O metrics. After applying changes, these should show significant improvement. Specifically, use `iostat` or similar tools to check disk read/write rates; thrashing will show high read I/O. Observe Qdrant's own metrics (e.g., via Prometheus if enabled) for cache hit ratios, segment merge times, and search times. A healthy system will exhibit lower disk I/O, higher cache hit ratios, and consistent, lower query latencies. For containerized deployments, ensure the container has sufficient memory limits and CPU allocated.",
    "date": "2026-08-19",
    "id": 1787132182,
    "type": "error"
});