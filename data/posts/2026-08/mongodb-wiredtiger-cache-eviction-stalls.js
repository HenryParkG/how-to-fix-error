window.onPostDataLoaded({
    "title": "MongoDB WiredTiger: Resolving Cache Eviction Stalls",
    "slug": "mongodb-wiredtiger-cache-eviction-stalls",
    "language": "MongoDB",
    "code": "CacheStall",
    "tags": [
        "MongoDB",
        "Database",
        "Performance",
        "Caching",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses an internal cache to hold frequently accessed data. When this cache becomes full, WiredTiger must evict pages to make room for new data. This eviction process is highly optimized but can become a significant performance bottleneck, leading to \"cache eviction stalls,\" especially under high write load or with inefficient schema designs. These stalls manifest as elevated latency for read and write operations, increased CPU utilization on database threads dedicated to eviction, and a backlog of dirty pages waiting to be written to disk.</p><p>The problem is exacerbated by \"concurrent dirty page pressure.\" Dirty pages are data pages that have been modified in cache but not yet written to storage. If the rate of dirty page creation exceeds the rate at which WiredTiger can flush them to disk (journaling and checkpointing), the cache fills up with dirty pages, leaving less space for clean data and increasing the eviction burden. When the percentage of dirty pages in cache hits critical thresholds (e.g., 5-20% for dirty pages, 80% for overall cache occupancy), WiredTiger aggressively throttles incoming write operations and prioritizes eviction, leading to noticeable application-level stalls.</p>",
    "root_cause": "The primary root cause is an imbalance between the data modification rate (writes, updates, inserts) and the storage engine's ability to flush dirty data from cache to disk. This can be due to:\n1.  **Insufficient WiredTiger Cache Size**: Not enough RAM allocated to `wiredTigerCacheSizeGB`, leading to frequent evictions.\n2.  **Slow I/O Subsystem**: Disk throughput (IOPS and bandwidth) cannot keep up with the write workload, causing dirty pages to accumulate.\n3.  **High Dirty Page Ratio**: Rapid bursts of writes, especially to non-indexed or large documents, quickly fill the cache with dirty data.\n4.  **Inefficient Schema/Workload**: Frequent updates to small portions of large documents (e.g., many counter increments on a single document) can lead to pages being re-dirtied rapidly, hindering efficient flushing.\n5.  **Checkpointing Overhead**: Frequent or long-running checkpoints can temporarily halt eviction and flushing, building up dirty page pressure.",
    "bad_code": "```yaml\n# mongod.conf (Example of an undersized cache and default settings that might struggle)\nstorage:\n  dbPath: /var/lib/mongodb\n  engine: wiredTiger\n  wiredTiger:\n    engineConfig:\n      # wiredTigerCacheSizeGB: <too_small_value> # e.g., 1GB on a 64GB RAM machine\n      # default configuration often leads to issues under heavy load\n      # with dirty page thresholds at default 5% min, 20% max\n      # verbose:\n      #   - eviction: true # Useful for debugging, but not a fix\n```\n*Explanation*: The \"bad code\" here isn't a programming error, but rather a scenario where the default `wiredTigerCacheSizeGB` (often 50% of RAM, capped at 10GB for older versions, or 1GB min) combined with high write throughput or an inadequate I/O subsystem leads to eviction stalls. The default dirty page thresholds can also be too aggressive for certain workloads, causing premature throttling.",
    "solution_desc": "Solving WiredTiger cache eviction stalls and dirty page pressure requires a multi-pronged approach focusing on resource allocation, configuration tuning, and workload optimization.\n1.  **Increase WiredTiger Cache Size**: Allocate sufficient RAM. A common recommendation is to set `wiredTigerCacheSizeGB` to 50% to 80% of total system RAM, leaving room for the operating system and other processes. This reduces eviction frequency.\n2.  **Improve I/O Subsystem Performance**: Upgrade to faster storage (NVMe SSDs are highly recommended) with high IOPS and throughput. Ensure disk settings (e.g., noatime, appropriate I/O scheduler) are optimized for database workloads.\n3.  **Tune Dirty Page Thresholds**: Adjust `wiredTiger.engineConfig.wiredTigerInternalRpt.eviction_dirty_trigger` and `eviction_dirty_target` thresholds. For very write-heavy workloads, increasing `eviction_dirty_target` (e.g., from default 20% to 30-40%) can allow more dirty pages to accumulate before aggressive eviction kicks in, potentially reducing stalls, provided the I/O can still catch up. However, this increases recovery time after a crash.\n4.  **Monitor & Optimize Workload**:\n    *   Analyze `db.serverStatus().wiredTiger.cache` metrics, especially `tracked dirty bytes in the cache` and `pages currently being evicted`.\n    *   Check for `read tickets available` and `write tickets available` \u2013 low numbers indicate throttling.\n    *   Optimize schema design (e.g., using `$_inc` instead of full document updates for counters, sharding large collections) to reduce write amplification and improve data locality.\n    *   Consider adding an opLog-specific disk or using replica sets with delayed members to offload some read pressure during peak writes.\n5.  **Adjust Checkpointing**: While not directly configurable for frequency, ensuring underlying I/O can handle checkpoint activity is key. Monitor `storage.wiredTiger.checkpoint.last checkpoint duration average` in `serverStatus`.",
    "good_code": "```yaml\n# mongod.conf (Example of an optimized configuration)\nstorage:\n  dbPath: /var/lib/mongodb\n  engine: wiredTiger\n  wiredTiger:\n    engineConfig:\n      # Allocate sufficient RAM for WiredTiger cache (e.g., 32GB on a 64GB RAM machine)\n      wiredTigerCacheSizeGB: 32\n      # Potentially adjust dirty page thresholds for very write-heavy workloads\n      # These are internal runtime settings and might not be directly configurable\n      # via mongod.conf in all versions. Often managed by the engine itself.\n      # For advanced tuning, check MongoDB documentation for current recommendations.\n      # Example of *potential* runtime settings that might be exposed in future versions\n      # or via internal commands for fine-tuning.\n      # In current stable versions (e.g., MongoDB 5.0+), these are usually dynamic\n      # and engine-managed. Direct configuration is rare.\n      # The key is *providing enough cache* and *fast I/O*.\n\n      # For illustrative purposes, if they were directly exposed:\n      # wiredTigerInternalRpt: # This path might vary or not be directly exposed.\n      #   eviction_dirty_trigger: 10 # Increase min dirty percent before eviction (e.g., 10%)\n      #   eviction_dirty_target: 30 # Increase max dirty percent (e.g., 30-40%)\n\n      # Important: The above are illustrative. For current versions, focus on cache size and I/O.\n      # For specific advanced internal settings, always consult MongoDB documentation for your version.\n\nsystemLog:\n  destination: file\n  path: \"/var/log/mongodb/mongod.log\"\n  logAppend: true\n  # Add more detailed logging for performance diagnostics\n  component:\n    storage:\n      verbosity: 1\n    query:\n      verbosity: 1\n    command:\n      verbosity: 1\n    control:\n      verbosity: 1\n```",
    "verification": "Monitor `db.serverStatus()` metrics, specifically the `wiredTiger.cache` section, during periods of high load.\n*   **Reduced Eviction Stalls**: Observe the `pages currently being evicted` and `eviction walks passed` metrics. Ideally, these should be low or decrease significantly.\n*   **Stable Dirty Bytes**: Track `tracked dirty bytes in the cache` and `tracked dirty bytes percent`. This percentage should remain within healthy bounds, typically not exceeding 20-30% for prolonged periods.\n*   **Increased Tickets**: Check `wiredTiger.concurrentTransactions.readTickets.available` and `writeTickets.available`. These values should remain high, indicating that the engine is not aggressively throttling operations.\n*   **Lower Latency**: Application-level monitoring should show a decrease in P95/P99 read and write latencies.\n*   **I/O Utilization**: Use OS-level tools (e.g., `iostat`, `vmstat`) to confirm that the I/O subsystem is not saturated, and that write throughput has improved, allowing MongoDB to flush dirty pages more efficiently. If disk I/O remains a bottleneck after cache tuning, hardware upgrade is the next step.",
    "date": "2026-08-25",
    "id": 1787618667,
    "type": "error"
});