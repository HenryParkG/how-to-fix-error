window.onPostDataLoaded({
    "title": "MongoDB WiredTiger Ticket Exhaustion & Stalls",
    "slug": "mongodb-wiredtiger-ticket-exhaustion-cache-eviction",
    "language": "MongoDB, Database",
    "code": "WiredTiger Stalls",
    "tags": [
        "MongoDB",
        "Database",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>MongoDB's WiredTiger storage engine uses a sophisticated concurrency control mechanism that relies on 'tickets' to manage concurrent read and write operations. Each operation requires a ticket, and a fixed number of tickets are available for reads and writes. When all tickets are consumed, new operations must wait, leading to application stalls and reduced throughput. This often indicates that the database is overwhelmed by concurrent requests or long-running, resource-intensive operations.</p><p>Separately, WiredTiger manages an in-memory cache to store frequently accessed data pages. When the cache is full and new data needs to be loaded, the engine must evict existing pages. If these evicted pages are 'dirty' (modified but not yet written to disk), they must be flushed to disk before being evicted. Aggressive eviction of dirty pages, known as 'cache eviction stalls', can block other operations, including foreground queries, as the database becomes I/O-bound. This typically points to insufficient RAM for the database's working set or an I/O subsystem that cannot keep up with write demands.</p>",
    "root_cause": "<ul><li><b>Ticket Exhaustion:</b> High concurrency due to too many client connections, inefficient queries (e.g., full collection scans without indexes), large batch writes, or long-running analytical queries. The default ticket limits (128 for reads, 128 for writes) are exceeded.</li><li><b>Cache Eviction Stalls:</b> Insufficient RAM allocated to WiredTiger cache relative to the database's working set size. Slow disk I/O, especially for write operations, causing backpressure when flushing dirty pages. Improper index usage leading to more data being read into cache than necessary.</li></ul>",
    "bad_code": "```javascript\n// Scenario: A highly concurrent application with many clients\n// performing unindexed queries and large bulk writes.\n\n// Example of an unindexed query leading to full collection scan:\ndb.myCollection.find({ \"status\": \"pending\", \"amount\": { $gt: 1000 } });\n\n// Example of a large bulk write blocking tickets:\ndb.myCollection.insertMany(largeArrayOfDocuments); // If array is too large\n\n// Default MongoDB configuration (implied):\n// wiredTiger.engineConfig.cacheSizeGB: (typically 50% of available RAM)\n// wiredTiger.concurrentTransactions:\n//   read: { enabled: true, available: 128 }\n//   write: { enabled: true, available: 128 }\n```",
    "solution_desc": "<ul><li><b>Fixing Ticket Exhaustion:</b>\n    <ol><li><b>Optimize Queries:</b> Ensure proper indexing for frequently queried fields. Use `explain()` to identify slow queries.</li>\n    <li><b>Reduce Concurrency:</b> Implement client-side connection pooling and throttling. Break down large batch writes into smaller, more manageable chunks.</li>\n    <li><b>Increase Tickets (with caution):</b> Adjust `wiredTiger.concurrentTransactions.read` and `write` tickets in `mongod.conf`. Only increase if CPU and I/O resources permit, as higher ticket counts consume more resources.</li>\n    <li><b>Identify Bottlenecks:</b> Use `db.currentOp()` to find long-running operations and `db.serverStatus()` to monitor ticket usage.</li></ol>\n</li><li><b>Resolving Cache Eviction Stalls:</b>\n    <ol><li><b>Increase RAM / Cache Size:</b> Provision more physical RAM for the server. Configure `wiredTiger.engineConfig.cacheSizeGB` appropriately (typically 50-75% of system RAM, but adjust based on working set).</li>\n    <li><b>Improve Disk I/O:</b> Upgrade to faster storage (e.g., NVMe SSDs). Ensure the I/O subsystem can handle the write load.</li>\n    <li><b>Optimize Working Set:</b> Design schemas and queries to minimize the amount of data that needs to be held in memory. Efficient indexing reduces the need to scan large amounts of data into cache.</li>\n    <li><b>Monitor Cache Usage:</b> Use `db.serverStatus().wiredTiger.cache` metrics to understand cache occupancy, dirty bytes, and eviction rates.</li></ol>\n</li></ul>",
    "good_code": "```javascript\n// Monitoring ticket usage and cache status\ndb.serverStatus({\"wiredTiger\":true, \"metrics\":true}).wiredTiger.concurrentTransactions;\ndb.serverStatus({\"wiredTiger\":true, \"metrics\":true}).wiredTiger.cache;\n\n// Example of adding an index to optimize a query:\ndb.myCollection.createIndex({ \"status\": 1, \"amount\": 1 });\n\n// Example mongod.conf settings (restart required for changes to take effect):\n// storage:\n//   wiredTiger:\n//     engineConfig:\n//       cacheSizeGB: 16 # Adjust based on your system RAM and working set\n//     concurrentTransactions:\n//       read: { available: 256 } # Doubled from default\n//       write: { available: 256 } # Doubled from default\n\n// To break down large insertMany (simplified concept):\n// const chunkSize = 1000;\n// for (let i = 0; i < largeArrayOfDocuments.length; i += chunkSize) {\n//   const chunk = largeArrayOfDocuments.slice(i, i + chunkSize);\n//   db.myCollection.insertMany(chunk);\n//   // Add a small delay if needed to prevent overwhelming the system\n// }\n```",
    "verification": "<p>Verify fixes by continuously monitoring MongoDB metrics. Check `db.serverStatus().wiredTiger.concurrentTransactions.read.available` and `.write.available` to ensure they are not consistently at 0. Observe `db.serverStatus().wiredTiger.cache` metrics, specifically `bytes currently in the cache`, `max bytes configured`, `pages evicted from cache`, and `pages written from cache`. A healthy system will show available tickets and reasonable cache eviction rates without prolonged periods of high dirty pages or frequent, aggressive evictions. Also, monitor system-level metrics like CPU utilization, disk I/O wait times, and overall system memory usage. Application-side, observe query latency and throughput improvements.</p>",
    "date": "2026-08-17",
    "id": 1786938382,
    "type": "error"
});