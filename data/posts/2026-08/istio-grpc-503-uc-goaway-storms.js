window.onPostDataLoaded({
    "title": "Istio gRPC: 503 UC GOAWAY Storms & Connection Term.",
    "slug": "istio-grpc-503-uc-goaway-storms",
    "language": "Istio",
    "code": "503 UC GOAWAY",
    "tags": [
        "Kubernetes",
        "Infra",
        "ServiceMesh",
        "Error Fix"
    ],
    "analysis": "<p>When using Istio with gRPC services, encountering `503 UC` (Upstream Connection) errors coupled with `HTTP/2 GOAWAY` frames indicates a critical issue in how connections between Envoy proxies (part of the Istio data plane) and your gRPC services are managed. A `503 UC` error from Envoy signifies that it could not establish or maintain a connection to the upstream service. `GOAWAY` frames are a standard HTTP/2 mechanism for gracefully shutting down a connection, instructing the client to stop sending new streams on the current connection and migrate to new ones.</p><p>A `GOAWAY storm` occurs when multiple clients (Envoy proxies) receive `GOAWAY` frames from an overloaded or misconfigured upstream gRPC service. This can lead to a cascading failure: clients try to establish new connections, which further strains the already struggling server, leading to more `GOAWAY` frames, connection churn, and ultimately, service unavailability. Common causes include aggressive idle timeouts, insufficient connection pool limits, or underlying resource exhaustion in the gRPC application itself, preventing it from handling connection handshakes.</p>",
    "root_cause": "Misconfigured HTTP/2 connection pooling (Envoy's default limits can be too low for high-concurrency gRPC), overly aggressive idle timeouts on either client or server side, an overwhelmed upstream gRPC service that sends `GOAWAY` to shed load, or the gRPC service not properly handling graceful shutdown or connection state.",
    "bad_code": "```yaml\n# A DestinationRule with default or overly aggressive connection settings\napiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: my-grpc-service-dr\nspec:\n  host: my-grpc-service\n  trafficPolicy:\n    connectionPool:\n      http:\n        maxRequests: 1024 # Default is typically high, but could be limiting\n        maxRetries: 3\n        # Missing or default http2 settings can lead to issues\n        # No specific maxRequestsPerConnection, so it's unlimited per connection.\n        # This can make a single connection hog resources or not cycle if idle.\n      tcp:\n        maxConnections: 100 # Default might be too low for high concurrency\n        connectTimeout: 10s # Could be too long for quick failover, too short for cold starts\n    outlierDetection:\n      consecutiveErrors: 5\n      interval: 30s\n      baseEjectionTime: 30s\n\n-- In the gRPC server itself, not sending keepalives or having a very short idle timeout:\n// In Go gRPC server options\ngrps.KeepaliveParams{Time: 10 * time.Second, Timeout: 5 * time.Second}\n// or a very short MaxConnectionIdle leading to premature GOAWAYs\n```",
    "solution_desc": "<p>To resolve `503 UC` and `GOAWAY` storms, focus on optimizing connection management between Envoy and your gRPC services:</p><ul><li><b>Tune Istio DestinationRules:</b> Adjust `connectionPool` settings for HTTP/2. Increase `maxRequests` and `maxConnections` if needed, but critically, consider `maxRequestsPerConnection` to ensure connections are regularly cycled. Set `idleTimeout` more generously or disable it if services manage their own timeouts.</li><li><b>Implement gRPC Keepalives:</b> Ensure your gRPC servers and clients send HTTP/2 PING frames (`grpc.KeepaliveParams`) to prevent connections from being idled out prematurely by proxies or firewalls. This is crucial for long-lived gRPC streams.</li><li><b>Graceful Shutdown:</b> Ensure your gRPC services implement proper graceful shutdown logic to drain existing requests before closing connections, rather than immediately sending `GOAWAY` frames. This means responding to `SIGTERM` by stopping new request acceptance but allowing in-flight requests to complete.</li><li><b>Monitor Envoy Metrics:</b> Use Prometheus/Grafana to monitor Envoy metrics such as `envoy_cluster_upstream_cx_active`, `envoy_cluster_upstream_rq_active`, `envoy_cluster_upstream_cx_destroy_with_active_rq`, and specific `5xx` response codes to pinpoint connection issues.</li><li><b>Resource Scaling:</b> Ensure your gRPC services have adequate CPU, memory, and network resources. An overloaded service will naturally struggle to accept new connections, leading to `GOAWAY`s.</li></ul>",
    "good_code": "```yaml\n# Optimized DestinationRule for gRPC services (HTTP/2)\napiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: my-grpc-service-dr\nspec:\n  host: my-grpc-service\n  trafficPolicy:\n    connectionPool:\n      http:\n        # Allow a higher number of total requests to avoid blocking\n        maxRequests: 5000\n        # Limit requests per connection to encourage connection cycling\n        # This helps distribute load and avoid long-lived, potentially stale connections\n        maxRequestsPerConnection: 100\n        # Set a reasonable idle timeout for HTTP/2 connections.\n        # Too short: premature GOAWAYs. Too long: stale connections.\n        # Adjust based on gRPC server's keepalive and expected idle times.\n        idleTimeout: 5m # 5 minutes is a good starting point for gRPC\n        # Number of pending requests allowed on connection pool.\n        # Default is high (1024), but can be tuned.\n        maxPendingRequests: 2048\n      tcp:\n        # Allow more TCP connections for gRPC services\n        maxConnections: 500\n        # Set connection timeout to allow for quick failure or retries\n        connectTimeout: 1s # Faster connection timeout\n    outlierDetection:\n      consecutive5xxErrors: 10 # Allow more errors before ejecting, given gRPC retries\n      interval: 10s\n      baseEjectionTime: 60s\n\n-- In the gRPC server (Go example), using proper keepalive and server options:\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"log\"\n\t\"net\"\n\t\"os\"\n\t\"os/signal\"\n\t\"syscall\"\n\t\"time\"\n\n\t\"google.golang.org/grpc\"\n\t\"google.golang.org/grpc/keepalive\"\n\t\"google.golang.org/grpc/reflection\"\n\t\"grpc-example/pb\" // Replace with your proto-generated package\n)\n\ntype server struct{ pb.UnimplementedGreeterServer }\n\nfunc (s *server) SayHello(ctx context.Context, in *pb.HelloRequest) (*pb.HelloReply, error) {\n\tlog.Printf(\"Received: %v\", in.GetName())\n\treturn &pb.HelloReply{Message: \"Hello \" + in.GetName()}, nil\n}\n\nfunc main() {\n\tlis, err := net.Listen(\"tcp\", \":50051\")\n\tif err != nil {\n\t\tlog.Fatalf(\"failed to listen: %v\", err)\n\t}\n\n\tkap := keepalive.ServerParameters{\n\t\tTime:    30 * time.Second, // Ping client if idle for this duration\n\t\tTimeout: 10 * time.Second, // Wait this long for ping ack before closing\n\t\tMaxConnectionIdle: 5 * time.Minute, // Close connection if idle for this long\n\t\tMaxConnectionAge:  30 * time.Minute, // Force connection recreation after this age\n\t}\n\n\tkep := keepalive.EnforcementPolicy{\n\t\tMinTime:             5 * time.Second, // Client must ping no more frequently than this\n\t\tPermitWithoutStream: true,          // Allow client pings even without active RPCs\n\t}\n\n\ts := grpc.NewServer(\n\t\tgrpc.KeepaliveParams(kap),\n\t\tgrpc.KeepaliveEnforcementPolicy(kep),\n\t\t// Optional: MaxConcurrentStreams to limit individual connection load\n\t\tgrpc.MaxConcurrentStreams(100),\n\t)\n\tpb.RegisterGreeterServer(s, &server{})\n\treflection.Register(s)\n\n\t// Graceful shutdown setup\n\tgo func() {\n\t\tif err := s.Serve(lis); err != nil && err != grpc.ErrServerStopped {\n\t\t\tlog.Fatalf(\"failed to serve: %v\", err)\n\t\t}\n\t}()\n\n\tquit := make(chan os.Signal, 1)\n\tsignal.Notify(quit, syscall.SIGINT, syscall.SIGTERM)\n\t<-quit\n\tlog.Println(\"Shutting down server...\")\n\ts.GracefulStop() // Allow in-flight RPCs to finish\n\tlog.Println(\"Server gracefully stopped.\")\n}\n```",
    "verification": "<p>After applying the `DestinationRule` changes and ensuring your gRPC services are configured with proper keepalives and graceful shutdowns, monitor your Istio/Envoy metrics. Look for a reduction in `503 UC` errors and `GOAWAY` messages in Envoy proxy logs (accessible via `kubectl logs -f <pod-name> -c istio-proxy`). Observe the stability of active connections (`envoy_cluster_upstream_cx_active`) and the rate of connection destruction (`envoy_cluster_upstream_cx_destroy_with_active_rq`). Use distributed tracing tools (like Jaeger) to trace gRPC requests and confirm successful connections without premature terminations. Conduct load tests to validate the system's behavior under high concurrency and prolonged idle periods.</p>",
    "date": "2026-08-31",
    "id": 1788143083,
    "type": "error"
});