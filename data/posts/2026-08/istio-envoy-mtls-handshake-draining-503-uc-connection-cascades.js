window.onPostDataLoaded({
    "title": "Istio mTLS Handshake Draining & 503 Cascades",
    "slug": "istio-envoy-mtls-handshake-draining-503-uc-connection-cascades",
    "language": "Istio",
    "code": "ISTIO_ENVOY_503",
    "tags": [
        "Istio",
        "Envoy",
        "Kubernetes",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>In an Istio service mesh, mutual TLS (mTLS) is enforced by Envoy sidecars, ensuring secure, encrypted communication between services. When a service (client) attempts to connect to another service (server), the client's Envoy sidecar initiates an mTLS handshake with the server's Envoy sidecar. If this handshake process is prolonged, fails, or connections are not properly terminated and reused, it can lead to a state where the upstream connection pool in the client-side Envoy becomes exhausted. This situation is often observed as a cascading failure, where clients start receiving HTTP 503 'UC' (Upstream Connection) errors.</p><p>This 'draining' of mTLS handshakes can occur for several reasons: slow network conditions, certificate rotation issues (where the client's Envoy might try to use an expired or invalid certificate), misconfigured timeouts causing handshakes to be abandoned prematurely or held open too long, or excessive load on the upstream service causing its Envoy to be slow in responding to new connections. As client-side Envoys fail to establish connections, they may retry, further exacerbating the problem by creating a thundering herd of connection attempts, ultimately leading to resource exhaustion (CPU, memory, file descriptors) in the Envoys and an inability to proxy requests.</p>",
    "root_cause": "Prolonged or failed mTLS handshakes between Envoy sidecars, leading to the client-side Envoy's upstream connection pool exhaustion. This can be triggered by slow network I/O, certificate validity issues, aggressive connection reuse settings, or upstream service overload causing delays in connection establishment or processing. The result is a cascade of 503 'UC' errors as client Envoys cannot establish valid connections to upstream services.",
    "bad_code": "This is typically an operational/configuration issue, not code. However, misconfigured `DestinationRule` settings can contribute. For instance, an overly aggressive `maxRequestsPerConnection` combined with long-lived connections, or too short `connectTimeout` for mTLS handshakes.\n\n<pre><code>apiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: my-service\nspec:\n  host: my-service.my-namespace.svc.cluster.local\n  trafficPolicy:\n    connectionPool:\n      http:\n        http1MaxPendingRequests: 1024 # Might be too high/low\n        http2MaxRequests: 1024\n        maxRequestsPerConnection: 1 # Can cause excessive connection churn\n    tls:\n      mode: ISTIO_MUTUAL\n    # Missing or too short connectTimeout can cause issues for slow handshakes\n    # outboundTrafficPolicy:\n    #   mode: REGISTRY_ONLY\n</code></pre>\n\nAn application that is slow to respond to readiness probes or is under high load can also implicitly cause this by delaying connection acceptance.",
    "solution_desc": "Addressing mTLS handshake draining requires a multi-faceted approach:<ul><li><b>Adjust Envoy/Istio Timeouts:</b> Increase `connectTimeout` in `DestinationRule`s to allow sufficient time for mTLS handshakes. Review `idleTimeout` and `maxRequestsPerConnection` to balance connection reuse with freshness.</li><li><b>Monitor Certificate Status:</b> Ensure certificate rotation is healthy and sidecars have valid certificates. Check `istio-proxy` logs for mTLS handshake failures.</li><li><b>Optimize Upstream Services:</b> Ensure upstream applications are healthy, responsive, and not overloaded. Optimize readiness and liveness probes.</li><li><b>Connection Pool Tuning:</b> Adjust `http1MaxPendingRequests` and `http2MaxRequests` in `DestinationRule`s to provide adequate buffer for connection spikes, but avoid excessively large pools that consume too much memory.</li><li><b>Resource Limits:</b> Verify that Envoy sidecars have sufficient CPU and memory resources to handle peak load.</li><li><b>Disable Mutual TLS (Temporarily):</b> As a last resort for debugging, temporarily disabling mTLS (`DISABLE_MTS=true` or `PERMISSIVE` mode) can isolate if the issue is mTLS-specific or a general connectivity problem.</li></ul>",
    "good_code": "Example `DestinationRule` with adjusted timeouts for better mTLS stability:\n\n<pre><code>apiVersion: networking.istio.io/v1beta1\nkind: DestinationRule\nmetadata:\n  name: my-service\nspec:\n  host: my-service.my-namespace.svc.cluster.local\n  trafficPolicy:\n    connectionPool:\n      http:\n        http1MaxPendingRequests: 200 # Default is 1024, adjust based on load\n        http2MaxRequests: 200        # Default is 1024, adjust based on load\n        maxRequestsPerConnection: 0 # Unlimited requests per connection (recommended for HTTP/2), avoids churn\n      tcp:\n        connectTimeout: 5s # Increased connect timeout for mTLS handshake\n        idleTimeout: 30m   # Ensure connections can live longer if needed\n    tls:\n      mode: ISTIO_MUTUAL\n---\n# Also ensure adequate sidecar resources in the deployment for the workload\napiVersion: apps/v1\nkind: Deployment\n...\n  template:\n    spec:\n      containers:\n      - name: istio-proxy\n        resources:\n          limits:\n            cpu: 500m\n            memory: 256Mi\n          requests:\n            cpu: 100m\n            memory: 128Mi\n</code></pre>",
    "verification": "Monitor Envoy metrics exposed via Prometheus: look at `envoy_cluster_upstream_rq_xx` for 503 error rates, `envoy_cluster_upstream_cx_active` for active connections, and `envoy_cluster_upstream_cx_connect_fail` for connection failures. Check `istio-proxy` logs for any certificate-related warnings (`cert expired`, `handshake error`). Use `istioctl proxy-status` to check the status of sidecar configurations. After applying changes, observe if the 503 'UC' errors decrease and if `connect_timeout` metrics are no longer indicating widespread timeouts. A stable number of active connections and successful handshakes indicate a healthy state.",
    "date": "2026-08-21",
    "id": 1787283800,
    "type": "error"
});