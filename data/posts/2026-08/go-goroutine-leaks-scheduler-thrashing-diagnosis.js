window.onPostDataLoaded({
    "title": "Go: Goroutine Leaks & Scheduler Thrashing Diagnosis",
    "slug": "go-goroutine-leaks-scheduler-thrashing-diagnosis",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Concurrency",
        "Performance",
        "Debugging",
        "Go",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks occur when goroutines are started but never complete their execution, perpetually consuming memory and CPU resources. Over time, an accumulation of leaked goroutines can lead to significant memory bloat, high CPU usage, and application instability. This problem is often compounded by 'scheduler thrashing,' where the Go runtime's scheduler struggles to efficiently manage an excessive number of runnable goroutines, leading to frequent context switches, reduced throughput, and increased latency. The scheduler spends more time managing goroutines than executing actual work.</p><p>Understanding and diagnosing these issues is critical for maintaining high-performance and stable Go applications. Leaks can be insidious, often manifesting under load or after extended uptime, making them difficult to reproduce and debug without proper tooling.</p>",
    "root_cause": "Common causes of goroutine leaks include: <ul><li>Goroutines blocked indefinitely on unbuffered channels where no corresponding sender/receiver exists.</li><li>Goroutines stuck in infinite loops without yielding control, or performing I/O operations that never complete.</li><li>HTTP handlers or other request-scoped goroutines that fail to exit cleanly, perhaps due to panics or unhandled errors.</li><li>Background workers that are started but lack a proper shutdown mechanism (e.g., using a `context.Context` for cancellation).</li><li>Resource mismanagement, such as not closing `io.Reader` or `io.Writer` interfaces.</li></ul>Scheduler thrashing is a consequence of too many active or runnable goroutines, often exacerbated by leaks or inefficient I/O patterns.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\n// simulateWorkForever launches a goroutine that never terminates.\nfunc simulateWorkForever() {\n\tgo func() {\n\t\t// This goroutine will run indefinitely, simulating a leak if not cancelled.\n\t\tfor {\n\t\t\tselect {\n\t\t\tcase <-time.After(1 * time.Second):\n\t\t\t\t// fmt.Println(\"Working...\") // Uncomment to see activity\n\t\t\t}\n\t\t}\n\t}()\n}\n\nfunc handler(w http.ResponseWriter, r *http.Request) {\n\t// Every request launches a new goroutine that leaks.\n\tsimulateWorkForever()\n\tfmt.Fprintf(w, \"Request processed, but a goroutine was leaked!\")\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/leak\", handler)\n\tfmt.Println(\"Server listening on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}\n```",
    "solution_desc": "The primary solution to goroutine leaks involves implementing robust cancellation mechanisms, typically using `context.Context`. Every goroutine that is not designed to run for the entire application lifetime should be cancellable. For background tasks, a `context.WithCancel` or `context.WithTimeout` should be passed down, allowing parent goroutines to signal termination. Channels must be managed carefully: ensure sends and receives are balanced, and close channels when no more data is expected. For long-running operations or I/O, always use contexts with deadlines or cancellations. Utilize `pprof` for runtime profiling, specifically the `/debug/pprof/goroutine` endpoint, to identify active goroutines and their stack traces. Monitoring tools can track the number of active goroutines to detect abnormal growth. For scheduler thrashing, profiling CPU usage (`/debug/pprof/cpu`) and tracing can reveal bottlenecks and identify goroutines consuming excessive CPU.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n\n\t\"github.com/pkg/errors\"\n)\n\n// performCancellableWork launches a goroutine that can be terminated via context.\nfunc performCancellableWork(ctx context.Context) error {\n\tgo func() {\n\t\tdefer fmt.Println(\"Worker goroutine finished.\")\n\t\tfor {\n\t\t\tselect {\n\t\t\tcase <-ctx.Done():\n\t\t\t\tfmt.Println(\"Worker goroutine cancelled.\")\n\t\t\t\treturn // Exit the goroutine cleanly\n\t\t\tcase <-time.After(1 * time.Second):\n\t\t\t\t// Simulate some work\n\t\t\t\t// fmt.Println(\"Working...\")\n\t\t\t}\n\t\t}\n\t}()\n\treturn nil\n}\n\nfunc handler(w http.ResponseWriter, r *http.Request) {\n\t// Use a context with a timeout for the request scope.\n\t// The goroutine launched will automatically terminate after the request or timeout.\n\tctx, cancel := context.WithTimeout(r.Context(), 5*time.Second)\n\tdefer cancel() // Ensure cancellation is called to release resources\n\n\tif err := performCancellableWork(ctx); err != nil {\n\t\thttp.Error(w, errors.Wrap(err, \"failed to start worker\").Error(), http.StatusInternalServerError)\n\t\treturn\n\t}\n\tfmt.Fprintf(w, \"Request processed, worker started with cancellation support.\")\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/safe\", handler)\n\tfmt.Println(\"Server listening on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}\n```",
    "verification": "To verify the fix: <ul><li>**pprof analysis:** Use `go tool pprof http://localhost:8080/debug/pprof/goroutine?debug=1` after hitting the `/safe` endpoint multiple times. The number of active goroutines should remain stable and not grow indefinitely. Compare this to the `/leak` endpoint which will show constant growth.</li><li>**Load testing:** Put the fixed application under sustained load and monitor its memory and CPU usage. These should remain within expected bounds, without continuous growth.</li><li>**Tracing:** For more advanced cases, use `go tool trace` to visualize scheduler activity and identify periods of thrashing or excessive context switching.</li><li>**Metrics:** Integrate Prometheus/Grafana to track `go_goroutines` and `go_memstats_alloc_bytes_total` for long-term trend analysis.</li></ul>",
    "date": "2026-08-25",
    "id": 1787639441,
    "type": "error"
});