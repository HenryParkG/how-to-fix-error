window.onPostDataLoaded({
    "title": "PyTorch DDP: NCCL Hangs & CUDA Stream Races",
    "slug": "pytorch-ddp-nccl-hangs-cuda-stream-race-conditions",
    "language": "Python",
    "code": "NCCLHangs",
    "tags": [
        "PyTorch",
        "DDP",
        "NCCL",
        "GPU",
        "Deep Learning",
        "Python",
        "Error Fix"
    ],
    "analysis": "<p>DistributedDataParallel (DDP) in PyTorch is a powerful tool for scaling deep learning training across multiple GPUs, potentially across multiple nodes. It leverages NVIDIA's Collective Communications Library (NCCL) for efficient inter-GPU and inter-node communication, primarily for gradient `AllReduce` operations. A common and frustrating issue encountered in DDP setups is a 'hang' during training, often manifesting as an `AllReduce` operation that never completes. These hangs are frequently a symptom of asynchronous CUDA stream race conditions.</p><p>CUDA operations are asynchronous by default, meaning the CPU dispatches a kernel or memory transfer and immediately moves on, while the GPU executes it in the background. PyTorch DDP manages its own internal CUDA streams for gradient communication. If user-defined CUDA operations (e.g., custom kernels, data preprocessing on GPU, or specific synchronization logic) are not properly synchronized with DDP's internal streams, race conditions can occur. For instance, one process might complete its local computation and send its gradients via NCCL, while another process is still busy with a separate CUDA operation on a different stream, preventing it from participating in the `AllReduce` collective. When `AllReduce` requires all participants to reach a specific point, a non-participant will cause the entire collective to hang indefinitely.</p><p>A common pitfall is the misuse of `model.no_sync()`. This context manager temporarily disables gradient synchronization, allowing gradients to accumulate locally for a few steps before a collective `AllReduce`. If `no_sync()` is used unevenly across processes (e.g., one process exits the `no_sync()` block while others are still inside), or if it's combined with complex custom CUDA operations that interfere with DDP's gradient handling, it can lead to deadlocks or hangs. Moreover, `torch.cuda.set_device()` calls or stream changes within the training loop can inadvertently alter the context DDP expects, leading to non-determinism and hangs.</p>",
    "root_cause": "Mismanagement of CUDA streams, especially when mixing DDP's internal communication streams with user-managed asynchronous CUDA operations. This can lead to some DDP processes failing to reach the `AllReduce` synchronization point, causing the collective communication operation (handled by NCCL) to hang indefinitely as it waits for all participants.",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\n\ndef setup(rank, world_size):\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\ndef cleanup():\n    dist.destroy_process_group()\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear = nn.Linear(10, 1)\n\n    def forward(self, x):\n        return self.linear(x)\n\ndef train(rank, world_size):\n    setup(rank, world_size)\n    torch.cuda.set_device(rank)\n\n    model = SimpleModel().to(rank)\n    ddp_model = DDP(model, device_ids=[rank])\n    optimizer = optim.SGD(ddp_model.parameters(), lr=0.01)\n\n    for epoch in range(5):\n        # Simulate an uneven number of no_sync() calls, or a custom CUDA op\n        # that is not synchronized, leading to a hang.\n        if rank == 0 and epoch % 2 == 0:\n            # Only rank 0 sometimes skips sync, but others expect it every step\n            with ddp_model.no_sync():\n                # This block might contain other asynchronous CUDA ops\n                # that are not properly synchronized with DDP's internal stream.\n                # If not all ranks enter/exit no_sync() consistently, or if\n                # other CUDA work delays one rank too much, it can hang.\n                output = ddp_model(torch.randn(1, 10).to(rank))\n                loss = output.sum()\n                loss.backward()\n        else:\n            output = ddp_model(torch.randn(1, 10).to(rank))\n            loss = output.sum()\n            loss.backward()\n            # If this rank calls optimizer.step() and AllReduce, but rank 0 \n            # is still in no_sync() from the previous step, a hang occurs.\n            optimizer.step()\n            optimizer.zero_grad()\n\n    cleanup()\n\n# To run: torchrun --nproc_per_node=2 your_script.py\n```",
    "solution_desc": "To fix NCCL hangs due to CUDA stream race conditions, ensure consistent synchronization across all DDP processes and proper management of CUDA streams.\n\n1.  **Consistent `no_sync()` Usage:** If `model.no_sync()` is used for gradient accumulation, ensure that *all* DDP processes enter and exit the `no_sync()` context manager simultaneously and for the same number of steps. The final `optimizer.step()` and `all_reduce` must occur when all processes are outside `no_sync()`.\n2.  **Explicit CUDA Synchronization:** Before any `dist.barrier()` or `optimizer.step()` (which implicitly triggers `AllReduce`), ensure all preceding CUDA operations on all relevant streams are completed. `torch.cuda.synchronize()` can be used as a blunt instrument for full synchronization, though often more fine-grained stream synchronization is preferable.\n3.  **Stream Management:** If you are using custom CUDA streams, ensure that operations on these streams are properly synchronized with the default stream (which DDP typically uses internally for communication) or with DDP's specific communication stream. Use `stream.wait_stream(other_stream)` to explicitly manage dependencies.\n4.  **Avoid `set_device()` in Loops:** Calling `torch.cuda.set_device()` repeatedly within a training loop can be problematic. The device should typically be set once per process during setup.\n5.  **Error Handling & Timeouts:** Implement `dist.barrier()` with a timeout to diagnose hangs more quickly, though this won't fix the root cause. Set `NCCL_ASYNC_ERROR_HANDLING=1` (environment variable) for more explicit NCCL error messages.\n6.  **Gradient Checkpointing:** If using gradient checkpointing with DDP, ensure that it's configured correctly, as it can interact with backward hooks and stream management.",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nimport torch.distributed as dist\nfrom torch.nn.parallel import DistributedDataParallel as DDP\nimport os\n\ndef setup(rank, world_size):\n    # Ensure environment variables are set for distributed training\n    os.environ['MASTER_ADDR'] = 'localhost'\n    os.environ['MASTER_PORT'] = '12355'\n    dist.init_process_group(\"nccl\", rank=rank, world_size=world_size)\n\ndef cleanup():\n    dist.destroy_process_group()\n\nclass SimpleModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear = nn.Linear(10, 1)\n\n    def forward(self, x):\n        return self.linear(x)\n\ndef train(rank, world_size, gradient_accumulation_steps=1):\n    setup(rank, world_size)\n    torch.cuda.set_device(rank) # Set device once per process\n\n    model = SimpleModel().to(rank)\n    ddp_model = DDP(model, device_ids=[rank])\n    optimizer = optim.SGD(ddp_model.parameters(), lr=0.01)\n\n    for epoch in range(5):\n        for i in range(10):\n            # Correct usage of no_sync() for gradient accumulation\n            if (i + 1) % gradient_accumulation_steps != 0:\n                # This ensures all ranks are in no_sync() for these steps\n                with ddp_model.no_sync():\n                    output = ddp_model(torch.randn(1, 10).to(rank))\n                    loss = output.sum()\n                    loss.backward()\n            else:\n                # All ranks will exit no_sync() here and perform all_reduce\n                output = ddp_model(torch.randn(1, 10).to(rank))\n                loss = output.sum()\n                loss.backward()\n                \n                # Ensure all CUDA operations are complete before AllReduce\n                # If using custom streams, ensure they are synchronized with the default stream.\n                # torch.cuda.synchronize() # Often not needed if DDP manages streams well, but can be a safeguard\n                \n                optimizer.step()\n                optimizer.zero_grad()\n\n    cleanup()\n\n# To run: \n# python -m torch.distributed.run --nproc_per_node=2 your_script.py\n\n# Or using torchrun with specific ports\n# torchrun --nproc_per_node=2 --master_addr localhost --master_port 12355 your_script.py\n```",
    "verification": "Verify the fix by running your DDP training script multiple times. The primary indicator of a successful fix is the absence of hangs and deadlocks. Monitor GPU utilization using `nvidia-smi` across all devices; if one GPU's utilization drops to zero while others remain high, it's often a sign of a hang. Additionally, ensure that the model converges correctly, indicating that gradients are being properly synchronized. For debugging, temporarily add `dist.barrier()` calls with a timeout around suspicious sections to pinpoint where the hang occurs, and check logs for any NCCL-related warnings or errors, especially with `NCCL_ASYNC_ERROR_HANDLING=1` enabled.",
    "date": "2026-08-26",
    "id": 1787705039,
    "type": "error"
});