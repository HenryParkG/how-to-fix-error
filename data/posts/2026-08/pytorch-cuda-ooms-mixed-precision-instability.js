window.onPostDataLoaded({
    "title": "PyTorch CUDA OOMs & Mixed Precision Training Instability",
    "slug": "pytorch-cuda-ooms-mixed-precision-instability",
    "language": "Python, PyTorch, CUDA",
    "code": "CUDA OOM",
    "tags": [
        "PyTorch",
        "CUDA",
        "Deep Learning",
        "Python",
        "Error Fix"
    ],
    "analysis": "<p>CUDA Out-Of-Memory (OOM) errors are a pervasive challenge in deep learning, especially when training large models or using high-resolution data. These errors occur when the GPU's dedicated memory is exhausted, preventing PyTorch from allocating new tensors. Common culprits include excessively large batch sizes, complex model architectures, accumulating computation graphs (e.g., not calling <code>.backward()</code> or <code>.zero_grad()</code> properly), or holding onto large intermediate tensors. When an OOM occurs, the training run typically crashes abruptly, losing all progress.</p><p>Mixed precision training, which combines FP32 (single-precision) and FP16 (half-precision) arithmetic, is a popular technique to mitigate OOMs and speed up training. By performing most computations in FP16, it halves memory footprint and leverages Tensor Cores for faster arithmetic. However, FP16 has a smaller dynamic range, making it susceptible to underflow (numbers becoming too small to represent, effectively zero) or overflow (numbers becoming too large, resulting in NaNs or Infs). This can lead to training instability, where loss values spike, gradients become zero, or the model fails to converge.</p>",
    "root_cause": "CUDA OOMs stem from exceeding GPU memory capacity with large models/batches. Mixed precision instability arises from the reduced dynamic range of FP16, causing gradients or loss values to underflow to zero or overflow to NaNs/Infs, especially during backward passes where gradients can be very small.",
    "bad_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\n\n# Assume a large model and data_loader\nclass LargeModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear1 = nn.Linear(10000, 10000)\n        self.linear2 = nn.Linear(10000, 10000)\n    def forward(self, x):\n        return self.linear2(self.linear1(x))\n\nmodel = LargeModel().cuda()\noptimizer = optim.Adam(model.parameters(), lr=0.001)\nloss_fn = nn.MSELoss()\n\n# Training loop - no mixed precision, potentially high memory usage\nfor epoch in range(num_epochs):\n    for batch_idx, (data, target) in enumerate(data_loader):\n        data, target = data.cuda(), target.cuda()\n        optimizer.zero_grad()\n        output = model(data)\n        loss = loss_fn(output, target)\n        loss.backward()\n        optimizer.step()\n        print(f\"Loss: {loss.item()}\")\n```",
    "solution_desc": "To address CUDA OOMs, reduce batch size, use gradient accumulation, offload model parts to CPU if possible, or adopt techniques like activation checkpointing. For mixed precision instability, PyTorch's Automatic Mixed Precision (AMP) utilities, specifically `torch.cuda.amp.autocast` and `torch.cuda.amp.GradScaler`, are crucial. `autocast` automatically selects the appropriate precision for operations, while `GradScaler` dynamically scales loss values up during the forward pass to prevent FP16 gradient underflow. Before the backward pass, it scales gradients back down. This combination ensures numerical stability while reaping the benefits of FP16.",
    "good_code": "```python\nimport torch\nimport torch.nn as nn\nimport torch.optim as optim\nfrom torch.cuda.amp import autocast, GradScaler # Import for AMP\n\n# Assume a large model and data_loader\nclass LargeModel(nn.Module):\n    def __init__(self):\n        super().__init__()\n        self.linear1 = nn.Linear(10000, 10000)\n        self.linear2 = nn.Linear(10000, 10000)\n    def forward(self, x):\n        return self.linear2(self.linear1(x))\n\nmodel = LargeModel().cuda()\noptimizer = optim.Adam(model.parameters(), lr=0.001)\nloss_fn = nn.MSELoss()\n\nscaler = GradScaler() # Initialize GradScaler for mixed precision\n\n# Training loop with mixed precision\nfor epoch in range(num_epochs):\n    for batch_idx, (data, target) in enumerate(data_loader):\n        data, target = data.cuda(), target.cuda()\n        optimizer.zero_grad()\n\n        with autocast(): # Context manager for automatic mixed precision\n            output = model(data)\n            loss = loss_fn(output, target)\n        \n        scaler.scale(loss).backward() # Scale loss before backward pass\n        scaler.step(optimizer)       # Update optimizer with scaled gradients\n        scaler.update()              # Update the scale for the next iteration\n        \n        print(f\"Loss: {loss.item()}\")\n\n        # Optional: Empty cache if OOMs persist with large models/batches\n        # torch.cuda.empty_cache()\n\n        # Optional: Gradient accumulation to simulate larger batch size\n        # if (batch_idx + 1) % accumulation_steps == 0:\n        #     scaler.step(optimizer)\n        #     scaler.update()\n        #     optimizer.zero_grad()\n```",
    "verification": "Run the training script on a GPU. Monitor GPU memory usage using `nvidia-smi` or `torch.cuda.max_memory_allocated()`. With mixed precision, observe if the training completes without OOMs and if the loss curve remains stable and converges. Compare the final model's performance (e.g., accuracy) to a full FP32 baseline to ensure numerical stability hasn't compromised model quality.",
    "date": "2026-08-19",
    "id": 1787121009,
    "type": "error"
});