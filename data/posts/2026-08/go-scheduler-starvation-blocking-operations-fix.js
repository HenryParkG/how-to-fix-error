window.onPostDataLoaded({
    "title": "Go Scheduler Starvation: Long Blocking Operations Fix",
    "slug": "go-scheduler-starvation-blocking-operations-fix",
    "language": "Go",
    "code": "SchedulerStarvation",
    "tags": [
        "Go",
        "Concurrency",
        "Performance",
        "Backend",
        "Goroutines",
        "Error Fix"
    ],
    "analysis": "<p>Go's scheduler is a sophisticated component designed to efficiently manage thousands to millions of goroutines on a limited number of OS threads. It operates on a G-M-P model (Goroutine, Machine/OS thread, Processor/Logical Core). The scheduler is cooperative: goroutines are expected to yield control at various points (e.g., system calls, I/O operations, function calls) to allow other goroutines to run. However, problems arise when a goroutine executes a long-running, *blocking* operation that doesn't properly signal the Go runtime or doesn't allow the goroutine to yield. This can lead to a state where the OS thread (M) associated with a logical processor (P) becomes blocked, preventing any other goroutines assigned to that P from executing, even if they are ready. If this happens across multiple Ps, it can lead to scheduler starvation and severely degrade application performance or even cause deadlocks.</p>",
    "root_cause": "A goroutine executes a long-running blocking operation (e.g., CGO calls that don't release the OS thread, synchronous disk I/O, or calls to external blocking libraries) that does not allow the Go scheduler to preempt or switch other goroutines onto the associated logical processor (P). This effectively 'starves' other goroutines waiting on that P.",
    "bad_code": "```go\npackage main\n\n/*\n#include <unistd.h>\nvoid block_for_seconds(int s) {\n    sleep(s);\n}\n*/\nimport \"C\"\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc main() {\n\truntime.GOMAXPROCS(2) // Limit to 2 logical CPUs for demonstration\n\n\tvar wg sync.WaitGroup\n\twg.Add(3)\n\n\t// Goroutine 1: Short task\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tfmt.Println(\"Goroutine 1: Starting short task...\")\n\t\ttime.Sleep(100 * time.Millisecond)\n\t\tfmt.Println(\"Goroutine 1: Finished short task.\")\n\t}()\n\n\t// Goroutine 2: Long blocking CGO call\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tfmt.Println(\"Goroutine 2: Starting long blocking CGO call...\")\n\t\tC.block_for_seconds(5) // This will block the OS thread\n\t\tfmt.Println(\"Goroutine 2: Finished long blocking CGO call.\")\n\t}()\n\n\t// Goroutine 3: Another short task, will be starved if G2 blocks a P\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tfmt.Println(\"Goroutine 3: Starting another short task...\")\n\t\ttime.Sleep(100 * time.Millisecond)\n\t\tfmt.Println(\"Goroutine 3: Finished another short task.\")\n\t}()\n\n\twg.Wait()\n\tfmt.Println(\"All goroutines finished.\")\n}\n```\n<p>In this example, the C.block_for_seconds(5) call will block the underlying OS thread, preventing other goroutines that are assigned to the same P from running efficiently, leading to G3 being delayed.</p>",
    "solution_desc": "The primary solution is to avoid or mitigate long-running blocking operations within goroutines that are part of the main application's concurrency pool. If blocking operations are unavoidable, they should be isolated or handled in a way that allows the Go scheduler to work around them. Key strategies include:\n<ol>\n    <li><b>Use non-blocking I/O:</b> For network and file I/O, Go's standard library provides non-blocking wrappers by default. Ensure you are using these correctly.</li>\n    <li><b>Isolate blocking calls:</b> If using CGO or syscalls that are known to block, consider running them in a dedicated pool of goroutines, or if appropriate, using <code>runtime.LockOSThread()</code> for very specific, short-lived operations that need a dedicated OS thread (and ensuring it's unlocked afterward).</li>\n    <li><b>Externalize long-running tasks:</b> For truly CPU-bound or long-running tasks that block an OS thread, consider offloading them to external processes or microservices. Communicate with them via IPC or network protocols.</li>\n    <li><b>Thread-safe wrappers for CGO:</b> When integrating C libraries, use CGO features like <code>go:linkname</code> or custom shims that release the Go M before calling blocking C functions, and re-acquire it afterward.</li>\n</ol>\n<p>For the specific CGO `sleep` example, `runtime.Gosched()` or moving it to a separate process is often better. The CGO `sleep` call itself should ideally be wrapped to `runtime.UnlockOSThread()` before calling C.sleep and then `runtime.LockOSThread()` after it returns, but this is often complex and error-prone.</p>",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"os/exec\"\n\t\"runtime\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc main() {\n\truntime.GOMAXPROCS(2) // Limit to 2 logical CPUs for demonstration\n\n\tvar wg sync.WaitGroup\n\twg.Add(3)\n\n\t// Goroutine 1: Short task\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tfmt.Println(\"Goroutine 1: Starting short task...\")\n\t\ttime.Sleep(100 * time.Millisecond)\n\t\tfmt.Println(\"Goroutine 1: Finished short task.\")\n\t}()\n\n\t// Goroutine 2: Long blocking operation, now externalized via exec.Command\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tfmt.Println(\"Goroutine 2: Starting long external process (sleep 5s)...\")\n\t\tcmd := exec.CommandContext(context.Background(), \"sleep\", \"5\")\n\t\t// exec.CommandContext runs the command in a separate OS process.\n\t\t// The Go runtime will be notified when this goroutine is waiting for process completion,\n\t\t// allowing the P to be reused by other goroutines.\n\t\terr := cmd.Run()\n\t\tif err != nil {\n\t\t\tfmt.Printf(\"Goroutine 2: Command failed: %v\\n\", err)\n\t\t}\n\t\tfmt.Println(\"Goroutine 2: Finished long external process.\")\n\t}()\n\n\t// Goroutine 3: Another short task, now less likely to be starved\n\tgo func() {\n\t\tdefer wg.Done()\n\t\tfmt.Println(\"Goroutine 3: Starting another short task...\")\n\t\ttime.Sleep(100 * time.Millisecond)\n\t\tfmt.Println(\"Goroutine 3: Finished another short task.\")\n\t}()\n\n\twg.Wait()\n\tfmt.Println(\"All goroutines finished.\")\n}\n```\n<p>By externalizing the long `sleep` operation to a separate `exec.Command`, the Go runtime is able to manage the blocking wait for the external process efficiently, allowing other goroutines to be scheduled on the same P. For CGO calls, more intricate solutions involving `runtime.LockOSThread()`/`UnlockOSThread()` or ensuring the C function itself yields might be necessary, but externalizing or using truly non-blocking Go library functions are generally preferred.</p>",
    "verification": "To verify the fix, monitor your Go application's runtime statistics and behavior:\n<ol>\n    <li><b>`go tool trace`:</b> Use `go tool trace` to visualize scheduler events. Look for periods where P's are idle or blocked, and compare before and after the fix. A healthy trace shows P's actively switching goroutines.</li>\n    <li><b>CPU Utilization:</b> If the application was previously underutilizing CPU despite having ready goroutines, you should see an improvement.</li>\n    <li><b>Goroutine & OS Thread Counts:</b> Monitor `runtime.NumGoroutine()` and OS-level thread counts. An excessive number of OS threads (M's) might indicate the scheduler is constantly creating new ones to compensate for blocked ones, which can be inefficient.</li>\n    <li><b>Latency & Throughput:</b> Measure the overall latency of requests or task completion times. Starvation typically manifests as increased tail latencies or reduced throughput.</li>\n    <li><b>Application Logs:</b> Look for any logs indicating goroutines are taking excessively long to complete or if the application becomes unresponsive.</li>\n</ol>",
    "date": "2026-08-05",
    "id": 1785908297,
    "type": "error"
});