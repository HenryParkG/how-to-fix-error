window.onPostDataLoaded({
    "title": "Fixing Elasticsearch Cluster Red State from Shard Allocation",
    "slug": "elasticsearch-cluster-red-state-shard-allocation-failures-disk-watermarks",
    "language": "Elasticsearch",
    "code": "Cluster Red State",
    "tags": [
        "Elasticsearch",
        "Search",
        "NoSQL",
        "Infra",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>An Elasticsearch cluster's health is critical for its operation, indicated by colors: green (all primary and replica shards are allocated), yellow (all primary shards are allocated, but some replicas are not), and red (at least one primary shard is unallocated). A 'red' cluster state signifies severe data loss or unavailability for the affected indices and prevents further write operations.</p><p>A common culprit for a red cluster state is shard allocation failure, often triggered by hitting disk watermarks. Elasticsearch proactively monitors disk usage on data nodes. It defines three thresholds:</p><ol><li><b><code>low</code> watermark:</b> Prevents new shard allocations to nodes with disk usage above this percentage.</li><li><b><code>high</code> watermark:</b> Attempts to relocate shards away from nodes exceeding this usage.</li><li><b><code>flood_stage</code> watermark:</b> Blocks writes to indices that have at least one shard on a node exceeding this usage, and copies those shards off to other nodes if possible.</li></ol><p>When a node hits the <code>high</code> or <code>flood_stage</code> watermark, Elasticsearch stops allocating new shards to it and tries to move existing ones. If a primary shard cannot be allocated (e.g., no healthy nodes below watermarks, or all replicas also failed), the cluster will transition to a red state. Even if replicas are available, if the primary is on a full node and cannot relocate, it remains unassigned.</p>",
    "root_cause": "The primary cause for an Elasticsearch cluster entering a red state due to shard allocation failures is insufficient disk space on one or more data nodes, leading to the cluster hitting its configured disk watermarks (<code>cluster.routing.allocation.disk.watermark.high</code> or <code>flood_stage</code>). When a node exceeds these thresholds, Elasticsearch prevents new shard allocations and attempts to move existing shards off. If a primary shard cannot be allocated to a healthy node with sufficient disk space, it remains unassigned, causing the cluster to go red.",
    "bad_code": "```json\n# Check current cluster disk watermarks (often defaults are too conservative for bursting traffic)\nGET _cluster/settings?flat=true&include_defaults=true\n\n# Look for: cluster.routing.allocation.disk.watermark.low, high, flood_stage\n# Default values might be e.g., 85% for high, 95% for flood_stage.\n\n# Check shard allocation status\nGET _cat/allocation?v\n\n# Observe 'unassigned' shards, especially primaries, on nodes nearing full disk capacity.\n# Example output indicating a node with high disk usage:\n# shards disk.avail disk.used disk.total disk.percent host         ip           node\n# 50     2gb        98gb      100gb      98           node-1       10.0.0.1     es-data-1\n# ...\n# And later, 'unassigned' primaries due to lack of space.\n```",
    "solution_desc": "The immediate fix is to free up disk space on the affected data nodes. This typically involves deleting old or unnecessary indices, shrinking indices, or adding more disk capacity. Once disk space is alleviated, Elasticsearch should automatically attempt to reallocate the unassigned shards. If not, manual intervention might be needed. For a more robust solution, proactively monitor disk usage, plan for capacity, and consider adjusting disk watermark settings (temporarily during a crisis, or permanently if your use case justifies it) to prevent future occurrences. <ol><li><b>Free up disk space:</b> Delete old indices, use `_forcemerge` on small segments, or add more storage.</li><li><b>Adjust Watermarks (Temporarily):</b> Increase the percentage thresholds for <code>high</code> and <code>flood_stage</code> to give the cluster breathing room. This is a temporary measure, not a permanent solution for full disks.</li><li><b>Re-enable Shard Allocation:</b> If allocation was explicitly disabled, re-enable it.</li><li><b>Monitor & Alert:</b> Set up alerts for disk usage exceeding predefined thresholds before watermarks are hit.</li></ol>",
    "good_code": "```bash\n# Step 1: Identify and delete unnecessary indices to free up space\n# Example: Delete indices older than 30 days. Use caution!\n# POST _delete_by_query\n# {\n#   \"query\": {\n#     \"range\": {\n#       \"@timestamp\": {\n#         \"lte\": \"now-30d\"\n#       }\n#     }\n#   }\n# }\nDELETE /my-old-index-2023.01.01\n\n# Step 2 (if disk clearing is not immediate or enough): Temporarily raise disk watermarks\n# WARNING: Only do this if you are actively working on freeing space or adding capacity.\n# Setting watermarks too high can lead to node instability or data loss if disks genuinely fill up.\nPUT _cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.disk.watermark.low\": \"90%\",\n    \"cluster.routing.allocation.disk.watermark.high\": \"95%\",\n    \"cluster.routing.allocation.disk.watermark.flood_stage\": \"98%\"\n  }\n}\n\n# Step 3: (If allocation was explicitly disabled) Re-enable shard allocation\nPUT _cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.enable\": \"all\"\n  }\n}\n\n# Step 4: After space is freed and watermarks are adjusted, verify unassigned shards.\n# If still unassigned, you might need to manually retry allocation (use caution and understand the implications):\n# For specific indices/shards, e.g., if a shard is stuck due to transient issues:\n# POST _cluster/reroute\n# {\n#   \"commands\" : [\n#     {\n#       \"allocate_stale_primary\" : {\n#         \"index\" : \"my_index\", \"shard\" : 0,\n#         \"node\" : \"node-1\", \"accept_data_loss\" : true \n#       }\n#     }\n#   ]\n# }\n# This is a last resort and can lead to data loss if not used correctly.\n```",
    "verification": "After implementing the fix, continuously monitor the cluster health and shard allocation. <ol><li><b>Check Cluster Health:</b> Repeatedly query `GET _cluster/health` until the status changes from `red` to `yellow` and then ideally to `green`.</li><li><b>Verify Shard Allocation:</b> Use `GET _cat/allocation?v` to ensure all primary shards are assigned and to track the progress of replica allocations.</li><li><b>Monitor Disk Usage:</b> Keep a close eye on `GET _cat/nodes?v` and your system-level disk monitoring tools (e.g., `df -h`) to confirm that disk usage on all data nodes is well below the watermarks.</li><li><b>Check Cluster Logs:</b> Review Elasticsearch logs for messages indicating successful shard allocation, or any new errors that might arise.</li></ol>The cluster should stabilize, allow writes, and gradually move towards a `green` state as replicas are allocated.",
    "date": "2026-08-15",
    "id": 1786774967,
    "type": "error"
});