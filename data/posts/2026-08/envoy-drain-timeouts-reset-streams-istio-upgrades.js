window.onPostDataLoaded({
    "title": "Envoy Graceful Drain Timeouts & Reset Streams in Istio Upgrades",
    "slug": "envoy-drain-timeouts-reset-streams-istio-upgrades",
    "language": "Kubernetes",
    "code": "Network Error",
    "tags": [
        "Kubernetes",
        "Envoy",
        "Istio",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>During an Istio upgrade or any Kubernetes rolling update involving pods with Istio sidecars, traffic flow management relies heavily on Envoy's graceful draining capabilities. When a pod is being shut down, its Envoy proxy needs to stop accepting new connections and gracefully terminate existing ones, allowing ongoing requests to complete before the pod fully exits. If this graceful draining process is misconfigured or encounters issues, clients can experience connection timeouts, network errors, or more commonly, HTTP/2 <code>RST_STREAM</code> frames, indicating an abrupt termination of the stream.</p><p>This problem frequently manifests during Istio control plane upgrades, sidecar injection updates, or application deployments where older versions of Envoy proxies are replaced. Clients might observe intermittent 5xx errors, particularly during the transition phase. The underlying cause is usually a mismatch between the time allocated for Envoy to drain connections (<code>drainDuration</code>, <code>parentShutdownDuration</code> in Istio's <code>ProxyConfig</code>) and the Kubernetes pod termination grace period (<code>terminationGracePeriodSeconds</code>).</p><p>If the Kubernetes termination grace period is too short, the `preStop` hook (which triggers Envoy draining) or the draining process itself might not complete before Kubernetes sends a `SIGKILL` to the pod, forcibly terminating Envoy. Similarly, if Envoy's internal drain durations are too short for the workload's typical connection longevity, it will reset active streams when its internal drain timer expires, even if Kubernetes hasn't yet sent a `SIGKILL`.</p>",
    "root_cause": "The primary root cause is insufficient time allocated for Envoy proxies to gracefully drain active connections during pod termination (e.g., Istio upgrades, deployments). This can stem from: <ul><li>A <code>terminationGracePeriodSeconds</code> in the Kubernetes pod definition being too short, causing Kubernetes to forcibly terminate Envoy before it completes draining.</li><li>Istio's <code>ProxyConfig</code> settings (specifically <code>drainDuration</code> and <code>parentShutdownDuration</code>) being too short for the workload's typical request/connection duration.</li><li>New connections still being routed to the old Envoy proxy during the draining phase, leading to resets when the proxy eventually shuts down.</li></ul>",
    "bad_code": "```yaml\n# Example of a Kubernetes Deployment with a potentially too short terminationGracePeriodSeconds\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  template:\n    spec:\n      containers:\n      - name: my-app-container\n        image: my-app:latest\n        ports:\n        - containerPort: 8080\n      # This value might be too short for long-lived connections or heavy traffic\n      terminationGracePeriodSeconds: 30 # Default is 30 seconds, often insufficient\n---\n# Example of Istio ProxyConfig (if explicitly configured and too low)\napiVersion: install.istio.io/v1alpha1\nkind: IstioOperator\nmetadata:\n  name: istio-control-plane\nspec:\n  profile: default\n  meshConfig:\n    # These values default to 45s and 60s respectively. May need adjustment.\n    # proxyMetadata:\n    #   ISTIO_META_ISTIO_PROXY_DRAIN_DURATION_SECONDS: \"30\"\n    #   ISTIO_META_ISTIO_PROXY_PARENT_SHUTDOWN_DURATION_SECONDS: \"45\"\n    # Actual fields are in ProxyConfig, typically inherited or configured via k8s annotations:\n    # proxyOverride:\n    #   drainDuration: 30s # Envoy's drain time\n    #   parentShutdownDuration: 45s # Total time for parent to shut down children\n```\n<p><b>Explanation:</b> If `terminationGracePeriodSeconds` is too low, Kubernetes will send a `SIGKILL` before Envoy has finished gracefully closing connections. While Istio's `ProxyConfig` offers Envoy-specific drain settings, these are capped by `terminationGracePeriodSeconds`. If `drainDuration` and `parentShutdownDuration` are also too low for the application's traffic patterns (e.g., long-polling, large file transfers), Envoy itself will reset streams prematurely.</p>",
    "solution_desc": "To resolve Envoy graceful drain timeouts and `RST_STREAM` issues during Istio upgrades, a coordinated approach is needed:</p><ol><li><b>Increase <code>terminationGracePeriodSeconds</code>:</b> Modify the Kubernetes deployment's `terminationGracePeriodSeconds` for the affected pods. A common starting point is 60-120 seconds, or even higher for very long-lived connections. This gives Envoy more time to drain before Kubernetes sends a `SIGKILL`.</li><li><b>Configure Istio <code>ProxyConfig</code> Drain Durations:</b> Adjust the `drainDuration` and `parentShutdownDuration` settings in your Istio <code>ProxyConfig</code> (or directly via annotations on the deployment). These should generally be aligned with or slightly less than `terminationGracePeriodSeconds`. For example, `drainDuration: 45s` and `parentShutdownDuration: 60s`.</li><li><b>Implement Client-Side Retries:</b> Ensure client applications (or client-side proxies/gateways) are configured with intelligent retry mechanisms for transient network errors. This helps them recover from connection resets that might still occur during complex transitions.</li><li><b>Optimize Readiness and Liveness Probes:</b> Ensure your application's readiness probe correctly reflects when it's ready to serve traffic and when it's actively draining. An effective `preStop` hook that signals graceful shutdown can also assist.</li><li><b>Traffic Shifting and Connection Draining:</b> For critical services, consider advanced traffic shifting strategies (e.g., canary deployments with careful rollout progression) to minimize the number of pods draining at once.</li></ol>",
    "good_code": "```yaml\n# 1. Increased terminationGracePeriodSeconds in Kubernetes Deployment\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  template:\n    spec:\n      containers:\n      - name: my-app-container\n        image: my-app:latest\n        ports:\n        - containerPort: 8080\n      # Increased to 120 seconds (adjust based on workload needs)\n      terminationGracePeriodSeconds: 120\n\n---\n# 2. Configure Istio ProxyConfig drain durations (via annotations for simplicity)\n# This can also be done at the IstioOperator level or via a custom ProxyConfig resource.\napiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-app\nspec:\n  template:\n    metadata:\n      annotations:\n        # Envoy will attempt to drain for 90 seconds\n        sidecar.istio.io/proxyDrainDuration: \"90s\"\n        # Total duration for the parent process (Envoy) to wait for children to exit\n        sidecar.istio.io/proxyParentShutdownDuration: \"110s\"\n    spec:\n      containers:\n      - name: my-app-container\n        image: my-app:latest\n        ports:\n        - containerPort: 8080\n      terminationGracePeriodSeconds: 120 # Ensure this is > proxyParentShutdownDuration\n```",
    "verification": "Verify the fix by performing a rolling upgrade of the Istio control plane or the application deployment. Monitor client-side error rates and application logs during the upgrade for any spikes in 5xx errors or connection resets. Check Envoy proxy logs (accessible via `kubectl logs -f <pod-name> -c istio-proxy`) for messages related to draining, such as 'drain_time_s: X' or 'terminate_drain_connections: true'. Use `istioctl proxy-status` to observe the status of proxies during the rollout. A successful fix will show minimal to no client-facing errors and smooth transitions between old and new proxy versions.",
    "date": "2026-08-14",
    "id": 1786691429,
    "type": "error"
});