window.onPostDataLoaded({
    "title": "PostgreSQL: XID Wraparound Panic & Autovacuum Freezing",
    "slug": "postgresql-xid-wraparound-panic-autovacuum-freezing",
    "language": "PostgreSQL",
    "code": "XID Wraparound",
    "tags": [
        "SQL",
        "Infra",
        "Database",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL's Transaction ID (XID) is a 32-bit counter used to uniquely identify transactions. Every write operation (INSERT, UPDATE, DELETE) consumes an XID. This counter is finite, and after approximately 2 billion transactions, it wraps around to zero. If older, unfrozen transactions exist with XIDs that are numerically 'greater' than the current XID, they would appear to be in the future, making them invisible to the database and leading to data corruption.</p><p>To prevent this, PostgreSQL employs a mechanism called 'transaction ID freezing'. The autovacuum daemon is responsible for regularly scanning tables and 'freezing' old transaction IDs, marking them as permanently completed (visible to all future transactions) so their XIDs are no longer considered relative to the current XID for visibility purposes. If autovacuum fails to freeze old transactions fast enough, and the XID counter approaches the wraparound point, PostgreSQL will enter an emergency autovacuum freezing mode, which is highly disruptive. If even this fails, the database will panic and shut down to prevent data loss, signaling an 'XID wraparound panic'.</p>",
    "root_cause": "Failure of the autovacuum daemon to freeze old transaction IDs (XIDs) on time, leading to the XID counter approaching its 32-bit limit and potential data corruption. This can be caused by long-running transactions, insufficient autovacuum workers, aggressive autovacuum thresholds, or I/O bottlenecks.",
    "bad_code": "-- Example of a problematic autovacuum configuration (too infrequent/slow)\n-- These values are often the default or not aggressive enough for high-write systems.\nALTER SYSTEM SET autovacuum_vacuum_scale_factor = 0.2; -- Default is 0.2\nALTER SYSTEM SET autovacuum_vacuum_threshold = 50;   -- Default is 50\nALTER SYSTEM SET autovacuum_freeze_max_age = 200000000; -- Default: 200 million XIDs\n\n-- Or, simply a lack of monitoring for XID age:\n-- SELECT datname, age(datfrozenxid) FROM pg_database;\n-- If age(datfrozenxid) gets close to autovacuum_freeze_max_age, it's a problem.",
    "solution_desc": "<p>Preventing XID wraparound requires proactive monitoring and proper autovacuum tuning. Monitor the `age(datfrozenxid)` for all databases using `SELECT datname, age(datfrozenxid) FROM pg_database;` to ensure it stays well below `autovacuum_freeze_max_age`. Tune autovacuum settings (e.g., `autovacuum_vacuum_scale_factor`, `autovacuum_vacuum_threshold`, `autovacuum_max_workers`, `autovacuum_vacuum_cost_delay`, `autovacuum_freeze_max_age`) to ensure it runs frequently and aggressively enough for your workload. Address any long-running transactions that prevent autovacuum from processing tables.</p><p>In an emergency (when `age(datfrozenxid)` is very high, e.g., >1.5 billion): immediately identify and terminate long-running transactions, temporarily increase `autovacuum_max_workers` and reduce `autovacuum_vacuum_cost_delay`, and manually run `VACUUM FREEZE` on critical tables if autovacuum can't catch up. As a last resort, for specific tables that are lagging, a `VACUUM FREEZE` can be run manually, though this is blocking.</p>",
    "good_code": "-- Proactive monitoring for XID age (run regularly):\nSELECT\n    d.datname,\n    pg_database_size(d.datname),\n    age(d.datfrozenxid) AS xid_age,\n    GREATEST(0, (SELECT setting::bigint FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') - age(d.datfrozenxid)) AS xids_until_panic_alert\nFROM pg_database d\nWHERE d.datallowconn AND d.datname NOT IN ('template0', 'template1')\nORDER BY xid_age DESC;\n\n-- Example of more aggressive autovacuum tuning for high-write systems:\nALTER SYSTEM SET autovacuum_vacuum_scale_factor = 0.05; -- Vacuum more frequently\nALTER SYSTEM SET autovacuum_vacuum_threshold = 1000;  -- Set a fixed threshold for busy tables\nALTER SYSTEM SET autovacuum_max_workers = 5;          -- Allow more workers\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = 10;   -- Reduce delay to make workers more active\n\n-- Emergency manual vacuum freeze (use with caution, can block):\n-- Identify problematic table(s) first:\n-- SELECT relname, age(relfrozenxid) FROM pg_class WHERE relkind = 'r' ORDER BY age(relfrozenxid) DESC LIMIT 10;\n-- Then run:\nVACUUM FREEZE VERBOSE your_problematic_table;\n-- Or for all tables in a database (more disruptive):\nVACUUM FREEZE ANALYZE;",
    "verification": "<p>Regularly monitor the `xid_age` of your databases. Ensure that no database's `xid_age` approaches `autovacuum_freeze_max_age`. Set up alerts in your monitoring system (e.g., Prometheus, Grafana, Datadog) to trigger when `xid_age` exceeds a safe threshold (e.g., 80% of `autovacuum_freeze_max_age`). Verify that autovacuum is actively running by checking `pg_stat_activity` for 'autovacuum' processes and inspecting `pg_stat_all_tables` for `last_autovacuum` and `n_mod_since_analyze` columns to confirm tables are being processed.</p>",
    "date": "2026-08-31",
    "id": 1788143081,
    "type": "error"
});