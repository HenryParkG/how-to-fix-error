window.onPostDataLoaded({
    "title": "C++20 Coroutine Frame Lifetime Hazards & Promise Leaks",
    "slug": "cpp20-coroutine-frame-lifetime-hazards-promise-leaks",
    "language": "C++20",
    "code": "CoroutineLifetime, MemoryLeak",
    "tags": [
        "C++",
        "Concurrency",
        "Memory Management",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>C++20 coroutines, while powerful, introduce a new set of memory management challenges, primarily concerning the lifetime of the coroutine frame. A coroutine frame is a dynamically allocated object that stores the coroutine's state, including local variables and the promise object. Lifetime hazards typically manifest as use-after-free errors if the coroutine handle attempts to resume a destroyed frame, or memory leaks if the frame is never properly deallocated.</p><p>These issues often stem from an incorrect understanding of when <code>std::coroutine_handle::destroy()</code> should be called, or from custom promise types that mismanage the allocation/deallocation lifecycle. For instance, if a coroutine completes without reaching a `co_return` or without its `final_suspend` awaitable ensuring destruction, the frame can leak. Similarly, if a `std::coroutine_handle` is stored and used after the coroutine's logical completion and destruction, it leads to undefined behavior.</p>",
    "root_cause": "Mismanagement of `std::coroutine_handle` destruction, particularly when detaching coroutines, or errors in custom `promise_type` implementations that fail to deallocate the coroutine frame via `operator delete` or do not ensure `coroutine_handle::destroy()` is invoked upon completion.",
    "bad_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <thread>\n\nstruct MyTask {\n    struct promise_type {\n        MyTask get_return_object() { return {}; }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_never final_suspend() noexcept { return {}; } // ERROR: No destruction here!\n        void return_void() {}\n        void unhandled_exception() {}\n    };\n};\n\nMyTask buggy_coroutine() {\n    std::cout << \"Coroutine started\\n\";\n    co_await std::suspend_always{}; // Suspend, but who will resume/destroy?\n    std::cout << \"Coroutine resumed (should not happen in this bug)\\n\";\n}\n\nvoid run_buggy_example() {\n    buggy_coroutine(); // Creates a coroutine, but the handle is immediately lost\n    // The coroutine frame is allocated and never destroyed, leading to a memory leak.\n    std::this_thread::sleep_for(std::chrono::milliseconds(100));\n    std::cout << \"Buggy example finished. Coroutine frame likely leaked.\\n\";\n}\n\nint main() {\n    run_buggy_example();\n    // Valgrind or AddressSanitizer would report a leak here.\n    return 0;\n}\n```",
    "solution_desc": "To prevent coroutine frame lifetime hazards and memory leaks, robust coroutine management involves two primary strategies: RAII wrappers for `std::coroutine_handle` and careful design of custom `promise_type`s. An RAII wrapper ensures `destroy()` is called when the handle goes out of scope. For custom promise types, ensure `final_suspend` either returns `std::suspend_never` (if the caller is responsible for destruction or the coroutine is fire-and-forget but without state) or, more commonly, manages destruction explicitly or via a custom awaitable. If using custom allocators, ensure `promise_type::operator new` and `operator delete` are correctly paired.",
    "good_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <thread>\n#include <memory>\n\n// RAII wrapper for coroutine_handle to ensure destruction\ntemplate<typename PromiseType>\nstruct CoroHandleGuard {\n    std::coroutine_handle<PromiseType> handle_;\n\n    explicit CoroHandleGuard(std::coroutine_handle<PromiseType> h) : handle_(h) {}\n    CoroHandleGuard(const CoroHandleGuard&) = delete;\n    CoroHandleGuard& operator=(const CoroHandleGuard&) = delete;\n\n    CoroHandleGuard(CoroHandleGuard&& other) noexcept : handle_(std::exchange(other.handle_, {})) {}\n    CoroHandleGuard& operator=(CoroHandleGuard&& other) noexcept {\n        if (this != &other) {\n            if (handle_) handle_.destroy();\n            handle_ = std::exchange(other.handle_, {});\n        }\n        return *this;\n    }\n\n    ~CoroHandleGuard() {\n        if (handle_) {\n            std::cout << \"Destroying coroutine frame via guard.\\n\";\n            handle_.destroy();\n        }\n    }\n\n    std::coroutine_handle<PromiseType> release() { return std::exchange(handle_, {}); }\n    operator std::coroutine_handle<PromiseType>() const { return handle_; }\n};\n\nstruct SafeTask {\n    struct promise_type {\n        SafeTask get_return_object() {\n            return SafeTask{std::coroutine_handle<promise_type>::from_promise(*this)};\n        }\n        std::suspend_never initial_suspend() { return {}; }\n        // Proper final_suspend that does *not* destroy the frame directly here,\n        // but suspends, allowing the caller (or RAII wrapper) to manage lifetime.\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void return_void() {}\n        void unhandled_exception() { std::terminate(); }\n    };\n\n    CoroHandleGuard<promise_type> coro_handle_guard_;\n\n    SafeTask(std::coroutine_handle<promise_type> h) : coro_handle_guard_(h) {}\n    SafeTask(SafeTask&&) = default;\n    SafeTask& operator=(SafeTask&&) = default;\n    // Destructor of CoroHandleGuard handles destruction.\n};\n\nSafeTask safe_coroutine() {\n    std::cout << \"Safe coroutine started\\n\";\n    co_await std::suspend_always{}; // Suspend; ownership passed to SafeTask object.\n    std::cout << \"Safe coroutine resumed (should not be reached in this example if only destroyed).\\n\";\n    // On destruction of SafeTask, the frame is destroyed.\n}\n\nvoid run_safe_example() {\n    SafeTask task = safe_coroutine(); // Coroutine created and handle wrapped in SafeTask\n    std::this_thread::sleep_for(std::chrono::milliseconds(100));\n    std::cout << \"Safe example finished. Task object will destroy frame.\\n\";\n    // When 'task' goes out of scope, its destructor (via CoroHandleGuard) will call destroy().\n}\n\nint main() {\n    run_safe_example();\n    // No leak reported by memory tools.\n    return 0;\n}\n```",
    "verification": "To verify the fix, compile your C++ application with memory debugging tools like Valgrind (Linux) or AddressSanitizer (Clang/GCC: `-fsanitize=address`). These tools will detect memory leaks and use-after-free errors. Look for output indicating no reachable memory leaks at exit and no runtime memory access violations. Additionally, introduce logging within your `promise_type`'s `operator new`, `operator delete`, and the `CoroHandleGuard` destructor to confirm that allocation and deallocation pairs are properly matched and occur at the expected times during the coroutine's lifecycle.",
    "date": "2026-08-17",
    "id": 1786959450,
    "type": "error"
});