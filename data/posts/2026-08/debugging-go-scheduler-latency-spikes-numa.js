window.onPostDataLoaded({
    "title": "Debugging Go Scheduler Latency Spikes on NUMA Architectures",
    "slug": "debugging-go-scheduler-latency-spikes-numa",
    "language": "Go",
    "code": "Scheduler Latency",
    "tags": [
        "Go",
        "Performance",
        "NUMA",
        "Backend",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Go's runtime scheduler is designed for efficiency, managing goroutines and mapping them to OS threads (Ms) which in turn run on processor cores. The <code>GOMAXPROCS</code> environment variable controls the number of OS threads that the Go runtime can use simultaneously. On modern servers, especially those with Non-Uniform Memory Access (NUMA) architectures, misconfiguring <code>GOMAXPROCS</code> can lead to significant and often baffling latency spikes.</p><p>NUMA systems have multiple memory controllers, each with local memory, connected to a subset of CPU cores (a NUMA node). Accessing memory local to a core is faster than accessing memory on a different NUMA node. When <code>GOMAXPROCS</code> is set incorrectly, Go's M threads might migrate frequently across NUMA nodes, or multiple Ms might be scheduled on cores belonging to different nodes, but all trying to access data primarily allocated on one specific node. This causes increased remote memory access latency, cache invalidations, and can manifest as seemingly random spikes in goroutine scheduling latency and overall application response times.</p>",
    "root_cause": "<code>GOMAXPROCS</code> is set to a value that doesn't align with the underlying hardware topology, particularly on NUMA systems. A common misconfiguration is setting <code>GOMAXPROCS</code> to the total number of logical CPUs, which might span across multiple NUMA nodes, without explicit NUMA awareness. This leads to Go runtime's M threads frequently crossing NUMA boundaries, resulting in higher memory access latency and CPU cache misses due to data not being local to the executing core.",
    "bad_code": "```bash\n# Example: Server with 2 physical CPUs, each with 1 NUMA node, 12 cores, and 24 threads (total 48 logical CPUs)\n# Incorrectly setting GOMAXPROCS to total logical CPUs without NUMA awareness\nexport GOMAXPROCS=48\n\n# Or, a high-concurrency Go application without explicit GOMAXPROCS setting\n# (defaults to logical CPU count, which can be problematic on NUMA)\ngo run my_high_throughput_service.go\n```",
    "solution_desc": "The solution involves understanding your server's NUMA topology and configuring <code>GOMAXPROCS</code> and potentially process affinity accordingly. First, identify the number of physical cores per NUMA node. Then, set <code>GOMAXPROCS</code> to the total number of *physical cores* (not logical threads) across all NUMA nodes, or, more specifically, target a multiple of physical cores per NUMA node. For critical, latency-sensitive services, consider using <code>numactl</code> to explicitly pin a process to a specific NUMA node and set <code>GOMAXPROCS</code> to the number of physical cores on that node. This minimizes cross-NUMA traffic. Additionally, profiling with <code>pprof</code> and observing scheduler traces can help identify if goroutines are experiencing undue delays.",
    "good_code": "```bash\n# Step 1: Identify NUMA topology (e.g., using lscpu or numactl --hardware)\n# Example output: 2 NUMA nodes, each with 12 physical cores\n\n# Option 1: Set GOMAXPROCS to total physical cores (2 * 12 = 24)\nexport GOMAXPROCS=24\ngo run my_high_throughput_service.go\n\n# Option 2: Pin process to a specific NUMA node and configure GOMAXPROCS for that node\n# (e.g., for node 0, with 12 physical cores)\nnumactl --membind=0 --cpunodebind=0 env GOMAXPROCS=12 go run my_latency_sensitive_service.go\n\n# Option 3: Use a Go library for NUMA awareness (e.g., 'go-numa' or 'numacpu')\n// go.mod: require github.com/jstemmer/go-numa v1.0.0\npackage main\n\nimport (\n\t\"fmt\"\n\t\"runtime\"\n\t\"github.com/jstemmer/go-numa\"\n)\n\nfunc main() {\n\t// Use go-numa to determine optimal GOMAXPROCS\n\tif nodes, err := numa.GetNumaNodes(); err == nil && len(nodes) > 0 {\n\t\tnumCores := 0\n\t\tfor _, node := range nodes {\n\t\t\tnumCores += len(node.CPUs)\n\t\t}\n\t\truntime.GOMAXPROCS(numCores)\n\t\tfmt.Printf(\"GOMAXPROCS set to optimal %d based on NUMA cores\\n\", numCores)\n\t} else {\n\t\t// Fallback if NUMA info not available or error\n\t\truntime.GOMAXPROCS(runtime.NumCPU())\n\t\tfmt.Printf(\"GOMAXPROCS set to default %d\\n\", runtime.NumCPU())\n\t}\n\n\t// Your application logic here\n}\n```",
    "verification": "After applying the fix, monitor system metrics using tools like `numastat` (to check NUMA memory hits/misses), `htop` (observing CPU utilization and process affinity), and `iostat` or `dstat` for I/O performance. Crucially, use Go's built-in `pprof` tool, specifically `go tool pprof http://localhost:PORT/debug/pprof/trace` to visualize scheduler activity over time. Look for reductions in 'GC wait time', 'syscall wait time', and overall goroutine scheduling delays. Application-level latency metrics should show a significant reduction in spikes and a more stable baseline.",
    "date": "2026-08-15",
    "id": 1786774965,
    "type": "error"
});