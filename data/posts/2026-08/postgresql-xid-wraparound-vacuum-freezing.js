window.onPostDataLoaded({
    "title": "PostgreSQL: Preventing XID Wraparound",
    "slug": "postgresql-xid-wraparound-vacuum-freezing",
    "language": "PostgreSQL",
    "code": "XID Wraparound",
    "tags": [
        "SQL",
        "Database",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit transaction ID (XID) to identify each transaction. This XID is a crucial component for MVCC (Multi-Version Concurrency Control), allowing concurrent transactions to see consistent snapshots of the database. The 32-bit nature means the XID will eventually 'wrap around' after approximately 2 billion transactions (specifically, 2<sup>31</sup> - 1). When this happens, old transactions with higher XIDs would appear to be in the 'future' relative to new transactions with lower XIDs, leading to severe data corruption and loss.</p><p>To prevent this catastrophic event, PostgreSQL has a mechanism called 'transaction ID freezing.' This involves marking old tuples (rows) as permanently visible to all future transactions by setting their <code>xmin</code> (transaction ID of tuple creation) to a special 'frozen' XID. This process is primarily handled by the <code>autovacuum</code> daemon. Specifically, <code>autovacuum</code> regularly scans tables and 'freezes' tuples whose XIDs are older than <code>autovacuum_freeze_max_age</code> (default 200 million transactions). If <code>autovacuum</code> fails to keep up, or if it's disabled/misconfigured, a table's XID age can approach the wraparound limit. PostgreSQL will then enter an emergency 'maintenance mode' or shut down completely to prevent data corruption, rendering the database inaccessible until the wraparound is resolved via manual <code>VACUUM FREEZE</code> operations, which can be a lengthy downtime event for large databases.</p>",
    "root_cause": "The specific technical reason for Transaction ID Wraparound and subsequent VACUUM freezing issues is a failure of the <code>autovacuum</code> daemon to adequately 'freeze' old transaction IDs on time. This failure is usually due to: 1) <b>Insufficient `autovacuum` tuning:</b> Parameters like <code>autovacuum_vacuum_scale_factor</code>, <code>autovacuum_vacuum_cost_delay</code>, or <code>autovacuum_max_workers</code> are too conservative for the workload. 2) <b>High transaction volume:</b> A very busy database generates XIDs faster than <code>autovacuum</code> can freeze them. 3) <b>Long-running transactions:</b> Transactions that remain open for extended periods (e.g., forgotten <code>BEGIN</code> blocks, long-running queries in interactive sessions) hold onto older XIDs, preventing <code>autovacuum</code> from processing affected tuples. 4) <b>Disabling `autovacuum`:</b> Manually disabling <code>autovacuum</code> for performance reasons, which is a critical mistake.",
    "bad_code": "```sql\n-- Example: Disabling autovacuum (HIGHLY DISCOURAGED!)\n-- This will eventually lead to transaction ID wraparound issues.\nALTER SYSTEM SET autovacuum = off;\n\n-- Example: Setting autovacuum to run very infrequently or with high cost delay\n-- This can cause autovacuum to fall behind in busy systems.\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '100ms'; -- Default is 10ms (100ms is too high for busy systems)\n\n-- Example: A long-running, idle transaction that can block autovacuum\nBEGIN;\nSELECT * FROM large_table FOR UPDATE; -- Holds a lock and prevents XID advancement for involved tuples\n-- ... user leaves for the day without COMMIT or ROLLBACK ...\n```",
    "solution_desc": "<p>Preventing transaction ID wraparound and VACUUM freezing issues requires diligent monitoring and proactive tuning of PostgreSQL's maintenance:</p><ol><li><b>Monitor XID Age:</b> Regularly check the XID age of databases and individual tables. The system should alert if any database or table approaches the <code>autovacuum_freeze_max_age</code> threshold.</li><li><b>Tune `autovacuum` Parameters:</b> Adjust <code>autovacuum_max_workers</code> (increase for busy systems), <code>autovacuum_vacuum_cost_delay</code> (decrease to speed up vacuuming), and <code>autovacuum_vacuum_scale_factor</code> (adjust based on table update frequency) to ensure <code>autovacuum</code> can keep up with the transaction rate.</li><li><b>Identify & Terminate Long Transactions:</b> Use <code>pg_stat_activity</code> to find long-running or idle transactions that might be holding onto old XIDs and terminate them if necessary.</li><li><b>Regular Maintenance:</b> For extremely large or problematic tables, manual <code>VACUUM FREEZE</code> operations might be necessary as a temporary measure if <code>autovacuum</code> is struggling, but this indicates deeper tuning needs.</li><li><b>Partitioning Large Tables:</b> For very large tables with high churn, partitioning can reduce the scope of <code>VACUUM</code> operations, making them more manageable.</li><li><b>Connection Pooling:</b> Efficiently manage database connections to reduce overhead and avoid spurious long-running sessions.</li><li><b>Alerting:</b> Set up monitoring and alerting for databases approaching XID wraparound thresholds (e.g., 80% of <code>autovacuum_freeze_max_age</code>).</li></ol>",
    "good_code": "```sql\n-- 1. Monitor database XID age and compare to autovacuum_freeze_max_age\nSELECT datname, age(datfrozenxid) AS xid_age, current_setting('autovacuum_freeze_max_age') AS freeze_max_age\nFROM pg_database\nORDER BY xid_age DESC;\n\n-- 2. Monitor table XID age (especially for large tables)\nSELECT c.relname, age(c.relfrozenxid) AS xid_age, pg_size_pretty(pg_relation_size(c.oid)) AS table_size\nFROM pg_class c JOIN pg_namespace n ON c.relnamespace = n.oid\nWHERE c.relkind = 'r' AND n.nspname NOT IN ('pg_catalog', 'information_schema')\nORDER BY xid_age DESC;\n\n-- 3. Identify long-running transactions (transactions older than a certain age)\nSELECT pid, usename, client_addr, backend_start, state, query_start,\n       extract(epoch FROM (now() - xact_start)) AS xact_duration_sec,\n       query\nFROM pg_stat_activity\nWHERE state != 'idle' AND xact_start < now() - INTERVAL '1 hour' -- Adjust interval as needed\nORDER BY xact_duration_sec DESC;\n\n-- 4. Example: Adjust autovacuum parameters in postgresql.conf (then reload config)\n-- autovacuum = on\n-- autovacuum_max_workers = 5         # Increase for high concurrency systems\n-- autovacuum_vacuum_cost_delay = 2ms   # Reduce to make autovacuum more aggressive\n-- autovacuum_vacuum_scale_factor = 0.05 # Smaller factor means vacuum runs more often\n\n-- 5. Force a VACUUM FREEZE on a critical table (use only if autovacuum falls behind severely)\n-- This will lock the table, so schedule during maintenance windows.\nVACUUM (FREEZE, VERBOSE) my_large_and_critical_table;\n```",
    "verification": "<p>After implementing fixes, continuously monitor the XID age using the provided SQL queries (<code>datfrozenxid</code> and <code>relfrozenxid</code>). Ensure that the XID age for all databases and critical tables remains well below the <code>autovacuum_freeze_max_age</code> threshold (e.g., ideally below 100 million). Check PostgreSQL logs for any warnings or errors related to XID wraparound, autovacuum failures, or emergency shutdowns. The goal is for <code>autovacuum</code> to manage freezing transparently without manual intervention or system-level warnings. Stable XID ages and healthy <code>autovacuum</code> activity in logs confirm the fix.</p>",
    "date": "2026-08-24",
    "id": 1787553541,
    "type": "error"
});