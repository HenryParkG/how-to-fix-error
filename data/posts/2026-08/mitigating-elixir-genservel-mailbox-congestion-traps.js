window.onPostDataLoaded({
    "title": "Mitigating Elixir GenServer Mailbox Congestion & Traps",
    "slug": "mitigating-elixir-genservel-mailbox-congestion-traps",
    "language": "Elixir, Erlang/OTP",
    "code": "MailboxCongestion, Performance",
    "tags": [
        "Elixir",
        "Erlang",
        "Concurrency",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Elixir's GenServers, built on Erlang's OTP, rely on message passing via process mailboxes. While robust, mailboxes can become a bottleneck under high load, leading to 'mailbox congestion'. This occurs when a GenServer receives messages faster than it can process them, causing its mailbox to grow unbounded, consuming excessive memory and increasing message latency. This can eventually lead to out-of-memory errors or significant performance degradation.</p><p>A related issue is the 'selective receive trap', where developers use Elixir's `receive` construct with overly specific pattern matching. If a GenServer only processes certain message types, other messages (especially common ones) can accumulate in the mailbox, exacerbating congestion. This selective processing can also lead to unfairness, where lower-priority messages are processed first, or even starvation, where some important messages are effectively ignored while the mailbox fills with 'unmatched' messages.</p>",
    "root_cause": "Unbounded message queues due to a mismatch between message ingress rate and processing capacity, often coupled with inefficient or overly selective `receive` clauses that leave unprocessed messages in the mailbox, contributing to memory pressure and latency.",
    "bad_code": "```elixir\n# lib/buggy_genserver.ex\ndefmodule BuggyGenServer do\n  use GenServer\n\n  def start_link(initial_state) do\n    GenServer.start_link(__MODULE__, initial_state, name: __MODULE__)\n  end\n\n  @impl true\n  def init(state) do\n    {:ok, state}\n  end\n\n  @impl true\n  def handle_call({:get, key}, _from, state) do\n    {:reply, Map.get(state, key), state}\n  end\n\n  @impl true\n  # This GenServer only processes :high_priority messages via `handle_cast`\n  # and ignores (leaves in mailbox) all other `:task` messages until a :high_priority arrives.\n  # If many :task messages arrive, the mailbox will grow.\n  def handle_cast({:high_priority, data}, state) do\n    Process.sleep(100) # Simulate slow processing for high-priority tasks\n    IO.puts \"Processed high priority: #{inspect(data)}\"\n    {:noreply, state}\n  end\n\n  # No explicit handle_cast for :task, so these are left in the mailbox.\n  # If a client sends BuggyGenServer.cast({:task, 'data'}), it's ignored until a matching receive.\n  # This could also be a `handle_info` that processes only specific PIDs, ignoring others.\nend\n\n# --- Usage example (simulating congestion) ---\n# BuggyGenServer.start_link(%{})\n# 1..1000 |> Enum.each(fn i -> BuggyGenServer.cast({:task, i}) end)\n# # Mailbox will fill up with {:task, i} messages\n# BuggyGenServer.cast({:high_priority, \"Urgent!\"})\n# # Only after the high_priority message arrives, the system might eventually\n# # process other messages if the next receive loop picks them up, but they've been sitting.\n```",
    "solution_desc": "Mitigating mailbox congestion and selective receive traps requires a multi-faceted approach. First, implement backpressure mechanisms: monitor mailbox size (`Process.info(pid, :message_queue_len)`), and if it exceeds a threshold, instruct producers to slow down (e.g., using `GenStage`, `Broadway`, or simple flow control). Second, optimize message processing: ensure `handle_call`, `handle_cast`, and `handle_info` functions are efficient. Avoid slow operations directly within these callbacks; delegate long-running tasks to separate processes or use `Task.async`. Third, for selective receives, carefully consider the order and completeness of pattern matches. Prefer `handle_cast` and `handle_info` for general message types and use `receive` only for specific, short-lived interactions. If messages must be processed selectively, consider iterating through the mailbox to find and process relevant messages efficiently, rather than leaving them indefinitely. Finally, prioritize messages using custom logic or a more sophisticated queue if necessary.",
    "good_code": "```elixir\n# lib/good_genserver.ex\ndefmodule GoodGenServer do\n  use GenServer\n\n  def start_link(initial_state) do\n    GenServer.start_link(__MODULE__, initial_state, name: __MODULE__)\n  end\n\n  @impl true\n  def init(state) do\n    # Optionally, start monitoring mailbox length and self-throttle\n    {:ok, state}\n  end\n\n  @impl true\n  def handle_call({:get, key}, _from, state) do\n    {:reply, Map.get(state, key), state}\n  end\n\n  @impl true\n  # Process all :task messages with appropriate priority\n  def handle_cast({:task, data}, state) do\n    # Simulate some work, perhaps delegate heavy work to a Task.async_nolink\n    # to prevent blocking the GenServer.\n    # Task.async_nolink(fn -> process_data_in_background(data) end)\n    IO.puts \"Processed task: #{inspect(data)}\"\n    {:noreply, state}\n  end\n\n  @impl true\n  # If there are truly high-priority messages that need to interrupt flow,\n  # they should be handled, but all other messages should also be processed eventually.\n  def handle_cast({:high_priority, data}, state) do\n    Process.sleep(50) # Shorter sleep than buggy example, still fast.\n    IO.puts \"Processed HIGH priority: #{inspect(data)}\"\n    {:noreply, state}\n  end\n\n  # Catch-all for unknown messages to prevent them from sitting in the mailbox indefinitely.\n  # Decide if they should be logged, dropped, or raise an error.\n  @impl true\n  def handle_cast(message, state) do\n    Logger.warn(\"Unhandled cast message: #{inspect(message)}\")\n    {:noreply, state}\n  end\n\n  # If messages need to be received selectively, ensure proper cleanup or iterative processing:\n  defp process_mailbox(state) do\n    receive do\n      {:urgent_message, data} ->\n        IO.puts \"URGENT: #{inspect(data)}\"\n        process_mailbox(state)\n      message -> # Process other messages too, don't leave them\n        IO.puts \"Processed other: #{inspect(message)}\"\n        process_mailbox(state)\n    after 0 -> # Non-blocking receive for quick scan\n      :ok\n    end\n    state\n  end\n\n  # Call process_mailbox from handle_continue or init if this pattern is needed.\n  # This example primarily shows using handle_cast for all message types.\nend\n\n# --- Usage example ---\n# GoodGenServer.start_link(%{})\n# 1..1000 |> Enum.each(fn i -> GoodGenServer.cast({:task, i}) end)\n# GoodGenServer.cast({:high_priority, \"Urgent!\"})\n# # All messages are processed efficiently and immediately by `handle_cast`.\n# # No growing mailbox due to ignored messages.\n```",
    "verification": "Verify the fix by monitoring key performance metrics under load. Use Erlang's Observer (`:observer.start()`) to inspect the GenServer's process information, specifically its 'Message Queue Length'. Under the fixed solution, this length should remain low and stable, or spike briefly and then quickly return to zero, even under sustained high message ingress. Compare memory usage before and after the fix using tools like `:recon.bin_leak/1`. Implement custom telemetry to track message processing times and identify bottlenecks. Conduct load tests to ensure the GenServer can handle expected traffic without excessive latency or memory growth, and that no messages are starved or ignored.",
    "date": "2026-08-17",
    "id": 1786959451,
    "type": "error"
});