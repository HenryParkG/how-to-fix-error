window.onPostDataLoaded({
    "title": "Airflow: DAG Dependency Deadlocks & Task Idempotency Issues",
    "slug": "airflow-dag-deadlocks-idempotency",
    "language": "Apache Airflow (Python)",
    "code": "Airflow Deadlock/Idempotency",
    "tags": [
        "Apache Airflow",
        "Data Orchestration",
        "Python",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Apache Airflow is a powerful platform for programmatically authoring, scheduling, and monitoring workflows (DAGs). However, as DAGs grow in complexity and interact with external systems, two common pitfalls emerge: dependency deadlocks and non-idempotent tasks. Dependency deadlocks occur when a cyclical dependency is accidentally introduced within a DAG or across multiple DAGs, causing tasks to wait indefinitely for each other. While Airflow's scheduler is designed to detect and prevent direct cycles within a single DAG, more subtle, cross-DAG or resource-based cycles can still arise, leading to stalled runs and resource starvation.</p><p>Idempotency, on the other hand, is a property of an operation where executing it multiple times produces the same result as executing it once. In Airflow, tasks often interact with external data sources (databases, data lakes, APIs). If a task fails mid-execution and is retried, a non-idempotent operation might lead to data duplication, corruption, or incorrect state. For example, a task that appends records to a table without checking for duplicates will add the same data multiple times if retried. Similarly, a task that moves files might lose data if retried and the target already exists without proper handling. Ensuring task idempotency is crucial for building robust, fault-tolerant data pipelines that can recover gracefully from failures without manual intervention or data integrity issues.</p>",
    "root_cause": "<b>1. DAG Dependency Deadlocks:</b>\n*   <b>Implicit Cross-DAG Dependencies:</b> Relying on the output of one DAG run as input for another without explicit Airflow sensors or mechanisms, leading to race conditions or waits that are not visible to the scheduler.\n*   <b>Shared Resource Contention:</b> Multiple tasks/DAGs attempting to acquire exclusive locks on the same external resource (e.g., a specific database table, a file system path, API rate limits) without proper back-off or retry strategies.\n*   <b>Misconfigured <code>ExternalTaskSensor</code>:</b> An <code>ExternalTaskSensor</code> waiting for a task in a different DAG that in turn waits for a task in the original DAG, forming a cycle.\n*   <b>Self-referential Logic:</b> A task within a DAG attempting to monitor or modify its <i>own</i> DAG run state in a way that creates a circular wait.\n<b>2. Task Idempotency Issues:</b>\n*   <b>Destructive Operations on Retry:</b> Tasks that truncate tables, delete files, or move data without first verifying the current state or backing up.\n*   <b>Append-Only Logic without Deduplication:</b> Inserting records into a database or data lake without unique constraints or a mechanism to check for existing data based on a natural key.\n*   <b>API Calls with Side Effects:</b> Calling external APIs that perform state changes without transaction IDs or idempotency keys, leading to duplicate actions on retry.\n*   <b>File System Operations:</b> Moving or renaming files where the source file might be missing or the destination already exists on retry, leading to errors or data loss.",
    "bad_code": "```python\nfrom airflow import DAG\nfrom airflow.operators.bash import BashOperator\nfrom airflow.sensors.external_task import ExternalTaskSensor\nfrom datetime import datetime\n\n# --- Non-idempotent task example ---\n# This task appends data to a file without checking if it already exists,\n# leading to duplicate entries on retry.\nwith DAG(\n    dag_id='non_idempotent_data_pipeline',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['bad_example', 'idempotency'],\n) as dag_non_idempotent:\n    append_data = BashOperator(\n        task_id='append_new_records',\n        bash_command=\"echo 'new_record_line' >> /tmp/my_data_log.txt\",\n        # If this task fails and retries, it will add 'new_record_line' again.\n    )\n\n# --- Implicit Cross-DAG Deadlock potential ---\n# DAG A waits for DAG B, and DAG B implicitly waits for DAG A (e.g., via shared resource or manual trigger)\n\nwith DAG(\n    dag_id='dag_a_producer',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['bad_example', 'deadlock'],\n) as dag_a:\n    start_a = BashOperator(task_id='start_a', bash_command=\"echo 'DAG A started'\")\n    produce_data_a = BashOperator(\n        task_id='produce_data_for_b',\n        bash_command=\"echo 'data from A' > /tmp/data_from_a.txt\",\n    )\n    # DAG A explicitly waits for a task in DAG B to complete.\n    wait_for_dag_b = ExternalTaskSensor(\n        task_id='wait_for_dag_b_consumer',\n        external_dag_id='dag_b_consumer',\n        external_task_id='consume_data_a', # This forms a potential cycle\n        poke_interval=5,\n        timeout=600,\n    )\n    finalizer_a = BashOperator(task_id='finalizer_a', bash_command=\"echo 'DAG A finished'\")\n\n    start_a >> produce_data_a >> wait_for_dag_b >> finalizer_a\n\nwith DAG(\n    dag_id='dag_b_consumer',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['bad_example', 'deadlock'],\n) as dag_b:\n    start_b = BashOperator(task_id='start_b', bash_command=\"echo 'DAG B started'\")\n    # DAG B implicitly waits for data from DAG A by polling a file.\n    # If 'produce_data_for_b' waits for 'consume_data_a' via sensor,\n    # and 'wait_for_file_from_a' waits for 'produce_data_for_b' via file,\n    # a deadlock occurs: A waits for B, B waits for A.\n    wait_for_file_from_a = BashOperator(\n        task_id='wait_for_file_from_a',\n        bash_command='while [ ! -f /tmp/data_from_a.txt ]; do sleep 5; done; echo \"File from A found!\"',\n    )\n    consume_data_a = BashOperator(\n        task_id='consume_data_a',\n        bash_command=\"cat /tmp/data_from_a.txt && echo 'DAG B consumed data'\",\n    )\n    # No explicit sensor back to DAG A, but DAG A relies on 'consume_data_a' completion.\n\n    start_b >> wait_for_file_from_a >> consume_data_a\n```",
    "solution_desc": "<b>1. Preventing Deadlocks:</b>\n*   <b>Explicit Dependencies:</b> Always model cross-DAG dependencies using Airflow's native <code>ExternalTaskSensor</code> or <a href=\"https://airflow.apache.org/docs/apache-airflow/stable/concepts/datasets.html\">Dataset</a> feature (Airflow 2.2+). Ensure these sensors point to tasks that <i>complete</i> a logical unit of work, not just intermediate steps.\n*   <b>Acyclic Graphs:</b> Design DAGs to be strictly acyclic. If a cyclical dependency appears necessary, refactor to break the cycle, perhaps by introducing a \"state\" or \"trigger\" DAG that orchestrates the others sequentially or concurrently without direct circular waiting.\n*   <b>Resource Management:</b> For shared external resources, implement robust locking mechanisms outside of Airflow, or use Airflow's \"pools\" feature to limit concurrent access, mitigating contention-based deadlocks.\n*   <b>Timeouts and Retries:</b> Configure appropriate timeouts for sensors and tasks. While not preventing deadlocks, they allow the scheduler to eventually mark tasks as failed, breaking infinite waits and making issues visible.\n<b>2. Ensuring Idempotency:</b>\n*   <b>Upsert Operations:</b> When writing to databases, prefer \"upsert\" (update if exists, insert if not) logic over simple inserts. Use <code>MERGE</code>, <code>INSERT ON CONFLICT</code>, or <code>UPDATE/INSERT</code> statements based on unique keys.\n*   <b>Versioned Data / Immutable Appends:</b> For data lakes, append new versions of data rather than overwriting existing files, or use partition schemes that prevent data collision on retry (e.g., <code>/date=YYYY-MM-DD/hour=HH/run_id=UUID</code>).\n*   <b>Transactional Boundaries:</b> Wrap multiple operations within a single transaction if supported by the external system. If a task fails, the entire transaction rolls back.\n*   <b>Idempotency Keys for APIs:</b> When interacting with external APIs, pass an idempotency key (e.g., a unique UUID per task instance run) if the API supports it, ensuring repeated calls with the same key have no additional side effects.\n*   <b>Pre-check Existence:</b> Before moving/deleting files or modifying data, check if the expected state already exists. For example, check if a target file exists before moving, or if records are already present before inserting.\n*   <b>Atomic Operations:</b> Design tasks to be as atomic as possible, completing their work in a single, indivisible step.",
    "good_code": "```python\nfrom airflow import DAG\nfrom airflow.operators.bash import BashOperator\nfrom airflow.operators.python import PythonOperator\nfrom airflow.sensors.external_task import ExternalTaskSensor\nfrom datetime import datetime\nimport os\n\n# --- Idempotent task example ---\n# This task appends data only if it's not already present, ensuring idempotency.\ndef write_idempotent_record(record_content, filepath):\n    if not os.path.exists(filepath):\n        with open(filepath, 'w') as f:\n            f.write('') # Create file if it doesn't exist to avoid append issues\n    with open(filepath, 'r+') as f:\n        content = f.read()\n        if record_content not in content:\n            f.write(record_content + '\\n')\n        else:\n            print(f\"Record '{record_content}' already exists. Skipping.\")\n\nwith DAG(\n    dag_id='idempotent_data_pipeline',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['good_example', 'idempotency'],\n) as dag_idempotent:\n    add_record_idempotent = PythonOperator(\n        task_id='add_record_idempotent',\n        python_callable=write_idempotent_record,\n        op_kwargs={\n            'record_content': 'my_unique_record_data',\n            'filepath': '/tmp/my_idempotent_data.txt'\n        },\n    )\n\n# --- Correct Cross-DAG Dependency ---\n# DAG A explicitly waits for DAG B's completion. DAG B does not wait for DAG A.\n\nwith DAG(\n    dag_id='dag_a_orchestrator',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['good_example', 'cross_dag'],\n) as dag_a_good:\n    start_a_good = BashOperator(task_id='start_a', bash_command=\"echo 'DAG A (Good) started'\")\n    produce_data_a_good = BashOperator(\n        task_id='produce_data_for_b_good',\n        bash_command=\"echo 'data from A (Good)' > /tmp/data_from_a_good.txt\",\n    )\n    # This sensor waits for the *completion* of dag_b_processor\n    # specifically for its finalizer task. This ensures DAG B has finished\n    # its work before DAG A proceeds. No cycle.\n    wait_for_dag_b_good = ExternalTaskSensor(\n        task_id='wait_for_dag_b_processor_completion',\n        external_dag_id='dag_b_processor',\n        external_task_id='finalizer_b_good', # Wait for DAG B's last task\n        poke_interval=5,\n        timeout=600,\n    )\n    finalizer_a_good = BashOperator(task_id='finalizer_a_good', bash_command=\"echo 'DAG A (Good) finished'\")\n\n    start_a_good >> produce_data_a_good >> wait_for_dag_b_good >> finalizer_a_good\n\nwith DAG(\n    dag_id='dag_b_processor',\n    start_date=datetime(2023, 1, 1),\n    schedule_interval=None,\n    catchup=False,\n    tags=['good_example', 'cross_dag'],\n) as dag_b_good:\n    start_b_good = BashOperator(task_id='start_b_good', bash_command=\"echo 'DAG B (Good) started'\")\n    # If DAG B absolutely needs data from DAG A, it should also use a sensor\n    # for DAG A's completion before processing that data.\n    # For this example, we assume dag_b_processor is triggered after\n    # dag_a_orchestrator, and the file is available.\n    consume_data_a_good = BashOperator(\n        task_id='consume_data_a_good',\n        bash_command=\"cat /tmp/data_from_a_good.txt && echo 'DAG B (Good) consumed data'\",\n    )\n    finalizer_b_good = BashOperator(task_id='finalizer_b_good', bash_command=\"echo 'DAG B (Good) finished'\")\n\n    start_b_good >> consume_data_a_good >> finalizer_b_good\n```",
    "verification": "1.  <b>Airflow UI Graph View:</b> Visually inspect DAGs in the Airflow UI's Graph View. For cross-DAG dependencies, ensure the arrows (if using Datasets) or logical flow represents a clear, acyclic path.\n2.  <b>Scheduler Logs:</b> Monitor the Airflow scheduler logs for any warnings about detected DAG cycles or tasks stuck in a 'scheduled' state for extended periods.\n3.  <b>Task Instance States:</b> In the Airflow UI, check the state of task instances. Tasks that remain in 'queued', 'scheduled', or 'up_for_retry' indefinitely without progress might indicate a deadlock or a resource contention issue.\n4.  <b>Data Integrity Checks:</b> After a DAG run, especially after retries, verify the integrity of the data processed or generated by the tasks. Check for duplicates, missing records, or inconsistencies that would point to non-idempotent operations.\n5.  <b>Simulated Failures:</b> Intentionally fail tasks and observe how the DAG recovers. If data is duplicated or pipelines stall, the tasks are likely not idempotent or there's a dependency issue.\n6.  <b>Unit and Integration Tests:</b> Write tests for individual tasks to ensure their logic is idempotent and for DAGs to validate their dependency structure.",
    "date": "2026-08-25",
    "id": 1787651186,
    "type": "error"
});