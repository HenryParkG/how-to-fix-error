window.onPostDataLoaded({
    "title": "Fixing BEAM Mailbox Bloat & Selective Receive Starvation",
    "slug": "elixir-otp-mailbox-bloat-starvation",
    "language": "Elixir OTP",
    "code": "BEAM Mailbox Bloat",
    "tags": [
        "Elixir",
        "Erlang",
        "OTP",
        "Concurrency",
        "Memory",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>In Elixir and Erlang OTP, processes communicate asynchronously via message passing to their mailboxes. This actor model is powerful, but an unbounded mailbox can become a significant source of memory bloat. When a producer process sends messages faster than a consumer process can handle them, the consumer's mailbox queue grows indefinitely, consuming system memory. This issue is often compounded by 'selective receive' patterns, where a process might only process specific message types or messages matching certain criteria (e.g., a specific `ref`).</p><p>If the desired message is deep within a large mailbox queue, the process might spend significant time scanning the queue, or worse, become 'starved' because it cannot process any other messages until the desired one arrives or a timeout occurs, even if other relevant messages are available further up the queue. This leads to increased latency, decreased throughput, and potential system instability due to excessive memory usage.</p>",
    "root_cause": "Unbounded growth of a process's message queue (mailbox) because producers outpace consumers, exacerbated by selective receive operations that can cause desired messages to be skipped or delayed if they are not at the head of the queue, leading to queue scanning or starvation.",
    "bad_code": "```elixir\ndefmodule BadConsumer do\n  use GenServer\n\n  def start_link(initial_state) do\n    GenServer.start_link(__MODULE__, initial_state, name: __MODULE__)\n  end\n\n  @impl true\n  def init(state), do: {:ok, state}\n\n  @impl true\n  def handle_cast({:task, task_id}, state) do\n    # Simulate slow processing or selective receive\n    if rem(task_id, 5) == 0 do\n      :timer.sleep(500) # Only process every 5th message slowly\n      IO.puts \"Processed important task #{task_id}\"\n    else\n      IO.puts \"Skipping non-important task #{task_id}\"\n    end\n    {:noreply, state}\n  end\n\n  # A producer might look like this:\n  def produce(count) do\n    Enum.each(1..count, fn i ->\n      GenServer.cast(BadConsumer, {:task, i})\n    end)\n  end\nend\n```",
    "solution_desc": "<p>To mitigate mailbox memory bloat and selective receive starvation, several architectural and coding patterns can be employed:</p><ol><li><b>Implement Flow Control (Backpressure):</b> The most robust solution is to prevent the producer from overwhelming the consumer. This can be achieved using various backpressure mechanisms, such as demand-driven consumers (e.g., using `GenStage`, Broadway, or custom supervision trees with explicit demand signaling), or by having producers wait for acknowledgements before sending more messages.</li><li><b>Avoid Selective Receive:</b> Wherever possible, design processes to handle messages in a sequential, FIFO manner. If different message types require different handlers, use `handle_info` or `handle_call`/`handle_cast` with distinct message formats, rather than complex `receive` blocks. Unsolicited messages that are not immediately relevant should be handled (e.g., stored, logged, or dropped) rather than skipped.</li><li><b>Explicit Mailbox Limits:</b> While not a primary solution, the BEAM VM allows setting maximum mailbox sizes (e.g., `process_flag(:max_heap_size, %{size: 1_000_000, kill: true})`). This prevents unbounded growth by crashing the process, which can be useful in detecting issues, but relies on supervision to restart.</li><li><b>Monitor Mailbox Size:</b> Regularly monitor mailbox sizes using tools like `:observer.start/0` or custom metrics to proactively identify processes with growing queues.</li><li><b>Process Offloading:</b> For long-running or CPU-intensive tasks, offload them to separate worker processes or pools (e.g., using `Task.Supervisor`) to keep the main GenServer's mailbox free for control messages.</li></ol>",
    "good_code": "```elixir\ndefmodule GoodConsumer do\n  use GenServer\n\n  def start_link(initial_state) do\n    GenServer.start_link(__MODULE__, initial_state, name: __MODULE__)\n  end\n\n  @impl true\n  def init(state), do: {:ok, state}\n\n  # Handle all messages sequentially and quickly.\n  # If a message needs 'slow' processing, offload it.\n  @impl true\n  def handle_cast({:task, task_id}, state) do\n    if rem(task_id, 5) == 0 do\n      # Offload slow processing to another process/task\n      Task.start_child(fn ->\n        :timer.sleep(500)\n        IO.puts \"Processed important task #{task_id}\"\n      end)\n    else\n      # Process quickly in the GenServer's own thread\n      IO.puts \"Handled non-important task #{task_id} immediately\"\n    end\n    {:noreply, state}\n  end\n\n  # Example of a producer using backpressure (simplified, usually with GenStage/Flow)\n  def produce_with_backpressure(consumer_pid, count) do\n    # In a real scenario, this would involve a mechanism where\n    # the consumer signals its readiness or capacity.\n    # For demonstration, let's simulate a small batch and wait.\n    Enum.each(1..count, fn i ->\n      GenServer.cast(consumer_pid, {:task, i})\n      # Simulate a very basic form of backpressure by slowing down the producer\n      # This is NOT a robust solution but illustrates the concept.\n      if rem(i, 10) == 0, do: :timer.sleep(100)\n    end)\n  end\nend\n```",
    "verification": "<p>Verification involves monitoring both memory usage and message queue lengths. Use `:observer.start/0` or `:sys.get_state/1` to inspect process mailboxes and their memory consumption. For more programmatic monitoring, `Process.info(pid, :message_queue_len)` can provide real-time queue lengths. Integrate custom metrics (e.g., using Telemetry) to track queue sizes and processing times. Observe that memory usage remains stable and does not grow unbounded under sustained load. Confirm that all messages are processed in a timely manner and no tasks are starved.</p>",
    "date": "2026-08-14",
    "id": 1786701334,
    "type": "error"
});