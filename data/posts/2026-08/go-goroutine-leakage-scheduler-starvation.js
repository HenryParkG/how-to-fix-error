window.onPostDataLoaded({
    "title": "Go Goroutine Leakage & Scheduler Starvation",
    "slug": "go-goroutine-leakage-scheduler-starvation",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Go",
        "Concurrency",
        "Performance",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutines are Go's lightweight concurrency primitives, enabling highly concurrent applications. However, if not managed carefully, goroutines can 'leak' \u2013 meaning they are started but never terminate, consuming system resources like memory and CPU cycles indefinitely. A small number of leaked goroutines might go unnoticed, but a large accumulation can lead to significant performance degradation, memory exhaustion, and ultimately, scheduler starvation. Scheduler starvation occurs when the Go runtime's scheduler becomes overwhelmed by an excessive number of runnable goroutines, or by goroutines that are perpetually blocked, preventing it from efficiently allocating CPU time to other, potentially critical, goroutines. This can manifest as increased latency, unresponsive services, or even application crashes.</p><p>Common scenarios for leakage include goroutines blocked indefinitely on I/O operations without timeouts, waiting on channels that are never sent to or closed, or executing infinite loops without any exit condition. The Go runtime does not automatically garbage collect goroutines; they must complete their execution or be explicitly cancelled. Understanding these mechanisms is crucial for writing robust and efficient concurrent Go applications.</p>",
    "root_cause": "Unbounded goroutine creation, lack of proper cancellation mechanisms (e.g., missing `context.Context` handling), blocking I/O or channel operations without timeouts, infinite loops without exit conditions, or resource contention preventing goroutine completion.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc startLeakyWorker(id int, dataChan <-chan int, wg *sync.WaitGroup) {\n\tdefer wg.Done()\n\n\t// This worker processes data but doesn't have a mechanism\n\t// to exit if dataChan is closed or the parent context is cancelled.\n\t// If the main goroutine stops sending data, this worker will block indefinitely\n\t// on '<-dataChan' and never terminate, thus leaking.\n\tfor data := range dataChan {\n\t\tfmt.Printf(\"Worker %d received: %d\\n\", id, data)\n\t\ttime.Sleep(100 * time.Millisecond) // Simulate work\n\t}\n\tfmt.Printf(\"Worker %d exiting (this might never happen if channel isn't closed)\\n\", id)\n}\n\nfunc main() {\n\tconst numWorkers = 5\n\tdataChan := make(chan int)\n\tvar wg sync.WaitGroup\n\n\tfor i := 0; i < numWorkers; i++ {\n\t\twg.Add(1)\n\t\tgo startLeakyWorker(i, dataChan, &wg)\n\t}\n\n\t// Send some data\n\tfor i := 0; i < 10; i++ {\n\t\tdataChan <- i\n\t}\n\n\t// Problem: dataChan is never closed here, and no cancellation context is passed.\n\t// The workers will block indefinitely on '<-dataChan', leading to a leak.\n\t// In a real application, the main goroutine might exit or stop sending,\n\t// leaving these workers hanging.\n\n\t// Simulating a scenario where main might try to wait but workers are blocked\n\t// wg.Wait() // This will block forever\n\tfmt.Println(\"Main finished sending data. Workers might be leaked.\")\n\ttime.Sleep(2 * time.Second) // Give some time, but workers are still alive\n}\n```",
    "solution_desc": "The primary solution to goroutine leakage and scheduler starvation involves implementing robust cancellation patterns using `context.Context` and ensuring all goroutines have defined exit conditions. Every goroutine should listen for a cancellation signal from its context and terminate gracefully. For blocking operations (e.g., network calls, database queries, channel reads), apply appropriate timeouts using `context.WithTimeout` or `context.WithDeadline`. Design your concurrent components to explicitly close channels when no more data will be sent, or ensure a receiver has a mechanism to stop waiting if the sender is gone. Employ worker pools to limit the number of active goroutines and prevent unbounded creation. Regular monitoring of goroutine counts using `runtime.NumGoroutine()` and profiling with `pprof` can help detect leaks early.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc startReliableWorker(ctx context.Context, id int, dataChan <-chan int, wg *sync.WaitGroup) {\n\tdefer wg.Done()\n\tfor {\n\t\tselect {\n\t\tcase data, ok := <-dataChan:\n\t\t\tif !ok {\n\t\t\t\tfmt.Printf(\"Worker %d: dataChan closed. Exiting.\\n\", id)\n\t\t\t\treturn // Channel closed, exit gracefully\n\t\t\t}\n\t\t\tfmt.Printf(\"Worker %d received: %d\\n\", id, data)\n\t\t\t// Simulate work with potential timeout for external operations\n\t\t\tworkCtx, cancelWork := context.WithTimeout(ctx, 50*time.Millisecond)\n\t\t\tselect {\n\t\t\tcase <-time.After(100 * time.Millisecond): // Simulate longer work\n\t\t\t\t// Work completed (or not, if context cancelled before this)\n\t\t\tcase <-workCtx.Done():\n\t\t\t\tfmt.Printf(\"Worker %d: Work cancelled/timed out for data %d: %v\\n\", id, data, workCtx.Err())\n\t\t\t}\n\t\t\tcancelWork()\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Printf(\"Worker %d: Context cancelled. Exiting.\\n\", id)\n\t\t\treturn // Parent context cancelled, exit gracefully\n\t\t}\n\t}\n}\n\nfunc main() {\n\tconst numWorkers = 5\n\tdataChan := make(chan int)\n\tvar wg sync.WaitGroup\n\n\t// Create a cancellable context for all workers\n\tctx, cancel := context.WithCancel(context.Background())\n\n\tfor i := 0; i < numWorkers; i++ {\n\t\twg.Add(1)\n\t\tgo startReliableWorker(ctx, i, dataChan, &wg)\n\t}\n\n\t// Send some data\n\tfor i := 0; i < 10; i++ {\n\t\tdataChan <- i\n\t}\n\n\tfmt.Println(\"Main finished sending data.\")\n\t// Signal workers to stop gracefully\n\tclose(dataChan) // Close the channel to signal no more data\n\n\t// Wait a bit, then cancel the context to ensure all workers exit\n\ttime.Sleep(500 * time.Millisecond)\n\tcancel() // Signal cancellation to all goroutines listening to ctx.Done()\n\n\t// Wait for all workers to finish\n\twg.Wait()\n\tfmt.Println(\"All workers have gracefully exited.\")\n}\n```",
    "verification": "To verify the fix, monitor the number of active goroutines using `runtime.NumGoroutine()` before and after the operations that previously caused leaks. The goroutine count should return to a baseline after the task completes and all goroutines are expected to terminate. Use Go's built-in `pprof` tool (`go tool pprof http://localhost:PORT/debug/pprof/goroutine?debug=1`) to inspect the stack traces of all live goroutines and ensure none are stuck in unexpected states. Observe application logs for graceful exit messages from worker goroutines. For long-running services, integrate goroutine count metrics into your observability stack to detect anomalies over time.",
    "date": "2026-08-27",
    "id": 1787809821,
    "type": "error"
});