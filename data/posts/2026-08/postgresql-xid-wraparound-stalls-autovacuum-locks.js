window.onPostDataLoaded({
    "title": "PostgreSQL XID Wraparound Stalls & Aggressive Autovacuum",
    "slug": "postgresql-xid-wraparound-stalls-autovacuum-locks",
    "language": "PostgreSQL",
    "code": "XIDWraparoundStall",
    "tags": [
        "PostgreSQL",
        "Database",
        "Performance",
        "Infra",
        "SQL",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL relies on Transaction IDs (XIDs) for Multi-Version Concurrency Control (MVCC). Each transaction is assigned a unique XID, which is a 32-bit integer. This means there are approximately 4 billion unique transaction IDs. Over time, as transactions occur, this counter increments. When the XID counter approaches its maximum value, it faces a 'wraparound' problem \u2013 new transactions would receive XIDs that are older than existing ones, leading to data visibility issues and potential data loss.</p><p>To prevent this, PostgreSQL has a mechanism: when a table's oldest visible XID (recorded as `relfrozenxid`) gets too close to the wraparound limit (controlled by `autovacuum_freeze_max_age`, default 200 million transactions), PostgreSQL enters an aggressive autovacuum mode. In this mode, `autovacuum` processes run with higher priority and acquire more aggressive locks (e.g., `ACCESS EXCLUSIVE` or `SHARE UPDATE EXCLUSIVE` depending on the operation) on tables to freeze old tuples rapidly. This aggressive vacuuming can block read and write queries, leading to application stalls and severe performance degradation, sometimes making the database appear unresponsive.</p><p>The root of the issue often lies in inadequate vacuuming, large tables with many dead tuples, or long-running transactions preventing old XIDs from being frozen. When `autovacuum` struggles to keep up or is misconfigured, the system reaches a critical state where it must prioritize XID wraparound prevention over application availability.</p>",
    "root_cause": "The PostgreSQL global Transaction ID (XID) counter approaching wraparound limit, triggering aggressive autovacuum on tables whose `relfrozenxid` is too old. This aggressive autovacuum acquires strong locks, blocking application queries and causing stalls. Contributing factors include large tables, insufficient regular vacuuming, and long-running transactions preventing XID advancement.",
    "bad_code": "SQL (Conceptual)\n\n-- Scenario: Lack of monitoring and proper autovacuum tuning\n-- Default autovacuum_freeze_max_age is 200,000,000\n-- If a large table accumulates transactions beyond this without effective vacuuming,\n-- PostgreSQL will initiate an aggressive autovacuum.\n\n-- Check XID age for tables\nSELECT\n    relname AS table_name,\n    age(relfrozenxid) AS xid_age_from_frozen,\n    current_setting('autovacuum_freeze_max_age') AS autovacuum_freeze_max_age_threshold\nFROM pg_class\nWHERE relkind IN ('r', 'matview')\nORDER BY xid_age_from_frozen DESC;\n\n-- If xid_age_from_frozen approaches autovacuum_freeze_max_age_threshold,\n-- the table is at risk of aggressive autovacuum.",
    "solution_desc": "To prevent XID wraparound stalls, a proactive approach to vacuuming and monitoring is crucial. First, regularly monitor the XID age of all tables, especially large ones, using `age(relfrozenxid)`. If any table's XID age approaches `autovacuum_freeze_max_age`, immediate action is needed. Tune autovacuum parameters, increasing `autovacuum_max_workers` and adjusting `autovacuum_vacuum_cost_delay` and `autovacuum_vacuum_cost_limit` to allow autovacuum to run more frequently and effectively without overwhelming the system. Identify and resolve long-running transactions that prevent XID advancement. Consider manual `VACUUM FREEZE` operations during maintenance windows for critical tables if XID age is dangerously high. Partitioning large tables can also help manage XID age on smaller segments. Finally, ensure sufficient disk I/O and CPU resources for vacuum operations.",
    "good_code": "SQL\n\n-- 1. Monitor XID age for all user tables\nSELECT\n    relname AS table_name,\n    age(relfrozenxid) AS xid_age,\n    round(current_setting('autovacuum_freeze_max_age')::numeric / 2) AS warning_threshold,\n    current_setting('autovacuum_freeze_max_age') AS critical_threshold\nFROM pg_class c\nJOIN pg_namespace n ON n.oid = c.relnamespace\nWHERE c.relkind IN ('r', 'matview')\n  AND n.nspname NOT IN ('pg_catalog', 'information_schema')\nORDER BY xid_age DESC;\n\n-- 2. Manually VACUUM FREEZE a critical table if XID age is high\n-- Use during a maintenance window or off-peak hours as it can be resource-intensive.\nVACUUM FREEZE VERBOSE your_schema.your_table_name;\n\n-- 3. Adjust autovacuum settings (example, adjust based on workload)\n-- These can be set globally or per-table using ALTER TABLE.\nALTER SYSTEM SET autovacuum_max_workers = '5'; -- Increase workers\nALTER SYSTEM SET autovacuum_vacuum_cost_delay = '10ms'; -- Lower delay for faster vacuuming\nALTER SYSTEM SET autovacuum_vacuum_cost_limit = '2000'; -- Increase resources for vacuum\n-- Reload config after ALTER SYSTEM: SELECT pg_reload_conf();\n\n-- 4. Check for long-running transactions (preventing XID advancement)\nSELECT pid, application_name, usename, client_addr, backend_start, xact_start, state, query_start, query\nFROM pg_stat_activity\nWHERE state IN ('active', 'idle in transaction')\nORDER BY xact_start;\n",
    "verification": "After implementing the solution, continuously monitor the `xid_age` of your tables using the provided query. Ensure that `xid_age` values remain well below the `autovacuum_freeze_max_age` threshold, ideally below half of it. Observe `pg_stat_activity` for `autovacuum` processes to confirm they are running regularly and efficiently, without prolonged `ACCESS EXCLUSIVE` locks on critical tables during peak hours. Monitor system resource utilization (CPU, disk I/O) to ensure autovacuum is not causing new bottlenecks. Application logs should show a reduction or elimination of query stalls and timeouts related to database locking.",
    "date": "2026-08-19",
    "id": 1787132180,
    "type": "error"
});