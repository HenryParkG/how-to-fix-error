window.onPostDataLoaded({
    "title": "K8s CrashLoopBackOff: Probes & Startup Ordering",
    "slug": "kubernetes-crashloopbackoff-probes-startup",
    "language": "Kubernetes",
    "code": "CrashLoopBackOff",
    "tags": [
        "Kubernetes",
        "Container Orchestration",
        "Deployment",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>The dreaded `CrashLoopBackOff` status in Kubernetes indicates that a pod's container is repeatedly starting, crashing, and restarting. While many factors can cause this (application bugs, OOM errors, missing configuration), a frequent and often overlooked root cause is a misconfiguration of liveness and readiness probes, especially for applications with slow startup times or those dependent on external services that might not be immediately available.</p><p>Liveness probes determine if an application is *running* and healthy enough to stay in the pod. If it fails, Kubernetes restarts the container. Readiness probes determine if an application is *ready* to serve traffic. If it fails, Kubernetes removes the pod from service endpoints. Issues arise when a liveness probe is too aggressive (e.g., checking for HTTP 200 too soon) causing restarts before the application has fully initialized. Simultaneously, an application might crash if it attempts to connect to a database or message queue that isn't yet live within the cluster, leading to a startup ordering problem where the application's dependencies are not met.</p>",
    "root_cause": "<ul><li><b>Overly aggressive Liveness Probes:</b> The liveness probe starts checking too soon (`initialDelaySeconds` too low) or fails too quickly (`periodSeconds`, `timeoutSeconds` too low), causing the container to be restarted before the application has finished its initialization process.</li><li><b>Misconfigured Readiness Probes:</b> The readiness probe continuously fails (e.g., checking a dependency that's not ready, or a non-existent endpoint), preventing the pod from ever becoming 'Ready', sometimes leading to cascading failures or the application eventually crashing.</li><li><b>Startup Ordering Issues:</b> Applications crash on startup because a required external dependency (e.g., database, message queue, another service) isn't yet available, and the application's internal logic doesn't handle transient connection failures gracefully or indefinitely.</li><li><b>Lack of `startupProbe`:</b> For slow-starting applications, the `livenessProbe` can be triggered before the application has even finished booting, causing unnecessary restarts.</li></ul>",
    "bad_code": "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-slow-app\nspec:\n  replicas: 1\n  selector:\n    matchLabels:\n      app: my-slow-app\n  template:\n    metadata:\n      labels:\n        app: my-slow-app\n    spec:\n      containers:\n      - name: my-app\n        image: myrepo/slow-starting-app:1.0\n        ports:\n        - containerPort: 8080\n        livenessProbe:\n          httpGet:\n            path: /healthz\n            port: 8080\n          initialDelaySeconds: 5  # Too short for a slow-starting app\n          periodSeconds: 3\n          timeoutSeconds: 1\n          failureThreshold: 1     # Very aggressive: fails on first try\n        readinessProbe:\n          httpGet:\n            path: /ready\n            port: 8080\n          initialDelaySeconds: 5  # Might be too short if /ready checks external DB\n          periodSeconds: 5\n          timeoutSeconds: 2\n      # Missing init containers or application-level retry logic for external deps",
    "solution_desc": "The primary solution is to correctly configure your probes, especially for slow-starting or dependency-heavy applications. Introduce a `startupProbe` to handle the initial application bootup, giving it enough time to become responsive. Once the `startupProbe` succeeds, the `livenessProbe` takes over. Adjust `initialDelaySeconds`, `periodSeconds`, `timeoutSeconds`, and `failureThreshold` parameters for both liveness and readiness probes to match your application's actual startup and health check requirements. For external dependencies, ensure your application has robust retry logic, or use Kubernetes `initContainers` to verify dependency availability before the main container starts.",
    "good_code": "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-slow-app\nspec:\n  replicas: 1\n  selector:\n    matchLabels:\n      app: my-slow-app\n  template:\n    metadata:\n      labels:\n        app: my-slow-app\n    spec:\n      initContainers:\n      - name: wait-for-db\n        image: busybox:1.36\n        command: ['sh', '-c', 'until nc -z database-service 5432; do echo waiting for db; sleep 2; done;']\n      containers:\n      - name: my-app\n        image: myrepo/slow-starting-app:1.0\n        ports:\n        - containerPort: 8080\n        startupProbe: # New probe to handle slow startup\n          httpGet:\n            path: /healthz\n            port: 8080\n          initialDelaySeconds: 10 # Give it plenty of time to start\n          periodSeconds: 5\n          failureThreshold: 12    # Allows 60 seconds (5*12) for startup\n        livenessProbe:\n          httpGet:\n            path: /healthz\n            port: 8080\n          periodSeconds: 10       # Check less frequently once up\n          timeoutSeconds: 5\n          failureThreshold: 3     # Allow some transient failures\n        readinessProbe:\n          httpGet:\n            path: /ready\n            port: 8080\n          periodSeconds: 5\n          timeoutSeconds: 3\n          failureThreshold: 2\n          initialDelaySeconds: 5 # Can still use a short delay, as startupProbe handled the main boot",
    "verification": "After applying the updated manifest, use `kubectl get pods`, `kubectl describe pod <pod-name>`, and `kubectl logs <pod-name>` to monitor the pod's status and logs. Look for the `startupProbe` to succeed, followed by the `livenessProbe` and `readinessProbe` indicating healthy status. Verify that the pod transitions to `Running` and `Ready` and remains stable, without entering `CrashLoopBackOff`.",
    "date": "2026-08-30",
    "id": 1788098803,
    "type": "error"
});