window.onPostDataLoaded({
    "title": "PostgreSQL TxID Wraparound: Prevent Database Freezing",
    "slug": "postgresql-transaction-id-wraparound-database-freezing",
    "language": "SQL",
    "code": "TxIDWraparound",
    "tags": [
        "SQL",
        "PostgreSQL",
        "Infra",
        "Database",
        "Error Fix"
    ],
    "analysis": "<p>PostgreSQL uses a 32-bit transaction ID (TxID) for MVCC (Multi-Version Concurrency Control) to manage transaction visibility. Every modification (insert, update, delete) assigns a new TxID. While 32 bits offer a huge range, it's finite (approximately 4 billion transactions). This value is effectively a counter that eventually wraps around to zero. If the database reaches a point where old transaction IDs are mistakenly interpreted as future ones (due to wraparound), it can lead to data corruption or, more commonly, a \"database freezing\" state where new transactions cannot be initiated. This critical state is PostgreSQL's self-preservation mechanism to prevent data loss. It typically occurs when the oldest visible transaction ID (OldestXID) gets too close to the wraparound point, or when a table's freeze map doesn't get updated.</p><p>The `autovacuum` daemon is primarily responsible for preventing TxID wraparound. It \"freezes\" old tuples, marking them as visible to all future transactions and resetting their associated TxID, thus preventing the OldestXID from advancing too far. If `autovacuum` is disabled, misconfigured, or cannot keep up with the transaction rate, the database can enter a panic state, requiring immediate intervention.</p>",
    "root_cause": "The 32-bit transaction ID counter approaches its wraparound limit (2 billion transactions away from the current `OldestXID`) without `autovacuum` adequately \"freezing\" old tuples. This can be caused by:\n1.  `autovacuum` being disabled or misconfigured (e.g., too infrequent, low workers, aggressive cost limits).\n2.  Long-running transactions that prevent `autovacuum` from cleaning up older transaction IDs.\n3.  Infrequently accessed tables that `autovacuum` might ignore until it's too late.\n4.  A very high transaction rate overwhelming `autovacuum`.",
    "bad_code": "-- Potentially problematic PostgreSQL configuration in postgresql.conf\n-- (Illustrative; disabling autovacuum is generally a bad idea for production)\n\n-- autovacuum = off\n-- This would disable autovacuum entirely, leading to eventual wraparound.\n\n-- autovacuum_max_workers = 1\n-- autovacuum_naptime = 1min\n-- autovacuum_vacuum_scale_factor = 0.2\n-- autovacuum_analyze_scale_factor = 0.1\n-- vacuum_freeze_min_age = 50000000\n-- vacuum_freeze_table_age = 400000000\n\n-- These settings, especially low workers, high naptime, or\n-- a high vacuum_freeze_min_age/table_age combined with high transaction volume,\n-- could make autovacuum unable to keep up, leading to the wraparound issue.\n\n-- Example of a long-running transaction (if left open)\nBEGIN;\nSELECT pg_sleep(3600); -- Simulates an hour-long transaction\n-- If this transaction is not committed/rolled back, it holds open its XID,\n-- preventing VACUUM FREEZE from fully processing older transactions.\n-- (You can find such transactions via `SELECT pid, xmin, query_start, state FROM pg_stat_activity WHERE state LIKE '%idle in transaction%' OR xmin IS NOT NULL;`)",
    "solution_desc": "The primary solution to prevent TxID wraparound is to ensure `autovacuum` is correctly configured and operational.\n1.  **Enable and Tune `autovacuum`**: Ensure `autovacuum = on` in `postgresql.conf`. Tune parameters like `autovacuum_max_workers`, `autovacuum_naptime`, `autovacuum_vacuum_scale_factor`, `autovacuum_analyze_scale_factor`, and `autovacuum_freeze_max_age`. A lower `autovacuum_freeze_max_age` (e.g., `200000000` or less) ensures more frequent freezing.\n2.  **Monitor TxID Usage**: Regularly monitor the `datfrozenxid` (or `age(datfrozenxid)`) of databases and `relfrozenxid` (`age(relfrozenxid)`) of tables using `pg_database` and `pg_class` views. Critical thresholds (e.g., when `age(datfrozenxid)` approaches `autovacuum_freeze_max_age` minus a safety margin, typically around 1.5 billion transactions) should trigger alerts.\n3.  **Identify and Kill Long-Running Transactions**: Long-running transactions prevent `autovacuum` from advancing `OldestXID`. Monitor `pg_stat_activity` for `idle in transaction` states or queries that run for extended periods and address them (e.g., optimize queries, kill sessions).\n4.  **Manual `VACUUM FREEZE`**: For specific tables or databases showing high TxID age, run `VACUUM (FREEZE, VERBOSE) <table_name>;` or `VACUUM FREEZE;` for the entire database if the situation is critical.\n5.  **Use `VACUUM FULL` Sparingly**: `VACUUM FULL` reclaims space and freezes all tuples, but it requires an exclusive lock and rewrites the entire table, making it an expensive operation. Use it only when absolutely necessary (e.g., after large data purges or severe bloat) and schedule it during maintenance windows.\n6.  **Read-only Replicas**: If possible, offload read-heavy operations to read-only replicas, reducing the load on the primary and allowing `autovacuum` more headroom.",
    "good_code": "-- Recommended PostgreSQL configuration in postgresql.conf\n-- (Adjust values based on your workload and hardware)\nautovacuum = on\nlog_autovacuum_min_duration = 0 # Log all autovacuum actions for monitoring\nautovacuum_max_workers = 3     # Increase workers for concurrent vacuuming\nautovacuum_naptime = 10s       # Check more frequently for work\nautovacuum_vacuum_cost_delay = 10ms # Delay between vacuum cycles (lower means faster vacuum)\nautovacuum_vacuum_cost_limit = -1 # Use autovacuum_max_workers * vacuum_cost_limit for total cost limit\nautovacuum_freeze_max_age = 200000000 # Lower this for proactive freezing (default is 200M)\n\n-- Monitoring script to check transaction ID age for all databases\nSELECT\n    datname,\n    age(datfrozenxid) AS txid_age,\n    (SELECT setting::int FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') AS freeze_max_age,\n    (SELECT setting::int FROM pg_settings WHERE name = 'autovacuum_freeze_max_age') - age(datfrozenxid) AS txid_remaining_to_freeze\nFROM pg_database\nORDER BY txid_age DESC;\n\n-- Monitoring script to check transaction ID age for tables in the current database\nSELECT\n    relname,\n    relkind,\n    age(relfrozenxid) AS txid_age,\n    pg_size_pretty(pg_relation_size(oid)) AS table_size\nFROM pg_class\nWHERE relkind IN ('r', 'm', 't') AND relnamespace = (SELECT oid FROM pg_namespace WHERE nspname = 'public')\nORDER BY txid_age DESC;\n\n-- Manual vacuuming for a specific table if its age is too high (replace 'your_table_name')\n-- This helps catch up on freezing without waiting for autovacuum.\nVACUUM (FREEZE, VERBOSE) your_table_name;",
    "verification": "Verification involves continuous monitoring and proactive adjustments.\n1.  **Monitor `txid_age`**: Regularly run the provided SQL queries (or integrate them into your monitoring system) to track the `txid_age` for databases and tables. Ensure that `txid_age` remains significantly below `autovacuum_freeze_max_age` (e.g., below 1 billion).\n2.  **Autovacuum Logs**: Review PostgreSQL logs for entries related to `autovacuum`. You should see `autovacuum` workers starting, vacuuming, and analyzing tables. The `log_autovacuum_min_duration = 0` setting helps here.\n3.  **Resource Usage**: Monitor CPU, I/O, and disk space to ensure `autovacuum` processes are not unduly stressing the system, while still being effective.\n4.  **Alerting**: Confirm that your monitoring system correctly triggers alerts if `txid_age` approaches critical thresholds, allowing for timely manual intervention before freezing occurs.",
    "date": "2026-08-17",
    "id": 1786948555,
    "type": "error"
});