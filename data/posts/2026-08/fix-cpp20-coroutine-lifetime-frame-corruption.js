window.onPostDataLoaded({
    "title": "Fix C++20 Coroutine Lifetime & Frame Corruption",
    "slug": "fix-cpp20-coroutine-lifetime-frame-corruption",
    "language": "C++",
    "code": "CoroutineError",
    "tags": [
        "C++",
        "Concurrency",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>C++20 coroutines introduce powerful asynchronous programming capabilities but, being stackless, their lifetime management can be a subtle source of errors. A coroutine's state (its 'frame') is allocated on the heap or stack, and its lifetime is managed explicitly via <code>std::coroutine_handle</code>. Corruption typically arises when a coroutine is resumed after its frame has been deallocated, or when data within the frame (e.g., local variables, parameters) is accessed by a caller or another coroutine after the original coroutine has completed and its frame destroyed. This leads to undefined behavior, often manifesting as crashes, corrupted data, or subtle memory errors that are hard to debug.</p><p>Common scenarios include returning a <code>coroutine_handle</code> from a function, then attempting to resume it from the caller after the underlying coroutine function's scope has ended, or improper use of RAII within coroutines where destructors are called prematurely relative to outstanding <code>co_await</code> points.</p>",
    "root_cause": "Mismanagement of <code>std::coroutine_handle</code> lifecycle or improper handling of <code>co_await</code> expressions, causing access to deallocated coroutine frame memory or dangling pointers to state within a completed coroutine.",
    "bad_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <thread>\n\nstruct MyTask {\n    struct promise_type {\n        MyTask get_return_object() { return {}; }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void return_void() {}\n        void unhandled_exception() {}\n    };\n};\n\nstd::coroutine_handle<> bad_coroutine_caller() {\n    int* my_local_data = new int(42); // Simulating data lifetime issue\n    auto my_coroutine = []() -> MyTask {\n        std::cout << \"Coroutine started. Data: \" << *my_local_data << std::endl; // Accessing potentially dangling data\n        co_await std::suspend_always{}; // Suspend, allowing caller to resume\n        std::cout << \"Coroutine resumed. Data: \" << *my_local_data << std::endl;\n    }();\n    delete my_local_data; // Premature deallocation relative to coroutine's potential resume\n    return std::coroutine_handle<MyTask::promise_type>::from_promise(my_coroutine.promise());\n}\n\nint main() {\n    std::coroutine_handle<> handle = bad_coroutine_caller();\n    // 'my_local_data' in bad_coroutine_caller is already deleted\n    // Resuming 'handle' here will access freed memory.\n    if (handle) {\n        std::cout << \"Attempting to resume coroutine after data deallocation...\\n\";\n        handle.resume();\n        handle.destroy(); // Proper destruction of coroutine frame\n    }\n    return 0;\n}\n```",
    "solution_desc": "The key to fixing coroutine lifetime issues is to ensure that all data accessed by the coroutine, including its own frame and any captured variables, outlives the coroutine's execution. This typically involves careful ownership management. Use smart pointers (<code>std::shared_ptr</code>, <code>std::unique_ptr</code>) for heap-allocated data that must persist across suspension points. For coroutines that return a handle, ensure the caller takes responsibility for its destruction (via <code>handle.destroy()</code>) only when it's certain the coroutine will not be resumed again and its internal state is no longer needed. Alternatively, design coroutines to return an object (like a <code>Task</code> or <code>Future</code>) that encapsulates the <code>coroutine_handle</code> and manages its destruction deterministically, often in its own destructor or through a well-defined state machine. Avoid capturing references or raw pointers to stack-allocated variables in lambda-based coroutines if the coroutine might outlive the stack frame where it was created.",
    "good_code": "```cpp\n#include <coroutine>\n#include <iostream>\n#include <thread>\n#include <memory> // For std::shared_ptr\n\nstruct MyTask {\n    struct promise_type {\n        MyTask get_return_object() { return {}; }\n        std::suspend_never initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void return_void() {}\n        void unhandled_exception() {}\n    };\n    // Encapsulate handle to ensure destruction\n    std::coroutine_handle<promise_type> handle_;\n    MyTask() = default;\n    MyTask(std::coroutine_handle<promise_type> h) : handle_(h) {}\n    MyTask(MyTask&& other) noexcept : handle_(other.handle_) { other.handle_ = nullptr; }\n    MyTask& operator=(MyTask&& other) noexcept {\n        if (this != &other) { if (handle_) handle_.destroy(); handle_ = other.handle_; other.handle_ = nullptr; }\n        return *this;\n    }\n    ~MyTask() { if (handle_) handle_.destroy(); }\n    void resume() { if (handle_ && !handle_.done()) handle_.resume(); }\n    bool done() { return handle_.done(); }\n};\n\nMyTask good_coroutine_factory(std::shared_ptr<int> shared_data) {\n    std::cout << \"Coroutine started. Data: \" << *shared_data << std::endl;\n    co_await std::suspend_always{};\n    std::cout << \"Coroutine resumed. Data: \" << *shared_data << std::endl;\n}\n\nint main() {\n    // Data is now managed by a shared_ptr, ensuring its lifetime extends beyond factory scope\n    auto shared_data_ptr = std::make_shared<int>(100);\n    MyTask task = good_coroutine_factory(shared_data_ptr);\n    \n    std::cout << \"Main thread: Data is valid: \" << *shared_data_ptr << std::endl;\n    task.resume(); // First resume\n    \n    std::cout << \"Main thread: Data is still valid after first resume: \" << *shared_data_ptr << std::endl;\n    // At this point, task's destructor will eventually call handle_.destroy() \n    // when 'task' goes out of scope, correctly deallocating the coroutine frame.\n    \n    // If needed to resume again (e.g., in a loop or executor)\n    // task.resume();\n\n    return 0;\n}\n```",
    "verification": "<p>To verify the fix, compile with address sanitizers (ASan) or similar memory diagnostic tools (e.g., Valgrind) to detect use-after-free errors. Implement thorough unit and integration tests that cover various coroutine lifecycle scenarios: immediate completion, suspension and resumption, suspension followed by destruction without resumption, and error paths. Observe logging to ensure the expected execution flow and data values are present. For complex cases, use debugger stepping to trace variable lifetimes across <code>co_await</code> points.</p>",
    "date": "2026-08-23",
    "id": 1787466312,
    "type": "error"
});