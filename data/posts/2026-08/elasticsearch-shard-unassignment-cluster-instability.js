window.onPostDataLoaded({
    "title": "Elasticsearch Shard Unassignment Storms & Cluster Instability",
    "slug": "elasticsearch-shard-unassignment-cluster-instability",
    "language": "Elasticsearch, Java, Cluster Management",
    "code": "ClusterInstability",
    "tags": [
        "Elasticsearch",
        "Java",
        "Search",
        "Infra",
        "Monitoring",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch, a distributed search and analytics engine, relies heavily on shard allocation for data distribution, high availability, and fault tolerance. Shards are fundamental units of data, and their proper assignment to nodes is crucial for cluster health. A \"shard unassignment storm\" occurs when a significant number of shards are suddenly unassigned from their nodes and then struggle to be re-assigned, leading to a cascade of issues including cluster instability, data unavailability, and degraded performance.</p><p>The most common trigger for such storms is disk pressure. Elasticsearch nodes continuously monitor their disk usage. When a node's disk usage crosses configured watermarks (e.g., `cluster.routing.allocation.disk.watermark.low`, `.high`, `.flood_stage`), Elasticsearch initiates actions to prevent disk exhaustion. At the `high` watermark, new shards are prevented from being allocated to the node, and existing shards might be relocated. At `flood_stage`, Elasticsearch aggressively attempts to move *all* shards off the node and places the node into a read-only state, effectively quarantining it. If multiple nodes hit these watermarks simultaneously, or if there aren't enough healthy nodes with sufficient disk space to receive the moved shards, a frantic cycle of unassignment and re-assignment attempts begins. This creates a \"storm\" of allocation events, overwhelming the master node and leading to a highly unstable cluster where shards continuously fail to allocate, data becomes unreachable, and the cluster state turns red or yellow.</p><p>This situation is particularly insidious because it can quickly spiral out of control, making it difficult to recover without intervention. Query and indexing performance degrade severely, and data loss can even occur if replication factors are compromised during the instability.</p>",
    "root_cause": "The root cause is sustained disk pressure on one or more Elasticsearch data nodes, causing them to breach configured disk watermarks. This triggers Elasticsearch's automatic shard relocation mechanisms, leading to a cascade of unassignments and re-assignments. The storm is exacerbated if there are insufficient available disk resources on other nodes to accommodate the relocated shards, resulting in a continuous loop of allocation failures and cluster instability.",
    "bad_code": "```json\n# No \"bad code\" per se, but misconfigured cluster settings or\n# unmanaged infrastructure leading to full disks.\n# This represents a common (bad) state or default that can cause issues.\n\n# Default disk watermarks (can vary by ES version, these are examples)\n# cluster.routing.allocation.disk.watermark.low: \"85%\"\n# cluster.routing.allocation.disk.watermark.high: \"90%\"\n# cluster.routing.allocation.disk.watermark.flood_stage: \"95%\"\n\n# This is a 'bad' scenario where nodes hit high disk usage:\n# Node 1 Disk Usage: 91%\n# Node 2 Disk Usage: 92%\n# Node 3 Disk Usage: 88% (still problematic)\n# This leads to Elasticsearch aggressively trying to move shards,\n# but there's no healthy place for them to go, causing a storm.\n\n# Example of a node being explicitly put in read-only due to flood_stage:\nPUT /_cluster/settings\n{\n  \"persistent\": {\n    \"cluster.blocks.read_only_allow_delete\": \"true\"\n  }\n}\n# While this can happen automatically, if nodes hit flood_stage,\n# it locks down the cluster further.\n```",
    "solution_desc": "The primary solution involves proactive disk space management and tuning Elasticsearch's disk allocation settings. First, ensure adequate disk provisioning and monitor disk usage closely, alerting well before watermarks are breached. If a storm is ongoing, immediate actions include adding more data nodes with ample disk space, or freeing up space on existing nodes by deleting old indices or snapshots. Adjusting the disk watermarks temporarily to less aggressive values (`cluster.routing.allocation.disk.watermark.high` to `98%`, `flood_stage` to `99%`) can buy time, but this is a temporary band-aid. Long-term, implement Index Lifecycle Management (ILM) to automatically manage index retention and roll over, ensuring old data is removed or moved to cheaper storage tiers. Investigate `index.routing.allocation.total_shards_per_node` to prevent a single node from accumulating too many shards. Use dedicated master nodes to offload coordination duties from data nodes, improving master stability during allocation events.",
    "good_code": "```json\n# Proactive cluster settings for disk allocation (tune carefully based on environment):\nPUT /_cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.disk.watermark.low\": \"80%\",\n    \"cluster.routing.allocation.disk.watermark.high\": \"85%\",\n    \"cluster.routing.allocation.disk.watermark.flood_stage\": \"90%\",\n    \"cluster.info.update.interval\": \"30s\" # Update cluster info more frequently (default 30s, can decrease for quicker\u53cd\u61c9)\n  }\n}\n\n# Example of applying Index Lifecycle Management (ILM) policy\n# to automatically delete indices after a certain age.\n# 1. Define an ILM policy:\nPUT /_ilm/policy/my_data_policy\n{\n  \"policy\": {\n    \"phases\": {\n      \"hot\": {\n        \"min_age\": \"0s\",\n        \"actions\": {\n          \"rollover\": {\n            \"max_primary_shard_size\": \"50gb\",\n            \"max_age\": \"30d\"\n          }\n        }\n      },\n      \"delete\": {\n        \"min_age\": \"90d\",\n        \"actions\": {\n          \"delete\": {}\n        }\n      }\n    }\n  }\n}\n\n# 2. Apply the policy to an index template so new indices get it:\nPUT /_index_template/my_data_template\n{\n  \"index_patterns\": [\"my_data-*\"],\n  \"template\": {\n    \"settings\": {\n      \"index.lifecycle.name\": \"my_data_policy\",\n      \"index.lifecycle.rollover_alias\": \"my_data_alias\",\n      \"index.number_of_shards\": 3,\n      \"index.number_of_replicas\": 1\n    }\n  },\n  \"priority\": 500\n}\n\n# 3. Create initial index and alias (or roll over an existing one)\n# POST /my_data-000001/_rollover\n# PUT /my_data-000001\n# { \"aliases\": { \"my_data_alias\": { \"is_write_index\": true }}}\n```",
    "verification": "To verify the fix, monitor Elasticsearch cluster health and disk usage metrics rigorously. Use the `_cluster/health` API to confirm the cluster status is green and remains stable over time. Observe `_cat/shards` and `_cat/allocation` to ensure shards are correctly assigned and that there are no unassigned shards or erratic movements. Monitor disk usage on all data nodes via host-level monitoring tools (e.g., Prometheus, Grafana, CloudWatch) to ensure they stay well below the `high` watermark. Check Elasticsearch logs for allocation errors or warnings. Over time, with ILM policies in place, verify that old indices are being correctly deleted or rolled over, preventing future disk pressure build-up. A sustained period of cluster stability and healthy disk usage across all nodes confirms the effectiveness of the solution.",
    "date": "2026-08-30",
    "id": 1788079935,
    "type": "error"
});