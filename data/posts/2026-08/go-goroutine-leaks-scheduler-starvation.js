window.onPostDataLoaded({
    "title": "Go Goroutine Leaks & Scheduler Starvation",
    "slug": "go-goroutine-leaks-scheduler-starvation",
    "language": "Go",
    "code": "GoroutineLeak",
    "tags": [
        "Go",
        "Backend",
        "Performance",
        "Error Fix"
    ],
    "analysis": "<p>Goroutines are lightweight threads managed by the Go runtime, making concurrency easy and efficient. However, their very nature \u2013 being cheap to create \u2013 can lead to problems if not managed correctly. A common pitfall is the goroutine leak, where a goroutine is started but never exits. This can occur due to indefinite blocking I/O operations without timeouts, or goroutines waiting on undrained channels that will never receive a value or be closed. As these leaked goroutines accumulate, they consume memory and other system resources, eventually leading to application instability or crashes.</p><p>Furthermore, an abundance of blocked goroutines can impact the Go scheduler. If all available OS threads (P's) managed by the Go runtime become occupied by goroutines performing blocking system calls (like network I/O or file operations without non-blocking variants), the Go scheduler might struggle to find available P's to run other runnable goroutines. This situation, known as scheduler starvation, can lead to performance degradation, increased latency, and even deadlocks, as the application struggles to make progress.</p>",
    "root_cause": "The primary technical reason for goroutine leaks and scheduler starvation is the failure to implement proper lifecycle management for concurrent tasks. This includes:<ul><li><b>Unbounded Goroutine Creation:</b> Spawning new goroutines for every request or event without a mechanism to terminate them.</li><li><b>Indefinite Blocking Operations:</b> Using `select {}` without a `default` case or a context-based cancellation, or performing I/O operations (e.g., `io.Reader.Read`, `conn.Accept`) that block indefinitely without timeouts or cancellation contexts.</li><li><b>Undrained Channels:</b> Goroutines waiting to send to or receive from channels that either never get a sender/receiver or are never closed. This often happens when a goroutine responsible for sending or closing a channel panics or exits prematurely.</li><li><b>Lack of Context Propagation:</b> Failing to pass `context.Context` to goroutines that perform cancellable operations, preventing graceful shutdown or timeout enforcement.</li></ul>These issues tie up the underlying OS threads and Go runtime resources, preventing their reuse and leading to resource exhaustion.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\n// This handler starts a goroutine that blocks indefinitely.\n// Each request to /leak will spawn a new, never-ending goroutine.\nfunc handleInfiniteStream(w http.ResponseWriter, r *http.Request) {\n\tgo func() {\n\t\tfmt.Println(\"Starting long processing goroutine...\")\n\t\t// This select {} blocks forever, simulating an unhandled long-running task\n\t\t// or waiting on a channel that will never receive data/be closed.\n\t\tselect {}\n\t\tfmt.Println(\"This will never be printed - goroutine leaks.\")\n\t}()\n\n\tw.WriteHeader(http.StatusOK)\n\tw.Write([]byte(\"Request received, processing started (potentially leaking goroutine).\"))\n}\n\n// This handler also has a potential leak if the channel is never closed/read.\nfunc handleChannelLeak(w http.ResponseWriter, r *http.Request) {\n\tdataCh := make(chan int)\n\tgo func() {\n\t\t// This goroutine waits indefinitely if no one reads from dataCh\n\t\tval := <-dataCh \n\t\tfmt.Printf(\"Received value: %d\\n\", val)\n\t}()\n\n\t// No one sends to or closes dataCh in this simplified example,\n\t// leading to the goroutine waiting forever.\n\n\tw.WriteHeader(http.StatusOK)\n\tw.Write([]byte(\"Request received, potential channel leak.\"))\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/leak\", handleInfiniteStream)\n\thttp.HandleFunc(\"/channel_leak\", handleChannelLeak)\n\tfmt.Println(\"Server listening on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}\n```",
    "solution_desc": "To fix goroutine leaks and prevent scheduler starvation, adopt robust concurrency patterns focused on graceful termination and resource management. The cornerstone of this approach is `context.Context` for cancellation and timeouts. Pass a context to every goroutine that might perform blocking operations or needs to be terminated. Use `select` statements to listen for `ctx.Done()` alongside other channel operations or blocking calls. For I/O operations, ensure client-side timeouts are configured. Channels must have clear ownership: whoever sends to a channel should eventually close it, or a clear agreement on who consumes from it must exist. Consider worker pool patterns for managing a bounded number of concurrent tasks.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"time\"\n)\n\n// longProcessing simulates work that can be cancelled or timed out.\nfunc longProcessing(ctx context.Context, id int) {\n\tfmt.Printf(\"Starting processing goroutine %d...\n\", id)\n\tdefer fmt.Printf(\"Processing goroutine %d finished.\\n\", id)\n\n\tselect {\n\tcase <-time.After(5 * time.Second): // Simulate work with a timeout\n\t\tfmt.Printf(\"Goroutine %d completed work.\\n\", id)\n\tcase <-ctx.Done(): // Context cancellation received\n\t\tfmt.Printf(\"Goroutine %d cancelled: %v\\n\", id, ctx.Err())\n\t}\n}\n\n// handleControlledStream manages goroutine lifecycle using context.\nfunc handleControlledStream(w http.ResponseWriter, r *http.Request) {\n\t// Create a context with a timeout for this specific request.\n\t// The request's own context (r.Context()) is the parent, ensuring cancellation if the client disconnects.\n\tctx, cancel := context.WithTimeout(r.Context(), 10*time.Second)\n\tdefer cancel() // IMPORTANT: Release resources associated with this context when the handler exits.\n\n\t// Pass the context to the goroutine, allowing it to respond to cancellation/timeout.\n\tgo longProcessing(ctx, time.Now().Nanosecond())\n\n\tw.WriteHeader(http.StatusOK)\n\tw.Write([]byte(\"Request received, processing started (controlled goroutine).\"))\n}\n\n// handleBoundedChannel uses a channel with proper closing/draining.\nfunc handleBoundedChannel(w http.ResponseWriter, r *http.Request) {\n\tdataCh := make(chan int, 1) // Buffered channel for simplicity, or unbuffered with sender/receiver sync\n\n\tgo func() {\n\t\tdefer close(dataCh) // Ensure channel is closed when sender exits\n\t\tfmt.Println(\"Sending value to channel...\")\n\t\tdataCh <- 123\n\t\tfmt.Println(\"Value sent.\")\n\t}()\n\n\tgo func() {\n\t\tfmt.Println(\"Waiting to receive from channel...\")\n\t\tval, ok := <-dataCh // Receive value\n\t\tif ok {\n\t\t\tfmt.Printf(\"Received value: %d\\n\", val)\n\t\t} else {\n\t\t\tfmt.Println(\"Channel closed, no more values.\")\n\t\t}\n\t\tfmt.Println(\"Channel receiver goroutine finished.\")\n\t}()\n\n\tw.WriteHeader(http.StatusOK)\n\tw.Write([]byte(\"Request received, channel handled.\"))\n}\n\nfunc main() {\n\thttp.HandleFunc(\"/controlled\", handleControlledStream)\n\thttp.HandleFunc(\"/bounded_channel\", handleBoundedChannel)\n\tfmt.Println(\"Server listening on :8080\")\n\thttp.ListenAndServe(\":8080\", nil)\n}\n```",
    "verification": "To verify the fix, continuously monitor the number of active goroutines using `runtime.NumGoroutine()` or Go's pprof profiling tools (specifically `go tool pprof http://localhost:PORT/debug/pprof/goroutine?debug=1`). After making repeated requests to the previously problematic endpoints, the goroutine count should stabilize or return to a baseline, rather than continually increasing. Also, observe the application's memory usage and CPU utilization. A healthy application should not show unbounded growth in memory or sustained high CPU if tasks are completing or being cancelled. If scheduler starvation was a concern, monitor application latency; the fix should lead to more consistent response times under load, indicating the scheduler can efficiently manage goroutines.",
    "date": "2026-08-18",
    "id": 1787024249,
    "type": "error"
});