window.onPostDataLoaded({
    "title": "Go Goroutine Leaks & Scheduler Starvation",
    "slug": "go-goroutine-leaks-scheduler-starvation",
    "language": "Go",
    "code": "Goroutine Leak",
    "tags": [
        "Go",
        "Concurrency",
        "Performance",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks occur when goroutines are started but never exit, consuming memory and CPU resources indefinitely. This is a common pitfall in concurrent Go applications, especially in long-running services. Leaked goroutines can accumulate over time, leading to memory exhaustion (out-of-memory errors), increased garbage collection pressure, and eventually, performance degradation. Scheduler starvation can arise when too many goroutines are runnable but non-yielding (e.g., CPU-bound loops without explicit yields) or when the Go scheduler struggles to fairly distribute CPU time among an excessively large number of goroutines, leading to high latency for legitimate tasks.</p><p>Typical scenarios causing leaks include:</p><ul><li><b>Missing Exit Conditions:</b> Goroutines blocking indefinitely on an unread channel, a locked mutex, or a <code>select {}</code> statement without a <code>default</code> case or context cancellation.</li><li><b>Unbounded Creation:</b> Launching new goroutines for every request without a mechanism to limit concurrency or ensure old ones terminate.</li><li><b>I/O Without Timeouts:</b> Blocking network or file I/O operations that never complete, holding the goroutine hostage.</li></ul><p>Scheduler starvation manifests as high CPU usage, increased P99 latencies for requests, and a general feeling of sluggishness, even if individual goroutines aren't consuming excessive resources, their sheer number overburdens the scheduler's overhead.</p>",
    "root_cause": "Unbounded goroutine creation, goroutines blocking indefinitely without exiting, or long-running CPU-bound tasks hogging resources without yielding to the scheduler. This leads to an ever-growing number of goroutines consuming memory and CPU, eventually overwhelming the scheduler and starving other legitimate tasks.",
    "bad_code": "package main\n\nimport (\n    \"fmt\"\n    \"time\"\n)\n\nfunc processRequests(requestID int) {\n    // Simulate a long-running, potentially blocking operation\n    // This goroutine will never exit on its own\n    select {} // A common cause of leaks: goroutine stuck indefinitely\n}\n\nfunc main() {\n    for i := 0; i < 1000; i++ {\n        go processRequests(i) // Launching goroutines without cleanup\n        fmt.Printf(\"Launched goroutine %d\\n\", i)\n        time.Sleep(10 * time.Millisecond) // Simulate request rate\n    }\n\n    // The main goroutine will exit, but the leaked goroutines will persist\n    // if this were a server process, they would accumulate\n    fmt.Println(\"Main finished, but goroutines are still running (leaking)...\")\n    time.Sleep(1 * time.Hour) // Keep main alive to observe memory/CPU\n}",
    "solution_desc": "To prevent goroutine leaks and scheduler starvation, ensure all launched goroutines have a defined exit condition. Use <code>context.Context</code> for cancellation signals, implement timeouts for blocking operations (I/O, channel reads/writes), and employ worker pools or semaphore patterns to cap concurrent goroutine execution. For long-running, CPU-bound tasks, ensure they periodically yield control or can be preempted. Architecturally, define clear ownership and lifecycle for goroutines, especially in server applications where requests might come indefinitely.",
    "good_code": "package main\n\nimport (\n    \"context\"\n    \"fmt\"\n    \"sync\"\n    \"time\"\n)\n\n// worker simulates a task that respects context cancellation\nfunc worker(ctx context.Context, id int, wg *sync.WaitGroup) {\n    defer wg.Done()\n    fmt.Printf(\"Worker %d started\\n\", id)\n    for {\n        select {\n        case <-ctx.Done():\n            fmt.Printf(\"Worker %d received cancellation signal: %v\\n\", id, ctx.Err())\n            return // Exit cleanly\n        case <-time.After(50 * time.Millisecond):\n            // Simulate some work\n            fmt.Printf(\"Worker %d working...\\n\", id)\n        }\n    }\n}\n\n// ProcessRequestsWithTimeout simulates a bounded task with a timeout\nfunc processRequestsWithTimeout(ctx context.Context, requestID int) {\n    select {\n    case <-ctx.Done():\n        fmt.Printf(\"Request %d cancelled: %v\\n\", requestID, ctx.Err())\n    case <-time.After(2 * time.Second): // Simulate a task that finishes or times out\n        fmt.Printf(\"Request %d processed successfully (or timed out after 2s if no work was done)\\n\", requestID)\n    }\n}\n\nfunc main() {\n    var wg sync.WaitGroup\n    ctx, cancel := context.WithCancel(context.Background())\n\n    // Example 1: Bounded number of workers that can be cancelled\n    for i := 0; i < 5; i++ {\n        wg.Add(1)\n        go worker(ctx, i, &wg)\n    }\n    time.Sleep(1 * time.Second)\n    cancel() // Signal workers to stop\n    wg.Wait()\n    fmt.Println(\"All workers stopped cleanly.\")\n\n    fmt.Println(\"\\n--- Demonstrating request-specific timeouts ---\")\n    // Example 2: Per-request goroutine with a timeout\n    for i := 0; i < 3; i++ {\n        requestCtx, requestCancel := context.WithTimeout(context.Background(), 1*time.Second)\n        go func(id int, reqCtx context.Context) {\n            defer requestCancel() // Ensure context is cancelled when goroutine exits\n            processRequestsWithTimeout(reqCtx, id)\n        }(i, requestCtx)\n        time.Sleep(100 * time.Millisecond) // Stagger requests\n    }\n\n    // Give some time for request goroutines to finish\n    time.Sleep(3 * time.Second)\n    fmt.Println(\"Main finished, all goroutines should have exited cleanly.\")\n}",
    "verification": "Monitor the number of active goroutines using <code>runtime.NumGoroutine()</code> or via Go's <code>pprof</code> package (<code>go tool pprof http://localhost:6060/debug/pprof/goroutine?debug=1</code>). Observe memory usage (<code>top</code>, <code>htop</code>, <code>ps</code> or <code>pprof</code>) to ensure it remains stable and doesn't grow indefinitely. Look for logs indicating goroutines exiting gracefully. For scheduler starvation, monitor CPU utilization and observe if requests are being processed within expected latencies, even under load. A healthy system shows consistent goroutine counts after initial startup and predictable memory consumption.",
    "date": "2026-08-20",
    "id": 1787207466,
    "type": "error"
});