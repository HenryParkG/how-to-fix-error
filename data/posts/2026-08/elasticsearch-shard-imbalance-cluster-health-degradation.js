window.onPostDataLoaded({
    "title": "Elasticsearch: Shard Imbalance & Cluster Health",
    "slug": "elasticsearch-shard-imbalance-cluster-health-degradation",
    "language": "Elasticsearch",
    "code": "ShardImbalance",
    "tags": [
        "Search",
        "Distributed Systems",
        "Performance",
        "Elasticsearch",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Elasticsearch relies on distributing shards (units of data) across data nodes to achieve scalability and high availability. 'Shard imbalance' occurs when shards, especially primary and replica shards, are not evenly distributed across the available data nodes. This leads to an uneven workload distribution, creating 'hot spots' on certain nodes, which can manifest as high CPU, memory, or disk I/O usage. This imbalance can severely degrade cluster health, often indicated by a 'yellow' (replicas not allocated) or 'red' (primary shards not allocated) cluster status, reduced query/indexing performance, and increased operational overhead. A healthy Elasticsearch cluster requires an even spread of both data and workload.</p>",
    "root_cause": "Shard imbalance can stem from several factors:<ul><li><b>Uneven Node Hardware</b>: Nodes with varying CPU, RAM, or disk capacities. Elasticsearch, by default, doesn't account for these differences perfectly.</li><li><b>Node Failures/Restarts</b>: When a node fails and then rejoins the cluster, shards may not rebalance optimally, especially if `shard_allocation_delay` is not configured.</li><li><b>Rapid Index Creation/Deletion</b>: Aggressive Index Lifecycle Management (ILM) policies or frequent creation of new indices can overwhelm the rebalancing mechanism, especially if new shards land disproportionately on certain nodes.</li><li><b>Disk Full Issues</b>: If a node runs out of disk space (or approaches a high water mark), Elasticsearch will stop allocating shards to it, leading to a concentrated load on remaining nodes.</li><li><b>Manual Allocation Mistakes</b>: Incorrect use of `_cluster/reroute` API or explicit shard filters can pin shards to specific nodes.</li><li><b>Hot Indices</b>: A few indices that are disproportionately large or receive a high volume of reads/writes can cause imbalance if their shards are concentrated on a few nodes.</li><li><b>Insufficient Nodes</b>: Simply not having enough data nodes to effectively distribute all shards.</li></ul>",
    "bad_code": "```json\n# Elasticsearch cluster settings that do not proactively manage shard allocation balance.\n# Assuming default settings, or settings that don't consider disk space or replica count.\n\nPUT _cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.enable\": \"all\",\n    \"cluster.routing.allocation.balance.shard\": \"0.5\",\n    \"cluster.routing.allocation.balance.index\": \"0.5\",\n    \"cluster.routing.allocation.balance.threshold\": \"1.0\",\n    \"cluster.routing.allocation.allow_rebalance\": \"always\",\n\n    # Missing critical settings like disk thresholds or awareness attributes.\n    # Without these, Elasticsearch might move shards onto nodes already struggling with disk space\n    # or won't try to spread shards across different racks/availability zones.\n\n    # Example of a problematic index template (not directly a bad_code, but a configuration that leads to imbalance)\n    \"index.routing.allocation.require._name\": \"node-1\" # Forcing specific shards to a single node\n  }\n}\n```\n<p>While the above JSON isn't 'bad code' in the traditional sense, it represents a default or incomplete configuration that lacks the necessary guardrails to prevent shard imbalance. The absence of disk allocation awareness or zone awareness settings, combined with potential manual routing rules, sets the stage for uneven distribution.</p>",
    "solution_desc": "Resolving shard imbalance requires a combination of configuration tuning and monitoring:<ul><li><b>Enable Allocation Awareness</b>: Configure `cluster.routing.allocation.awareness.attributes` (e.g., `rack`, `zone`) to ensure shards are spread across different physical locations.</li><li><b>Tune Rebalance Settings</b>: Adjust `cluster.routing.allocation.balance.shard`, `cluster.routing.allocation.balance.index`, and `cluster.routing.allocation.balance.threshold` to encourage more aggressive rebalancing.</li><li><b>Disk Watermarks</b>: Set `cluster.routing.allocation.disk.threshold.watermark.low` and `high` to prevent nodes from running out of disk space, triggering automatic shard movement.</li><li><b>Shard Allocation Delay</b>: Use `index.unassigned.node_left.delayed_timeout` (e.g., `5m`) to delay replica reallocation when a node temporarily leaves, preventing unnecessary rebalancing.</li><li><b>Force Allocation</b>: For persistent issues, use `_cluster/reroute` API to manually move shards, but use with caution.</li><li><b>Index Lifecycle Management (ILM)</b>: Review ILM policies to ensure they align with your cluster's capacity and don't create too many shards on specific nodes.</li><li><b>Node Tagging</b>: Use `node.attr.<attribute_name>` in `elasticsearch.yml` and `index.routing.allocation.require.<attribute_name>` in index settings to guide shard placement.</li><li><b>Upgrade/Expand Hardware</b>: If imbalance is due to insufficient capacity, adding more homogeneous data nodes is often the best long-term solution.</li></ul>",
    "good_code": "```json\nPUT _cluster/settings\n{\n  \"persistent\": {\n    \"cluster.routing.allocation.enable\": \"all\",\n    \"cluster.routing.allocation.balance.shard\": \"0.7\",\n    \"cluster.routing.allocation.balance.index\": \"0.7\",\n    \"cluster.routing.allocation.balance.threshold\": \"1.5\",\n    \"cluster.routing.allocation.allow_rebalance\": \"indices_all_active\",\n\n    \"cluster.routing.allocation.disk.threshold.enabled\": true,\n    \"cluster.routing.allocation.disk.watermark.low\": \"85%\",  # Start moving shards at 85% disk usage\n    \"cluster.routing.allocation.disk.watermark.high\": \"90%\", # Prevent allocating to nodes over 90%\n    \"cluster.routing.allocation.disk.watermark.flood_stage\": \"95%\", # Read-only block at 95%\n\n    \"cluster.routing.allocation.awareness.attributes\": \"zone\", # Enable zone awareness\n    \"index.unassigned.node_left.delayed_timeout\": \"5m\" # Delay replica re-allocation for 5 minutes\n  }\n}\n\n# Example of using node attributes for even distribution in elasticsearch.yml\n# node.attr.zone: us-east-1a\n# node.attr.tier: hot\n\n# And then in an index template (not bad_code for the template, but how it's used)\n# PUT _index_template/my_template\n# {\n#   \"index_patterns\": [\"logstash-*\"],\n#   \"template\": {\n#     \"settings\": {\n#       \"index.routing.allocation.total_shards_per_node\": 5, # Limit shards per node\n#       \"index.routing.allocation.include.zone\": \"us-east-1a,us-east-1b\" # Allow specific zones\n#     }\n#   }\n# }\n```",
    "verification": "To verify the fix:<ul><li><b>Cluster Health</b>: Regularly check `GET _cluster/health`. The cluster status should be 'green', indicating all primary and replica shards are allocated.</li><li><b>Shard Allocation Overview</b>: Use `GET _cat/shards` and `GET _cat/allocation?v` to see the distribution of shards across nodes and their disk usage. Look for an even distribution of primary and replica shards.</li><li><b>Node Statistics</b>: Monitor `GET _nodes/stats` for individual node resource usage (CPU, memory, disk I/O, JVM heap). Ensure no single node is consistently overloaded.</li><li><b>Kibana Monitoring</b>: Leverage Kibana's built-in monitoring (Stack Monitoring) or external tools like Grafana/Prometheus to visualize shard distribution, node resource usage, and overall cluster performance over time. Look for stable metrics and the absence of spikes or hot spots.</li><li><b>Audit Logs</b>: Check Elasticsearch logs for events related to shard rebalancing and allocation decisions.</li></ul>",
    "date": "2026-08-25",
    "id": 1787639443,
    "type": "error"
});