window.onPostDataLoaded({
    "title": "Kafka Consumer Group Rebalance Storms & Fencing Errors",
    "slug": "kafka-consumer-group-rebalance-storms-fencing-errors-high-load",
    "language": "Kafka",
    "code": "Rebalance Storm",
    "tags": [
        "Kafka",
        "Messaging",
        "Distributed Systems",
        "Java",
        "Backend",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Kafka consumer groups are designed for high availability and scalability, allowing multiple consumers to share the workload of consuming from a set of topics. When a consumer joins or leaves a group, or when a topic's partition count changes, a 'rebalance' occurs, redistributing partitions among active consumers. Under high load, these rebalances can spiral into 'rebalance storms' where consumers repeatedly drop out and rejoin, leading to service disruption, high latency, and increased CPU usage on brokers and clients.</p><p>A primary cause is consumers failing to send heartbeats to the group coordinator within the <code>session.timeout.ms</code> period. This might happen if message processing takes too long (exceeding <code>max.poll.interval.ms</code>), if the consumer thread is blocked (e.g., by long-running synchronous I/O or garbage collection pauses), or due to network issues. Once deemed 'dead', the coordinator initiates a rebalance. If multiple consumers experience similar issues under load, they all might drop out and trigger a cascading series of rebalances\u2014a storm.</p><p>Fencing errors (e.g., <code>GroupCoordinatorNotAvailableException</code> or <code>FencedInstanceIdException</code> with static membership) occur when a consumer attempts to commit offsets or interact with the group coordinator after it has already been 'fenced out' (i.e., its membership has been revoked) due to missing heartbeats or rebalancing. This indicates that the consumer is operating on stale membership information, further confirming that it's out of sync with the group.</p>",
    "root_cause": "Consumers fail to send heartbeats to the Kafka group coordinator within the configured <code>session.timeout.ms</code> or take too long to process a batch of messages, exceeding <code>max.poll.interval.ms</code>. This often stems from: 1) Excessive message processing time that blocks the consumer's poll loop. 2) Prolonged Garbage Collection (GC) pauses in Java applications. 3) Network instability or high latency between consumer and broker. These issues are exacerbated under high message load, leading to the consumer being evicted and triggering a rebalance.",
    "bad_code": "```java\n// Problematic Kafka consumer configuration (defaults or too low for high load)\nProperties props = new Properties();\nprops.setProperty(\"bootstrap.servers\", \"localhost:9092\");\nprops.setProperty(\"group.id\", \"my_consumer_group\");\nprops.setProperty(\"enable.auto.commit\", \"false\");\n// Default session.timeout.ms = 10000 (10 seconds)\n// Default max.poll.interval.ms = 300000 (5 minutes)\n// Default max.poll.records = 500\n\nKafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(Collections.singletonList(\"my_topic\"));\n\nwhile (true) {\n    ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n    for (ConsumerRecord<String, String> record : records) {\n        // Simulate long-running, CPU-intensive, or blocking processing\n        // This blocks the consumer thread, preventing heartbeats/polls\n        processMessageSynchronously(record.value()); // Takes > 5 minutes under load\n    }\n    consumer.commitSync(); // Fails with Fencing error if rebalance occurred during processing\n}\n\nvoid processMessageSynchronously(String message) {\n    // Complex business logic, external API calls, database writes, etc.\n    // Potentially takes minutes for large batches or under heavy contention\n    try { Thread.sleep(300 * 1000); } catch (InterruptedException e) {} // Example of long processing\n}\n```",
    "solution_desc": "The core solution involves ensuring consumers can reliably send heartbeats and process messages efficiently. This can be achieved by: <ol><li><b>Optimizing Processing Logic:</b> Make message processing faster and non-blocking. Offload heavy processing to separate threads, thread pools, or asynchronous queues, allowing the main consumer thread to quickly return from <code>poll()</code> and send heartbeats.</li><li><b>Adjusting Timeouts:</b> Increase <code>session.timeout.ms</code> and <code>max.poll.interval.ms</code> to give consumers more grace period, but be cautious not to make them too long (delays detection of truly dead consumers). <code>max.poll.records</code> can also be lowered to reduce the processing burden per poll.</li><li><b>Monitoring & Alerting:</b> Implement robust monitoring for consumer group rebalances, lag, message processing times, GC pauses, and network latency.</li><li><b>Static Membership:</b> For critical services, enable <a href=\"https://kafka.apache.org/documentation/#static_membership\">static membership</a> by setting <code>group.instance.id</code>. This avoids rebalances for transient consumer restarts, only triggering one if a consumer is truly dead after its instance ID expires.</li><li><b>Resource Allocation:</b> Ensure consumer instances have sufficient CPU, memory, and network resources to prevent slowdowns and GC pauses.</li></ol>",
    "good_code": "```java\n// Optimized Kafka consumer configuration and processing pattern\nProperties props = new Properties();\nprops.setProperty(\"bootstrap.servers\", \"localhost:9092\");\nprops.setProperty(\"group.id\", \"my_consumer_group\");\nprops.setProperty(\"enable.auto.commit\", \"false\");\n// Increased timeouts for robustness under high load\nprops.setProperty(\"session.timeout.ms\", \"45000\"); // e.g., 45 seconds\nprops.setProperty(\"max.poll.interval.ms\", \"300000\"); // e.g., 5 minutes (adjust based on max batch processing)\nprops.setProperty(\"heartbeat.interval.ms\", \"15000\"); // should be ~1/3 of session.timeout.ms\nprops.setProperty(\"max.poll.records\", \"100\"); // Process smaller batches at a time\n// Optional: For static membership (requires unique ID for each consumer instance)\n// props.setProperty(\"group.instance.id\", \"my_consumer_instance_1\");\n\nKafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\nconsumer.subscribe(Collections.singletonList(\"my_topic\"), new ConsumerRebalanceListener() {\n    @Override\n    public void onPartitionsRevoked(Collection<TopicPartition> partitions) {\n        System.out.println(\"Partitions revoked: \" + partitions);\n        // Clean up or commit any in-progress work for these partitions\n    }\n\n    @Override\n    public void onPartitionsAssigned(Collection<TopicPartition> partitions) {\n        System.out.println(\"Partitions assigned: \" + partitions);\n        // (Optional) Seek to committed offsets or beginning/end\n    }\n});\n\n// Use a separate thread pool for asynchronous processing\nExecutorService executor = Executors.newFixedThreadPool(Runtime.getRuntime().availableProcessors());\n\nwhile (true) {\n    try {\n        ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n        if (records.isEmpty()) continue;\n\n        List<Future<?>> futures = new ArrayList<>();\n        for (ConsumerRecord<String, String> record : records) {\n            futures.add(executor.submit(() -> {\n                try {\n                    processMessageAsynchronously(record.value());\n                } catch (Exception e) {\n                    System.err.println(\"Error processing record: \" + e.getMessage());\n                    // Handle individual record failures (e.g., dead-letter queue)\n                }\n            }));\n        }\n\n        // Wait for all messages in the batch to be processed before committing\n        for (Future<?> future : futures) {\n            future.get(); // Blocks until task completes (or throws exception)\n        }\n\n        consumer.commitSync(); // Commit offsets only after successful processing of the batch\n\n    } catch (WakeupException e) {\n        // Consumer was closed, exit loop\n        break;\n    } catch (Exception e) {\n        System.err.println(\"Consumer loop error: \" + e.getMessage());\n        // Implement robust error handling and retry logic\n    }\n}\n\n// Graceful shutdown\nconsumer.close();\nexecutor.shutdown();\ntry { executor.awaitTermination(60, TimeUnit.SECONDS); } catch (InterruptedException e) {}\n\nvoid processMessageAsynchronously(String message) {\n    // Your business logic for processing a single message\n    // This should be optimized, but is now off the main consumer thread\n    // Example: Database writes, API calls, complex calculations\n}\n```",
    "verification": "Verify the fix by monitoring your Kafka cluster and consumer applications. Check Kafka broker logs for `REBALANCE_IN_PROGRESS` or `FENCED_INSTANCE_ID` messages. Use Kafka monitoring tools (e.g., Kafka Manager, Confluent Control Center, or custom JMX metrics) to observe: <ol><li><b>Consumer group state:</b> Ensure no frequent rebalances.</li><li><b>Consumer lag:</b> Verify that lag does not build up uncontrollably.</li><li><b>Application metrics:</b> Monitor CPU, memory, GC activity, and message processing latency within your consumer application. Ensure GC pauses are minimal and CPU usage is stable.</li><li><b>Network metrics:</b> Confirm stable network connectivity between consumers and brokers.</li></ol>Run load tests to simulate high message throughput and observe stability.",
    "date": "2026-08-15",
    "id": 1786774966,
    "type": "error"
});