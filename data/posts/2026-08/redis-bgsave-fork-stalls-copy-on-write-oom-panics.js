window.onPostDataLoaded({
    "title": "Redis BGSAVE: Mitigating Fork Stalls & CoW OOMs",
    "slug": "redis-bgsave-fork-stalls-copy-on-write-oom-panics",
    "language": "Redis, Linux",
    "code": "REDIS_BGSAVE_FAILURE",
    "tags": [
        "Redis",
        "Databases",
        "Performance",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>Redis's `BGSAVE` command allows the server to save a snapshot of the dataset to disk in the background without blocking the main event loop. It achieves this by performing a `fork()` system call. The parent process (the Redis server) continues to serve client requests, while the child process writes the RDB file. This copy-on-write (CoW) mechanism is central to its non-blocking nature: when `fork()` is called, both parent and child initially share all memory pages. A page is copied only when either the parent or the child attempts to modify it.</p><p>Two common problems arise from this: <b>fork stalls</b> and <b>Copy-on-Write (CoW) OOM panics</b>. Fork stalls occur because the `fork()` syscall, especially on systems with a large amount of resident memory (RSS) being used by Redis, can be a time-consuming operation. The Linux kernel needs to set up new process structures and duplicate page table entries, which can briefly block the parent process. During this stall, Redis is unresponsive. CoW OOM panics happen if the rate of writes to Redis is high during a `BGSAVE`. Each write to a shared page by the parent triggers a page copy. If the total memory consumed by these new CoW pages, combined with the existing memory footprint, exceeds available system RAM or swap space, the Linux OOM killer might intervene, often terminating the Redis process.</p>",
    "root_cause": "High Redis memory usage (large RSS) leading to prolonged `fork()` syscall blocking, or excessive write operations during `BGSAVE` causing rapid creation of Copy-on-Write pages, exhausting system memory and triggering the Linux OOM killer.",
    "bad_code": "```bash\n# redis.conf snippet (default/problematic configuration)\n# save 900 1   # BGSAVE every 15 mins if 1 key changed\n# save 300 10  # BGSAVE every 5 mins if 10 keys changed\n# save 60 10000 # BGSAVE every 1 min if 10000 keys changed\n\n# Or manual trigger on a busy instance\nredis-cli BGSAVE\n\n# System settings that exacerbate the issue\n# vm.overcommit_memory = 0  (default, strict overcommit check)\n# vm.swappiness = 60      (default, aggressive swapping)\n# Transparent Huge Pages (THP) enabled (can fragment memory)\n```",
    "solution_desc": "To mitigate fork stalls and CoW OOM panics, several strategies can be employed. First, tune kernel parameters: set `vm.overcommit_memory = 1` to allow the kernel to overcommit memory, reducing OOM killer's aggressiveness during CoW page allocation. Disable Transparent Huge Pages (THP) by setting `echo 'never' > /sys/kernel/mm/transparent_hugepage/enabled` as THP can exacerbate fork latency and memory fragmentation. Second, optimize Redis memory usage: use data structures efficiently, set appropriate key expirations, and consider memory-saving serialization formats. Third, manage `BGSAVE` frequency: adjust `save` directives in `redis.conf` to fewer, longer intervals, or schedule `BGSAVE` during off-peak hours. Consider using AOF persistence (especially in `everysec` mode) as a primary persistence mechanism, possibly coupled with RDB for disaster recovery. For very large instances, consider sharding or using Redis Sentinel/Cluster for high availability and better distributed persistence.",
    "good_code": "```ini\n# /etc/sysctl.conf (apply with sudo sysctl -p)\nvm.overcommit_memory = 1\nvm.swappiness = 10 # Reduce swap usage unless critical\n\n# /etc/rc.local or systemd service to disable THP at boot\n# (Can also be done via kernel boot parameter 'transparent_hugepage=never')\necho 'never' > /sys/kernel/mm/transparent_hugepage/enabled\n\n# redis.conf (optimized for large datasets/high writes)\n# Disable automatic RDB saves if using AOF as primary persistence\n# save \"\"\n\n# If RDB is still desired, schedule it judiciously or use replication\n# save 3600 10000 # Only save if 10k keys change over 1 hour\n\n# Append Only File (AOF) settings\nappendonly yes\nappendfsync everysec # Good balance of durability and performance\nno-appendfsync-on-rewrite yes # Prevent AOF rewrite from blocking writes\nauto-aof-rewrite-percentage 100\nauto-aof-rewrite-min-size 64mb # Adjust based on dataset size\n\n# For replication, consider using a replica for BGSAVE\n# Have one replica dedicated to taking RDB snapshots and periodically restart it\n```",
    "verification": "To verify the fix, monitor key metrics during `BGSAVE` operations. Use `INFO persistence` to observe `rdb_last_bgsave_status`, `rdb_last_cow_size`, and `rdb_last_save_time`. A `rdb_last_cow_size` that is a small percentage of the total dataset size indicates fewer CoW pages were created. Monitor system memory with `free -h` and `dmesg -T | grep -i oom` for OOM killer invocations. Observe `fork_time_usec` in `INFO CPU` output to track how long `fork()` takes. A decrease in `fork_time_usec` and the absence of OOM events or service interruptions during `BGSAVE` confirm the solution's effectiveness. Continuously monitor Redis and system logs for any signs of instability.",
    "date": "2026-08-24",
    "id": 1787543154,
    "type": "error"
});