window.onPostDataLoaded({
    "title": "Elixir/BEAM: Mailbox Exhaustion & Selective Receive",
    "slug": "elixir-beam-mailbox-exhaustion-selective-receive-performance",
    "language": "Elixir / BEAM",
    "code": "Mailbox Exhaustion",
    "tags": [
        "Elixir",
        "BEAM",
        "Backend",
        "Concurrency",
        "Performance",
        "OTP",
        "Error Fix"
    ],
    "analysis": "<p>In Elixir, processes communicate via asynchronous message passing, where each process has a mailbox. When a process receives messages faster than it can process them, its mailbox can grow unbounded, leading to 'Mailbox Exhaustion'. This condition can severely impact system stability: the BEAM (Erlang Virtual Machine) allocates memory for each message in the mailbox, and an excessively large mailbox can consume all available RAM, causing the Erlang VM to crash or become unresponsive. This is particularly problematic for processes that are either too slow to process messages, or are subjected to bursty traffic without proper backpressure mechanisms. Furthermore, 'Selective Receive Performance Degradation' exacerbates this issue. Elixir's `receive` block allows pattern matching to pick specific messages from the mailbox. If a process frequently receives messages it isn't immediately interested in, these 'unmatched' messages accumulate in the mailbox until a matching one is found. The BEAM must iterate through the mailbox for each `receive` call, moving non-matching messages to a temporary 'save queue' and then back, leading to `O(N)` or worse performance characteristics for large mailboxes, where N is the number of messages.</p><p>This degradation is insidious because it doesn't just consume memory; it also consumes CPU cycles for message traversal, further slowing down processing and contributing to mailbox growth. In high-throughput systems, a single bottlenecked process with a large, unsorted mailbox can become a major point of contention, impacting the entire application. Common culprits include `GenServer` processes that handle a high volume of requests without proper flow control, or processes waiting for a specific response while general 'ping' or 'status' messages flood their mailbox.</p>",
    "root_cause": "The root causes are: 1) **Lack of Backpressure:** Senders are not throttled when consumers are overwhelmed, leading to unbounded message production. 2) **Slow Message Processing:** The process's logic within the `handle_call`/`handle_cast`/`receive` functions is too computationally intensive or involves blocking operations, preventing rapid mailbox emptying. 3) **Inefficient Selective Receive:** Pattern matching in `receive` blocks is overly selective, causing many messages to be scanned and temporarily moved, especially when the desired message arrives late in the queue. 4) **Ignoring Unwanted Messages:** Processes receive messages they don't care about and don't explicitly handle or discard them, allowing them to accumulate.",
    "bad_code": "```elixir\n# lib/bad_processor.ex\ndefmodule BadProcessor do\n  use GenServer\n\n  # This GenServer processes messages very slowly\n  # and accepts all messages, even uninteresting ones.\n  def start_link(initial_state) do\n    GenServer.start_link(__MODULE__, initial_state, name: __MODULE__)\n  end\n\n  @impl true\n  def init(state) do\n    {:ok, state}\n  end\n\n  @impl true\n  def handle_cast({:process_data, data}, state) do\n    # Simulate slow processing - this could be a heavy computation\n    # or an external synchronous API call that takes time.\n    :timer.sleep(500) # Simulating a 500ms blocking operation\n    IO.puts \"Processed data: #{data}\"\n    {:noreply, state}\n  end\n\n  @impl true\n  def handle_cast(:ping, state) do\n    # This process doesn't care about ping, but it still gets handled.\n    # If 'ping' messages are frequent, they consume CPU cycles.\n    IO.puts \"Received ping, ignoring (slowly)\"\n    :timer.sleep(10) # Even ignoring can take time\n    {:noreply, state}\n  end\n\n  # Catch-all for other messages, could be a problem if many unwanted messages arrive\n  @impl true\n  def handle_cast(msg, state) do\n    IO.puts \"Received unhandled cast: #{inspect msg}\"\n    {:noreply, state}\n  end\n\n  @impl true\n  def handle_info(msg, state) do\n    # Any async message that doesn't match a handle_cast/handle_call goes here.\n    # This can easily accumulate if the process is busy.\n    IO.puts \"Received unhandled info: #{inspect msg}\"\n    {:noreply, state}\n  end\nend\n\n# --- Simulation of a problematic client --- \n# A client sending messages much faster than BadProcessor can handle\ndefmodule BadClient do\n  def flood_processor(n) do\n    Enum.each(1..n, fn i ->\n      GenServer.cast(BadProcessor, {:process_data, \"item_#{i}\"})\n      if rem(i, 100) == 0 do\n        IO.puts \"Sent #{i} messages.\"\n      end\n      # Also send frequent 'ping' messages that clog the mailbox\n      if rem(i, 10) == 0 do\n        GenServer.cast(BadProcessor, :ping)\n      end\n    end)\n  end\nend\n\n# To run: BadProcessor.start_link([]) ; BadClient.flood_processor(10000)\n```",
    "solution_desc": "The solution involves a combination of architectural patterns and careful process design. 1) **Implement Backpressure:** Use techniques like `GenStage`, `Flow`, or explicit acknowledgments to ensure producers don't overwhelm consumers. For `GenServer`, consider using `handle_call` for critical messages (which inherently provides backpressure by blocking the sender) or implement a custom flow control mechanism (e.g., a counter for pending messages that blocks new sends when a threshold is reached). 2) **Optimize Message Processing:** Ensure `handle_call`/`handle_cast` functions are non-blocking and perform minimal work. Delegate long-running tasks to other processes (e.g., workers, task supervisors) or use asynchronous libraries. 3) **Efficient Selective Receive:** Design messages to be less selective or use message tagging to move the desired message to the head of the queue. If a `receive` block has many patterns, ensure the most frequent or critical messages are matched first. Consider using `receive timeout -> ... end` to handle cases where the expected message doesn't arrive promptly, preventing indefinite blocking and allowing mailbox cleanup. 4) **Discard Unwanted Messages:** Use a catch-all clause in `receive` or `handle_info` to explicitly discard or log uninteresting messages without moving them to a save queue. A simple `handle_info(_, state)` or `receive _ -> continue end` at the end of an explicit receive block helps clear the mailbox.",
    "good_code": "```elixir\n# lib/good_processor.ex\ndefmodule GoodProcessor do\n  use GenServer\n\n  # Define a maximum mailbox size to prevent OOMs\n  # This is a soft limit, enforced by the developer, not a BEAM hard limit.\n  @max_mailbox_size 1000\n\n  def start_link(initial_state) do\n    GenServer.start_link(__MODULE__, initial_state, name: __MODULE__)\n  end\n\n  @impl true\n  def init(state) do\n    # Initialize state with a counter for pending messages\n    {:ok, %{state | pending_messages: 0}}\n  end\n\n  # Critical operations use handle_call for inherent backpressure\n  @impl true\n  def handle_call({:process_data, data}, _from, %{pending_messages: pm} = state) when pm < @max_mailbox_size do\n    # Spawn a temporary task for potentially slow work, keeping the GenServer responsive\n    Task.start_link(fn ->\n      :timer.sleep(50) # Simulate faster, non-blocking work\n      IO.puts \"Processed data: #{data}\"\n      # Acknowledge completion back to the GenServer\n      send(self(), :data_processed)\n    end)\n    {:reply, :ok, %{state | pending_messages: pm + 1}}\n  end\n\n  # If mailbox is full, apply backpressure by refusing new calls\n  @impl true\n  def handle_call({:process_data, _data}, _from, state) do\n    {:reply, {:error, :mailbox_full}, state}\n  end\n\n  # Efficiently handle pings or other common but non-critical messages\n  @impl true\n  def handle_cast(:ping, state) do\n    # Handle or simply discard, don't block. This implies no expensive work.\n    {:noreply, state}\n  end\n\n  # Handle acknowledgment from worker tasks\n  @impl true\n  def handle_info(:data_processed, %{pending_messages: pm} = state) do\n    {:noreply, %{state | pending_messages: pm - 1}}\n  end\n\n  # Catch-all for other info messages, effectively discarding them efficiently\n  @impl true\n  def handle_info(_msg, state) do\n    {:noreply, state}\n  end\nend\n\n# --- Simulation of a robust client --- \n# A client respecting backpressure\ndefmodule GoodClient do\n  def send_data_with_backpressure(n) do\n    Enum.each(1..n, fn i ->\n      case GenServer.call(GoodProcessor, {:process_data, \"item_#{i}\"}) do\n        :ok ->\n          IO.puts \"Sent item_#{i}\"\n        {:error, :mailbox_full} ->\n          IO.puts \"Processor mailbox full, waiting...\"\n          :timer.sleep(100) # Wait and retry\n          send_data_with_backpressure(n - i + 1) # Retry remaining\n          break\n      end\n      # Send ping occasionally, but it's handled efficiently\n      if rem(i, 10) == 0 do\n        GenServer.cast(GoodProcessor, :ping)\n      end\n    end)\n  end\nend\n\n# To run: GoodProcessor.start_link([]) ; GoodClient.send_data_with_backpressure(10000)\n```",
    "verification": "To verify the fix, monitor the process mailbox size using tools like `Observer.start()` (from `:observer` module) or `recon.info(pid, :message_queue_len)` from `recon`. The mailbox size should remain bounded and not grow indefinitely. Measure end-to-end latency for message processing. Profiling tools like `fprof` or `eper` (e.g., `eper:lprof`) can identify bottlenecks in `handle_call`/`handle_cast` or excessive CPU spent on message scanning. Introduce high-volume load tests and observe system memory usage. A successful fix will show stable memory consumption, consistent processing rates, and no OOM errors or warnings about excessive mailbox sizes.",
    "date": "2026-08-29",
    "id": 1788005609,
    "type": "error"
});