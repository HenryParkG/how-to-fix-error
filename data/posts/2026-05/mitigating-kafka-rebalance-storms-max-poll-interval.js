window.onPostDataLoaded({
    "title": "Mitigating Kafka Rebalance Storms (Max Poll Interval)",
    "slug": "mitigating-kafka-rebalance-storms-max-poll-interval",
    "language": "Java",
    "code": "CommitFailedException",
    "tags": [
        "Kafka",
        "Java",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Kafka rebalance storms occur when a consumer in a consumer group takes too long to process messages returned by a single <code>poll()</code> invocation. If the processing duration exceeds the configured <code>max.poll.interval.ms</code>, the Kafka coordinator presumes the consumer thread is dead or hung.</p><p>As a response, the coordinator evicts the consumer from the group and triggers a rebalance. The messages that were being processed are reassigned to another consumer. When the evicted consumer finally finishes processing and tries to commit its offsets, it encounters a <code>CommitFailedException</code>. If the next consumer also fails to process the new heavy workload within the limit, it gets evicted too, causing a cascading rebalance loop across the entire cluster.</p>",
    "root_cause": "Synchronous, heavy execution operations (like bulk DB writes, slow APIs, or complex computations) in the main polling thread exceed the `max.poll.interval.ms` limit, causing the broker to mark the consumer as dead and trigger a rebalance.",
    "bad_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\n\npublic class SlowConsumer {\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"heavy-processing-group\");\n        props.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes\n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"heavy-tasks\"));\n\n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            for (ConsumerRecord<String, String> record : records) {\n                // BUG: Heavy synchronous task taking 15 seconds per message.\n                // A batch of 50 records will take 750 seconds, exceeding the 300s limit!\n                processHeavyTask(record.value()); \n            }\n            consumer.commitSync(); // Throws CommitFailedException\n        }\n    }\n    private static void processHeavyTask(String value) { /* Slow API or DB call */ }\n}",
    "solution_desc": "To mitigate this, lower the maximum batch size via `max.poll.records` to ensure processing always finishes within the polling interval window. Additionally, increase the `max.poll.interval.ms` threshold or decouple the ingestion thread from the processing pool using an internal thread executor.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\n\npublic class OptimizedConsumer {\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"heavy-processing-group\");\n        // FIX 1: Increase the threshold to allow safe execution boundaries\n        props.put(\"max.poll.interval.ms\", \"600000\"); // 10 minutes\n        // FIX 2: Restrict the batch size returned per poll\n        props.put(\"max.poll.records\", \"10\"); \n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"heavy-tasks\"));\n\n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            for (ConsumerRecord<String, String> record : records) {\n                // Safe execution guaranteed: 10 records * 15s = 150s, far below 600s limit\n                processHeavyTask(record.value());\n            }\n            consumer.commitSync(); // Commit succeeds\n        }\n    }\n    private static void processHeavyTask(String value) { /* Slow API or DB call */ }\n}",
    "verification": "Deploy the consumer and simulate high-latency tasks. Monitor client logs for `CommitFailedException` or `PreparingRebalance` messages. Ensure the consumer group status in Kafka (`kafka-consumer-groups.sh --describe`) remains stable with a steady lag decline under peak loads.",
    "date": "2026-05-20",
    "id": 1779244124,
    "type": "error"
});