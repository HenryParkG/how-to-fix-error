window.onPostDataLoaded({
    "title": "Fixing C++20 Coroutine Frame Memory Leaks",
    "slug": "fixing-cpp20-coroutine-frame-memory-leaks",
    "language": "C++",
    "code": "Memory Leak",
    "tags": [
        "Rust",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>C++20 coroutines allocate their state\u2014including parameters, local variables, and the promise object\u2014on the heap via <code>operator new</code> (unless the compiler can perform Coroutine Heap Allocation Elision, or CHAE). The lifecycle of this coroutine frame is tied directly to the <code>std::coroutine_handle</code>. A critical leak occurs when a coroutine suspends indefinitely, and its managing object (e.g., a custom Task or Future class) goes out of scope without invoking <code>.destroy()</code> on the handle, leaving the heap-allocated frame abandoned forever.</p><p>Unlike traditional call stacks, a suspended coroutine's stack frame is not cleaned up by ordinary scope unwinding. If the coroutine does not reach its <code>final_suspend</code> point (which can automatically destroy the frame if configured to do so), manual intervention is required. This often manifests in asynchronous runtime implementations where tasks are cancelled or abandoned midway.</p>",
    "root_cause": "The RAII wrapper (Task class) manages the coroutine handle but lacks a destructor that calls handle.destroy(), or fails to account for instances where the coroutine is abandoned in a suspended state prior to completing its final suspension point.",
    "bad_code": "#include <coroutine>\n#include <iostream>\n\nstruct SimpleTask {\n    struct promise_type {\n        SimpleTask get_return_object() {\n            return SimpleTask{std::coroutine_handle<promise_type>::from_promise(*this)};\n        }\n        std::suspend_always initial_suspend() { return {}; }\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void unreturn_void() {}\n        void return_void() {}\n        void unhandled_exception() { std::terminate(); }\n    };\n\n    std::coroutine_handle<promise_type> handle;\n    \n    // BUG: Missing destructor to destroy the suspended frame.\n    // If the coroutine never finishes, the frame leaks when SimpleTask goes out of scope.\n    SimpleTask(std::coroutine_handle<promise_type> h) : handle(h) {}\n};",
    "solution_desc": "Implement a rigorous RAII wrapper for the <code>std::coroutine_handle</code>. The wrapper must have exclusive ownership of the handle, implying that copy constructors must be deleted, and move constructors must properly nullify the source handle. The destructor must safely call <code>handle.destroy()</code> if the handle is valid and has not completed, or if the design mandates that the wrapper manages the full lifecycle of suspended frames.",
    "good_code": "#include <coroutine>\n#include <utility>\n\nstruct SimpleTask {\n    struct promise_type {\n        SimpleTask get_return_object() {\n            return SimpleTask{std::coroutine_handle<promise_type>::from_promise(*this)};\n        }\n        std::suspend_always initial_suspend() { return {}; }\n        // Use suspend_always to allow safe external inspection and manual destruction\n        std::suspend_always final_suspend() noexcept { return {}; }\n        void return_void() {}\n        void unhandled_exception() { std::terminate(); }\n    };\n\n    std::coroutine_handle<promise_type> handle = nullptr;\n\n    explicit SimpleTask(std::coroutine_handle<promise_type> h) : handle(h) {}\n    \n    // Prevent copying to avoid double destruction of the frame\n    SimpleTask(const SimpleTask&) = delete;\n    SimpleTask& operator=(const SimpleTask&) = delete;\n\n    // Safe move semantics transfer handle ownership\n    SimpleTask(SimpleTask&& other) noexcept : handle(other.handle) {\n        other.handle = nullptr;\n    }\n    \n    SimpleTask& operator=(SimpleTask&& other) noexcept {\n        if (this != &other) {\n            if (handle) {\n                handle.destroy();\n            }\n            handle = other.handle;\n            other.handle = nullptr;\n        }\n        return *this;\n    }\n\n    // RAII destructor guarantees frame cleanup\n    ~SimpleTask() {\n        if (handle) {\n            handle.destroy();\n        }\n    }\n};",
    "verification": "Compile the code using GCC or Clang with LeakSanitizer enabled (<code>-fsanitize=address</code>) and run the execution path that leaves the coroutine suspended. Verify that no memory leaks are reported. Alternatively, override <code>operator new</code> and <code>operator delete</code> inside the <code>promise_type</code> to assert that the allocation count matches the deallocation count at the end of the execution scope.",
    "date": "2026-05-31",
    "id": 1780194907,
    "type": "error"
});