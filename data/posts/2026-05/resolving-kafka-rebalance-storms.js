window.onPostDataLoaded({
    "title": "Resolving Kafka Consumer Group Rebalance Storms",
    "slug": "resolving-kafka-rebalance-storms",
    "language": "Java",
    "code": "CommitFailedException",
    "tags": [
        "Java",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>In high-throughput Kafka consumers, processing individual message batches can occasionally take longer than expected due to external database lockups, slow network calls, or intensive CPU tasks. When the processing time of a single batch returned by <code>poll()</code> exceeds the configured <code>max.poll.interval.ms</code>, the Kafka Coordinator assumes the consumer has hung or died.</p><p>As a result, the coordinator expels the consumer from the group and triggers a rebalance. During a rebalance, the partitions assigned to the expelled consumer are reassigned to other consumers in the group. When those consumers attempt to process their reassigned partitions, they also experience processing delays, hit their own timeouts, and trigger subsequent rebalances. This creates a destructive feedback loop known as a 'Rebalance Storm', causing message processing to stall completely and filling logs with <code>CommitFailedException</code> errors.</p>",
    "root_cause": "The consumer's main thread is doing both message fetching and heavy, blocking synchronous processing in a single loop. When processing time exceeds max.poll.interval.ms, the group coordinator triggers a rebalance because the poll loop didn't invoke poll() in time.",
    "bad_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.Collections;\nimport java.util.Properties;\n\npublic class SlowConsumer {\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"heavy-processor\");\n        props.put(\"max.poll.interval.ms\", \"300000\"); // 5 minutes limit\n        \n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"heavy-tasks\"));\n\n        while (true) {\n            ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n            for (ConsumerRecord<String, String> record : records) {\n                // BUG: Synchronously processing complex task that might take over 5 mins in total\n                heavyTaskProcessing(record.value()); \n            }\n            consumer.commitSync();\n        }\n    }\n    \n    private static void heavyTaskProcessing(String data) {\n        try { Thread.sleep(60000); } catch (InterruptedException e) {} // Simulating heavy 1 min task\n    }\n}",
    "solution_desc": "Decouple the message ingestion thread from the processing thread pool. The main thread should quickly poll Kafka, pass work to a bounded <code>ExecutorService</code> (thread pool), and monitor task progress. Also, configure <code>max.poll.records</code> to a smaller value to limit the size of each batch, and increase <code>max.poll.interval.ms</code> to allow a safe buffer. Additionally, ensure manual commit management handles partial batch execution carefully or use a state-tracking pause/resume mechanism.",
    "good_code": "import org.apache.kafka.clients.consumer.*;\nimport java.time.Duration;\nimport java.util.*;\nimport java.util.concurrent.*;\n\npublic class ResilientConsumer {\n    public static void main(String[] args) {\n        Properties props = new Properties();\n        props.put(\"bootstrap.servers\", \"localhost:9092\");\n        props.put(\"group.id\", \"heavy-processor-fixed\");\n        props.put(\"max.poll.interval.ms\", \"600000\"); // Increased buffer (10 mins)\n        props.put(\"max.poll.records\", \"10\");         // Restrict batch size\n        props.put(\"enable.auto.commit\", \"false\");\n\n        KafkaConsumer<String, String> consumer = new KafkaConsumer<>(props);\n        consumer.subscribe(Collections.singletonList(\"heavy-tasks\"));\n        \n        // Decouple processing with bounded thread pool\n        ExecutorService executor = new ThreadPoolExecutor(\n            4, 4, 0L, TimeUnit.MILLISECONDS,\n            new LinkedBlockingQueue<>(20), \n            new ThreadPoolExecutor.CallerRunsPolicy() // Backpressure on main thread\n        );\n\n        try {\n            while (true) {\n                ConsumerRecords<String, String> records = consumer.poll(Duration.ofMillis(100));\n                if (records.isEmpty()) continue;\n\n                List<Future<?>> futures = new ArrayList<>();\n                for (ConsumerRecord<String, String> record : records) {\n                    futures.add(executor.submit(() -> heavyTaskProcessing(record.value())));\n                }\n\n                // Wait for the entire polled batch to finish before polling again\n                for (Future<?> future : futures) {\n                    try {\n                        future.get(); // Blocking within bounds of max.poll.interval.ms\n                    } catch (Exception e) {\n                        // Handle execution exceptions safely\n                    }\n                }\n                consumer.commitSync();\n            }\n        } finally {\n            consumer.close();\n            executor.shutdown();\n        }\n    }\n\n    private static void heavyTaskProcessing(String data) {\n        try { Thread.sleep(5000); } catch (InterruptedException e) { Thread.currentThread().interrupt(); }\n    }\n}",
    "verification": "Deploy the fixed consumer alongside Kafka under maximum processing load. Monitor the consumer group state using <code>kafka-consumer-groups.sh --describe</code>. Verify that partition reassignments remain at zero during high-latency operations, and check that no <code>CommitFailedException</code> occurrences are reported in application logs.",
    "date": "2026-05-31",
    "id": 1780194908,
    "type": "error"
});