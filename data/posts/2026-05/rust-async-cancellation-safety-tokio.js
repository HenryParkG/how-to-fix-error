window.onPostDataLoaded({
    "title": "Fixing Rust Async Cancellation Safety in Tokio Pipelines",
    "slug": "rust-async-cancellation-safety-tokio",
    "language": "Rust",
    "code": "CancellationSafety",
    "tags": [
        "Rust",
        "Backend",
        "Tokio",
        "Error Fix"
    ],
    "analysis": "<p>In Rust asynchronous programming, cancellation safety is a critical safety guarantee that is frequently violated when working with <code>tokio::select!</code>. In Tokio, futures are canceled by simply dropping them when a competing branch in a select block completes. If a dropped future was holding state midway through an operation\u2014such as holding bytes read from a socket buffer before framing them\u2014that state is permanently lost, resulting in silent data corruption, partial reads, or connection stalls.</p><p>This bug typically emerges in pipelines handling streaming network protocols where developers mix non-reentrant async operations (like reading raw bytes into a local buffer) with control signals inside a select loop. Ensuring cancellation safety requires that futures either do not hold intermediate state across yield points, or that state is externalized and preserved if the future is dropped prematurely.</p>",
    "root_cause": "When tokio::select! decides to abort an incomplete branch, it drops the corresponding future. If that future was mid-execution of tokio::io::AsyncReadExt::read, bytes read into the socket's internal OS buffers may have been transferred to the user-space buffer but not processed. Dropping the future discards this buffer, causing data loss because those bytes cannot be read again from the socket.",
    "bad_code": "use tokio::io::{AsyncReadExt, AsyncWriteExt};\nuse tokio::net::TcpStream;\nuse std::time::Duration;\n\nasync fn handle_connection(mut stream: TcpStream) -> Result<(), Box<dyn std::error::Error>> {\n    let mut buffer = vec![0u8; 1024];\n    loop {\n        tokio::select! {\n            // BUG: read() is NOT cancellation safe in this context.\n            // If the timeout branch fires while read() is suspended,\n            // bytes already read from the socket into 'buffer' are lost.\n            res = stream.read(&mut buffer) => {\n                let n = res?;\n                if n == 0 { break; }\n                process_data(&buffer[..n]).await?;\n            }\n            _ = tokio::time::sleep(Duration::from_secs(30)) => {\n                println!(\"Connection timed out due to inactivity\");\n                break;\n            }\n        }\n    }\n    Ok(())\n}",
    "solution_desc": "To fix this issue, move the buffer management outside of the transient select futures. Use a stateful stream reader or a persistent buffer structure that survives across select iterations. Alternatively, use tokio::codec's FramedRead or tokio's pinned reader utilities. By keeping the buffer pinned and persistent, even if the select branch is canceled, no read data is discarded because the next iteration resumes reading into the exact same buffer without losing the already-received bytes.",
    "good_code": "use tokio::io::AsyncReadExt;\nuse tokio::net::TcpStream;\nuse std::time::Duration;\nuse bytes::BytesMut;\n\nasync fn handle_connection_fixed(mut stream: TcpStream) -> Result<(), Box<dyn std::error::Error>> {\n    // Preserve state in a persistent buffer external to the select loop\n    let mut buffer = BytesMut::with_capacity(1024);\n    \n    loop {\n        tokio::select! {\n            // FIX: Using read_buf on a persistent BytesMut buffer is cancellation safe\n            // because bytes remain appended to the buffer even if the future is dropped.\n            res = stream.read_buf(&mut buffer) => {\n                let n = res?;\n                if n == 0 { break; }\n                \n                // Process and drain the buffer safely\n                let data = buffer.split_to(n);\n                process_data(&data).await?;\n            }\n            _ = tokio::time::sleep(Duration::from_secs(30)) => {\n                println!(\"Connection timed out safely without data loss\");\n                break;\n            }\n        }\n    }\n    Ok(())\n}\n\nasync fn process_data(data: &[u8]) -> Result<(), Box<dyn std::error::Error>> {\n    // Processing logic here\n    Ok(())\n}",
    "verification": "Compile the code using the latest stable Rust toolchain. Run integration tests simulating slow, fragmented network frames interleaved with random cancellations (triggered by timeouts). Assert that the total byte count sent matches the total byte count processed, verifying no data is lost or corrupted.",
    "date": "2026-05-27",
    "id": 1779849238,
    "type": "error"
});