window.onPostDataLoaded({
    "title": "Fixing eBPF Verifier Failures from Unbounded Loops",
    "slug": "fixing-ebpf-verifier-failures-unbounded-loops",
    "language": "C",
    "code": "Verifier Reject (Loop/Map Tracking)",
    "tags": [
        "Kubernetes",
        "C",
        "Infra",
        "Error Fix"
    ],
    "analysis": "<p>The eBPF verifier acts as a static analysis engine that ensures safety, termination, and isolation for code running inside the Linux kernel. Historically, the verifier prohibited any form of looping to guarantee that programs would not hang the kernel. While newer kernels (5.17+) introduce mechanisms like <code>bpf_loop</code> and bounded loop tracking, developers frequently encounter hard verifier rejections due to complex state tracking and unbounded iteration paths.</p><p>When loop iteration counts depend on dynamic packet data, structural configuration arrays, or map values, the verifier cannot statically determine the maximum execution state size. Additionally, modifying map pointer states inside dynamic loops triggers pruning analysis failures. To maintain reliability, developers must program defensively to prove mathematical execution boundaries to the verifier.</p>",
    "root_cause": "The eBPF verifier evaluates programs as a Directed Acyclic Graph (DAG) of instruction states. If a loop dynamic condition cannot be mapped to a constant loop boundary or if pointer tracking information is lost during state pruning (where the verifier discards redundant paths but mistakenly forgets path-specific value constraints), the program is rejected with errors like 'infinite loop detected' or 'back-edge from insn to insn'.",
    "bad_code": "#include <linux/bpf.h>\n#include <bpf/bpf_helpers.h>\n\nstruct properties {\n    __u32 count;\n    __u32 values[16];\n};\n\nstruct { \n    __uint(type, BPF_MAP_TYPE_HASH);\n    __type(key, __u32);\n    __type(value, struct properties);\n    __uint(max_entries, 128);\n} config_map SEC(\".maps\");\n\nSEC(\"xdp\")\nint bad_loop_filter(struct xdp_md *ctx) {\n    __u32 key = 0;\n    struct properties *prop = bpf_map_lookup_elem(&config_map, &key);\n    if (!prop) {\n        return XDP_PASS;\n    }\n\n    __u32 sum = 0;\n    // BUG: The verifier cannot prove prop->count is less than 16\n    // and within absolute bounded limits without helper assertions.\n    for (__u32 i = 0; i < prop->count; i++) {\n        sum += prop->values[i];\n    }\n\n    return sum > 1000 ? XDP_DROP : XDP_PASS;\n}",
    "solution_desc": "To resolve verifier rejections caused by unbounded iteration, you must explicitly enforce upper-limit assertions that the compiler and the verifier can mathematically prove. This is done by applying a bitwise mask, asserting strict logical conditions before the loop, or utilizing a loop limit check directly inside the execution path. For modern setups, we can also use `#pragma clang loop unroll(disable)` coupled with strict guard boundaries to allow the loop to pass verification without exhausting instruction limits (1 million instructions max).",
    "good_code": "#include <linux/bpf.h>\n#include <bpf/bpf_helpers.h>\n\nstruct properties {\n    __u32 count;\n    __u32 values[16];\n};\n\nstruct { \n    __uint(type, BPF_MAP_TYPE_HASH);\n    __type(key, __u32);\n    __type(value, struct properties);\n    __uint(max_entries, 128);\n} config_map SEC(\".maps\");\n\nSEC(\"xdp\")\nint good_loop_filter(struct xdp_md *ctx) {\n    __u32 key = 0;\n    struct properties *prop = bpf_map_lookup_elem(&config_map, &key);\n    if (!prop) {\n        return XDP_PASS;\n    }\n\n    __u32 sum = 0;\n    __u32 limit = prop->count;\n\n    // Fix: Explicitly assert boundaries to the verifier using bounds checking\n    if (limit > 16) {\n        limit = 16;\n    }\n\n    // Use explicit bounding patterns so compiler unrolling works safely\n    #pragma clang loop unroll(full)\n    for (__u32 i = 0; i < 16; i++) {\n        if (i >= limit) {\n            break;\n        }\n        sum += prop->values[i];\n    }\n\n    return sum > 1000 ? XDP_DROP : XDP_PASS;\n}",
    "verification": "Compile the code using clang: `clang -O2 -target bpf -c program.c -o program.o`. Verify and inspect load states using bpftool: `bpftool prog load program.o /sys/fs/bpf/my_prog type xdp`. The verifier output should print a clean trace and successfully return 0 without triggering bounded state errors.",
    "date": "2026-05-30",
    "id": 1780137828,
    "type": "error"
});