window.onPostDataLoaded({
    "title": "Debugging Go Goroutine Leaks from Channels & Contexts",
    "slug": "debugging-go-goroutine-leaks-channels-contexts",
    "language": "Go",
    "code": "Goroutine Leak",
    "tags": [
        "Go",
        "Concurrency",
        "Memory Leak",
        "Backend",
        "Error Fix"
    ],
    "analysis": "<p>Goroutine leaks are a common source of memory and CPU exhaustion in Go applications. Unlike traditional threads, goroutines are lightweight, making it easy to launch many without immediate resource warnings. However, if a goroutine is started but never exits, it will indefinitely consume memory (for its stack, variables, and channel buffers) and potentially hold onto system resources. Over time, accumulated leaked goroutines can lead to application slowdowns, out-of-memory errors, and even system crashes. Identifying and resolving these leaks is crucial for maintaining the stability and performance of concurrent Go services.</p>",
    "root_cause": "The primary causes of goroutine leaks often stem from unclosed or unread channels and improper handling of context cancellation. If a goroutine is waiting to send data on a channel that no other goroutine is reading from, or waiting to receive from a channel that no other goroutine is sending to (and the channel is not closed), it will block indefinitely. Similarly, if a goroutine is performing an operation that should be cancellable via a `context.Context`, but fails to check `ctx.Done()` or propagate the context correctly, it may continue executing past its intended lifecycle, becoming a zombie.",
    "bad_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"time\"\n)\n\nfunc leakyWorker(ctx context.Context, dataStream <-chan int) {\n\tgo func() {\n\t\tfor {\n\t\t\tselect {\n\t\t\tcase val := <-dataStream:\n\t\t\t\tfmt.Printf(\"Processed: %d\\n\", val)\n\t\t\t// Missing context cancellation check\n\t\t\t}\n\t\t}\n\t}()\n}\n\nfunc main() {\n\tctx, cancel := context.WithCancel(context.Background())\n\tdefer cancel()\n\n\tdata := make(chan int)\n\tleakyWorker(ctx, data)\n\n\tfor i := 0; i < 5; i++ {\n\t\tdata <- i // Only send a few values\n\t}\n\n\t// The main goroutine continues, but leakyWorker's goroutine is still running\n\t// and waiting on 'data', which will eventually stop receiving.\n\tfmt.Println(\"Main finished, but worker might still be alive...\")\n\n\t// If we uncomment cancel(), the worker still wouldn't exit cleanly\n\t// cancel()\n\n\ttime.Sleep(2 * time.Second)\n\t// In a real application, 'data' channel might never be closed or read further\n\t// causing the leakyWorker's goroutine to block indefinitely.\n}\n```",
    "solution_desc": "To fix goroutine leaks, ensure that all goroutines launched have a clear exit strategy. This primarily involves robust channel management and proper context propagation/cancellation. For channels, always ensure a corresponding sender/receiver exists, and that channels are closed when no more values will be sent (or use a `done` channel pattern). For context, consistently pass `context.Context` to all functions and goroutines that should respect cancellation. Inside goroutines, use a `select` statement to check for `ctx.Done()` alongside channel operations, allowing the goroutine to gracefully exit when the context is cancelled. Tools like `sync.WaitGroup` or `errgroup.Group` can help manage the lifecycle of a set of goroutines, ensuring they all complete before proceeding.",
    "good_code": "```go\npackage main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\t\"sync\"\n\t\"time\"\n)\n\nfunc wellBehavedWorker(ctx context.Context, dataStream <-chan int, wg *sync.WaitGroup) {\n\tdefer wg.Done()\n\tfor {\n\t\tselect {\n\t\tcase val, ok := <-dataStream:\n\t\t\tif !ok {\n\t\t\t\tfmt.Println(\"Data stream closed, worker exiting.\")\n\t\t\t\treturn\n\t\t\t}\n\t\t\tfmt.Printf(\"Processed: %d\\n\", val)\n\t\tcase <-ctx.Done():\n\t\t\tfmt.Printf(\"Context cancelled, worker exiting: %v\\n\", ctx.Err())\n\t\t\treturn\n\t\t}\n\t}\n}\n\nfunc main() {\n\tctx, cancel := context.WithCancel(context.Background())\n\tvar wg sync.WaitGroup\n\n\tdata := make(chan int)\n\n\twg.Add(1)\n\tgo wellBehavedWorker(ctx, data, &wg)\n\n\tfor i := 0; i < 5; i++ {\n\t\tdata <- i\n\t}\n\n\t// Simulate some work, then close the data channel\n\ttime.Sleep(100 * time.Millisecond)\n\tclose(data) // Signal that no more data will be sent\n\n\t// Or, alternatively, cancel the context after some time\n\t// cancel()\n\n\t// Wait for the worker goroutine to finish\n\twg.Wait()\n\tfmt.Println(\"Main finished, worker exited cleanly.\")\n\n\t// If using context.WithTimeout/WithDeadline, cancellation happens automatically\n\t// ctx, cancel := context.WithTimeout(context.Background(), time.Second)\n\t// defer cancel()\n}\n```",
    "verification": "To verify the fix, use Go's built-in profiling tools. The `runtime.NumGoroutine()` function can be used to track the number of active goroutines, which should return to a baseline after the worker goroutines are expected to exit. For more detailed analysis, use `go tool pprof` with a `debug=1` HTTP endpoint (e.g., `http://localhost:6060/debug/pprof/goroutine?debug=1`) to inspect stack traces of active goroutines. Look for goroutines that remain blocked on channel operations (`chan receive`, `chan send`) or `select` statements long after they should have completed their work. Additionally, monitor memory usage for stability over time, ensuring no continuous upward trend indicative of leaks.",
    "date": "2026-05-29",
    "id": 1780056589,
    "type": "error"
});